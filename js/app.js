(function e(t,n,r){function s(o,u){if(!n[o]){if(!t[o]){var a=typeof require=="function"&&require;if(!u&&a)return a(o,!0);if(i)return i(o,!0);var f=new Error("Cannot find module '"+o+"'");throw f.code="MODULE_NOT_FOUND",f}var l=n[o]={exports:{}};t[o][0].call(l.exports,function(e){var n=t[o][1][e];return s(n?n:e)},l,l.exports,e,t,n,r)}return n[o].exports}var i=typeof require=="function"&&require;for(var o=0;o<r.length;o++)s(r[o]);return s})({1:[function(require,module,exports){
'use strict';

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _reactDom = require('react-dom');

var _reactRouter = require('react-router');

var _amazeuiReact = require('amazeui-react');

require('whatwg-fetch');

var _components = require('./components');

var _auth = require('./components/auth');

var _auth2 = _interopRequireDefault(_auth);

var _pages = require('./pages');

var _ManageRoot = require('./components/ManageRoot');

var _ManageRoot2 = _interopRequireDefault(_ManageRoot);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

//所有页面必须要import才有能被正确处理 ps 想要import from 文件夹 需要在文件夹的index.js 里export一下

// style
//import '../less/app.less';

// components
var pages = {
  order: _pages.Order,
  index_home: _pages.Index_home,
  login: _pages.Login,
  register_user: _pages.Register_user,
  register_doctor: _pages.Register_doctor,
  checkReservation: _pages.CheckReservation,
  register_hospital: _pages.Register_hospital,
  manageRoot: _pages.ManageRoot,
  user_reservation: _pages.User_reservation,
  doctor_list: _pages.Doctor_list,
  institution_list: _pages.Institution_list,
  query_result: _pages.Query_result,
  system_manage: _pages.System_manage,
  user_manage: _pages.User_manage,
  price_manage: _pages.Price_manage,
  account_manage: _pages.Account_manage,
  user_index: _pages.User_index,
  about: _pages.About,
  userManage: _ManageRoot2.default,
  about_us: _pages.About_us
};

//下面的router里面有一个遍历这个map的操作，将map里所有的页面加载到router中 也就是说不在这个数组里声明一下的话，必须要在下面的router组里写一遍，否则无法通过url访问到对应的组件
//1个人2医生助理3医生4医疗机构5系统管理员
var auth_map = {
  order: ['2'],
  index_home: ['5', '4', '3', '1', '0'],
  '': ['5', '4', '3', '1', '0'],
  login: ['5', '4', '3', '2', '1', '0'],
  register_user: ['5', '4', '3', '2', '1', '0'],
  register_doctor: ['5', '4', '3', '2', '1', '0'],
  checkReservation: ['1'],
  register_hospital: ['5', '4', '3', '2', '1', '0'],
  manageRoot: ['5'],
  user_reservation: ['1'],
  doctor_list: ['5', '4', '3', '0', '1'],
  institution_list: ['5', '4', '3', '0', '1'],
  query_result: ['5', '4', '3', '0', '1'],
  system_manage: ['5'],
  user_manage: ['5'],
  price_manage: ['5'],
  account_manage: ['5'],
  user_index: ['1'],
  adminlogin: ['5', '4', '3', '2', '1', '0'],
  about_us: ['5', '4', '3', '2', '1', '0']
};
var role_replace = {
  '5': 'user_manage',
  '4': 'index_home',
  '3': 'index_home',
  '2': 'order',
  '1': 'index_home',
  '0': 'login'
};

//存储了页面的访问权限
var App = _react2.default.createClass({
  displayName: 'App',
  getInitialState: function getInitialState() {
    return {
      sidebarActive: false,
      loggedIn: _auth2.default.loggedIn()
    };
  },
  updateAuth: function updateAuth(loggedIn) {
    this.setState({
      loggedIn: loggedIn
    });
  },
  toggleSidebar: function toggleSidebar() {
    this.setState({
      sidebarActive: !this.state.sidebarActive
    });
  },
  componentWillMount: function componentWillMount() {
    _auth2.default.onChange = this.updateAuth;
  },
  render: function render() {
    var sidebarActive = this.state.sidebarActive;


    return _react2.default.createElement(
      'div',
      { className: 'adm-container' },
      _react2.default.createElement(_components.Header, null),
      _react2.default.createElement(_components.Sidebar, { active: sidebarActive }),
      _react2.default.createElement(
        'div',
        { className: 'adm-main12' },
        this.props.children
      ),
      _react2.default.createElement(_amazeuiReact.Icon, {
        button: true,
        amStyle: 'primary',
        icon: sidebarActive ? 'close' : 'bars',
        className: 'adm-sidebar-toggle am-show-sm-only',
        onClick: this.toggleSidebar
      })
    );
  }
});

var Page = _react2.default.createClass({
  displayName: 'Page',
  render: function render() {
    var page = this.props.params.page;
    var query = this.props.location.query;

    var breadcrumb = query && query.breadcrumb;
    var location = this.props.location;
    if (pages[page]) {
      return _react2.default.createElement(pages[page], {
        breadcrumb: breadcrumb,
        query: query
      });
    }

    return _react2.default.createElement(
      _components.PageContainer,
      {
        query: query,
        breadcrumb: breadcrumb

      },
      '你访问页面是: 「',
      this.props.params.page,
      '」'
    );
  }
});
var Logout = _react2.default.createClass({
  displayName: 'Logout',
  componentDidMount: function componentDidMount() {
    console.log("logout");
    _auth2.default.logout();
  },
  render: function render() {
    return _react2.default.createElement(
      'p',
      null,
      'You are now logged out'
    );
  }
});
//根据每个页面的访问控制表以及未授权用户访问强制跳转表控制页面权限
function requireAuth(nextState, replace) {
  var pathname = nextState.location.pathname;
  if (pathname != undefined) {
    pathname = pathname.replace('/', '');
  }
  var role = _auth2.default.getRole();

  if (auth_map[pathname] != undefined && auth_map[pathname].indexOf(role) != -1) {} else {
    console.log(pathname);
    console.log(role);
    var message = '您无权访问此页面';
    if (pathname == 'user_reservation') {
      message = '请先登录';
    }
    alert(message);
    replace({
      pathname: role_replace[role],
      state: { nextPathname: nextState.location.pathname }
    });
  }
}
var index_component = _pages.Index_home;
var routes = _react2.default.createElement(
  _reactRouter.Router,
  { history: _reactRouter.hashHistory },
  _react2.default.createElement(
    _reactRouter.Route,
    { path: '/', component: App },
    _react2.default.createElement(_reactRouter.Route, { path: 'userManage', component: _ManageRoot2.default }),
    _react2.default.createElement(_reactRouter.Route, { path: 'about', component: _pages.About }),
    _react2.default.createElement(_reactRouter.Route, { path: 'login', component: _pages.Login }),
    _react2.default.createElement(_reactRouter.Route, { path: 'logout', component: Logout }),
    _react2.default.createElement(_reactRouter.Route, { path: 'adminlogin', component: _pages.AdminLogin, onEnter: requireAuth }),
    _react2.default.createElement(_reactRouter.Route, { path: 'register_user', component: _pages.Register_user, onEnter: requireAuth }),
    _react2.default.createElement(_reactRouter.Route, { path: 'register_doctor', component: _pages.Register_doctor, onEnter: requireAuth }),
    _react2.default.createElement(_reactRouter.Route, { path: 'register_hospital', component: _pages.Register_hospital, onEnter: requireAuth }),
    _react2.default.createElement(_reactRouter.Route, { path: 'index_home', component: _pages.Index_home, onEnter: requireAuth }),
    _react2.default.createElement(_reactRouter.Route, { path: 'query_result', component: _pages.Query_result, onEnter: requireAuth }),
    _react2.default.createElement(_reactRouter.Route, { path: 'doctor_list', component: _pages.Doctor_list, onEnter: requireAuth }),
    _react2.default.createElement(_reactRouter.Route, { path: 'institution_list', component: _pages.Institution_list, onEnter: requireAuth }),
    _react2.default.createElement(_reactRouter.Route, { path: ':page', component: Page, onEnter: requireAuth }),
    _react2.default.createElement(_reactRouter.IndexRoute, { component: _pages.Index_home, onEnter: requireAuth })
  )
);
document.addEventListener('DOMContentLoaded', function () {
  (0, _reactDom.render)(routes, document.getElementById('app-root'));
});

},{"./components":26,"./components/ManageRoot":11,"./components/auth":25,"./pages":51,"amazeui-react":52,"react":356,"react-dom":189,"react-router":219,"whatwg-fetch":360}],2:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _react = require("react");

var _react2 = _interopRequireDefault(_react);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var AboutUs = _react2.default.createClass({
    displayName: "AboutUs",
    render: function render() {
        return _react2.default.createElement(
            "div",
            null,
            _react2.default.createElement("link", { href: "i/resources/css/axure_rp_page.css", type: "text/css", rel: "stylesheet" }),
            _react2.default.createElement("link", { href: "i/data/styles.css", type: "text/css", rel: "stylesheet" }),
            _react2.default.createElement("link", { href: "i/css/commonButtom/style.css", type: "text/css", rel: "stylesheet" }),
            _react2.default.createElement(
                "div",
                { id: "11111", className: "ax_paragraph" },
                _react2.default.createElement("img", { id: "u111_img", className: "img ", src: "i/resources/images/transparent.gif" }),
                _react2.default.createElement(
                    "div",
                    { id: "1111", className: "text" },
                    _react2.default.createElement(
                        "p",
                        null,
                        _react2.default.createElement(
                            "span",
                            null,
                            "关于我们"
                        )
                    )
                )
            ),
            _react2.default.createElement(
                "div",
                { id: "22222", className: "ax_paragraph" },
                _react2.default.createElement("img", { id: "u113_img", className: "img ", src: "i/resources/images/transparent.gif" }),
                _react2.default.createElement(
                    "div",
                    { id: "2222", className: "text" },
                    _react2.default.createElement(
                        "p",
                        null,
                        _react2.default.createElement(
                            "span",
                            null,
                            "联系我们"
                        )
                    )
                )
            ),
            _react2.default.createElement(
                "div",
                { id: "33333", className: "ax_paragraph" },
                _react2.default.createElement("img", { id: "u115_img", className: "img ", src: "i/resources/images/transparent.gif" }),
                _react2.default.createElement(
                    "div",
                    { id: "u116", className: "text" },
                    _react2.default.createElement(
                        "p",
                        null,
                        _react2.default.createElement(
                            "span",
                            null,
                            "服务条款"
                        )
                    )
                )
            ),
            _react2.default.createElement(
                "div",
                { id: "u888", className: "ax_paragraph" },
                _react2.default.createElement("img", { id: "u125_img", className: "img ", src: "i/resources/images/transparent.gif" }),
                _react2.default.createElement(
                    "div",
                    { id: "u126", className: "text" },
                    _react2.default.createElement(
                        "p",
                        null,
                        _react2.default.createElement(
                            "span",
                            null,
                            _react2.default.createElement(
                                "a",
                                { href: "http://www.miitbeian.gov.cn" },
                                "           京ICP备案16059247号"
                            )
                        )
                    ),
                    _react2.default.createElement(
                        "p",
                        null,
                        _react2.default.createElement(
                            "span",
                            null,
                            _react2.default.createElement("img", { src: "http://www.beian.gov.cn/img/ghs.png" }),
                            _react2.default.createElement(
                                "a",
                                { href: "http://www.beian.gov.cn/portal/registerSystemInfo?recordcode=11010802022799" },
                                "京公网安备 11010802022799号"
                            )
                        )
                    )
                )
            ),
            _react2.default.createElement(
                "div",
                { id: "44444", className: "ax_paragraph" },
                _react2.default.createElement("img", { id: "u117_img", className: "img ", src: "i/resources/images/transparent.gif" }),
                _react2.default.createElement(
                    "div",
                    { id: "u118", className: "text" },
                    _react2.default.createElement(
                        "p",
                        null,
                        _react2.default.createElement(
                            "span",
                            null,
                            "媒体更新"
                        )
                    )
                ),
                _react2.default.createElement(
                    "div",
                    { id: "haha" },
                    _react2.default.createElement("input", null)
                )
            )
        );
    }
});
exports.default = AboutUs;

},{"react":356}],3:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _reactRouter = require('react-router');

var _amazeuiReact = require('amazeui-react');

var _SelectArea = require('./SelectArea');

var _SelectArea2 = _interopRequireDefault(_SelectArea);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

//头部
var HeaderTop = _react2.default.createClass({
  displayName: 'HeaderTop',
  render: function render() {
    return _react2.default.createElement(
      _amazeuiReact.Grid,
      null,
      _react2.default.createElement(
        _amazeuiReact.Col,
        { sm: 4 },
        _react2.default.createElement(_SelectArea2.default, null)
      ),
      _react2.default.createElement(
        _amazeuiReact.Col,
        { sm: 5 },
        _react2.default.createElement(_amazeuiReact.Nav, { pills: true })
      )
    );
  }
});
var HeaderSearchBar = _react2.default.createClass({
  displayName: 'HeaderSearchBar',
  render: function render() {
    return _react2.default.createElement(
      _amazeuiReact.Grid,
      null,
      _react2.default.createElement(
        _amazeuiReact.Col,
        { sm: 7 },
        _react2.default.createElement(
          _amazeuiReact.Form,
          { inline: true },
          _react2.default.createElement(_amazeuiReact.Input, { placeholder: '搜索点什么东西吧', icon: 'search' }),
          ' ',
          _react2.default.createElement(_amazeuiReact.Input, { type: 'submit', amStyle: 'primary', value: '搜索', standalone: true })
        )
      ),
      _react2.default.createElement(
        _amazeuiReact.Col,
        { sm: 5 },
        _react2.default.createElement(
          _amazeuiReact.Button,
          { amStyle: 'secondary' },
          '直接预约'
        )
      )
    );
  }
});
var HeaderLogo = _react2.default.createClass({
  displayName: 'HeaderLogo',
  render: function render() {
    return _react2.default.createElement(_amazeuiReact.Image, {
      standalone: true,
      width: '120',
      heigth: '40',
      src: '/i/logo.png' });
  }
});
var Header1 = _react2.default.createClass({
  displayName: 'Header1',
  render: function render() {
    return _react2.default.createElement(
      'div',
      null,
      _react2.default.createElement(
        _amazeuiReact.Grid,
        null,
        _react2.default.createElement(
          _amazeuiReact.Col,
          { sm: 12 },
          _react2.default.createElement(HeaderTop, null)
        )
      ),
      _react2.default.createElement(
        _amazeuiReact.Grid,
        null,
        _react2.default.createElement(
          _amazeuiReact.Col,
          { sm: 12 },
          _react2.default.createElement(_amazeuiReact.Image, {
            src: '/i/touch/icon-128x128.png',
            width: '100%',
            height: '80',
            radius: true })
        )
      ),
      _react2.default.createElement(
        _amazeuiReact.Grid,
        null,
        _react2.default.createElement(
          _amazeuiReact.Col,
          { sm: 3 },
          _react2.default.createElement(HeaderLogo, null)
        ),
        _react2.default.createElement(
          _amazeuiReact.Col,
          { sm: 9, end: true },
          _react2.default.createElement(HeaderSearchBar, null)
        )
      )
    );
  }
});

exports.default = Header1;

},{"./SelectArea":5,"amazeui-react":52,"react":356,"react-router":219}],4:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _reactRouter = require('react-router');

var _amazeuiReact = require('amazeui-react');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Qbzz = _react2.default.createClass({
  displayName: 'Qbzz',
  render: function render() {

    return _react2.default.createElement(
      'div',
      null,
      _react2.default.createElement(
        _amazeuiReact.Grid,
        null,
        _react2.default.createElement(
          _amazeuiReact.Col,
          { sm: 3, className: 'padding-left-rem' },
          _react2.default.createElement(
            'div',
            { className: 'w-bg-green' },
            _react2.default.createElement(
              'h1',
              null,
              '全部症状'
            )
          )
        ),
        _react2.default.createElement(
          _amazeuiReact.Col,
          { sm: 9, className: 'padding-right-rem' },
          _react2.default.createElement(
            'div',
            { className: 'height30' },
            ' '
          ),
          _react2.default.createElement(
            'div',
            { className: 'height10 w-bg-green' },
            ' '
          )
        ),
        _react2.default.createElement(
          _amazeuiReact.Col,
          { sm: 3, className: 'padding-left-rem' },
          _react2.default.createElement(
            _amazeuiReact.List,
            { className: 'list-green' },
            _react2.default.createElement(
              _amazeuiReact.ListItem,
              { href: 'http://www.amazeui.org' },
              '关节'
            ),
            _react2.default.createElement(
              _amazeuiReact.ListItem,
              { href: 'http://www.amazeui.org' },
              '脊椎'
            ),
            _react2.default.createElement(
              _amazeuiReact.ListItem,
              { href: 'http://www.amazeui.org' },
              '四肢'
            ),
            _react2.default.createElement(
              _amazeuiReact.ListItem,
              { href: 'http://www.amazeui.org' },
              '脑袋'
            )
          )
        ),
        _react2.default.createElement(
          _amazeuiReact.Col,
          { sm: 6, className: 'padding-right-rem' },
          _react2.default.createElement(_amazeuiReact.Image, {
            src: '/i/touch/icon-128x128.png',
            width: '100%',
            height: '200',
            radius: true })
        ),
        _react2.default.createElement(
          _amazeuiReact.Col,
          { sm: 3, className: 'padding-right-rem' },
          _react2.default.createElement(
            _amazeuiReact.List,
            { 'static': true, border: true },
            _react2.default.createElement(
              _amazeuiReact.ListItem,
              null,
              _react2.default.createElement(
                _amazeuiReact.Badge,
                { amStyle: 'success' },
                '10分钟'
              ),
              '脱下长日的假面'
            ),
            _react2.default.createElement(
              _amazeuiReact.ListItem,
              null,
              _react2.default.createElement(
                _amazeuiReact.Badge,
                { amStyle: 'success' },
                '11分钟'
              ),
              '奔向梦幻的疆界'
            ),
            _react2.default.createElement(
              _amazeuiReact.ListItem,
              null,
              _react2.default.createElement(
                _amazeuiReact.Badge,
                { amStyle: 'success' },
                '12分钟'
              ),
              '南瓜马车的午夜'
            ),
            _react2.default.createElement(
              _amazeuiReact.ListItem,
              null,
              _react2.default.createElement(
                _amazeuiReact.Badge,
                { amStyle: 'success' },
                '13分钟'
              ),
              '昨天太近　明天太远'
            )
          )
        )
      )
    );
  }
});
exports.default = Qbzz;

},{"amazeui-react":52,"react":356,"react-router":219}],5:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _reactRouter = require('react-router');

var _amazeuiReact = require('amazeui-react');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var SelectArea = _react2.default.createClass({
  displayName: 'SelectArea',
  render: function render() {
    var data = [{ value: '北京', label: '北京' }, { value: '北京2', label: '北京' }, { value: '北京3', label: '北京' }, { value: '北京4', label: '北京' }, { value: '北京5', label: '北京' }, { value: '北京6', label: '北京' }, { value: '北京7', label: '北京' }, { value: '北京8', label: '北京' }, { value: '北京9', label: '北京' }, { value: '北京0', label: '北京' }, { value: '北京11', label: '北京' }, { value: '北京12', label: '北京' }, { value: '北京21', label: '北京' }, { value: '北京23', label: '北京' }, { value: '北京12', label: '北京' }, { value: '北京312', label: '北京' }, { value: '北京42', label: '北京' }];

    var props = {
      data: data,
      onChange: function onChange(value) {
        console.log('当前值为：', value);
      }
    };
    return _react2.default.createElement(
      _amazeuiReact.ButtonToolbar,
      null,
      /* 设置默认值 */' ',
      _react2.default.createElement(_amazeuiReact.Selected, _extends({}, props, { value: '全国', placeholder: '选择手术地点' }))
    );
  }
});
exports.default = SelectArea;

},{"amazeui-react":52,"react":356,"react-router":219}],6:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.AboutUs = undefined;

var _AboutUs2 = require('./AboutUs');

var _AboutUs3 = _interopRequireDefault(_AboutUs2);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.AboutUs = _AboutUs3.default;

},{"./AboutUs":2}],7:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _reactRouter = require('react-router');

var _amazeuiReact = require('amazeui-react');

var _auth = require('./auth');

var _auth2 = _interopRequireDefault(_auth);

var _Logout = require('../pages/Logout');

var _Logout2 = _interopRequireDefault(_Logout);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Header = _react2.default.createClass({
  displayName: 'Header',
  handleClick: function handleClick() {
    if (matchMedia && matchMedia('(max-width: 640px)').matches) {
      this.refs.topbar.handleToggle();
    }
  },
  render: function render() {
    return _react2.default.createElement(
      _amazeuiReact.Topbar,
      {
        brand: '谷雨医生',
        toggleNavKey: 'nav',
        inverse: true,
        fluid: true,
        ref: 'topbar'
      },
      _react2.default.createElement(
        _amazeuiReact.CollapsibleNav,
        {
          eventKey: 'nav'
        },
        _react2.default.createElement(
          _amazeuiReact.Nav,
          {
            className: 'am-topbar-right',
            topbar: true
          },
          _auth2.default.getRole() == '1' || _auth2.default.getRole() == '3' || _auth2.default.getRole() == '4' || _auth2.default.getRole() == '5' ? _react2.default.createElement(
            _amazeuiReact.NavItem,
            { className: 'am-dropdown', href: '#' },
            _react2.default.createElement(
              _reactRouter.Link,
              { to: '/index_home' },
              _react2.default.createElement(_amazeuiReact.Icon, { icon: 'home' }),
              ' ',
              '网站首页'
            )
          ) : '',
          _auth2.default.getRole() == '1' ? _react2.default.createElement(
            _amazeuiReact.Dropdown,
            {
              title: [_react2.default.createElement(_amazeuiReact.Icon, { icon: 'group', key: 'hey' }), _auth2.default.getUsername()],
              navItem: true
            },
            _react2.default.createElement(
              _amazeuiReact.Dropdown.Item,
              {
                closeOnClick: true,
                linkComponent: _reactRouter.Link,
                linkProps: {
                  to: { pathname: '/user_index' },
                  onClick: this.handleClick
                }
              },
              _react2.default.createElement(_amazeuiReact.Icon, { icon: 'user' }),
              ' ',
              '用户首页'
            )
          ) : _react2.default.createElement(_amazeuiReact.NavItem, null),
          _auth2.default.getRole() == '5' ? _react2.default.createElement(
            _amazeuiReact.Dropdown,
            {
              title: [_react2.default.createElement(_amazeuiReact.Icon, { icon: 'group', key: 'hey' }), _auth2.default.getUsername()],
              navItem: true
            },
            _react2.default.createElement(
              _amazeuiReact.Dropdown.Item,
              {
                closeOnClick: true,
                linkComponent: _reactRouter.Link,
                linkProps: {
                  to: { pathname: '/manageRoot' },
                  onClick: this.handleClick
                }
              },
              _react2.default.createElement(_amazeuiReact.Icon, { icon: 'user' }),
              ' ',
              '系统管理'
            ),
            _react2.default.createElement(
              _amazeuiReact.Dropdown.Item,
              {
                closeOnClick: true,
                linkComponent: _reactRouter.Link,
                linkProps: {
                  to: { pathname: '/system_manage' },
                  onClick: this.handleClick
                }
              },
              _react2.default.createElement(_amazeuiReact.Icon, { icon: 'user' }),
              ' ',
              '订单管理'
            ),
            _react2.default.createElement(
              _amazeuiReact.Dropdown.Item,
              {
                closeOnClick: true,
                linkComponent: _reactRouter.Link,
                linkProps: {
                  to: { pathname: '/user_manage' },
                  onClick: this.handleClick
                }
              },
              _react2.default.createElement(_amazeuiReact.Icon, { icon: 'user' }),
              ' ',
              '用户管理'
            ),
            _react2.default.createElement(
              _amazeuiReact.Dropdown.Item,
              {
                closeOnClick: true,
                linkComponent: _reactRouter.Link,
                linkProps: {
                  to: { pathname: '/price_manage' },
                  onClick: this.handleClick
                }
              },
              _react2.default.createElement(_amazeuiReact.Icon, { icon: 'user' }),
              ' ',
              '价格管理'
            ),
            _react2.default.createElement(
              _amazeuiReact.Dropdown.Item,
              {
                closeOnClick: true,
                linkComponent: _reactRouter.Link,
                linkProps: {
                  to: { pathname: '/account_manage' },
                  onClick: this.handleClick
                }
              },
              _react2.default.createElement(_amazeuiReact.Icon, { icon: 'user' }),
              ' ',
              '账户管理'
            )
          ) : _react2.default.createElement(_amazeuiReact.NavItem, null),
          _react2.default.createElement(
            _amazeuiReact.NavItem,
            {
              className: 'am-dropdown',
              href: '#'
            },
            _auth2.default.loggedIn() ? _react2.default.createElement(
              _reactRouter.Link,
              { to: '/Logout' },
              _react2.default.createElement(_amazeuiReact.Icon, { icon: 'sign-out' }),
              ' ',
              ' 退出登录'
            ) : _react2.default.createElement(
              _reactRouter.Link,
              { to: '/login' },
              _react2.default.createElement(_amazeuiReact.Icon, { icon: 'sign-in' }),
              ' ',
              ' 登录'
            )
          )
        )
      )
    );
  }
});

exports.default = Header;

},{"../pages/Logout":37,"./auth":25,"amazeui-react":52,"react":356,"react-router":219}],8:[function(require,module,exports){
"use strict";

},{}],9:[function(require,module,exports){
"use strict";

},{}],10:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _amazeuiReact = require('amazeui-react');

var _PageContainer = require('../PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

var _auth = require('../auth');

var _auth2 = _interopRequireDefault(_auth);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var UserManage = _react2.default.createClass({
    displayName: 'UserManage',
    getInitialState: function getInitialState() {
        return {
            dataSource: { re: [{ "user_name": "ffff", "user_role": "iii", "user_mobile": "111", "user_last_login": "qqq", "state": "0" }], totalpage: 1 },
            nowpage: '1',
            totalpage: '1'
        };
    },
    query: function query() {
        var _this = this;

        var page = arguments.length <= 0 || arguments[0] === undefined ? this.state.nowpage : arguments[0];

        _auth2.default.myact({ to: 'manager/userlist.do',
            parms: [{ 'key': 'page', 'value': page }]
        }, function (res) {
            _this.updateDataSource(res);
        });
        this.setState({ nowpage: page });
    },
    querynext: function querynext() {
        var nowpage = this.state.nowpage;
        var totalpage = this.state.totalpage;
        if (nowpage + 1 <= totalpage) {
            nowpage += 1;
            this.query(nowpage);
        }
    },
    querybefore: function querybefore() {

        var nowpage = this.state.nowpage;
        var totalpage = this.state.totalpage;
        if (nowpage - 1 >= 1) {
            nowpage -= 1;
            this.query(nowpage);
        }
    },
    deletelist: function deletelist(id) {
        console.log(id);
        _auth2.default.myact({ to: 'manager/deleteuser.do',
            parms: [{ 'key': 'id', 'value': id }]
        }, function (res) {
            console.log(res);
        });
        this.query();
    },
    disable: function disable(id) {
        console.log(id);
        _auth2.default.myact({ to: 'manager/disableuser.do',
            parms: [{ 'key': 'id', 'value': id }]
        }, function (res) {
            console.log(res);
        });
        this.query();
    },
    componentDidMount: function componentDidMount() {
        this.query(1);
    },
    updateDataSource: function updateDataSource(data) {
        this.setState({
            dataSource: data,
            totalpage: data.totalpage
        });
        console.log(data);
    },
    judege: function judege(intState) {
        var state = "";
        if (intState == 0) {
            state = "禁用";
        } else if (intState == 1) {
            state = "启用";
        } else {
            state = "";
        }
        return state;
    },
    kaifazhong: function kaifazhong(str) {
        console.log(str);
    },
    change: function change(id) {
        console.log(id);
        _auth2.default.myact({ to: '/manager/changeuser.do',
            parms: [{ 'key': 'id', 'value': id }, { key: 'name', value: this.refs.un.value }, { key: 'mobile', value: this.refs.um.value }, { key: 'role', value: this.refs.ur.value }, { key: 'state', value: this.refs.ps.value }]
        }, function (res) {
            Console.log(name);
        });
        this.query();
    },
    renderTable: function renderTable() {
        var _this2 = this;

        var data = this.state.dataSource;
        console.log(data.re.length);
        return _react2.default.createElement(
            _amazeuiReact.Table,
            { bordered: true, striped: true, hover: true, radius: true, responsive: true },
            _react2.default.createElement(
                'thead',
                null,
                _react2.default.createElement(
                    'tr',
                    null,
                    _react2.default.createElement(
                        'th',
                        null,
                        '用户名'
                    ),
                    _react2.default.createElement(
                        'th',
                        null,
                        '账户类型 '
                    ),
                    _react2.default.createElement(
                        'th',
                        null,
                        '电话'
                    ),
                    _react2.default.createElement(
                        'th',
                        null,
                        '上次登录时间'
                    ),
                    _react2.default.createElement(
                        'th',
                        null,
                        '状态'
                    ),
                    _react2.default.createElement(
                        'th',
                        null,
                        '操作'
                    )
                )
            ),
            _react2.default.createElement(
                'tbody',
                null,
                data.re.map(function (item, index) {
                    return _react2.default.createElement(
                        'tr',
                        { key: index },
                        _react2.default.createElement(
                            'td',
                            null,
                            item.user_name
                        ),
                        _react2.default.createElement(
                            'td',
                            null,
                            item.user_role
                        ),
                        _react2.default.createElement(
                            'td',
                            null,
                            item.user_mobile
                        ),
                        _react2.default.createElement(
                            'td',
                            null,
                            _this2.judege(item.state)
                        ),
                        _react2.default.createElement(
                            'td',
                            null,
                            item.user_last_login
                        ),
                        _react2.default.createElement(
                            'td',
                            null,
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '删除'
                                ),
                                '  /',
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '禁用'
                                ),
                                '  /',
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '修改'
                                )
                            )
                        )
                    );
                })
            )
        );
    },
    render: function render() {
        return _react2.default.createElement(
            _PageContainer2.default,
            null,
            _react2.default.createElement(
                'div',
                null,
                this.renderTable()
            ),
            _react2.default.createElement(
                'div',
                { id: 'u48', 'class': 'text' },
                _react2.default.createElement(
                    'p',
                    { onClick: this.querybefore },
                    _react2.default.createElement(
                        'span',
                        null,
                        '上一页'
                    ),
                    '        ',
                    _react2.default.createElement(
                        'span',
                        { onClick: this.querynext },
                        _react2.default.createElement(
                            'span',
                            null,
                            '下一页'
                        )
                    )
                ),
                _react2.default.createElement('div', { id: 'ubu', 'class': 'ax_html_button' })
            )
        );
    }
});
exports.default = UserManage;

},{"../PageContainer":19,"../auth":25,"amazeui-react":52,"react":356}],11:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.AccountManage = exports.PriceManage = exports.UserManage = undefined;

var _UserManage2 = require('./UserManage');

var _UserManage3 = _interopRequireDefault(_UserManage2);

var _PriceManage2 = require('./PriceManage');

var _PriceManage3 = _interopRequireDefault(_PriceManage2);

var _AccountManage2 = require('./AccountManage');

var _AccountManage3 = _interopRequireDefault(_AccountManage2);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.UserManage = _UserManage3.default;
exports.PriceManage = _PriceManage3.default;
exports.AccountManage = _AccountManage3.default;

},{"./AccountManage":8,"./PriceManage":9,"./UserManage":10}],12:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

var _amazeuiReact = require('amazeui-react');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Message = _react2.default.createClass({
  displayName: 'Message',

  propTypes: {
    event: _react2.default.PropTypes.object
  },

  renderDetail: function renderDetail() {
    var event = this.props.event;

    if (event) {
      var created_at = event.created_at;
      var type = event.type;
      var repo = event.repo;
      var actor = event.actor;

      return _react2.default.createElement(
        'div',
        null,
        _react2.default.createElement(
          'a',
          {
            href: 'https://github.com/' + actor.login,
            target: '_blank',
            className: 'msg-avatar'

          },
          _react2.default.createElement(_amazeuiReact.Image, {
            src: actor.avatar_url,
            alt: actor.login,
            width: '140',
            height: '140',
            radius: true
          }),
          _react2.default.createElement(
            'strong',
            null,
            actor.login
          )
        ),
        _react2.default.createElement(
          'ul',
          null,
          _react2.default.createElement(
            'li',
            null,
            _react2.default.createElement(
              'em',
              null,
              '日期：'
            ),
            created_at
          ),
          _react2.default.createElement(
            'li',
            null,
            _react2.default.createElement(
              'em',
              null,
              '项目：'
            ),
            _react2.default.createElement(
              'a',
              {
                href: 'https://github.com/' + repo.name,
                target: '_blank'
              },
              repo.name
            )
          ),
          _react2.default.createElement(
            'li',
            null,
            _react2.default.createElement(
              'em',
              null,
              '操作：'
            ),
            type
          )
        )
      );
    }

    return _react2.default.createElement(
      'p',
      null,
      'Oops, 无法获取消息...',
      _react2.default.createElement('br', null),
      'Message ID: ',
      this.props.params.id
    );
  },
  render: function render() {
    return _react2.default.createElement(
      'div',
      {
        className: 'adm-msg-body'
      },
      _react2.default.createElement(
        'div',
        { className: 'adm-msg-content' },
        this.renderDetail()
      )
    );
  }
});
//公用message详情组件
exports.default = Message;

},{"../components/PageContainer":19,"amazeui-react":52,"react":356}],13:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _amazeuiReact = require('amazeui-react');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _objectWithoutProperties(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }

var MessageBar = _react2.default.createClass({
  displayName: 'MessageBar',
  render: function render() {
    var _props = this.props;
    var className = _props.className;

    var props = _objectWithoutProperties(_props, ['className']);

    return _react2.default.createElement(
      'div',
      _extends({}, props, {
        className: 'adm-msg-bar ' + className
      }),
      this.props.children
    );
  }
});
//消息列表页
exports.default = MessageBar;

},{"amazeui-react":52,"react":356}],14:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _amazeuiReact = require('amazeui-react');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var NoMessage = _react2.default.createClass({
  displayName: 'NoMessage',
  render: function render() {
    return _react2.default.createElement(
      'div',
      { className: 'adm-msg-default' },
      _react2.default.createElement(_amazeuiReact.Icon, { icon: 'frown-o' }),
      _react2.default.createElement(
        'div',
        null,
        '未选取消息'
      )
    );
  }
});
// 没有消息的特殊页面
exports.default = NoMessage;

},{"amazeui-react":52,"react":356}],15:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _amazeuiReact = require('amazeui-react');

var _auth = require('../auth');

var _auth2 = _interopRequireDefault(_auth);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Order_done = _react2.default.createClass({
  displayName: 'Order_done',
  getInitialState: function getInitialState() {
    return {
      dataSource: []
    };
  },
  formatstsate: function formatstsate(id) {
    if (id == 0) {
      return '未完成';
    }
    if (id == 1) {
      return '进行中';
    }
    if (id == 2) {
      return '已完成';
    }
  },
  componentWillMount: function componentWillMount() {
    this.re_render();
  },
  componentWillReceiveProps: function componentWillReceiveProps() {
    this.re_render();
  },
  re_render: function re_render() {
    var _this = this;

    _auth2.default.myact({ to: 'orderAdmin.do',
      parms: [{ 'key': 'state', 'value': '2' }]
    }, function (res) {
      _this.updateDataSource(res);
    });
  },
  updateDataSource: function updateDataSource(data) {
    this.setState({
      dataSource: data
    });
  },
  renderTable: function renderTable() {
    var _this2 = this;

    var dataSource = this.state.dataSource;

    return _react2.default.createElement(
      _amazeuiReact.Table,
      { bordered: true, striped: true, hover: true, radius: true, responsive: true },
      _react2.default.createElement(
        'thead',
        null,
        _react2.default.createElement(
          'tr',
          null,
          _react2.default.createElement(
            'th',
            null,
            '状态'
          ),
          _react2.default.createElement(
            'th',
            null,
            '订单号'
          ),
          _react2.default.createElement(
            'th',
            null,
            '患者姓名'
          ),
          _react2.default.createElement(
            'th',
            null,
            '年龄'
          ),
          _react2.default.createElement(
            'th',
            null,
            '地区'
          ),
          _react2.default.createElement(
            'th',
            null,
            '预约时间'
          ),
          _react2.default.createElement(
            'th',
            null,
            '病症'
          ),
          _react2.default.createElement(
            'th',
            null,
            '系统分析病症'
          ),
          _react2.default.createElement(
            'th',
            null,
            '系统推荐团队'
          ),
          _react2.default.createElement(
            'th',
            null,
            '偏好专家'
          ),
          _react2.default.createElement(
            'th',
            null,
            '跟进助理'
          ),
          _react2.default.createElement(
            'th',
            null,
            '手机号'
          )
        )
      ),
      _react2.default.createElement(
        'tbody',
        null,
        dataSource.map(function (item, index) {
          return _react2.default.createElement(
            'tr',
            { key: index },
            _react2.default.createElement(
              'td',
              null,
              _this2.formatstsate(item.orderState)
            ),
            _react2.default.createElement(
              'td',
              null,
              item.orderNumber
            ),
            _react2.default.createElement(
              'td',
              null,
              item.patientRealname
            ),
            _react2.default.createElement(
              'td',
              null,
              item.patinetAge
            ),
            _react2.default.createElement(
              'td',
              null,
              item.area
            ),
            _react2.default.createElement(
              'td',
              null,
              item.orderReserveTime
            ),
            _react2.default.createElement(
              'td',
              null,
              item.status
            ),
            _react2.default.createElement(
              'td',
              null,
              item.patientDiagnose
            ),
            _react2.default.createElement(
              'td',
              null,
              item.systemals
            ),
            _react2.default.createElement(
              'td',
              null,
              item.adviceteam
            ),
            _react2.default.createElement(
              'td',
              null,
              item.proferdoctor
            ),
            _react2.default.createElement(
              'td',
              null,
              item.ass
            ),
            _react2.default.createElement(
              'td',
              null,
              item.mobile
            )
          );
        })
      )
    );
  },
  render: function render() {
    return _react2.default.createElement(
      'div',
      null,
      this.renderTable()
    );
  }
});

exports.default = Order_done;

},{"../auth":25,"amazeui-react":52,"react":356}],16:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _amazeuiReact = require('amazeui-react');

var _auth = require('../auth');

var _auth2 = _interopRequireDefault(_auth);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Order_null = _react2.default.createClass({
  displayName: 'Order_null',
  getInitialState: function getInitialState() {
    return {
      dataSource: []
    };
  },
  formatstsate: function formatstsate(id) {
    if (id == 0) {
      return '未完成';
    }
    if (id == 1) {
      return '进行中';
    }
    if (id == 2) {
      return '已完成';
    }
  },
  componentWillMount: function componentWillMount() {

    this.regetinfo();
  },
  regetinfo: function regetinfo() {
    var _this = this;

    _auth2.default.myact({ to: 'orderAdmin.do',
      parms: [{ 'key': 'state', 'value': '1' }]
    }, function (res) {
      _this.updateDataSource(res);
    });
  },
  cancel_follow_up: function cancel_follow_up(orderNumber) {
    var _this2 = this;

    _auth2.default.get('cancelFollowUp.do', { 'orderNumber': orderNumber }, function (re) {
      _this2.regetinfo();
    });
  },
  finish_follow_up: function finish_follow_up(orderNumber) {
    var _this3 = this;

    _auth2.default.get('finishFollowUp.do', { 'orderNumber': orderNumber }, function (re) {
      _this3.regetinfo();
    });
  },
  componentWillReceiveProps: function componentWillReceiveProps() {
    this.regetinfo();
  },
  updateDataSource: function updateDataSource(data) {
    this.setState({
      dataSource: data
    });
  },
  renderTable: function renderTable() {
    var _this4 = this;

    var dataSource = this.state.dataSource;

    return _react2.default.createElement(
      _amazeuiReact.Table,
      { striped: true, className: 'am-margin-bottom', responsive: true },
      _react2.default.createElement(
        'thead',
        null,
        _react2.default.createElement(
          'tr',
          null,
          _react2.default.createElement(
            'th',
            null,
            '状态'
          ),
          _react2.default.createElement(
            'th',
            null,
            '订单号'
          ),
          _react2.default.createElement(
            'th',
            null,
            '患者姓名'
          ),
          _react2.default.createElement(
            'th',
            null,
            '年龄'
          ),
          _react2.default.createElement(
            'th',
            null,
            '地区'
          ),
          _react2.default.createElement(
            'th',
            null,
            '手术时间'
          ),
          _react2.default.createElement(
            'th',
            null,
            '手术名称'
          ),
          _react2.default.createElement(
            'th',
            null,
            '手术团队'
          ),
          _react2.default.createElement(
            'th',
            null,
            '更多信息'
          ),
          _react2.default.createElement(
            'th',
            null,
            '跟进助理'
          ),
          _react2.default.createElement(
            'th',
            null,
            '手机号'
          )
        )
      ),
      _react2.default.createElement(
        'tbody',
        null,
        dataSource.map(function (item, index) {
          return _react2.default.createElement(
            'tr',
            { key: index },
            _react2.default.createElement(
              'td',
              null,
              _this4.formatstsate(item.orderState)
            ),
            ' //也需要转化',
            _react2.default.createElement(
              'td',
              null,
              item.orderNumber
            ),
            _react2.default.createElement(
              'td',
              null,
              item.patientRealname
            ),
            _react2.default.createElement(
              'td',
              null,
              item.patinetAge
            ),
            _react2.default.createElement(
              'td',
              null,
              item.patientProvince
            ),
            _react2.default.createElement(
              'td',
              null,
              item.orderReserveTime
            ),
            _react2.default.createElement(
              'td',
              null,
              item.patientDiagnose
            ),
            _react2.default.createElement(
              'td',
              null,
              item.team
            ),
            _react2.default.createElement(
              'td',
              null,
              item.more
            ),
            _react2.default.createElement(
              'td',
              null,
              _react2.default.createElement(
                'span',
                { onClick: function onClick() {
                    _this4.cancel_follow_up(item.orderNumber);
                  } },
                '取消'
              ),
              '-OR-',
              _react2.default.createElement(
                'span',
                { onClick: function onClick() {
                    _this4.finish_follow_up(item.orderNumber);
                  } },
                '完成'
              )
            ),
            _react2.default.createElement(
              'td',
              null,
              item.mobile
            )
          );
        })
      )
    );
  },
  render: function render() {
    return _react2.default.createElement(
      'div',
      null,
      this.renderTable()
    );
  }
});

exports.default = Order_null;

},{"../auth":25,"amazeui-react":52,"react":356}],17:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _amazeuiReact = require('amazeui-react');

var _auth = require('../auth');

var _auth2 = _interopRequireDefault(_auth);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Order_null = _react2.default.createClass({
  displayName: 'Order_null',
  getInitialState: function getInitialState() {
    return {
      dataSource: []
    };
  },
  formatstsate: function formatstsate(id) {
    if (id == 0) {
      return '未完成';
    }
    if (id == 1) {
      return '进行中';
    }
    if (id == 2) {
      return '已完成';
    }
  },
  re_get_info: function re_get_info() {
    var _this = this;

    _auth2.default.myact({ to: 'orderAdmin.do',
      parms: [{ 'key': 'state', 'value': '0' }]
    }, function (res) {
      _this.updateDataSource(res);
    });
  },
  componentWillReceiveProps: function componentWillReceiveProps() {
    this.re_get_info();
  },
  componentWillMount: function componentWillMount() {

    this.re_get_info();
  },
  fllow_up: function fllow_up(orderNumber) {
    var _this2 = this;

    _auth2.default.get('followUp.do', { 'orderNumber': orderNumber }, function (re) {
      _this2.re_get_info();
    });
  },
  updateDataSource: function updateDataSource(data) {
    this.setState({
      dataSource: data
    });
  },
  renderTable: function renderTable() {
    var _this3 = this;

    var dataSource = this.state.dataSource;

    return _react2.default.createElement(
      _amazeuiReact.Table,
      { bordered: true, striped: true, responsive: true },
      _react2.default.createElement(
        'thead',
        null,
        _react2.default.createElement(
          'tr',
          null,
          _react2.default.createElement(
            'th',
            null,
            '状态'
          ),
          _react2.default.createElement(
            'th',
            null,
            '订单号'
          ),
          _react2.default.createElement(
            'th',
            null,
            '患者姓名'
          ),
          _react2.default.createElement(
            'th',
            null,
            '年龄'
          ),
          _react2.default.createElement(
            'th',
            null,
            '地区'
          ),
          _react2.default.createElement(
            'th',
            null,
            '预约时间'
          ),
          _react2.default.createElement(
            'th',
            null,
            '病症'
          ),
          _react2.default.createElement(
            'th',
            null,
            '系统分析病症'
          ),
          _react2.default.createElement(
            'th',
            null,
            '系统推荐团队'
          ),
          _react2.default.createElement(
            'th',
            null,
            '偏好专家'
          ),
          _react2.default.createElement(
            'th',
            null,
            '跟进助理'
          ),
          _react2.default.createElement(
            'th',
            null,
            '手机号'
          )
        )
      ),
      _react2.default.createElement(
        'tbody',
        null,
        dataSource.map(function (item, index) {
          return _react2.default.createElement(
            'tr',
            { key: index },
            _react2.default.createElement(
              'td',
              null,
              _this3.formatstsate(item.orderState)
            ),
            '//需要转换一下 0 未完成 1 进行中 2 已完成',
            _react2.default.createElement(
              'td',
              null,
              item.orderNumber
            ),
            _react2.default.createElement(
              'td',
              null,
              item.patientRealname
            ),
            _react2.default.createElement(
              'td',
              null,
              item.patinetAge
            ),
            _react2.default.createElement(
              'td',
              null,
              item.patientProvince
            ),
            _react2.default.createElement(
              'td',
              null,
              item.orderReserveTime
            ),
            _react2.default.createElement(
              'td',
              null,
              item.patientDiagnose
            ),
            _react2.default.createElement(
              'td',
              null,
              item.systemals
            ),
            _react2.default.createElement(
              'td',
              null,
              item.adviceteam
            ),
            _react2.default.createElement(
              'td',
              null,
              item.proferdoctor
            ),
            _react2.default.createElement(
              'td',
              null,
              _react2.default.createElement(
                'span',
                { onClick: function onClick() {
                    _this3.fllow_up(item.orderNumber);
                  } },
                '我要跟进'
              )
            ),
            _react2.default.createElement(
              'td',
              null,
              item.mobile
            )
          );
        })
      )
    );
  },
  render: function render() {
    return _react2.default.createElement(
      'div',
      null,
      this.renderTable()
    );
  }
});

exports.default = Order_null;

},{"../auth":25,"amazeui-react":52,"react":356}],18:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Order_done = exports.Order_ing = exports.Order_null = undefined;

var _Order_null2 = require('./Order_null');

var _Order_null3 = _interopRequireDefault(_Order_null2);

var _Order_ing2 = require('./Order_ing');

var _Order_ing3 = _interopRequireDefault(_Order_ing2);

var _Order_done2 = require('./Order_done');

var _Order_done3 = _interopRequireDefault(_Order_done2);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.Order_null = _Order_null3.default; //Order存放了订单相关的三个页面

exports.Order_ing = _Order_ing3.default;
exports.Order_done = _Order_done3.default;

},{"./Order_done":15,"./Order_ing":16,"./Order_null":17}],19:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _reactRouter = require('react-router');

var _amazeuiReact = require('amazeui-react');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _objectWithoutProperties(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }

var BreadCrumbLink = _react2.default.createClass({
  displayName: 'BreadCrumbLink',
  render: function render() {
    var _props = this.props;
    var active = _props.active;

    var props = _objectWithoutProperties(_props, ['active']);

    return _react2.default.createElement(
      _amazeuiReact.Breadcrumb.Item,
      {
        active: active,
        linkComponent: _reactRouter.Link,
        linkProps: props
      },
      this.props.children
    );
  }
});

var PageContainer = _react2.default.createClass({
  displayName: 'PageContainer',

  propTypes: {
    breadcrumb: _react2.default.PropTypes.string,
    noBreadcrumb: _react2.default.PropTypes.bool
  },

  renderBreadcrumb: function renderBreadcrumb() {
    var _props2 = this.props;
    var noBreadcrumb = _props2.noBreadcrumb;
    var breadcrumb = _props2.breadcrumb;


    return noBreadcrumb ? null : _react2.default.createElement(
      _amazeuiReact.Breadcrumb,
      { slash: true },
      _react2.default.createElement(
        BreadCrumbLink,
        {
          to: '/home',
          active: !!breadcrumb
        },
        _react2.default.createElement(_amazeuiReact.Icon, { icon: 'home' }),
        '首页'
      ),
      breadcrumb ? _react2.default.createElement(
        _amazeuiReact.Breadcrumb.Item,
        { active: true },
        breadcrumb
      ) : null
    );
  },

  // {this.renderBreadcrumb()}
  render: function render() {
    return _react2.default.createElement(
      'div',
      {
        className: 'adm-page ' + this.props.className
      },
      this.props.children
    );
  }
});

exports.default = PageContainer;

},{"amazeui-react":52,"react":356,"react-router":219}],20:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _reactRouter = require('react-router');

var _amazeuiReact = require('amazeui-react');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var navs = [{
  id: 'price_manage',
  title: '价格管理',
  icon: 'info'
}, {
  id: 'account_manage',
  title: '账户管理',
  icon: 'info'

}, {
  id: 'user_manage',
  title: '用户管理',
  icon: 'info'
}, {
  id: 'doctor_list',
  title: '医生列表',
  icon: 'info'
}, {
  id: 'user_index',
  title: '用户首页',
  icon: 'phone'
}, {
  id: 'institution_list',
  title: '机构列表',
  icon: 'phone'
}, {
  id: 'order',
  title: '订单管理',
  icon: 'info'
}, {
  id: 'index_home',
  title: '网站首页',
  icon: 'info'
}, {
  id: 'login',
  title: '用户登录',
  icon: 'info'
}, {
  id: 'adminLogin',
  title: '管理员登录',
  icon: 'info'

}, {
  id: 'register_user',
  title: '用户注册',
  icon: 'info'
}, {
  id: 'register_doctor',
  title: '医生注册',
  icon: 'phone'
}, {
  id: 'register_hospital',
  title: '机构注册',
  icon: 'phone'
}, {
  id: 'checkReservation',
  title: '用户核对预约',
  icon: 'phone'
}, {
  id: 'user_reservation',
  title: '用户预约',
  icon: 'phone'
}, {
  id: 'query_result',
  title: '查询结果',
  icon: 'phone'
}, {
  id: 'system_manage',
  title: '飞刀平台订单管理',
  icon: 'phone'
}, {
  id: 'manageRoot',
  title: '飞刀平台系统管理',
  icon: 'info'
}];
var Siderbar = _react2.default.createClass({
  displayName: 'Siderbar',

  propTypes: {
    active: _react2.default.PropTypes.bool
  },

  getInitialState: function getInitialState() {
    return {
      activeIndex: null
    };
  },
  handleClick: function handleClick(index, e) {
    e.preventDefault();

    this.setState({
      activeIndex: this.state.activeIndex === index ? null : index
    });
  },
  renderSubNavs: function renderSubNavs(lenth) {
    var subNavs = [];

    for (var i = 0; i <= lenth; i++) {
      subNavs.push(_react2.default.createElement(
        _amazeuiReact.ListItem,
        {
          key: 'subNav-' + i
        },
        _react2.default.createElement(
          _reactRouter.Link,
          {
            to: { pathname: 'page-' + i, query: { breadcrumb: '子页面 ' + i } },
            activeClassName: 'active'
          },
          _react2.default.createElement(_amazeuiReact.Icon, { icon: 'angle-right' }),
          ' 子菜单 ' + i
        )
      ));
    }

    return _react2.default.createElement(
      _amazeuiReact.List,
      {
        className: 'adm-sidebar-sub'
      },
      subNavs
    );
  },
  renderItems: function renderItems() {
    var _this = this;

    return navs.map(function (nav, i) {
      var subNav = nav.subNav;
      var id = nav.id;
      var icon = nav.icon;
      var title = nav.title;

      var subActive = _this.state.activeIndex === i ? 'sub-active' : null;

      return _react2.default.createElement(
        _amazeuiReact.ListItem,
        {
          key: 'nav-' + i,
          className: subActive
        },
        _react2.default.createElement(
          _reactRouter.Link,
          {
            activeClassName: 'active',
            to: { pathname: '/' + id, query: { breadcrumb: title } },
            onClick: subNav ? _this.handleClick.bind(_this, i) : null
          },
          _react2.default.createElement(_amazeuiReact.Icon, { icon: subActive ? 'folder-open' : icon }),
          ' ' + title
        ),
        subNav ? _this.renderSubNavs(subNav) : null
      );
    });
  },
  render: function render() {
    var active = this.props.active ? 'active' : '';

    return _react2.default.createElement(
      'div',
      {
        className: 'adm-sidebar ' + active
      },
      _react2.default.createElement(_amazeuiReact.List, null)
    );
  }
});

exports.default = Siderbar;

},{"amazeui-react":52,"react":356,"react-router":219}],21:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _amazeuiReact = require('amazeui-react');

var _auth = require('../auth');

var _auth2 = _interopRequireDefault(_auth);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Order_done = _react2.default.createClass({
  displayName: 'Order_done',
  getInitialState: function getInitialState() {
    return {
      dataSource: []

    };
  },
  componentWillMount: function componentWillMount() {
    var _this = this;

    var usernName = _auth2.default.getUsername();
    _auth2.default.myact({
      to: 'queryOrder.do',
      parms: [{ 'key': 'year', 'value': '2016' }, { 'key': 'pay', 'value': '3' }, { 'key': 'username', value: usernName }]
    }, function (res) {
      _this.updateDataSource(res);
    });
  },
  updateDataSource: function updateDataSource(data) {
    this.setState({
      dataSource: data
    });
  },
  dataOption: function dataOption(id) {
    var _this2 = this;

    var year = id.refs.year.value;
    var usernName = _auth2.default.getUsername();
    console.log("好的");
    console.log(year);
    _auth2.default.myact({
      to: 'queryOrder.do',
      parms: [{ 'key': 'year', 'value': year }, { 'key': 'pay', 'value': '1' }, { 'key': 'username', value: usernName }]
    }, function (res) {
      _this2.updateDataSource(res);
    });
  },
  renderTable: function renderTable() {
    var dataSource = this.state.dataSource;

    console.log(dataSource);
    return _react2.default.createElement(
      _amazeuiReact.Table,
      { bordered: true, striped: true, hover: true, radius: true, responsive: true },
      _react2.default.createElement(
        'thead',
        null,
        _react2.default.createElement(
          'tr',
          null,
          _react2.default.createElement(
            'th',
            null,
            _react2.default.createElement(
              'select',
              { ref: 'year', onChange: this.dataOption.bind(this, this) },
              _react2.default.createElement(
                'option',
                { value: '2016' },
                '2016年'
              ),
              _react2.default.createElement(
                'option',
                { value: '2015' },
                '2015年'
              ),
              _react2.default.createElement(
                'option',
                { value: '2014' },
                '2014年'
              )
            )
          ),
          _react2.default.createElement(
            'th',
            null,
            '订单详情'
          ),
          _react2.default.createElement(
            'th',
            null,
            '患者姓名'
          ),
          _react2.default.createElement(
            'th',
            null,
            '金额'
          ),
          _react2.default.createElement(
            'th',
            null,
            '状态'
          ),
          _react2.default.createElement(
            'th',
            null,
            '操作'
          )
        )
      ),
      _react2.default.createElement(
        'tbody',
        null,
        dataSource.map(function (item, index) {
          return _react2.default.createElement(
            'tr',
            { key: index },
            _react2.default.createElement(
              'td',
              null,
              item.orderDate
            ),
            _react2.default.createElement(
              'td',
              null,
              item.patientProvince,
              item.patientDiagnose
            ),
            _react2.default.createElement(
              'td',
              null,
              item.patientRealname
            ),
            _react2.default.createElement(
              'td',
              null,
              item.orderPrice
            ),
            _react2.default.createElement(
              'td',
              null,
              item.orderPay
            ),
            _react2.default.createElement(
              'td',
              null,
              item.orderReserveTime
            )
          );
        })
      )
    );
  },
  render: function render() {
    return _react2.default.createElement(
      'div',
      null,
      this.renderTable()
    );
  }
});

exports.default = Order_done;

},{"../auth":25,"amazeui-react":52,"react":356}],22:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _amazeuiReact = require('amazeui-react');

var _auth = require('../auth');

var _auth2 = _interopRequireDefault(_auth);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Order_done = _react2.default.createClass({
  displayName: 'Order_done',
  getInitialState: function getInitialState() {
    return {
      dataSource: []
    };
  },
  formatstsate: function formatstsate(id) {
    if (id == 0) {
      return '全部订单';
    }
    if (id == 1) {
      return '已支付';
    }
    if (id == 2) {
      return '未支付';
    }
  },
  componentWillMount: function componentWillMount() {
    var _this = this;

    var usernName = _auth2.default.getUsername();
    _auth2.default.myact({
      to: 'queryOrder.do',
      parms: [{ 'key': 'year', 'value': 2016 }, { 'key': 'pay', 'value': '1' }, { 'key': 'username', value: usernName }]
    }, function (res) {
      _this.updateDataSource(res);
    });
  },
  updateDataSource: function updateDataSource(data) {
    this.setState({
      dataSource: data
    });
  },
  dataOption: function dataOption(id) {
    var _this2 = this;

    var year = id.refs.year.value;
    var usernName = _auth2.default.getUsername();
    console.log("好的");
    console.log(year);
    _auth2.default.myact({
      to: 'queryOrder.do',
      parms: [{ 'key': 'year', 'value': year }, { 'key': 'pay', 'value': '1' }, { 'key': 'username', value: usernName }]
    }, function (res) {
      _this2.updateDataSource(res);
    });
  },
  renderTable: function renderTable() {
    var dataSource = this.state.dataSource;

    console.log(dataSource);
    return _react2.default.createElement(
      _amazeuiReact.Table,
      { bordered: true, striped: true, hover: true, radius: true, responsive: true },
      _react2.default.createElement(
        'thead',
        null,
        _react2.default.createElement(
          'tr',
          null,
          _react2.default.createElement(
            'th',
            null,
            _react2.default.createElement(
              'select',
              { ref: 'year', onChange: this.dataOption.bind(this, this) },
              _react2.default.createElement(
                'option',
                { value: '2016' },
                '2016年'
              ),
              _react2.default.createElement(
                'option',
                { value: '2015' },
                '2015年'
              ),
              _react2.default.createElement(
                'option',
                { value: '2014' },
                '2014年'
              )
            )
          ),
          _react2.default.createElement(
            'th',
            null,
            '订单详情'
          ),
          _react2.default.createElement(
            'th',
            null,
            '患者姓名'
          ),
          _react2.default.createElement(
            'th',
            null,
            '金额'
          ),
          _react2.default.createElement(
            'th',
            null,
            '状态'
          ),
          _react2.default.createElement(
            'th',
            null,
            '操作'
          )
        )
      ),
      _react2.default.createElement(
        'tbody',
        null,
        dataSource.map(function (item, index) {
          return _react2.default.createElement(
            'tr',
            { key: index },
            _react2.default.createElement(
              'td',
              null,
              item.orderDate
            ),
            _react2.default.createElement(
              'td',
              null,
              item.patientProvince,
              item.patientDiagnose
            ),
            _react2.default.createElement(
              'td',
              null,
              item.patientRealname
            ),
            _react2.default.createElement(
              'td',
              null,
              item.orderPrice
            ),
            _react2.default.createElement(
              'td',
              null,
              item.orderPay
            ),
            _react2.default.createElement(
              'td',
              null,
              item.orderReserveTime
            )
          );
        })
      )
    );
  },
  render: function render() {
    return _react2.default.createElement(
      'div',
      null,
      this.renderTable()
    );
  }
});

exports.default = Order_done;

},{"../auth":25,"amazeui-react":52,"react":356}],23:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _amazeuiReact = require('amazeui-react');

var _auth = require('../auth');

var _auth2 = _interopRequireDefault(_auth);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Order_done = _react2.default.createClass({
  displayName: 'Order_done',
  getInitialState: function getInitialState() {
    return {
      dataSource: []
    };
  },
  componentWillMount: function componentWillMount() {
    var _this = this;

    var usernName = _auth2.default.getUsername();
    console.log(usernName);
    _auth2.default.myact({ to: 'queryOrder.do',
      parms: [{ 'key': 'year', 'value': '2016' }, { 'key': 'pay', 'value': '0' }, { 'key': 'username', value: usernName }]
    }, function (res) {
      _this.updateDataSource(res);
    });
  },
  updateDataSource: function updateDataSource(data) {
    this.setState({
      dataSource: data
    });
  },
  dataOption: function dataOption(id) {
    var _this2 = this;

    var year = id.refs.year.value;
    var usernName = _auth2.default.getUsername();
    console.log("好的");
    console.log(year);
    _auth2.default.myact({
      to: 'queryOrder.do',
      parms: [{ 'key': 'year', 'value': year }, { 'key': 'pay', 'value': '1' }, { 'key': 'username', value: usernName }]
    }, function (res) {
      _this2.updateDataSource(res);
    });
  },
  renderTable: function renderTable() {
    var dataSource = this.state.dataSource;

    console.log(dataSource);
    return _react2.default.createElement(
      _amazeuiReact.Table,
      { bordered: true, striped: true, hover: true, radius: true, responsive: true },
      _react2.default.createElement(
        'thead',
        null,
        _react2.default.createElement(
          'tr',
          null,
          _react2.default.createElement(
            'th',
            null,
            _react2.default.createElement(
              'select',
              { ref: 'year', onChange: this.dataOption.bind(this, this) },
              _react2.default.createElement(
                'option',
                { value: '2016' },
                '2016年'
              ),
              _react2.default.createElement(
                'option',
                { value: '2015' },
                '2015年'
              ),
              _react2.default.createElement(
                'option',
                { value: '2014' },
                '2014年'
              )
            )
          ),
          _react2.default.createElement(
            'th',
            null,
            '订单详情'
          ),
          _react2.default.createElement(
            'th',
            null,
            '患者姓名'
          ),
          _react2.default.createElement(
            'th',
            null,
            '金额'
          ),
          _react2.default.createElement(
            'th',
            null,
            '状态'
          ),
          _react2.default.createElement(
            'th',
            null,
            '操作'
          )
        )
      ),
      _react2.default.createElement(
        'tbody',
        null,
        dataSource.map(function (item, index) {
          return _react2.default.createElement(
            'tr',
            { key: index },
            _react2.default.createElement(
              'td',
              null,
              item.orderDate
            ),
            _react2.default.createElement(
              'td',
              null,
              item.patientProvince,
              item.patientDiagnose
            ),
            _react2.default.createElement(
              'td',
              null,
              item.patientRealname
            ),
            _react2.default.createElement(
              'td',
              null,
              item.orderPrice
            ),
            _react2.default.createElement(
              'td',
              null,
              item.orderPay
            ),
            _react2.default.createElement(
              'td',
              null,
              item.orderReserveTime
            )
          );
        })
      )
    );
  },
  render: function render() {
    return _react2.default.createElement(
      'div',
      null,
      this.renderTable()
    );
  }
});

exports.default = Order_done;

},{"../auth":25,"amazeui-react":52,"react":356}],24:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Order_remain = exports.Order_finished = exports.Order_all = undefined;

var _Order_all2 = require('./Order_all');

var _Order_all3 = _interopRequireDefault(_Order_all2);

var _Order_finished2 = require('./Order_finished');

var _Order_finished3 = _interopRequireDefault(_Order_finished2);

var _Order_remain2 = require('./Order_remain');

var _Order_remain3 = _interopRequireDefault(_Order_remain2);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.Order_all = _Order_all3.default; //Order存放了订单相关的三个页面

exports.Order_finished = _Order_finished3.default;
exports.Order_remain = _Order_remain3.default;

},{"./Order_all":21,"./Order_finished":22,"./Order_remain":23}],25:[function(require,module,exports){
'use strict';

var fetch_url = 'http://123.56.133.208:8080/myfd/';
//var fetch_url = 'http://10.3.142.54:8080/myfd/'
module.exports = {
  get_config: function get_config() {
    return {
      'about_us_url': 'about_us',
      'connect_us_url': 'connect_us'
    };
  },
  user_login: function user_login(email, pass, cb) {
    var _this = this;

    cb = arguments[arguments.length - 1];
    if (localStorage.token) {
      if (cb) cb(true);
      this.onChange(true);
      return;
    }
    pretendRequest(email, pass, function (res) {
      if (res.authenticated) {
        localStorage.role = res.role;
        localStorage.token = res.token;
        if (cb) cb(true);
        _this.onChange(true);
      } else {
        if (cb) cb(false);
        _this.onChange(false);
      }
    });
  },
  getToken: function getToken() {
    return localStorage.token;
  },
  getUsername: function getUsername() {
    return localStorage.username;
  },
  custom_login: function custom_login(email, pass, role, cb) {
    var _this2 = this;

    cb = arguments[arguments.length - 1];
    if (localStorage.token) {
      if (cb) cb(true);
      this.onChange(true);
      return;
    }
    pretendRequest1(email, pass, function (res) {
      console.log(res);
      if (res.authenticated) {
        localStorage.role = res.role;
        localStorage.token = res.token;
        if (cb) cb(true);
        _this2.onChange(true);
      } else {
        if (cb) cb(false);
        _this2.onChange(false);
      }
    });
  },
  getRole: function getRole() {
    return localStorage.role == undefined ? '0' : localStorage.role;
  },
  logout: function logout(cb) {
    delete localStorage.token;
    delete localStorage.username;
    delete localStorage.role;
    console.log(localStorage);
    if (cb) cb();
    this.onChange(false);
  },
  loggedIn: function loggedIn() {
    return !!localStorage.token;
  },
  onChange: function onChange() {},
  myact: function myact(val, cb) {
    var to = val['to'];
    var parms = val['parms'];
    var url = fetch_url + to + '?';
    parms.map(function (parm) {
      if (String(parm.value).length > 0) {
        url += parm.key + '=' + parm.value + '&';
      }
    });
    if (to != 'regist.do') {
      url = url + '&user_name=' + this.getUsername();
    }
    fetch(url).then(checkStatus).then(function (res) {
      return res.json();
    }).then(function (data) {
      cb(data);
    }).catch(function (error) {
      console.log('Request failed: ', error);
    });
  },

  //get('',{"role":'333'},function(res){
  //            console.log(res)
  //        }) 
  get: function get(apipath, data, cb) {
    var url = fetch_url;
    url = url + apipath + '?';
    var _iteratorNormalCompletion = true;
    var _didIteratorError = false;
    var _iteratorError = undefined;

    try {
      for (var _iterator = Object.keys(data)[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
        var k = _step.value;

        url = url + k + '=' + data[k] + '&';
      }
    } catch (err) {
      _didIteratorError = true;
      _iteratorError = err;
    } finally {
      try {
        if (!_iteratorNormalCompletion && _iterator.return) {
          _iterator.return();
        }
      } finally {
        if (_didIteratorError) {
          throw _iteratorError;
        }
      }
    }

    url = url + '&user_name=' + this.getUsername();
    fetch(url, {
      mode: 'cors'
    }).then(checkStatus).then(function (res) {
      return res.json();
    }).then(function (data) {
      cb(data);
    }).catch(function (error) {
      console.log('Request failed: ', error);
    });
  },
  post: function post(apipath, form, cb) {
    //var f = document.getElementById('myform');
    //var form = new FormData(f)
    //for (let k of Object.keys(data)){
    //    form.append(k,data[k])
    //}
    //form.append("userfile", fileInputElement.files[0]);
    if (form.get("user_name" == null)) {
      form.append('user_name', this.getUsername());
    }
    var url = fetch_url;
    url = url + apipath;
    fetch(url, {
      method: 'POST',
      body: form,
      mode: 'cors'
    }).then(checkStatus).then(function (res) {
      return res.json();
    }).then(function (data) {
      cb(data);
    }).catch(function (error) {
      console.log('Request failed: ', error);
    });
  }
};
function checkStatus(response) {
  if (response.status >= 200 && response.status < 300) {
    return response;
  } else {
    var error = new Error(response.statusText);
    error.response = response;
    throw error;
  }
}
function pretendRequest(email, pass, cb) {
  localStorage.username = email;
  fetch(fetch_url + 'login.do?username' + '=' + email + '&' + 'password' + '=' + pass + '&' + 'role' + '=1').then(checkStatus).then(function (res) {
    return res.json();
  }).then(function (data) {
    console.log(data);
    if (data['result'] == 'true') {
      cb({
        role: data.role,
        authenticated: true,
        token: Math.random().toString(36).substring(7)
      });
    } else {
      cb({ authenticated: false });
      alert(data['result']);
    }
  }).catch(function (error) {
    console.log('Request failed: ', error);
  });
}
function pretendRequest1(email, pass, cb) {
  localStorage.username = email;
  fetch(fetch_url + 'adminLogin.do?username' + '=' + email + '&' + 'password' + '=' + pass + '&' + 'role' + '=5').then(checkStatus).then(function (res) {
    return res.json();
  }).then(function (data) {
    console.log(data);
    if (data['result'] == 'true') {
      cb({
        role: data.role,
        authenticated: true,
        token: Math.random().toString(36).substring(7)
      });
    } else {
      cb({ authenticated: false });
      alert(data['result']);
    }
  }).catch(function (error) {
    console.log('Request failed: ', error);
  });
}

},{}],26:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.UserManage = exports.User_index = exports.Order = exports.Sidebar = exports.PageContainer = exports.NoMessage = exports.Message = exports.Header = undefined;

var _Header2 = require('./Header');

var _Header3 = _interopRequireDefault(_Header2);

var _Message2 = require('./Message');

var _Message3 = _interopRequireDefault(_Message2);

var _NoMessage2 = require('./NoMessage');

var _NoMessage3 = _interopRequireDefault(_NoMessage2);

var _PageContainer2 = require('./PageContainer');

var _PageContainer3 = _interopRequireDefault(_PageContainer2);

var _Sidebar2 = require('./Sidebar');

var _Sidebar3 = _interopRequireDefault(_Sidebar2);

var _Order2 = require('./Order');

var _Order3 = _interopRequireDefault(_Order2);

var _User_index2 = require('./User_index');

var _User_index3 = _interopRequireDefault(_User_index2);

var _UserManage2 = require('./ManageRoot/UserManage');

var _UserManage3 = _interopRequireDefault(_UserManage2);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.Header = _Header3.default;
exports.Message = _Message3.default;
exports.NoMessage = _NoMessage3.default;
exports.PageContainer = _PageContainer3.default;
exports.Sidebar = _Sidebar3.default;
exports.Order = _Order3.default;
exports.User_index = _User_index3.default;
exports.UserManage = _UserManage3.default;

},{"./Header":7,"./ManageRoot/UserManage":10,"./Message":12,"./NoMessage":14,"./Order":18,"./PageContainer":19,"./Sidebar":20,"./User_index":24}],27:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _amazeuiReact = require('amazeui-react');

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

var _auth = require('../components/auth');

var _auth2 = _interopRequireDefault(_auth);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var UserManage = _react2.default.createClass({
    displayName: 'UserManage',
    getInitialState: function getInitialState() {
        return {
            dataSource: { re: [{ "user_name": "ffff", "user_role": "iii", "user_mobile": "111", "user_last_login": "qqq", "state": "0" }], totalpage: 1 },
            nowpage: '1',
            totalpage: '1'
        };
    },
    query: function query() {
        var _this = this;

        var page = arguments.length <= 0 || arguments[0] === undefined ? this.state.nowpage : arguments[0];

        _auth2.default.myact({ to: 'manager/userlist.do',
            parms: [{ 'key': 'page', 'value': page }]
        }, function (res) {
            _this.updateDataSource(res);
        });
        this.setState({ nowpage: page });
    },
    querynext: function querynext() {
        var nowpage = this.state.nowpage;
        var totalpage = this.state.totalpage;
        if (nowpage + 1 <= totalpage) {
            nowpage += 1;
            this.query(nowpage);
        }
    },
    querybefore: function querybefore() {

        var nowpage = this.state.nowpage;
        var totalpage = this.state.totalpage;
        if (nowpage - 1 >= 1) {
            nowpage -= 1;
            this.query(nowpage);
        }
    },
    deletelist: function deletelist(id) {
        console.log(id);
        _auth2.default.myact({ to: 'manager/deleteuser.do',
            parms: [{ 'key': 'id', 'value': id }]
        }, function (res) {
            console.log(res);
        });
        this.query();
    },
    disable: function disable(id) {
        console.log(id);
        _auth2.default.myact({ to: 'manager/disableuser.do',
            parms: [{ 'key': 'id', 'value': id[0] }, { 'key': 'state', 'value': id[1] }]
        }, function (res) {
            console.log(res);
        });
        this.query();
    },
    componentDidMount: function componentDidMount() {
        this.query(1);
    },
    updateDataSource: function updateDataSource(data) {
        this.setState({
            dataSource: data,
            totalpage: data.totalpage
        });
        console.log(data);
    },
    judege: function judege(intState) {
        var state = "";
        if (intState == 0) {
            state = "禁用";
        } else if (intState == 1) {
            state = "启用";
        } else {
            state = "";
        }
        return state;
    },
    judege1: function judege1(intState) {
        var state = "";
        if (intState == 0) {
            state = "启用";
        } else if (intState == 1) {
            state = "禁用";
        } else {
            state = "";
        }
        return state;
    },
    kaifazhong: function kaifazhong(str) {
        console.log(str);
    },
    change: function change(id) {
        console.log(id);
        _auth2.default.myact({ to: '/manager/changeuser.do',
            parms: [{ 'key': 'id', 'value': id }, { key: 'name', value: this.refs.un.value }, { key: 'mobile', value: this.refs.um.value }, { key: 'role', value: this.refs.ur.value }, { key: 'state', value: this.refs.ps.value }]
        }, function (res) {
            Console.log(name);
        });
        this.query();
    },
    renderTable: function renderTable() {
        var _this2 = this;

        var data = this.state.dataSource;
        console.log(data.re.length);
        return _react2.default.createElement(
            _amazeuiReact.Table,
            { bordered: true, striped: true, hover: true, radius: true, responsive: true },
            _react2.default.createElement(
                'thead',
                null,
                _react2.default.createElement(
                    'tr',
                    null,
                    _react2.default.createElement(
                        'th',
                        null,
                        '用户名'
                    ),
                    _react2.default.createElement(
                        'th',
                        null,
                        '账户类型 '
                    ),
                    _react2.default.createElement(
                        'th',
                        null,
                        '电话'
                    ),
                    _react2.default.createElement(
                        'th',
                        null,
                        '状态'
                    ),
                    _react2.default.createElement(
                        'th',
                        null,
                        '上次登录时间'
                    ),
                    _react2.default.createElement(
                        'th',
                        null,
                        '操作'
                    )
                )
            ),
            _react2.default.createElement(
                'tbody',
                null,
                data.re.map(function (item, index) {
                    return _react2.default.createElement(
                        'tr',
                        { key: index },
                        _react2.default.createElement(
                            'td',
                            null,
                            item.user_name
                        ),
                        _react2.default.createElement(
                            'td',
                            null,
                            item.user_role
                        ),
                        _react2.default.createElement(
                            'td',
                            null,
                            item.user_mobile
                        ),
                        _react2.default.createElement(
                            'td',
                            null,
                            _this2.judege(item.state)
                        ),
                        _react2.default.createElement(
                            'td',
                            null,
                            item.user_last_login
                        ),
                        _react2.default.createElement(
                            'td',
                            null,
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    { onClick: _this2.deletelist.bind(_this2, item.id) },
                                    '删除'
                                ),
                                '  /',
                                _react2.default.createElement(
                                    'span',
                                    { onClick: _this2.disable.bind(_this2, [item.id, item.state]) },
                                    _this2.judege1(item.state)
                                ),
                                '  /',
                                _react2.default.createElement(
                                    'span',
                                    { onClick: _this2.change.bind(_this2, item.id) },
                                    '修改'
                                )
                            )
                        )
                    );
                })
            )
        );
    },
    render: function render() {
        return _react2.default.createElement(
            _PageContainer2.default,
            null,
            _react2.default.createElement(
                'div',
                null,
                this.renderTable()
            ),
            _react2.default.createElement(
                'div',
                { id: 'u48', 'class': 'text' },
                _react2.default.createElement(
                    'p',
                    { onClick: this.querybefore },
                    _react2.default.createElement(
                        'span',
                        null,
                        '上一页'
                    ),
                    '        ',
                    _react2.default.createElement(
                        'span',
                        { onClick: this.querynext },
                        _react2.default.createElement(
                            'span',
                            null,
                            '下一页'
                        )
                    )
                ),
                _react2.default.createElement('div', { id: 'ubu', 'class': 'ax_html_button' })
            )
        );
    }
});
exports.default = UserManage;

},{"../components/PageContainer":19,"../components/auth":25,"amazeui-react":52,"react":356}],28:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

var _reactDom = require('react-dom');

var _amazeuiReact = require('amazeui-react');

var _auth = require('../components/auth');

var _auth2 = _interopRequireDefault(_auth);

var _reactRouter = require('react-router');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var path = "i/companyLogo.png";
var iconUser = _react2.default.createElement(_amazeuiReact.Icon, { icon: 'user' });
var iconPwd = _react2.default.createElement(_amazeuiReact.Icon, { icon: 'lock' });

var About_us = (0, _reactRouter.withRouter)(_react2.default.createClass({
  displayName: 'About_us',
  render: function render() {
    return _react2.default.createElement(
      'div',
      null,
      _react2.default.createElement(
        'div',
        { id: 'base', 'class': '' },
        _react2.default.createElement('link', { href: 'i/resources/css/axure_rp_page.css', type: 'text/css', rel: 'stylesheet' }),
        _react2.default.createElement('link', { href: 'i/data/styles.css', type: 'text/css', rel: 'stylesheet' }),
        _react2.default.createElement('link', { href: 'i/css/user_login_page/styles.css', type: 'text/css', rel: 'stylesheet' }),
        _react2.default.createElement(
          'form',
          { onSubmit: this.handleSubmit },
          _react2.default.createElement(
            'div',
            { id: 'u0', 'class': 'ax_shape' },
            _react2.default.createElement('img', { id: 'u0_img', 'class': 'img ', src: 'i/images/about_us/about.png' }),
            _react2.default.createElement(
              'div',
              { id: 'u1', 'class': 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement('span', null)
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { id: 'u18', 'class': 'ax_paragraph' },
            _react2.default.createElement('img', { id: 'u18_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
            _react2.default.createElement(
              'div',
              { id: 'u19', 'class': 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement('span', null)
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { id: 'u20', 'class': 'ax_paragraph' },
            _react2.default.createElement('img', { id: 'u20_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
            _react2.default.createElement(
              'div',
              { id: 'u21', 'class': 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement('span', null)
              )
            )
          ),
          _react2.default.createElement(
            _reactRouter.Link,
            { to: '/index_home' },
            _react2.default.createElement(
              'div',
              { id: 'u24', 'class': 'ax_shape' },
              _react2.default.createElement('img', { id: 'u24_img', 'class': 'img ', src: 'i/images/user_check_operation_reservation_page/u23.png' }),
              _react2.default.createElement(
                'div',
                { id: 'u25', 'class': 'text' },
                _react2.default.createElement(
                  'p',
                  null,
                  _react2.default.createElement('span', null)
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u170', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u170_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u171', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                _react2.default.createElement(
                  _reactRouter.Link,
                  { to: _auth2.default.get_config()['about_us_url'] },
                  '关于我们'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u172', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u172_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u173', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                _react2.default.createElement(
                  _reactRouter.Link,
                  { to: _auth2.default.get_config()['connect_us_url'] },
                  '联系我们'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u174', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u174_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u175', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '服务条款'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u888', className: 'ax_paragraph', style: {
              'position': 'absolute',
              'left': '450px',
              'top': '530px',
              'width': '200px',
              'height': '1px',
              'font-size': '14px',
              'color': '#666666'
            } },
          _react2.default.createElement('img', { id: 'u125_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u126', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                _react2.default.createElement(
                  'a',
                  { href: 'http://www.miitbeian.gov.cn' },
                  '           京ICP备案16059247号'
                )
              )
            ),
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                _react2.default.createElement('img', { src: 'http://www.beian.gov.cn/img/ghs.png' }),
                _react2.default.createElement(
                  'a',
                  { href: 'http://www.beian.gov.cn/portal/registerSystemInfo?recordcode=11010802022799' },
                  '京公网安备 11010802022799号'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u176', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u176_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u177', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '媒体更新'
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { id: 'u194', 'class': 'ax_html_button' },
            _react2.default.createElement('input', null)
          )
        )
      )
    );
  }
}));
exports.default = About_us;

},{"../components/PageContainer":19,"../components/auth":25,"amazeui-react":52,"react":356,"react-dom":189,"react-router":219}],29:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

require('amazeui-react');

var _auth = require('../components/auth');

var _auth2 = _interopRequireDefault(_auth);

var _reactRouter = require('react-router');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Account_manage = _react2.default.createClass({
    displayName: 'Account_manage',
    getInitialState: function getInitialState() {
        return {
            dataSource: [{
                agencydoctorid: "无",
                agencyprovince: "无",
                agencydistrict: "无",
                agencyname: "无",
                agencybed: "无",
                agencydiscount: "无",
                agencyhot: "无",
                agencyyytime: "无",
                agencyclass: "无"
            }, {
                agencydoctorid: "无",
                agencyprovince: "无",
                agencydistrict: "无",
                agencyname: "无",
                agencybed: "无",
                agencydiscount: "无",
                agencyhot: "无",
                agencyyytime: "无",
                agencyclass: "无"
            }, {
                agencydoctorid: "无",
                agencyprovince: "无",
                agencydistrict: "无",
                agencyname: "无",
                agencybed: "无",
                agencydiscount: "无",
                agencyhot: "无",
                agencyyytime: "无",
                agencyclass: "无"
            }],
            nowpage: 1,
            totalpage: 2,
            now: '' };
    },
    query: function query(page) {
        var _this = this;

        _auth2.default.myact({ to: 'manager/pricelist.do',
            parms: [{ 'key': 'page', 'value': page }]
        }, function (res) {
            _this.updateDataSource(res);
        });
        this.setState({ nowpage: page });
    },
    querynext: function querynext() {
        var nowpage = this.state.nowpage;
        var totalpage = this.state.totalpage;
        if (nowpage + 1 <= totalpage) {
            nowpage += 1;
            this.query(nowpage);
        }
    },
    querybefore: function querybefore() {

        var nowpage = this.state.nowpage;
        var totalpage = this.state.totalpage;
        if (nowpage - 1 >= 1) {
            nowpage -= 1;
            this.query(nowpage);
        }
    },
    componentDidMount: function componentDidMount() {
        this.query(1);
    },
    updateDataSource: function updateDataSource(data) {
        this.setState({
            dataSource: data
        });
        console.log(data);
    },
    kaifazhong: function kaifazhong(str) {
        console.log(str);
    },
    render: function render() {
        var _this2 = this;

        return _react2.default.createElement(
            _PageContainer2.default,
            null,
            _react2.default.createElement('link', { href: 'i/resources/css/axure_rp_page.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('link', { href: 'i/data/styles.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('link', { href: 'i/css/web_manage_page_root/styles.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement(
                'div',
                { id: 'base', 'class': '' },
                _react2.default.createElement(
                    'div',
                    { id: 'u0', 'class': 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u0_start', 'class': 'img ', src: 'i/resources/images/transparent.gif', alt: 'u0_start' }),
                    _react2.default.createElement('img', { id: 'u0_end', 'class': 'img ', src: 'i/resources/images/transparent.gif', alt: 'u0_end' }),
                    _react2.default.createElement('img', { id: 'u0_line', 'class': 'img ', src: 'i/images/web_manage_page_root/u0_line.png', alt: 'u0_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u1', 'class': 'ax_h1' },
                    _react2.default.createElement('img', { id: 'u1_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u2', 'class': 'text' },
                        _react2.default.createElement(
                            'strong',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '谷雨医生平台系统管理'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u3', 'class': 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u3_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u4', 'class': 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                { className: 'u209' },
                                ' '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u209' },
                                '    '
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u5', 'class': 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u5_start', 'class': 'img ', src: 'i/resources/images/transparent.gif', alt: 'u5_start' }),
                    _react2.default.createElement('img', { id: 'u5_end', 'class': 'img ', src: 'i/resources/images/transparent.gif', alt: 'u5_end' }),
                    _react2.default.createElement('img', { id: 'u5_line', 'class': 'img ', src: 'i/images/web_manage_page_root/u0_line.png', alt: 'u5_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u6', 'class': 'ax_dynamic_panel', 'data-label': '系统管理' },
                    _react2.default.createElement(
                        'div',
                        { id: 'u6_state0', 'class': 'panel_state', 'data-label': '管理账号' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u6_state0_content', 'class': 'panel_state_content' },
                            _react2.default.createElement(
                                'div',
                                { id: 'u7', 'class': 'ax_table' },
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u8', 'class': 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u8_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u9', 'class': 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '用户名'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u10', 'class': 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u10_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u11', 'class': 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '所属组'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u12', 'class': 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u12_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u13', 'class': 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '电话'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u14', 'class': 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u14_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u15', 'class': 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '电子邮件'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u16', 'class': 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u16_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u17', 'class': 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '状态'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u18', 'class': 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u18_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u18.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u19', 'class': 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '操作'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u20', 'class': 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u20_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u21', 'class': 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '张三'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u22', 'class': 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u22_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u23', 'class': 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '手术助理-骨科'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u24', 'class': 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u24_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u25', 'class': 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '13912345678'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u26', 'class': 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u26_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u27', 'class': 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                'ZhangSan @FD.com'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u28', 'class': 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u28_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u29', 'class': 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '启用'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u30', 'class': 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u30_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u30.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u31', 'class': 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '删除/禁用/修改'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u32', 'class': 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u32_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u32.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u33', 'class': 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '李四'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u34', 'class': 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u34_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u32.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u35', 'class': 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '手术助理-骨科'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u36', 'class': 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u36_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u32.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u37', 'class': 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '18812345678'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u38', 'class': 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u38_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u32.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u39', 'class': 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u40', 'class': 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u40_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u32.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u41', 'class': 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '启用'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u42', 'class': 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u42_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u42.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u43', 'class': 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '删除/禁用/修改'
                                            )
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u44', 'class': 'ax_horizontal_line' },
                                _react2.default.createElement('img', { id: 'u44_start', 'class': 'img ', src: 'i/resources/images/transparent.gif', alt: 'u44_start' }),
                                _react2.default.createElement('img', { id: 'u44_end', 'class': 'img ', src: 'i/resources/images/transparent.gif', alt: 'u44_end' }),
                                _react2.default.createElement('img', { id: 'u44_line', 'class': 'img ', src: 'i/images/web_manage_page_root/u0_line.png', alt: 'u44_line' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u45', 'class': 'ax_h4' },
                                _react2.default.createElement('img', { id: 'u45_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u46', 'class': 'text' },
                                    _react2.default.createElement(
                                        'strong',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '所有成员'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u47', 'class': 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u47_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u48', 'class': 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '首页 上一页 '
                                        ),
                                        _react2.default.createElement(
                                            'span',
                                            { className: 'u211' },
                                            '1'
                                        ),
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            ' 2 下一页 尾页'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u49', 'class': 'ax_h4' },
                                _react2.default.createElement('img', { id: 'u49_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u50', 'class': 'text' },
                                    _react2.default.createElement(
                                        'strong',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '增加成员'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u51', 'class': 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u51_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u52', 'class': 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '账号'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u53', 'class': 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u53_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u54', 'class': 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '密码'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u55', 'class': 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u55_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u56', 'class': 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '确认密码'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u57', 'class': 'ax_droplist' },
                                _react2.default.createElement(
                                    'select',
                                    { id: 'u57_input' },
                                    _react2.default.createElement(
                                        'option',
                                        { selected: true, value: '手术助理-骨科' },
                                        '手术助理-骨科'
                                    ),
                                    _react2.default.createElement(
                                        'option',
                                        { value: '客服' },
                                        '客服'
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u58', 'class': 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u58_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u59', 'class': 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '所属组'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u60', 'class': 'ax_text_field' },
                                _react2.default.createElement('input', { id: 'u60_input', type: 'text', value: '' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u61', 'class': 'ax_text_field' },
                                _react2.default.createElement('input', { id: 'u61_input', type: 'text', value: '' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u62', 'class': 'ax_text_field' },
                                _react2.default.createElement('input', { id: 'u62_input', type: 'text', value: '' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u63', 'class': 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u63_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u64', 'class': 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '姓名'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u65', 'class': 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u65_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u66', 'class': 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '电话'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u67', 'class': 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u67_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u68', 'class': 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '电子邮件'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u69', 'class': 'ax_text_field' },
                                _react2.default.createElement('input', { id: 'u69_input', type: 'text', value: '' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u70', 'class': 'ax_text_field' },
                                _react2.default.createElement('input', { id: 'u70_input', type: 'text', value: '' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u71', 'class': 'ax_text_field' },
                                _react2.default.createElement('input', { id: 'u71_input', type: 'text', value: '' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u72', 'class': 'ax_html_button' },
                                _react2.default.createElement('input', { id: 'u72_input', type: 'submit', value: '重置' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u73', 'class': 'ax_html_button' },
                                _react2.default.createElement('input', { id: 'u73_input', type: 'submit', value: '提交' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u209', 'class': 'ax_html_button' },
                                _react2.default.createElement('input', { id: 'u209_input' })
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u189', 'class': 'ax_menu' },
                    _react2.default.createElement('img', { id: 'u189_menu', 'class': 'img ', src: 'i/images/web_manage_page_root/u189_menu.png', alt: 'u189_menu' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u190', 'class': 'ax_table' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u191', 'class': 'ax_table_cell' },
                            _react2.default.createElement('img', { id: 'u191_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u191.png' }),
                            _react2.default.createElement(
                                'div',
                                { id: 'u192', 'class': 'text' },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    _react2.default.createElement(
                                        'span',
                                        null,
                                        '仪表盘'
                                    )
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'div',
                            { id: 'u193', 'class': 'ax_table_cell' },
                            _react2.default.createElement('img', { id: 'u193_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u191_mouseOver.png' }),
                            _react2.default.createElement(
                                'div',
                                { id: 'u194', 'class': 'text', style: { color: 'white' }, onClick: function onClick() {
                                        _this2.setState({ 'now': '管理账号' });
                                    } },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    _react2.default.createElement(
                                        'span',
                                        null,
                                        '管理账号'
                                    )
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'div',
                            { id: 'u195', 'class': 'ax_table_cell' },
                            _react2.default.createElement('img', { id: 'u195_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u191.png' }),
                            _react2.default.createElement(
                                'div',
                                { id: 'u196', 'class': 'text' },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    _react2.default.createElement(
                                        _reactRouter.Link,
                                        { to: '/user_manage' },
                                        _react2.default.createElement(
                                            'span',
                                            { onClick: function onClick() {
                                                    _this2.setState({ 'now': '管理用户' });
                                                } },
                                            '管理用户'
                                        )
                                    )
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'div',
                            { id: 'u197', 'class': 'ax_table_cell' },
                            _react2.default.createElement('img', { id: 'u197_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u191.png' }),
                            _react2.default.createElement(
                                'div',
                                { id: 'u198', 'class': 'text', style: { color: this.state.now == '价格管理' ? 'white' : 'black' } },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    _react2.default.createElement(
                                        _reactRouter.Link,
                                        { to: '/price_manage' },
                                        _react2.default.createElement(
                                            'span',
                                            { onClick: function onClick() {
                                                    _this2.setState({ 'now': '价格管理' });
                                                } },
                                            '价格管理'
                                        )
                                    )
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'div',
                            { id: 'u199', 'class': 'ax_table_cell' },
                            _react2.default.createElement('img', { id: 'u199_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u199.png' }),
                            _react2.default.createElement(
                                'div',
                                { id: 'u200', 'class': 'text' },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    _react2.default.createElement(
                                        'span',
                                        null,
                                        '网站配置'
                                    )
                                )
                            )
                        )
                    ),
                    _react2.default.createElement(
                        'div',
                        { id: 'u201', 'class': 'ax_menu sub_menu' },
                        _react2.default.createElement('img', { id: 'u201_menu', 'class': 'img ', src: 'i/images/web_manage_page_root/u201_menu.png', alt: 'u201_menu' }),
                        _react2.default.createElement(
                            'div',
                            { id: 'u202', 'class': 'ax_table' },
                            _react2.default.createElement(
                                'div',
                                { id: 'u203', 'class': 'ax_table_cell' },
                                _react2.default.createElement('img', { id: 'u203_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u203.png' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u204', 'class': 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '求医用户'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u205', 'class': 'ax_table_cell' },
                                _react2.default.createElement('img', { id: 'u205_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u203.png' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u206', 'class': 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '医生'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u207', 'class': 'ax_table_cell' },
                                _react2.default.createElement('img', { id: 'u207_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u207.png' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u208', 'class': 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '机构'
                                        )
                                    )
                                )
                            )
                        )
                    )
                )
            )
        );
    }
});

exports.default = Account_manage;

},{"../components/PageContainer":19,"../components/auth":25,"amazeui-react":52,"react":356,"react-router":219}],30:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

var _reactDom = require('react-dom');

var _amazeuiReact = require('amazeui-react');

var _auth = require('../components/auth');

var _auth2 = _interopRequireDefault(_auth);

var _reactRouter = require('react-router');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var AdminLogin = (0, _reactRouter.withRouter)(_react2.default.createClass({
  displayName: 'AdminLogin',
  getInitialState: function getInitialState() {
    return {
      error: false
    };
  },

  handleSubmit: function handleSubmit(e) {
    var _this = this;

    e.preventDefault();
    var username = document.getElementById('u2_input').value;
    var password = document.getElementById('u3_input').value;
    if (!username || !password) {
      alert("账户或密码不可以为空");
    } else {
      _auth2.default.logout();
      _auth2.default.custom_login(username, password, '5', function (loggedIn) {
        if (!loggedIn) {
          return _this.setState({ error: true });
        }
        var location = _this.props.location;

        var role = _auth2.default.getRole();
        if (location.state && location.state.nextPathname) {
          _this.props.router.replace(location.state.nextPathname);
        } else {
          _this.props.router.replace('/user_manage');
        }
      });
    }
  },
  render: function render() {
    return _react2.default.createElement(
      'div',
      { id: 'base', 'class': '' },
      _react2.default.createElement('link', { href: 'i/resources/css/axure_rp_page.css', type: 'text/css', rel: 'stylesheet' }),
      _react2.default.createElement('link', { href: 'i/css/system_manage_login_page/styles.css', type: 'text/css', rel: 'stylesheet' }),
      _react2.default.createElement(
        'div',
        { id: 'u0', 'class': 'ax_shape' },
        _react2.default.createElement('img', { id: 'u0_img', 'class': 'img ', src: 'i/images/system_manage_login_page/u0.png' }),
        _react2.default.createElement(
          'div',
          { id: 'u1', 'class': 'text' },
          _react2.default.createElement(
            'p',
            null,
            _react2.default.createElement('span', null)
          )
        )
      ),
      _react2.default.createElement(
        _amazeuiReact.Form,
        { className: 'commentForm', onSubmit: this.handleSubmit },
        _react2.default.createElement(
          'div',
          { id: 'u2', className: 'ax_text_field', 'data-label': 'AdminUserName' },
          _react2.default.createElement(_amazeuiReact.Input, { id: 'u2_input', type: 'text', ref: 'username' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u3', className: 'ax_text_field' },
          _react2.default.createElement(_amazeuiReact.Input, { id: 'u3_input', type: 'password', ref: 'password' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u4', className: 'ax_h1' },
          _react2.default.createElement('img', { id: 'u4_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u5', 'class': 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '管理员登录'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u6', 'class': 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u6_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u7', 'class': 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '用户名'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u8', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u8_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u9', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '密码'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u10', className: 'ax_html_button' },
          _react2.default.createElement('input', { id: 'u10_input', type: 'submit', value: '提交' })
        )
      )
    );
  }
}));
exports.default = AdminLogin;

},{"../components/PageContainer":19,"../components/auth":25,"amazeui-react":52,"react":356,"react-dom":189,"react-router":219}],31:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

var _auth = require('../components/auth');

var _auth2 = _interopRequireDefault(_auth);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var CheckReservation = _react2.default.createClass({
    displayName: 'CheckReservation',
    getInitialState: function getInitialState() {
        return {
            dataSource: [{
                PatientName: "郑国伟", //患者名字
                Date: "5", //几天内
                DueDay: "2016年5月20日前", //具体日期
                DoctorName: "XXX", //医生名字
                InstitutionType: "团队", //主任 or 主任团队 or 医院
                OperationFor: "脑残", //治疗
                Disctict: "北京", //预约地点
                Information: "已提交", //确诊信息是否提交
                CorrespondingInformation: "已提交", //相关信息
                ContactName: "郑国伟", //联系人姓名
                ContactNumber: "13812345678", //联系人电话
                id: '',
                Cost: {
                    OperationDiscount: "0", //
                    Discount: "0", //
                    TotalFee: "888", //
                    basicPrice: ''
                } //费用明细
            }]
        };
    },
    updateDataSource: function updateDataSource(data) {
        this.setState({
            dataSource: data
        });
    },
    componentWillMount: function componentWillMount() {
        var par = this.props.query;
        console.log(par);
        if (par.xingming != undefined) {
            this.state.dataSource[0].PatientName = par.xingming;
            this.state.dataSource[0].DueDay = par.yuyueshijian;
            this.state.dataSource[0].DoctorName = par.zhuanjia;
            this.state.dataSource[0].Disctict = par.didian;
            this.state.dataSource[0].OperationFor = par.shoushuneirong;
            this.state.dataSource[0].ContactName = par.lianxirenxingming;
            this.state.dataSource[0].ContactNumber = par.shouji;
            this.state.dataSource[0].Cost.TotalFee = par.totalFee;
            this.state.dataSource[0].Cost.basicPrice = par.basicPrice;
            this.state.dataSource[0].id = par.id;
        }
    },
    render: function render() {
        var _this = this;

        var info = {};
        var data = this.state.dataSource;
        if (data.length >= 1) {
            info = data[0];
        }
        console.log(info);
        return _react2.default.createElement(
            _PageContainer2.default,
            null,
            _react2.default.createElement(
                'div',
                { id: 'base', className: '' },
                _react2.default.createElement('link', { href: 'i/resources/css/axure_rp_page.css', type: 'text/css', rel: 'stylesheet' }),
                _react2.default.createElement('link', { href: 'i/data/styles.css', type: 'text/css', rel: 'stylesheet' }),
                _react2.default.createElement('link', { href: 'i/css/user_check_operation_reservation_page/styles.css', type: 'text/css', rel: 'stylesheet' }),
                _react2.default.createElement('script', { type: 'text/babel' }),
                _react2.default.createElement(
                    'div',
                    { id: 'u0', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u0_img', 'class': 'img ' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'service', className: 'u101' },
                        '服务内容: 预约(',
                        _react2.default.createElement(
                            'span',
                            { className: 'content' },
                            info.DueDay
                        ),
                        ') ',
                        _react2.default.createElement(
                            'span',
                            { className: 'content' },
                            info.DoctorName
                        ),
                        _react2.default.createElement(
                            'span',
                            { className: 'content' },
                            info.InstitutionType
                        ),
                        '对',
                        _react2.default.createElement(
                            'span',
                            { className: 'content' },
                            info.PatientName
                        ),
                        '进行',
                        _react2.default.createElement(
                            'span',
                            { className: 'content' },
                            info.OperationFor
                        ),
                        '方面手术',
                        _react2.default.createElement('br', null),
                        '地点: ',
                        _react2.default.createElement(
                            'span',
                            { className: 'content' },
                            info.Disctict
                        ),
                        _react2.default.createElement('br', null),
                        '确诊信息: ',
                        _react2.default.createElement(
                            'span',
                            { className: 'content' },
                            info.Information
                        ),
                        _react2.default.createElement('br', null),
                        _react2.default.createElement('br', null),
                        'CT及相关影像信息：',
                        _react2.default.createElement(
                            'span',
                            { className: 'content' },
                            info.CorrespondingInformation
                        ),
                        _react2.default.createElement('br', null),
                        _react2.default.createElement('br', null),
                        _react2.default.createElement('br', null),
                        '联系人：',
                        _react2.default.createElement(
                            'span',
                            { className: 'content' },
                            info.ContactName
                        ),
                        _react2.default.createElement('br', null),
                        ' 手机：',
                        _react2.default.createElement(
                            'span',
                            { className: 'content' },
                            info.ContactNumber
                        ),
                        _react2.default.createElement('br', null),
                        _react2.default.createElement('br', null),
                        _react2.default.createElement('br', null),
                        _react2.default.createElement('br', null),
                        _react2.default.createElement('br', null)
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u2', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u2_img', className: 'img ' }),
                    _react2.default.createElement(
                        'div',
                        null,
                        _react2.default.createElement(
                            'div',
                            { id: 'buy' },
                            '已购服务信息：',
                            _react2.default.createElement('br', null),
                            '基础信息预审及专家预约     ',
                            _react2.default.createElement(
                                'span',
                                null,
                                info.Cost.basicPrice
                            ),
                            _react2.default.createElement('br', null),
                            '医院床位优惠包                     ',
                            _react2.default.createElement(
                                'span',
                                null,
                                info.Cost.TotalFee - info.Cost.basicPrice
                            ),
                            _react2.default.createElement('hr', null),
                            '优惠                                          ',
                            _react2.default.createElement(
                                'span',
                                null,
                                '0'
                            ),
                            _react2.default.createElement('br', null),
                            '应付总计                                  ',
                            _react2.default.createElement(
                                'span',
                                null,
                                info.Cost.TotalFee
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u4', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u4_img', className: 'img ', src: 'i/images/user_check_operation_reservation_page/u4.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u5', className: 'pay' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '  '
                            ),
                            _react2.default.createElement('span', null)
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '  '
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '  '
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '  '
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '  '
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '  '
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u6', className: 'ax_html_button' },
                    _react2.default.createElement('input', { id: 'u6_input', type: 'submit', value: '在线支付' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u7', className: 'ax_html_button' },
                    _react2.default.createElement('input', { id: 'u7_input', type: 'submit', value: '汇款' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u8', className: 'ax_html_button' },
                    _react2.default.createElement('input', { id: 'u8_input', type: 'submit', value: '公司转账' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u9', className: 'ax_shape', onClick: function onClick() {
                            window.history.go(-1);
                        } },
                    _react2.default.createElement('img', { id: 'u9_img', className: 'img ', src: 'i/images/user_check_operation_reservation_page/u9.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u10', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                ' 修改预约信息'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u11', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u11_img', className: 'img ', src: 'i/images/user_check_operation_reservation_page/u9.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u12', className: 'text', onClick: function onClick() {
                                _auth2.default.myact({ to: 'pay.do',
                                    parms: [{ 'key': 'id', 'value': _this.state.dataSource[0].id }]
                                }, function (res) {
                                    console.log(res);
                                    if (res.state == 'success') {
                                        alert('预约成功');
                                    }
                                });
                            } },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '提交  '
                            )
                        )
                    ),
                    _react2.default.createElement(
                        'div',
                        { id: 'u39', 'class': 'ax_html_button' },
                        _react2.default.createElement('input', null)
                    )
                ),
                _react2.default.createElement('div', null),
                _react2.default.createElement(
                    'div',
                    { id: 'u13', className: 'ax_radio_button' },
                    _react2.default.createElement(
                        'label',
                        { htmlFor: 'u13_input' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u14', className: 'text' },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '支付宝'
                                )
                            )
                        )
                    ),
                    _react2.default.createElement('input', { id: 'u13_input', type: 'radio', value: 'radio', name: 'u13' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u15', className: 'ax_radio_button' },
                    _react2.default.createElement(
                        'label',
                        { htmlFor: 'u15_input' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u16', className: 'text' },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '微信支付'
                                )
                            )
                        )
                    ),
                    _react2.default.createElement('input', { id: 'u15_input', type: 'radio', value: 'radio', name: 'u15' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u17', className: 'ax_radio_button' },
                    _react2.default.createElement(
                        'label',
                        { htmlFor: 'u17_input' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u18', className: 'text' },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '百度支付'
                                )
                            )
                        )
                    ),
                    _react2.default.createElement('input', { id: 'u17_input', type: 'radio', value: 'radio', name: 'u17' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u19', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u19_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u20', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '手术地点：北京 '
                            )
                        )
                    ),
                    _react2.default.createElement(
                        'div',
                        { id: 'hi' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', { className: 'hi' })
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u21', className: 'ax_image' },
                    _react2.default.createElement('img', { id: 'u21_img', className: 'img ', src: 'i/images/user_check_operation_reservation_page/u21.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u22', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u23', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u23_img', className: 'img ', src: 'i/images/user_check_operation_reservation_page/u23.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u24', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u25', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u25_img', className: 'img ', src: 'i/images/user_check_operation_reservation_page/u25.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u26', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u27', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u27_img', className: 'img ', src: 'i/images/user_check_operation_reservation_page/u27.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u28', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u29', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u29_img', className: 'img ', src: 'i/resources/images/transparent.gif' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u31', className: 'ax_dynamic_panel' },
                    _react2.default.createElement(
                        'div',
                        { id: 'u31_state0', className: 'panel_state', 'data-label': 'State1' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u31_state0_content', className: 'panel_state_content' },
                            _react2.default.createElement(
                                'div',
                                { id: 'u32', className: 'ax_menu' },
                                _react2.default.createElement('img', { id: 'u32_menu', className: 'img ', src: 'i/images/user_check_operation_reservation_page/u32_menu.png', alt: 'u32_menu' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u33', className: 'ax_table' },
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u34', className: 'ax_table_cell' },
                                        _react2.default.createElement('img', { id: 'u34_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                                        _react2.default.createElement(
                                            'div',
                                            { id: 'u35', className: 'text' },
                                            _react2.default.createElement(
                                                'p',
                                                null,
                                                _react2.default.createElement(
                                                    'span',
                                                    null,
                                                    '我的订单'
                                                )
                                            )
                                        )
                                    ),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u36', className: 'ax_table_cell' },
                                        _react2.default.createElement('img', { id: 'u36_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                                        _react2.default.createElement(
                                            'div',
                                            { id: 'u37', className: 'text' },
                                            _react2.default.createElement(
                                                'p',
                                                null,
                                                _react2.default.createElement(
                                                    'span',
                                                    null,
                                                    '退出'
                                                )
                                            )
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u38', className: 'ax_horizontal_line' },
                                _react2.default.createElement('img', { id: 'u38_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u38_start' }),
                                _react2.default.createElement('img', { id: 'u38_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u38_end' }),
                                _react2.default.createElement('img', { id: 'u38_line', className: 'img ', src: 'i/images/user_check_operation_reservation_page/u38_line.png', alt: 'u38_line' })
                            )
                        )
                    )
                )
            )
        );
    }
});

exports.default = CheckReservation;

},{"../components/PageContainer":19,"../components/auth":25,"react":356}],32:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

require('amazeui-react');

var _auth = require('../components/auth');

var _auth2 = _interopRequireDefault(_auth);

var _reactRouter = require('react-router');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }
//{line2.honor!=undefined?(<div>
//</div>):""}


var Doctor_list = (0, _reactRouter.withRouter)(_react2.default.createClass({
  displayName: 'Doctor_list',
  getInitialState: function getInitialState() {
    return {
      dataSource: [{
        reserveTime: 0,
        title: "主任医师教授",
        realname: "",
        honor: "",
        province: "北京",
        hot: ".00",
        excel: [],
        hospital: "积水潭医院"
      }, {
        reserveTime: 0,
        realname: "",
        title: "主任医师教授",
        honor: "",
        province: "北京",
        hot: ".00",
        excel: [],
        hospital: "积水潭医院"
      }, {
        realname: "",
        reserveTime: 0,
        title: "主任医师教授",
        honor: "",
        province: "北京",
        hot: ".00",
        excel: [],
        hospital: "积水潭医院"
      }],
      leixing: '2', //or 专家团队 or 医疗机构
      didian: '', //or 北京上海广州赤峰
      check_yuyue: false,
      check_youyouhui: false,
      search_paixu: '1', //or人气 预约时间,
      nowpage: 1,
      totalpage: 10,
      keyword: ''

    };
  },
  query: function query() {
    var _this = this;

    var page = arguments.length <= 0 || arguments[0] === undefined ? this.state.nowpage : arguments[0];

    console.log(page);
    var to = "http://123.56.133.208:8080/myfd/doctorlist.do";
    _auth2.default.myact({
      to: 'list.do',
      parms: [{ 'key': 'page', 'value': page }, { 'key': 'role', 'value': this.state.leixing }, { 'key': 'province', 'value': this.state.didian }, { 'key': 'paixu', 'value': this.state.search_paixu }, { 'key': 'disease', 'value': '' }]
    }, function (res) {
      _this.updateDataSource(res);
    });
    this.state.nowpage = page;
  },
  querynext: function querynext() {
    var nowpage = this.state.nowpage;
    var totalpage = this.state.totalpage;
    if (nowpage + 1 <= totalpage) {
      nowpage += 1;
      this.query(nowpage);
    }
  },
  querybefore: function querybefore() {

    var nowpage = this.state.nowpage;
    var totalpage = this.state.totalpage;
    if (nowpage - 1 >= 1) {
      nowpage -= 1;
      this.query(nowpage);
    }
  },
  componentDidMount: function componentDidMount() {
    this.query(1);
  },
  updateDataSource: function updateDataSource(data) {
    this.setState({
      dataSource: data
    });
  },
  kaifazhong: function kaifazhong(str) {
    console.log(str);
  },
  render: function render() {
    var _this2 = this,
        _React$createElement2;

    var line1 = { excel: [] };
    var data = this.state.dataSource;
    if (data.length >= 1) {
      line1 = data[0];
    }
    var line2 = { excel: [] };
    if (data.length >= 2) {
      line2 = data[1];
    }
    var line3 = { excel: [] };
    if (data.length >= 3) {
      line3 = data[2];
    }

    return _react2.default.createElement(
      _PageContainer2.default,
      null,
      _react2.default.createElement('link', { href: 'i/resources/css/axure_rp_page.css', type: 'text/css', rel: 'stylesheet' }),
      _react2.default.createElement('link', { href: 'i/data/styles.css', type: 'text/css', rel: 'stylesheet' }),
      _react2.default.createElement('link', { href: 'i/css/doctor_list/styles.css', type: 'text/css', rel: 'stylesheet' }),
      _react2.default.createElement(
        'div',
        { id: 'base', className: '' },
        _react2.default.createElement(
          _reactRouter.Link,
          { to: '/doctor_list' },
          _react2.default.createElement(
            'div',
            { id: 'u0', className: 'ax_shape' },
            _react2.default.createElement('img', { id: 'u0_img', className: 'img ', src: 'i/images/doctor_list/u0.png' }),
            _react2.default.createElement(
              'div',
              { id: 'u1', className: 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement('span', null)
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u2', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u2_img', className: 'img ', src: 'i/images/query_result/u0.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u3', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u4', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u4_img', className: 'img ', src: 'i/images/query_result/u4.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u5', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u6', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u6_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u7', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '类型：'
              )
            )
          )
        ),
        _react2.default.createElement(
          _reactRouter.Link,
          { to: '/institution_list' },
          _react2.default.createElement(
            'div',
            { id: 'u8', className: 'ax_paragraph' },
            _react2.default.createElement('img', { id: 'u8_img', className: 'img ', src: this.state.leixing == '1' ? 'i/images/doctor_list/u10.png' : 'i/images/institution_list/u10.png' }),
            _react2.default.createElement(
              'div',
              { id: 'u9', className: 'text', style: { color: this.state.leixing == '1' ? 'white' : 'black' } },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement(
                  'span',
                  null,
                  '医疗机构'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          _reactRouter.Link,
          { to: '/doctor_list' },
          _react2.default.createElement(
            'div',
            { id: 'u10', className: 'ax_paragraph', style: { color: this.state.leixing == '2' ? 'white' : 'black' } },
            _react2.default.createElement('img', { id: 'u10_img', className: 'img ', src: this.state.leixing == '2' ? 'i/images/doctor_list/u10.png' : 'i/images/institution_list/u10.png' }),
            _react2.default.createElement(
              'div',
              { id: 'u11', className: 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement(
                  'span',
                  null,
                  '专家团队 '
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          _reactRouter.Link,
          { to: '/query_result' },
          _react2.default.createElement(
            'div',
            { id: 'u12', className: 'ax_paragraph' },
            _react2.default.createElement('img', { id: 'u12_img', className: 'img ', src: this.state.leixing == '0' ? 'i/images/query_result/u14.png' : 'i/images/institution_list/u10.png' }),
            _react2.default.createElement(
              'div',
              { id: 'u13', className: 'text', style: { color: this.state.leixing == '0' ? 'white' : 'black' } },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement(
                  'span',
                  null,
                  '不限'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u14', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u14_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u15', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '地点：'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u16', className: 'ax_paragraph', style: { color: this.state.didian == '北京' ? 'white' : 'black' }, onClick: function onClick() {
              _this2.state.didian = '北京';_this2.query(_this2.state.nowpage);
            } },
          _react2.default.createElement('img', { id: 'u16_img', className: 'img ', src: this.state.didian == '北京' ? 'i/images/query_result/u14.png' : 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u17', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '北京'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u18', className: 'ax_paragraph', style: { color: this.state.didian == '' ? 'white' : 'black' }, onClick: function onClick() {
              _this2.state.didian = '';_this2.query(_this2.state.nowpage);
            } },
          _react2.default.createElement('img', { id: 'u18_img', className: 'img ', src: this.state.didian == '' ? 'i/images/query_result/u14.png' : 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u19', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '不限'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u20', className: 'ax_horizontal_line' },
          _react2.default.createElement('img', { id: 'u20_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u20_start' }),
          _react2.default.createElement('img', { id: 'u20_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u20_end' }),
          _react2.default.createElement('img', { id: 'u20_line', className: 'img ', src: 'i/images/user_home/u68_line.png', alt: 'u20_line' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u21', className: 'ax_checkbox' },
          _react2.default.createElement(
            'label',
            { htmlFor: 'u21_input' },
            _react2.default.createElement(
              'div',
              { id: 'u22', className: 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement(
                  'span',
                  null,
                  '7日内可预约'
                )
              )
            )
          ),
          _react2.default.createElement('input', { id: 'u21_input', type: 'checkbox', value: 'checkbox', checked: this.state.check_yuyue ? 'checked' : '', onClick: function onClick() {
              _this2.state.check_yuyue = !_this2.state.check_yuyue;_this2.query(_this2.state.nowpage);
            } })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u23', className: 'ax_checkbox' },
          _react2.default.createElement(
            'label',
            { htmlFor: 'u23_input' },
            _react2.default.createElement(
              'div',
              { id: 'u24', className: 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement(
                  'span',
                  null,
                  '有优惠'
                )
              )
            )
          ),
          _react2.default.createElement('input', { id: 'u23_input', type: 'checkbox', value: 'checkbox', checked: this.state.check_youyouhui ? 'checked' : '', onClick: function onClick() {
              _this2.state.check_youyouhui = !_this2.state.check_youyouhui;_this2.query(_this2.state.nowpage);
            } })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u25', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u25_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u26', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                { style: { color: this.state.search_paixu == '1' ? 'red' : 'black' }, onClick: function onClick() {
                    _this2.state.search_paixu = '1';_this2.query(_this2.state.nowpage);
                  } },
                '智能'
              ),
              _react2.default.createElement(
                'span',
                null,
                '  '
              ),
              _react2.default.createElement(
                'span',
                null,
                ' |  ',
                _react2.default.createElement(
                  'span',
                  {
                    style: { color: this.state.search_paixu == '2' ? 'red' : 'black' }, onClick: function onClick() {
                      _this2.state.search_paixu = '2';_this2.query(_this2.state.nowpage);
                    } },
                  '人气'
                ),
                '  | ',
                _react2.default.createElement(
                  'span',
                  {
                    style: { color: this.state.search_paixu == '3' ? 'red' : 'black' }, onClick: function onClick() {
                      _this2.state.search_paixu = '3';_this2.query(_this2.state.nowpage);
                    }
                  },
                  '预约时间'
                ),
                '    '
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u29', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u29_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u30', className: 'text' },
            _react2.default.createElement(
              'p',
              { onClick: this.querybefore },
              ' ',
              _react2.default.createElement(
                'span',
                null,
                '上一页'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u31', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u31_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u32', className: 'text' },
            _react2.default.createElement(
              'p',
              { onClick: this.querynext },
              _react2.default.createElement(
                'span',
                null,
                '下一页'
              )
            )
          )
        ),
        true ? _react2.default.createElement(
          'div',
          null,
          _react2.default.createElement(
            'div',
            { id: 'u33', className: 'ax_paragraph' },
            _react2.default.createElement('img', { id: 'u33_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
            _react2.default.createElement(
              'div',
              { id: 'u34', className: 'text' },
              _react2.default.createElement(
                'p',
                { className: 'u130' },
                _react2.default.createElement('span', { className: 'u131' }),
                _react2.default.createElement(
                  'span',
                  { className: 'u131' },
                  ' ',
                  line1.realname + line1.title
                ),
                _react2.default.createElement(
                  'span',
                  { className: 'u132' },
                  '  '
                ),
                _react2.default.createElement(
                  'span',
                  { className: 'u132' },
                  '        '
                ),
                _react2.default.createElement(
                  'span',
                  { className: 'u132' },
                  '  '
                ),
                _react2.default.createElement(
                  'span',
                  { className: 'u134' },
                  '人气指数 ',
                  line1.hot
                )
              ),
              _react2.default.createElement(
                'p',
                { className: 'u133' },
                _react2.default.createElement(
                  'span',
                  { className: 'u132' },
                  '擅长治疗: ',
                  line1.excel.join(',')
                )
              ),
              _react2.default.createElement(
                'p',
                { className: 'u133' },
                _react2.default.createElement(
                  'span',
                  { className: 'u134' },
                  '可预约地点: ',
                  line1.province
                )
              ),
              _react2.default.createElement(
                'p',
                { className: 'u135' },
                _react2.default.createElement(
                  'span',
                  { className: 'u134' },
                  '最快手术时间: ',
                  line1.reserveTime
                ),
                _react2.default.createElement(
                  'span',
                  { className: 'u141' },
                  '  '
                )
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { id: 'u35', className: 'ax_paragraph' },
            _react2.default.createElement('img', { id: 'u35_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
            _react2.default.createElement(
              'div',
              { id: 'u36', className: 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement(
                  'span',
                  { className: 'u140' },
                  '第一'
                ),
                _react2.default.createElement(
                  'span',
                  { className: 'u140' },
                  '执业点： ',
                  line1.hospital
                )
              ),
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement(
                  'span',
                  { className: 'u142' },
                  '荣誉：',
                  line1.honor
                )
              ),
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement(
                  'span',
                  { className: 'u140' },
                  '  '
                )
              )
            )
          )
        ) : '',
        true ? _react2.default.createElement(
          'div',
          null,
          _react2.default.createElement(
            'div',
            { id: 'u37', className: 'ax_paragraph' },
            _react2.default.createElement('img', { id: 'u37_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
            _react2.default.createElement(
              'div',
              { id: 'u38', className: 'text' },
              _react2.default.createElement(
                'p',
                { className: 'u130' },
                _react2.default.createElement(
                  'span',
                  { className: 'u137' },
                  ' '
                ),
                _react2.default.createElement(
                  'span',
                  { className: 'u137' },
                  ' ',
                  line2.realname + line2.title
                ),
                _react2.default.createElement('span', { className: 'u137' }),
                _react2.default.createElement(
                  'span',
                  { className: 'u137' },
                  '    '
                ),
                _react2.default.createElement(
                  'span',
                  { className: 'u139' },
                  '  '
                ),
                _react2.default.createElement(
                  'span',
                  { className: 'u136' },
                  '人气指数 '
                ),
                _react2.default.createElement(
                  'span',
                  { className: 'u136' },
                  line2.hot
                )
              ),
              _react2.default.createElement(
                'p',
                { className: 'u135' },
                _react2.default.createElement(
                  'span',
                  { className: 'u139' },
                  '擅长治疗: ',
                  line2.excel.join(','),
                  '  '
                ),
                _react2.default.createElement('span', { className: 'u139' })
              ),
              _react2.default.createElement(
                'p',
                { className: 'u133' },
                _react2.default.createElement(
                  'span',
                  { className: 'u138' },
                  '可预约地点: ',
                  line2.province
                )
              ),
              _react2.default.createElement(
                'p',
                { className: 'u135' },
                _react2.default.createElement(
                  'span',
                  { className: 'u138' },
                  '最快手术时间: ',
                  line2.reserveTime
                ),
                _react2.default.createElement(
                  'span',
                  { className: 'u139' },
                  '  '
                )
              )
            )
          )
        ) : "",
        _react2.default.createElement(
          'div',
          { id: 'u39', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u39_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u40', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '第一执业点：'
              ),
              _react2.default.createElement(
                'span',
                null,
                line2.hospital
              )
            )
          )
        ),
        true ? _react2.default.createElement(
          'div',
          null,
          _react2.default.createElement(
            'div',
            { id: 'u41', className: 'ax_horizontal_line' },
            _react2.default.createElement('img', { id: 'u41_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u41_start' }),
            _react2.default.createElement('img', { id: 'u41_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u41_end' }),
            _react2.default.createElement('img', { id: 'u41_line', className: 'img ', src: 'i/images/query_result/u43_line.png', alt: 'u41_line' })
          ),
          _react2.default.createElement(
            'div',
            { id: 'u42', className: 'ax_paragraph' },
            _react2.default.createElement('img', { id: 'u42_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
            _react2.default.createElement(
              'div',
              { id: 'u43', className: 'text' },
              line1.discount != undefined ? _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement(
                  'span',
                  null,
                  '优惠'
                ),
                _react2.default.createElement(
                  'span',
                  null,
                  '： '
                ),
                _react2.default.createElement(
                  'span',
                  null,
                  line1.discount
                )
              ) : ''
            )
          )
        ) : "",
        _react2.default.createElement(
          'div',
          { id: 'u44', className: 'ax_horizontal_line' },
          _react2.default.createElement('img', { id: 'u44_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u44_start' }),
          _react2.default.createElement('img', { id: 'u44_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u44_end' }),
          _react2.default.createElement('img', { id: 'u44_line', className: 'img ', src: 'i/images/query_result/u43_line.png', alt: 'u44_line' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u45', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u45_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u46', className: 'text' },
            line2.discount != undefined ? _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '优惠'
              ),
              _react2.default.createElement(
                'span',
                null,
                '： '
              ),
              _react2.default.createElement(
                'span',
                null,
                line2.discount
              )
            ) : ''
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u47', className: 'ax_text_field' },
          _react2.default.createElement('input', _defineProperty({ id: 'u47_input', ref: 'desces', type: 'text' }, 'ref', 'des'))
        ),
        _react2.default.createElement(
          'div',
          { id: 'u48', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u48_img', className: 'img ', src: 'i/images/query_result/u55.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u49', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '快速预约'
              )
            )
          )
        ),
        _react2.default.createElement(
          _reactRouter.Link,
          { to: 'index_home' },
          _react2.default.createElement(
            'div',
            { id: 'u50', className: 'ax_shape' },
            _react2.default.createElement('img', { id: 'u50_img', className: 'img ', src: 'i/images/user_check_operation_reservation_page/u23.png' }),
            _react2.default.createElement(
              'div',
              { id: 'u51', className: 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement('span', null)
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u52', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u52_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u53', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '手术地点：'
              ),
              _react2.default.createElement(
                'span',
                null,
                '全国'
              ),
              _react2.default.createElement(
                'span',
                null,
                '  '
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u57', className: 'ax_shape',
            onClick: function onClick() {
              _this2.props.router.replace({ pathname: '/query_result', query: { query: _this2.refs.des.value } });
            } },
          _react2.default.createElement('img', { id: 'u57_img', className: 'img ', src: 'i/images/query_result/u64.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u58', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '搜索'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u59', className: 'ax_horizontal_line' },
          _react2.default.createElement('img', { id: 'u59_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u59_start' }),
          _react2.default.createElement('img', { id: 'u59_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u59_end' }),
          _react2.default.createElement('img', { id: 'u59_line', className: 'img ', src: 'i/images/user_home/u68_line.png', alt: 'u59_line' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u60', className: 'ax_horizontal_line' },
          _react2.default.createElement('img', { id: 'u60_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u60_start' }),
          _react2.default.createElement('img', { id: 'u60_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u60_end' }),
          _react2.default.createElement('img', { id: 'u60_line', className: 'img ', src: 'i/images/user_home/u68_line.png', alt: 'u60_line' })
        ),
        true ? _react2.default.createElement(
          'div',
          null,
          _react2.default.createElement(
            _reactRouter.Link,
            { to: { pathname: '/user_reservation', query: line1 } },
            _react2.default.createElement(
              'div',
              { id: 'u61', className: 'ax_shape' },
              _react2.default.createElement('img', { id: 'u61_img', className: 'img ', src: 'i/images/query_result/u68.png' }),
              _react2.default.createElement(
                'div',
                { id: 'u62', className: 'text' },
                _react2.default.createElement(
                  'p',
                  null,
                  _react2.default.createElement(
                    'span',
                    null,
                    '立即预约'
                  )
                )
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { id: 'u63', className: 'ax_image' },
            _react2.default.createElement('img', { id: 'u63_img', className: 'img ', src: line1.id }),
            _react2.default.createElement(
              'div',
              { id: 'u64', className: 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement('span', null)
              )
            )
          )
        ) : '',
        _react2.default.createElement(
          _reactRouter.Link,
          { to: { pathname: '/user_reservation', query: line2 } },
          _react2.default.createElement(
            'div',
            { id: 'u65', className: 'ax_shape' },
            _react2.default.createElement('img', { id: 'u65_img', className: 'img ', src: 'i/images/query_result/u68.png' }),
            _react2.default.createElement(
              'div',
              { id: 'u66', className: 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement(
                  'span',
                  null,
                  '立即预约'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u67', className: 'ax_image' },
          _react2.default.createElement('img', { id: 'u67_img', className: 'img ', src: line2.id }),
          _react2.default.createElement(
            'div',
            { id: 'u68', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u69', className: 'ax_dynamic_panel' },
          _react2.default.createElement(
            'div',
            { id: 'u69_state0', className: 'panel_state', 'data-label': 'State1' },
            _react2.default.createElement(
              'div',
              { id: 'u69_state0_content', className: 'panel_state_content' },
              _react2.default.createElement(
                'div',
                { id: 'u70', className: 'ax_shape' },
                _react2.default.createElement('img', { id: 'u70_img', className: 'img ', src: 'i/images/query_result/u81.png' }),
                _react2.default.createElement(
                  'div',
                  { id: 'u71', className: 'text' },
                  _react2.default.createElement(
                    'p',
                    null,
                    _react2.default.createElement('span', null)
                  )
                )
              ),
              _react2.default.createElement(
                'div',
                { id: 'u72', className: 'ax_paragraph' },
                _react2.default.createElement('img', { id: 'u72_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                _react2.default.createElement(
                  'div',
                  { id: 'u73', className: 'text' },
                  _react2.default.createElement(
                    'p',
                    null,
                    _react2.default.createElement(
                      'span',
                      null,
                      '创伤'
                    )
                  )
                )
              ),
              _react2.default.createElement(
                'div',
                { id: 'u74', className: 'ax_paragraph' },
                _react2.default.createElement('img', { id: 'u74_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                _react2.default.createElement(
                  'div',
                  { id: 'u75', className: 'text' },
                  _react2.default.createElement(
                    'p',
                    null,
                    _react2.default.createElement(
                      'span',
                      null,
                      '脊椎'
                    )
                  )
                )
              ),
              _react2.default.createElement(
                'div',
                { id: 'u76', className: 'ax_paragraph' },
                _react2.default.createElement('img', { id: 'u76_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                _react2.default.createElement(
                  'div',
                  { id: 'u77', className: 'text' },
                  _react2.default.createElement(
                    'p',
                    null,
                    _react2.default.createElement(
                      'span',
                      null,
                      '儿骨'
                    )
                  )
                )
              ),
              _react2.default.createElement(
                'div',
                { id: 'u78', className: 'ax_shape' },
                _react2.default.createElement('img', { id: 'u78_img', className: 'img ', src: 'i/images/query_result/u89.png' }),
                _react2.default.createElement(
                  'div',
                  { id: 'u79', className: 'text' },
                  _react2.default.createElement(
                    'p',
                    null,
                    _react2.default.createElement('span', null)
                  )
                )
              ),
              _react2.default.createElement(
                'div',
                { id: 'u80', className: 'ax_shape' },
                _react2.default.createElement('img', { id: 'u80_img', className: 'img ', src: 'i/images/query_result/u89.png' }),
                _react2.default.createElement(
                  'div',
                  { id: 'u81', className: 'text' },
                  _react2.default.createElement(
                    'p',
                    null,
                    _react2.default.createElement('span', null)
                  )
                )
              ),
              _react2.default.createElement(
                'div',
                { id: 'u82', className: 'ax_shape' },
                _react2.default.createElement('img', { id: 'u82_img', className: 'img ', src: 'i/images/query_result/u89.png' }),
                _react2.default.createElement(
                  'div',
                  { id: 'u83', className: 'text' },
                  _react2.default.createElement(
                    'p',
                    null,
                    _react2.default.createElement('span', null)
                  )
                )
              ),
              _react2.default.createElement(
                'div',
                { id: 'u84', className: 'ax_paragraph' },
                _react2.default.createElement('img', { id: 'u84_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                _react2.default.createElement(
                  'div',
                  { id: 'u85', className: 'text' },
                  _react2.default.createElement(
                    'p',
                    null,
                    _react2.default.createElement(
                      'span',
                      null,
                      '关节'
                    )
                  )
                )
              ),
              _react2.default.createElement(
                'div',
                { id: 'u86', className: 'ax_shape' },
                _react2.default.createElement('img', { id: 'u86_img', className: 'img ', src: 'i/images/query_result/u89.png' }),
                _react2.default.createElement(
                  'div',
                  { id: 'u87', className: 'text' },
                  _react2.default.createElement(
                    'p',
                    null,
                    _react2.default.createElement('span', null)
                  )
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u88', className: 'ax_horizontal_line' },
          _react2.default.createElement('img', { id: 'u88_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u88_start' }),
          _react2.default.createElement('img', { id: 'u88_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u88_end' }),
          _react2.default.createElement('img', { id: 'u88_line', className: 'img ', src: 'i/images/query_result/u99_line.png', alt: 'u88_line' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u89', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u89_img', className: 'img ', src: 'i/images/query_result/u100.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u90', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '全部症状'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u91', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u91_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u92', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '医生团队'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u93', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u93_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u94', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '合作机构'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u95', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u95_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u96', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                _reactRouter.Link,
                { to: '/index_home' },
                _react2.default.createElement(
                  'span',
                  null,
                  '首页'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u97', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u97_img', className: 'img ', src: 'i/images/query_result/u108.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u98', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u99', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u99_img', className: 'img ', src: 'i/images/index_home/u13.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u100', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u101', className: 'ax_paragraph', style: { color: this.state.didian == '上海' ? 'white' : 'black' }, onClick: function onClick() {
              _this2.state.didian = '上海';_this2.query(_this2.state.nowpage);
            } },
          _react2.default.createElement('img', (_React$createElement2 = { id: 'u101_img', className: 'img ' }, _defineProperty(_React$createElement2, 'className', 'img '), _defineProperty(_React$createElement2, 'src', this.state.didian == '上海' ? 'i/images/query_result/u14.png' : 'i/resources/images/transparent.gif'), _React$createElement2)),
          _react2.default.createElement(
            'div',
            { id: 'u102', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '上海'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u103', className: 'ax_paragraph', style: { color: this.state.didian == '广州' ? 'black' : 'black' }, onClick: function onClick() {
              _this2.state.didian = '广州';_this2.query(_this2.state.nowpage);
            } },
          _react2.default.createElement('img', { id: 'u103_img', className: 'img ', src: this.state.didian == '广州' ? 'i/images/query_result/u14.png' : 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u104', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '广州'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u105', className: 'ax_paragraph', style: { color: this.state.didian == '赤峰' ? 'white' : 'black' }, onClick: function onClick() {
              _this2.state.didian = '赤峰';_this2.query(_this2.state.nowpage);
            } },
          _react2.default.createElement('img', { id: 'u105_img', className: 'img ', src: this.state.didian == '赤峰' ? 'i/images/query_result/u14.png' : 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u106', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '赤峰'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u107', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u107_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u108', className: 'text' },
            _react2.default.createElement(
              'p',
              { className: 'u130' },
              _react2.default.createElement('span', { className: 'u131' }),
              _react2.default.createElement(
                'span',
                { className: 'u131' },
                ' ',
                line3.realname + line3.title
              ),
              _react2.default.createElement(
                'span',
                { className: 'u132' },
                '  '
              ),
              _react2.default.createElement(
                'span',
                { className: 'u132' },
                '        '
              ),
              _react2.default.createElement(
                'span',
                { className: 'u132' },
                '  '
              ),
              _react2.default.createElement(
                'span',
                { className: 'u134' },
                '人气指数 ',
                line3.hot
              )
            ),
            _react2.default.createElement(
              'p',
              { className: 'u133' },
              _react2.default.createElement(
                'span',
                { className: 'u132' },
                '擅长治疗: ',
                line3.excel.join(','),
                ' '
              ),
              _react2.default.createElement('span', { className: 'u132' }),
              _react2.default.createElement('span', { className: 'u132' })
            ),
            _react2.default.createElement(
              'p',
              { className: 'u133' },
              _react2.default.createElement(
                'span',
                { className: 'u134' },
                '可预约地点: ',
                line3.province
              )
            ),
            _react2.default.createElement(
              'p',
              { className: 'u135' },
              _react2.default.createElement(
                'span',
                { className: 'u134' },
                '最快手术时间: ',
                line3.reserveTime
              ),
              _react2.default.createElement(
                'span',
                { className: 'u141' },
                '  '
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u109', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u109_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u110', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                { className: 'u140' },
                '第一'
              ),
              _react2.default.createElement(
                'span',
                { className: 'u140' },
                '执业点：',
                line3.hospital
              )
            ),
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                { className: 'u142' },
                '荣誉：',
                line3.honor
              )
            ),
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                { className: 'u143' },
                '  '
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u111', className: 'ax_horizontal_line' },
          _react2.default.createElement('img', { id: 'u111_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u111_start' }),
          _react2.default.createElement('img', { id: 'u111_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u111_end' }),
          _react2.default.createElement('img', { id: 'u111_line', className: 'img ', src: 'i/images/query_result/u43_line.png', alt: 'u111_line' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u112', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u112_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u113', className: 'text' },
            line3.discount != undefined ? _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '优惠'
              ),
              _react2.default.createElement(
                'span',
                null,
                '： '
              ),
              _react2.default.createElement(
                'span',
                null,
                line3.discount
              )
            ) : ''
          )
        ),
        _react2.default.createElement(
          _reactRouter.Link,
          { to: { pathname: '/user_reservation', query: line3 } },
          _react2.default.createElement(
            'div',
            { id: 'u114', className: 'ax_shape' },
            _react2.default.createElement('img', { id: 'u114_img', className: 'img ', src: 'i/images/query_result/u68.png' }),
            _react2.default.createElement(
              'div',
              { id: 'u115', className: 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement(
                  'span',
                  null,
                  '立即预约'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u116', className: 'ax_image' },
          _react2.default.createElement('img', { id: 'u116_img', className: 'img ', src: line3.id }),
          _react2.default.createElement(
            'div',
            { id: 'u117', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          _reactRouter.Link,
          { to: '/institution_list' },
          _react2.default.createElement('div', { id: 'u118', className: 'ax_hot_spot' })
        ),
        _react2.default.createElement(
          _reactRouter.Link,
          { to: '/index_home' },
          _react2.default.createElement('div', { id: 'u119', className: 'ax_hot_spot' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u120', className: 'ax_horizontal_line' },
          _react2.default.createElement('img', { id: 'u120_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u120_start' }),
          _react2.default.createElement('img', { id: 'u120_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u120_end' }),
          _react2.default.createElement('img', { id: 'u120_line', className: 'img ', src: 'i/images/user_home/u68_line.png', alt: 'u120_line' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u121', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u121_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u122', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                _react2.default.createElement(
                  _reactRouter.Link,
                  { to: _auth2.default.get_config()['about_us_url'] },
                  '关于我们'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u123', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u123_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u124', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                _react2.default.createElement(
                  _reactRouter.Link,
                  { to: _auth2.default.get_config()['connect_us_url'] },
                  '联系我们'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u125', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u125_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u126', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '服务条款'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u888', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u125_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u126', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                _react2.default.createElement(
                  'a',
                  { href: 'http://www.miitbeian.gov.cn' },
                  '           京ICP备案16059247号'
                )
              )
            ),
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                _react2.default.createElement('img', { src: 'http://www.beian.gov.cn/img/ghs.png' }),
                _react2.default.createElement(
                  'a',
                  { href: 'http://www.beian.gov.cn/portal/registerSystemInfo?recordcode=11010802022799' },
                  '京公网安备 11010802022799号'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u127', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u127_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u128', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '媒体更新'
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { id: 'u146', 'class': 'ax_html_button' },
            _react2.default.createElement('input', null)
          )
        )
      )
    );
  }
}));
exports.default = Doctor_list;

},{"../components/PageContainer":19,"../components/auth":25,"amazeui-react":52,"react":356,"react-router":219}],33:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

var _amazeuiReact = require('amazeui-react');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Home = _react2.default.createClass({
  displayName: 'Home',
  renderIntroList: function renderIntroList() {
    var list = [{
      color: 'success',
      icon: 'file-text',
      title: '新增页面',
      number: 2300
    }, {
      color: 'warning',
      icon: 'briefcase',
      title: '成交订单',
      number: 308
    }, {
      color: 'danger',
      icon: 'group',
      title: '昨日访问',
      number: 800223
    }, {
      color: 'secondary',
      icon: 'desktop',
      title: '在线用户',
      number: 3200
    }];

    return list.map(function (item, index) {
      var color = item.color;
      var icon = item.icon;
      var title = item.title;
      var number = item.number;

      var handleClick = function handleClick(title, e) {
        e.preventDefault();
        console.log('点击了：【' + title + '】');
      };

      return _react2.default.createElement(
        'li',
        {
          key: index,
          className: 'am-text-center'
        },
        _react2.default.createElement(
          'a',
          {
            href: '#',
            className: 'am-text-' + color,
            onClick: handleClick.bind(null, title)
          },
          _react2.default.createElement(_amazeuiReact.Icon, { button: true, icon: icon }),
          _react2.default.createElement('br', null),
          title,
          _react2.default.createElement('br', null),
          number
        )
      );
    });
  },
  renderTable: function renderTable() {
    var data = [{
      user: 'John Clark',
      task: 'Business management',
      orders: '20'
    }, {
      user: '样清风',
      task: '公司 Logo 设计',
      orders: '2',
      last: true
    }, {
      user: '詹姆斯',
      task: '业务数据软件开发',
      orders: '10'
    }, {
      user: '菲尔德',
      task: '适配所有网站',
      orders: '50'
    }, {
      user: '贺绿汀',
      task: '费尔多操心',
      orders: '22'
    }];

    return _react2.default.createElement(
      _amazeuiReact.Table,
      { striped: true, className: 'am-margin-bottom' },
      _react2.default.createElement(
        'thead',
        null,
        _react2.default.createElement(
          'tr',
          null,
          _react2.default.createElement(
            'th',
            null,
            'ID'
          ),
          _react2.default.createElement(
            'th',
            null,
            '用户名'
          ),
          _react2.default.createElement(
            'th',
            null,
            '最后任务'
          ),
          _react2.default.createElement(
            'th',
            null,
            '成交订单'
          ),
          _react2.default.createElement(
            'th',
            null,
            '管理'
          )
        )
      ),
      _react2.default.createElement(
        'tbody',
        null,
        data.map(function (item, index) {
          return _react2.default.createElement(
            'tr',
            { key: index },
            _react2.default.createElement(
              'td',
              null,
              index + 1
            ),
            _react2.default.createElement(
              'td',
              null,
              item.user
            ),
            _react2.default.createElement(
              'td',
              null,
              item.task
            ),
            _react2.default.createElement(
              'td',
              null,
              _react2.default.createElement(
                _amazeuiReact.Badge,
                {
                  amStyle: item.last ? 'danger' : 'secondary'
                },
                item.orders
              )
            ),
            _react2.default.createElement(
              'td',
              null,
              _react2.default.createElement(
                _amazeuiReact.Dropdown,
                {
                  title: _react2.default.createElement(_amazeuiReact.Icon, { icon: 'cog' }),
                  btnStyle: 'default',
                  btnSize: 'xs'
                },
                _react2.default.createElement(
                  _amazeuiReact.Dropdown.Item,
                  null,
                  '编辑'
                ),
                _react2.default.createElement(
                  _amazeuiReact.Dropdown.Item,
                  null,
                  '删除'
                )
              )
            )
          );
        })
      )
    );
  },
  renderPanles: function renderPanles() {
    var toolbar = _react2.default.createElement(
      'div',
      { className: 'am-cf' },
      _react2.default.createElement(
        _amazeuiReact.ButtonToolbar,
        { className: 'am-fl' },
        _react2.default.createElement(
          _amazeuiReact.ButtonGroup,
          null,
          _react2.default.createElement(
            _amazeuiReact.Button,
            { amSize: 'xs' },
            _react2.default.createElement(_amazeuiReact.Icon, { icon: 'check' })
          ),
          _react2.default.createElement(
            _amazeuiReact.Button,
            { amSize: 'xs' },
            _react2.default.createElement(_amazeuiReact.Icon, { icon: 'pencil' })
          ),
          _react2.default.createElement(
            _amazeuiReact.Button,
            { amSize: 'xs' },
            _react2.default.createElement(_amazeuiReact.Icon, { icon: 'times' })
          )
        )
      ),
      _react2.default.createElement(
        'div',
        { className: 'am-fr' },
        _react2.default.createElement(
          _amazeuiReact.Button,
          {
            amSize: 'xs'
          },
          '删除'
        )
      )
    );

    return _react2.default.createElement(
      _amazeuiReact.Grid,
      {
        className: 'adm-home-panels',
        collapse: true
      },
      _react2.default.createElement(
        _amazeuiReact.Col,
        {
          sm: 12,
          md: 6
        },
        _react2.default.createElement(
          _amazeuiReact.Panel,
          {
            header: [_react2.default.createElement(_amazeuiReact.Icon, { icon: 'angle-down', className: 'am-fr', key: 'uploadIcon' }), '文件上传'],
            collapsible: true,
            defaultExpanded: true
          },
          _react2.default.createElement(
            _amazeuiReact.List,
            {
              className: 'adm-up-list'
            },
            _react2.default.createElement(
              _amazeuiReact.ListItem,
              null,
              _react2.default.createElement(
                'h3',
                null,
                _react2.default.createElement(_amazeuiReact.Icon, { icon: 'upload' }),
                'Kong-cetian.mp3'
              ),
              _react2.default.createElement(
                'p',
                null,
                '3.3 of 5MB - 5 mins - 1MB/Sec'
              ),
              _react2.default.createElement(_amazeuiReact.Progress, { now: 50, active: true, amStyle: 'secondary', amSize: 'sm' })
            ),
            _react2.default.createElement(
              _amazeuiReact.ListItem,
              null,
              _react2.default.createElement(
                'h3',
                null,
                _react2.default.createElement(_amazeuiReact.Icon, { icon: 'check' }),
                '好人-cetian.mp3'
              ),
              _react2.default.createElement(
                'p',
                null,
                '3.3 of 5MB - 5 mins - 3MB/Sec'
              )
            ),
            _react2.default.createElement(
              _amazeuiReact.ListItem,
              null,
              _react2.default.createElement(
                'h3',
                null,
                _react2.default.createElement(_amazeuiReact.Icon, { icon: 'check' }),
                '其实都没有.mp3'
              ),
              _react2.default.createElement(
                'p',
                null,
                '3.3 of 5MB - 5 mins - 3MB/Sec'
              )
            )
          )
        )
      ),
      _react2.default.createElement(
        _amazeuiReact.Col,
        {
          sm: 12,
          md: 6
        },
        _react2.default.createElement(
          _amazeuiReact.Panel,
          {
            header: [_react2.default.createElement(_amazeuiReact.Icon, { icon: 'bars', className: 'am-fr', key: 'uploadIcon' }), '任务列表'],
            collapsible: true,
            defaultExpanded: true
          },
          _react2.default.createElement(
            _amazeuiReact.List,
            { className: 'adm-home-tasks' },
            _react2.default.createElement(
              _amazeuiReact.ListItem,
              null,
              _react2.default.createElement(
                'div',
                { className: 'adm-task-meta' },
                ' Posted on 25/1/2120 by John Clark'
              ),
              _react2.default.createElement(
                'div',
                { className: 'adm-task-bd' },
                'The starting place for exploring business management; helping new managers get started and experienced managers get better.'
              ),
              toolbar
            ),
            _react2.default.createElement(
              _amazeuiReact.ListItem,
              null,
              _react2.default.createElement(
                'div',
                { className: 'adm-task-meta' },
                ' Posted on 25/1/2120 by 呵呵呵'
              ),
              _react2.default.createElement(
                'div',
                { className: 'adm-task-bd' },
                '基兰和狗熊出现在不同阵营时。基兰会获得BUFF，“装甲熊憎恨者”。狗熊会获得BUFF，“时光老人憎恨者”。'
              ),
              toolbar
            )
          )
        )
      )
    );
  },
  render: function render() {
    return _react2.default.createElement(
      _PageContainer2.default,
      null,
      _react2.default.createElement(
        _amazeuiReact.AvgGrid,
        {
          sm: 2,
          md: 4,
          className: 'am-margin-bottom am-padding-vertical am-text-center adm-intro-list'
        },
        this.renderIntroList()
      ),
      this.renderTable(),
      this.renderPanles()
    );
  }
});

exports.default = Home;

},{"../components/PageContainer":19,"amazeui-react":52,"react":356}],34:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

var _Header = require('../components/FontComponents/Header');

var _Header2 = _interopRequireDefault(_Header);

var _Qbzz = require('../components/FontComponents/Qbzz');

var _Qbzz2 = _interopRequireDefault(_Qbzz);

var _amazeuiReact = require('amazeui-react');

var _auth = require('../components/auth');

var _auth2 = _interopRequireDefault(_auth);

var _reactRouter = require('react-router');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Index_home = (0, _reactRouter.withRouter)(_react2.default.createClass({
  displayName: 'Index_home',
  getInitialState: function getInitialState() {
    return {
      disctrict: '',
      position: 5,
      agency_left: 0,
      fourdoc: [{
        reserveTime: 0,
        title: "主任医师教授",
        honor: "",
        province: "北京",
        hot: ".00",
        excel: [],
        hospital: "积水潭医院"
      }],
      forhospital: [{
        agencydoctorid: 0,
        agencyprovince: "北京",
        agencydistrict: "朝阳区",
        agencyname: "北京市朝阳医院",
        excel: [],
        agencybed: 2,
        agencydiscount: "有优惠",
        agencyhot: "20.00",
        agencyyytime: 10,
        agencyclass: "三乙"
      }], fornotice: [{
        notice: "xxx"
      }]
    };
  },
  queryhos: function queryhos() {
    var _this = this;

    _auth2.default.myact({
      to: 'fourpagelist.do',
      parms: []
    }, function (res) {
      _this.setState({ forhospital: res });
    });
  },
  querydoc: function querydoc() {
    var _this2 = this;

    _auth2.default.myact({
      to: 'fourdoctorlist.do',
      parms: []
    }, function (res) {
      _this2.setState({ fourdoc: res });
    });
  },
  componentWillMount: function componentWillMount() {
    this.queryhos();
    this.querydoc();
    this.notice();
  },
  mouseOver1: function mouseOver1() {
    //关节右菜单
    document.getElementById("u184").style.visibility = "visible";
  },
  mouseOut1: function mouseOut1() {
    document.getElementById("u184").style.visibility = "hidden";
  },
  mouseOver2: function mouseOver2() {
    //脊椎右菜单
    document.getElementById("u190").style.visibility = "visible";
    document.getElementById("u184_state1").style.visibility = "visible";
  },
  mouseOut2: function mouseOut2() {
    document.getElementById("u190").style.visibility = "hidden";
    document.getElementById("u184_state1").style.visibility = "hidden";
  },
  mouseOver3: function mouseOver3() {
    //创伤右菜单
    document.getElementById("u184_state2").style.visibility = "visible";
  },
  mouseOut3: function mouseOut3() {
    document.getElementById("u184_state2").style.visibility = "hidden";
  },
  mouseOver4: function mouseOver4() {
    //手外右菜单
    document.getElementById("u184_state3").style.visibility = "visible";
  },
  mouseOut4: function mouseOut4() {
    document.getElementById("u184_state3").style.visibility = "hidden";
  },
  mouseOver5: function mouseOver5() {
    //小儿骨科右菜单
    document.getElementById("u184_state4").style.visibility = "visible";
  },
  mouseOut5: function mouseOut5() {
    document.getElementById("u184_state4").style.visibility = "hidden";
  },
  mouseOver6: function mouseOver6() {
    //骨与软组织肿瘤右菜单
    document.getElementById("u184_state5").style.visibility = "visible";
  },
  mouseOut6: function mouseOut6() {
    document.getElementById("u184_state5").style.visibility = "hidden";
  },
  search: function search(str) {
    console.log(str);
  },
  shanchang: function shanchang(str) {
    console.log(str.substr(3, 9));
    var end = '';
    if (str.length >= 21) {
      end = '···';
    }
    return str.substr(3, 9) == 'undefined' ? str.substr(0, 3) : str.substr(0, 21) + end;
  },
  notice: function notice() {
    var _this3 = this;

    _auth2.default.myact({
      to: 'notice.do',
      parms: []
    }, function (res) {
      console.log(res);
      _this3.setState({ fornotice: res });
    });
  },
  render: function render() {
    var _this4 = this;

    var dat = this.state.fornotice;

    console.log(dat);

    var data = this.state.forhospital;
    var line1 = { excel: [] };
    if (data.length >= 1) {
      line1 = data[0];
    }
    var line2 = { excel: [] };
    if (data.length >= 2) {
      line2 = data[1];
    }
    var line3 = { excel: [] };
    if (data.length >= 3) {
      line3 = data[2];
    }var line4 = { excel: [] };
    if (data.length >= 4) {
      line4 = data[3];
    }
    var ddata = this.state.fourdoc;

    var dline1 = { excel: [] };
    if (ddata.length >= 1) {
      dline1 = ddata[0];
    }
    var dline2 = { excel: [] };
    if (ddata.length >= 2) {
      dline2 = ddata[1];
    }
    var dline3 = { excel: [] };
    if (ddata.length >= 3) {
      dline3 = ddata[2];
    }
    var dline4 = { excel: [] };
    if (ddata.length >= 4) {
      dline4 = ddata[3];
    }
    console.log(ddata);
    var goodAt = '擅长:';
    var goodAt1 = goodAt + dline1.excel[0] + ',' + dline1.excel[1] + ',' + dline1.excel[2] + dline1.excel[3];
    var goodAt2 = goodAt + dline2.excel[0] + ',' + dline2.excel[1] + ',' + dline2.excel[2] + dline2.excel[3];
    var goodAt3 = goodAt + dline3.excel[0] + ',' + dline3.excel[1] + ',' + dline3.excel[2] + dline3.excel[3];
    var goodAt4 = goodAt + dline4.excel[0] + ',' + dline4.excel[1] + ',' + dline4.excel[2] + dline4.excel[3];
    console.log(dline1.length);
    console.log(goodAt3.length);
    console.log(goodAt3);
    var notice1 = '';
    var notice2 = '';
    var notice3 = '';
    var notice4 = '';
    var notices = dat.notice;
    var noticeList = String(notices).split("\n");
    if (noticeList.length > 1) {
      notice1 = noticeList[0];
      notice2 = noticeList[1];
      console.log(notice1, notice2);
      if (noticeList.length > 2) {
        notice3 = noticeList[2];
        console.log(notice3);
      }
    }

    console.log(noticeList.length);
    return _react2.default.createElement(
      _PageContainer2.default,
      null,
      _react2.default.createElement('script', { type: 'text/javascript' }),
      _react2.default.createElement('link', { href: 'i/resources/css/axure_rp_page.css', type: 'text/css', rel: 'stylesheet' }),
      _react2.default.createElement('link', { href: 'i/data/styles.css', type: 'text/css', rel: 'stylesheet' }),
      _react2.default.createElement('link', { href: 'i/css/index_home/styles.css', type: 'text/css', rel: 'stylesheet' }),
      _react2.default.createElement(
        'div',
        { id: 'base', className: '' },
        _react2.default.createElement(
          'div',
          { id: 'u0', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u0_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u1', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '手术地点：'
              ),
              _react2.default.createElement(
                'select',
                null,
                _react2.default.createElement(
                  'option',
                  null,
                  '北京'
                ),
                _react2.default.createElement(
                  'option',
                  null,
                  '上海'
                ),
                _react2.default.createElement(
                  'option',
                  null,
                  '广州'
                ),
                _react2.default.createElement(
                  'option',
                  null,
                  '赤峰'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u5', className: 'ax_text_field' },
          _react2.default.createElement('input', { id: 'u5_input', type: 'text', ref: 'des' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u6', className: 'ax_html_button', 'data-label': '搜索', onClick: function onClick() {
              _this4.props.router.replace({ pathname: '/query_result', query: { query: _this4.refs.des.value } });
            } },
          _react2.default.createElement('input', { id: 'u6_input', type: 'submit', value: '搜索' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u7', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u7_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u8', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '医生团队'
              )
            )
          )
        ),
        _react2.default.createElement(
          _reactRouter.Link,
          { to: '/doctor_list' },
          _react2.default.createElement(
            'div',
            { id: 'u9', className: 'ax_paragraph' },
            _react2.default.createElement('img', { id: 'u9_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
            _react2.default.createElement(
              'div',
              { id: 'u10', className: 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement(
                  'span',
                  null,
                  '> '
                ),
                _react2.default.createElement(
                  'span',
                  null,
                  '> 更多'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u11', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u11_img', className: 'img ', src: 'i/images/query_result/u55.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u12', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '快速预约'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u13', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u13_img', className: 'img ', src: 'i/images/index_home/u13.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u14', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          _reactRouter.Link,
          { to: '/index_home' },
          _react2.default.createElement(
            'div',
            { id: 'u15', className: 'ax_shape' },
            _react2.default.createElement('img', { id: 'u15_img', className: 'img ', src: 'i/images/user_check_operation_reservation_page/u23.png' }),
            _react2.default.createElement(
              'div',
              { id: 'u16', className: 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement('span', null)
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u17', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u17_img', className: 'img ', src: 'i/images/index_home/u17.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u18', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u19', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u19_img', className: 'img ', src: 'i/images/query_result/u100.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u20', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '全部症状'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u21', className: 'ax_horizontal_line' },
          _react2.default.createElement('img', { id: 'u21_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u21_start' }),
          _react2.default.createElement('img', { id: 'u21_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u21_end' }),
          _react2.default.createElement('img', { id: 'u21_line', className: 'img ', src: 'i/images/index_home/u21_line.png', alt: 'u21_line' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u22', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u22_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u23', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '关节'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u24', className: 'ax_horizontal_line' },
          _react2.default.createElement('img', { id: 'u24_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u24_start' }),
          _react2.default.createElement('img', { id: 'u24_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u24_end' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u25', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u25_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u26', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '脊柱'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u27', className: 'ax_horizontal_line' },
          _react2.default.createElement('img', { id: 'u27_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u27_start' }),
          _react2.default.createElement('img', { id: 'u27_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u27_end' }),
          _react2.default.createElement('img', { id: 'u27_line', className: 'img ', src: 'i/images/index_home/u21_line.png', alt: 'u27_line' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u28', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u28_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u29', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '创伤'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u30', className: 'ax_horizontal_line' },
          _react2.default.createElement('img', { id: 'u30_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u30_start' }),
          _react2.default.createElement('img', { id: 'u30_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u30_end' }),
          _react2.default.createElement('img', { id: 'u30_line', className: 'img ', src: 'i/images/index_home/u21_line.png', alt: 'u30_line' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u31', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u31_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u32', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '手外'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u203', className: 'ax_horizontal_line' },
          _react2.default.createElement('img', { id: 'u203_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u203_start' }),
          _react2.default.createElement('img', { id: 'u203_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u203_end' }),
          _react2.default.createElement('img', { id: 'u203_line', className: 'img ', src: 'i/images/index_home/u21_line.png', alt: 'u203_line' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u204', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u204_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u207', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '小儿骨科'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u205', className: 'ax_horizontal_line' },
          _react2.default.createElement('img', { id: 'u205_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u205_start' }),
          _react2.default.createElement('img', { id: 'u205_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u205_end' }),
          _react2.default.createElement('img', { id: 'u205_line', className: 'img ', src: 'i/images/index_home/u21_line.png', alt: 'u205_line' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u206', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u206_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u208', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '骨与软组织肿瘤'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u33', className: 'ax_horizontal_line' },
          _react2.default.createElement('img', { id: 'u33_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u33_start' }),
          _react2.default.createElement('img', { id: 'u33_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u33_end' }),
          _react2.default.createElement('img', { id: 'u33_line', className: 'img ', src: 'i/images/query_result/u99_line.png', alt: 'u33_line' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u34', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u34_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u35', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '医生团队'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u36', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u36_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u37', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '医疗'
              ),
              _react2.default.createElement(
                'span',
                null,
                '机构'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u38', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u38_img', className: 'img ', src: 'i/images/index_home/u38.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u39', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u40', className: 'ax_shape' },
          _react2.default.createElement(
            'div',
            { id: 'u41', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u42', className: 'ax_image' },
          _react2.default.createElement('img', { id: 'u42_img', className: 'img ', src: 'i/images/index_home/u42.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u43', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u44', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u44_img', className: 'img ', src: 'i/images/index_home/u44.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u45', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u46', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u46_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u47', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '公告'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u48', className: 'ax_horizontal_line' },
          _react2.default.createElement('img', { id: 'u48_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u48_start' }),
          _react2.default.createElement('img', { id: 'u48_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u48_end' }),
          _react2.default.createElement('img', { id: 'u48_line', className: 'img ', src: 'i/images/index_home/u48_line.png', alt: 'u48_line' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u1000' },
          _react2.default.createElement(
            'p',
            null,
            notice1
          ),
          _react2.default.createElement(
            'p',
            null,
            notice2
          ),
          _react2.default.createElement(
            'p',
            null,
            notice3
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u55', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u55_img', className: 'img ', src: 'i/images/index_home/u55.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u56', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u57', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u57_img', className: 'img ', src: 'i/images/index_home/u55.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u58', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u59', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u59_img', className: 'img ', src: 'i/images/index_home/u55.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u60', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u61', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u61_img', className: 'img ', src: 'i/images/index_home/u61.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u62', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u63', className: 'ax_image' },
          _react2.default.createElement('img', { id: 'u63_img', className: 'img ', src: dline1.id }),
          _react2.default.createElement(
            'div',
            { id: 'u64', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u65', className: 'ax_h2' },
          _react2.default.createElement('img', { id: 'u65_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u66', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                dline1.realname
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u67', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u67_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u68', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '  ',
                dline1.hospital,
                '     ',
                dline1.title
              )
            )
          )
        ),
        _react2.default.createElement(
          _reactRouter.Link,
          { to: { pathname: '/user_reservation', query: dline1 } },
          _react2.default.createElement(
            'div',
            { id: 'u69', className: 'ax_shape' },
            _react2.default.createElement('img', { id: 'u69_img', className: 'img ', src: 'i/images/index_home/u69.png' }),
            _react2.default.createElement(
              'div',
              { id: 'u70', className: 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement(
                  'span',
                  null,
                  '立即预约'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u71', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u71_img', className: 'img ', src: 'i/images/index_home/u71.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u72', className: 'text' },
            _react2.default.createElement(
              'span',
              { id: 'u302' },
              this.shanchang(goodAt1)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u75', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u75_img', className: 'img ', src: 'i/images/index_home/u61.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u76', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u77', className: 'ax_image' },
          _react2.default.createElement('img', { id: 'u77_img', className: 'img ', src: dline2.id }),
          _react2.default.createElement(
            'div',
            { id: 'u78', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u79', className: 'ax_h2' },
          _react2.default.createElement('img', { id: 'u79_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u80', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '   ',
                dline2.realname
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u81', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u81_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u82', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                dline2.hospital,
                '    ',
                dline2.title
              )
            )
          )
        ),
        _react2.default.createElement(
          _reactRouter.Link,
          { to: { pathname: '/user_reservation', query: dline2 } },
          _react2.default.createElement(
            'div',
            { id: 'u83', className: 'ax_shape' },
            _react2.default.createElement('img', { id: 'u83_img', className: 'img ', src: 'i/images/index_home/u69.png' }),
            _react2.default.createElement(
              'div',
              { id: 'u84', className: 'text' },
              _react2.default.createElement(
                'p',
                null,
                '立即预约'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u85', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u85_img', className: 'img ', src: 'i/images/index_home/u71.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u86', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                this.shanchang(goodAt2)
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u89', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u89_img', className: 'img ', src: 'i/images/index_home/u61.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u90', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u91', className: 'ax_image' },
          _react2.default.createElement('img', { id: 'u91_img', className: 'img ', src: dline3.id }),
          _react2.default.createElement(
            'div',
            { id: 'u92', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u93', className: 'ax_h2' },
          _react2.default.createElement('img', { id: 'u93_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u94', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                dline3.realname
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u95', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u95_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u96', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                dline3.hospital,
                '     ',
                dline3.title
              )
            )
          )
        ),
        _react2.default.createElement(
          _reactRouter.Link,
          { to: { pathname: '/user_reservation', query: dline3 } },
          _react2.default.createElement(
            'div',
            { id: 'u97', className: 'ax_shape' },
            _react2.default.createElement('img', { id: 'u97_img', className: 'img ', src: 'i/images/index_home/u69.png' }),
            _react2.default.createElement(
              'div',
              { id: 'u98', className: 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement(
                  'span',
                  null,
                  '立即预约'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u99', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u99_img', className: 'img ', src: 'i/images/index_home/u71.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u100', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                this.shanchang(goodAt3)
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u102', className: 'text' },
          _react2.default.createElement(
            'p',
            null,
            _react2.default.createElement('span', null)
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u103', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u103_img', className: 'img ', src: 'i/images/index_home/u61.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u104', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u105', className: 'ax_image' },
          _react2.default.createElement('img', { id: 'u105_img', className: 'img ', src: dline4.id }),
          _react2.default.createElement(
            'div',
            { id: 'u106', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u107', className: 'ax_h2' },
          _react2.default.createElement('img', { id: 'u107_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u108', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                dline4.realname
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u109', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u109_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u110', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                dline4.hospital,
                '    ',
                dline4.title
              )
            )
          )
        ),
        _react2.default.createElement(
          _reactRouter.Link,
          { to: { pathname: '/user_reservation', query: dline4 } },
          _react2.default.createElement(
            'div',
            { id: 'u111', className: 'ax_shape' },
            _react2.default.createElement('img', { id: 'u111_img', className: 'img ', src: 'i/images/index_home/u69.png' }),
            _react2.default.createElement(
              'div',
              { id: 'u112', className: 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement(
                  'span',
                  null,
                  '立即预约'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u113', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u113_img', className: 'img ', src: 'i/images/index_home/u71.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u114', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                this.shanchang(goodAt4)
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u117', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u117_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u118', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '合作机构'
              )
            )
          )
        ),
        _react2.default.createElement(
          _reactRouter.Link,
          { to: '/institution_list' },
          _react2.default.createElement(
            'div',
            { id: 'u119', className: 'ax_paragraph' },
            _react2.default.createElement('img', { id: 'u119_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
            _react2.default.createElement(
              'div',
              { id: 'u120', className: 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement(
                  'span',
                  null,
                  '> '
                ),
                _react2.default.createElement(
                  'span',
                  null,
                  '> 更多'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u121', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u121_img', className: 'img ', src: 'i/images/index_home/u61.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u122', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u123', className: 'ax_h2' },
          _react2.default.createElement('img', { id: 'u123_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u124', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                line1.agencyname
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u125', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u125_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u126', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                line1.agencyclass,
                '医院      ',
                line1.agencyprovince
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u127', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u127_img', className: 'img ', src: 'i/images/index_home/u71.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u128', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '医保'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u129', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u129_img', className: 'img ', src: 'i/images/index_home/u73.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u130', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '可预约'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u131', className: 'ax_image' },
          _react2.default.createElement('img', { id: 'u131_img', className: 'img ', src: line1.agencyid }),
          _react2.default.createElement(
            'div',
            { id: 'u132', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u133', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u133_img', className: 'img ', src: 'i/images/index_home/u61.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u134', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u135', className: 'ax_h2' },
          _react2.default.createElement('img', { id: 'u135_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u136', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                line2.agencyname
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u137', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u137_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u138', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                line2.agencyclass,
                '医院      ',
                line2.agencyprovince
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u139', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u139_img', className: 'img ', src: 'i/images/index_home/u71.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u140', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '医保'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u141', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u141_img', className: 'img ', src: 'i/images/index_home/u73.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u142', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '可预约'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u143', className: 'ax_image' },
          _react2.default.createElement('img', { id: 'u143_img', className: 'img ', src: line2.agencyid }),
          _react2.default.createElement(
            'div',
            { id: 'u144', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u145', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u145_img', className: 'img ', src: 'i/images/index_home/u61.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u146', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u147', className: 'ax_h2' },
          _react2.default.createElement('img', { id: 'u147_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u148', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                line3.agencyname
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u149', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u149_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u150', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                line3.agencyclass,
                '医院      ',
                line3.agencyprovince
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u151', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u151_img', className: 'img ', src: 'i/images/index_home/u71.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u152', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '医保'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u153', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u153_img', className: 'img ', src: 'i/images/index_home/u73.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u154', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '可预约'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u155', className: 'ax_image' },
          _react2.default.createElement('img', { id: 'u155_img', className: 'img ', src: line3.agencyid }),
          _react2.default.createElement(
            'div',
            { id: 'u156', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u157', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u157_img', className: 'img ', src: 'i/images/index_home/u61.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u158', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u159', className: 'ax_h2' },
          _react2.default.createElement('img', { id: 'u159_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u160', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                line4.agencyname
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u161', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u161_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u162', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                line4.agencyclass,
                '医院      ',
                line4.agencyprovince
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u163', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u163_img', className: 'img ', src: 'i/images/index_home/u71.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u164', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '医保'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u165', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u165_img', className: 'img ', src: 'i/images/index_home/u73.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u166', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '可预约'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u167', className: 'ax_image' },
          _react2.default.createElement('img', { id: 'u167_img', className: 'img ', src: line4.agencyid }),
          _react2.default.createElement(
            'div',
            { id: 'u168', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u169', className: 'ax_horizontal_line' },
          _react2.default.createElement('img', { id: 'u169_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u169_start' }),
          _react2.default.createElement('img', { id: 'u169_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u169_end' }),
          _react2.default.createElement('img', { id: 'u169_line', className: 'img ', src: 'i/images/index_home/u169_line.png', alt: 'u169_line' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u170', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u170_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u171', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                _react2.default.createElement(
                  _reactRouter.Link,
                  { to: _auth2.default.get_config()['about_us_url'] },
                  '关于我们'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u172', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u172_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u173', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                _react2.default.createElement(
                  _reactRouter.Link,
                  { to: _auth2.default.get_config()['connect_us_url'] },
                  '联系我们'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u174', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u174_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u175', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '服务条款'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u889', className: 'ax_paragraph', style: {
              'position': 'absolute',
              'left': '40px',
              'top': '1049px',
              'width': '950px',
              'height': '135px',
              'color': '#ffffff'
            } },
          _react2.default.createElement('img', { src: 'i/images/index_home/bot.png' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u888', className: 'ax_paragraph', style: {
              'position': 'absolute',
              'left': '450px',
              'top': '1245px',
              'width': '57px',
              'height': '1px',
              'font-size': '14px',
              'color': '#666666'
            } },
          _react2.default.createElement('img', { id: 'u125_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u126', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                _react2.default.createElement(
                  'a',
                  { href: 'http://www.miitbeian.gov.cn' },
                  '           京ICP备案16059247号'
                )
              )
            ),
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                _react2.default.createElement('img', { src: 'http://www.beian.gov.cn/img/ghs.png' }),
                _react2.default.createElement(
                  'a',
                  { href: 'http://www.beian.gov.cn/portal/registerSystemInfo?recordcode=11010802022799' },
                  '京公网安备 11010802022799号'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u176', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u176_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u177', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '媒体更新'
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { id: 'u194', 'class': 'ax_html_button' },
            _react2.default.createElement('input', null)
          )
        ),
        _react2.default.createElement(
          _reactRouter.Link,
          { to: '/doctor_list' },
          _react2.default.createElement('div', { id: 'u178', className: 'ax_hot_spot' })
        ),
        _react2.default.createElement(
          _reactRouter.Link,
          { to: '/institution_list' },
          _react2.default.createElement('div', { id: 'u179', className: 'ax_hot_spot' })
        ),
        _react2.default.createElement('div', { id: 'u180', className: 'ax_hot_spot', onMouseOver: this.mouseOver1, onMouseOut: this.mouseOut1 }),
        _react2.default.createElement('div', { id: 'u181', className: 'ax_hot_spot', onMouseOver: this.mouseOver2, onMouseOut: this.mouseOut2 }),
        _react2.default.createElement('div', { id: 'u182', className: 'ax_hot_spot', onMouseOver: this.mouseOver3, onMouseOut: this.mouseOut3 }),
        _react2.default.createElement('div', { id: 'u183', className: 'ax_hot_spot', onMouseOver: this.mouseOver4, onMouseOut: this.mouseOut4 }),
        _react2.default.createElement('div', { id: 'u210', className: 'ax_hot_spot', onMouseOver: this.mouseOver5, onMouseOut: this.mouseOut5 }),
        _react2.default.createElement('div', { id: 'u211', className: 'ax_hot_spot', onMouseOver: this.mouseOver6, onMouseOut: this.mouseOut6 }),
        _react2.default.createElement(
          'div',
          { id: 'u184', className: 'ax_dynamic_panel' },
          _react2.default.createElement(
            'div',
            { id: 'u184_state0', className: 'panel_state', 'data-label': '关节疾病右菜单', onMouseOver: this.mouseOver1, onMouseOut: this.mouseOut1 },
            _react2.default.createElement(
              'div',
              { id: 'u184_state0_content', className: 'panel_state_content' },
              _react2.default.createElement(
                'div',
                { id: 'u185', className: 'ax_shape' },
                _react2.default.createElement('img', { id: 'u185_img', className: 'img ', src: 'i/images/index_home/u185.png' }),
                _react2.default.createElement(
                  'div',
                  { id: 'u186', className: 'text' },
                  _react2.default.createElement(
                    'p',
                    null,
                    _react2.default.createElement('span', null)
                  )
                )
              ),
              _react2.default.createElement(
                'div',
                { id: 'u187', className: 'ax_paragraph' },
                _react2.default.createElement('img', { id: 'u187_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                _react2.default.createElement(
                  'div',
                  { id: 'u188', className: 'text' },
                  _react2.default.createElement(
                    'p',
                    { style: { color: '#333333' } },
                    '髋关节  |  ',
                    _react2.default.createElement(
                      'a',
                      { id: 'u189', className: 'link', style: { color: '#0000FF' } },
                      _react2.default.createElement('span', { onClick: this.search.bind(this, '股骨头坏死') }),
                      '    ',
                      _react2.default.createElement('span', { onClick: this.search.bind(this, '髋关节骨性关节炎') }),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '髋关节发育不良') },
                        '髋关节发育不良'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '类风湿性髋关节炎') },
                        '类风湿性髋关节炎'
                      ),
                      _react2.default.createElement('br', null),
                      '               ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '强直性脊柱炎髋关节强直') },
                        '强直性脊柱炎髋关节强直'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '髋臼-股骨撞击综合症') },
                        '髋臼-股骨撞击综合症'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '髋关节置换术后感染') },
                        '髋关节置换术后感染'
                      ),
                      _react2.default.createElement('br', null),
                      '               ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '髋关节置换术后假体松动') },
                        '髋关节置换术后假体松动'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '膝关节骨性关节炎') },
                        '膝关节骨性关节炎'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '膝关节内外翻畸形') },
                        '膝关节内外翻畸形'
                      ),
                      _react2.default.createElement('br', null),
                      '               ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '类风湿性膝关节炎') },
                        '类风湿性膝关节炎'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '膝关节僵直') },
                        '膝关节僵直'
                      )
                    ),
                    _react2.default.createElement('span', { style: { color: '#333333' } })
                  ),
                  _react2.default.createElement(
                    'p',
                    { style: { color: '#333333' } },
                    '足踝     |  ',
                    _react2.default.createElement(
                      'a',
                      { className: 'link', style: { color: '#0000FF' } },
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '踝关节骨性关节炎') },
                        '踝关节骨性关节炎'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '踝关节创伤性关节炎') },
                        '踝关节创伤性关节炎'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '马蹄内翻足') },
                        '马蹄内翻足'
                      )
                    ),
                    _react2.default.createElement('span', { style: { color: '#333333' } })
                  )
                )
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { id: 'u184_state1', className: 'panel_state', 'data-label': '脊柱右菜单', onMouseOver: this.mouseOver2, onMouseOut: this.mouseOut2 },
            _react2.default.createElement(
              'div',
              { id: 'u184_state1_content', className: 'panel_state_content' },
              _react2.default.createElement(
                'div',
                { id: 'u190', className: 'ax_shape' },
                _react2.default.createElement('img', { id: 'u190_img', className: 'img ', src: 'i/images/index_home/u185.png' }),
                _react2.default.createElement(
                  'div',
                  { id: 'u191', className: 'text' },
                  _react2.default.createElement(
                    'p',
                    null,
                    _react2.default.createElement('span', null)
                  )
                )
              ),
              _react2.default.createElement(
                'div',
                { id: 'u192', className: 'ax_paragraph' },
                _react2.default.createElement('img', { id: 'u192_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                _react2.default.createElement(
                  'div',
                  { id: 'u193', className: 'text' },
                  _react2.default.createElement(
                    'p',
                    { style: { color: '#333333' } },
                    '脊柱     |  ',
                    _react2.default.createElement(
                      'a',
                      { className: 'link', style: { color: '#0000FF' } },
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '颈椎骨折伴或不伴脱位，颈髓损伤') },
                        '颈椎骨折伴或不伴脱位，颈髓损伤'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '颈椎间盘突出伴或不伴滑脱') },
                        '颈椎间盘突出伴或不伴滑脱'
                      ),
                      _react2.default.createElement('br', null),
                      '               ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '胸腰椎骨折伴或不伴脱位，脊髓损伤') },
                        '胸腰椎骨折伴或不伴脱位，脊髓损伤'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '胸腰椎间盘突出伴或不伴滑脱') },
                        '胸腰椎间盘突出伴或不伴滑脱'
                      ),
                      _react2.default.createElement('br', null),
                      '               ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '脊柱畸形矫正') },
                        '脊柱畸形矫正'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '脊柱结核') },
                        '脊柱结核'
                      )
                    ),
                    _react2.default.createElement('span', { style: { color: '#333333' } })
                  )
                )
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { id: 'u184_state2', className: 'panel_state', 'data-label': '创伤右菜单', onMouseOver: this.mouseOver3, onMouseOut: this.mouseOut3 },
            _react2.default.createElement(
              'div',
              { id: 'u184_state2_content', className: 'panel_state_content' },
              _react2.default.createElement(
                'div',
                { id: 'u197', className: 'ax_shape' },
                _react2.default.createElement('img', { id: 'u197_img', className: 'img ', src: 'i/images/index_home/u185.png' }),
                _react2.default.createElement(
                  'div',
                  { id: 'u198', className: 'text' },
                  _react2.default.createElement(
                    'p',
                    null,
                    _react2.default.createElement('span', null)
                  )
                )
              ),
              _react2.default.createElement(
                'div',
                { id: 'u195', className: 'ax_paragraph' },
                _react2.default.createElement('img', { id: 'u195_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                _react2.default.createElement(
                  'div',
                  { id: 'u196', className: 'text' },
                  _react2.default.createElement(
                    'p',
                    { style: { color: '#333333' } },
                    '创伤     |  ',
                    _react2.default.createElement(
                      'a',
                      { className: 'link', style: { color: '#0000FF' } },
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '四肢长骨干骨折') },
                        '四肢长骨干骨折'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '关节周围骨折') },
                        '关节周围骨折'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '老年髋部骨折') },
                        '老年髋部骨折'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '骨盆骨折') },
                        '骨盆骨折'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '骨折不愈合') },
                        '骨折不愈合'
                      ),
                      _react2.default.createElement('br', null),
                      '               ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '骨折术后感染') },
                        '骨折术后感染'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '骨折畸形愈合矫正') },
                        '骨折畸形愈合矫正'
                      )
                    ),
                    _react2.default.createElement('span', { style: { color: '#333333' } })
                  )
                )
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { id: 'u184_state3', className: 'panel_state', 'data-label': '手外右菜单', onMouseOver: this.mouseOver4, onMouseOut: this.mouseOut4 },
            _react2.default.createElement(
              'div',
              { id: 'u184_state3_content', className: 'panel_state_content' },
              _react2.default.createElement(
                'div',
                { id: 'u199', className: 'ax_shape' },
                _react2.default.createElement('img', { id: 'u199_img', className: 'img ', src: 'i/images/index_home/u185.png' }),
                _react2.default.createElement(
                  'div',
                  { id: 'u200', className: 'text' },
                  _react2.default.createElement(
                    'p',
                    null,
                    _react2.default.createElement('span', null)
                  )
                )
              ),
              _react2.default.createElement(
                'div',
                { id: 'u201', className: 'ax_paragraph' },
                _react2.default.createElement('img', { id: 'u201_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                _react2.default.createElement(
                  'div',
                  { id: 'u202', className: 'text' },
                  _react2.default.createElement(
                    'p',
                    { style: { color: '#333333' } },
                    '手外     |  ',
                    _react2.default.createElement(
                      'a',
                      { className: 'link', style: { color: '#0000FF' } },
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '臂丛神经损伤') },
                        '臂丛神经损伤'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '周围神经损伤') },
                        '周围神经损伤'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '手指再造') },
                        '手指再造'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '手部肿瘤') },
                        '手部肿瘤'
                      ),
                      '    '
                    ),
                    _react2.default.createElement('span', { style: { color: '#333333' } })
                  )
                )
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { id: 'u184_state4', className: 'panel_state', 'data-label': '小儿骨科右菜单', onMouseOver: this.mouseOver5, onMouseOut: this.mouseOut5 },
            _react2.default.createElement(
              'div',
              { id: 'u184_state4_content', className: 'panel_state_content' },
              _react2.default.createElement(
                'div',
                { id: 'u212', className: 'ax_shape' },
                _react2.default.createElement('img', { id: 'u212_img', className: 'img ', src: 'i/images/index_home/u185.png' }),
                _react2.default.createElement(
                  'div',
                  { id: 'u213', className: 'text' },
                  _react2.default.createElement(
                    'p',
                    null,
                    _react2.default.createElement('span', null)
                  )
                )
              ),
              _react2.default.createElement(
                'div',
                { id: 'u214', className: 'ax_paragraph' },
                _react2.default.createElement('img', { id: 'u214_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                _react2.default.createElement(
                  'div',
                  { id: 'u215', className: 'text' },
                  _react2.default.createElement(
                    'p',
                    { style: { color: '#333333' } },
                    '小儿骨科  |  ',
                    _react2.default.createElement(
                      'a',
                      { className: 'link', style: { color: '#0000FF' } },
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '小儿先天性髋关节脱位') },
                        '小儿先天性髋关节脱位'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '小儿骨折') },
                        '小儿骨折'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '小儿股骨头坏死') },
                        '小儿股骨头坏死'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '先天性关节畸形') },
                        '先天性关节畸形'
                      ),
                      '    '
                    ),
                    _react2.default.createElement('span', { style: { color: '#333333' } })
                  )
                )
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { id: 'u184_state5', className: 'panel_state', 'data-label': '骨与软组织肿瘤右菜单', onMouseOver: this.mouseOver6, onMouseOut: this.mouseOut6 },
            _react2.default.createElement(
              'div',
              { id: 'u184_state5_content', className: 'panel_state_content' },
              _react2.default.createElement(
                'div',
                { id: 'u216', className: 'ax_shape' },
                _react2.default.createElement('img', { id: 'u216_img', className: 'img ', src: 'i/images/index_home/u185.png' }),
                _react2.default.createElement(
                  'div',
                  { id: 'u217', className: 'text' },
                  _react2.default.createElement(
                    'p',
                    null,
                    _react2.default.createElement('span', null)
                  )
                )
              ),
              _react2.default.createElement(
                'div',
                { id: 'u218', className: 'ax_paragraph' },
                _react2.default.createElement('img', { id: 'u218_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                _react2.default.createElement(
                  'div',
                  { id: 'u219', className: 'text' },
                  _react2.default.createElement(
                    'p',
                    { style: { color: '#333333' } },
                    '骨与软组织肿瘤  |  ',
                    _react2.default.createElement(
                      'a',
                      { className: 'link', style: { color: '#0000FF' } },
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '骨肿瘤（恶性）') },
                        '骨肿瘤（恶性）'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '骨肿瘤（良性）') },
                        '骨肿瘤（良性）'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '软组织肿瘤（恶性）') },
                        '软组织肿瘤（恶性）'
                      ),
                      _react2.default.createElement('br', null),
                      '                              ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '软组织肿瘤（良性）') },
                        '软组织肿瘤（良性）'
                      ),
                      '    ',
                      _react2.default.createElement(
                        'span',
                        { onClick: this.search.bind(this, '骨肿瘤保肢治疗') },
                        '骨肿瘤保肢治疗'
                      ),
                      '    '
                    ),
                    _react2.default.createElement('span', { style: { color: '#333333' } })
                  )
                )
              )
            )
          )
        )
      )
    );
  }
}));
//地点选择下拉列表
exports.default = Index_home;

},{"../components/FontComponents/Header":3,"../components/FontComponents/Qbzz":4,"../components/PageContainer":19,"../components/auth":25,"amazeui-react":52,"react":356,"react-router":219}],35:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

var _auth = require('../components/auth');

var _auth2 = _interopRequireDefault(_auth);

var _reactRouter = require('react-router');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Institution_list = (0, _reactRouter.withRouter)(_react2.default.createClass({
    displayName: 'Institution_list',
    getInitialState: function getInitialState() {
        return {
            dataSource: [{
                agencydoctorid: 0,
                agencyprovince: "北京",
                agencydistrict: "朝阳区",
                agencyname: "北京市朝阳医院",
                excel: [],
                agencybed: 2,
                agencydiscount: "有优惠",
                agencyhot: "20.00",
                agencyyytime: 10,
                agencyclass: "三乙"
            }, {}, {}],
            leixing: '1', //or 专家团队 or 医疗机构
            didian: '', //or 北京上海广州赤峰
            check_yuyue: false,
            check_youyouhui: false,
            search_paixu: '1', //or人气 预约时间,
            nowpage: 1,
            totalpage: 10,
            keyword: ''

        };
    },
    query: function query(page) {
        var _this = this;

        _auth2.default.myact({ to: 'list.do',
            parms: [{ 'key': 'page', 'value': page }, { 'key': 'role', 'value': this.state.leixing }, { 'key': 'province', 'value': this.state.didian }, { 'key': 'paixu', 'value': this.state.search_paixu }, { 'key': 'disease', 'value': '' }]
        }, function (res) {
            _this.updateDataSource(res);
        });
        this.state.nowpage = page;
    },
    querynext: function querynext() {
        var nowpage = this.state.nowpage;
        var totalpage = this.state.totalpage;
        if (nowpage + 1 <= totalpage) {
            nowpage += 1;
            this.query(nowpage);
        }
    },
    querybefore: function querybefore() {

        var nowpage = this.state.nowpage;
        var totalpage = this.state.totalpage;
        if (nowpage - 1 >= 1) {
            nowpage -= 1;
            this.query(nowpage);
        }
    },
    componentDidMount: function componentDidMount() {
        this.query(1);
    },
    updateDataSource: function updateDataSource(data) {
        this.setState({
            dataSource: data
        });
        console.log(data);
    },
    kaifazhong: function kaifazhong(str) {
        console.log(str);
    },
    render: function render() {
        var _this2 = this;

        var page = '';
        var line1 = { excel: [] };
        var data = this.state.dataSource;
        if (data.length >= 1) {
            line1 = data[0];
        }
        var line2 = { excel: [] };
        if (data.length >= 2) {
            line2 = data[1];
        }
        var line3 = { excel: [] };

        if (data.length >= 3) {
            line3 = data[2];
        }
        var black = {
            color: 'black'
        };
        return _react2.default.createElement(
            _PageContainer2.default,
            null,
            _react2.default.createElement('link', { href: 'i/resources/css/axure_rp_page.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('link', { href: 'i/data/styles.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('link', { href: 'i/css/institution_list/styles.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement(
                'div',
                { id: 'base', className: '' },
                _react2.default.createElement(
                    'div',
                    { id: 'u0', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u0_img', className: 'img ', src: 'i/images/doctor_list/u0.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u1', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u2', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u2_img', className: 'img ', src: 'i/images/query_result/u0.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u3', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u4', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u4_img', className: 'img ', src: 'i/images/query_result/u4.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u5', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u6', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u6_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u7', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '类型：'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    _reactRouter.Link,
                    { to: '/institution_list' },
                    _react2.default.createElement(
                        'div',
                        { id: 'u8', className: 'ax_paragraph' },
                        _react2.default.createElement('img', { id: 'u8_img', className: 'img ', src: this.state.leixing == '1' ? 'i/images/doctor_list/u10.png' : 'i/images/institution_list/u10.png' }),
                        _react2.default.createElement(
                            'div',
                            { id: 'u9', className: 'text', style: { color: this.state.leixing == '1' ? 'white' : 'black' } },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '医疗机构'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    _reactRouter.Link,
                    { to: 'doctor_list' },
                    _react2.default.createElement(
                        'div',
                        { id: 'u10', className: 'ax_paragraph', style: { color: this.state.leixing == '2' ? 'white' : 'black' } },
                        _react2.default.createElement('img', { id: 'u10_img', className: 'img ', src: this.state.leixing == '2' ? 'i/images/doctor_list/u10.png' : 'i/images/institution_list/u10.png' }),
                        _react2.default.createElement(
                            'div',
                            { id: 'u11', className: 'text' },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '专家团队 '
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    _reactRouter.Link,
                    { to: '/query_result' },
                    _react2.default.createElement(
                        'div',
                        { id: 'u12', className: 'ax_paragraph' },
                        _react2.default.createElement('img', { id: 'u12_img', className: 'img ', src: this.state.leixing == '0' ? 'i/images/query_result/u14.png' : 'i/images/institution_list/u10.png' }),
                        _react2.default.createElement(
                            'div',
                            { id: 'u13', className: 'text', style: { color: this.state.leixing == '0' ? 'white' : 'black' } },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '不限'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u14', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u14_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u15', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '地点：'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u16', className: 'ax_paragraph', style: { color: this.state.didian == '北京' ? 'white' : 'black' }, onClick: function onClick() {
                            _this2.state.didian = '北京';_this2.query(_this2.state.nowpage);
                        } },
                    _react2.default.createElement('img', { id: 'u16_img', className: 'img ', src: this.state.didian == '北京' ? 'i/images/query_result/u14.png' : 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u17', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '北京'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u18', className: 'ax_paragraph', style: { color: this.state.didian == '' ? 'white' : 'black' }, onClick: function onClick() {
                            _this2.state.didian = '';_this2.query(_this2.state.nowpage);
                        } },
                    _react2.default.createElement('img', { id: 'u18_img', className: 'img ', src: this.state.didian == '' ? 'i/images/query_result/u14.png' : 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u19', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '不限'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u20', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u20_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u20_start' }),
                    _react2.default.createElement('img', { id: 'u20_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u20_end' }),
                    _react2.default.createElement('img', { id: 'u20_line', className: 'img ', src: 'i/images/user_home/u68_line.png', alt: 'u20_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u21', className: 'ax_checkbox' },
                    _react2.default.createElement(
                        'label',
                        { htmlFor: 'u21_input' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u22', className: 'text' },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '7日内可预约'
                                )
                            )
                        )
                    ),
                    _react2.default.createElement('input', { id: 'u21_input', type: 'checkbox', value: 'checkbox', checked: this.state.check_yuyue ? 'checked' : '', onClick: function onClick() {
                            _this2.state.check_yuyue = !_this2.state.check_yuyue;_this2.query(_this2.state.nowpage);
                        } })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u23', className: 'ax_checkbox' },
                    _react2.default.createElement(
                        'label',
                        { htmlFor: 'u23_input' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u24', className: 'text' },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '有优惠'
                                )
                            )
                        )
                    ),
                    _react2.default.createElement('input', { id: 'u23_input', type: 'checkbox', value: 'checkbox', checked: this.state.check_youyouhui ? 'checked' : '', onClick: function onClick() {
                            _this2.state.check_youyouhui = !_this2.state.check_youyouhui;_this2.query(_this2.state.nowpage);
                        } })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u25', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u25_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u26', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                { style: { color: this.state.search_paixu == '1' ? 'red' : 'black' }, onClick: function onClick() {
                                        _this2.state.search_paixu = '1';_this2.query(_this2.state.nowpage);
                                    } },
                                '智能'
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                '  '
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                ' |  ',
                                _react2.default.createElement(
                                    'span',
                                    {
                                        style: { color: this.state.search_paixu == '2' ? 'red' : 'black' }, onClick: function onClick() {
                                            _this2.state.search_paixu = '2';_this2.query(_this2.state.nowpage);
                                        } },
                                    '人气'
                                ),
                                '  |  ',
                                _react2.default.createElement(
                                    'span',
                                    {
                                        style: { color: this.state.search_paixu == '3' ? 'red' : 'black' }, onClick: function onClick() {
                                            _this2.state.search_paixu = '3';_this2.query(_this2.state.nowpage);
                                        }
                                    },
                                    '预约时间'
                                ),
                                '    '
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u29', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u29_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u30', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            { onClick: this.querybefore },
                            ' ',
                            _react2.default.createElement(
                                'span',
                                null,
                                '上一页'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u31', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u31_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u32', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            { onClick: this.querynext },
                            ' ',
                            _react2.default.createElement(
                                'span',
                                null,
                                '下一页'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u33', className: 'ax_text_field' },
                    _react2.default.createElement('input', { id: 'u33_input', ref: 'des', type: 'text' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u34', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u34_img', className: 'img ', src: 'i/images/query_result/u55.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u35', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '快速预约'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    _reactRouter.Link,
                    { to: '/index_home' },
                    _react2.default.createElement(
                        'div',
                        { id: 'u36', className: 'ax_shape' },
                        _react2.default.createElement('img', { id: 'u36_img', className: 'img ', src: 'i/images/user_check_operation_reservation_page/u23.png' }),
                        _react2.default.createElement(
                            'div',
                            { id: 'u37', className: 'text' },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement('span', null)
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u38', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u38_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u39', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '手术地点：'
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                '全国'
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                '  '
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u43', className: 'ax_shape',
                        onClick: function onClick() {
                            _this2.props.router.replace({ pathname: '/query_result', query: { query: _this2.refs.des.value } });
                        } },
                    _react2.default.createElement('img', { id: 'u43_img', className: 'img ', src: 'i/images/query_result/u64.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u44', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '搜索'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u45', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u45_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u45_start' }),
                    _react2.default.createElement('img', { id: 'u45_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u45_end' }),
                    _react2.default.createElement('img', { id: 'u45_line', className: 'img ', src: 'i/images/user_home/u68_line.png', alt: 'u45_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u46', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u46_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u46_start' }),
                    _react2.default.createElement('img', { id: 'u46_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u46_end' }),
                    _react2.default.createElement('img', { id: 'u46_line', className: 'img ', src: 'i/images/user_home/u68_line.png', alt: 'u46_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u47', className: 'ax_dynamic_panel' },
                    _react2.default.createElement(
                        'div',
                        { id: 'u47_state0', className: 'panel_state', 'data-label': 'State1' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u47_state0_content', className: 'panel_state_content' },
                            _react2.default.createElement(
                                'div',
                                { id: 'u48', className: 'ax_shape' },
                                _react2.default.createElement('img', { id: 'u48_img', className: 'img ', src: 'i/images/query_result/u81.png' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u49', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement('span', null)
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u50', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u50_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u51', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '创伤'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u52', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u52_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u53', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '脊椎'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u54', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u54_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u55', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '儿骨'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u56', className: 'ax_shape' },
                                _react2.default.createElement('img', { id: 'u56_img', className: 'img ', src: 'i/images/query_result/u89.png' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u57', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement('span', null)
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u58', className: 'ax_shape' },
                                _react2.default.createElement('img', { id: 'u58_img', className: 'img ', src: 'i/images/query_result/u89.png' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u59', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement('span', null)
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u60', className: 'ax_shape' },
                                _react2.default.createElement('img', { id: 'u60_img', className: 'img ', src: 'i/images/query_result/u89.png' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u61', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement('span', null)
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u62', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u62_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u63', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '关节'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u64', className: 'ax_shape' },
                                _react2.default.createElement('img', { id: 'u64_img', className: 'img ', src: 'i/images/query_result/u89.png' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u65', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement('span', null)
                                    )
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u66', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u66_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u66_start' }),
                    _react2.default.createElement('img', { id: 'u66_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u66_end' }),
                    _react2.default.createElement('img', { id: 'u66_line', className: 'img ', src: 'i/images/query_result/u99_line.png', alt: 'u66_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u67', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u67_img', className: 'img ', src: 'i/images/query_result/u100.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u68', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '全部症状'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u69', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u69_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u70', className: 'text' },
                        _react2.default.createElement(
                            _reactRouter.Link,
                            { to: '/doctor_list' },
                            ' ',
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '医生团队'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u71', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u71_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u72', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '合作机构'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u73', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u73_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u74', className: 'text' },
                        _react2.default.createElement(
                            _reactRouter.Link,
                            { to: '/index_home' },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '首页'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u75', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u75_img', className: 'img ', src: 'i/images/query_result/u108.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u76', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u77', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u77_img', className: 'img ', src: 'i/images/index_home/u13.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u78', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u79', className: 'ax_paragraph', style: { color: this.state.didian == '上海' ? 'white' : 'black' }, onClick: function onClick() {
                            _this2.state.didian = '上海';_this2.query(_this2.state.nowpage);
                        } },
                    _react2.default.createElement('img', { id: 'u79_img', className: 'img ', src: this.state.didian == '上海' ? 'i/images/query_result/u14.png' : 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u80', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '上海'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u81', className: 'ax_paragraph', style: { color: this.state.didian == '广州' ? 'black' : 'black' }, onClick: function onClick() {
                            _this2.state.didian = '广州';_this2.query(_this2.state.nowpage);
                        } },
                    _react2.default.createElement('img', { id: 'u81_img', className: 'img ', src: this.state.didian == '广州' ? 'i/images/query_result/u14.png' : 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u82', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '广州'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u83', className: 'ax_paragraph', style: { color: this.state.didian == '赤峰' ? 'white' : 'black' }, onClick: function onClick() {
                            _this2.state.didian = '赤峰';_this2.query(_this2.state.nowpage);
                        } },
                    _react2.default.createElement('img', { id: 'u83_img', className: 'img ', src: this.state.didian == '赤峰' ? 'i/images/query_result/u14.png' : 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u84', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '赤峰'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u85', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u85_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u86', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            { className: 'u129' },
                            _react2.default.createElement('span', { className: 'u124' }),
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                '  '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                line1.agencyname
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u125' },
                                ' ， '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u130' },
                                '人气指数 '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u130' },
                                line1.agencyhot
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            { className: 'u128' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u127' },
                                '地点：'
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u127' },
                                line1.agencydistrict
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u127' },
                                '  '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u126' },
                                line1.agencyclass
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            { className: 'u128' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u127' },
                                '可预约专家：知名三甲医院骨科高职及以上'
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            { className: 'u128' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u127' },
                                '最快手术时间：',
                                line1.agencyyytime
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            { className: 'u128' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u127' },
                                '床位：',
                                line1.agencybed
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u87', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u87_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u87_start' }),
                    _react2.default.createElement('img', { id: 'u87_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u87_end' }),
                    _react2.default.createElement('img', { id: 'u87_line', className: 'img ', src: 'i/images/query_result/u43_line.png', alt: 'u87_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u88', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u88_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u89', className: 'text' },
                        line1.agencydiscount != undefined ? _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '优惠: '
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                line1.agencydiscount
                            )
                        ) : ''
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u90', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u90_img', className: 'img ', src: 'i/images/query_result/u68.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u91', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '立即预约'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u92', className: 'ax_image' },
                    _react2.default.createElement('img', { id: 'u92_img', className: 'img ', src: line3.id }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u93', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u94', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u94_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u95', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            { className: 'u129' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                line2.agencyname
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u125' },
                                ' ， '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u130' },
                                '人气指数 '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u130' },
                                line2.agencyhot
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            { className: 'u128' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u127' },
                                '地点：'
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u127' },
                                line2.agencyprovince
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u127' },
                                line2.agencydistrict
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u127' },
                                '区'
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u127' },
                                '  '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u126' },
                                line2.agencyclass
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            { className: 'u128' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u127' },
                                '可预约专家：知名三甲医院骨科高职及以上'
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            { className: 'u128' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u127' },
                                '最快手术时间：',
                                line2.agencyyytime
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            { className: 'u128' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u127' },
                                '床位：',
                                line2.agencybed
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u96', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u96_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u96_start' }),
                    _react2.default.createElement('img', { id: 'u96_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u96_end' }),
                    _react2.default.createElement('img', { id: 'u96_line', className: 'img ', src: 'i/images/query_result/u43_line.png', alt: 'u96_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u97', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u97_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u98', className: 'text' },
                        line1.agencydiscount != undefined ? _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '优惠: '
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                line1.agencydiscount
                            )
                        ) : ''
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u99', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u99_img', className: 'img ', src: 'i/images/query_result/u68.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u100', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '立即预约'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u101', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u101_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u102', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            { className: 'u129' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                line3.agencyname
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                '  '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                '医院'
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u125' },
                                ' ， '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u130' },
                                '人气指数 '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u130' },
                                line3.agencyhot
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            { className: 'u128' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u127' },
                                '地点：'
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u127' },
                                line3.agencyprovince
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u127' },
                                line3.agencydistrict
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u127' },
                                '  '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u126' },
                                line3.agencyclass
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            { className: 'u128' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u127' },
                                '可预约专家：知名三甲医院骨科高职及以上'
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            { className: 'u128' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u127' },
                                '最快手术时间：',
                                line3.agencyyytime
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            { className: 'u128' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u127' },
                                '床位：',
                                line3.agencybed
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u103', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u103_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u103_start' }),
                    _react2.default.createElement('img', { id: 'u103_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u103_end' }),
                    _react2.default.createElement('img', { id: 'u103_line', className: 'img ', src: 'i/images/query_result/u43_line.png', alt: 'u103_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u104', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u104_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u105', className: 'text' },
                        line1.agencydiscount != undefined ? _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '优惠: '
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                line1.agencydiscount
                            )
                        ) : ''
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u106', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u106_img', className: 'img ', src: 'i/images/query_result/u68.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u107', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '立即预约'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u108', className: 'ax_image' },
                    _react2.default.createElement('img', { id: 'u108_img', className: 'img ', src: line2.id }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u109', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u110', className: 'ax_image' },
                    _react2.default.createElement('img', { id: 'u110_img', className: 'img ', src: line1.id }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u111', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    _reactRouter.Link,
                    { to: '/index_home' },
                    _react2.default.createElement('div', { id: 'u112', className: 'ax_hot_spot' })
                ),
                _react2.default.createElement(
                    _reactRouter.Link,
                    { to: '/doctor_list' },
                    _react2.default.createElement('div', { id: 'u113', className: 'ax_hot_spot' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u114', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u114_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u114_start' }),
                    _react2.default.createElement('img', { id: 'u114_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u114_end' }),
                    _react2.default.createElement('img', { id: 'u114_line', className: 'img ', src: 'i/images/user_home/u68_line.png', alt: 'u114_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u115', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u115_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u116', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                _react2.default.createElement(
                                    _reactRouter.Link,
                                    { to: _auth2.default.get_config()['about_us_url'] },
                                    '关于我们'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u117', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u117_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u118', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                _react2.default.createElement(
                                    _reactRouter.Link,
                                    { to: _auth2.default.get_config()['connect_us_url'] },
                                    '联系我们'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u119', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u119_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u120', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '服务条款'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u888', className: 'ax_paragraph', style: {
                            'position': 'absolute',
                            'left': '450px',
                            'top': '998px',
                            'width': '200px',
                            'height': '1px',
                            'font-size': '14px',
                            'color': '#666666'
                        } },
                    _react2.default.createElement('img', { id: 'u125_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u126', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                _react2.default.createElement(
                                    'a',
                                    { href: 'http://www.miitbeian.gov.cn' },
                                    '           京ICP备案16059247号'
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                _react2.default.createElement('img', { src: 'http://www.beian.gov.cn/img/ghs.png' }),
                                _react2.default.createElement(
                                    'a',
                                    { href: 'http://www.beian.gov.cn/portal/registerSystemInfo?recordcode=11010802022799' },
                                    '京公网安备 11010802022799号'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u121', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u121_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u122', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '媒体更新'
                            )
                        )
                    ),
                    _react2.default.createElement(
                        'div',
                        { id: 'haha' },
                        _react2.default.createElement('input', null)
                    )
                )
            )
        );
    }
}));
exports.default = Institution_list;

},{"../components/PageContainer":19,"../components/auth":25,"react":356,"react-router":219}],36:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

var _reactDom = require('react-dom');

var _FontComponents = require('../components/FontComponents');

var _amazeuiReact = require('amazeui-react');

var _auth = require('../components/auth');

var _auth2 = _interopRequireDefault(_auth);

var _reactRouter = require('react-router');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var path = "i/companyLogo.png";
var iconUser = _react2.default.createElement(_amazeuiReact.Icon, { icon: 'user' });
var iconPwd = _react2.default.createElement(_amazeuiReact.Icon, { icon: 'lock' });
var FindPassword = _react2.default.createClass({
  displayName: 'FindPassword',
  render: function render() {
    return _react2.default.createElement(
      _amazeuiReact.Grid,
      null,
      _react2.default.createElement(_amazeuiReact.Col, { sm: 4 }),
      _react2.default.createElement(
        _amazeuiReact.Col,
        { sm: 4 },
        _react2.default.createElement(
          'p',
          null,
          _react2.default.createElement(
            'span',
            null,
            _react2.default.createElement(
              'a',
              { href: '#' },
              ' 找回密码'
            )
          )
        )
      )
    );
  }
});
var role_index = {
  '5': 'user_manage',
  '4': 'index_home',
  '3': 'index_home',
  '2': 'order',
  '1': 'index_home',
  '0': 'index_home'
};
var Login = (0, _reactRouter.withRouter)(_react2.default.createClass({
  displayName: 'Login',
  getInitialState: function getInitialState() {
    return {
      error: false
    };
  },

  handleSubmit: function handleSubmit(e) {
    var _this = this;

    e.preventDefault();
    var email = this.refs.email.value;
    var pass = this.refs.pass.value;
    var role = '1';
    if (!email || !pass) {
      alert("用户名或密码不可为空");
    } else {
      _auth2.default.logout();
      _auth2.default.user_login(email, pass, function (loggedIn) {
        if (!loggedIn) {

          console.log(email + pass);
          return _this.setState({ error: true });
        }

        var location = _this.props.location;


        if (location.state && location.state.nextPathname) {
          _this.props.router.replace(location.state.nextPathname);
        } else {
          _this.props.router.replace(role_index[_auth2.default.getRole()]);
        }
      });
    }
  },
  render: function render() {
    return _react2.default.createElement(
      'div',
      null,
      _react2.default.createElement(
        'div',
        { id: 'base', 'class': '' },
        _react2.default.createElement('link', { href: 'i/resources/css/axure_rp_page.css', type: 'text/css', rel: 'stylesheet' }),
        _react2.default.createElement('link', { href: 'i/data/styles.css', type: 'text/css', rel: 'stylesheet' }),
        _react2.default.createElement('link', { href: 'i/css/user_login_page/styles.css', type: 'text/css', rel: 'stylesheet' }),
        _react2.default.createElement(
          'form',
          { onSubmit: this.handleSubmit },
          _react2.default.createElement(
            'div',
            { id: 'u0', 'class': 'ax_shape' },
            _react2.default.createElement('img', { id: 'u0_img', 'class': 'img ', src: 'i/images/user_login_page/u0.png' }),
            _react2.default.createElement(
              'div',
              { id: 'u1', 'class': 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement('span', null)
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { id: 'u2', 'class': 'ax_h1' },
            _react2.default.createElement('img', { id: 'u2_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
            _react2.default.createElement(
              'div',
              { id: 'u3', 'class': 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement(
                  'span',
                  null,
                  '欢迎登录'
                )
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { id: 'u6', 'class': 'ax_shape' },
            _react2.default.createElement('img', { id: 'u6_img', 'class': 'img ', src: 'i/images/user_login_page/u6.png' }),
            _react2.default.createElement(
              'div',
              { id: 'u7', 'class': 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement('span', null)
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { id: 'u8', 'class': 'ax_paragraph' },
            _react2.default.createElement('img', { id: 'u8_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
            _react2.default.createElement(
              _reactRouter.Link,
              { to: '/register_user' },
              _react2.default.createElement(
                'div',
                { id: 'u9', 'class': 'text' },
                _react2.default.createElement(
                  'p',
                  null,
                  _react2.default.createElement(
                    'span',
                    null,
                    '立即注册'
                  )
                )
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { id: 'u10', 'class': 'ax_text_field' },
            _react2.default.createElement('input', { ref: 'email', id: 'u10_input', type: 'text' })
          ),
          _react2.default.createElement(
            'div',
            { id: 'u11', 'class': 'ax_text_field' },
            _react2.default.createElement('input', { ref: 'pass', id: 'u11_input', type: 'password' })
          ),
          _react2.default.createElement(
            'div',
            { id: 'u12', 'class': 'ax_shape' },
            _react2.default.createElement('img', { id: 'u12_img', 'class': 'img ', src: 'i/images/user_login_page/u12.png' }),
            _react2.default.createElement(
              'div',
              { id: 'u13', 'class': 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement(
                  'span',
                  null,
                  '账号'
                )
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { id: 'u14', 'class': 'ax_shape' },
            _react2.default.createElement('img', { id: 'u14_img', 'class': 'img ', src: 'i/images/user_login_page/u12.png' }),
            _react2.default.createElement(
              'div',
              { id: 'u15', 'class': 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement(
                  'span',
                  null,
                  '密码'
                )
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { id: 'u16', 'class': 'ax_paragraph' },
            _react2.default.createElement('img', { id: 'u16_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
            _react2.default.createElement(
              'div',
              { id: 'u17', 'class': 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement(
                  'span',
                  null,
                  '找回密码'
                )
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { id: 'u18', 'class': 'ax_paragraph' },
            _react2.default.createElement('img', { id: 'u18_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
            _react2.default.createElement(
              'div',
              { id: 'u19', 'class': 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement('span', null)
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { id: 'u20', 'class': 'ax_paragraph' },
            _react2.default.createElement('img', { id: 'u20_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
            _react2.default.createElement(
              'div',
              { id: 'u21', 'class': 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement('span', null)
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { id: 'u22', 'class': 'ax_h3' },
            _react2.default.createElement('img', { id: 'u22_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
            _react2.default.createElement(
              'div',
              { id: 'u23', 'class': 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement(
                  'span',
                  null,
                  '会员登录'
                )
              )
            )
          ),
          _react2.default.createElement(
            _reactRouter.Link,
            { to: '/index_home' },
            _react2.default.createElement(
              'div',
              { id: 'u24', 'class': 'ax_shape' },
              _react2.default.createElement('img', { id: 'u24_img', 'class': 'img ', src: 'i/images/user_check_operation_reservation_page/u23.png' }),
              _react2.default.createElement(
                'div',
                { id: 'u25', 'class': 'text' },
                _react2.default.createElement(
                  'p',
                  null,
                  _react2.default.createElement('span', null)
                )
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { id: 'u26', 'class': 'ax_shape', onClick: this.handleSubmit },
            _react2.default.createElement('img', { id: 'u26_img', 'class': 'img ', src: 'i/images/user_login_page/u26.png' }),
            _react2.default.createElement(
              'div',
              { id: 'u27', 'class': 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement(
                  'span',
                  null,
                  '登录'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u170', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u170_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u171', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                _react2.default.createElement(
                  _reactRouter.Link,
                  { to: _auth2.default.get_config()['about_us_url'] },
                  '关于我们'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u172', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u172_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u173', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                _react2.default.createElement(
                  _reactRouter.Link,
                  { to: _auth2.default.get_config()['connect_us_url'] },
                  '联系我们'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u174', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u174_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u175', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '服务条款'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u888', className: 'ax_paragraph', style: {
              'position': 'absolute',
              'left': '450px',
              'top': '530px',
              'width': '200px',
              'height': '1px',
              'font-size': '14px',
              'color': '#666666'
            } },
          _react2.default.createElement('img', { id: 'u125_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u126', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                _react2.default.createElement(
                  'a',
                  { href: 'http://www.miitbeian.gov.cn' },
                  '           京ICP备案16059247号'
                )
              )
            ),
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                _react2.default.createElement('img', { src: 'http://www.beian.gov.cn/img/ghs.png' }),
                _react2.default.createElement(
                  'a',
                  { href: 'http://www.beian.gov.cn/portal/registerSystemInfo?recordcode=11010802022799' },
                  '京公网安备 11010802022799号'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u176', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u176_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u177', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '媒体更新'
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { id: 'u194', 'class': 'ax_html_button' },
            _react2.default.createElement('input', null)
          )
        )
      )
    );
  }
}));
exports.default = Login;

},{"../components/FontComponents":6,"../components/PageContainer":19,"../components/auth":25,"amazeui-react":52,"react":356,"react-dom":189,"react-router":219}],37:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

var _reactDom = require('react-dom');

var _amazeuiReact = require('amazeui-react');

var _auth = require('../components/auth');

var _auth2 = _interopRequireDefault(_auth);

var _reactRouter = require('react-router');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Logout = _react2.default.createClass({
  displayName: 'Logout',
  componentDidMount: function componentDidMount() {
    _auth2.default.logout();
  },
  render: function render() {
    return _react2.default.createElement(
      'p',
      null,
      'You are now logged out'
    );
  }
});
exports.default = Logout;

},{"../components/PageContainer":19,"../components/auth":25,"amazeui-react":52,"react":356,"react-dom":189,"react-router":219}],38:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

var _amazeuiReact = require('amazeui-react');

var _auth = require('../components/auth');

var _auth2 = _interopRequireDefault(_auth);

var _reactRouter = require('react-router');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var ManageRoot = _react2.default.createClass({
    displayName: 'ManageRoot',
    getInitialState: function getInitialState() {
        return {
            dataSource: { re: [{ "user_name": "ffff", "user_group": "iiiii", "user_mobile": "111", "user_mail": "qqq", "state": "0" }], totalpage: 1 },
            nowpage: 1,
            totalpage: 1
        };
    },
    query: function query() {
        var _this = this;

        var page = arguments.length <= 0 || arguments[0] === undefined ? this.state.nowpage : arguments[0];

        _auth2.default.myact({ to: 'manager/accountlist.do',
            parms: [{ 'key': 'page', 'value': page }]
        }, function (res) {
            _this.updateDataSource(res);
        });
        this.setState({ nowpage: page });
    },
    querynext: function querynext() {
        var nowpage = this.state.nowpage;
        var totalpage = this.state.totalpage;
        if (nowpage + 1 <= totalpage) {
            nowpage += 1;
            this.query(nowpage);
        }
    },
    querybefore: function querybefore() {

        var nowpage = this.state.nowpage;
        var totalpage = this.state.totalpage;
        if (nowpage - 1 >= 1) {
            nowpage -= 1;
            this.query(nowpage);
        }
    },
    deletelist: function deletelist(id) {
        _auth2.default.myact({ to: 'manager/deleteaccount.do',
            parms: [{ 'key': 'id', 'value': id }]
        }, function (res) {
            console.log(res);
        });
        this.query();
    },
    disable: function disable(id) {
        console.log(id);
        _auth2.default.myact({ to: 'manager/disableaccount.do',
            parms: [{ 'key': 'id', 'value': id[0] }, { 'key': 'state', 'value': id[1] }]
        }, function (res) {
            console.log(res);
        });
        this.query();
    },
    componentDidMount: function componentDidMount() {
        this.query(1);
    },
    updateDataSource: function updateDataSource(data) {
        this.setState({
            dataSource: data,
            totalpage: data.totalpage
        });
        console.log(data);
    },
    judege: function judege(intState) {
        var state = "";
        if (intState == 0) {
            state = "禁用";
        } else if (intState == 1) {
            state = "启用";
        } else {
            state = "";
        }
        return state;
    },
    judege1: function judege1(intState) {
        var state = "";
        if (intState == 0) {
            state = "启用";
        } else if (intState == 1) {
            state = "禁用";
        } else {
            state = "";
        }
        return state;
    },

    handleSubmit: function handleSubmit(e) {
        _auth2.default.myact({ to: '/manager/insertaccount.do',
            parms: [{ key: 'username', value: this.refs.username.value }, { key: 'password', value: this.refs.password.value }, { key: 'realname', value: this.refs.realname.value }, { key: 'mobile', value: this.refs.mobile.value }, { key: 'mail', value: this.refs.mail.value }, { key: 'usergroup', value: this.refs.usergroup.value }]
        }, function (res) {
            Console.log(usergroup);
        });
    },
    change: function change(id) {
        console.log(id);
        _auth2.default.myact({ to: '/manager/changeaccount.do',
            parms: [{ 'key': 'id', 'value': id }, { key: 'username', value: this.refs.un.value }, { key: 'usergroup', value: this.refs.ug.value }, { key: 'mobile', value: this.refs.um.value }, { key: 'mail', value: this.refs.uma.value }, { key: 'state', value: this.refs.ps.value }]
        }, function (res) {
            Console.log(username);
        });
        this.query();
    },
    change1: function change1() {
        var data = this.state.dataSource;
        var username = data.re[0].user_name;
        var mobile = data.re[0].user_mobile;
        var mail = data.re[0].user_mail;
        var usergroup = data.re[0].user_group;
        console.log(name);
        document.getElementById("u60_input").value = username;
        //    document.getElementById("u60_input").value=mobile;
        //    document.getElementById("u60_input").value=mail;
        //    document.getElementById("u60_input").value=usergroup;

        //   console.log(id)
        //   auth.myact({to:'/manager/findaccountbyid.do',
        //            parms:[
        //            {'key':'id','value':id},
        //            ]
        //             },(res)=>{
        //                 Console.log(res)
        //             });
        //             this.query()
        //   var aa = document.getElementById("textfiled").value;
        //   console.l
    },
    change2: function change2() {},
    render: function render() {
        var _this2 = this;

        var data = this.state.dataSource;
        var person = data.re[0];
        var person2 = '';

        if (data.re.length >= 2) {
            var person2 = data.re[1];
        }
        //console.log(person2.state)
        return _react2.default.createElement(
            _PageContainer2.default,
            null,
            _react2.default.createElement('link', { href: 'i/resources/css/axure_rp_page.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('link', { href: 'i/data/styles.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('link', { href: 'i/css/web_manage_page_root/styles.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement(
                'div',
                { id: 'base', className: '' },
                _react2.default.createElement(
                    'div',
                    { id: 'u0', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u0_start', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif', alt: 'u0_start' }),
                    _react2.default.createElement('img', { id: 'u0_end', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif', alt: 'u0_end' }),
                    _react2.default.createElement('img', { id: 'u0_line', className: 'img ', src: 'i/images/web_manage_page_root/u0_line.png', alt: 'u0_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u1', className: 'ax_h1' },
                    _react2.default.createElement('img', { id: 'u1_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u2', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '谷雨医生平台系统管理'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u3', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u3_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u4', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                { className: 'hi' },
                                '您好，Admin    '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'quit' },
                                '退出'
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'hi' },
                                '    '
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u5', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u5_start', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif', alt: 'u5_start' }),
                    _react2.default.createElement('img', { id: 'u5_end', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif', alt: 'u5_end' }),
                    _react2.default.createElement('img', { id: 'u5_line', className: 'img ', src: 'i/images/web_manage_page_root/u0_line.png', alt: 'u5_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u6', className: 'ax_dynamic_panel', 'data-label': '系统管理' },
                    _react2.default.createElement(
                        'div',
                        { id: 'u6_state0', className: 'panel_state', 'data-label': '管理账号' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u6_state0_content', className: 'panel_state_content' },
                            _react2.default.createElement(
                                'div',
                                { id: 'u7', className: 'ax_table' },
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u8', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u8_img', className: 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u9', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '用户名'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u10', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u10_img', className: 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u11', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '所属组'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u12', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u12_img', className: 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u13', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '电话'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u14', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u14_img', className: 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u15', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '电子邮件'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u16', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u16_img', className: 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u17', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '状态'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u18', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u18_img', className: 'img ', src: 'i/images/web_manage_page_root/u18.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u19', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '操作'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u20', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u20_img', className: 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u21', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                person.user_name
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u22', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u22_img', className: 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u23', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                person.user_group
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u24', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u24_img', className: 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u25', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                person.user_mobile
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u26', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u26_img', className: 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u27', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                person.user_mail
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u28', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u28_img', className: 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u29', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                this.judege(person.state)
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u30', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u30_img', className: 'img ', src: 'i/images/web_manage_page_root/u30.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u31', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                person.user_name != undefined ? _react2.default.createElement(
                                                    'p',
                                                    null,
                                                    _react2.default.createElement(
                                                        'a',
                                                        { onClick: this.deletelist.bind(this, person.user_id) },
                                                        '删除'
                                                    ),
                                                    '/',
                                                    _react2.default.createElement(
                                                        'a',
                                                        { onClick: this.disable.bind(this, [person.user_id, person.state]) },
                                                        this.judege1(person.state)
                                                    ),
                                                    '/',
                                                    _react2.default.createElement(
                                                        'a',
                                                        { onClick: this.change1 },
                                                        '修改'
                                                    )
                                                ) : ''
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u32', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u32_img', className: 'img ', src: 'i/images/web_manage_page_root/u32.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u33', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                person2.user_name
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u34', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u34_img', className: 'img ', src: 'i/images/web_manage_page_root/u32.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u35', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                person2.user_group
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u36', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u36_img', className: 'img ', src: 'i/images/web_manage_page_root/u32.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u37', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                person2.user_mobile
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u38', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u36_img', className: 'img ', src: 'i/images/web_manage_page_root/u32.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u37', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                person2.user_mail
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u40', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u40_img', className: 'img ', src: 'i/images/web_manage_page_root/u32.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u41', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                this.judege(person2.state)
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u42', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u42_img', className: 'img ', src: 'i/images/web_manage_page_root/u42.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u43', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                person2.user_name != undefined ? _react2.default.createElement(
                                                    'p',
                                                    null,
                                                    _react2.default.createElement(
                                                        'a',
                                                        { onClick: this.deletelist.bind(this, person2.user_id) },
                                                        '删除'
                                                    ),
                                                    '/',
                                                    _react2.default.createElement(
                                                        'a',
                                                        { onClick: this.disable.bind(this, [person2.user_id, person2.state]) },
                                                        this.judege1(person2.state)
                                                    ),
                                                    '/',
                                                    _react2.default.createElement(
                                                        'a',
                                                        { onClick: this.change.bind(this, person2.user_id) },
                                                        '修改'
                                                    )
                                                ) : ''
                                            )
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u44', className: 'ax_horizontal_line' },
                                _react2.default.createElement('img', { id: 'u44_start', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif', alt: 'u44_start' }),
                                _react2.default.createElement('img', { id: 'u44_end', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif', alt: 'u44_end' }),
                                _react2.default.createElement('img', { id: 'u44_line', className: 'img ', src: 'i/images/web_manage_page_root/u0_line.png', alt: 'u44_line' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u45', className: 'ax_h4' },
                                _react2.default.createElement('img', { id: 'u45_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u46', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '所有成员'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u47', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u47_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u48', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        { onClick: this.querybefore },
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '上一页'
                                        )
                                    ),
                                    _react2.default.createElement(
                                        'p',
                                        { onClick: this.querynext },
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '下一页'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u49', className: 'ax_h4' },
                                _react2.default.createElement('img', { id: 'u49_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u50', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '增加成员'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u51', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u51_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u52', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '账号'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u53', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u53_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u54', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '密码'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u55', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u55_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u56', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '确认密码'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u57', className: 'ax_droplist' },
                                _react2.default.createElement(
                                    'select',
                                    { id: 'u57_input', ref: 'usergroup' },
                                    _react2.default.createElement(
                                        'option',
                                        { selected: true, value: '手术助理-骨科' },
                                        '手术助理-骨科'
                                    ),
                                    _react2.default.createElement(
                                        'option',
                                        { value: '客服' },
                                        '客服'
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u58', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u58_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u59', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '所属组'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u60', className: 'ax_text_field' },
                                _react2.default.createElement('input', { id: 'u60_input', ref: 'username', type: 'text' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u61', className: 'ax_text_field' },
                                _react2.default.createElement('input', { id: 'u61_input', ref: 'password', type: 'password' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u62', className: 'ax_text_field' },
                                _react2.default.createElement('input', { id: 'u62_input', ref: 'password', type: 'password' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u63', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u63_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u64', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '姓名'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u65', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u65_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u66', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '电话'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u67', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u67_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u68', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '电子邮件'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u69', className: 'ax_text_field' },
                                _react2.default.createElement('input', { id: 'u69_input', ref: 'realname', type: 'text' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u70', className: 'ax_text_field' },
                                _react2.default.createElement('input', { id: 'u70_input', ref: 'mobile', type: 'text' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u71', className: 'ax_text_field' },
                                _react2.default.createElement('input', { id: 'u71_input', ref: 'mail', type: 'text' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u72', className: 'ax_html_button' },
                                _react2.default.createElement(
                                    'p',
                                    { onClick: this.change },
                                    _react2.default.createElement('input', { id: 'u72_input', type: 'submit', value: '修改' })
                                ),
                                _react2.default.createElement(
                                    'p',
                                    { id: 'u209', 'class': 'ax_html_button' },
                                    _react2.default.createElement('input', null)
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u73', className: 'ax_html_button' },
                                _react2.default.createElement(
                                    'p',
                                    { onClick: this.handleSubmit },
                                    _react2.default.createElement('input', { id: 'u73_input', type: 'submit', value: '提交' })
                                ),
                                _react2.default.createElement(
                                    'p',
                                    { id: 'u210', 'class': 'ax_html_button' },
                                    _react2.default.createElement('input', null)
                                )
                            )
                        )
                    ),
                    _react2.default.createElement(
                        'div',
                        { id: 'u6_state1', className: 'panel_state', 'data-label': '账户管理' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u6_state1_content', className: 'panel_state_content' },
                            _react2.default.createElement(
                                'div',
                                { id: 'u74', className: 'ax_table' },
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u75', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u75_img', className: 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u76', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '用户名'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u77', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u77_img', className: 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u78', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '账户类型'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u79', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u79_img', className: 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u80', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '电话'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u81', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u81_img', className: 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u82', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '上次登录时间'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u83', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u83_img', className: 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u84', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '状态'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u85', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u85_img', className: 'img ', src: 'i/images/web_manage_page_root/u18.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u86', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '操作'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u87', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u87_img', className: 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u88', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '海涛'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u89', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u89_img', className: 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u90', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '患者'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u91', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u91_img', className: 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u92', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '13912345678'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u93', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u93_img', className: 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u94', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '2016-3-8 23：15：00'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u95', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u95_img', className: 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u96', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '启用'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u97', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u97_img', className: 'img ', src: 'i/images/web_manage_page_root/u30.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u98', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '删除/禁用/修改'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u99', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u99_img', className: 'img ', src: 'i/images/web_manage_page_root/u99.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u100', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '张无忌'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u101', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u101_img', className: 'img ', src: 'i/images/web_manage_page_root/u99.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u102', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '患者'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u103', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u103_img', className: 'img ', src: 'i/images/web_manage_page_root/u99.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u104', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '18812345678'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u105', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u105_img', className: 'img ', src: 'i/images/web_manage_page_root/u99.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u106', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '2016-3-1 12：30：15'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u107', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u107_img', className: 'img ', src: 'i/images/web_manage_page_root/u99.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u108', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '启用'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u109', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u109_img', className: 'img ', src: 'i/images/web_manage_page_root/u109.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u110', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '删除/禁用/修改'
                                            )
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u111', className: 'ax_h4' },
                                _react2.default.createElement('img', { id: 'u111_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u112', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '所有'
                                        ),
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '账户'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u113', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u113_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u114', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            { onClick: this.querybefore },
                                            ' 上一页 '
                                        )
                                    ),
                                    _react2.default.createElement(
                                        'p',
                                        { onClick: this.querynext },
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            ' 下一页'
                                        )
                                    )
                                )
                            )
                        )
                    ),
                    _react2.default.createElement(
                        'div',
                        { id: 'u6_state2', className: 'panel_state', 'data-label': '价格管理' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u6_state2_content', className: 'panel_state_content' },
                            _react2.default.createElement(
                                'div',
                                { id: 'u115', className: 'ax_table' },
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u116', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u116_img', className: 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u117', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '编号'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u118', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u118_img', className: 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u119', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '简述'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u120', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u120_img', className: 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u121', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '详细描述'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u122', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u122_img', className: 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u123', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '价格'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u124', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u124_img', className: 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u125', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '状态'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u126', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u126_img', className: 'img ', src: 'i/images/web_manage_page_root/u18.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u127', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '操作'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u128', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u128_img', className: 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u129', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '1'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u130', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u130_img', className: 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u131', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '基础服务费'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u132', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u132_img', className: 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u133', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '基础服务费'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u134', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u134_img', className: 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u135', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '6'
                                            ),
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '00'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u136', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u136_img', className: 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u137', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '启用'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u138', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u138_img', className: 'img ', src: 'i/images/web_manage_page_root/u30.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u139', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '删除/禁用/修改'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u140', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u140_img', className: 'img ', src: 'i/images/web_manage_page_root/u99.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u141', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '2'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u142', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u142_img', className: 'img ', src: 'i/images/web_manage_page_root/u99.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u143', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '康复礼包'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u144', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u144_img', className: 'img ', src: 'i/images/web_manage_page_root/u99.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u145', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '康复床位升级，康复营养餐X1'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u146', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u146_img', className: 'img ', src: 'i/images/web_manage_page_root/u99.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u147', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '1'
                                            ),
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '500'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u148', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u148_img', className: 'img ', src: 'i/images/web_manage_page_root/u99.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u149', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '启用'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u150', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u150_img', className: 'img ', src: 'i/images/web_manage_page_root/u109.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u151', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '删除/禁用/修改'
                                            )
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u152', className: 'ax_h4' },
                                _react2.default.createElement('img', { id: 'u152_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u153', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '所有费用项目'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u154', className: 'ax_horizontal_line' },
                                _react2.default.createElement('img', { id: 'u154_start', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif', alt: 'u154_start' }),
                                _react2.default.createElement('img', { id: 'u154_end', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif', alt: 'u154_end' }),
                                _react2.default.createElement('img', { id: 'u154_line', className: 'img ', src: 'i/images/web_manage_page_root/u0_line.png', alt: 'u154_line' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u155', className: 'ax_h4' },
                                _react2.default.createElement('img', { id: 'u155_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u156', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '增加'
                                        ),
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '费用项'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u157', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u157_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u158', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '简述'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u159', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u159_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u160', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '详细描述'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u161', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u161_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u162', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '价格'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u163', className: 'ax_droplist' },
                                _react2.default.createElement(
                                    'select',
                                    { id: 'u163_input' },
                                    _react2.default.createElement(
                                        'option',
                                        { value: '启用' },
                                        '启用'
                                    ),
                                    _react2.default.createElement(
                                        'option',
                                        { value: '禁用' },
                                        '禁用'
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u164', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u164_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u165', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '状态'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u166', className: 'ax_text_field' },
                                _react2.default.createElement('input', { id: 'u166_input', type: 'text' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u167', className: 'ax_text_field' },
                                _react2.default.createElement('input', { id: 'u167_input', type: 'text' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u168', className: 'ax_text_field' },
                                _react2.default.createElement('input', { id: 'u168_input', type: 'text' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u169', className: 'ax_html_button' },
                                _react2.default.createElement('input', { id: 'u169_input', type: 'submit', value: '修改' })
                            ),
                            '>',
                            _react2.default.createElement(
                                'div',
                                { id: 'u170', className: 'ax_html_button' },
                                _react2.default.createElement('input', { id: 'u170_input', type: 'submit', value: '提交' })
                            )
                        )
                    ),
                    _react2.default.createElement(
                        'div',
                        { id: 'u6_state3', className: 'panel_state', 'data-label': '网站配置管理' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u6_state3_content', className: 'panel_state_content' },
                            _react2.default.createElement(
                                'div',
                                { id: 'u171', className: 'ax_text_field' },
                                _react2.default.createElement('input', { id: 'u171_input', type: 'text', value: '4' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u172', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u172_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u173', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '首页医生'
                                        ),
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '显示数'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u174', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u174_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u175', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '首页'
                                        ),
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '机构显示数'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u176', className: 'ax_text_field' },
                                _react2.default.createElement('input', { id: 'u176_input', type: 'text', value: '4' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u177', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u177_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u178', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '首页广告地址'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u179', className: 'ax_text_field' },
                                _react2.default.createElement('input', { id: 'u179_input', type: 'text' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u180', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u180_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u181', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '显示首页滚动条'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u182', className: 'ax_droplist' },
                                _react2.default.createElement(
                                    'select',
                                    { id: 'u182_input' },
                                    _react2.default.createElement(
                                        'option',
                                        { selected: true, value: '是' },
                                        '是'
                                    ),
                                    _react2.default.createElement(
                                        'option',
                                        { value: '否' },
                                        '否'
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u183', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u183_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u184', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '医生详表每页数'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u185', className: 'ax_text_field' },
                                _react2.default.createElement('input', { id: 'u185_input', type: 'text', value: '10' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u186', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u186_img', className: 'img ', src: 'i/images/web_manage_page_root/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u187', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '机构详页没页数'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u188', className: 'ax_text_field' },
                                _react2.default.createElement('input', { id: 'u188_input', type: 'text', value: '10' })
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u189', className: 'ax_menu' },
                    _react2.default.createElement('img', { id: 'u189_menu', className: 'img ', src: 'i/images/web_manage_page_root/u189_menu.png', alt: 'u189_menu' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u190', className: 'ax_table' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u191', className: 'ax_table_cell' },
                            _react2.default.createElement('img', { id: 'u191_img', className: 'img ', src: 'i/images/web_manage_page_root/u191.png' }),
                            _react2.default.createElement(
                                'div',
                                { id: 'u192', className: 'text' },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    _react2.default.createElement(
                                        'span',
                                        null,
                                        '仪表盘'
                                    )
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'div',
                            { id: 'u193', className: 'ax_table_cell' },
                            _react2.default.createElement('img', { id: 'u193_img', className: 'img ', src: 'i/images/web_manage_page_root/u193.png' }),
                            _react2.default.createElement(
                                'div',
                                { id: 'u194', className: 'text' },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    _react2.default.createElement(
                                        'span',
                                        null,
                                        '管理账号'
                                    )
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'div',
                            { id: 'u195', className: 'ax_table_cell' },
                            _react2.default.createElement('img', { id: 'u195_img', className: 'img ', src: 'i/images/web_manage_page_root/u191.png' }),
                            _react2.default.createElement(
                                'div',
                                { id: 'u196', className: 'text' },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    _react2.default.createElement(
                                        _reactRouter.Link,
                                        { to: '/user_manage' },
                                        _react2.default.createElement(
                                            'span',
                                            { onClick: function onClick() {
                                                    _this2.setState({ 'now': '用户管理' });
                                                } },
                                            '用户管理'
                                        )
                                    )
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'div',
                            { id: 'u197', className: 'ax_table_cell' },
                            _react2.default.createElement('img', { id: 'u197_img', className: 'img ', src: 'i/images/web_manage_page_root/u191.png' }),
                            _react2.default.createElement(
                                'div',
                                { id: 'u198', className: 'text' },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    _react2.default.createElement(
                                        _reactRouter.Link,
                                        { to: '/price_manage' },
                                        _react2.default.createElement(
                                            'span',
                                            { onClick: function onClick() {
                                                    _this2.setState({ 'now': '价格管理' });
                                                } },
                                            '价格管理'
                                        )
                                    )
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'div',
                            { id: 'u199', className: 'ax_table_cell' },
                            _react2.default.createElement('img', { id: 'u199_img', className: 'img ', src: 'i/images/web_manage_page_root/u199.png' }),
                            _react2.default.createElement(
                                'div',
                                { id: 'u200', className: 'text' },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    _react2.default.createElement(
                                        'span',
                                        null,
                                        '网站配置'
                                    )
                                )
                            )
                        )
                    ),
                    _react2.default.createElement(
                        'div',
                        { id: 'u201', className: 'ax_menu sub_menu' },
                        _react2.default.createElement('img', { id: 'u201_menu', className: 'img ', src: 'i/images/web_manage_page_root/u201_menu.png', alt: 'u201_menu' }),
                        _react2.default.createElement(
                            'div',
                            { id: 'u202', className: 'ax_table' },
                            _react2.default.createElement(
                                'div',
                                { id: 'u203', className: 'ax_table_cell' },
                                _react2.default.createElement('img', { id: 'u203_img', className: 'img ', src: 'i/images/web_manage_page_root/u203.png' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u204', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '求医用户'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u205', className: 'ax_table_cell' },
                                _react2.default.createElement('img', { id: 'u205_img', className: 'img ', src: 'i/images/web_manage_page_root/u203.png' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u206', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '医生'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u207', className: 'ax_table_cell' },
                                _react2.default.createElement('img', { id: 'u207_img', className: 'img ', src: 'i/images/web_manage_page_root/u207.png' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u208', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '机构'
                                        )
                                    )
                                )
                            )
                        )
                    )
                )
            )
        );
    }
});
exports.default = ManageRoot;

},{"../components/PageContainer":19,"../components/auth":25,"amazeui-react":52,"react":356,"react-router":219}],39:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _reactRouter = require('react-router');

var _amazeuiReact = require('amazeui-react');

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

var _MessageBar = require('../components/MessageBar');

var _MessageBar2 = _interopRequireDefault(_MessageBar);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _objectWithoutProperties(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }

var Messages = _react2.default.createClass({
  displayName: 'Messages',
  getInitialState: function getInitialState() {
    return {
      dataSource: [],
      event: null
    };
  },
  componentWillMount: function componentWillMount() {
    var _this = this;

    if (!window.fetch) {
      return console.error('fetch API is not supported!');
    }

    function checkStatus(response) {
      if (response.status >= 200 && response.status < 300) {
        return response;
      } else {
        var error = new Error(response.statusText);
        error.response = response;
        throw error;
      }
    }

    fetch('https://api.github.com/orgs/amazeui/events').then(checkStatus).then(function (res) {
      return res.json();
    }).then(function (data) {
      _this.updateDataSource(data);
    }).catch(function (error) {
      console.log('Request failed: ', error);
    });
  },
  updateDataSource: function updateDataSource(data) {
    this.setState({
      dataSource: data
    });
  },
  formatDate: function formatDate(date) {
    date = new Date(date);

    return date.getFullYear() + '/' + (date.getMonth() + 1) + '/' + date.getDate() + ' ' + date.getHours() + ':' + date.getMinutes();
  },
  setEventData: function setEventData(event) {
    this.setState({
      event: event
    });
  },
  renderEventType: function renderEventType(type) {
    type = type.replace('Event', '');
    var styleMap = {
      Watch: 'success',
      Fork: 'secondary',
      Issues: 'danger',
      IssueComment: 'warning'
    };

    return _react2.default.createElement(
      _amazeuiReact.Badge,
      {
        amStyle: styleMap[type],
        round: true
      },
      type
    );
  },
  renderList: function renderList() {
    var _this2 = this;

    var dataSource = this.state.dataSource;


    return dataSource.length ? _react2.default.createElement(
      _amazeuiReact.List,
      {
        className: 'adm-msg-list'
      },
      dataSource.map(function (event, index) {
        var id = event.id;
        var type = event.type;
        var created_at = event.created_at;
        var actor = event.actor;


        return _react2.default.createElement(
          _amazeuiReact.ListItem,
          {
            key: 'message-' + id,
            onClick: _this2.setEventData.bind(_this2, event),
            linkComponent: _reactRouter.Link,
            linkProps: {
              to: '/messages/' + id
            }
          },
          _react2.default.createElement(
            'div',
            {
              className: 'adm-msg-list-main am-text-truncate'
            },
            _this2.renderEventType(type),
            _this2.formatDate(created_at),
            ' ',
            _react2.default.createElement(
              'em',
              null,
              'by'
            ),
            ' ',
            actor.login
          ),
          _react2.default.createElement(_amazeuiReact.Icon, {
            icon: 'chevron-right',
            className: 'adm-msg-icon'
          })
        );
      })
    ) : _react2.default.createElement(
      'div',
      {
        className: 'adm-msg-loading'
      },
      _react2.default.createElement(_amazeuiReact.Icon, { spin: true, icon: 'spinner' })
    );
  },
  render: function render() {
    var _props = this.props;
    var children = _props.children;
    var params = _props.params;

    var props = _objectWithoutProperties(_props, ['children', 'params']);

    var hasId = params.id ? 'has-id' : '';

    return _react2.default.createElement(
      _PageContainer2.default,
      _extends({}, props, {
        noBreadcrumb: true,
        className: 'adm-page-msg'
      }),
      _react2.default.createElement(
        _amazeuiReact.Grid,
        {
          collapse: true,
          className: hasId
        },
        _react2.default.createElement(
          _amazeuiReact.Col,
          {
            sm: 12,
            md: 5,
            className: 'adm-msg-col'
          },
          _react2.default.createElement(
            _MessageBar2.default,
            null,
            '通知'
          ),
          this.renderList()
        ),
        _react2.default.createElement(
          _amazeuiReact.Col,
          {
            sm: 12,
            md: 7,
            className: 'adm-msg-detail'
          },
          _react2.default.createElement(
            _MessageBar2.default,
            null,
            _react2.default.createElement(
              _reactRouter.Link,
              {
                to: '/messages',
                className: 'am-show-sm-only'
              },
              'adsfasdfasdf',
              _react2.default.createElement(_amazeuiReact.Icon, { icon: 'mail-reply' })
            )
          ),
          'console.log(children)',
          children ? _react2.default.cloneElement(children, Object.assign({}, children.props, {
            event: this.state.event
          })) : null
        )
      )
    );
  }
});

exports.default = Messages;

},{"../components/MessageBar":13,"../components/PageContainer":19,"amazeui-react":52,"react":356,"react-router":219}],40:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _amazeuiReact = require('amazeui-react');

var _Order = require('../components/Order');

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Order = _react2.default.createClass({
  displayName: 'Order',
  getInitialState: function getInitialState() {
    return {
      update_times: 0
    };
  },
  update: function update() {
    this.forceUpdate();
    console.log('update');
  },
  render: function render() {
    var _this = this;

    return _react2.default.createElement(
      _PageContainer2.default,
      null,
      _react2.default.createElement(
        _amazeuiReact.Tabs,
        { animation: 'slide', onSelect: function onSelect() {
            _this.setState({ update_times: _this.state.update_times + 1 });
          } },
        _react2.default.createElement(
          _amazeuiReact.Tabs.Item,
          { eventKey: '1', title: '未完成' },
          _react2.default.createElement(_Order.Order_null, { update_times: this.state.update_times })
        ),
        _react2.default.createElement(
          _amazeuiReact.Tabs.Item,
          { eventKey: '2', title: '进行中' },
          _react2.default.createElement(_Order.Order_ing, { update_times: this.state.update_times })
        ),
        _react2.default.createElement(
          _amazeuiReact.Tabs.Item,
          { eventKey: '3', title: '已完成' },
          _react2.default.createElement(_Order.Order_done, { update_times: this.state.update_times })
        )
      )
    );
  }
});

exports.default = Order;

},{"../components/Order":18,"../components/PageContainer":19,"amazeui-react":52,"react":356}],41:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

var _auth = require('../components/auth');

var _auth2 = _interopRequireDefault(_auth);

var _reactRouter = require('react-router');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Price_manage = _react2.default.createClass({
    displayName: 'Price_manage',
    getInitialState: function getInitialState() {
        return {
            dataSource: {
                're': [{
                    id: "无",
                    detail: "无",
                    price: "无",
                    description: "无",
                    state: "无"
                }, {
                    id: "无",
                    detail: "无",
                    price: "无",
                    description: "无",
                    state: "无"
                }, {
                    id: "无",
                    detail: "无",
                    price: "无",
                    description: "无",
                    state: "无"
                }],
                totalpage: 2,
                nowpage: 1,
                now: '', //仪表盘，管理账号，用户管理，价格管理，网站配置 
                change: false
            }

        };
    },

    handleSubmit: function handleSubmit(e) {
        var _this = this;

        _auth2.default.myact({
            to: 'manager/insertprice.do',
            parms: [{ key: 'description', value: this.refs.description.value }, { key: 'detail', value: this.refs.detail.value }, { key: 'price', value: this.refs.price.value }, { key: 'state', value: this.refs.state.value }]
        }, function (res) {
            if (res.regist_error) {
                alert(res.regist_error);
            } else {
                _this.setState({ nowpage: _this.state.page });
                _this.query(nowpage);
                _this.updateDataSource(res);
            }
        });
        this.query(nowpage);
    },
    query: function query(page) {
        var _this2 = this;

        _auth2.default.myact({
            to: 'manager/pricelist.do',
            parms: [{ 'key': 'page', 'value': page }]
        }, function (res) {
            _this2.updateDataSource(res);
            console.log("你妹的2");
        });
        this.setState({ nowpage: page });
    },
    querynext: function querynext() {
        console.log("你妹的1");
        var nowpage = this.state.nowpage;
        var totalpage = this.state.totalpage;
        console.log(totalpage);
        if (nowpage + 1 <= totalpage) {
            console.log("有病");
            nowpage += 1;
            this.query(nowpage);
        }
    },
    querybefore: function querybefore() {

        var nowpage = this.state.nowpage;
        var totalpage = this.state.totalpage;
        if (nowpage - 1 >= 1) {
            nowpage -= 1;
            this.query(nowpage);
        }
    },
    componentDidMount: function componentDidMount() {
        this.query(1);
    },
    updateDataSource: function updateDataSource(data) {
        this.setState({
            dataSource: data,
            totalpage: data.totalpage
        });
        console.log(data);
    },
    kaifazhong: function kaifazhong(str) {
        console.log(str);
    },
    jude: function jude(intState) {
        var state = "";
        if (intState == 0) {
            state = "禁用";
        } else if (intState == 1) {
            state = "启用";
        } else {
            state = "";
        }
        return state;
    },
    judgeOperaton: function judgeOperaton(id) {
        var judge1 = false;
        if (id == 0 || id == 1) {
            judge1 = true;
        }
        console.log(id);
        console.log(judge1);
        return judge1;
    },
    deletePrice: function deletePrice(id) {
        var _this3 = this;

        var nowpage = this.state.nowpage;
        _auth2.default.myact({
            to: 'manager/deleteprice.do',
            parms: [{ 'key': 'id', 'value': id }]
        }, function (res) {
            _this3.updateDataSource(res);
            console.log("你妹的2");
        });
        this.setState({ nowpage: this.state.page });
        this.query(nowpage);
    },
    disableprice: function disableprice(id) {
        var _this4 = this;

        var nowpage = this.state.nowpage;
        _auth2.default.myact({
            to: 'manager/disableprice.do',
            parms: [{ 'key': 'id', 'value': id }]
        }, function (res) {
            _this4.updateDataSource(res);
            console.log("你妹的2");
        });
        this.setState({ nowpage: this.state.page });
        this.query(nowpage);
    },
    changeContent: function changeContent(id) {
        this.setState({
            change: true
        });
    },
    render: function render() {
        var _this5 = this;

        var page = '';
        var line1 = {};
        var data = this.state.dataSource.re;
        if (data.length >= 1) {
            line1 = data[0];
        }
        var line2 = {};
        if (data.length >= 2) {
            line2 = data[1];
        }
        var line3 = {};

        if (data.length >= 3) {
            line3 = data[2];
        }
        var black = {
            color: 'black'
        };

        return _react2.default.createElement(
            _PageContainer2.default,
            null,
            _react2.default.createElement('link', { href: 'i/resources/css/axure_rp_page.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('link', { href: 'i/data/styles.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('link', { href: 'i/css/web_manage_page_root/style1.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement(
                'div',
                { id: 'base', className: '' },
                _react2.default.createElement(
                    'div',
                    { id: 'u0', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u0_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u0_start' }),
                    _react2.default.createElement('img', { id: 'u0_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u0_end' }),
                    _react2.default.createElement('img', { id: 'u0_line', className: 'img ', src: 'i/images/web_manage_page_root/u0_line.png', alt: 'u0_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u1', className: 'ax_h1' },
                    _react2.default.createElement('img', { id: 'u1_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u2', className: 'text' },
                        _react2.default.createElement(
                            'strong',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '谷雨医生平台系统管理'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u3', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u3_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u4', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                { classNameName: 'u209' },
                                ' '
                            ),
                            _react2.default.createElement(
                                'span',
                                { classNameName: 'u209' },
                                '    '
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u5', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u5_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u5_start' }),
                    _react2.default.createElement('img', { id: 'u5_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u5_end' }),
                    _react2.default.createElement('img', { id: 'u5_line', className: 'img ', src: 'i/images/web_manage_page_root/u0_line.png', alt: 'u5_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u6', className: 'ax_dynamic_panel', 'data-label': '系统管理' },
                    _react2.default.createElement(
                        'div',
                        { id: 'u6_state0', className: 'panel_state', 'data-label': '管理账号' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u6_state0_content', className: 'panel_state_content' },
                            _react2.default.createElement(
                                'div',
                                { id: 'u7', className: 'ax_table' },
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u8', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u8_img', className: 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u9', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '编号'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u10', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u10_img', className: 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u11', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '简述'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u12', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u12_img', className: 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u13', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '详细描述'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u14', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u14_img', className: 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u15', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '价格'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u16', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u16_img', className: 'img ', src: 'i/images/web_manage_page_root/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u17', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '状态'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u18', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u18_img', className: 'img ', src: 'i/images/web_manage_page_root/u18.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u19', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '操作'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u20', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u20_img', className: 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u21', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                line1.id
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u22', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u22_img', className: 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u23', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                line1.description
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u24', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u24_img', className: 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u25', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                line1.detail
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u26', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u26_img', className: 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u27', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                line1.price
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u28', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u28_img', className: 'img ', src: 'i/images/web_manage_page_root/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u29', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                this.jude(line1.state)
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u30', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u30_img', className: 'img ', src: 'i/images/web_manage_page_root/u30.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u31', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'div',
                                                null,
                                                this.judgeOperaton(line1.state) ? _react2.default.createElement(
                                                    'p',
                                                    null,
                                                    _react2.default.createElement(
                                                        'span',
                                                        { onClick: this.deletePrice.bind(this, line1.id) },
                                                        '删除'
                                                    ),
                                                    '/',
                                                    _react2.default.createElement(
                                                        'span',
                                                        { onClick: this.disableprice.bind(this, line1.id) },
                                                        '禁用'
                                                    ),
                                                    '/',
                                                    _react2.default.createElement(
                                                        'span',
                                                        null,
                                                        '修改'
                                                    )
                                                ) : ''
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u32', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u32_img', className: 'img ', src: 'i/images/web_manage_page_root/u32.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u33', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                line2.id
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u34', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u34_img', className: 'img ', src: 'i/images/web_manage_page_root/u32.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u35', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                line2.description
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u36', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u36_img', className: 'img ', src: 'i/images/web_manage_page_root/u32.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u37', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                line2.detail
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u38', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u38_img', className: 'img ', src: 'i/images/web_manage_page_root/u32.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u39', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                line2.price
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u40', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u40_img', className: 'img ', src: 'i/images/web_manage_page_root/u32.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u41', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                this.jude(line2.state)
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u42', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u42_img', className: 'img ', src: 'i/images/web_manage_page_root/u42.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u43', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'div',
                                                null,
                                                this.judgeOperaton(line2.state) ? _react2.default.createElement(
                                                    'p',
                                                    null,
                                                    _react2.default.createElement(
                                                        'span',
                                                        { onClick: this.deletePrice.bind(this, line2.id) },
                                                        '删除'
                                                    ),
                                                    '/',
                                                    _react2.default.createElement(
                                                        'span',
                                                        { onClick: this.disableprice.bind(this, line2.id) },
                                                        '禁用'
                                                    ),
                                                    '/',
                                                    _react2.default.createElement(
                                                        'span',
                                                        null,
                                                        '修改'
                                                    )
                                                ) : ''
                                            )
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u44', className: 'ax_horizontal_line' },
                                _react2.default.createElement('img', { id: 'u44_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u44_start' }),
                                _react2.default.createElement('img', { id: 'u44_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u44_end' }),
                                _react2.default.createElement('img', { id: 'u44_line', className: 'img ', src: 'i/images/web_manage_page_root/u0_line.png', alt: 'u44_line' })
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u45', className: 'ax_h4' },
                                _react2.default.createElement('img', { id: 'u45_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u46', className: 'text' },
                                    _react2.default.createElement(
                                        'strong',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '所有成员'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u47', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u47_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u48', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            { onClick: this.query.bind(this, 1) },
                                            '首页'
                                        ),
                                        ' ',
                                        _react2.default.createElement(
                                            'span',
                                            { onClick: this.querybefore },
                                            '上一页'
                                        ),
                                        ' ',
                                        _react2.default.createElement('span', { classNameName: 'u211' }),
                                        _react2.default.createElement(
                                            'span',
                                            { onClick: this.querynext },
                                            '下一页 '
                                        ),
                                        ' ',
                                        _react2.default.createElement(
                                            'span',
                                            { onClick: this.query.bind(this, this.state.totalpage) },
                                            '尾页'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u49', className: 'ax_h4' },
                                _react2.default.createElement('img', { id: 'u49_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u50', className: 'text' },
                                    _react2.default.createElement(
                                        'strong',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '增加费用项'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u51', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u51_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u52', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '简述'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u53', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u53_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u54', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '详细描述'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u55', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u55_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u56', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '价格'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u57', className: 'ax_droplist' },
                                _react2.default.createElement(
                                    'select',
                                    { id: 'u57_input', ref: 'state' },
                                    _react2.default.createElement(
                                        'option',
                                        { selecte: true, value: '1' },
                                        '启用'
                                    ),
                                    _react2.default.createElement(
                                        'option',
                                        { value: '0' },
                                        '禁用'
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u58', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u58_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u59', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '状态'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'form',
                                null,
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u60', className: 'ax_text_field' },
                                    _react2.default.createElement('input', { id: 'u60_input', type: 'text', ref: 'description' })
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u61', className: 'ax_text_field' },
                                    _react2.default.createElement('input', { id: 'u61_input', type: 'text', ref: 'detail' })
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u62', className: 'ax_text_field' },
                                    _react2.default.createElement('input', { id: 'u62_input', type: 'text', ref: 'price' })
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u72', className: 'ax_html_button' },
                                    _react2.default.createElement('input', { id: 'u72_input', type: 'reset', value: '重置' })
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u73', className: 'ax_html_button' },
                                _react2.default.createElement(
                                    'span',
                                    { onClick: this.handleSubmit },
                                    _react2.default.createElement('input', { id: 'u73_input', type: 'submit', value: '提交' })
                                )
                            ),
                            _react2.default.createElement('div', { id: 'u70', className: 'ax_html_button' })
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u189', className: 'ax_menu' },
                    _react2.default.createElement('img', { id: 'u189_menu', className: 'img ', src: 'i/images/web_manage_page_root/u189_menu.png', alt: 'u189_menu' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u190', className: 'ax_table' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u191', className: 'ax_table_cell' },
                            _react2.default.createElement('img', { id: 'u191_img', className: 'img ', src: 'i/images/web_manage_page_root/u191.png' }),
                            _react2.default.createElement(
                                'div',
                                { id: 'u192', className: 'text' },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    _react2.default.createElement(
                                        'span',
                                        null,
                                        '仪表盘'
                                    )
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'div',
                            { id: 'u193', className: 'ax_table_cell' },
                            _react2.default.createElement('img', { id: 'u193_img', className: 'img ', src: 'i/images/web_manage_page_root/u191.png' }),
                            _react2.default.createElement(
                                'div',
                                { id: 'u194', className: 'text', style: { color: this.state.now == '管理账号' ? 'white' : 'black' } },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    _react2.default.createElement(
                                        _reactRouter.Link,
                                        { to: '/manageRoot' },
                                        _react2.default.createElement(
                                            'span',
                                            { onClick: function onClick() {
                                                    _this5.setState({ 'now': '管理账号' });
                                                } },
                                            '管理账号'
                                        )
                                    )
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'div',
                            { id: 'u195', className: 'ax_table_cell' },
                            _react2.default.createElement('img', { id: 'u195_img', className: 'img ', src: 'i/images/web_manage_page_root/u191.png' }),
                            _react2.default.createElement(
                                'div',
                                { id: 'u196', className: 'text' },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    _react2.default.createElement(
                                        _reactRouter.Link,
                                        { to: '/user_manage' },
                                        _react2.default.createElement(
                                            'span',
                                            { onClick: function onClick() {
                                                    _this5.setState({ 'now': '用户管理' });
                                                } },
                                            '用户管理'
                                        )
                                    )
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'div',
                            { id: 'u197', className: 'ax_table_cell' },
                            _react2.default.createElement('img', { id: 'u197_img', className: 'img ', src: 'i/images/web_manage_page_root/u191_mouseOver.png' }),
                            _react2.default.createElement(
                                'div',
                                { id: 'u198', className: 'text', style: { color: 'white' }, onClick: function onClick() {
                                        _this5.setState({ 'now': '价格管理' });
                                    } },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    _react2.default.createElement(
                                        'span',
                                        null,
                                        '价格管理'
                                    )
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'div',
                            { id: 'u199', className: 'ax_table_cell' },
                            _react2.default.createElement('img', { id: 'u199_img', className: 'img ', src: 'i/images/web_manage_page_root/u199.png' }),
                            _react2.default.createElement(
                                'div',
                                { id: 'u200', className: 'text' },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    _react2.default.createElement(
                                        'span',
                                        null,
                                        '网站配置'
                                    )
                                )
                            )
                        )
                    ),
                    _react2.default.createElement(
                        'div',
                        { id: 'u201', className: 'ax_menu sub_menu' },
                        _react2.default.createElement('img', { id: 'u201_menu', className: 'img ', src: 'i/images/web_manage_page_root/u201_menu.png', alt: 'u201_menu' }),
                        _react2.default.createElement(
                            'div',
                            { id: 'u202', className: 'ax_table' },
                            _react2.default.createElement(
                                'div',
                                { id: 'u203', className: 'ax_table_cell' },
                                _react2.default.createElement('img', { id: 'u203_img', className: 'img ', src: 'i/images/web_manage_page_root/u203.png' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u204', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '求医用户'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u205', className: 'ax_table_cell' },
                                _react2.default.createElement('img', { id: 'u205_img', className: 'img ', src: 'i/images/web_manage_page_root/u203.png' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u206', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '医生'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u207', className: 'ax_table_cell' },
                                _react2.default.createElement('img', { id: 'u207_img', className: 'img ', src: 'i/images/web_manage_page_root/u207.png' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u208', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '机构'
                                        )
                                    )
                                )
                            )
                        )
                    )
                )
            )
        );
    }
});

exports.default = Price_manage;

},{"../components/PageContainer":19,"../components/auth":25,"react":356,"react-router":219}],42:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

var _amazeuiReact = require('amazeui-react');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Profile = _react2.default.createClass({
  displayName: 'Profile',
  render: function render() {
    return _react2.default.createElement(
      _PageContainer2.default,
      this.props,
      _react2.default.createElement(
        'h2',
        null,
        '个人资料'
      ),
      _react2.default.createElement('hr', null),
      _react2.default.createElement(
        _amazeuiReact.Grid,
        null,
        _react2.default.createElement(
          _amazeuiReact.Col,
          {
            sm: 12,
            lg: 4,
            lgPush: 8
          },
          _react2.default.createElement(
            _amazeuiReact.Panel,
            null,
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('img', {
                className: 'am-circle am-img-thumbnail am-img-responsive',
                src: 'http://s.amazeui.org/media/i/demos/bw-2014-06-19.jpg?imageView/1/w/120/h/120/q/80', alt: '' })
            ),
            _react2.default.createElement(
              'p',
              null,
              '你可以使用 ',
              _react2.default.createElement(
                'a',
                { href: 'http://gravatar.com', target: '_blank' },
                'Gravatar'
              ),
              ' 提供的头像或者使用本地上传头像。 '
            ),
            _react2.default.createElement(
              _amazeuiReact.Form,
              null,
              _react2.default.createElement(_amazeuiReact.Input, {
                type: 'file',
                help: '选择一个文件'
              }),
              _react2.default.createElement(_amazeuiReact.Input, {
                type: 'submit',
                amStyle: 'primary',
                value: '上传'
              })
            )
          )
        ),
        _react2.default.createElement(
          _amazeuiReact.Col,
          {
            sm: 12,
            lg: 8,
            lgPull: 4
          },
          _react2.default.createElement(
            _amazeuiReact.Form,
            { horizontal: true },
            _react2.default.createElement(_amazeuiReact.Input, {
              label: '用户名',
              placeholder: 'User name'
            }),
            _react2.default.createElement(_amazeuiReact.Input, {
              label: 'Email',
              type: 'email',
              placeholder: 'Your email'
            }),
            _react2.default.createElement(_amazeuiReact.Input, {
              label: '密码',
              type: 'password',
              placeholder: 'Your password'
            }),
            _react2.default.createElement(_amazeuiReact.Input, {
              type: 'textarea',
              label: '简介',
              placeholder: 'BIO'
            }),
            _react2.default.createElement(_amazeuiReact.Input, {
              type: 'submit',
              amStyle: 'primary',
              value: '保存'
            })
          )
        )
      )
    );
  }
});

exports.default = Profile;

},{"../components/PageContainer":19,"amazeui-react":52,"react":356}],43:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

var _auth = require('../components/auth');

var _auth2 = _interopRequireDefault(_auth);

var _reactRouter = require('react-router');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Query_result = (0, _reactRouter.withRouter)(_react2.default.createClass({
    displayName: 'Query_result',
    getInitialState: function getInitialState() {
        return {
            dataSource: [{
                agencydoctorid: 0,
                agencyprovince: "北京",
                agencyname: "北京市第三医院3",
                excel: ["股骨头坏死"],
                agencybed: 0,
                agencyhot: 0
            }, {
                agencydoctorid: 0,
                agencyprovince: "北京",
                agencyname: "北京市第三医院3",
                excel: ["股骨头坏死"],
                agencybed: 0,
                agencyhot: 0
            }, {
                agencydoctorid: 0,
                agencyprovince: "北京",
                agencyname: "北京市第三医院3",
                excel: ["股骨头坏死"],
                agencybed: 0,
                agencyhot: 0
            }],
            type: '0', //or 专家团队2 or 综合医院1 or 专科医院3
            place: '', //or 行政区
            check_appointment: false,
            check_discount: false,
            search_inOrder: '1', //or人气 or 预约时间,
            nowpage: 1,
            totalpage: 10,
            des: ''
        };
    },
    query: function query(page) {
        var _this = this;

        _auth2.default.myact({ to: 'list.do',
            parms: [{ 'key': 'page', 'value': page }, { 'key': 'role', 'value': this.state.type }, { 'key': 'province', 'value': this.state.place }, { 'key': 'paixu', 'value': this.state.search_inOrder }, { 'key': 'disease', 'value': this.state.des }]
        }, function (res) {
            _this.updateDataSource(res);
        });
        this.state.nowpage = page;
    },
    querynext: function querynext() {
        var nowpage = this.state.nowpage;
        var totalpage = this.state.totalpage;
        if (nowpage + 1 <= totalpage) {
            nowpage += 1;
            this.query(nowpage);
        }
    },
    querybefore: function querybefore() {
        var nowpage = this.state.nowpage;
        var totalpage = this.state.totalpage;
        console.log(nowpage);
        if (nowpage - 1 >= 1) {
            nowpage -= 1;
            this.query(nowpage);
        }
    },
    componentDidMount: function componentDidMount() {
        if (this.props.location.query.query != undefined) {
            this.state.des = this.props.location.query.query;
        }
        this.query(1);
    },
    updateDataSource: function updateDataSource(data) {
        this.setState({
            dataSource: data
        });
        console.log(data);
    },
    render: function render() {
        var _this2 = this;

        var page = '';
        var line1 = { excel: [] };
        var data = this.state.dataSource;
        if (data.length >= 1) {
            line1 = data[0];
        }
        var line2 = { excel: [] };
        if (data.length >= 2) {
            line2 = data[1];
        }
        var line3 = { excel: [] };
        if (data.length >= 3) {
            line3 = data[2];
        }
        var black = {
            color: 'black'
        };
        return _react2.default.createElement(
            _PageContainer2.default,
            null,
            _react2.default.createElement('link', { href: 'i/resources/css/axure_rp_page.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('link', { href: 'i/data/styles.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('link', { href: 'i/css/query_result/styles.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement(
                'div',
                { id: 'base', className: '' },
                _react2.default.createElement(
                    'div',
                    { id: 'u0', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u0_img', className: 'img ', src: 'i/images/query_result/u0.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u1', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u2', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u2_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u3', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '全部'
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                '结果'
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                ' > ',
                                this.state.des
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                '   '
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u4', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u4_img', className: 'img ', src: 'i/images/query_result/u4.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u5', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u6', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u6_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u7', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '类型：'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    _reactRouter.Link,
                    { to: 'institution_list' },
                    _react2.default.createElement(
                        'div',
                        { id: 'u8', className: 'ax_paragraph' },
                        _react2.default.createElement('img', { id: 'u8_img', className: 'img ', src: this.state.type == '1' ? 'i/images/doctor_list/u10.png' : 'i/images/institution_list/u10.png' }),
                        _react2.default.createElement(
                            'div',
                            { id: 'u9', className: 'text', style: { color: this.state.type == '1' ? 'white' : 'black' }, onClick: function onClick() {
                                    _this2.state.type = '1';_this2.query(_this2.state.nowpage);
                                } },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '综合医院'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    _reactRouter.Link,
                    { to: '/doctor_list' },
                    _react2.default.createElement(
                        'div',
                        { id: 'u12', className: 'ax_paragraph' },
                        _react2.default.createElement('img', { id: 'u12_img', className: 'img ', src: this.state.type == '2' ? 'i/images/doctor_list/u10.png' : 'i/images/institution_list/u10.png' }),
                        _react2.default.createElement(
                            'div',
                            { id: 'u13', className: 'text', style: { color: this.state.type == '2' ? 'white' : 'black' }, onClick: function onClick() {
                                    _this2.state.type = '2';_this2.query(_this2.state.nowpage);
                                } },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '专家团队'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    _reactRouter.Link,
                    { to: '/query_result' },
                    _react2.default.createElement(
                        'div',
                        { id: 'u14', className: 'ax_paragraph' },
                        _react2.default.createElement('img', { id: 'u14_img', className: 'img ', src: this.state.type == '0' ? 'i/images/query_result/u14.png' : 'i/images/institution_list/u10.png' }),
                        _react2.default.createElement(
                            'div',
                            { id: 'u15', className: 'text', style: { color: this.state.type == '0' ? 'white' : 'black' } },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '不限'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u16', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u16_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u17', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '地点：'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u18', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u18_img', className: 'img ', src: this.state.place == '行政区' ? 'i/images/query_result/u14.png' : 'i/images/institution_list/u10.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u19', className: 'text', style: { color: this.state.place == '行政区' ? 'white' : 'black' }, onClick: function onClick() {
                                _this2.state.place = '行政区';_this2.query(_this2.state.nowpage);
                            } },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '行政区'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u20', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u20_img', className: 'img ', src: this.state.place == '' ? 'i/images/query_result/u14.png' : 'i/images/institution_list/u10.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u21', className: 'text', style: { color: this.state.place == '' ? 'white' : 'black' }, onClick: function onClick() {
                                _this2.state.place = '';_this2.query(_this2.state.nowpage);
                            } },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '不限'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u22', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u22_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u22_start' }),
                    _react2.default.createElement('img', { id: 'u22_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u22_end' }),
                    _react2.default.createElement('img', { id: 'u22_line', className: 'img ', src: 'i/images/user_home/u68_line.png', alt: 'u22_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u23', className: 'ax_checkbox' },
                    _react2.default.createElement(
                        'label',
                        { htmlFor: 'u23_input' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u24', className: 'text' },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '7日内可预约'
                                )
                            )
                        )
                    ),
                    _react2.default.createElement('input', { id: 'u23_input', type: 'checkbox', value: 'checkbox', checked: this.state.check_appointment ? 'checked' : '', onClick: function onClick() {
                            _this2.state.check_appointment = !_this2.state.check_appointment;_this2.query(_this2.state.nowpage);
                        } })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u25', className: 'ax_checkbox' },
                    _react2.default.createElement(
                        'label',
                        { htmlFor: 'u25_input' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u26', className: 'text' },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '有优惠'
                                )
                            )
                        )
                    ),
                    _react2.default.createElement('input', { id: 'u25_input', type: 'checkbox', value: 'checkbox', checked: this.state.check_discount ? 'checked' : '', onClick: function onClick() {
                            _this2.state.check_discount = !_this2.state.check_discount;_this2.query(_this2.state.nowpage);
                        } })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u27', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u27_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u28', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                { style: { color: this.state.search_inOrder == '1' ? 'red' : 'black' }, onClick: function onClick() {
                                        _this2.state.search_inOrder = '1';_this2.query(_this2.state.nowpage);
                                    } },
                                '智能'
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                '  '
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                ' | ',
                                _react2.default.createElement(
                                    'span',
                                    { style: { color: this.state.search_inOrder == '2' ? 'red' : 'black' }, onClick: function onClick() {
                                            _this2.state.search_inOrder = '2';_this2.query(_this2.state.nowpage);
                                        } },
                                    '人气'
                                ),
                                '   | ',
                                _react2.default.createElement(
                                    'span',
                                    { style: { color: this.state.search_inOrder == '3' ? 'red' : 'black' }, onClick: function onClick() {
                                            _this2.state.search_inOrder = '3';_this2.query(_this2.state.nowpage);
                                        }
                                    },
                                    '预约时间'
                                ),
                                '    '
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u29', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u29_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u30', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            { onClick: this.querybefore },
                            _react2.default.createElement(
                                'span',
                                null,
                                '上一页'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u33', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u33_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u34', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            { onClick: this.querynext },
                            _react2.default.createElement(
                                'span',
                                null,
                                '下一页'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u35', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u35_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u36', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            { className: 'u131' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u120' },
                                line1.agencyname
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u120' },
                                ' ',
                                line1.InstitutionType
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u129' },
                                ' ， '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                '人气指数 '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                line1.agencyhot
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            { className: 'u130' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u129' },
                                '擅长治疗: '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u129' },
                                line1.excel.join(',')
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            { className: 'u130' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                '可预约地点: '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                line1.agencyprovince
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            { className: 'u122' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                '最快手术时间: '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                '预计'
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                '0'
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                '日内'
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u123' },
                                '  '
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u37', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u37_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u38', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', { className: 'u128' })
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                { className: 'u128' },
                                '  '
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u39', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u39_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u40', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            { className: 'u131' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u120' },
                                line2.agencyname
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u120' },
                                ' ',
                                line2.InstitutionType
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u123' },
                                ' ， '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                '人气指数 '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                line2.agencyhot
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            { className: 'u130' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u129' },
                                '擅长治疗: '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u129' },
                                line2.excel.join(',')
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            { className: 'u130' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                '可预约地点: '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                line2.agencyprovince
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            { className: 'u122' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                '最快手术时间: '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                '预计'
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                '0'
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                '日内'
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u123' },
                                '  '
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u41', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u41_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u42', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                { className: 'u128' },
                                line2.Staff
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u43', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u43_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u43_start' }),
                    _react2.default.createElement('img', { id: 'u43_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u43_end' }),
                    _react2.default.createElement('img', { id: 'u43_line', className: 'img ', src: 'i/images/query_result/u43_line.png', alt: 'u43_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u44', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u44_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u45', className: 'text' },
                        line1.agencydiscount != undefined ? _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '优惠: '
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                line1.agencydiscount
                            )
                        ) : ''
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u46', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u46_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u46_start' }),
                    _react2.default.createElement('img', { id: 'u46_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u46_end' }),
                    _react2.default.createElement('img', { id: 'u46_line', className: 'img ', src: 'i/images/query_result/u43_line.png', alt: 'u46_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u47', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u47_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u48', className: 'text' },
                        line2.agencydiscount != undefined ? _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '优惠: '
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                line2.agencydiscount
                            )
                        ) : ''
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u49', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u49_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u50', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            { className: 'u131' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u120' },
                                line3.agencyname
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u120' },
                                '  '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u120' },
                                ' ',
                                line3.InstitutionType
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u123' },
                                ' ， '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                '人气指数 '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                line3.agencyhot
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            { className: 'u130' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u129' },
                                '擅长治疗: '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u129' },
                                line3.excel.join(',')
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            { className: 'u130' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                '可预约地点: '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                line3.agencyprovince
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u121' },
                                '  '
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            { className: 'u122' },
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                '最快手术时间: '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                '预计'
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                '0'
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u124' },
                                '日内'
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'u123' },
                                '  '
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u133', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u133_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u134', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                { className: 'u128' },
                                line3.Staff
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                { className: 'u128' },
                                '  '
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u51', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u51_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u51_start' }),
                    _react2.default.createElement('img', { id: 'u51_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u51_end' }),
                    _react2.default.createElement('img', { id: 'u51_line', className: 'img ', src: 'i/images/query_result/u43_line.png', alt: 'u51_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u52', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u52_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u53', className: 'text' },
                        line3.agencydiscount != undefined ? _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '优惠: '
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                line3.agencydiscount
                            )
                        ) : ''
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u54', className: 'ax_text_field' },
                    _react2.default.createElement('input', { id: 'u54_input', type: 'text', ref: 'desces', defaultValue: this.state.des })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u55', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u55_img', className: 'img ', src: 'i/images/query_result/u55.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u56', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '快速预约'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    _reactRouter.Link,
                    { to: '/index_home' },
                    _react2.default.createElement(
                        'div',
                        { id: 'u57', className: 'ax_shape' },
                        _react2.default.createElement('img', { id: 'u57_img', className: 'img ', src: 'i/images/user_check_operation_reservation_page/u23.png' }),
                        _react2.default.createElement(
                            'div',
                            { id: 'u58', className: 'text' },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement('span', null)
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u59', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u59_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u60', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '手术地点：北京 '
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u64', className: 'ax_shape', onClick: function onClick() {
                            _this2.state.des = _this2.refs.desces.value != undefined ? _this2.refs.desces.value : '';
                            _this2.query(1);
                        } },
                    _react2.default.createElement('img', { id: 'u64_img', className: 'img ', src: 'i/images/query_result/u64.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u65', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '搜索'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u66', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u66_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u66_start' }),
                    _react2.default.createElement('img', { id: 'u66_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u66_end' }),
                    _react2.default.createElement('img', { id: 'u66_line', className: 'img ', src: 'i/images/user_home/u68_line.png', alt: 'u66_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u67', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u67_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u67_start' }),
                    _react2.default.createElement('img', { id: 'u67_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u67_end' }),
                    _react2.default.createElement('img', { id: 'u67_line', className: 'img ', src: 'i/images/user_home/u68_line.png', alt: 'u67_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u68', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u68_img', className: 'img ', src: 'i/images/query_result/u68.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u69', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '立即预约'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u70', className: 'ax_image' },
                    _react2.default.createElement('img', { id: 'u70_img', className: 'img ', src: line1.id }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u71', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u72', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u72_img', className: 'img ', src: 'i/images/query_result/u68.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u73', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '立即预约'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u74', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u74_img', className: 'img ', src: 'i/images/query_result/u68.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u75', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '立即预约'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u76', className: 'ax_image' },
                    _react2.default.createElement('img', { id: 'u76_img', className: 'img ', src: line2.id }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u77', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u78', className: 'ax_image' },
                    _react2.default.createElement('img', { id: 'u78_img', className: 'img ', src: line3.id }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u79', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u80', className: 'ax_dynamic_panel' },
                    _react2.default.createElement(
                        'div',
                        { id: 'u80_state0', className: 'panel_state', 'data-label': 'State1' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u80_state0_content', className: 'panel_state_content' },
                            _react2.default.createElement(
                                'div',
                                { id: 'u81', className: 'ax_shape' },
                                _react2.default.createElement('img', { id: 'u81_img', className: 'img ', src: 'i/images/query_result/u81.png' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u82', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement('span', null)
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u83', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u83_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u84', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '创伤'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u85', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u85_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u86', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '脊椎'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u87', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u87_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u88', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '儿骨'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u89', className: 'ax_shape' },
                                _react2.default.createElement('img', { id: 'u89_img', className: 'img ', src: 'i/images/query_result/u89.png' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u90', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement('span', null)
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u91', className: 'ax_shape' },
                                _react2.default.createElement('img', { id: 'u91_img', className: 'img ', src: 'i/images/query_result/u89.png' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u92', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement('span', null)
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u93', className: 'ax_shape' },
                                _react2.default.createElement('img', { id: 'u93_img', className: 'img ', src: 'i/images/query_result/u89.png' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u94', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement('span', null)
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u95', className: 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u95_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u96', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '关节'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u97', className: 'ax_shape' },
                                _react2.default.createElement('img', { id: 'u97_img', className: 'img ', src: 'i/images/query_result/u89.png' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u98', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement('span', null)
                                    )
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u99', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u99_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u99_start' }),
                    _react2.default.createElement('img', { id: 'u99_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u99_end' }),
                    _react2.default.createElement('img', { id: 'u99_line', className: 'img ', src: 'i/images/query_result/u99_line.png', alt: 'u99_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u100', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u100_img', className: 'img ', src: 'i/images/query_result/u100.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u101', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '全部症状'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u102', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u102_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        _reactRouter.Link,
                        { to: '/doctor_list' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u103', className: 'text' },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '医生团队'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u104', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u104_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        _reactRouter.Link,
                        { to: '/institution_list' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u105', className: 'text' },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '合作机构'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u106', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u106_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u107', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                _reactRouter.Link,
                                { to: '/index_home' },
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '首页'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u108', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u108_img', className: 'img ', src: 'i/images/query_result/u108.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u109', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u110', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u110_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u110_start' }),
                    _react2.default.createElement('img', { id: 'u110_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u110_end' }),
                    _react2.default.createElement('img', { id: 'u110_line', className: 'img ', src: 'i/images/user_home/u68_line.png', alt: 'u110_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u111', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u111_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u112', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                _react2.default.createElement(
                                    _reactRouter.Link,
                                    { to: _auth2.default.get_config()['about_us_url'] },
                                    '关于我们'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u113', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u113_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u114', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                _react2.default.createElement(
                                    _reactRouter.Link,
                                    { to: _auth2.default.get_config()['connect_us_url'] },
                                    '联系我们'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u115', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u115_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u116', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '服务条款'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u888', className: 'ax_paragraph', style: {
                            'position': 'absolute',
                            'left': '450px',
                            'top': '956px',
                            'width': '200px',
                            'height': '1px',
                            'font-size': '14px',
                            'color': '#666666'
                        } },
                    _react2.default.createElement('img', { id: 'u125_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u126', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                _react2.default.createElement(
                                    'a',
                                    { href: 'http://www.miitbeian.gov.cn' },
                                    '           京ICP备案16059247号'
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                _react2.default.createElement('img', { src: 'http://www.beian.gov.cn/img/ghs.png' }),
                                _react2.default.createElement(
                                    'a',
                                    { href: 'http://www.beian.gov.cn/portal/registerSystemInfo?recordcode=11010802022799' },
                                    '京公网安备 11010802022799号'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u117', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u117_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u118', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '媒体更新'
                            )
                        )
                    ),
                    _react2.default.createElement(
                        'div',
                        { id: 'haha' },
                        _react2.default.createElement('input', null)
                    )
                )
            )
        );
    }
}));
exports.default = Query_result;

},{"../components/PageContainer":19,"../components/auth":25,"react":356,"react-router":219}],44:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

var _Header = require('../components/FontComponents/Header');

var _Header2 = _interopRequireDefault(_Header);

var _Qbzz = require('../components/FontComponents/Qbzz');

var _Qbzz2 = _interopRequireDefault(_Qbzz);

var _reactRouter = require('react-router');

var _auth = require('../components/auth');

var _auth2 = _interopRequireDefault(_auth);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Register_doctor = (0, _reactRouter.withRouter)(_react2.default.createClass({
    displayName: 'Register_doctor',
    getInitialState: function getInitialState() {
        return {
            InterValObj: '',
            code: '',
            startTime: '',
            phoneNum: '',
            count: '60', //间隔函数，1秒执行
            curCount: '', //当前剩余秒数
            username: '',
            password: '',
            cfmPwd: '',
            province: '',
            hospital: '',
            department: '',
            title: '',
            excel: '',
            experience: '',
            honor: '',
            checkedTeam: '',
            num: 1,
            select_group: {
                "关节": {
                    "髋关节": { "股骨头坏死": '1', "髋关节骨性关节炎": "2", "髋关节发育不良": "3", "类风湿性髋关节炎": "4", "强直性脊柱炎髋关节强直": "5", "髋臼-股骨撞击综合症": "6", "髋关节置换术后感染": "7", "髋关节置换术后假体松动": "8" },
                    "足踝": { "踝关节骨性关节炎": "13", "踝关节创伤性关节炎": "14", "马蹄内翻足": "15" },
                    "膝关节": { "膝关节骨性关节炎": "9", "膝关节内外翻畸形": "10", "类风湿性膝关节炎": "11", "膝关节僵直": "12" }
                },
                "脊柱": {
                    "脊柱": { "颈椎骨折伴或不伴脱位，颈髓损伤": '16', "颈椎间盘突出伴或不伴滑脱": '17', "胸腰椎骨折伴或不伴脱位，脊髓损伤": "18", "胸腰椎间盘突出伴或不伴滑脱": '19', "脊柱畸形矫正": '20', '脊柱结核': '21' }
                },
                "创伤": {
                    "创伤": { "四肢长骨干骨折": "22", "关节周围骨折": "23", "老年髋部骨折": '24', "骨盆骨折": '25', "骨折不愈合": "26", "骨折术后感染": "27", "骨折畸形愈合矫正": '28' }
                },
                "手外": {
                    "手外": { "先天多指并指畸形": "29", "臂丛神经损伤": "30", "周围神经损伤": "31", "手指再造": "32", "手部肿瘤": "33" }
                },
                "小儿骨科": {
                    "小儿骨科": { "小儿先天性髋关节脱位": "34", "小儿骨折": "35", "小儿股骨头坏死": "36", "先天性关节畸形": "37" }
                },
                "骨与软组织肿瘤": {
                    "骨与软组织肿瘤": { "骨肿瘤（恶性）": "38", "骨肿瘤（良性）": "39", "软组织肿瘤（恶性）": "40", "软组织肿瘤（良性）": "41", "骨肿瘤保肢治疗": "42" }
                }
            },
            select_state: []
        };
    },

    //基于配置文件的不定组三级联动下拉选框
    my_foreach: function my_foreach(obj) {
        var childrens = [];
        var _iteratorNormalCompletion = true;
        var _didIteratorError = false;
        var _iteratorError = undefined;

        try {
            for (var _iterator = Object.keys(obj)[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
                var k = _step.value;

                childrens.push(_react2.default.createElement(
                    'option',
                    { value: k },
                    k
                ));
            }
        } catch (err) {
            _didIteratorError = true;
            _iteratorError = err;
        } finally {
            try {
                if (!_iteratorNormalCompletion && _iterator.return) {
                    _iterator.return();
                }
            } finally {
                if (_didIteratorError) {
                    throw _iteratorError;
                }
            }
        }

        return childrens;
    },
    option1: function option1() {
        var a = this.state.select_group;
        return this.my_foreach(a);
    },
    option2: function option2(fid) {
        var s = this.state.select_state[fid][0];
        var select_group = this.state.select_group;
        try {
            return this.my_foreach(select_group[s]);
        } catch (err) {
            return [];
        }
    },
    option3: function option3(fid) {
        var s = this.state.select_state[fid][0];
        var s1 = this.state.select_state[fid][1];
        try {
            return this.my_foreach(this.state.select_group[s][s1]);
        } catch (err) {
            return;
        }
    },
    select_item: function select_item(fid) {
        var group = [];
        var base_ref = 'sel-' + fid + '-';
        group.push(_react2.default.createElement(
            'select',
            { ref: base_ref + '1', onChange: this.handleSelect.bind(this, [fid, 1]) },
            _react2.default.createElement(
                'option',
                { value: '' },
                '请选择'
            ),
            this.option1()
        ));
        group.push(_react2.default.createElement(
            'select',
            { ref: base_ref + '2', onChange: this.handleSelect.bind(this, [fid, 2]) },
            _react2.default.createElement(
                'option',
                { value: '' },
                '请选择'
            ),
            this.option2(fid)
        ));
        group.push(_react2.default.createElement(
            'select',
            { ref: base_ref + '3', onChange: this.handleSelect.bind(this, [fid, 3]) },
            _react2.default.createElement(
                'option',
                { value: '' },
                '请选择'
            ),
            this.option3(fid)
        ));

        return group;
    },
    handleSelect: function handleSelect(par) {
        var fid = par[0];
        var index = par[1];
        var base_ref = 'sel-' + fid + '-' + index;
        var value = this.refs[base_ref].value;
        var select_state = this.state.select_state;
        select_state[fid][index - 1] = value;
        this.setState({ select_state: select_state });
    },
    select_group: function select_group() {
        var length = this.state.select_state.length;
        var group = [];
        for (var i = 0; i < length; i++) {
            group.push(_react2.default.createElement(
                'div',
                null,
                this.select_item(i)
            ));
        }
        return group;
    },
    addSelect: function addSelect() {
        var new_select = ['', '', ''];
        var before_select = this.state.select_state || [];
        before_select.push(new_select);
        this.setState({ select_state: before_select });
    },
    delSelect: function delSelect() {
        var before_select = this.state.select_state || [];
        before_select.pop();
        this.setState({ select_state: before_select });
    },

    //基于配置文件的不定组三级联动下拉选框 end
    submitForm: function submitForm() {
        this.state.username = document.getElementById("u4_input").value;
        this.state.password = document.getElementById("u5_input").value;
        this.state.cfmPwd = document.getElementById("u6_input").value;
        this.state.mobile = document.getElementById("u7_input").value;
        this.state.code = document.getElementById("u9_input").value; //短信验证码
        this.state.province = document.getElementById("u31_input").value;
        this.state.hospital = document.getElementById("u32_input").value;
        this.state.department = document.getElementById("u33_input").value;
        this.state.title = document.getElementById("u34_input").value;

        this.state.experience = document.getElementById("u40_input").value;
        this.state.honor = document.getElementById("u41_input").value;
        var team = document.getElementsByName("team");
        this.state.checkedTeam = "";
        for (var i = 0; i < team.length; i++) {
            if (team[i].checked == true) {
                this.state.checkedTeam += team[i].value + ",";
            }
        }
        this.handleSubmit();
    },
    handleSubmit: function handleSubmit() {
        var _this = this;

        var parm = [{ 'key': 'regist_username', 'value': this.state.username }, { 'key': 'regist_password', 'value': this.state.password }, { 'key': 'regist_confirmPwd', 'value': this.state.cfmPwd }, { 'key': 'regist_mobile', 'value': this.state.phoneNum }, { 'key': 'province', 'value': this.state.province }, { 'key': 'hospital', 'value': this.state.hospital }, { 'key': 'department', 'value': this.state.department }, { 'key': 'title', 'value': this.state.title }, { 'key': 'experience', 'value': this.state.experience }, { 'key': 'honor', 'value': this.state.honor }, { 'key': 'team', 'value': this.state.checkedTeam }, { 'key': 'role', 'value': '3' }, { 'key': 'user_name', 'value': this.state.username }, { 'key': 'regist_validate', 'value': this.state.code }, { 'key': 'realName', 'value': this.refs.realName.value }];

        for (var i = 0; i < this.state.select_state.length; i++) {
            var s = this.state.select_state[i];
            var m = this.state.select_group;
            if (s[0] != '' && s[1] != '' && s[2] != '') {
                var value = m[s[0]][s[1]][s[2]];
                parm.push({ 'key': 'name', 'value': value });
            }
        }
        _auth2.default.myact({ to: 'regist.do',
            parms: parm
        }, function (res) {
            if (res.regist_error) {
                alert(res.regist_error);
            } else {
                var form = new FormData(_react2.default.createElement('form', { encType: 'multipart/form-data', method: 'post' }));
                form.append("imgFile", _this.refs.my_file.files[0]);
                form.append("username", _this.state.username);
                form.append("certificate", 'true');
                form.append("role", res.role);
                form.append("user_name", _this.state.username);
                _auth2.default.post('upload.do', form, function (res) {});
                var form = new FormData(_react2.default.createElement('form', { encType: 'multipart/form-data', method: 'post' }));
                form.append("imgFile", _this.refs.my_file_1.files[0]);
                form.append("username", _this.state.username);
                form.append("role", res.role);
                form.append("user_name", _this.state.username);
                _auth2.default.post('upload.do', form, function (res) {});
                alert('注册申请已提交');
                _this.props.router.replace('/login');
            }
        });
    },
    queryCode: function queryCode() {
        _auth2.default.myact({
            to: 'send.do',
            parms: [{ 'key': 'phone', 'value': this.state.phoneNum }, { 'key': 'code', 'value': this.state.code }, { 'key': 'startTime', 'value': this.state.startTime }]
        }, function (res) {
            console.log(res);
        });
    },
    sendMessage: function sendMessage() {
        var codeLength = 6; //验证码长度
        this.state.curCount = this.state.count;
        this.state.phoneNum = document.getElementById("u7_input").value; //手机号码
        if (this.state.phoneNum.length > 0) {
            //产生验证码
            this.state.code = '';
            for (var i = 0; i < codeLength; i++) {
                this.state.code += parseInt(Math.random() * 9).toString();
            }
            //设置button效果，开始计时
            $("#u10_input").attr("disabled", "true");
            $("#u10_input").val("请在" + this.state.curCount + "秒内输入验证码");
            this.state.InterValObj = window.setInterval(this.SetRemainTime, 1000); //启动计时器，1秒执行一次
            var myDate = new Date();
            this.state.startTime = myDate.getTime();
            //向后台发送处理数据
            console.log(this.state.phoneNum);
            console.log(this.state.code);
            this.queryCode();
        } else {
            alert("手机号码不能为空！");
        }
    },
    SetRemainTime: function SetRemainTime() {
        if (this.state.curCount == 0) {
            window.clearInterval(this.state.InterValObj); //停止计时器
            $("#u10_input").removeAttr("disabled"); //启用按钮
            $("#u10_input").val("重新发送验证码");
        } else {
            this.state.curCount--;
            $("#u10_input").val("请在" + this.state.curCount + "秒内输入验证码");
        }
    },
    render: function render() {
        var sel = this.select_group();
        return _react2.default.createElement(
            _PageContainer2.default,
            null,
            _react2.default.createElement('link', { href: 'i/resources/css/axure_rp_page.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('link', { href: 'i/data/styles.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('link', { href: 'i/css/register_doctor/styles.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement(
                'div',
                { id: 'base', className: '' },
                _react2.default.createElement(
                    'div',
                    { id: 'u0', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u0_img', className: 'img ', src: 'i/images/register_doctor/u0.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u1', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u2', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u2_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u3', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '用户名'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u4', className: 'ax_text_field' },
                    _react2.default.createElement('input', { ref: 'regist_username', id: 'u4_input', type: 'text' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u5', className: 'ax_text_field' },
                    _react2.default.createElement('input', { ref: 'regist_password', id: 'u5_input', type: 'password' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u6', className: 'ax_text_field' },
                    _react2.default.createElement('input', { ref: 'regist_confirmPwd', id: 'u6_input', type: 'password' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u7', className: 'ax_text_field' },
                    _react2.default.createElement('input', { ref: 'regist_mobile', id: 'u7_input', type: 'text' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u8', className: 'ax_text_field' },
                    _react2.default.createElement('input', { ref: 'realName', id: 'u8_input', type: 'text' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u9', className: 'ax_text_field' },
                    _react2.default.createElement('input', { ref: '', id: 'u9_input', type: 'text' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u10', className: 'ax_html_button' },
                    _react2.default.createElement('input', { ref: '', id: 'u10_input', type: 'submit', value: '获取短信验证码', onClick: this.sendMessage })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u15', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u15_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u16', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '密码'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u17', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u17_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u18', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '确认密码'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u19', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u19_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u20', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '验证手机'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u21', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u21_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u22', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '真实姓名'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u23', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u23_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u24', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '短信验证码'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u25', className: 'ax_h3' },
                    _react2.default.createElement('img', { id: 'u25_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u26', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '账户信息'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u27', className: 'ax_h3' },
                    _react2.default.createElement('img', { id: 'u27_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u28', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '专业信息'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u29', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u29_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u30', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '第一执业医院'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u31', className: 'ax_droplist' },
                    _react2.default.createElement(
                        'select',
                        { id: 'u31_input', ref: 'province' },
                        _react2.default.createElement(
                            'option',
                            { selected: true, value: '北京' },
                            '北京'
                        ),
                        _react2.default.createElement(
                            'option',
                            { value: '上海' },
                            '上海'
                        ),
                        _react2.default.createElement(
                            'option',
                            { value: '广州' },
                            '广州'
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u32', className: 'ax_droplist' },
                    _react2.default.createElement(
                        'select',
                        { id: 'u32_input', ref: 'hospital' },
                        _react2.default.createElement(
                            'option',
                            { selected: true, value: '积水潭医院' },
                            '积水潭医院'
                        ),
                        _react2.default.createElement(
                            'option',
                            { value: '北京协和医院' },
                            '北京协和医院'
                        ),
                        _react2.default.createElement(
                            'option',
                            { value: '301医院' },
                            '301医院'
                        ),
                        _react2.default.createElement(
                            'option',
                            { value: '北医三院' },
                            '北医三院'
                        ),
                        _react2.default.createElement(
                            'option',
                            { value: '人民医院' },
                            '人民医院'
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u33', className: 'ax_droplist' },
                    _react2.default.createElement(
                        'select',
                        { id: 'u33_input', ref: 'department' },
                        _react2.default.createElement(
                            'option',
                            { selected: true, value: '骨科' },
                            '骨科'
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u34', className: 'ax_droplist' },
                    _react2.default.createElement(
                        'select',
                        { id: 'u34_input', ref: 'title' },
                        _react2.default.createElement(
                            'option',
                            { selected: true, value: '主任医师' },
                            '主任医师'
                        ),
                        _react2.default.createElement(
                            'option',
                            { value: '副主任医师' },
                            '副主任医师'
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u39', className: 'ax_droplist' },
                    sel
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u39_add', className: 'ax_paragraph' },
                    _react2.default.createElement('input', { type: 'button', value: '添加', onClick: this.addSelect }),
                    _react2.default.createElement('input', { type: 'button', value: '移除', onClick: this.delSelect })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u35', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u35_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u36', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '科室'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u37', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u37_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u38', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '职称'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u40', className: 'ax_text_area' },
                    _react2.default.createElement('textarea', { id: 'u40_input', ref: 'experience' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u41', className: 'ax_text_area' },
                    _react2.default.createElement('textarea', { id: 'u41_input', ref: 'honor' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u42', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u42_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u43', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '擅长'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u44', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u44_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u45', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '执业经历'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u46', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u46_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u47', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '荣誉'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'form',
                    { action: 'upload.do', method: 'post', enctype: 'multipart/form-data', id: 'form3' },
                    _react2.default.createElement(
                        'div',
                        { id: 'u500', className: 'ax_paragraph' },
                        _react2.default.createElement('img', { id: 'u50_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                        _react2.default.createElement(
                            'div',
                            { id: 'u51', className: 'text' },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '头像'
                                )
                            )
                        )
                    ),
                    _react2.default.createElement(
                        'div',
                        { id: 'u520', className: 'ax_text_field' },
                        _react2.default.createElement('input', { ref: 'my_file_1', type: 'file' })
                    )
                ),
                _react2.default.createElement(
                    'form',
                    { action: 'upload.do', method: 'post', enctype: 'multipart/form-data', id: 'form2' },
                    _react2.default.createElement(
                        'div',
                        { id: 'u50', className: 'ax_paragraph' },
                        _react2.default.createElement('img', { id: 'u50_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                        _react2.default.createElement(
                            'div',
                            { id: 'u51', className: 'text' },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '资历包'
                                )
                            )
                        )
                    ),
                    _react2.default.createElement(
                        'div',
                        { id: 'u52', className: 'ax_text_field' },
                        _react2.default.createElement('input', { ref: 'my_file', type: 'file' })
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u54', className: 'ax_h3' },
                    _react2.default.createElement('img', { id: 'u54_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u55', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '团队信息'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u56', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u56_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u57', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '我有自己的'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u58', className: 'ax_checkbox' },
                    _react2.default.createElement(
                        'label',
                        { htmlFor: 'u58_input' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u59', className: 'text' },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '助理'
                                )
                            )
                        )
                    ),
                    _react2.default.createElement('input', { ref: 'team', id: 'u58_input', type: 'checkbox', name: 'team', value: '助理' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u60', className: 'ax_checkbox' },
                    _react2.default.createElement(
                        'label',
                        { htmlFor: 'u60_input' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u61', className: 'text' },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '麻醉师'
                                )
                            )
                        )
                    ),
                    _react2.default.createElement('input', { ref: '', id: 'u60_input', type: 'checkbox', name: 'team', value: '麻醉师' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u62', className: 'ax_checkbox' },
                    _react2.default.createElement(
                        'label',
                        { htmlFor: 'u62_input' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u63', className: 'text' },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '护士'
                                )
                            )
                        )
                    ),
                    _react2.default.createElement('input', { ref: '', id: 'u62_input', type: 'checkbox', name: 'team', value: '护士' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u64', className: 'ax_checkbox' },
                    _react2.default.createElement(
                        'label',
                        { htmlFor: 'u64_input' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u65', className: 'text' },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '跟台'
                                )
                            )
                        )
                    ),
                    _react2.default.createElement('input', { ref: '', id: 'u64_input', type: 'checkbox', name: 'team', value: '跟台' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u66', className: 'ax_checkbox' },
                    _react2.default.createElement(
                        'label',
                        { htmlFor: 'u66_input' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u67', className: 'text' },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '我已阅读并同意 ',
                                    _react2.default.createElement(
                                        'a',
                                        { href: '' },
                                        '<谷雨医生平台注册协议>'
                                    ),
                                    ' '
                                )
                            )
                        )
                    ),
                    _react2.default.createElement('input', { ref: '', id: 'u66_input', type: 'checkbox', value: 'checkbox' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u68', className: 'ax_shape', onClick: this.submitForm },
                    _react2.default.createElement('img', { id: 'u68_img', className: 'img ', src: 'i/images/register_doctor/u68.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u69', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '立即注册'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u70', className: 'ax_h1' },
                    _react2.default.createElement('img', { id: 'u70_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u71', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '欢迎注册'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u72', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u72_img', className: 'img ', src: 'i/images/user_check_operation_reservation_page/u23.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u73', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u74', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u74_img', className: 'img ', src: 'i/images/register_doctor/u74.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u75', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                _reactRouter.Link,
                                { to: '/register_user' },
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '我要寻医'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u76', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u76_img', className: 'img ', src: 'i/images/register_doctor/u76.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u77', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                _reactRouter.Link,
                                { to: '/register_doctor' },
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '我是医生'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u78', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u78_img', className: 'img ', src: 'i/images/register_doctor/u74.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u79', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                _reactRouter.Link,
                                { to: '/register_hospital' },
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '我是机构'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u80', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u80_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u80_start' }),
                    _react2.default.createElement('img', { id: 'u80_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u80_end' }),
                    _react2.default.createElement('img', { id: 'u80_line', className: 'img ', src: 'i/images/user_home/u68_line.png', alt: 'u80_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u81', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u81_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u81_start' }),
                    _react2.default.createElement('img', { id: 'u81_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u81_end' }),
                    _react2.default.createElement('img', { id: 'u81_line', className: 'img ', src: 'i/images/user_home/u68_line.png', alt: 'u81_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u82', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u82_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u82_start' }),
                    _react2.default.createElement('img', { id: 'u82_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u82_end' }),
                    _react2.default.createElement('img', { id: 'u82_line', className: 'img ', src: 'i/images/user_home/u68_line.png', alt: 'u82_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u83', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u83_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u83_start' }),
                    _react2.default.createElement('img', { id: 'u83_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u83_end' }),
                    _react2.default.createElement('img', { id: 'u83_line', className: 'img ', src: 'i/images/user_home/u68_line.png', alt: 'u83_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u84', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u84_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u84_start' }),
                    _react2.default.createElement('img', { id: 'u84_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u84_end' }),
                    _react2.default.createElement('img', { id: 'u84_line', className: 'img ', src: 'i/images/user_home/u68_line.png', alt: 'u84_line' })
                ),
                _react2.default.createElement(
                    'div',
                    null,
                    _react2.default.createElement('br', null),
                    _react2.default.createElement('hr', null)
                )
            )
        );
    }
}));
exports.default = Register_doctor;

},{"../components/FontComponents/Header":3,"../components/FontComponents/Qbzz":4,"../components/PageContainer":19,"../components/auth":25,"react":356,"react-router":219}],45:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

var _Header = require('../components/FontComponents/Header');

var _Header2 = _interopRequireDefault(_Header);

var _Qbzz = require('../components/FontComponents/Qbzz');

var _Qbzz2 = _interopRequireDefault(_Qbzz);

var _reactRouter = require('react-router');

var _auth = require('../components/auth');

var _auth2 = _interopRequireDefault(_auth);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Register_hospital = (0, _reactRouter.withRouter)(_react2.default.createClass({
  displayName: 'Register_hospital',
  getInitialState: function getInitialState() {
    return {
      InterValObj: '',
      startTime: '',
      phoneNum: '',
      count: '60', //间隔函数，1秒执行
      curCount: '', //当前剩余秒数
      username: '',
      password: '',
      cfmPwd: '',
      hospital: '',
      province: '',
      district: '',
      address: '',
      class: '',
      avaliable: '',
      insurance: '',
      contactName: '',
      phone: '',
      mobile: '',
      code: ''
    };
  },
  submitForm: function submitForm() {
    this.state.username = document.getElementById("u9_input").value;
    this.state.password = document.getElementById("u10_input").value;
    this.state.cfmPwd = document.getElementById("u11_input").value;
    this.state.hospital = document.getElementById("u19_input").value;
    this.state.province = document.getElementById("u22_input").value;
    this.state.district = document.getElementById("u23_input").value;
    this.state.address = document.getElementById("u26_input").value;
    this.state.class = document.getElementById("u27_input").value;
    this.state.avaliable = document.getElementById("u28_input").value;
    this.state.insurance = document.getElementById("u35_input").value;
    this.state.contactName = document.getElementById("u45_input").value;
    this.state.phone = document.getElementById("u66_input").value;
    this.state.mobile = document.getElementById("u46_input").value;
    this.state.code = document.getElementById("u48_input").value; //短信验证码
    this.handleSubmit();
  },

  handleSubmit: function handleSubmit(e) {
    var _this = this;

    _auth2.default.myact({ to: 'regist.do',
      parms: [{ key: 'regist_username', value: this.state.username }, { key: 'regist_confirmPwd', value: this.state.password }, { key: 'regist_password', value: this.state.cfmPwd }, { key: 'regist_mobile', value: this.state.mobile }, { key: 'agencyName', value: this.state.hospital }, { key: 'province', value: this.state.province }, { key: 'district', value: this.state.district }, { key: 'address', value: this.state.address }, { key: 'class', value: this.state.class }, { key: 'bed', value: this.state.avaliable }, { key: 'insurance', value: this.state.insurance }, { key: 'contactName', value: this.state.contactName }, { key: 'phone', value: this.state.phone }, { key: 'regist_validate', value: this.state.code }, { key: 'role', value: '4' }]
    }, function (res) {
      if (res.regist_error) {
        alert(res.regist_error);
      } else {
        alert('注册申请已提交');
        _this.props.router.replace('/login');
      }
    });
  },
  queryCode: function queryCode() {
    _auth2.default.myact({
      to: 'send.do',
      parms: [{ 'key': 'phone', 'value': this.state.phoneNum }, { 'key': 'code', 'value': this.state.code }, { 'key': 'startTime', 'value': this.state.startTime }]
    }, function (res) {
      console.log(res);
    });
  },
  sendMessage: function sendMessage() {
    var codeLength = 6; //验证码长度
    this.state.curCount = this.state.count;
    this.state.phoneNum = document.getElementById("u46_input").value; //手机号码
    if (this.state.phoneNum.length > 0) {
      //产生验证码
      this.state.code = '';
      for (var i = 0; i < codeLength; i++) {
        this.state.code += parseInt(Math.random() * 9).toString();
      }
      //设置button效果，开始计时
      $("#u49_input").attr("disabled", "true");
      $("#u49_input").val("请在" + this.state.curCount + "秒内输入验证码");
      this.state.InterValObj = window.setInterval(this.SetRemainTime, 1000); //启动计时器，1秒执行一次
      var myDate = new Date();
      this.state.startTime = myDate.getTime();
      //向后台发送处理数据
      console.log(this.state.phoneNum);
      console.log(this.state.code);
      this.queryCode();
    } else {
      alert("手机号码不能为空！");
    }
  },
  SetRemainTime: function SetRemainTime() {
    if (this.state.curCount == 0) {
      window.clearInterval(this.state.InterValObj); //停止计时器
      $("#u49_input").removeAttr("disabled"); //启用按钮
      $("#u49_input").val("重新发送验证码");
    } else {
      this.state.curCount--;
      $("#u49_input").val("请在" + this.state.curCount + "秒内输入验证码");
    }
  },
  render: function render() {
    return _react2.default.createElement(
      _PageContainer2.default,
      null,
      _react2.default.createElement('link', { href: 'i/resources/css/axure_rp_page.css', type: 'text/css', rel: 'stylesheet' }),
      _react2.default.createElement('link', { href: 'i/data/styles.css', type: 'text/css', rel: 'stylesheet' }),
      _react2.default.createElement('link', { href: 'i/css/register_hospital/styles.css', type: 'text/css', rel: 'stylesheet' }),
      _react2.default.createElement('script', { type: 'text/babel' }),
      _react2.default.createElement(
        'div',
        { id: 'base', className: '' },
        _react2.default.createElement(
          'div',
          { id: 'u0', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u0_img', className: 'img ', src: 'i/images/register_hospital/u0.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u1', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u2', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u2_img', className: 'img ', src: 'i/images/register_hospital/u2.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u3', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u4', className: 'ax_horizontal_line' },
          _react2.default.createElement('img', { id: 'u4_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u4_start' }),
          _react2.default.createElement('img', { id: 'u4_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u4_end' }),
          _react2.default.createElement('img', { id: 'u4_line', className: 'img ', src: 'i/images/user_home/u68_line.png', alt: 'u4_line' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u5', className: 'ax_h3' },
          _react2.default.createElement('img', { id: 'u5_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u6', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '账户信息'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u7', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u7_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u8', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '用户名'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u9', className: 'ax_text_field' },
          _react2.default.createElement('input', { id: 'u9_input', ref: 'regist_username', type: 'text' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u10', className: 'ax_text_field' },
          _react2.default.createElement('input', { id: 'u10_input', ref: 'regist_password', type: 'password' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u11', className: 'ax_text_field' },
          _react2.default.createElement('input', { id: 'u11_input', ref: 'regist_confirmPwd', type: 'password' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u12', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u12_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u13', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '密码'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u14', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u14_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u15', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '确认密码'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u16', className: 'ax_horizontal_line' },
          _react2.default.createElement('img', { id: 'u16_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u16_start' }),
          _react2.default.createElement('img', { id: 'u16_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u16_end' }),
          _react2.default.createElement('img', { id: 'u16_line', className: 'img ', src: 'i/images/user_home/u68_line.png', alt: 'u16_line' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u17', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u17_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u18', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '医疗机构名称'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u19', className: 'ax_text_field' },
          _react2.default.createElement('input', { id: 'u19_input', ref: 'agencyName', type: 'text' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u20', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u20_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u21', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '所在地区'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u22', className: 'ax_droplist' },
          _react2.default.createElement(
            'select',
            { id: 'u22_input', ref: 'province' },
            _react2.default.createElement(
              'option',
              { selected: true, value: '北京' },
              '北京'
            ),
            _react2.default.createElement(
              'option',
              { value: '上海' },
              '上海'
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u23', className: 'ax_droplist' },
          _react2.default.createElement(
            'select',
            { id: 'u23_input', ref: 'district' },
            _react2.default.createElement(
              'option',
              { value: '东城区' },
              '东城区'
            ),
            _react2.default.createElement(
              'option',
              { value: '西城区' },
              '西城区'
            ),
            _react2.default.createElement(
              'option',
              { value: '海淀区' },
              '海淀区'
            ),
            _react2.default.createElement(
              'option',
              { value: '朝阳区' },
              '朝阳区'
            ),
            _react2.default.createElement(
              'option',
              { selected: true, value: '丰台区' },
              '丰台区'
            ),
            _react2.default.createElement(
              'option',
              { value: '大兴区' },
              '大兴区'
            ),
            _react2.default.createElement(
              'option',
              { value: '昌平区' },
              '昌平区'
            ),
            _react2.default.createElement(
              'option',
              { value: '通州区' },
              '通州区'
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u24', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u24_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u25', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '地址'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u26', className: 'ax_text_field' },
          _react2.default.createElement('input', { id: 'u26_input', ref: 'address', type: 'text' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u27', className: 'ax_droplist' },
          _react2.default.createElement(
            'select',
            { id: 'u27_input', ref: 'class' },
            _react2.default.createElement(
              'option',
              { selected: true, value: '三甲' },
              '三甲'
            ),
            _react2.default.createElement(
              'option',
              { value: '三乙' },
              '三乙'
            ),
            _react2.default.createElement(
              'option',
              { value: '民营' },
              '民营'
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u28', className: 'ax_droplist' },
          _react2.default.createElement(
            'select',
            { id: 'u28_input', ref: 'bed' },
            _react2.default.createElement(
              'option',
              { selected: true, value: '0' },
              '10以下'
            ),
            _react2.default.createElement(
              'option',
              { value: '1' },
              '10-50'
            ),
            _react2.default.createElement(
              'option',
              { value: '2' },
              '50-100'
            ),
            _react2.default.createElement(
              'option',
              { value: '3' },
              '100以上'
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u29', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u29_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u30', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '可用床位'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u31', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u31_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u32', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '级别'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u33', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u33_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u34', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '可医保'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u35', className: 'ax_droplist' },
          _react2.default.createElement(
            'select',
            { id: 'u35_input', ref: 'insurance' },
            _react2.default.createElement(
              'option',
              { selected: true, value: '1' },
              '是'
            ),
            _react2.default.createElement(
              'option',
              { value: '0' },
              '否'
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u36', className: 'ax_horizontal_line' },
          _react2.default.createElement('img', { id: 'u36_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u36_start' }),
          _react2.default.createElement('img', { id: 'u36_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u36_end' }),
          _react2.default.createElement('img', { id: 'u36_line', className: 'img ', src: 'i/images/user_home/u68_line.png', alt: 'u36_line' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u37', className: 'ax_h3' },
          _react2.default.createElement('img', { id: 'u37_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u38', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '机构信息'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u39', className: 'ax_horizontal_line' },
          _react2.default.createElement('img', { id: 'u39_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u39_start' }),
          _react2.default.createElement('img', { id: 'u39_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u39_end' }),
          _react2.default.createElement('img', { id: 'u39_line', className: 'img ', src: 'i/images/user_home/u68_line.png', alt: 'u39_line' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u40', className: 'ax_horizontal_line' },
          _react2.default.createElement('img', { id: 'u40_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u40_start' }),
          _react2.default.createElement('img', { id: 'u40_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u40_end' }),
          _react2.default.createElement('img', { id: 'u40_line', className: 'img ', src: 'i/images/user_home/u68_line.png', alt: 'u40_line' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u41', className: 'ax_h3' },
          _react2.default.createElement('img', { id: 'u41_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u42', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '联系人信息'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u43', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u43_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u44', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '联系人姓名'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u45', className: 'ax_text_field' },
          _react2.default.createElement('input', { id: 'u45_input', ref: 'contactName', type: 'text' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u46', className: 'ax_text_field' },
          _react2.default.createElement('input', { id: 'u46_input', ref: 'regist_mobile', type: 'text' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u48', className: 'ax_text_field' },
          _react2.default.createElement('input', { id: 'u48_input', type: 'text' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u49', className: 'ax_html_button' },
          _react2.default.createElement('input', { id: 'u49_input', onClick: this.sendMessage, type: 'submit', value: '获取短信验证码' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u54', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u54_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u55', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '验证手机'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u58', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u58_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u59', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '短信验证码'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u60', className: 'ax_checkbox' },
          _react2.default.createElement(
            'label',
            { htmlFor: 'u60_input' },
            _react2.default.createElement(
              'div',
              { id: 'u61', className: 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement(
                  'span',
                  null,
                  '我已阅读并同意',
                  _react2.default.createElement(
                    'a',
                    { href: '#' },
                    '<谷雨医生平台注册协议>'
                  )
                )
              )
            )
          ),
          _react2.default.createElement('input', { id: 'u60_input', type: 'checkbox', value: 'checkbox' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u62', className: 'ax_shape', onClick: this.submitForm },
          _react2.default.createElement('img', { id: 'u62_img', className: 'img ', src: 'i/images/register_doctor/u68.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u63', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '立即注册'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u64', className: 'ax_paragraph' },
          _react2.default.createElement('img', { id: 'u64_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u65', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '固定电话'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u66', className: 'ax_text_field' },
          _react2.default.createElement('input', { id: 'u66_input', ref: 'phone', type: 'text' })
        ),
        _react2.default.createElement(
          'div',
          { id: 'u67', className: 'ax_h1' },
          _react2.default.createElement('img', { id: 'u67_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
          _react2.default.createElement(
            'div',
            { id: 'u68', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                'span',
                null,
                '欢迎注册'
              )
            )
          )
        ),
        _react2.default.createElement(
          _reactRouter.Link,
          { to: '/index_home' },
          _react2.default.createElement(
            'div',
            { id: 'u69', className: 'ax_shape' },
            _react2.default.createElement('img', { id: 'u69_img', className: 'img ', src: 'i/images/user_check_operation_reservation_page/u23.png' }),
            _react2.default.createElement(
              'div',
              { id: 'u70', className: 'text' },
              _react2.default.createElement(
                'p',
                null,
                _react2.default.createElement('span', null)
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u71', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u71_img', className: 'img ', src: 'i/images/register_doctor/u74.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u72', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                _reactRouter.Link,
                { to: '/register_user' },
                _react2.default.createElement(
                  'span',
                  null,
                  '我要寻医'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u73', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u73_img', className: 'img ', src: 'i/images/register_doctor/u74.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u74', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                _reactRouter.Link,
                { to: '/register_doctor' },
                _react2.default.createElement(
                  'span',
                  null,
                  '我是医生'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u75', className: 'ax_shape' },
          _react2.default.createElement('img', { id: 'u75_img', className: 'img ', src: 'i/images/register_doctor/u76.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u76', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement(
                _reactRouter.Link,
                { to: '/register_hospital' },
                _react2.default.createElement(
                  'span',
                  null,
                  '我是机构'
                )
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { id: 'u77', className: 'ax_image' },
          _react2.default.createElement('img', { id: 'u77_img', className: 'img ', src: 'i/images/register_hospital/u77.png' }),
          _react2.default.createElement(
            'div',
            { id: 'u78', className: 'text' },
            _react2.default.createElement(
              'p',
              null,
              _react2.default.createElement('span', null)
            )
          )
        )
      )
    );
  }
}));
exports.default = Register_hospital;

},{"../components/FontComponents/Header":3,"../components/FontComponents/Qbzz":4,"../components/PageContainer":19,"../components/auth":25,"react":356,"react-router":219}],46:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

var _amazeuiReact = require('amazeui-react');

var _auth = require('../components/auth');

var _auth2 = _interopRequireDefault(_auth);

var _reactRouter = require('react-router');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var path1 = "i/companyLogo.png";
var Register_user = (0, _reactRouter.withRouter)(_react2.default.createClass({
    displayName: 'Register_user',
    getInitialState: function getInitialState() {
        return {
            InterValObj: '',
            code: '',
            startTime: '',
            phoneNum: '',
            count: '60', //间隔函数，1秒执行
            curCount: '', //当前剩余秒数
            username: '',
            password: '',
            cfmPwd: ''
        };
    },
    submitForm: function submitForm() {
        this.state.username = document.getElementById("u10_input").value;
        this.state.password = document.getElementById("u11_input").value;
        this.state.cfmPwd = document.getElementById("u12_input").value;
        this.state.phoneNum = document.getElementById("u21_input").value;
        this.state.code = document.getElementById("u23_input").value; //短信验证码
        this.handleSubmit();
    },

    handleSubmit: function handleSubmit(e) {
        var _this = this;

        _auth2.default.myact({ to: 'regist.do',
            parms: [{ key: 'regist_username', value: this.state.username }, { key: 'regist_password', value: this.state.password }, { key: 'regist_confirmPwd', value: this.state.cfmPwd }, { key: 'regist_mobile', value: this.state.phoneNum }, { key: 'regist_validate', value: this.state.code }, { key: 'role', value: '1' }]
        }, function (res) {
            if (res.regist_error) {
                alert(res.regist_error);
            } else {
                alert('注册申请已提交');
                _this.props.router.replace('/login');
            }
        });
    },
    queryCode: function queryCode() {
        _auth2.default.myact({
            to: 'send.do',
            parms: [{ 'key': 'phone', 'value': this.state.phoneNum }, { 'key': 'code', 'value': this.state.code }, { 'key': 'startTime', 'value': this.state.startTime }]
        }, function (res) {
            console.log(res);
        });
    },
    sendMessage: function sendMessage() {
        var codeLength = 6; //验证码长度
        this.state.curCount = this.state.count;
        this.state.phoneNum = document.getElementById("u21_input").value; //手机号码
        if (this.state.phoneNum.length > 0) {
            //产生验证码
            this.state.code = '';
            for (var i = 0; i < codeLength; i++) {
                this.state.code += parseInt(Math.random() * 9).toString();
            }
            //设置button效果，开始计时
            $("#u24_input").attr("disabled", "true");
            $("#u24_input").val("请在" + this.state.curCount + "秒内输入验证码");
            this.state.InterValObj = window.setInterval(this.SetRemainTime, 1000); //启动计时器，1秒执行一次
            var myDate = new Date();
            this.state.startTime = myDate.getTime();
            //向后台发送处理数据
            console.log(this.state.phoneNum);
            console.log(this.state.code);
            this.queryCode();
        } else {
            alert("手机号码不能为空！");
        }
    },
    SetRemainTime: function SetRemainTime() {
        if (this.state.curCount == 0) {
            window.clearInterval(this.state.InterValObj); //停止计时器
            $("#u24_input").removeAttr("disabled"); //启用按钮
            $("#u24_input").val("重新发送验证码");
        } else {
            this.state.curCount--;
            $("#u24_input").val("请在" + this.state.curCount + "秒内输入验证码");
        }
    },
    render: function render() {
        return _react2.default.createElement(
            _PageContainer2.default,
            null,
            _react2.default.createElement('link', { href: 'i/resources/css/axure_rp_page.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('link', { href: 'i/data/styles.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('link', { href: 'i/css/register_person/styles.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('script', { type: 'text/babel' }),
            _react2.default.createElement(
                'div',
                { id: 'base', className: '' },
                _react2.default.createElement(
                    'div',
                    { id: 'u0', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u0_img', className: 'img ', src: 'i/images/register_person/u0.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u1', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u2', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u2_img', className: 'img ', src: 'i/images/register_person/u2.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u3', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                _reactRouter.Link,
                                { to: '/register_user' },
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '我要寻医'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u4', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u4_img', className: 'img ', src: 'i/images/register_doctor/u74.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u5', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                _reactRouter.Link,
                                { to: '/register_doctor' },
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '我是医生'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u6', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u6_img', className: 'img ', src: 'i/images/register_doctor/u74.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u7', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                _reactRouter.Link,
                                { to: '/register_hospital' },
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '我是机构'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u8', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u8_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u9', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '用户名'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u10', className: 'ax_text_field' },
                    _react2.default.createElement('input', { id: 'u10_input', type: 'text' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u11', className: 'ax_text_field' },
                    _react2.default.createElement('input', { id: 'u11_input', type: 'password' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u12', className: 'ax_text_field' },
                    _react2.default.createElement('input', { id: 'u12_input', type: 'password' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u13', className: 'ax_checkbox' },
                    _react2.default.createElement(
                        'label',
                        { htmlFor: 'u13_input' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u14', className: 'text' },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    '我已阅读并同意',
                                    _react2.default.createElement(
                                        'a',
                                        { href: '#' },
                                        '< 谷雨医生平台注册协议>'
                                    ),
                                    ' '
                                )
                            )
                        )
                    ),
                    _react2.default.createElement('input', { id: 'u13_input', type: 'checkbox', value: 'checkbox' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u15', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u15_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u16', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '密码'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u17', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u17_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u18', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '确认密码'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u19', className: 'ax_shape', onClick: this.submitForm },
                    _react2.default.createElement('img', { id: 'u19_img', className: 'img ', src: 'i/images/register_doctor/u68.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u20', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '立即注册'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u21', className: 'ax_text_field' },
                    _react2.default.createElement('input', { id: 'u21_input', type: 'text' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u23', className: 'ax_text_field' },
                    _react2.default.createElement('input', { id: 'u23_input', ref: '', type: 'text' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u24', className: 'ax_html_button' },
                    _react2.default.createElement('input', { id: 'u24_input', type: 'submit', value: '获取短信验证码', onClick: this.sendMessage })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u27', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u27_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u28', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '验证手机'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u31', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u31_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u32', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '短信验证码'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u33', className: 'ax_h1' },
                    _react2.default.createElement('img', { id: 'u33_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u34', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '欢迎注册'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    _reactRouter.Link,
                    { to: '/index_home' },
                    _react2.default.createElement(
                        'div',
                        { id: 'u35', className: 'ax_shape' },
                        _react2.default.createElement('img', { id: 'u35_img', className: 'img ', src: 'i/images/register_person/u23.png' }),
                        _react2.default.createElement(
                            'div',
                            { id: 'u36', className: 'text' },
                            _react2.default.createElement(
                                'p',
                                null,
                                _react2.default.createElement('span', null)
                            )
                        )
                    )
                )
            )
        );
    }
}));
exports.default = Register_user;

},{"../components/PageContainer":19,"../components/auth":25,"amazeui-react":52,"react":356,"react-router":219}],47:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var System_manage = _react2.default.createClass({
    displayName: 'System_manage',
    render: function render() {
        return _react2.default.createElement(
            _PageContainer2.default,
            null,
            _react2.default.createElement('link', { href: 'i/resources/css/axure_rp_page.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('link', { href: 'i/data/styles.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('link', { href: 'i/css/system_manage_operation_assistant_page/styles.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement(
                'div',
                { id: 'base', className: '' },
                _react2.default.createElement(
                    'div',
                    { id: 'u0', className: 'ax_dynamic_panel' },
                    _react2.default.createElement(
                        'div',
                        { id: 'u0_state0', className: 'panel_state', 'data-label': 'State2' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u0_state0_content', className: 'panel_state_content' },
                            _react2.default.createElement(
                                'div',
                                { id: 'u1', className: 'ax_table' },
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u2', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u2_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u2.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u3', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '状态'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u4', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u4_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u4.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u5', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '订单号'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u6', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u6_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u6.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u7', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '患者姓名'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u8', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u8_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u9', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '年龄'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u10', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u10_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u10.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u11', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '地区'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u12', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u12_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u12.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u13', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '预约时间'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u14', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u14_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u14.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u15', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '病症'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u16', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u16_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u16.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u17', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '系统分析病症'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u18', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u18_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u18.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u19', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '系统推荐团队'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u20', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u20_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u21', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '偏好专家'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u22', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u22_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u22.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u23', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '跟进助理'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u24', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u24_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u2.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u25', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '未处理'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u26', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u26_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u4.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u27', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                'B3011'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u28', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u28_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u6.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u29', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                { className: 'u147' },
                                                '杜海涛'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u30', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u30_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u8.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u31', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '20'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u32', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u32_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u10.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u33', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '北京'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u34', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u34_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u12.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u35', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '2016年3月20日'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u36', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u36_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u14.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u37', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '髌骨手术'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u38', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u38_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u16.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u39', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u40', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u40_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u18.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u41', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                { className: 'u147' },
                                                'XXX主任'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u42', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u42_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u20.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u43', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                { className: 'u147' },
                                                '无'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u44', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u44_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u22.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u45', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                { className: 'u147' },
                                                '我要跟进'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u46', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u46_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u46.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u47', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u48', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u48_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u48.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u49', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u50', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u50_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u50.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u51', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u52', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u52_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u52.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u53', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u54', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u54_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u54.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u55', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u56', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u56_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u56.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u57', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u58', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u58_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u58.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u59', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u60', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u60_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u60.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u61', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u62', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u62_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u62.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u63', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u64', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u64_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u64.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u65', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u66', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u66_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u66.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u67', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                )
                            )
                        )
                    ),
                    _react2.default.createElement(
                        'div',
                        { id: 'u0_state1', className: 'panel_state', 'data-label': 'State1' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u0_state1_content', className: 'panel_state_content' },
                            _react2.default.createElement(
                                'div',
                                { id: 'u68', className: 'ax_shape' },
                                _react2.default.createElement('img', { id: 'u68_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u68.png' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u69', className: 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement('span', null)
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u70', className: 'ax_table' },
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u71', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u71_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u71.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u72', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '状态'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u73', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u73_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u71.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u74', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '订单号'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u75', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u75_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u71.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u76', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '患者姓名'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u77', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u77_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u71.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u78', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '年龄'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u79', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u79_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u71.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u80', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '地区'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u81', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u81_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u71.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u82', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '手术时间'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u83', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u83_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u71.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u84', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '手术名称'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u85', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u85_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u71.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u86', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '手术团队'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u87', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u87_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u71.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u88', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '更多信息'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u89', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u89_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u89.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u90', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '跟进助理'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u91', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u91_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u71.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u92', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                '进行中'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u93', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u93_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u71.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u94', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement(
                                                'span',
                                                null,
                                                'B3008'
                                            )
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u95', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u95_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u71.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u96', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u97', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u97_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u71.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u98', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u99', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u99_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u71.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u100', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u101', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u101_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u71.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u102', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u103', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u103_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u71.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u104', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u105', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u105_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u71.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u106', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u107', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u107_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u71.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u108', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u109', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u109_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u89.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u110', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u111', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u111_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u111.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u112', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u113', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u113_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u111.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u114', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u115', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u115_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u111.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u116', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u117', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u117_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u111.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u118', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u119', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u119_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u111.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u120', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u121', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u121_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u111.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u122', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u123', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u123_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u111.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u124', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u125', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u125_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u111.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u126', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u127', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u127_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u111.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u128', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                ),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u129', className: 'ax_table_cell' },
                                    _react2.default.createElement('img', { id: 'u129_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u129.png' }),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u130', className: 'text' },
                                        _react2.default.createElement(
                                            'p',
                                            null,
                                            _react2.default.createElement('span', null)
                                        )
                                    )
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u131', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u131_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u131_start' }),
                    _react2.default.createElement('img', { id: 'u131_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u131_end' }),
                    _react2.default.createElement('img', { id: 'u131_line', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u131_line.png', alt: 'u131_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u132', className: 'ax_h1' },
                    _react2.default.createElement('img', { id: 'u132_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u133', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '谷雨医生平台订单管理'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u134', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u134_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u135', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', { className: 'u149' }),
                            _react2.default.createElement(
                                'span',
                                { className: 'u149' },
                                '    '
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u136', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u136_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u136_start' }),
                    _react2.default.createElement('img', { id: 'u136_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u136_end' }),
                    _react2.default.createElement('img', { id: 'u136_line', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u131_line.png', alt: 'u136_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u137', className: 'ax_menu' },
                    _react2.default.createElement('img', { id: 'u137_menu', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u137_menu.png', alt: 'u137_menu' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u138', className: 'ax_table' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u139', className: 'ax_table_cell', 'data-label': 'Order_unhandle' },
                            _react2.default.createElement('img', { id: 'u139_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/order_unhandle_u139.png' }),
                            _react2.default.createElement(
                                'div',
                                { id: 'u140', className: 'text' },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    _react2.default.createElement(
                                        'span',
                                        null,
                                        '未处理'
                                    )
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'div',
                            { id: 'u141', className: 'ax_table_cell', 'data-label': 'Order_Inprogress' },
                            _react2.default.createElement('img', { id: 'u141_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/order_inprogress_u141.png' }),
                            _react2.default.createElement(
                                'div',
                                { id: 'u142', className: 'text' },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    _react2.default.createElement(
                                        'span',
                                        null,
                                        '进行中'
                                    )
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'div',
                            { id: 'u143', className: 'ax_table_cell', 'data-label': 'order_Complete' },
                            _react2.default.createElement('img', { id: 'u143_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/order_complete_u143.png' }),
                            _react2.default.createElement(
                                'div',
                                { id: 'u144', className: 'text' },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    _react2.default.createElement(
                                        'span',
                                        null,
                                        '已完成'
                                    )
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u145', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u145_img', className: 'img ', src: 'i/images/system_manage_operation_assistant_page/u145.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u146', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '医生信息维护'
                            )
                        )
                    )
                )
            )
        );
    }
});
exports.default = System_manage;

},{"../components/PageContainer":19,"react":356}],48:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _amazeuiReact = require('amazeui-react');

var _auth = require('../components/auth');

var _auth2 = _interopRequireDefault(_auth);

var _User_index = require('../components/User_index');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var User_index = _react2.default.createClass({
    displayName: 'User_index',
    render: function render() {
        return _react2.default.createElement(
            'div',
            null,
            _react2.default.createElement('link', { href: 'i/resources/css/axure_rp_page.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('link', { href: 'i/data/styles.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('link', { href: 'i/css/user_home/style1.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement(
                'div',
                { id: 'base', className: '' },
                _react2.default.createElement(
                    'div',
                    { id: 'u0', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u0_img', className: 'img ', src: 'i/images/user_home/u0.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u1', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u2', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u2_img', className: 'img ', src: 'i/images/user_home/u2.png' }),
                    _react2.default.createElement('div', { id: 'u3', className: 'text' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u4', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u4_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u5', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '订单中心'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u6', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u6_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u7', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '电子病历'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u8', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u8_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u9', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '医患互动'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u10', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u10_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u11', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '客户服务'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u12', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u12_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u13', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '个人信息'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u14', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u14_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u15', className: 'text' },
                        _react2.default.createElement(
                            _amazeuiReact.Tabs,
                            { animation: 'slide' },
                            _react2.default.createElement(
                                _amazeuiReact.Tabs.Item,
                                { eventKey: '1', title: '全部订单' },
                                _react2.default.createElement(_User_index.Order_all, null)
                            ),
                            _react2.default.createElement(
                                _amazeuiReact.Tabs.Item,
                                { eventKey: '2', title: '已支付' },
                                _react2.default.createElement(_User_index.Order_finished, null)
                            ),
                            _react2.default.createElement(
                                _amazeuiReact.Tabs.Item,
                                { eventKey: '3', title: '未支付' },
                                _react2.default.createElement(_User_index.Order_remain, null)
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u60', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u60_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u61', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '手术地点：北京 '
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u64', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u64_img', className: 'img ', src: 'i/images/user_check_operation_reservation_page/u23.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u65', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u66', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u66_img', className: 'img ', src: 'i/images/user_home/u66.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u67', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u68', className: 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u68_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u68_start' }),
                    _react2.default.createElement('img', { id: 'u68_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u68_end' }),
                    _react2.default.createElement('img', { id: 'u68_line', className: 'img ', src: 'i/images/user_home/u68_line.png', alt: 'u68_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u69', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u69_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u70', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                _react2.default.createElement(
                                    Link,
                                    { to: _auth2.default.get_config()['about_us_url'] },
                                    '关于我们'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u71', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u71_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u72', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                _react2.default.createElement(
                                    Link,
                                    { to: _auth2.default.get_config()['connect_us_url'] },
                                    '联系我们'
                                )
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u73', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u73_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u74', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '服务条款'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u75', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u75_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u76', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '媒体更新'
                            )
                        )
                    ),
                    _react2.default.createElement(
                        'div',
                        { id: 'u82', 'class': 'ax_html_button' },
                        _react2.default.createElement('input', null)
                    )
                )
            )
        );
    }
});
exports.default = User_index;

},{"../components/User_index":24,"../components/auth":25,"amazeui-react":52,"react":356}],49:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

var _amazeuiReact = require('amazeui-react');

var _auth = require('../components/auth');

var _auth2 = _interopRequireDefault(_auth);

var _About = require('./About');

var _About2 = _interopRequireDefault(_About);

var _reactRouter = require('react-router');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var User_manage = _react2.default.createClass({
    displayName: 'User_manage',
    getInitialState: function getInitialState() {
        return {
            dataSource: { re: [{ "user_name": "ffff", "user_role": "iii", "user_mobile": "111", "user_last_login": "qqq", "state": "0" }], totalpage: 1 },
            nowpage: '1',
            totalpage: '1'
        };
    },
    query: function query() {
        var _this = this;

        var page = arguments.length <= 0 || arguments[0] === undefined ? this.state.nowpage : arguments[0];

        _auth2.default.myact({ to: 'manager/userlist.do',
            parms: [{ 'key': 'page', 'value': page }]
        }, function (res) {
            _this.updateDataSource(res);
        });
        this.setState({ nowpage: page });
    },
    querynext: function querynext() {
        var nowpage = this.state.nowpage;
        var totalpage = this.state.totalpage;
        if (nowpage + 1 <= totalpage) {
            nowpage += 1;
            this.query(nowpage);
        }
    },
    querybefore: function querybefore() {

        var nowpage = this.state.nowpage;
        var totalpage = this.state.totalpage;
        if (nowpage - 1 >= 1) {
            nowpage -= 1;
            this.query(nowpage);
        }
    },
    deletelist: function deletelist(id) {
        console.log(id);
        _auth2.default.myact({ to: 'manager/deleteuser.do',
            parms: [{ 'key': 'id', 'value': id }]
        }, function (res) {
            console.log(res);
        });
        this.query();
    },
    disable: function disable(id) {
        console.log(id);
        _auth2.default.myact({ to: 'manager/disableuser.do',
            parms: [{ 'key': 'id', 'value': id }]
        }, function (res) {
            console.log(res);
        });
        this.query();
    },
    componentDidMount: function componentDidMount() {
        this.query(1);
    },
    updateDataSource: function updateDataSource(data) {
        this.setState({
            dataSource: data,
            totalpage: data.totalpage
        });
        console.log(data);
    },
    judege: function judege(intState) {
        var state = "";
        if (intState == 0) {
            state = "禁用";
        } else if (intState == 1) {
            state = "启用";
        } else {
            state = "";
        }
        return state;
    },
    kaifazhong: function kaifazhong(str) {
        console.log(str);
    },
    change: function change(id) {
        console.log(id);
        _auth2.default.myact({ to: '/manager/changeuser.do',
            parms: [{ 'key': 'id', 'value': id }, { key: 'name', value: this.refs.un.value }, { key: 'mobile', value: this.refs.um.value }, { key: 'role', value: this.refs.ur.value }, { key: 'state', value: this.refs.ps.value }]
        }, function (res) {
            Console.log(name);
        });
        this.query();
    },
    render: function render() {
        var _this2 = this;

        return _react2.default.createElement(
            _PageContainer2.default,
            null,
            _react2.default.createElement('link', { href: 'i/resources/css/axure_rp_page.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('link', { href: 'i/data/styles.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('link', { href: 'i/css/web_manage_page_root/style2.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement(
                'div',
                { id: 'base', 'class': '' },
                _react2.default.createElement(
                    'div',
                    { id: 'u0', 'class': 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u0_start', 'class': 'img ', src: 'i/resources/images/transparent.gif', alt: 'u0_start' }),
                    _react2.default.createElement('img', { id: 'u0_end', 'class': 'img ', src: 'i/resources/images/transparent.gif', alt: 'u0_end' }),
                    _react2.default.createElement('img', { id: 'u0_line', 'class': 'img ', src: 'i/images/web_manage_page_root/u0_line.png', alt: 'u0_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u1', 'class': 'ax_h1' },
                    _react2.default.createElement('img', { id: 'u1_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u2', 'class': 'text' },
                        _react2.default.createElement(
                            'strong',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '谷雨医生平台系统管理'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u3', 'class': 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u3_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u4', 'class': 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', { className: 'u209' }),
                            _react2.default.createElement(
                                'span',
                                { className: 'u209' },
                                '    '
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u5', 'class': 'ax_horizontal_line' },
                    _react2.default.createElement('img', { id: 'u5_start', 'class': 'img ', src: 'i/resources/images/transparent.gif', alt: 'u5_start' }),
                    _react2.default.createElement('img', { id: 'u5_end', 'class': 'img ', src: 'i/resources/images/transparent.gif', alt: 'u5_end' }),
                    _react2.default.createElement('img', { id: 'u5_line', 'class': 'img ', src: 'i/images/web_manage_page_root/u0_line.png', alt: 'u5_line' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u6', 'class': 'ax_dynamic_panel', 'data-label': '系统管理' },
                    _react2.default.createElement(
                        'div',
                        { id: 'u6_state0', 'class': 'panel_state', 'data-label': '管理账号' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u6_state0_content', 'class': 'panel_state_content' },
                            _react2.default.createElement(
                                'div',
                                { id: 'u7', 'class': 'ax_table' },
                                _react2.default.createElement(_About2.default, null)
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'butt' },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    ' ""'
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u45', 'class': 'ax_h4' },
                                _react2.default.createElement('img', { id: 'u45_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u46', 'class': 'text' },
                                    _react2.default.createElement(
                                        'strong',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '所有账户'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u47', 'class': 'ax_paragraph' },
                                _react2.default.createElement('img', { id: 'u47_img', 'class': 'img ', src: 'i/resources/images/transparent.gif' })
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u189', 'class': 'ax_menu' },
                    _react2.default.createElement('img', { id: 'u189_menu', 'class': 'img ', src: 'i/images/web_manage_page_root/u189_menu.png', alt: 'u189_menu' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u190', 'class': 'ax_table' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u191', 'class': 'ax_table_cell' },
                            _react2.default.createElement('img', { id: 'u191_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u191.png' }),
                            _react2.default.createElement(
                                'div',
                                { id: 'u192', 'class': 'text' },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    _react2.default.createElement(
                                        'span',
                                        null,
                                        '仪表盘'
                                    )
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'div',
                            { id: 'u193', 'class': 'ax_table_cell' },
                            _react2.default.createElement('img', { id: 'u193_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u191.png' }),
                            _react2.default.createElement(
                                'div',
                                { id: 'u194', 'class': 'text' },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    _react2.default.createElement(
                                        _reactRouter.Link,
                                        { to: '/manageRoot' },
                                        _react2.default.createElement(
                                            'span',
                                            { onClick: function onClick() {
                                                    _this2.setState({ 'now': '管理账号' });
                                                } },
                                            '管理账号'
                                        )
                                    )
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'div',
                            { id: 'u195', 'class': 'ax_table_cell' },
                            _react2.default.createElement('img', { id: 'u195_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u191_mouseOver.png' }),
                            _react2.default.createElement(
                                'div',
                                { id: 'u196', 'class': 'text', style: { color: 'white' }, onClick: function onClick() {
                                        _this2.setState({ 'now': '用户管理' });
                                    } },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    _react2.default.createElement(
                                        'span',
                                        null,
                                        '用户管理'
                                    )
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'div',
                            { id: 'u197', 'class': 'ax_table_cell' },
                            _react2.default.createElement('img', { id: 'u197_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u191.png' }),
                            _react2.default.createElement(
                                'div',
                                { id: 'u198', 'class': 'text' },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    _react2.default.createElement(
                                        _reactRouter.Link,
                                        { to: '/price_manage' },
                                        _react2.default.createElement(
                                            'span',
                                            { onClick: function onClick() {
                                                    _this2.setState({ 'now': '价格管理' });
                                                } },
                                            '价格管理'
                                        )
                                    )
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'div',
                            { id: 'u199', 'class': 'ax_table_cell' },
                            _react2.default.createElement('img', { id: 'u199_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u199.png' }),
                            _react2.default.createElement(
                                'div',
                                { id: 'u200', 'class': 'text' },
                                _react2.default.createElement(
                                    'p',
                                    null,
                                    _react2.default.createElement(
                                        'span',
                                        null,
                                        '网站配置'
                                    )
                                )
                            )
                        )
                    ),
                    _react2.default.createElement(
                        'div',
                        { id: 'u201', 'class': 'ax_menu sub_menu' },
                        _react2.default.createElement('img', { id: 'u201_menu', 'class': 'img ', src: 'i/images/web_manage_page_root/u201_menu.png', alt: 'u201_menu' }),
                        _react2.default.createElement(
                            'div',
                            { id: 'u202', 'class': 'ax_table' },
                            _react2.default.createElement(
                                'div',
                                { id: 'u203', 'class': 'ax_table_cell' },
                                _react2.default.createElement('img', { id: 'u203_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u203.png' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u204', 'class': 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '求医用户'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u205', 'class': 'ax_table_cell' },
                                _react2.default.createElement('img', { id: 'u205_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u203.png' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u206', 'class': 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '医生'
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u207', 'class': 'ax_table_cell' },
                                _react2.default.createElement('img', { id: 'u207_img', 'class': 'img ', src: 'i/images/web_manage_page_root/u207.png' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u208', 'class': 'text' },
                                    _react2.default.createElement(
                                        'p',
                                        null,
                                        _react2.default.createElement(
                                            'span',
                                            null,
                                            '机构'
                                        )
                                    )
                                )
                            )
                        )
                    )
                )
            )
        );
    }
});
exports.default = User_manage;

},{"../components/PageContainer":19,"../components/auth":25,"./About":27,"amazeui-react":52,"react":356,"react-router":219}],50:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _PageContainer = require('../components/PageContainer');

var _PageContainer2 = _interopRequireDefault(_PageContainer);

var _reactRouter = require('react-router');

var _auth = require('../components/auth');

var _auth2 = _interopRequireDefault(_auth);

var _amazeuiReact = require('amazeui-react');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var User_reservation = (0, _reactRouter.withRouter)(_react2.default.createClass({
    displayName: 'User_reservation',
    getInitialState: function getInitialState() {
        return {
            yuyue: {
                notice: '',
                didian: '',
                zhuanjia: '',
                yuyueshijian: '2016-8-20',
                shoushuneirong: '',
                shifouyibao: '否',
                xingming: '',
                chushengriqi: '1990-8-8',
                quezhenxinxi: '无',
                bingqingmiaoshu: '',
                lianxirenxingming: '',
                yuhuanzheguanxi: '',
                shouji: '',
                dizhi: '',
                totalFee: '0',
                basicPrice: '0',
                id: ''
            },
            qitafuwu: [],
            qitafuwu1: [{
                description: '大保健1',
                price: '20',
                id: '1'
            }, {
                description: '大保健2',
                price: '20',
                id: '2'
            }]

        };
    },
    query: function query() {
        var _this = this;

        var pars = this.state.yuyue;

        var parms = [{ 'key': 'orderFocus', 'value': pars.notice }, { 'key': 'orderReserveTime', 'value': pars.yuyueshijian }, { 'key': 'orderOperation', 'value': pars.shoushuneirong }, { 'key': 'orderInsurance', 'value': '1' }, { 'key': 'contactRealname', 'value': pars.lianxirenxingming }, { 'key': 'relation', 'value': pars.yuhuanzheguanxi }, { 'key': 'mobile', 'value': pars.shouji }, { 'key': 'contactAddress', 'value': pars.dizhi }, { 'key': 'patientRealname', 'value': pars.xingming }, { 'key': 'patientAge', 'value': '' }, { 'key': 'patientDiagnose', 'value': pars.quezhenxinxi }, { 'key': 'patientDisease', 'value': '' }, { 'key': 'username', 'value': _auth2.default.getUsername() }];
        var qitafuwu = this.get_check_box_value();
        qitafuwu.map(function (l) {
            parms.push({ 'key': 'service', 'value': l });
        });
        console.log(parms);
        _auth2.default.myact({ to: 'orderGen.do',
            parms: parms
        }, function (res) {
            var form = new FormData(_react2.default.createElement('form', { encType: 'multipart/form-data', method: 'post' }));
            form.append("imgFile", _this.refs.my_file.files[0]);
            console.log(_this.refs.my_file.files[0]);
            form.append("username", _auth2.default.getUsername());
            form.append("role", _auth2.default.getRole());
            _auth2.default.post('upload.do', form, function (res) {});
            _this.state.yuyue.basicPrice = res.basicPrice;
            _this.state.yuyue.totalFee = res.totalFee;
            _this.state.yuyue.id = res.id;
            _this.props.router.push({ pathname: '/checkReservation', query: _this.state.yuyue });
        });
    },
    componentWillMount: function componentWillMount() {
        var _this2 = this;

        _auth2.default.get('book.do', {}, function (res) {
            _this2.state.yuyue.notice = res.shift().focus;
            _this2.setState({ qitafuwu1: res });
        });
        _auth2.default.get('list.do', { 'page': '1', 'role': '2', 'paixu': '1' }, function (res) {
            console.log(res);
        });
        var par = this.props.query;
        if (par.realname != undefined) {
            this.state.yuyue.didian = par.reservePlace;
            this.state.yuyue.zhuanjia = par.realname + par.title;
        }
    },
    submitForm: function submitForm() {
        var refss = this.refs;
        console.log(refss.service);
        this.state.yuyue.didian = refss.didian.value;
        this.state.yuyue.yuyueshijian = refss.yuyueshijian.value;
        this.state.yuyue.shoushuneirong = refss.shoushuneirong.value;
        this.state.yuyue.xingming = refss.xingming.value;
        this.state.yuyue.chushengriqi = refss.chushengriqi.value;
        this.state.yuyue.quezhenxinxi = refss.quezhenxinxi.value;
        this.state.yuyue.bingqingmiaoshu = refss.bingqingmiaoshu.value;
        this.state.yuyue.lianxirenxingming = refss.lianxirenxingming.value;
        this.state.yuyue.yuhuanzheguanxi = refss.yuhuanzheguanxi.value;
        this.state.yuyue.shouji = refss.shouji.value;
        this.state.yuyue.dizhi = refss.dizhi.value;
        this.query();
    },
    get_check_box_value: function get_check_box_value() {
        var re = [];
        var inputs = this.refs.mycheckbox.getElementsByTagName('input');
        var _iteratorNormalCompletion = true;
        var _didIteratorError = false;
        var _iteratorError = undefined;

        try {
            for (var _iterator = inputs[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
                var e_input = _step.value;

                if (e_input.checked) {
                    re.push(e_input.value);
                }
            }
        } catch (err) {
            _didIteratorError = true;
            _iteratorError = err;
        } finally {
            try {
                if (!_iteratorNormalCompletion && _iterator.return) {
                    _iterator.return();
                }
            } finally {
                if (_didIteratorError) {
                    throw _iteratorError;
                }
            }
        }

        return re;
    },
    render: function render() {

        var check_boxs = this.state.qitafuwu1.map(function (line) {
            return _react2.default.createElement(
                'div',
                null,
                _react2.default.createElement('input', { type: 'checkbox', value: line.id }),
                '描述：' + line.description + '---价格：' + line.price
            );
        });
        var pr = this.state.yuyue;

        return _react2.default.createElement(
            _PageContainer2.default,
            null,
            _react2.default.createElement('link', { href: 'i/resources/css/axure_rp_page.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('link', { href: 'i/data/styles.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('link', { href: 'i/css/user_reservation_operation_fill_page/styles.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement('link', { href: 'i/css/react-select.css', type: 'text/css', rel: 'stylesheet' }),
            _react2.default.createElement(
                'div',
                { id: 'base', className: '' },
                _react2.default.createElement(
                    'div',
                    { id: 'u0', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u0_img', className: 'img ', src: 'i/images/user_reservation_operation_fill_page/u0.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u1', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '      '
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                '注意'
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                '事项'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u2', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u2_img', className: 'img ', src: 'i/images/user_reservation_operation_fill_page/u2.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u3', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '    '
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                '患者'
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                '信息'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u4', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u4_img', className: 'img ', src: 'i/images/user_reservation_operation_fill_page/u4.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u5', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '      联系人'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u6', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u6_img', className: 'img ', src: 'i/images/user_reservation_operation_fill_page/u4.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u7', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '  '
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                '  '
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                '其他服务'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u8', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u8_img', className: 'img ', src: 'i/images/user_reservation_operation_fill_page/u0.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u9', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '    使用须知'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u10', className: 'ax_shape', onClick: this.submitForm },
                    _react2.default.createElement('img', { id: 'u10_img', className: 'img ', src: 'i/images/user_reservation_operation_fill_page/u10.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u11', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '核对手术预约信息'
                            )
                        )
                    ),
                    _react2.default.createElement(
                        'div',
                        { id: 'u45', 'class': 'ax_html_button' },
                        _react2.default.createElement('input', null)
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u12', className: 'ax_image' },
                    _react2.default.createElement('img', { id: 'u12_img', className: 'img ', src: 'i/images/user_check_operation_reservation_page/u21.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u13', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u14', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u14_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u15', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '手术地点： 北京 '
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u16', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u16_img', className: 'img ', src: 'i/images/user_reservation_operation_fill_page/u16.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u17', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null),
                            _react2.default.createElement(
                                'span',
                                null,
                                pr.notice
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u18', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u18_img', className: 'img ', src: 'i/images/user_reservation_operation_fill_page/u18.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u19', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                ' 姓名：'
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                '            '
                            ),
                            _react2.default.createElement('input', { type: 'text', defaultValue: pr.xingming, ref: 'xingming' })
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                ' 出生日期：'
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                '  '
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                '  '
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                _react2.default.createElement('input', { type: 'text', defaultValue: pr.chushengriqi, ref: 'chushengriqi' })
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                ' 确诊信息：'
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                '    '
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                _react2.default.createElement('input', { type: 'text', defaultValue: pr.quezhenxinxi, ref: 'quezhenxinxi' })
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                ' 病情描述：'
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '  '
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '  '
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '  '
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                ' CT或相关图片资料：'
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '  '
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u20', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u20_img', className: 'img ', src: 'i/images/user_reservation_operation_fill_page/u16.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u21', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                ' 姓名：'
                            ),
                            _react2.default.createElement('input', { type: 'text', defaultValue: pr.lianxirenxingming, ref: 'lianxirenxingming' })
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                ' 与患者关系：'
                            ),
                            _react2.default.createElement('input', { type: 'text', defaultValue: pr.yuhuanzheguanxi, ref: 'yuhuanzheguanxi' })
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                ' 手机：'
                            ),
                            _react2.default.createElement('input', { type: 'text', defaultValue: pr.shouji, ref: 'shouji' })
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                ' 地址：'
                            ),
                            _react2.default.createElement('input', { type: 'text', defaultValue: pr.dizhi, ref: 'dizhi' })
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u22', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u22_img', className: 'img ', src: 'i/images/user_reservation_operation_fill_page/u16.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u23', className: 'text' },
                        _react2.default.createElement(
                            'div',
                            { ref: 'mycheckbox' },
                            check_boxs
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u24', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u24_img', className: 'img ', src: 'i/images/user_reservation_operation_fill_page/u16.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u25', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                { className: 'read' },
                                ' 提交手术预约单意味着您仔细阅读并同意以下预约服务协议 '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'protocal' },
                                '谷雨医生平台服务协议'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u26', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u26_img', className: 'img ', src: 'i/images/user_reservation_operation_fill_page/u4.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u27', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '    服务信息'
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u28', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u28_img', className: 'img ', src: 'i/images/user_reservation_operation_fill_page/u16.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u29', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '  '
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                '地点：',
                                _react2.default.createElement('input', { ref: 'didian', defaultValue: pr.didian })
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '  '
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                '专家：',
                                pr.zhuanjia
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '  '
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                '预约时间:',
                                _react2.default.createElement('input', { defaultValue: pr.yuyueshijian, ref: 'yuyueshijian' })
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '  '
                            ),
                            _react2.default.createElement(
                                'span',
                                null,
                                '手术内容：',
                                _react2.default.createElement('input', { defaultValue: pr.shoushuneirong, ref: 'shoushuneirong' })
                            )
                        ),
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement(
                                'span',
                                null,
                                '  '
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u31', className: 'ax_html_button' },
                    _react2.default.createElement('input', { ref: 'my_file', type: 'file', id: 'u31_input', value: '上传' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u32', className: 'ax_text_area' },
                    _react2.default.createElement('textarea', { ref: 'bingqingmiaoshu', id: 'u32_input' })
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u33', className: 'ax_shape' },
                    _react2.default.createElement('img', { id: 'u33_img', className: 'img ', src: 'i/images/user_check_operation_reservation_page/u23.png' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u34', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', null)
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u35', className: 'ax_paragraph' },
                    _react2.default.createElement('img', { id: 'u35_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                    _react2.default.createElement(
                        'div',
                        { id: 'u36', className: 'text' },
                        _react2.default.createElement(
                            'p',
                            null,
                            _react2.default.createElement('span', { className: 'read' }),
                            _react2.default.createElement('span', { className: 'hi' }),
                            _react2.default.createElement(
                                'span',
                                { className: 'read' },
                                '    '
                            ),
                            _react2.default.createElement(
                                'span',
                                { className: 'read' },
                                '              '
                            )
                        )
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { id: 'u37', className: 'ax_dynamic_panel' },
                    _react2.default.createElement(
                        'div',
                        { id: 'u37_state0', className: 'panel_state', 'data-label': 'State1' },
                        _react2.default.createElement(
                            'div',
                            { id: 'u37_state0_content', className: 'panel_state_content' },
                            _react2.default.createElement(
                                'div',
                                { id: 'u38', className: 'ax_menu' },
                                _react2.default.createElement('img', { id: 'u38_menu', className: 'img ', src: 'i/images/user_check_operation_reservation_page/u32_menu.png', alt: 'u38_menu' }),
                                _react2.default.createElement(
                                    'div',
                                    { id: 'u39', className: 'ax_table' },
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u40', className: 'ax_table_cell' },
                                        _react2.default.createElement('img', { id: 'u40_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                                        _react2.default.createElement(
                                            'div',
                                            { id: 'u41', className: 'text' },
                                            _react2.default.createElement(
                                                'p',
                                                null,
                                                _react2.default.createElement(
                                                    'span',
                                                    null,
                                                    '我的订单'
                                                )
                                            )
                                        )
                                    ),
                                    _react2.default.createElement(
                                        'div',
                                        { id: 'u42', className: 'ax_table_cell' },
                                        _react2.default.createElement('img', { id: 'u42_img', className: 'img ', src: 'i/resources/images/transparent.gif' }),
                                        _react2.default.createElement(
                                            'div',
                                            { id: 'u43', className: 'text' },
                                            _react2.default.createElement(
                                                'p',
                                                null,
                                                _react2.default.createElement(
                                                    'span',
                                                    null,
                                                    '退出'
                                                )
                                            )
                                        )
                                    )
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { id: 'u44', className: 'ax_horizontal_line' },
                                _react2.default.createElement('img', { id: 'u44_start', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u44_start' }),
                                _react2.default.createElement('img', { id: 'u44_end', className: 'img ', src: 'i/resources/images/transparent.gif', alt: 'u44_end' }),
                                _react2.default.createElement('img', { id: 'u44_line', className: 'img ', src: 'i/images/user_check_operation_reservation_page/u38_line.png', alt: 'u44_line' })
                            )
                        )
                    )
                )
            )
        );
    }
}));

// Be sure to include styles at some point, probably during your bootstrapping
exports.default = User_reservation;

},{"../components/PageContainer":19,"../components/auth":25,"amazeui-react":52,"react":356,"react-router":219}],51:[function(require,module,exports){
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.About_us = exports.About = exports.Logout = exports.User_index = exports.Account_manage = exports.Price_manage = exports.User_manage = exports.System_manage = exports.Query_result = exports.Institution_list = exports.Doctor_list = exports.User_reservation = exports.ManageRoot = exports.Register_hospital = exports.CheckReservation = exports.Register_doctor = exports.Register_user = exports.AdminLogin = exports.Login = exports.Index_home = exports.Order = exports.Profile = exports.Messages = exports.Home = undefined;

var _Home2 = require('./Home');

var _Home3 = _interopRequireDefault(_Home2);

var _Messages2 = require('./Messages');

var _Messages3 = _interopRequireDefault(_Messages2);

var _Profile2 = require('./Profile');

var _Profile3 = _interopRequireDefault(_Profile2);

var _Order2 = require('./Order');

var _Order3 = _interopRequireDefault(_Order2);

var _Index_home2 = require('./Index_home');

var _Index_home3 = _interopRequireDefault(_Index_home2);

var _Login2 = require('./Login');

var _Login3 = _interopRequireDefault(_Login2);

var _AdminLogin2 = require('./AdminLogin');

var _AdminLogin3 = _interopRequireDefault(_AdminLogin2);

var _Register_user2 = require('./Register_user');

var _Register_user3 = _interopRequireDefault(_Register_user2);

var _Register_doctor2 = require('./Register_doctor');

var _Register_doctor3 = _interopRequireDefault(_Register_doctor2);

var _CheckReservation2 = require('./CheckReservation');

var _CheckReservation3 = _interopRequireDefault(_CheckReservation2);

var _Register_hospital2 = require('./Register_hospital');

var _Register_hospital3 = _interopRequireDefault(_Register_hospital2);

var _ManageRoot2 = require('./ManageRoot');

var _ManageRoot3 = _interopRequireDefault(_ManageRoot2);

var _User_reservation2 = require('./User_reservation');

var _User_reservation3 = _interopRequireDefault(_User_reservation2);

var _Doctor_list2 = require('./Doctor_list');

var _Doctor_list3 = _interopRequireDefault(_Doctor_list2);

var _Institution_list2 = require('./Institution_list');

var _Institution_list3 = _interopRequireDefault(_Institution_list2);

var _Query_result2 = require('./Query_result');

var _Query_result3 = _interopRequireDefault(_Query_result2);

var _System_manage2 = require('./System_manage');

var _System_manage3 = _interopRequireDefault(_System_manage2);

var _User_manage2 = require('./User_manage');

var _User_manage3 = _interopRequireDefault(_User_manage2);

var _Price_manage3 = require('./Price_manage');

var _Price_manage4 = _interopRequireDefault(_Price_manage3);

var _Account_manage2 = require('./Account_manage');

var _Account_manage3 = _interopRequireDefault(_Account_manage2);

var _User_index2 = require('./User_index');

var _User_index3 = _interopRequireDefault(_User_index2);

var _Logout2 = require('./Logout');

var _Logout3 = _interopRequireDefault(_Logout2);

var _About2 = require('./About');

var _About3 = _interopRequireDefault(_About2);

var _About_us2 = require('./About_us');

var _About_us3 = _interopRequireDefault(_About_us2);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.Home = _Home3.default;
exports.Messages = _Messages3.default;
exports.Profile = _Profile3.default;
exports.Order = _Order3.default;
exports.Index_home = _Index_home3.default;
exports.Login = _Login3.default;
exports.AdminLogin = _AdminLogin3.default;
exports.Register_user = _Register_user3.default;
exports.Register_doctor = _Register_doctor3.default;
exports.CheckReservation = _CheckReservation3.default;
exports.Register_hospital = _Register_hospital3.default;
exports.ManageRoot = _ManageRoot3.default;
exports.User_reservation = _User_reservation3.default;
exports.Doctor_list = _Doctor_list3.default;
exports.Institution_list = _Institution_list3.default;
exports.Query_result = _Query_result3.default;
exports.System_manage = _System_manage3.default;
exports.User_manage = _User_manage3.default;
exports.Price_manage = _Price_manage4.default;
exports.Account_manage = _Account_manage3.default;
exports.Price_manage = _Price_manage4.default;
exports.User_index = _User_index3.default;
exports.Logout = _Logout3.default;
exports.About = _About3.default;
exports.About_us = _About_us3.default;

},{"./About":27,"./About_us":28,"./Account_manage":29,"./AdminLogin":30,"./CheckReservation":31,"./Doctor_list":32,"./Home":33,"./Index_home":34,"./Institution_list":35,"./Login":36,"./Logout":37,"./ManageRoot":38,"./Messages":39,"./Order":40,"./Price_manage":41,"./Profile":42,"./Query_result":43,"./Register_doctor":44,"./Register_hospital":45,"./Register_user":46,"./System_manage":47,"./User_index":48,"./User_manage":49,"./User_reservation":50}],52:[function(require,module,exports){
'use strict';

var React = require('react');

if (!React) {
  throw new Error('AMUIReact requires React.');
}

module.exports = {
  VERSION: '1.2.2',

  // layout
  Grid: require('./Grid'),
  Col: require('./Col'),
  Container: require('./Container'),
  AvgGrid: require('./AvgGrid'),

  // elements
  Button: require('./Button'),
  ButtonToolbar: require('./ButtonToolbar'),
  ButtonCheck: require('./ButtonCheck'),
  ButtonGroup: require('./ButtonGroup'),

  // form related
  Form: require('./Form'),
  FormGroup: require('./FormGroup'),
  FormFile: require('./FormFile'),
  Input: require('./Input'),
  UCheck: require('./UCheck'),

  Image: require('./Image'),
  Thumbnail: require('./Thumbnail'),
  Thumbnails: require('./Thumbnails'),
  Table: require('./Table'),
  // Code: require('./Code'),

  // Navs
  Nav: require('./Nav'),
  NavItem: require('./NavItem'),
  Breadcrumb: require('./Breadcrumb'),
  Pagination: require('./Pagination'),
  Topbar: require('./Topbar'),
  Tabs: require('./Tabs'),
  CollapsibleNav: require('./CollapsibleNav'),

  Article: require('./Article'),
  Badge: require('./Badge'),
  Close: require('./Close'),
  Icon: require('./Icon'),
  List: require('./List'),
  ListItem: require('./ListItem'),
  Panel: require('./Panel'),
  PanelGroup: require('./PanelGroup'),
  Progress: require('./Progress'),

  Alert: require('./Alert'),
  // DatePicker: require('./DateTimePicker'),
  // TimePicker: require('./DateTimePicker'),
  DateTimeInput: require('./DateTimePicker').DateTimeInput,
  DateTimePicker: require('./DateTimePicker').DateTimePicker,
  Dropdown: require('./Dropdown'),
  Modal: require('./Modal'),
  ModalTrigger: require('./ModalTrigger'),
  Popover: require('./Popover'),
  PopoverTrigger: require('./PopoverTrigger'),
  NProgress: require('./NProgress'),
  ScrollSpy: require('./ScrollSpy'),
  ScrollSpyNav: require('./ScrollSpyNav'),
  Selected: require('./Selected'),
  Slider: require('./Slider'),
  Sticky: require('./Sticky'),

  // widgets
  Accordion: require('./Accordion'),
  Divider: require('./Divider'),
  Footer: require('./Footer'),
  Gallery: require('./Gallery'),
  GoTop: require('./GoTop'),
  Header: require('./Header'),
  ListNews: require('./ListNews'),
  Menu: require('./Menu'),
  Navbar: require('./Navbar'),
  Titlebar: require('./Titlebar'),

  // mixins
  mixins: require('./mixins')
};
},{"./Accordion":53,"./Alert":54,"./Article":55,"./AvgGrid":56,"./Badge":57,"./Breadcrumb":58,"./Button":59,"./ButtonCheck":60,"./ButtonGroup":61,"./ButtonToolbar":62,"./Close":63,"./Col":64,"./CollapsibleNav":65,"./Container":66,"./DateTimePicker":71,"./Divider":72,"./Dropdown":73,"./Footer":74,"./Form":75,"./FormFile":76,"./FormGroup":77,"./Gallery":78,"./GoTop":79,"./Grid":80,"./Header":81,"./Icon":82,"./Image":83,"./Input":84,"./List":85,"./ListItem":86,"./ListNews":87,"./Menu":88,"./Modal":89,"./ModalTrigger":90,"./NProgress":91,"./Nav":92,"./NavItem":93,"./Navbar":94,"./Pagination":95,"./Panel":96,"./PanelGroup":97,"./Popover":98,"./PopoverTrigger":99,"./Progress":100,"./ScrollSpy":101,"./ScrollSpyNav":102,"./Selected":103,"./Slider":104,"./Sticky":105,"./Table":106,"./Tabs":107,"./Thumbnail":108,"./Thumbnails":109,"./Titlebar":110,"./Topbar":111,"./UCheck":112,"./mixins":119,"react":356}],53:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var ReactDOM = require('react-dom');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var CollapseMixin = require('./mixins/CollapseMixin');

var Accordion = React.createClass({
  displayName: 'Accordion',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    theme: React.PropTypes.oneOf(['default', 'basic', 'gapped']),
    data: React.PropTypes.array,
    activeKey: React.PropTypes.any,
    defaultActiveKey: React.PropTypes.any
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'accordion',
      theme: 'default'
    };
  },

  getInitialState: function getInitialState() {
    return {
      activeKey: this.props.defaultActiveKey
    };
  },

  handleSelect: function handleSelect(e, key) {
    e.preventDefault();

    if (this.state.activeKey === key) {
      key = null;
    }

    this.setState({
      activeKey: key
    });
  },

  render: function render() {
    var classSet = this.getClassSet();
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    classSet[this.prefixClass(this.props.theme)] = true;

    return React.createElement(
      'section',
      _extends({}, restProps, {
        'data-am-widget': this.props.classPrefix,
        className: classNames(classSet, this.props.className)
      }),
      this.props.data.map(function (item, index) {
        return React.createElement(
          Accordion.Item,
          {
            title: item.title,
            expanded: item.active && item.disabled,
            defaultExpanded: item.active && !item.disabled,
            eventKey: index,
            key: index
          },
          item.content
        );
      })
    );
  }
});

Accordion.Item = React.createClass({
  displayName: 'Item',

  mixins: [ClassNameMixin, CollapseMixin],

  propTypes: {
    title: React.PropTypes.node,
    expanded: React.PropTypes.bool
  },

  handleToggle: function handleToggle() {
    this.setState({
      expanded: !this.state.expanded
    });
  },

  getCollapsibleDimensionValue: function getCollapsibleDimensionValue() {
    return ReactDOM.findDOMNode(this.refs.panel).scrollHeight;
  },

  getCollapsibleDOMNode: function getCollapsibleDOMNode() {
    if (!this.isMounted() || !this.refs || !this.refs.panel) {
      return null;
    }

    return ReactDOM.findDOMNode(this.refs.panel);
  },

  render: function render() {
    return React.createElement(
      'dl',
      {
        className: classNames(this.setClassNamespace('accordion-item'), this.isExpanded() ? this.setClassNamespace('active') : null, this.props.expanded ? this.setClassNamespace('disabled') : null)
      },
      React.createElement(
        'dt',
        {
          onClick: this.handleToggle,
          className: this.setClassNamespace('accordion-title')
        },
        this.props.title
      ),
      React.createElement(
        'dd',
        {
          className: classNames(this.getCollapsibleClassSet()),
          ref: 'panel' },
        React.createElement('div', {
          className: this.setClassNamespace('accordion-content'),
          dangerouslySetInnerHTML: { __html: this.props.children }
        })
      )
    );
  }
});

module.exports = Accordion;
},{"./mixins/ClassNameMixin":114,"./mixins/CollapseMixin":115,"classnames":132,"object.omit":186,"react":356,"react-dom":189}],54:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');

var Alert = React.createClass({
  displayName: 'Alert',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string.isRequired,
    amStyle: React.PropTypes.oneOf(['secondary', 'success', 'warning', 'danger']),
    onClose: React.PropTypes.func
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'alert'
    };
  },

  renderCloseBtn: function renderCloseBtn() {
    return React.createElement(
      'button',
      {
        type: 'button',
        className: this.setClassNamespace('close'),
        onClick: this.props.onClose
      },
      '×'
    );
  },

  render: function render() {
    var classSet = this.getClassSet();
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));
    var isCloseable = !!this.props.onClose;

    if (this.props.amStyle) {
      classSet[this.prefixClass(this.props.amStyle)] = true;
    }

    classSet[this.prefixClass('closeable')] = isCloseable;

    return React.createElement(
      'div',
      _extends({}, restProps, {
        className: classNames(this.props.className, classSet)
      }),
      isCloseable ? this.renderCloseBtn() : null,
      this.props.children
    );
  }
});

module.exports = Alert;
},{"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],55:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');

var Article = React.createClass({
  displayName: 'Article',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    title: React.PropTypes.node,
    meta: React.PropTypes.node,
    lead: React.PropTypes.node
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'article'
    };
  },

  render: function render() {
    var classSet = this.getClassSet();
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    return React.createElement(
      'article',
      _extends({}, restProps, {
        className: classNames(classSet, this.props.className)
      }),
      React.createElement(
        'header',
        { className: this.prefixClass('hd') },
        this.props.title ? React.createElement(
          Article.Child,
          { role: 'title' },
          this.props.title
        ) : null,
        this.props.meta ? React.createElement(
          Article.Child,
          { role: 'meta' },
          this.props.meta
        ) : null
      ),
      React.createElement(
        'div',
        { className: this.prefixClass('bd') },
        this.props.lead ? React.createElement(
          Article.Child,
          { role: 'lead' },
          this.props.lead
        ) : null,
        this.props.children
      )
    );
  }
});

Article.Child = React.createClass({
  displayName: 'Child',

  mixins: [ClassNameMixin],

  propTypes: {
    role: React.PropTypes.string.isRequired
  },

  getDefaultProps: function getDefaultProps() {
    return {
      role: 'title'
    };
  },

  render: function render() {
    var role = this.props.role;
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));
    var Component;
    var classes = classNames(this.props.className, this.setClassNamespace('article-' + role));

    switch (role) {
      case 'meta':
      case 'lead':
        Component = 'p';
        break;
      case 'title':
        Component = 'h1';
        break;
      default:
        Component = 'div';
    }

    return role === 'divider' ? React.createElement('hr', _extends({}, restProps, {
      className: classes
    })) : React.createElement(
      Component,
      _extends({}, restProps, {
        className: classes
      }),
      this.props.children
    );
  }
});

module.exports = Article;
},{"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],56:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');

var AvgGrid = React.createClass({
  displayName: 'AvgGrid',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string.isRequired,
    component: React.PropTypes.node,
    sm: React.PropTypes.number,
    md: React.PropTypes.number,
    lg: React.PropTypes.number
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'avg',
      component: 'ul'
    };
  },

  render: function render() {
    var Component = this.props.component;
    var classSet = {};
    var prefixClass = this.prefixClass;
    var props = this.props;
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    ['sm', 'md', 'lg'].forEach(function (size) {
      if (props[size]) {
        classSet[prefixClass(size + '-' + props[size])] = true;
      }
    });

    return React.createElement(
      Component,
      _extends({}, restProps, {
        className: classNames(this.props.className, classSet)
      }),
      this.props.children
    );
  }
});

module.exports = AvgGrid;
},{"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],57:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');

var Badge = React.createClass({
  displayName: 'Badge',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    component: React.PropTypes.node,
    href: React.PropTypes.string,
    round: React.PropTypes.bool,
    radius: React.PropTypes.bool,
    amStyle: React.PropTypes.string
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'badge',
      component: 'span'
    };
  },

  renderAnchor: function renderAnchor(classSet) {
    var Component = this.props.component || 'a';
    var href = this.props.href || '#';
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    return React.createElement(
      Component,
      _extends({}, restProps, {
        href: href,
        className: classNames(classSet, this.props.className),
        role: 'badge'
      }),
      this.props.children
    );
  },

  render: function render() {
    var classSet = this.getClassSet();
    var Component = this.props.component;
    var renderAnchor = this.props.href;
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    if (renderAnchor) {
      return this.renderAnchor(classSet);
    }

    return React.createElement(
      Component,
      _extends({}, restProps, {
        className: classNames(classSet, this.props.className)
      }),
      this.props.children
    );
  }
});

module.exports = Badge;
},{"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],58:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var assign = require('object-assign');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');

var Breadcrumb = React.createClass({
  displayName: 'Breadcrumb',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    component: React.PropTypes.node.isRequired,
    slash: React.PropTypes.bool
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'breadcrumb',
      component: 'ul'
    };
  },

  render: function render() {
    var classes = this.getClassSet();
    var Component = this.props.component;
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    classes[this.prefixClass('slash')] = this.props.slash;

    return React.createElement(
      Component,
      _extends({}, restProps, {
        className: classNames(classes, this.props.className)
      }),
      this.props.children
    );
  }
});

Breadcrumb.Item = React.createClass({
  displayName: 'Item',

  mixins: [ClassNameMixin],

  propTypes: {
    active: React.PropTypes.bool,
    href: React.PropTypes.string,
    title: React.PropTypes.string,
    target: React.PropTypes.string,
    component: React.PropTypes.any,
    linkComponent: React.PropTypes.any,
    linkProps: React.PropTypes.object
  },

  getDefaultProps: function getDefaultProps() {
    return {
      component: 'li'
    };
  },


  renderAnchor: function renderAnchor(classes) {
    var Component = this.props.component;
    var linkComponent = this.props.linkComponent || 'a';
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    return React.createElement(
      Component,
      _extends({}, restProps, {
        className: classes
      }),
      React.createElement(linkComponent, assign({
        href: this.props.href,
        title: this.props.title,
        target: this.props.target
      }, this.props.linkProps), this.props.children)
    );
  },

  render: function render() {
    var classes = classNames(this.props.className);
    var Component = this.props.component;
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    if (this.props.href || this.props.linkComponent) {
      return this.renderAnchor(classes);
    }

    return React.createElement(
      Component,
      _extends({}, restProps, {
        className: classes
      }),
      this.props.children
    );
  }
});

module.exports = Breadcrumb;
},{"./mixins/ClassNameMixin":114,"classnames":132,"object-assign":185,"object.omit":186,"react":356}],59:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var omit = require('object.omit');

var Button = React.createClass({
  displayName: 'Button',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string.isRequired,
    active: React.PropTypes.bool,
    block: React.PropTypes.bool,
    disabled: React.PropTypes.bool,
    radius: React.PropTypes.bool,
    round: React.PropTypes.bool,
    component: React.PropTypes.node,
    href: React.PropTypes.string,
    target: React.PropTypes.string,
    type: React.PropTypes.string,
    amSize: React.PropTypes.string,
    amStyle: React.PropTypes.string
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'btn',
      type: 'button',
      amStyle: 'default'
    };
  },

  removeUnknownProps: function removeUnknownProps() {
    return omit(this.props, ['classPrefix', 'active', 'block', 'radius', 'round', 'component', 'amSize', 'amStyle']);
  },


  renderAnchor: function renderAnchor(classSet) {
    var Component = this.props.component || 'a';
    var href = this.props.href || '#';
    var restProps = this.removeUnknownProps();

    return React.createElement(
      Component,
      _extends({}, restProps, {
        href: href,
        target: this.props.target,
        className: classNames(this.props.className, classSet),
        role: 'button'
      }),
      this.props.children
    );
  },

  renderButton: function renderButton(classSet) {
    var Component = this.props.component || 'button';
    var restProps = this.removeUnknownProps();

    return React.createElement(
      Component,
      _extends({}, restProps, {
        className: classNames(this.props.className, classSet)
      }),
      this.props.children
    );
  },

  render: function render() {
    var classSet = this.getClassSet();
    var renderType = this.props.href || this.props.target ? 'renderAnchor' : 'renderButton';

    // block button
    classSet[this.prefixClass('block')] = this.props.block;

    return this[renderType](classSet);
  }
});

module.exports = Button;
},{"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],60:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var omit = require('object.omit');
var CSSCore = require('./utils/CSSCore');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var ButtonGroup = require('./ButtonGroup');
var constants = require('./constants');

var ButtonCheck = React.createClass({
  displayName: 'ButtonCheck',

  mixins: [ClassNameMixin],

  propTypes: {
    clickHandler: React.PropTypes.func
  },

  getDefaultProps: function getDefaultProps() {
    return {
      clickHandler: function clickHandler() {}
    };
  },

  handleClick: function handleClick(e) {
    var changed = true;
    var target = e.target;
    var activeClassName = constants.CLASSES.active;

    if (target && target.nodeName === 'INPUT') {
      var parent = target.parentNode;

      if (target.type === 'radio') {
        if (target.checked && CSSCore.hasClass(parent, activeClassName)) {
          changed = false;
        } else {
          var siblings = parent.parentNode.children;

          // remove siblings activeClassName
          for (var i = 0; i < siblings.length; i++) {
            siblings[i] !== parent && CSSCore.removeClass(siblings[i], activeClassName);
          }
        }
      }

      if (changed) {
        CSSCore.toggleClass(parent, activeClassName);
      }
    }

    this.props.clickHandler.call(this);
  },

  render: function render() {
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    return React.createElement(
      ButtonGroup,
      _extends({}, restProps, {
        onClick: this.handleClick,
        className: this.setClassNamespace('btn-group-check')
      }),
      this.props.children
    );
  }
});

module.exports = ButtonCheck;
},{"./ButtonGroup":61,"./constants":113,"./mixins/ClassNameMixin":114,"./utils/CSSCore":120,"object.omit":186,"react":356}],61:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');

var ButtonGroup = React.createClass({
  displayName: 'ButtonGroup',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string.isRequired,
    stacked: React.PropTypes.bool,
    justify: React.PropTypes.bool
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'btn-group'
    };
  },

  render: function render() {
    var classSet = this.getClassSet();
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    classSet[this.prefixClass('stacked')] = this.props.stacked;
    classSet[this.prefixClass('justify')] = this.props.justify;

    return React.createElement(
      'div',
      _extends({}, restProps, {
        className: classNames(this.props.className, classSet)
      }),
      this.props.children
    );
  }
});

module.exports = ButtonGroup;
},{"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],62:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');

var ButtonToolbar = React.createClass({
  displayName: 'ButtonToolbar',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string.isRequired
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'btn-toolbar'
    };
  },

  render: function render() {
    var classSet = this.getClassSet();
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    return React.createElement(
      'div',
      _extends({}, restProps, {
        className: classNames(this.props.className, classSet)
      }),
      this.props.children
    );
  }
});

module.exports = ButtonToolbar;
},{"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],63:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var Icon = require('./Icon');

var Close = React.createClass({
  displayName: 'Close',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string.isRequired,
    component: React.PropTypes.node,
    spin: React.PropTypes.bool,
    alt: React.PropTypes.bool,
    icon: React.PropTypes.bool,
    type: React.PropTypes.string
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'close',
      type: 'button'
    };
  },

  render: function render() {
    var Component = this.props.component || 'button';
    var classSet = this.getClassSet();
    var props = this.props;
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    // transfer type
    if (Component !== 'button') {
      props.type = undefined;
    }

    // className am-close-alt am-close-spin
    classSet[this.prefixClass('alt')] = this.props.alt;
    classSet[this.prefixClass('spin')] = this.props.spin;

    return React.createElement(
      Component,
      _extends({}, restProps, {
        className: classNames(classSet, this.props.className),
        role: 'close'
      }),
      this.props.icon ? React.createElement(Icon, { icon: 'times' }) : '×'
    );
  }
});

module.exports = Close;
},{"./Icon":82,"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],64:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');

var Col = React.createClass({
  displayName: 'Col',

  mixins: [ClassNameMixin],

  propTypes: {
    sm: React.PropTypes.number,
    md: React.PropTypes.number,
    lg: React.PropTypes.number,
    smOffset: React.PropTypes.number,
    mdOffset: React.PropTypes.number,
    lgOffset: React.PropTypes.number,
    smPush: React.PropTypes.number,
    mdPush: React.PropTypes.number,
    lgPush: React.PropTypes.number,
    smPull: React.PropTypes.number,
    mdPull: React.PropTypes.number,
    lgPull: React.PropTypes.number,
    classPrefix: React.PropTypes.string.isRequired,
    component: React.PropTypes.node.isRequired,
    end: React.PropTypes.bool
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'u',
      component: 'div'
    };
  },

  render: function render() {
    var Component = this.props.component;
    var classSet = {};
    var props = this.props;
    var prefixClass = this.prefixClass;
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    ['sm', 'md', 'lg'].forEach(function (size) {
      var prop = size;

      if (props[size]) {
        classSet[prefixClass(size + '-' + props[prop])] = true;
      }

      prop = size + 'Offset';
      if (props[prop] >= 0) {
        classSet[prefixClass(size + '-offset-') + props[prop]] = true;
      }
      restProps = omit(restProps, prop);

      prop = size + 'Push';
      if (props[prop] >= 0) {
        classSet[prefixClass(size + '-push-') + props[prop]] = true;
      }
      restProps = omit(restProps, prop);

      prop = size + 'Pull';
      if (props[prop] >= 0) {
        classSet[prefixClass(size + '-pull-') + props[prop]] = true;
      }
      restProps = omit(restProps, prop);

      // `xxResetOrder` prop
      // - smResetOrder
      // - mdResetOrder
      // - lgResetOrder
      if (props[size + 'ResetOrder']) {
        classSet[prefixClass(size + '-reset-order')] = true;
      }
      restProps = omit(restProps, size + 'ResetOrder');

      // `xxCentered` prop
      // - smCentered
      // - mdCentered
      // - lgCentered
      if (props[size + 'Centered']) {
        classSet[prefixClass(size + '-centered')] = true;
      }
      restProps = omit(restProps, size + 'Centered');

      // `xxUnCentered` prop
      // - smUnCentered
      // - mdUnCentered
      // - lgUnCentered
      if (props[size + 'UnCentered']) {
        classSet[prefixClass(size + '-uncentered')] = true;
      }
      restProps = omit(restProps, size + 'UnCentered');
    });

    // `end` prop - end column
    classSet[prefixClass('end')] = props.end;

    return React.createElement(
      Component,
      _extends({}, restProps, {
        className: classNames(this.props.className, classSet)
      }),
      this.props.children
    );
  }
});

module.exports = Col;
},{"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],65:[function(require,module,exports){
'use strict';

/*
* https://github.com/react-bootstrap/react-bootstrap/blob/master/src/CollapsibleNav.js
* */

var React = require('react');
var ReactDOM = require('react-dom');
var classNames = require('classnames');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var CollapseMixin = require('./mixins/CollapseMixin');
var createChainedFunction = require('./utils/createChainedFunction');

var CollapsibleNav = React.createClass({
  displayName: 'CollapsibleNav',

  mixins: [ClassNameMixin, CollapseMixin],

  propTypes: {
    collapsible: React.PropTypes.bool,
    onSelect: React.PropTypes.func,
    activeHref: React.PropTypes.string,
    activeKey: React.PropTypes.any,
    expanded: React.PropTypes.bool,
    eventKey: React.PropTypes.any
  },

  handleToggle: function handleToggle() {
    this.setState({ expanded: !this.state.expanded });
  },

  getCollapsibleDimensionValue: function getCollapsibleDimensionValue() {
    var height = 0;
    var nodes = this.refs;

    for (var key in nodes) {
      if (nodes.hasOwnProperty(key)) {
        var n = ReactDOM.findDOMNode(nodes[key]);
        var h = n.offsetHeight;
        var computedStyles = getComputedStyle(n, null);

        height += h + parseInt(computedStyles.marginTop, 10) + parseInt(computedStyles.marginBottom, 10);
      }
    }

    return height;
  },

  getCollapsibleDOMNode: function getCollapsibleDOMNode() {
    return ReactDOM.findDOMNode(this);
  },

  getChildActiveProp: function getChildActiveProp(child) {
    if (child.props.active) {
      return true;
    }

    if (this.props.activeKey != null) {
      if (child.props.eventKey === this.props.activeKey) {
        return true;
      }
    }

    if (this.props.activeHref != null) {
      if (child.props.href === this.props.activeHref) {
        return true;
      }
    }

    return child.props.active;
  },

  renderChildren: function renderChildren(child, index) {
    var key = child.key ? child.key : index;

    return React.cloneElement(child, {
      activeKey: this.props.activeKey,
      activeHref: this.props.activeHref,
      ref: 'nocollapse_' + key,
      key: key,
      navItem: true
    });
  },

  renderCollapsibleNavChildren: function renderCollapsibleNavChildren(child, index) {
    var key = child.key ? child.key : index;

    return React.cloneElement(child, {
      active: this.getChildActiveProp(child),
      activeKey: this.props.activeKey,
      activeHref: this.props.activeHref,
      onSelect: createChainedFunction(child.props.onSelect, this.props.onSelect),
      ref: 'collapsible_' + key,
      key: key
    });
  },

  render: function render() {
    var collapsible = this.props.collapsible;
    var classSet = collapsible ? this.getCollapsibleClassSet() : {};

    classSet[this.setClassNamespace('topbar-collapse')] = this.props.topbar;

    return React.createElement(
      'div',
      {
        className: classNames(classSet, this.props.className)
      },
      React.Children.map(this.props.children, collapsible ? this.renderCollapsibleNavChildren : this.renderChildren)
    );
  }
});

module.exports = CollapsibleNav;
},{"./mixins/ClassNameMixin":114,"./mixins/CollapseMixin":115,"./utils/createChainedFunction":124,"classnames":132,"react":356,"react-dom":189}],66:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');

var Container = React.createClass({
  displayName: 'Container',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string.isRequired,
    component: React.PropTypes.node.isRequired
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'container',
      component: 'div'
    };
  },

  render: function render() {
    var Component = this.props.component;
    var classSet = this.getClassSet();
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    return React.createElement(
      Component,
      _extends({}, restProps, {
        className: classNames(this.props.className, classSet)
      }),
      this.props.children
    );
  }
});

module.exports = Container;
},{"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],67:[function(require,module,exports){
'use strict';

var React = require('react');
var classNames = require('classnames');
var fecha = require('fecha');
var ClassNameMixin = require('../mixins/ClassNameMixin');
var dateUtils = require('../utils/dateUtils');

var DatePicker = React.createClass({
  displayName: 'DatePicker',

  mixins: [ClassNameMixin],

  propTypes: {
    onSelect: React.PropTypes.func.isRequired,
    onClose: React.PropTypes.func,
    viewMode: React.PropTypes.string,
    minViewMode: React.PropTypes.string,
    daysOfWeekDisabled: React.PropTypes.array,
    format: React.PropTypes.string,
    date: React.PropTypes.object,
    weekStart: React.PropTypes.number,
    minDate: React.PropTypes.string,
    maxDate: React.PropTypes.string,
    locale: React.PropTypes.string
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'datepicker',
      date: new Date(),
      daysOfWeekDisabled: [],
      viewMode: 'days',
      minViewMode: 'days',
      format: 'YYYY-MM-DD',
      displayed: {
        days: { display: 'block' },
        months: { display: 'none' },
        years: { display: 'none' }
      }
    };
  },

  getInitialState: function getInitialState() {
    var displayed;

    switch (this.props.viewMode) {
      case 'days':
        displayed = {
          days: { display: 'block' },
          months: { display: 'none' },
          years: { display: 'none' }
        };
        break;

      case 'months':
        displayed = {
          days: { display: 'none' },
          months: { display: 'block' },
          years: { display: 'none' }
        };
        break;

      case 'years':
        displayed = {
          days: { display: 'none' },
          months: { display: 'none' },
          years: { display: 'block' }
        };
        break;
    }

    return {
      locale: dateUtils.getLocale(this.props.locale),
      viewDate: this.props.date,
      selectedDate: this.props.date,
      displayed: displayed
    };
  },

  // DaysPicker props function

  subtractMonth: function subtractMonth() {
    var viewDate = this.state.viewDate;
    var newDate = new Date(viewDate.valueOf());

    newDate.setMonth(viewDate.getMonth() - 1);
    this.setState({
      viewDate: newDate
    });
  },

  addMonth: function addMonth() {
    var viewDate = this.state.viewDate;
    var newDate = new Date(viewDate.valueOf());

    newDate.setMonth(viewDate.getMonth() + 1);
    this.setState({
      viewDate: newDate
    });
  },

  setSelectedDate: function setSelectedDate(event) {
    if (/disabled/ig.test(event.target.className)) {
      return;
    }

    var viewDate = this.state.viewDate;

    if (/new/ig.test(event.target.className)) {
      viewDate.setMonth(viewDate.getMonth() + 1);
    } else if (/old/ig.test(event.target.className)) {
      viewDate.setMonth(viewDate.getMonth() - 1);
    }

    viewDate.setDate(event.target.innerHTML);

    this.setViewDate(viewDate);
  },

  setViewDate: function setViewDate(viewDate) {
    this.setState({
      viewDate: viewDate,
      selectedDate: new Date(viewDate.valueOf())
    }, function () {
      this.props.onSelect(this.state.selectedDate);
      this.props.onClose && this.props.onClose();
    });
  },

  showMonths: function showMonths() {
    return this.setState({
      displayed: {
        days: { display: 'none' },
        months: { display: 'block' },
        years: { display: 'none' }
      }
    });
  },

  // MonthsPicker props function

  subtractYear: function subtractYear() {
    var viewDate = this.state.viewDate;
    var newDate = new Date(viewDate.valueOf());

    newDate.setFullYear(viewDate.getFullYear() - 1);

    return this.setState({
      viewDate: newDate
    });
  },

  addYear: function addYear() {
    var viewDate = this.state.viewDate;
    var newDate = new Date(viewDate.valueOf());

    newDate.setFullYear(viewDate.getFullYear() + 1);
    return this.setState({
      viewDate: newDate
    });
  },

  showYears: function showYears() {
    return this.setState({
      displayed: {
        days: { display: 'none' },
        months: { display: 'none' },
        years: { display: 'block' }
      }
    });
  },

  setViewMonth: function setViewMonth(event) {
    var viewDate = this.state.viewDate;
    var month = event.target.innerHTML;
    var months = this.state.locale.monthsShort;
    var i = 0;
    var len = months.length;

    for (; i < len; i++) {
      if (month === months[i]) {
        viewDate.setMonth(i);
      }
    }

    if (this.props.minViewMode === 'months') {
      this.setViewDate(viewDate);
    }

    this.setState({
      viewDate: viewDate,
      displayed: {
        days: { display: 'block' },
        months: { display: 'none' },
        years: { display: 'none' }
      }
    });
  },

  // YearsPicker props function

  setViewYear: function setViewYear(event) {
    var year = event.target.innerHTML;
    var viewDate = this.state.viewDate;

    viewDate.setFullYear(year);

    if (this.props.minViewMode === 'years') {
      this.setViewDate(viewDate);
    }

    this.setState({
      viewDate: viewDate,
      displayed: {
        days: { display: 'none' },
        months: { display: 'block' },
        years: { display: 'none' }
      }
    });
  },

  addDecade: function addDecade() {
    var viewDate = this.state.viewDate;
    var newDate = new Date(viewDate.valueOf());

    newDate.setFullYear(viewDate.getFullYear() + 10);
    this.setState({
      viewDate: newDate
    });
  },

  subtractDecade: function subtractDecade() {
    var viewDate = this.state.viewDate;
    var newDate = new Date(viewDate.valueOf());

    newDate.setFullYear(viewDate.getFullYear() - 10);

    this.setState({
      viewDate: newDate
    });
  },

  // render children
  renderDays: function renderDays() {
    return React.createElement(DaysPicker, {
      style: this.state.displayed.days,
      selectedDate: this.state.selectedDate,
      viewDate: this.state.viewDate,

      subtractMonth: this.subtractMonth,
      addMonth: this.addMonth,
      setSelectedDate: this.setSelectedDate,
      showMonths: this.showMonths,

      format: this.props.format,
      locale: this.state.locale,
      weekStart: this.props.weekStart,
      daysOfWeekDisabled: this.props.daysOfWeekDisabled,
      minDate: this.props.minDate,
      maxDate: this.props.maxDate
    });
  },

  renderMonths: function renderMonths() {
    return React.createElement(MonthsPicker, {
      style: this.state.displayed.months,
      locale: this.state.locale,
      addYear: this.addYear,
      subtractYear: this.subtractYear,
      viewDate: this.state.viewDate,
      selectedDate: this.state.selectedDate,
      showYears: this.showYears,
      setViewMonth: this.setViewMonth });
  },

  renderYears: function renderYears() {
    return React.createElement(YearsPicker, {
      style: this.state.displayed.years,
      viewDate: this.state.viewDate,
      selectDate: this.state.selectedDate,
      setViewYear: this.setViewYear,
      addDecade: this.addDecade,
      subtractDecade: this.subtractDecade });
  },

  render: function render() {
    return React.createElement(
      'div',
      { className: this.prefixClass('body') },
      this.renderDays(),
      this.renderMonths(),
      this.renderYears()
    );
  }
});

var DaysPicker = React.createClass({
  displayName: 'DaysPicker',

  mixins: [ClassNameMixin],

  propTypes: {
    subtractMonth: React.PropTypes.func.isRequired,
    addMonth: React.PropTypes.func.isRequired,

    setSelectedDate: React.PropTypes.func.isRequired,
    selectedDate: React.PropTypes.object.isRequired,

    viewDate: React.PropTypes.object.isRequired,
    showMonths: React.PropTypes.func.isRequired,

    locale: React.PropTypes.object,
    weekStart: React.PropTypes.number,
    daysOfWeekDisabled: React.PropTypes.array,
    minDate: React.PropTypes.string,
    maxDate: React.PropTypes.string
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'datepicker'
    };
  },

  renderDays: function renderDays() {
    var row;
    var i;
    var _ref;
    var _i;
    var _len;
    var prevY;
    var prevM;
    var classes = {};
    var html = [];
    var cells = [];
    var weekStart = this.props.weekStart || this.props.locale.weekStart;

    var weekEnd = (weekStart + 6) % 7;

    var d = this.props.viewDate;
    var year = d.getFullYear();
    var month = d.getMonth();
    var selectedDate = this.props.selectedDate;

    var currentDate = new Date(selectedDate.getFullYear(), selectedDate.getMonth(), selectedDate.getDate(), 0, 0, 0, 0).valueOf();

    var prevMonth = new Date(year, month - 1, 28, 0, 0, 0, 0);
    var day = dateUtils.getDaysInMonth(prevMonth.getFullYear(), prevMonth.getMonth());

    prevMonth.setDate(day);
    prevMonth.setDate(day - (prevMonth.getDay() - weekStart + 7) % 7);

    var nextMonth = new Date(prevMonth);

    nextMonth.setDate(nextMonth.getDate() + 42);
    nextMonth = nextMonth.valueOf();

    var minDate = this.props.minDate && fecha.parse(this.props.minDate);
    var maxDate = this.props.maxDate && fecha.parse(this.props.maxDate);

    while (prevMonth.valueOf() < nextMonth) {
      classes[this.prefixClass('day')] = true;

      prevY = prevMonth.getFullYear();
      prevM = prevMonth.getMonth();

      // set className old new
      if (prevM < month && prevY === year || prevY < year) {
        classes[this.prefixClass('old')] = true;
      } else if (prevM > month && prevY === year || prevY > year) {
        classes[this.prefixClass('new')] = true;
      }

      // set className active
      if (prevMonth.valueOf() === currentDate) {
        classes[this.setClassNamespace('active')] = true;
      }

      // set className disabled
      if (minDate && prevMonth.valueOf() < minDate || maxDate && prevMonth.valueOf() > maxDate) {
        classes[this.setClassNamespace('disabled')] = true;
      }

      // week disabled
      if (this.props.daysOfWeekDisabled) {
        _ref = this.props.daysOfWeekDisabled;
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          i = _ref[_i];
          if (prevMonth.getDay() === this.props.daysOfWeekDisabled[i]) {
            classes[this.setClassNamespace('disabled')] = true;
            break;
          }
        }
      }

      cells.push(React.createElement(
        'td',
        {
          key: prevMonth.getMonth() + '-' + prevMonth.getDate(),
          className: classNames(classes),
          onClick: this.props.setSelectedDate
        },
        prevMonth.getDate()
      ));

      // add tr
      if (prevMonth.getDay() === weekEnd) {
        row = React.createElement(
          'tr',
          { key: prevMonth.getMonth() + '-' + prevMonth.getDate() },
          cells
        );
        html.push(row);
        cells = [];
      }

      classes = {};
      prevMonth.setDate(prevMonth.getDate() + 1);
    }

    return html;
  },

  renderWeek: function renderWeek() {
    var ths = [];
    var locale = this.props.locale;
    var weekStart = this.props.weekStart || this.props.locale.weekStart;
    var weekEnd = weekStart + 7;

    while (weekStart < weekEnd) {
      ths.push(React.createElement(
        'th',
        { key: weekStart, className: this.prefixClass('dow') },
        locale.daysMin[weekStart++ % 7]
      ));
    }

    return React.createElement(
      'tr',
      null,
      ths
    );
  },

  render: function render() {
    var viewDate = this.props.viewDate;
    var prefixClass = this.prefixClass;
    var locale = this.props.locale;

    return React.createElement(
      'div',
      {
        className: prefixClass('days'),
        style: this.props.style },
      React.createElement(
        'table',
        { className: prefixClass('table') },
        React.createElement(
          'thead',
          null,
          React.createElement(
            'tr',
            { className: prefixClass('header') },
            React.createElement(
              'th',
              { className: prefixClass('prev'), onClick: this.props.subtractMonth },
              React.createElement('i', { className: prefixClass('prev-icon') })
            ),
            React.createElement(
              'th',
              {
                className: prefixClass('switch'),
                colSpan: '5',
                onClick: this.props.showMonths
              },
              React.createElement(
                'div',
                { className: this.prefixClass('select') },
                locale.monthsShort[viewDate.getMonth()],
                viewDate.getFullYear()
              )
            ),
            React.createElement(
              'th',
              { className: prefixClass('next'), onClick: this.props.addMonth },
              React.createElement('i', { className: prefixClass('next-icon') })
            )
          ),
          this.renderWeek()
        ),
        React.createElement(
          'tbody',
          null,
          this.renderDays()
        )
      )
    );
  }
});

var MonthsPicker = React.createClass({
  displayName: 'MonthsPicker',

  mixins: [ClassNameMixin],

  propTypes: {
    locale: React.PropTypes.object,
    subtractYear: React.PropTypes.func.isRequired,
    addYear: React.PropTypes.func.isRequired,
    viewDate: React.PropTypes.object.isRequired,
    selectedDate: React.PropTypes.object.isRequired,
    showYears: React.PropTypes.func.isRequired,
    setViewMonth: React.PropTypes.func.isRequired,
    minDate: React.PropTypes.string,
    maxDate: React.PropTypes.string
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'datepicker'
    };
  },

  renderMonths: function renderMonths() {
    var classes = {};
    var month = this.props.selectedDate.getMonth();
    var year = this.props.selectedDate.getFullYear();
    var i = 0;
    var months = [];
    var minDate = this.props.minDate && fecha.parse(this.props.minDate);
    var maxDate = this.props.maxDate && fecha.parse(this.props.maxDate);
    var prevMonth = new Date(year, month);

    // TODO: minDate maxDate months
    while (i < 12) {
      classes[this.prefixClass('month')] = true;

      if (this.props.viewDate.getFullYear() === this.props.selectedDate.getFullYear() && i === month) {
        classes[this.setClassNamespace('active')] = true;
      }

      // set className disabled
      if (minDate && prevMonth.valueOf() < minDate || maxDate && prevMonth.valueOf() > maxDate) {
        classes[this.setClassNamespace('disabled')] = true;
      }

      months.push(React.createElement(
        'span',
        {
          className: classNames(classes),
          onClick: this.props.setViewMonth,
          key: i },
        this.props.locale.monthsShort[i]
      ));

      classes = {};
      i++;
    }

    return months;
  },

  render: function render() {
    return React.createElement(SubPicker, {
      displayName: 'months',
      style: this.props.style,
      subtract: this.props.subtractYear,
      add: this.props.addYear,
      showFunc: this.props.showYears,
      showText: this.props.viewDate.getFullYear(),
      body: this.renderMonths() });
  }
});

var YearsPicker = React.createClass({
  displayName: 'YearsPicker',

  mixins: [ClassNameMixin],

  propTypes: {
    viewDate: React.PropTypes.object.isRequired,
    selectDate: React.PropTypes.object.isRequired,
    subtractDecade: React.PropTypes.func.isRequired,
    addDecade: React.PropTypes.func.isRequired,
    setViewYear: React.PropTypes.func.isRequired
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'datepicker'
    };
  },

  renderYears: function renderYears() {
    var classes = {};
    var years = [];
    var i = -1;
    var year = parseInt(this.props.viewDate.getFullYear() / 10, 10) * 10;

    year--;

    while (i < 11) {
      classes[this.prefixClass('year')] = true;

      if (i === -1 || i === 10) {
        classes[this.prefixClass('old')] = true;
      }

      if (this.props.selectDate.getFullYear() === year) {
        classes[this.setClassNamespace('active')] = true;
      }

      years.push(React.createElement(
        'span',
        {
          className: classNames(classes),
          onClick: this.props.setViewYear,
          key: year },
        year
      ));

      classes = {};
      year++;
      i++;
    }

    return years;
  },

  render: function render() {
    var year = parseInt(this.props.viewDate.getFullYear() / 10, 10) * 10;
    var addYear = year + 9;
    var showYear = year + '-' + addYear;

    return React.createElement(SubPicker, {
      displayName: 'years',
      style: this.props.style,
      subtract: this.props.subtractDecade,
      add: this.props.addDecade,
      showText: showYear,
      body: this.renderYears() });
  }
});

var SubPicker = React.createClass({
  displayName: 'SubPicker',

  mixins: [ClassNameMixin],

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'datepicker'
    };
  },

  render: function render() {
    var prefixClass = this.prefixClass;

    return React.createElement(
      'div',
      {
        className: prefixClass(this.props.displayName),
        style: this.props.style },
      React.createElement(
        'table',
        { className: prefixClass('table') },
        React.createElement(
          'thead',
          null,
          React.createElement(
            'tr',
            { className: prefixClass('header') },
            React.createElement(
              'th',
              { className: prefixClass('prev'), onClick: this.props.subtract },
              React.createElement('i', { className: prefixClass('prev-icon') })
            ),
            React.createElement(
              'th',
              {
                className: prefixClass('switch'),
                colSpan: '5',
                onClick: this.props.showFunc },
              React.createElement(
                'div',
                { className: this.prefixClass('select') },
                this.props.showText
              )
            ),
            React.createElement(
              'th',
              { className: prefixClass('next'), onClick: this.props.add },
              React.createElement('i', { className: prefixClass('next-icon') })
            )
          )
        ),
        React.createElement(
          'tbody',
          null,
          React.createElement(
            'tr',
            null,
            React.createElement(
              'td',
              { colSpan: '7' },
              this.props.body
            )
          )
        )
      )
    );
  }
});

module.exports = DatePicker;
},{"../mixins/ClassNameMixin":114,"../utils/dateUtils":125,"classnames":132,"fecha":163,"react":356}],68:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var ReactDOM = require('react-dom');
var fecha = require('fecha');
var omit = require('object.omit');
var Events = require('../utils/Events');
var isNodeInTree = require('../utils/isNodeInTree');
var Input = require('../Input');
var DateTimePicker = require('./DateTimePicker');

var DateTimeInput = React.createClass({
  displayName: 'DateTimeInput',

  propTypes: {
    format: React.PropTypes.string,
    dateTime: React.PropTypes.string,
    date: React.PropTypes.string,
    onSelect: React.PropTypes.func,
    showTimePicker: React.PropTypes.bool,
    showDatePicker: React.PropTypes.bool,
    amStyle: React.PropTypes.oneOf(['success', 'danger', 'warning']),
    viewMode: React.PropTypes.string,
    minViewMode: React.PropTypes.string,
    daysOfWeekDisabled: React.PropTypes.array,
    locale: React.PropTypes.string,
    weekStart: React.PropTypes.number,
    minDate: React.PropTypes.string,
    maxDate: React.PropTypes.string
  },

  getDefaultProps: function getDefaultProps() {
    return {
      dateTime: '',
      format: 'YYYY-MM-DD HH:mm'
    };
  },

  getInitialState: function getInitialState() {
    return {
      value: this.props.dateTime || fecha.format(new Date(), this.props.format),
      showPicker: false
    };
  },

  handleOuterClick: function handleOuterClick(event) {
    var picker = ReactDOM.findDOMNode(this.refs.DateTimePicker);

    if (!isNodeInTree(event.target, picker)) {
      this.handleClose();
    }
  },

  bindOuterHandlers: function bindOuterHandlers() {
    Events.on(document, 'click', this.handleOuterClick);
  },

  unbindOuterHandlers: function unbindOuterHandlers() {
    Events.off(document, 'click', this.handleOuterClick);
  },

  handleClose: function handleClose() {
    this.unbindOuterHandlers();
    return this.setState({
      showPicker: false
    });
  },

  handleClick: function handleClick() {
    this.bindOuterHandlers();

    var positionNode = ReactDOM.findDOMNode(this.refs.dateInput);
    // fixes #57
    // @see http://stackoverflow.com/questions/1044988/getting-offsettop-of-element-in-a-table
    var rect = positionNode.getBoundingClientRect();
    var offset = {
      top: rect.top + positionNode.offsetHeight,
      left: rect.left
    };

    var styles = {
      display: 'block',
      top: offset.top,
      left: offset.left,
      position: 'fixed',
      zIndex: 1120
    };

    this.setState({
      showPicker: true,
      pickerStyle: styles
    });
  },

  handleChange: function handleChange(event) {
    this.setState({
      value: event.target.value
    });
  },

  handleSelect: function handleSelect(date) {
    this.setState({
      value: date
    });

    this.props.onSelect && this.props.onSelect.call(this, date);
  },

  renderPicker: function renderPicker() {
    if (this.state.showPicker) {
      return React.createElement(DateTimePicker, {
        style: this.state.pickerStyle,
        ref: 'DateTimePicker',
        showDatePicker: this.props.showDatePicker,
        showTimePicker: this.props.showTimePicker,
        onSelect: this.handleSelect,
        onClose: this.handleClose,
        amStyle: this.props.amStyle,
        dateTime: this.state.value,
        viewMode: this.props.viewMode,
        minViewMode: this.props.minViewMode,
        daysOfWeekDisabled: this.props.daysOfWeekDisabled,
        weekStart: this.props.weekStart,
        format: this.props.format,
        locale: this.props.locale,
        maxDate: this.props.maxDate,
        minDate: this.props.minDate
      });
    }
  },

  render: function render() {
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    return React.createElement(
      'div',
      null,
      React.createElement(Input, _extends({}, restProps, {
        type: 'text',
        value: this.state.value,
        onClick: this.handleClick,
        onChange: this.handleChange,
        onSelect: null,
        ref: 'dateInput'
      })),
      this.renderPicker()
    );
  }
});

module.exports = DateTimeInput;

// TODO: 动画
},{"../Input":84,"../utils/Events":121,"../utils/isNodeInTree":130,"./DateTimePicker":69,"fecha":163,"object.omit":186,"react":356,"react-dom":189}],69:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var fecha = require('fecha');
var omit = require('object.omit');
var ClassNameMixin = require('../mixins/ClassNameMixin');
var Icon = require('../Icon');
var DatePicker = require('./DatePicker');
var TimePicker = require('./TimePicker');

var DateTimePicker = React.createClass({
  displayName: 'DateTimePicker',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    showTimePicker: React.PropTypes.bool,
    showDatePicker: React.PropTypes.bool,
    caretDisplayed: React.PropTypes.bool,
    amStyle: React.PropTypes.oneOf(['success', 'danger', 'warning']),
    viewMode: React.PropTypes.string,
    minViewMode: React.PropTypes.string,
    onSelect: React.PropTypes.func.isRequired,
    onClose: React.PropTypes.func,
    daysOfWeekDisabled: React.PropTypes.array,
    format: React.PropTypes.string,
    dateTime: React.PropTypes.string,
    locale: React.PropTypes.string,
    weekStart: React.PropTypes.number,
    minDate: React.PropTypes.string,
    maxDate: React.PropTypes.string
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'datepicker',
      dateTime: '',
      format: 'YYYY-MM-DD HH:mm',
      showTimePicker: true,
      showDatePicker: true,
      caretDisplayed: true
    };
  },

  getInitialState: function getInitialState() {
    var showToggle;
    var showTimePicker;

    if (this.props.showTimePicker && this.props.showDatePicker) {
      showToggle = true;
      showTimePicker = false;
    }

    if (!showToggle && !this.props.showDatePicker) {
      showTimePicker = true;
    }

    // `fecha.parse` return `false` when passed invalid parameter
    // fixes: https://github.com/amazeui/amazeui-react/issues/119
    var date = fecha.parse(this.props.dateTime, this.props.format);
    !date && (date = new Date());

    return {
      showTimePicker: showTimePicker,
      showDatePicker: this.props.showDatePicker,
      caretDisplayed: this.props.caretDisplayed,
      showToggle: showToggle,
      date: date,
      toggleDisplay: {
        toggleTime: {
          display: 'block'
        },
        toggleDate: {
          display: 'none'
        }
      }
    };
  },

  handleToggleTime: function handleToggleTime() {
    this.setState({
      showDatePicker: false,
      showTimePicker: true,
      toggleDisplay: {
        toggleTime: {
          display: 'none'
        },
        toggleDate: {
          display: 'block'
        }
      }
    });
  },

  handleToggleDate: function handleToggleDate() {
    this.setState({
      showDatePicker: true,
      showTimePicker: false,
      toggleDisplay: {
        toggleTime: {
          display: 'block'
        },
        toggleDate: {
          display: 'none'
        }
      }
    });
  },

  handleSelect: function handleSelect(date) {
    this.setState({
      date: date
    });
    this.props.onSelect(fecha.format(date, this.props.format));
  },

  renderToggleTime: function renderToggleTime() {
    if (this.state.showToggle) {
      return React.createElement(
        'div',
        {
          style: this.state.toggleDisplay.toggleTime,
          className: this.prefixClass('toggle'),
          onClick: this.handleToggleTime
        },
        React.createElement(Icon, { icon: 'clock-o' })
      );
    }
  },

  renderToggleDate: function renderToggleDate() {
    if (this.state.showToggle) {
      return React.createElement(
        'div',
        {
          style: this.state.toggleDisplay.toggleDate,
          className: this.prefixClass('toggle'),
          onClick: this.handleToggleDate
        },
        React.createElement(Icon, { icon: 'calendar' })
      );
    }
  },

  renderDatePicker: function renderDatePicker() {
    if (this.state.showDatePicker) {
      return React.createElement(DatePicker, {
        onSelect: this.handleSelect,
        onClose: this.props.onClose,
        weekStart: this.props.weekStart,
        viewMode: this.props.viewMode,
        minViewMode: this.props.minViewMode,
        daysOfWeekDisabled: this.props.daysOfWeekDisabled,
        format: this.props.format,
        date: this.state.date,
        locale: this.props.locale,
        minDate: this.props.minDate,
        maxDate: this.props.maxDate
      });
    }
  },

  renderTimePicker: function renderTimePicker() {
    if (this.state.showTimePicker) {
      return React.createElement(TimePicker, {
        onSelect: this.handleSelect,
        date: this.state.date,
        format: this.props.format
      });
    }
  },

  renderCaret: function renderCaret() {
    if (this.state.caretDisplayed) {
      return React.createElement('div', { className: this.prefixClass('caret') });
    }
  },

  render: function render() {
    var classSet = this.getClassSet();
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    this.props.amStyle && (classSet[this.prefixClass(this.props.amStyle)] = true);

    return React.createElement(
      'div',
      _extends({}, restProps, {
        className: classNames(classSet, this.props.className)
      }),
      this.renderCaret(),
      React.createElement(
        'div',
        { className: this.prefixClass('date') },
        this.renderDatePicker()
      ),
      React.createElement(
        'div',
        { className: this.prefixClass('time') },
        this.renderTimePicker()
      ),
      this.renderToggleTime(),
      this.renderToggleDate()
    );
  }
});

module.exports = DateTimePicker;
},{"../Icon":82,"../mixins/ClassNameMixin":114,"./DatePicker":67,"./TimePicker":70,"classnames":132,"fecha":163,"object.omit":186,"react":356}],70:[function(require,module,exports){
'use strict';

var React = require('react');
var classNames = require('classnames');
var ClassNameMixin = require('../mixins/ClassNameMixin');

var TimePicker = React.createClass({
  displayName: 'TimePicker',

  mixins: [ClassNameMixin],

  propTypes: {
    onSelect: React.PropTypes.func.isRequired,
    date: React.PropTypes.object,
    format: React.PropTypes.string
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'datepicker',
      format: 'HH:mm'
    };
  },

  getInitialState: function getInitialState() {
    return {
      viewDate: this.props.date,
      selectedDate: this.props.date,
      displayed: {
        times: { display: 'block' },
        minutes: { display: 'none' },
        hours: { display: 'none' }
      }
    };
  },

  // Minutes
  addMinute: function addMinute() {
    var viewDate = this.state.viewDate;

    viewDate.setMinutes(viewDate.getMinutes() + 1);

    this.setTime(viewDate);
  },

  subtractMinute: function subtractMinute() {
    var viewDate = this.state.viewDate;

    viewDate.setMinutes(viewDate.getMinutes() - 1);

    this.setTime(viewDate);
  },

  setTime: function setTime(viewDate) {
    this.setState({
      viewDate: viewDate,
      selectedDate: new Date(viewDate.valueOf())
    }, function () {
      this.props.onSelect(this.state.selectedDate);
    });
  },

  // set Minutes
  setSelectedMinute: function setSelectedMinute(event) {
    var viewDate = this.state.viewDate;
    var minute = parseInt(event.target.innerHTML.split(':')[1]);

    viewDate.setMinutes(minute);
    this.setTime(viewDate);

    this.setState({
      displayed: {
        times: { display: 'block' },
        minutes: { display: 'none' },
        hours: { display: 'none' }
      }
    });
  },

  showMinutes: function showMinutes() {
    this.setState({
      displayed: {
        times: { display: 'none' },
        minutes: { display: 'block' },
        hours: { display: 'none' }
      }
    });
  },

  // Hours
  showHours: function showHours() {
    this.setState({
      displayed: {
        times: { display: 'none' },
        minutes: { display: 'none' },
        hours: { display: 'block' }
      }
    });
  },

  setSelectedHour: function setSelectedHour(event) {
    var viewDate = this.state.viewDate;
    var hour = parseInt(event.target.innerHTML);

    viewDate.setHours(hour);
    this.setTime(viewDate);

    this.setState({
      displayed: {
        times: { display: 'block' },
        minutes: { display: 'none' },
        hours: { display: 'none' }
      }
    });
  },

  addHour: function addHour() {
    var viewDate = this.state.viewDate;

    viewDate.setHours(viewDate.getHours() + 1);

    this.setTime(viewDate);
  },

  subtractHour: function subtractHour() {
    var viewDate = this.state.viewDate;

    viewDate.setHours(viewDate.getHours() - 1);

    this.setTime(viewDate);
  },

  showTimeText: function showTimeText() {
    var hour = this.state.viewDate.getHours();
    var minute = this.state.viewDate.getMinutes();

    if (minute < 10) {
      minute = '0' + minute;
    }

    if (hour < 10) {
      hour = '0' + hour;
    }

    return {
      hour: hour,
      minute: minute
    };
  },

  renderHours: function renderHours() {
    var time = this.showTimeText().hour + ':' + this.showTimeText().minute;

    return React.createElement(HoursPicker, {
      style: this.state.displayed.hours,
      setSelectedHour: this.setSelectedHour,
      selectedDate: this.state.selectedDate,
      addHour: this.addHour,
      subtractHour: this.subtractHour,
      showTime: time
    });
  },

  renderMinutes: function renderMinutes() {
    var time = this.showTimeText().hour + ':' + this.showTimeText().minute;

    return React.createElement(MinutesPicker, {
      style: this.state.displayed.minutes,
      setSelectedMinute: this.setSelectedMinute,
      selectedDate: this.state.selectedDate,
      addMinute: this.addMinute,
      subtractMinute: this.subtractMinute,
      showTime: time
    });
  },

  render: function render() {
    var time = this.showTimeText();

    var content = React.createElement(
      'div',
      { className: this.prefixClass('time-box') },
      React.createElement(
        'strong',
        { onClick: this.showHours },
        time.hour
      ),
      React.createElement(
        'em',
        null,
        ':'
      ),
      React.createElement(
        'strong',
        { onClick: this.showMinutes },
        time.minute
      )
    );

    return React.createElement(
      'div',
      { className: this.prefixClass('body') },
      React.createElement(SubPicker, {
        style: this.state.displayed.times,
        displayName: 'time-wrapper',
        body: content,
        add: this.addMinute,
        subtract: this.subtractMinute,
        showFunc: this.props.showDate,
        showText: 'today'
      }),
      this.renderHours(),
      this.renderMinutes()
    );
  }
});

var HoursPicker = React.createClass({
  displayName: 'HoursPicker',

  mixins: [ClassNameMixin],

  propTypes: {
    setSelectedHour: React.PropTypes.func.isRequired,
    selectedDate: React.PropTypes.object.isRequired
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'datepicker'
    };
  },

  renderHour: function renderHour() {
    var classes;
    var hour = this.props.selectedDate.getHours();
    var i = 0;
    var hours = [];

    while (i < 24) {
      classes = {};
      classes[this.prefixClass('hour')] = true;

      if (i === hour) {
        classes[this.setClassNamespace('active')] = true;
      }

      hours.push(React.createElement(
        'span',
        {
          className: classNames(classes),
          onClick: this.props.setSelectedHour,
          key: i
        },
        i < 10 ? '0' + i + ':00' : i + ':00'
      ));

      i++;
    }

    return hours;
  },

  render: function render() {
    return React.createElement(SubPicker, {
      displayName: 'hours',
      style: this.props.style,
      subtract: this.props.subtractHour,
      add: this.props.addHour,
      showText: this.props.showTime,
      body: this.renderHour()
    });
  }
});

var MinutesPicker = React.createClass({
  displayName: 'MinutesPicker',

  mixins: [ClassNameMixin],

  propTypes: {
    setSelectedMinute: React.PropTypes.func.isRequired,
    selectedDate: React.PropTypes.object.isRequired
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'datepicker'
    };
  },

  renderMinute: function renderMinute() {
    var classes;
    var minute = this.props.selectedDate.getMinutes();
    var hour = this.props.selectedDate.getHours();
    var i = 0;
    var minutes = [];

    while (i < 60) {
      classes = {};
      classes[this.prefixClass('minute')] = true;

      if (i === minute) {
        classes[this.setClassNamespace('active')] = true;
      }

      if (i % 5 === 0) {
        minutes.push(React.createElement(
          'span',
          {
            className: classNames(classes),
            onClick: this.props.setSelectedMinute,
            key: i
          },
          i < 10 ? hour + ':0' + i : hour + ':' + i
        ));
      }

      i++;
    }

    return minutes;
  },

  render: function render() {
    return React.createElement(SubPicker, {
      displayName: 'minutes',
      style: this.props.style,
      subtract: this.props.subtractMinute,
      add: this.props.addMinute,
      showText: this.props.showTime,
      body: this.renderMinute()
    });
  }
});

var SubPicker = React.createClass({
  displayName: 'SubPicker',

  mixins: [ClassNameMixin],

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'datepicker'
    };
  },

  render: function render() {
    var prefixClass = this.prefixClass;

    return React.createElement(
      'div',
      {
        className: prefixClass(this.props.displayName),
        style: this.props.style },
      React.createElement(
        'table',
        { className: prefixClass('table') },
        React.createElement(
          'thead',
          null,
          React.createElement(
            'tr',
            { className: prefixClass('header') },
            React.createElement(
              'th',
              { className: prefixClass('prev'), onClick: this.props.subtract },
              React.createElement('i', { className: prefixClass('prev-icon') })
            ),
            React.createElement(
              'th',
              {
                className: prefixClass('switch'),
                colSpan: '5',
                onClick: this.props.showFunc
              },
              React.createElement(
                'div',
                { className: this.prefixClass('select') },
                this.props.showText
              )
            ),
            React.createElement(
              'th',
              { className: prefixClass('next'), onClick: this.props.add },
              React.createElement('i', { className: prefixClass('next-icon') })
            )
          )
        ),
        React.createElement(
          'tbody',
          null,
          React.createElement(
            'tr',
            null,
            React.createElement(
              'td',
              { colSpan: '7' },
              this.props.body
            )
          )
        )
      )
    );
  }
});

module.exports = TimePicker;
},{"../mixins/ClassNameMixin":114,"classnames":132,"react":356}],71:[function(require,module,exports){
'use strict';

module.exports = {
  DateTimeInput: require('./DateTimeInput'),
  DateTimePicker: require('./DateTimePicker')
};
},{"./DateTimeInput":68,"./DateTimePicker":69}],72:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');

var Divider = React.createClass({
  displayName: 'Divider',

  mixins: [ClassNameMixin],

  propTypes: {
    theme: React.PropTypes.oneOf(['default', 'dotted', 'dashed']),
    classPrefix: React.PropTypes.string
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'divider',
      theme: 'default'
    };
  },

  render: function render() {
    var classSet = this.getClassSet();
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    return React.createElement('hr', _extends({}, restProps, {
      'data-am-widget': this.props.classPrefix,
      className: classNames(this.props.className, classSet)
    }));
  }
});

module.exports = Divider;
},{"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],73:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var ReactDOM = require('react-dom');
var classNames = require('classnames');
var assign = require('object-assign');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var constants = require('./constants');
var Button = require('./Button');
var Icon = require('./Icon');
var Events = require('./utils/Events');
var isNodeInTree = require('./utils/isNodeInTree');
var createChainedFunction = require('./utils/createChainedFunction');
var canUseDOM = require('./utils/canUseDOM');

var Dropdown = React.createClass({
  displayName: 'Dropdown',

  mixins: [ClassNameMixin],

  propTypes: {
    title: React.PropTypes.node.isRequired,
    dropup: React.PropTypes.bool,
    navItem: React.PropTypes.bool,
    btnStyle: React.PropTypes.string,
    btnSize: React.PropTypes.string,
    btnInlineStyle: React.PropTypes.object,
    contentInlineStyle: React.PropTypes.object,
    contentComponent: React.PropTypes.node,
    toggleClassName: React.PropTypes.string,
    caretClassName: React.PropTypes.string,
    contentClassName: React.PropTypes.string,
    onOpen: React.PropTypes.func, // open callback
    onClose: React.PropTypes.func // close callback
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'dropdown',
      contentComponent: 'ul'
    };
  },

  getInitialState: function getInitialState() {
    return {
      open: false
    };
  },

  componentWillMount: function componentWillMount() {
    this.unbindOuterHandlers();
  },

  componentWillUnmount: function componentWillUnmount() {
    this.unbindOuterHandlers();
  },

  /**
   * setDropdown
   * @param {bool} isOpen - Dropdown state, `true` -> open, `close` -> false
   * @param {function} [callback]
   */
  setDropdown: function setDropdown(isOpen, callback) {
    if (isOpen) {
      this.bindOuterHandlers();
    } else {
      this.unbindOuterHandlers();
    }

    this.setState({
      open: isOpen
    }, function () {
      callback && callback();

      isOpen && this.props.onOpen && this.props.onOpen();
      !isOpen && this.props.onClose && this.props.onClose();
    });
  },

  // close dropdown on `esc` keyup
  handleKeyup: function handleKeyup(e) {
    e.keyCode === 27 && this.setDropdown(false);
  },

  // close dropdown when click outer dropdown
  handleOuterClick: function handleOuterClick(e) {
    if (isNodeInTree(e.target, ReactDOM.findDOMNode(this))) {
      return false;
    }

    this.setDropdown(false);
  },

  bindOuterHandlers: function bindOuterHandlers() {
    if (canUseDOM) {
      Events.on(document, 'click', this.handleOuterClick);
      Events.on(document, 'keyup', this.handleKeyup);
    }
  },

  unbindOuterHandlers: function unbindOuterHandlers() {
    if (canUseDOM) {
      Events.off(document, 'click', this.handleOuterClick);
      Events.off(document, 'keyup', this.handleKeyup);
    }
  },

  handleDropdownClick: function handleDropdownClick(e) {
    e.preventDefault();

    this.setDropdown(!this.state.open);
  },

  renderChildren: function renderChildren() {
    var _this = this;

    return React.Children.map(this.props.children, function (child, index) {
      if (React.isValidElement(child)) {
        var closeOnClick = child.props.closeOnClick;
        var onClick = child.props.onClick;
        var handleClick = closeOnClick ? createChainedFunction(onClick, function () {
          _this.setDropdown(false);
        }) : onClick;

        return React.cloneElement(child, assign({}, child.props, {
          key: 'dropdownItem-' + index,
          onClick: handleClick
        }));
      }
    });
  },

  render: function render() {
    var classSet = this.getClassSet();
    var Component = this.props.navItem ? 'li' : 'div';
    var btnClassPrefix = this.props.navItem ? '' : 'btn';
    var btnComponent = this.props.navItem ? 'a' : null;
    var caret = React.createElement(Icon, {
      className: this.props.caretClassName,
      icon: 'caret-' + (this.props.dropup ? 'up' : 'down')
    });
    var animation = this.state.open ? this.setClassNamespace('animation-slide-top-fixed') : this.setClassNamespace('dropdown-animation');
    var ContentComponent = this.props.contentComponent;

    classSet[constants.CLASSES.active] = this.state.open;
    classSet[this.prefixClass('up')] = this.props.dropup;

    return React.createElement(
      Component,
      {
        className: classNames(this.props.className, classSet)
      },
      React.createElement(
        Button,
        {
          onClick: this.handleDropdownClick,
          amStyle: this.props.btnStyle,
          amSize: this.props.btnSize,
          style: this.props.btnInlineStyle,
          className: classNames(this.prefixClass('toggle'), this.props.toggleClassName),
          classPrefix: btnClassPrefix,
          component: btnComponent,
          ref: 'dropdownToggle'
        },
        this.props.title,
        ' ',
        caret
      ),
      React.createElement(
        ContentComponent,
        {
          ref: 'dropdownContent',
          style: this.props.contentInlineStyle,
          className: classNames(this.prefixClass('content'), animation, this.props.contentClassName)
        },
        this.renderChildren()
      )
    );
  }
});

Dropdown.Item = React.createClass({
  displayName: 'Item',

  mixins: [ClassNameMixin],

  propTypes: {
    closeOnClick: React.PropTypes.bool,
    href: React.PropTypes.string,
    target: React.PropTypes.string,
    title: React.PropTypes.string,
    header: React.PropTypes.bool,
    divider: React.PropTypes.bool,
    linkComponent: React.PropTypes.any,
    linkProps: React.PropTypes.object,
    active: React.PropTypes.bool
  },

  render: function render() {
    var classSet = this.getClassSet();
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));
    var children = null;

    classSet[this.setClassNamespace('dropdown-header')] = this.props.header;

    if (this.props.header) {
      children = this.props.children;
    } else if (!this.props.divider) {
      var Component = this.props.linkComponent || 'a';

      children = React.createElement(
        Component,
        _extends({
          onClick: this.handleClick,
          href: this.props.href,
          target: this.props.target,
          title: this.props.title
        }, this.props.linkProps),
        this.props.children
      );
    }

    return React.createElement(
      'li',
      _extends({}, restProps, {
        title: null,
        href: null,
        className: classNames(this.props.className, classSet)
      }),
      children
    );
  }
});

module.exports = Dropdown;

/*
* TODO:
*   1. 关闭动画
*   2. 位置检测/宽度适应
* */
},{"./Button":59,"./Icon":82,"./constants":113,"./mixins/ClassNameMixin":114,"./utils/Events":121,"./utils/canUseDOM":123,"./utils/createChainedFunction":124,"./utils/isNodeInTree":130,"classnames":132,"object-assign":185,"object.omit":186,"react":356,"react-dom":189}],74:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');

var Footer = React.createClass({
  displayName: 'Footer',

  mixins: [ClassNameMixin],

  propTypes: {
    theme: React.PropTypes.oneOf(['default']),
    classPrefix: React.PropTypes.string,
    mobileTitle: React.PropTypes.string,
    mobileLink: React.PropTypes.string,
    desktopTitle: React.PropTypes.string,
    desktopLink: React.PropTypes.string,
    onRequestMobile: React.PropTypes.func,
    onRequestDesktop: React.PropTypes.func,
    data: React.PropTypes.array
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'footer',
      theme: 'default',
      mobileTitle: '适配版',
      desktopTitle: '电脑版'
    };
  },

  render: function render() {
    var classSet = this.getClassSet();
    var MobileTag = this.props.mobileLink ? 'a' : 'span';
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    return React.createElement(
      'footer',
      _extends({}, restProps, {
        'data-am-widget': this.props.classPrefix,
        className: classNames(this.props.className, classSet)
      }),
      React.createElement(
        'div',
        { className: this.prefixClass('switch') },
        React.createElement(
          MobileTag,
          {
            className: this.prefixClass('ysp'),
            onClick: this.props.onRequestMobile,
            href: this.props.mobileLink,
            'data-rel': 'mobile'
          },
          this.props.mobileTitle
        ),
        React.createElement(
          'span',
          { className: this.prefixClass('divider') },
          '|'
        ),
        React.createElement(
          'a',
          {
            'data-rel': 'desktop',
            href: this.props.desktopLink,
            onClick: this.props.onRequestDesktop,
            className: this.prefixClass('desktop')
          },
          this.props.desktopTitle
        )
      ),
      React.createElement(
        'div',
        { className: this.prefixClass('miscs') },
        this.props.data ? this.props.data.map(function (item, i) {
          return React.createElement(
            'p',
            { key: i },
            item
          );
        }) : this.props.children
      )
    );
  }
});

module.exports = Footer;
},{"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],75:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');

var Form = React.createClass({
  displayName: 'Form',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string.isRequired,
    horizontal: React.PropTypes.bool,
    inline: React.PropTypes.bool
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'form'
    };
  },

  render: function render() {
    var classSet = this.getClassSet();
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    classSet[this.prefixClass('horizontal')] = this.props.horizontal;
    classSet[this.prefixClass('inline')] = this.props.inline;

    return React.createElement(
      'form',
      _extends({}, restProps, {
        className: classNames(classSet, this.props.className)
      }),
      this.props.children
    );
  }
});

module.exports = Form;
},{"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],76:[function(require,module,exports){
'use strict';

var React = require('react');
var classNames = require('classnames');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var Button = require('./Button');
var Input = require('./Input');

var FormFile = React.createClass({
  displayName: 'FormFile',

  mixins: [ClassNameMixin],

  propTypes: {},

  getDefaultProps: function getDefaultProps() {
    return {};
  },

  render: function render() {
    return React.createElement(
      FormGroup,
      {
        className: this.setClassNamespace('form-file')
      },
      React.createElement(Input, { type: 'file', standalone: true })
    );
  }
});

module.exports = FormFile;
},{"./Button":59,"./Input":84,"./mixins/ClassNameMixin":114,"classnames":132,"react":356}],77:[function(require,module,exports){
'use strict';

var React = require('react');
var classNames = require('classnames');
var ClassNameMixin = require('./mixins/ClassNameMixin');

var FormGroup = React.createClass({
  displayName: 'FormGroup',

  mixins: [ClassNameMixin],

  propTypes: {
    validation: React.PropTypes.string,
    amSize: React.PropTypes.oneOf(['sm', 'lg']),
    hasFeedback: React.PropTypes.bool
  },

  render: function render() {
    var classSet = {};

    classSet[this.setClassNamespace('form-group')] = true;
    this.props.validation && (classSet[this.setClassNamespace('form-' + this.props.validation)] = true);
    classSet[this.setClassNamespace('form-feedback')] = this.props.hasFeedback;
    classSet[this.setClassNamespace('form-icon')] = this.props.hasFeedback;

    if (this.props.amSize) {
      classSet[this.setClassNamespace('form-group-' + this.props.amSize)] = true;
    }

    return React.createElement(
      'div',
      { className: classNames(classSet, this.props.className) },
      this.props.children
    );
  }
});

module.exports = FormGroup;
},{"./mixins/ClassNameMixin":114,"classnames":132,"react":356}],78:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var AvgGrid = require('./AvgGrid');
var omit = require('object.omit');

var Gallery = React.createClass({
  displayName: 'Gallery',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    theme: React.PropTypes.oneOf(['default', 'overlay', 'bordered', 'imgbordered']),
    data: React.PropTypes.array,
    sm: React.PropTypes.number,
    md: React.PropTypes.number,
    lg: React.PropTypes.number
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'gallery',
      theme: 'default',
      data: []
    };
  },

  renderItem: function renderItem(item) {
    var img = item.img ? React.createElement('img', {
      src: item.img,
      key: 'galeryImg',
      alt: item.alt || item.title || null
    }) : null;
    var title = item.title ? React.createElement(
      'h3',
      {
        key: 'galleryTitle',
        className: this.prefixClass('title')
      },
      item.title
    ) : null;
    var desc = item.desc ? React.createElement(
      'div',
      {
        key: 'galleryDesc',
        className: this.prefixClass('desc')
      },
      item.desc
    ) : null;
    var galleryItem = item.link ? React.createElement(
      'a',
      { href: item.link },
      img,
      title,
      desc
    ) : [img, title, desc];

    return React.createElement(
      'div',
      {
        className: classNames(this.props.className, this.prefixClass('item'))
      },
      galleryItem
    );
  },

  render: function render() {
    var classSet = this.getClassSet();
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    return React.createElement(
      AvgGrid,
      _extends({}, restProps, {
        sm: this.props.sm || 2,
        md: this.props.md || 3,
        lg: this.props.lg || 4,
        'data-am-widget': this.props.classPrefix,
        className: classNames(this.props.className, classSet)
      }),
      this.props.data.map(function (item, i) {
        return React.createElement(
          'li',
          { key: i },
          this.renderItem(item)
        );
      }.bind(this))
    );
  }
});

module.exports = Gallery;
},{"./AvgGrid":56,"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],79:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var ReactDOM = require('react-dom');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var SmoothScrollMixin = require('./mixins/SmoothScrollMixin');
var Events = require('./utils/Events');
var debounce = require('./utils/debounce');
var dom = require('./utils/domUtils');
var CSSCore = require('./utils/CSSCore');
var Icon = require('./Icon');

var GoTop = React.createClass({
  displayName: 'GoTop',

  mixins: [ClassNameMixin, SmoothScrollMixin],

  propTypes: {
    classPrefix: React.PropTypes.string.isRequired,
    theme: React.PropTypes.oneOf(['default', 'fixed']),
    title: React.PropTypes.string,
    src: React.PropTypes.string,
    icon: React.PropTypes.string,
    autoHide: React.PropTypes.bool
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'gotop',
      theme: 'default'
    };
  },

  componentDidMount: function componentDidMount() {
    if (this.isAutoHide()) {
      var check = this.checkPosition;

      check();

      this._listener = Events.on(window, 'scroll', debounce(check, 100));
    }
  },

  componentWillUnmount: function componentWillUnmount() {
    this._listener && this._listener.off();
  },

  checkPosition: function checkPosition() {
    var action = (dom.scrollTop(window) > 50 ? 'add' : 'remove') + 'Class';

    CSSCore[action](ReactDOM.findDOMNode(this), this.setClassNamespace('active'));
  },

  isAutoHide: function isAutoHide() {
    return this.props.theme === 'fixed' && this.props.autoHide;
  },

  handleClick: function handleClick(e) {
    e.preventDefault();
    this.smoothScroll();
  },

  renderIcon: function renderIcon() {
    return this.props.src ? React.createElement('img', {
      className: this.prefixClass('icon-custom'),
      src: this.props.src,
      alt: this.props.title
    }) : React.createElement(Icon, {
      className: this.prefixClass('icon'),
      icon: this.props.icon || 'chevron-up'
    });
  },

  render: function render() {
    var classSet = this.getClassSet();
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    classSet[this.prefixClass(this.props.theme)] = true;
    classSet[this.setClassNamespace('active')] = !this.isAutoHide();

    return React.createElement(
      'div',
      _extends({}, restProps, {
        'data-am-widget': this.props.classPrefix,
        className: classNames(classSet, this.props.className)
      }),
      React.createElement(
        'a',
        {
          href: '#top',
          onClick: this.handleClick,
          title: this.props.title
        },
        this.props.title ? React.createElement(
          'span',
          { className: this.prefixClass('title') },
          this.props.title
        ) : null,
        this.renderIcon()
      )
    );
  }
});

module.exports = GoTop;
},{"./Icon":82,"./mixins/ClassNameMixin":114,"./mixins/SmoothScrollMixin":118,"./utils/CSSCore":120,"./utils/Events":121,"./utils/debounce":126,"./utils/domUtils":127,"classnames":132,"object.omit":186,"react":356,"react-dom":189}],80:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');

var Grid = React.createClass({
  displayName: 'Grid',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string.isRequired,
    component: React.PropTypes.node.isRequired,
    collapse: React.PropTypes.bool,
    fixed: React.PropTypes.bool
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'g',
      component: 'div'
    };
  },

  render: function render() {
    var Component = this.props.component;
    var classSet = this.getClassSet();
    var props = this.props;
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    // .am-g-fixed
    classSet[this.prefixClass('fixed')] = props.fixed;

    // .am-g-collapse
    classSet[this.prefixClass('collapse')] = props.collapse;

    return React.createElement(
      Component,
      _extends({}, restProps, {
        className: classNames(this.props.className, classSet)
      }),
      this.props.children
    );
  }
});

module.exports = Grid;
},{"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],81:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var Icon = require('./Icon');
var omit = require('object.omit');

var Header = React.createClass({
  displayName: 'Header',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    theme: React.PropTypes.oneOf(['default']),
    data: React.PropTypes.object,
    fixed: React.PropTypes.bool,
    title: React.PropTypes.node,
    link: React.PropTypes.string,
    onSelect: React.PropTypes.func
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'header',
      theme: 'default',
      onSelect: function onSelect() {}
    };
  },

  renderTitle: function renderTitle() {
    return this.props.title ? React.createElement(
      'h1',
      {
        className: this.prefixClass('title'),
        onClick: this.props.onSelect.bind(this, {
          title: this.props.title,
          link: this.props.link
        })
      },
      this.props.link ? React.createElement(
        'a',
        { href: this.props.link },
        this.props.title
      ) : this.props.title
    ) : null;
  },

  renderNav: function renderNav(position) {
    var data = this.props.data;
    var renderItem = function (item, i) {
      var handleClick = item.onSelect || this.props.onSelect;

      return React.createElement(
        'a',
        { href: item.link,
          onClick: handleClick.bind(this, item),
          key: 'headerNavItem' + i
        },
        item.title ? React.createElement(
          'span',
          { className: this.prefixClass('nav-title') },
          item.title
        ) : null,
        item.customIcon ? React.createElement('img', { src: item.customIcon, alt: item.title || null }) : item.icon ? React.createElement(Icon, {
          className: this.prefixClass('icon'),
          icon: item.icon
        }) : null
      );
    }.bind(this);

    return data && data[position] ? React.createElement(
      'div',
      {
        className: classNames(this.prefixClass('nav'), this.prefixClass(position))
      },
      data[position].map(function (item, i) {
        return renderItem(item, i);
      })
    ) : null;
  },

  render: function render() {
    var classSet = this.getClassSet();
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    // am-header-fixed: fixed header
    classSet[this.prefixClass('fixed')] = this.props.fixed;

    return React.createElement(
      'header',
      _extends({}, restProps, {
        'data-am-widget': this.props.classPrefix,
        className: classNames(this.props.className, classSet)
      }),
      this.renderNav('left'),
      this.renderTitle(),
      this.renderNav('right')
    );
  }
});

module.exports = Header;
},{"./Icon":82,"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],82:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var PropTypes = React.PropTypes;

var Icon = React.createClass({
  displayName: 'Icon',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: PropTypes.string,
    component: PropTypes.node.isRequired,
    amStyle: PropTypes.string,
    amSize: PropTypes.string,
    fw: PropTypes.bool,
    spin: PropTypes.bool,
    button: PropTypes.bool,
    size: PropTypes.string,
    href: PropTypes.string,
    icon: PropTypes.string.isRequired
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'icon',
      component: 'span'
    };
  },

  render: function render() {
    var classes = this.getClassSet(true);
    var props = this.props;
    var Component = props.href ? 'a' : props.component;
    var prefixClass = this.prefixClass;
    var setClassNamespace = this.setClassNamespace;
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    // am-icon-[iconName]
    classes[prefixClass(props.icon)] = true;

    // am-icon-btn
    classes[prefixClass('btn')] = props.button;

    // button style
    props.button && props.amStyle && (classes[setClassNamespace(props.amStyle)] = true);

    // am-icon-fw
    classes[prefixClass('fw')] = props.fw;

    // am-icon-spin
    classes[prefixClass('spin')] = props.spin;

    return React.createElement(
      Component,
      _extends({}, restProps, {
        href: this.props.href,
        className: classNames(classes, this.props.className)
      }),
      this.props.children
    );
  }
});

module.exports = Icon;
},{"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],83:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var constants = require('./constants');
var PropTypes = React.PropTypes;

var Image = React.createClass({
  displayName: 'Image',

  mixins: [ClassNameMixin],

  propTypes: {
    src: PropTypes.string.isRequired,
    circle: PropTypes.bool,
    radius: PropTypes.bool,
    round: PropTypes.bool,
    responsive: PropTypes.bool,
    thumbnail: PropTypes.bool,
    placeholder: PropTypes.string,
    threshold: PropTypes.number,
    callback: PropTypes.func,
    asBgImage: PropTypes.bool
  },

  render: function render() {
    var classSet = {};
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    classSet[constants.CLASSES.radius] = this.props.radius;
    classSet[constants.CLASSES.round] = this.props.round;
    classSet[constants.CLASSES.circle] = this.props.circle;
    classSet[this.setClassNamespace('img-responsive')] = this.props.responsive;
    classSet[this.setClassNamespace('img-thumbnail')] = this.props.thumbnail;

    return React.createElement('img', _extends({
      src: this.props.src
    }, restProps, {
      className: classNames(this.props.className, classSet)
    }));
  }
});

module.exports = Image;

/*
TODO:
- srcset/sizes 支持
  - http://caniuse.com/#feat=srcset
  - http://www.w3.org/html/wg/drafts/html/master/semantics.html#attr-img-srcset
  - https://css-tricks.com/responsive-images-youre-just-changing-resolutions-use-srcset/
- lazyload
- asBackground ?
*/
/*
 http://odin.s0.no/web/srcset/polyfill.htm
 https://github.com/borismus/srcset-polyfill
 https://github.com/JimBobSquarePants/srcset-polyfill
 http://www.html5rocks.com/en/mobile/high-dpi/
 http://www.html5rocks.com/en/tutorials/responsive/picture-element/
 https://ericportis.com/posts/2014/srcset-sizes/

 gif 占位符
 http://proger.i-forge.net/The_smallest_transparent_pixel/eBQ
 http://stackoverflow.com/questions/9126105/blank-image-encoded-as-data-uri
*/
},{"./constants":113,"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],84:[function(require,module,exports){
'use strict';

/**
 * Inputs Components
 * @desc includes input, input-group
 */

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var ReactDOM = require('react-dom');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var FormGroup = require('./FormGroup');
var Button = require('./Button');
var Icon = require('./Icon');
var constants = require('./constants');

var Input = React.createClass({
  displayName: 'Input',

  mixins: [ClassNameMixin],

  propTypes: {
    type: React.PropTypes.string,
    disabled: React.PropTypes.bool,
    radius: React.PropTypes.bool,
    round: React.PropTypes.bool,
    amSize: React.PropTypes.oneOf(['sm', 'lg']),
    amStyle: React.PropTypes.string,
    validation: React.PropTypes.oneOf(['success', 'warning', 'error']),
    label: React.PropTypes.node,
    help: React.PropTypes.node,
    addonBefore: React.PropTypes.node,
    addonAfter: React.PropTypes.node,
    btnBefore: React.PropTypes.node,
    btnAfter: React.PropTypes.node,
    id: React.PropTypes.string,
    groupClassName: React.PropTypes.string,
    wrapperClassName: React.PropTypes.string,
    labelClassName: React.PropTypes.string,
    helpClassName: React.PropTypes.string,
    icon: React.PropTypes.string,
    standalone: React.PropTypes.bool,
    inline: React.PropTypes.bool,
    hasFeedback: React.PropTypes.bool
  },

  getDefaultProps: function getDefaultProps() {
    return {
      type: 'text'
    };
  },

  getFieldDOMNode: function getFieldDOMNode() {
    return ReactDOM.findDOMNode(this.refs.field);
  },

  getValue: function getValue() {
    if (this.props.type === 'select' && this.props.multiple) {
      return this.getSelectedOptions();
    } else {
      return this.getFieldDOMNode().value;
    }
  },

  getChecked: function getChecked() {
    return this.getFieldDOMNode().checked;
  },

  getSelectedOptions: function getSelectedOptions() {
    var values = [];
    var options = this.getFieldDOMNode().getElementsByTagName('option');

    options.forEach(function (option) {
      if (option.selected) {
        var value = option.getAttribute('value') || option.innerHtml;

        values.push(value);
      }
    });

    return values;
  },

  isCheckboxOrRadio: function isCheckboxOrRadio() {
    return this.props.type === 'radio' || this.props.type === 'checkbox';
  },

  isFile: function isFile() {
    return this.props.type === 'file';
  },

  renderInput: function renderInput() {
    var input = null;
    var fieldClassName = this.isCheckboxOrRadio() || this.isFile() ? '' : this.setClassNamespace('form-field');
    var classSet = {};

    classSet[constants.CLASSES.round] = this.props.round;
    classSet[constants.CLASSES.radius] = this.props.radius;

    if (this.props.amSize && !this.props.standalone) {
      classSet[this.setClassNamespace('input-' + this.props.amSize)] = true;
    }

    var classes = classNames(this.props.className, fieldClassName, classSet);
    var props = omit(this.props, ['radius', 'round', 'amSize', 'amStyle', 'validation', 'label', 'help', 'addonBefore', 'addonAfter', 'btnBefore', 'btnAfter', 'groupClassName', 'wrapperClassName', 'labelClassName', 'helpClassName', 'icon', 'standalone', 'inline', 'hasFeedback']);

    switch (this.props.type) {
      case 'select':
        input = React.createElement(
          'select',
          _extends({}, props, {
            className: classes,
            ref: 'field',
            key: 'field'
          }),
          this.props.children
        );
        break;
      case 'textarea':
        input = React.createElement('textarea', _extends({}, props, {
          className: classes,
          ref: 'field',
          key: 'field'
        }));
        break;
      case 'submit':
      case 'reset':
        input = React.createElement(Button, _extends({}, props, {
          component: 'input',
          ref: 'field',
          key: 'field'
        }));
        break;
      default:
        input = React.createElement('input', _extends({}, props, {
          className: classes,
          ref: 'field',
          key: 'field'
        }));
    }

    return input;
  },

  // Input wrapper if wrapperClassName set
  renderWrapper: function renderWrapper(children) {
    return this.props.wrapperClassName ? React.createElement(
      'div',
      {
        className: this.props.wrapperClassName,
        key: 'wrapper'
      },
      children
    ) : children;
  },

  // Wrap block checkbox/radio
  renderCheckboxAndRadioWrapper: function renderCheckboxAndRadioWrapper(children) {
    // Don't wrap inline checkbox/radio
    return this.props.inline ? children : React.createElement(
      'div',
      {
        className: this.setClassNamespace(this.props.type),
        key: 'checkboxAndRadioWrapper'
      },
      children
    );
  },

  renderLabel: function renderLabel(children) {
    // label doesn't work with icon
    /*if (this.props.icon) {
      return null;
    }*/

    var classSet = {};

    if (this.isCheckboxOrRadio()) {
      // inline checkbox and radio
      classSet[this.setClassNamespace(this.props.type + '-inline')] = this.props.inline;
    } else {
      // normal form label
      classSet[this.setClassNamespace('form-label')] = true;
    }

    return this.props.label ? React.createElement(
      'label',
      {
        htmlFor: this.props.id,
        className: classNames(this.props.labelClassName, classSet),
        key: 'label'
      },
      children,
      this.props.label
    ) : children;
  },

  renderInputGroup: function renderInputGroup(children) {
    var groupPrefix = this.setClassNamespace('input-group');
    var addonClassName = groupPrefix + '-label';
    var btnClassName = groupPrefix + '-btn';
    var addonBefore = this.props.addonBefore ? React.createElement(
      'span',
      { className: addonClassName, key: 'addonBefore' },
      this.props.addonBefore
    ) : null;
    var addonAfter = this.props.addonAfter ? React.createElement(
      'span',
      { className: addonClassName, key: 'addonAfter' },
      this.props.addonAfter
    ) : null;
    var btnBefore = this.props.btnBefore ? React.createElement(
      'span',
      { className: btnClassName, key: 'btnBefore' },
      this.props.btnBefore
    ) : null;
    var btnAfter = this.props.btnAfter ? React.createElement(
      'span',
      { className: btnClassName, key: 'btnAfter' },
      this.props.btnAfter
    ) : null;
    var classSet = {};

    if (this.props.amSize) {
      classSet[groupPrefix + '-' + this.props.amSize] = true;
    }

    if (this.props.amStyle) {
      classSet[groupPrefix + '-' + this.props.amStyle] = true;
    }

    return addonBefore || addonAfter || btnBefore || btnAfter ? React.createElement(
      'div',
      {
        className: classNames(groupPrefix, classSet),
        key: 'inputGroup'
      },
      addonBefore,
      btnBefore,
      children,
      addonAfter,
      btnAfter
    ) : children;
  },

  // form help
  renderHelp: function renderHelp() {
    return this.props.help ? React.createElement(
      'p',
      {
        className: classNames(this.setClassNamespace('form-help'), this.props.helpClassName),
        key: 'help'
      },
      this.props.help
    ) : '';
  },

  renderIcon: function renderIcon() {
    var props = this.props;
    var feedbackIcon = {
      success: 'check',
      warning: 'warning',
      error: 'times'
    };
    var icon = props.icon || props.hasFeedback && props.validation && feedbackIcon[props.validation];

    return icon ? React.createElement(Icon, { icon: icon, key: 'icon' }) : null;
  },

  render: function render() {
    // standalone mode
    if (this.props.standalone) {
      return this.renderInput();
    }

    // render checkbox and radio, without FormGroup wrapper
    if (this.isCheckboxOrRadio()) {
      return this.renderWrapper(this.renderCheckboxAndRadioWrapper(this.renderLabel(this.renderInput())));
    }

    var groupClassName = classNames(this.props.type === 'select' ? this.setClassNamespace('form-select') : null, this.props.icon && this.setClassNamespace('form-icon'), this.props.groupClassName);

    return React.createElement(
      FormGroup,
      {
        className: groupClassName,
        validation: this.props.validation,
        amSize: this.props.amSize,
        hasFeedback: this.props.hasFeedback
      },
      [this.renderLabel(), this.renderWrapper(this.renderInputGroup(this.renderInput())), this.renderIcon(), this.renderHelp()]
    );
  }
});

module.exports = Input;
},{"./Button":59,"./FormGroup":77,"./Icon":82,"./constants":113,"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356,"react-dom":189}],85:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');

var List = React.createClass({
  displayName: 'List',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    component: React.PropTypes.node.isRequired,
    border: React.PropTypes.bool,
    bordered: React.PropTypes.bool,
    striped: React.PropTypes.bool,
    static: React.PropTypes.bool
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'list',
      component: 'ul'
    };
  },

  render: function render() {
    var classes = this.getClassSet();
    var Component = this.props.component;
    var props = this.props;
    var prefixClass = this.prefixClass;
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    // am-list-border
    classes[prefixClass('border')] = props.border || props.bordered;

    // am-list-striped
    classes[prefixClass('striped')] = props.striped;

    // am-list-static
    classes[prefixClass('static')] = props.static;

    return React.createElement(
      Component,
      _extends({}, restProps, {
        className: classNames(classes, props.className)
      }),
      props.children
    );
  }
});

module.exports = List;
},{"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],86:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var assign = require('object-assign');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');

var ListItem = React.createClass({
  displayName: 'ListItem',

  mixins: [ClassNameMixin],

  propTypes: {
    href: React.PropTypes.string,
    truncate: React.PropTypes.bool,
    component: React.PropTypes.any.isRequired,
    linkComponent: React.PropTypes.any,
    linkProps: React.PropTypes.object
  },

  getDefaultProps: function getDefaultProps() {
    return {
      component: 'li'
    };
  },

  render: function render() {
    var classes = {};
    var Component = this.props.component;
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    // set .am-text-truncate
    classes['am-text-truncate'] = this.props.truncate;

    // render Anchor
    if (this.props.href || this.props.linkComponent) {
      return this.renderAnchor(classes);
    }

    return React.createElement(
      Component,
      _extends({}, restProps, {
        className: classNames(classes, this.props.className)
      }),
      this.props.children
    );
  },

  renderAnchor: function renderAnchor(classes) {
    var props = this.props;
    var Component = props.component;
    var truncate = props.truncate ? 'am-text-truncate' : '';
    var linkComponent = this.props.linkComponent || 'a';
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    return React.createElement(
      Component,
      _extends({}, restProps, {
        className: classNames(classes, this.props.className)
      }),
      React.createElement(linkComponent, assign({
        className: truncate,
        href: this.props.href,
        title: this.props.title,
        target: this.props.target
      }, this.props.linkProps), this.props.children)
    );
  }
});

module.exports = ListItem;
},{"./mixins/ClassNameMixin":114,"classnames":132,"object-assign":185,"object.omit":186,"react":356}],87:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var Button = require('./Button');
var Col = require('./Col');

var ListNews = React.createClass({
  displayName: 'ListNews',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    theme: React.PropTypes.oneOf(['default']),
    data: React.PropTypes.object,
    header: React.PropTypes.node,
    footer: React.PropTypes.node,
    morePosition: React.PropTypes.oneOf(['top', 'bottom']),
    moreText: React.PropTypes.string,
    thumbPosition: React.PropTypes.oneOf(['top', 'left', 'right', 'bottom-left', 'bottom-right'])
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'list-news',
      theme: 'default',
      moreText: '更多 »'
    };
  },

  renderHeader: function renderHeader() {
    var data = this.props.data;

    return data && data.header && data.header.title ? React.createElement(
      'div',
      {
        className: classNames(this.prefixClass('hd'), this.setClassNamespace('cf'))
      },
      data.header.link ? React.createElement(
        'a',
        { href: data.header.link },
        React.createElement(
          'h2',
          null,
          data.header.title
        ),
        this.props.morePosition === 'top' ? React.createElement(
          'span',
          {
            className: classNames(this.prefixClass('more'), this.setClassNamespace('fr')) },
          this.props.moreText
        ) : null
      ) : React.createElement(
        'h2',
        null,
        data.header.title
      )
    ) : null;
  },

  // `more` on bottom
  renderFooter: function renderFooter() {
    return this.props.morePosition === 'bottom' && this.props.data.header.link ? React.createElement(
      'div',
      { className: this.prefixClass('ft') },
      React.createElement(
        Button,
        {
          className: this.prefixClass('more'),
          href: this.props.data.header.link },
        this.props.moreText
      )
    ) : null;
  },

  getListItemClasses: function getListItemClasses(item) {
    return classNames(this.setClassNamespace('g'), item.date ? this.setClassNamespace('list-item-dated') : false, item.desc ? this.setClassNamespace('list-item-desced') : false, item.img ? this.setClassNamespace('list-item-thumbed') : false, this.props.thumbPosition ? this.setClassNamespace('list-item-thumb-' + this.props.thumbPosition) : false);
  },

  renderBody: function renderBody(children) {
    return React.createElement(
      'div',
      { className: this.prefixClass('bd') },
      React.createElement(
        'ul',
        { className: this.setClassNamespace('list') },
        children
      )
    );
  },

  renderList: function renderList() {
    var position = this.props.thumbPosition;
    var orderChildren = function (item, i) {
      var thumb = this.renderItemThumb(item, i);
      var main = this.renderItemMain(item, i);

      return position === 'right' || position === 'bottom-right' ? [main, thumb] : [thumb, main];
    }.bind(this);

    return this.props.data.main.map(function (item, i) {
      return React.createElement(
        'li',
        {
          key: i,
          className: this.getListItemClasses(item)
        },
        position === 'bottom-left' || position === 'bottom-right' ? this.renderThumbItemTitle(item) : null,
        orderChildren(item, i)
      );
    }.bind(this));
  },

  renderItemMisc: function renderItemMisc(item, type) {
    var Tag = type === 'date' ? 'span' : 'div';
    var className;

    switch (type) {
      case 'date':
        className = 'list-date';
        break;
      case 'desc':
        className = 'list-item-text';
        break;
      case 'mainAddition':
        className = 'list-news-addon';
        break;
      case 'thumbAddition':
        className = 'list-thumb-addon';
    }

    return item[type] ? React.createElement(
      Tag,
      { className: this.setClassNamespace(className) },
      item[type]
    ) : null;
  },

  renderItemThumb: function renderItemThumb(item, i) {
    var Link = item.component || 'a';
    var cols = this.props.thumbPosition === 'top' ? 12 : 4;

    return item.img ? React.createElement(
      Col,
      {
        key: 'thumb' + i,
        sm: cols,
        className: this.setClassNamespace('list-thumb')
      },
      React.createElement(
        Link,
        {
          href: item.link
        },
        React.createElement('img', { src: item.img, alt: item.title })
      ),
      this.renderItemMisc(item, 'thumbAddition')
    ) : null;
  },

  renderItemMain: function renderItemMain(item, i) {
    var Link = item.component || 'a';
    var position = this.props.thumbPosition;
    var date = this.renderItemMisc(item, 'date');
    var desc = this.renderItemMisc(item, 'desc');
    var addon = this.renderItemMisc(item, 'mainAddition');
    // title of list without thumbnail
    var itemWithoutThumbTitle = !position && item.title ? React.createElement(
      Link,
      {
        key: 'title' + i,
        className: this.setClassNamespace('list-item-hd'),
        href: item.link
      },
      item.title
    ) : null;
    var cols = position === 'top' ? 12 : item.img ? 8 : 12;

    return position ? React.createElement(
      Col,
      {
        sm: cols,
        className: this.setClassNamespace('list-main'),
        key: 'itemMain' + i
      },
      position !== 'bottom-left' && position !== 'bottom-right' ? this.renderThumbItemTitle(item) : null,
      date,
      desc,
      addon
    ) : [itemWithoutThumbTitle, date, desc, addon];
  },

  renderThumbItemTitle: function renderThumbItemTitle(item) {
    var Link = item.component || 'a';

    return item.title ? React.createElement(
      'h3',
      { className: this.setClassNamespace('list-item-hd') },
      React.createElement(
        Link,
        { href: item.link },
        item.title
      )
    ) : null;
  },

  render: function render() {
    var classSet = this.getClassSet();
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    return React.createElement(
      'div',
      _extends({}, restProps, {
        'data-am-widget': this.props.classPrefix,
        className: classNames(this.props.className, classSet)
      }),
      this.props.header || this.renderHeader(),
      this.renderBody(this.renderList()),
      this.props.footer || this.renderFooter()
    );
  }
});

module.exports = ListNews;
},{"./Button":59,"./Col":64,"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],88:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var Icon = require('./Icon');
var AvgGrid = require('./AvgGrid');

var Menu = React.createClass({
  displayName: 'Menu',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    theme: React.PropTypes.oneOf(['default', 'dropdown1', 'dropdown2', 'slide1', 'stack']),
    data: React.PropTypes.array,
    onSelect: React.PropTypes.func,
    toggleTitle: React.PropTypes.string,
    toggleCustomIcon: React.PropTypes.string,
    toggleIcon: React.PropTypes.string,
    cols: React.PropTypes.number
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'menu',
      theme: 'default',
      data: [],
      onSelect: function onSelect() {}
    };
  },

  getInitialState: function getInitialState() {
    return {
      data: this.props.data,
      expanded: !this.isDropdown()
    };
  },

  handleClick: function handleClick(nav, index, closeAll, e) {
    if (nav && nav.subMenu) {
      this.handleParentClick(nav, index, closeAll, e);
    }

    this.props.onSelect.call(this, nav, index, e);
  },

  /**
   * handle nav with subMenu click
   * @param {object} nav - clicked nav
   * @param {number} index - clicked nav index
   * @param {bool} closeAll - close all submenu
   * @param {object} e
   */
  handleParentClick: function handleParentClick(nav, index, closeAll, e) {
    e && e.preventDefault();

    var data = this.state.data.map(function (item, i) {
      item.subActive = closeAll ? false : index === i ? !item.subActive : false;
      return item;
    });

    this.setState({
      data: data
    });
  },

  closeAll: function closeAll() {
    this.handleParentClick(null, null, true, undefined);
  },

  // handle toggle button click for dropdown/slide theme
  handleToggle: function handleToggle(e) {
    e && e.preventDefault();
    this.setState({
      expanded: !this.state.expanded
    }, function () {
      !this.state.expanded && this.closeAll();
    }.bind(this));
  },

  isDropdown: function isDropdown() {
    return ['dropdown1', 'dropdown2', 'slide1'].indexOf(this.props.theme) > -1;
  },

  renderMenuToggle: function renderMenuToggle() {
    var title = this.props.toggleTitle ? React.createElement(
      'span',
      {
        className: this.prefixClass('toggle-title')
      },
      this.props.toggleTitle
    ) : null;
    var icon = this.props.toggleCustomIcon ? React.createElement('img', {
      src: this.props.toggleCustomIcon,
      alt: 'Menu Toggle'
    }) : React.createElement(Icon, {
      className: this.prefixClass('toggle-icon'),
      icon: this.props.toggleIcon || 'bars'
    });

    return React.createElement(
      'a',
      {
        href: '#',
        onClick: this.handleToggle,
        className: classNames(this.prefixClass('toggle'), this.state.expanded ? this.setClassNamespace('active') : null)
      },
      title,
      icon
    );
  },

  renderNavs: function renderNavs() {
    var _this = this;
    var openClassName = this.setClassNamespace('open');
    var inClassName = this.setClassNamespace('in');

    return this.state.data.map(function (nav, i) {
      var Link = nav.component || 'a';
      var LinkProps = nav.props || {};

      return React.createElement(
        'li',
        {
          key: i,
          className: classNames(nav.subMenu ? _this.setClassNamespace('parent') : null, nav.subActive ? openClassName : null)
        },
        React.createElement(
          Link,
          _extends({
            onClick: _this.handleClick.bind(_this, nav, i, false),
            href: nav.link
          }, LinkProps),
          nav.title
        ),
        nav.subMenu ? React.createElement(
          AvgGrid,
          {
            sm: nav.subCols || 1,
            className: classNames(_this.prefixClass('sub'), _this.setClassNamespace('collapse'), nav.subActive ? inClassName : null)
          },
          nav.subMenu.map(function (subNav, index) {
            var SubLink = subNav.component || 'a';
            var SubLinkProps = subNav.props || {};

            return React.createElement(
              'li',
              { key: index },
              React.createElement(
                SubLink,
                _extends({
                  onClick: _this.handleClick.bind(_this, subNav, [i, index], false),
                  target: subNav.target,
                  href: subNav.link
                }, SubLinkProps),
                subNav.title
              )
            );
          })
        ) : null
      );
    });
  },

  render: function render() {
    var classSet = this.getClassSet();
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));
    var hideTopLevel = !this.state.expanded ? this.setClassNamespace('collapse') : null;

    return React.createElement(
      'nav',
      _extends({}, restProps, {
        'data-am-widget': this.props.classPrefix,
        className: classNames(this.props.className, classSet)
      }),
      this.renderMenuToggle(),
      React.createElement(
        AvgGrid,
        {
          sm: this.props.cols,
          className: classNames(this.prefixClass('nav'), hideTopLevel)
        },
        this.renderNavs()
      )
    );
  }
});

module.exports = Menu;
},{"./AvgGrid":56,"./Icon":82,"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],89:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var ReactDOM = require('react-dom');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var DimmerMixin = require('./mixins/DimmerMixin');
var Events = require('./utils/Events');
var Close = require('./Close');
var Icon = require('./Icon');

var Modal = React.createClass({
  displayName: 'Modal',

  mixins: [ClassNameMixin, DimmerMixin],

  propTypes: {
    classPrefix: React.PropTypes.string.isRequired,
    type: React.PropTypes.oneOf(['alert', 'confirm', 'prompt', 'loading', 'actions', 'popup']),
    title: React.PropTypes.node,
    confirmText: React.PropTypes.string,
    cancelText: React.PropTypes.string,
    closeIcon: React.PropTypes.bool,
    closeViaDimmer: React.PropTypes.bool,
    onRequestClose: React.PropTypes.func,
    marginTop: React.PropTypes.string,
    marginLeft: React.PropTypes.string,
    modalWidth: React.PropTypes.string,
    modalHeight: React.PropTypes.string,
    onConfirm: React.PropTypes.func,
    onCancel: React.PropTypes.func
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'modal',
      closeIcon: true,
      confirmText: '确定',
      cancelText: '取消',
      onRequestClose: function onRequestClose() {}
    };
  },

  getInitialState: function getInitialState() {
    return {
      transitioning: false
    };
  },

  componentDidMount: function componentDidMount() {
    this._documentKeyupListener = Events.on(document, 'keyup', this.handleDocumentKeyUp, false);

    this.setDimmerContainer();

    // TODO: 何为添加动画效果的最佳时机？ render 完成以后添加动画 Class？
    this.setState({
      transitioning: true
    });
  },

  componentWillUnmount: function componentWillUnmount() {
    this._documentKeyupListener.off();
    this.resetDimmerContainer();
  },

  handleDimmerClick: function handleDimmerClick() {
    if (this.props.closeViaDimmer) {
      this.props.onRequestClose();
    }
  },

  handleBackdropClick: function handleBackdropClick(e) {
    if (e.target !== e.currentTarget) {
      return;
    }

    this.props.onRequestClose();
  },

  handleDocumentKeyUp: function handleDocumentKeyUp(e) {
    if (!this.props.keyboard && e.keyCode === 27) {
      this.props.onRequestClose();
    }
  },

  isPopup: function isPopup() {
    return this.props.type === 'popup';
  },

  isActions: function isActions() {
    return this.props.type === 'actions';
  },

  // Get input data for prompt modal
  getPromptData: function getPromptData() {
    var data = [];
    var inputs = ReactDOM.findDOMNode(this).querySelectorAll('input');

    if (inputs) {
      var i = 0;

      for (; i < inputs.length; i++) {
        data.push(inputs[i].value);
      }
    }

    return data.length === 0 ? null : data.length === 1 ? data[0] : data;
  },

  handleConfirm: function handleConfirm(e) {
    var data = e;

    if (this.props.type === 'prompt') {
      data = this.getPromptData();
    }

    this.props.onConfirm(data);
  },

  renderActions: function renderActions() {
    return React.createElement(
      'div',
      {
        style: { display: 'block' },
        className: classNames(this.props.className, this.setClassNamespace('modal-actions'), this.setClassNamespace('modal-active'))
      },
      this.props.children
    );
  },

  renderPopup: function renderPopup() {
    return React.createElement(
      'div',
      {
        style: { display: 'block' },
        className: classNames(this.props.className, this.setClassNamespace('popup'), this.setClassNamespace('modal-active'))
      },
      React.createElement(
        'div',
        { className: this.setClassNamespace('popup-inner') },
        React.createElement(
          'div',
          { className: this.setClassNamespace('popup-hd') },
          this.props.title ? React.createElement(
            'h4',
            { className: this.setClassNamespace('popup-title') },
            this.props.title
          ) : null,
          React.createElement(Close, { onClick: this.props.onRequestClose })
        ),
        React.createElement(
          'div',
          { className: this.setClassNamespace('popup-bd') },
          this.props.children
        )
      )
    );
  },

  renderHeader: function renderHeader() {
    var title = this.props.title;
    var closeIcon = this.props.closeIcon && !this.props.type ? React.createElement(Close, {
      spin: true,
      onClick: this.props.onRequestClose
    }) : null;

    return this.props.title || closeIcon ? React.createElement(
      'div',
      { className: this.prefixClass('hd') },
      title ? React.createElement(
        'h4',
        {
          className: this.setClassNamespace('margin-bottom-sm')
        },
        title
      ) : null,
      closeIcon
    ) : null;
  },

  // Render alert/confirm/prompt buttons
  renderFooter: function renderFooter() {
    var buttons;
    var btnClass = this.prefixClass('btn');
    var props = this.props;

    switch (this.props.type) {
      case 'alert':
        buttons = React.createElement(
          'span',
          {
            onClick: this.props.onConfirm,
            className: btnClass
          },
          this.props.confirmText
        );
        break;
      case 'confirm':
      case 'prompt':
        buttons = [props.cancelText, props.confirmText].map(function (text, i) {
          return React.createElement(
            'span',
            {
              key: i,
              onClick: i === 0 ? this.props.onCancel : this.handleConfirm,
              className: btnClass
            },
            text
          );
        }.bind(this));
        break;
      default:
        buttons = null;
    }

    return buttons ? React.createElement(
      'div',
      { className: this.prefixClass('footer') },
      buttons
    ) : null;
  },

  render: function render() {
    if (this.isActions()) {
      return this.renderDimmer(this.renderActions());
    }

    if (this.isPopup()) {
      return this.renderDimmer(this.renderPopup());
    }

    var classSet = this.getClassSet();
    var props = this.props;
    var footer = this.renderFooter();
    var style = {
      display: 'block'
    };
    var dialogDimension = {
      width: props.modalWidth,
      height: props.modalHeight
    };
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    classSet[this.prefixClass('active')] = this.state.transitioning;

    // .am-modal-no-btn -> refactor this style using `~` selector
    classSet[this.prefixClass('no-btn')] = !footer;
    props.type && (classSet[this.prefixClass(props.type)] = true);

    var modal = React.createElement(
      'div',
      _extends({}, restProps, {
        style: style,
        ref: 'modal',
        title: null,
        className: classNames(classSet, props.className)
      }),
      React.createElement(
        'div',
        {
          className: this.prefixClass('dialog'),
          style: dialogDimension
        },
        this.renderHeader(),
        React.createElement(
          'div',
          {
            className: this.prefixClass('bd'),
            ref: 'modalBody'
          },
          props.type === 'loading' ? props.children ? props.children : React.createElement(Icon, { icon: 'spinner', spin: true }) : props.children
        ),
        footer
      )
    );

    return this.renderDimmer(modal);
  }
});

module.exports = Modal;

// TODO: Modal 动画效果实现
// -> 如何关闭 Loading Modal?
// -> 关闭 Modal 以后窗口滚动会原来滚动条所在位置
},{"./Close":63,"./Icon":82,"./mixins/ClassNameMixin":114,"./mixins/DimmerMixin":116,"./utils/Events":121,"classnames":132,"object.omit":186,"react":356,"react-dom":189}],90:[function(require,module,exports){
'use strict';

var React = require('react');
var cloneElement = React.cloneElement;
var OverlayMixin = require('./mixins/OverlayMixin');
var DimmerMixin = require('./mixins/DimmerMixin');
var createChainedFunction = require('./utils/createChainedFunction');

var ModalTrigger = React.createClass({
  displayName: 'ModalTrigger',

  mixins: [OverlayMixin, DimmerMixin],

  propTypes: {
    modal: React.PropTypes.node.isRequired,
    onConfirm: React.PropTypes.func,
    onCancel: React.PropTypes.func,
    title: React.PropTypes.string,
    show: React.PropTypes.bool,
    onClose: React.PropTypes.func
  },

  getInitialState: function getInitialState() {
    return {
      isModalActive: false,
      modalWidth: null,
      modalMarginLeft: null,
      modalHeight: null,
      modalMarginTop: null
    };
  },

  componentDidMount: function componentDidMount() {
    if (this.props.show) {
      this.open();
    }
  },

  componentWillReceiveProps: function componentWillReceiveProps(nextProps) {
    if (nextProps.show && nextProps.show !== this.props.show) {
      this.open();
    }
  },

  open: function open() {
    this.setState({
      isModalActive: true
    }, this.setModalStyle);
  },

  close: function close() {
    this.setState({
      isModalActive: false
    });

    if (this.props.onClose) {
      this.props.onClose();
    }
  },

  toggle: function toggle() {
    if (this.state.isModalActive) {
      this.close();
    } else {
      this.open();
    }
  },

  setModalStyle: function setModalStyle() {
    if (!this.isMounted()) {
      return;
    }

    // TODO: selector
    var modal = this.getOverlayDOMNode().querySelector('.am-modal');

    if (!modal) {
      return;
    }

    var style = {};

    if (this.props.modalHeight) {
      style.modalHeight = this.props.modalHeight;
      // @since 1.1.0, requires Amaze UI 2.6.0+
      // style.modalMarginTop = -this.props.height / 2;
    }
    /*
    else {
      style.modalMarginTop = -modal.offsetHeight / 2;
    }
    */

    if (this.props.modalWidth) {
      style.modalWidth = this.props.modalWidth;
      // style.modalMarginLeft = -this.props.modalWidth / 2;
    }

    this.setState(style);
  },

  // overlay is the modal
  renderOverlay: function renderOverlay() {
    if (!this.state.isModalActive) {
      return React.createElement('span', null);
    }

    return cloneElement(this.props.modal, {
      onRequestClose: this.close,
      marginTop: this.state.modalMarginTop,
      marginLeft: this.state.modalMarginLeft,
      modalWidth: this.state.modalWidth,
      modalHeight: this.state.modalHeight,
      title: this.props.modal.props.title || this.props.title,
      onConfirm: createChainedFunction(this.props.onConfirm, this.close),
      onCancel: createChainedFunction(this.props.onCancel, this.close)
    });
  },

  render: function render() {
    // if "show" is defined, use "show" to control the modal
    if (typeof this.props.show !== 'undefined') {
      return React.createElement(
        'div',
        null,
        ' ',
        this.props.children,
        ' '
      );
    }

    var child = React.Children.only(this.props.children);
    var props = {};

    props.onClick = createChainedFunction(child.props.onClick, this.toggle);
    props.onMouseOver = createChainedFunction(child.props.onMouseOver, this.props.onMouseOver);
    props.onMouseOut = createChainedFunction(child.props.onMouseOut, this.props.onMouseOut);
    props.onFocus = createChainedFunction(child.props.onFocus, this.props.onFocus);
    props.onBlur = createChainedFunction(child.props.onBlur, this.props.onBlur);

    return cloneElement(child, props);
  }
});

module.exports = ModalTrigger;
},{"./mixins/DimmerMixin":116,"./mixins/OverlayMixin":117,"./utils/createChainedFunction":124,"react":356}],91:[function(require,module,exports){
'use strict';

/**
 * React version of NProgress
 * https://github.com/rstacruz/nprogress/
 */

var React = require('react');
var ReactDOM = require('react-dom');
var ClassNameMixin = require('./mixins/ClassNameMixin');

function clamp(n, min, max) {
  if (n < min) {
    return min;
  }

  if (n > max) {
    return max;
  }

  return n;
}

function toBarPercentage(n) {
  return (-1 + n) * 100;
}

var NProgress = React.createClass({
  displayName: 'NProgress',

  mixins: [ClassNameMixin],

  propTypes: {
    minimum: React.PropTypes.number,
    easing: React.PropTypes.string,
    speed: React.PropTypes.number,
    spinner: React.PropTypes.bool,
    trickle: React.PropTypes.bool,
    trickleRate: React.PropTypes.number,
    trickleSpeed: React.PropTypes.number
  },

  getInitialState: function getInitialState() {
    return {
      status: null
    };
  },

  getDefaultProps: function getDefaultProps() {
    return {
      minimum: 0.08,
      easing: 'ease',
      speed: 200,
      trickle: true,
      trickleRate: 0.02,
      trickleSpeed: 800
    };
  },

  start: function start() {
    var _this = this;
    var n = this.state.status; // this.set() is not sync to affected this.state.status

    if (!this.state.status) {
      this.set(this.props.minimum);
      n = this.props.minimum;
    }

    var work = function work() {
      setTimeout(function () {
        if (!n || n === 1) {
          return;
        }

        _this.trickle();
        work();
      }, _this.props.trickleSpeed);
    };

    this.props.trickle && work();
  },

  set: function set(n) {
    var _this = this;

    n = clamp(n, this.props.minimum, 1);
    this.setState({
      status: n
    });

    if (n === 1) {
      var progress = ReactDOM.findDOMNode(this.refs.progress);

      progress.style.opacity = 1;
      progress.style.transition = 'none';
      progress.offsetWidth;

      setTimeout(function () {
        progress.style.opacity = 0;
        progress.style.transition = 'all ' + _this.props.speed + 'ms linear';

        setTimeout(function () {
          _this.reset();
        }, _this.props.speed + 100);
      }, _this.props.speed);
    }
  },

  reset: function reset() {
    this.setState({
      status: null
    });
  },

  done: function done() {
    if (this.state.status) {
      this.inc(0.3 + 0.5 * Math.random());
      this.set(1);
    }
  },

  inc: function inc(amount) {
    var n = this.state.status;

    if (!n) {
      return this.start();
    } else {
      if (typeof amount !== 'number') {
        amount = (1 - n) * clamp(Math.random() * n, 0.1, 0.95);
      }

      n = clamp(n + amount, 0, 0.994);
      return this.set(n);
    }
  },

  trickle: function trickle() {
    if (this.state.status < 1) {
      this.inc(Math.random() * this.props.trickleRate);
    }
  },

  render: function render() {
    var props = this.props;
    var percent = this.state.status === null ? '-100' : toBarPercentage(this.state.status);
    var barStyle = {
      transition: 'all ' + props.speed + 'ms ' + props.easing,
      transform: 'translate(' + percent + '%,0)'
    };
    var spinner = props.spinner ? React.createElement(
      'div',
      { className: 'nprogress-spinner', ref: 'spinner' },
      React.createElement('div', { className: 'nprogress-spinner-icon' })
    ) : null;

    return this.state.status ? React.createElement(
      'div',
      { id: 'nprogress', ref: 'progress' },
      React.createElement(
        'div',
        { className: 'nprogress-bar', ref: 'bar', style: barStyle },
        React.createElement('div', { className: 'nprogress-peg' })
      ),
      spinner
    ) : null;
  }
});

module.exports = NProgress;
},{"./mixins/ClassNameMixin":114,"react":356,"react-dom":189}],92:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');

var Nav = React.createClass({
  displayName: 'Nav',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    justify: React.PropTypes.bool,
    pills: React.PropTypes.bool,
    tabs: React.PropTypes.bool,
    topbar: React.PropTypes.bool,
    component: React.PropTypes.node.isRequired
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'nav',
      component: 'ul'
    };
  },

  render: function render() {
    var classes = this.getClassSet();
    var Component = this.props.component;
    var restProps = omit(this.props, ['classPrefix', 'justify', 'pills', 'tabs', 'topbar', 'component', 'active', 'activeKey', 'activeHref']);

    // set classes
    classes[this.prefixClass('pills')] = this.props.pills || this.props.topbar;
    classes[this.prefixClass('tabs')] = this.props.tabs;
    classes[this.prefixClass('justify')] = this.props.justify;

    // topbar class
    classes[this.setClassNamespace('topbar-nav')] = this.props.topbar;

    return React.createElement(
      Component,
      _extends({}, restProps, {
        className: classNames(classes, this.props.className)
      }),
      this.props.children
    );
  }
});

module.exports = Nav;
},{"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],93:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var assign = require('object-assign');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');

var NavItem = React.createClass({
  displayName: 'NavItem',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    active: React.PropTypes.bool,
    disabled: React.PropTypes.bool,
    header: React.PropTypes.bool,
    divider: React.PropTypes.bool,
    href: React.PropTypes.any,
    component: React.PropTypes.any,
    linkComponent: React.PropTypes.any,
    linkProps: React.PropTypes.object
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'nav',
      component: 'li'
    };
  },

  render: function render() {
    var classes = this.getClassSet();
    var props = this.props;
    var Component = props.component;
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    // del am-nav
    classes[this.setClassNamespace(props.classPrefix)] = false;

    // set classes
    classes[this.prefixClass('header')] = props.header;
    classes[this.prefixClass('divider')] = props.divider;

    if (props.href || props.linkComponent) {
      return this.renderAnchor(classes);
    }

    return React.createElement(
      Component,
      _extends({}, restProps, {
        className: classNames(classes, props.className)
      }),
      this.props.children
    );
  },

  renderAnchor: function renderAnchor(classes) {
    var Component = this.props.component;
    var linkComponent = this.props.linkComponent || 'a';
    var style = {};
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    this.props.disabled && (style.pointerEvents = 'none');

    var linkProps = {
      href: this.props.href,
      title: this.props.title,
      target: this.props.target,
      style: style
    };

    return React.createElement(
      Component,
      _extends({}, restProps, {
        className: classNames(classes, this.props.className)
      }),
      React.createElement(linkComponent, assign(linkProps, this.props.linkProps), this.props.children)
    );
  }
});

module.exports = NavItem;

// TODO: DropDown Tab 处理
},{"./mixins/ClassNameMixin":114,"classnames":132,"object-assign":185,"object.omit":186,"react":356}],94:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var Icon = require('./Icon');
var omit = require('object.omit');

var Navbar = React.createClass({
  displayName: 'Navbar',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    theme: React.PropTypes.oneOf(['default']),
    data: React.PropTypes.array,
    onSelect: React.PropTypes.func
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'navbar',
      theme: 'default',
      data: [],
      onSelect: function onSelect() {}
    };
  },

  render: function render() {
    var classSet = this.getClassSet();
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    return React.createElement(
      'div',
      _extends({}, restProps, {
        'data-am-widget': this.props.classPrefix,
        className: classNames(this.props.className, classSet)
      }),
      React.createElement(
        'ul',
        { className: this.prefixClass('nav') },
        this.props.data.map(function (item, i) {
          var Link = item.component || 'a';
          var LinkProps = item.props || {};

          return React.createElement(
            'li',
            {
              key: i,
              onClick: this.props.onSelect.bind(this, item.link)
            },
            React.createElement(
              Link,
              _extends({
                href: item.link
              }, LinkProps),
              item.customIcon ? React.createElement('img', {
                src: item.customIcon,
                alt: item.title
              }) : item.icon ? React.createElement(Icon, { icon: item.icon }) : null,
              item.title ? React.createElement(
                'span',
                { className: this.prefixClass('label') },
                item.title
              ) : null
            )
          );
        }.bind(this))
      )
    );
  }
});

module.exports = Navbar;
},{"./Icon":82,"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],95:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var ReactDOM = require('react-dom');
var classNames = require('classnames');
var assign = require('object-assign');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');

var Pagination = React.createClass({
  displayName: 'Pagination',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    component: React.PropTypes.node.isRequired,
    centered: React.PropTypes.bool,
    right: React.PropTypes.bool,
    theme: React.PropTypes.oneOf(['default', 'select']),
    data: React.PropTypes.object,
    onSelect: React.PropTypes.func
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'pagination',
      component: 'ul'
    };
  },

  renderItem: function renderItem(type) {
    var data = this.props.data;

    return data && data[type + 'Title'] && data[type + 'Link'] ? React.createElement(
      Pagination.Item,
      {
        onClick: this.props.onSelect && this.props.onSelect.bind(this, data[type + 'Link']),
        key: type,
        href: data[type + 'Link'],
        className: this.prefixClass(type)
      },
      data[type + 'Title']
    ) : null;
  },

  handleChange: function handleChange(e) {
    if (this.props.onSelect) {
      var select = ReactDOM.findDOMNode(this.refs.select);

      this.props.onSelect.call(this, select && select.value, e);
    }
  },

  renderPages: function renderPages() {
    var data = this.props.data;

    if (data.pages) {
      return this.props.theme === 'select' ? React.createElement(
        'li',
        { className: this.prefixClass('select') },
        React.createElement(
          'select',
          {
            onChange: this.handleChange,
            ref: 'select'
          },
          data.pages.map(function (page, i) {
            return React.createElement(
              'option',
              { value: page.link, key: i },
              page.title,
              ' / ',
              data.pages.length
            );
          })
        )
      ) : data.pages.map(function (page, i) {
        return React.createElement(
          Pagination.Item,
          {
            key: i,
            onClick: this.props.onSelect && this.props.onSelect.bind(this, page.link),
            active: page.active,
            disabled: page.disabled,
            href: page.link
          },
          page.title
        );
      }.bind(this));
    }
  },

  render: function render() {
    var props = this.props;
    var Component = props.component;
    var classSet = this.getClassSet();
    var notSelect = props.theme !== 'select';
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    // .am-pagination-right
    classSet[this.prefixClass('right')] = props.right;

    // .am-pagination-centered
    classSet[this.prefixClass('centered')] = props.centered;

    return props.data ? React.createElement(
      Component,
      _extends({}, restProps, {
        className: classNames(classSet, props.className)
      }),
      notSelect && this.renderItem('first'),
      this.renderItem('prev'),
      this.renderPages(),
      this.renderItem('next'),
      notSelect && this.renderItem('last')
    ) : React.createElement(
      Component,
      _extends({}, restProps, {
        className: classNames(classSet, props.className)
      }),
      this.props.children
    );
  }
});

Pagination.Item = React.createClass({
  displayName: 'Item',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    active: React.PropTypes.bool,
    disabled: React.PropTypes.bool,
    prev: React.PropTypes.bool,
    next: React.PropTypes.bool,
    href: React.PropTypes.string,
    component: React.PropTypes.any,
    linkComponent: React.PropTypes.any,
    linkProps: React.PropTypes.object
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'pagination',
      component: 'li'
    };
  },

  render: function render() {
    var Component = this.props.component;
    var classSet = this.getClassSet(true);
    var props = this.props;
    var linkComponent = this.props.linkComponent || (this.props.href || this.props.active ? 'a' : null);
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    // .am-pagination-prev
    classSet[this.prefixClass('prev')] = props.prev;

    // .am-pagination-next
    classSet[this.prefixClass('next')] = props.next;

    return React.createElement(
      Component,
      _extends({}, restProps, {
        className: classNames(classSet, this.props.className)
      }),
      linkComponent ? React.createElement(linkComponent, assign({
        href: this.props.href || this.props.active && 'javascript: void(0)',
        title: this.props.title,
        target: this.props.target,
        ref: 'anchor'
      }, this.props.linkProps), this.props.children) : this.props.children
    );
  }
});

module.exports = Pagination;
},{"./mixins/ClassNameMixin":114,"classnames":132,"object-assign":185,"object.omit":186,"react":356,"react-dom":189}],96:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var ReactDOM = require('react-dom');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var CollapseMixin = require('./mixins/CollapseMixin');

var Panel = React.createClass({
  displayName: 'Panel',

  mixins: [ClassNameMixin, CollapseMixin],

  propTypes: {
    collapsible: React.PropTypes.bool,
    header: React.PropTypes.node,
    footer: React.PropTypes.node,
    id: React.PropTypes.string,
    classPrefix: React.PropTypes.string,
    amStyle: React.PropTypes.string,
    onSelect: React.PropTypes.func,
    eventKey: React.PropTypes.any
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'panel',
      amStyle: 'default'
    };
  },

  handleClick: function handleClick(e) {
    var selected = true;

    if (this.props.onSelect) {
      this.props.onSelect(e, this.props.eventKey);
    } else {
      e.preventDefault();
    }

    if (selected) {
      this.handleToggle();
    }
  },

  handleToggle: function handleToggle() {
    this.setState({ expanded: !this.state.expanded });
  },

  getCollapsibleDimensionValue: function getCollapsibleDimensionValue() {
    return ReactDOM.findDOMNode(this.refs.panel).scrollHeight;
  },

  getCollapsibleDOMNode: function getCollapsibleDOMNode() {
    if (!this.isMounted() || !this.refs || !this.refs.panel) {
      return null;
    }

    return ReactDOM.findDOMNode(this.refs.panel);
  },

  renderHeader: function renderHeader() {
    if (!this.props.header) {
      return null;
    }

    var header = this.props.header;

    return React.createElement(
      'div',
      { className: this.prefixClass('hd') },
      this.props.collapsible ? React.createElement(
        'h4',
        {
          'data-am-collapse': true // just for `pointer` style
          , className: classNames(this.prefixClass('title'), this.isExpanded() ? null : this.setClassNamespace('collapsed')),
          onClick: this.handleClick
        },
        header
      ) : header
    );
  },

  renderBody: function renderBody() {
    var bodyClass = this.prefixClass('bd');
    var bodyChildren = this.props.children;
    var bodyElements = [];
    var panelBodyChildren = [];

    function getProps() {
      return {
        key: bodyElements.length
      };
    }

    function addFillChild(child) {
      bodyElements.push(React.cloneElement(child, getProps()));
    }

    function addPanelBody(child) {
      bodyElements.push(React.createElement(
        'div',
        _extends({ className: bodyClass }, getProps, { key: 'panelBody' }),
        child
      ));
    }

    function maybeRenderPanelBody() {
      if (panelBodyChildren.length === 0) {
        return;
      }

      addPanelBody(panelBodyChildren);
      panelBodyChildren = [];
    }

    if (Array.isArray(bodyChildren)) {
      bodyChildren.forEach(function (child) {
        // props fill and isValidElement
        if (this.shouldRenderFill(child)) {
          maybeRenderPanelBody();

          addFillChild(child);
        } else {
          panelBodyChildren.push(child);
        }
      }.bind(this));

      maybeRenderPanelBody();
    } else {
      if (this.shouldRenderFill(bodyChildren)) {
        addFillChild(bodyChildren);
      } else {
        addPanelBody(bodyChildren);
      }
    }

    return bodyElements;
  },

  renderCollapsibleBody: function renderCollapsibleBody() {
    var collapseClass = this.prefixClass('collapse');

    return React.createElement(
      'div',
      {
        className: classNames(this.getCollapsibleClassSet(collapseClass)),
        id: this.props.id,
        ref: 'panel'
      },
      this.renderBody()
    );
  },

  shouldRenderFill: function shouldRenderFill(child) {
    return React.isValidElement(child) && child.props.fill;
  },

  renderFooter: function renderFooter() {
    if (!this.props.footer) {
      return null;
    }

    return React.createElement(
      'div',
      { className: this.prefixClass('footer') },
      this.props.footer
    );
  },

  render: function render() {
    var classes = this.getClassSet();
    var collapsible = this.props.collapsible;
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    return React.createElement(
      'div',
      _extends({}, restProps, {
        id: collapsible ? null : this.props.id,
        className: classNames(classes, this.props.className)
      }),
      this.renderHeader(),
      collapsible ? this.renderCollapsibleBody() : this.renderBody(),
      this.renderFooter()
    );
  }
});

module.exports = Panel;
},{"./mixins/ClassNameMixin":114,"./mixins/CollapseMixin":115,"classnames":132,"object.omit":186,"react":356,"react-dom":189}],97:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');

var PanelGroup = React.createClass({
  displayName: 'PanelGroup',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    amStyle: React.PropTypes.string,
    activeKey: React.PropTypes.any,
    defaultActiveKey: React.PropTypes.any,
    onSelect: React.PropTypes.func,
    accordion: React.PropTypes.bool
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'panel-group'
    };
  },

  getInitialState: function getInitialState() {
    return {
      activeKey: this.props.defaultActiveKey
    };
  },

  shouldComponentUpdate: function shouldComponentUpdate() {
    // Defer any updates to this component during the `onSelect` handler.
    return !this._isChanging;
  },

  handleSelect: function handleSelect(e, key) {
    e.preventDefault();

    if (this.props.onSelect) {
      this._isChanging = true;
      this.props.onSelect(key);
      this._isChanging = false;
    }

    if (this.state.activeKey === key) {
      key = null;
    }

    this.setState({
      activeKey: key
    });
  },

  renderPanel: function renderPanel(child, index) {
    var activeKey = this.props.activeKey != null ? this.props.activeKey : this.state.activeKey;

    var props = {
      amStyle: child.props.amStyle || this.props.amStyle,
      key: child.key ? child.key : index,
      ref: child.ref
    };

    if (this.props.accordion) {
      props.collapsible = true;
      props.expanded = child.props.eventKey === activeKey;
      props.onSelect = this.handleSelect;
    }

    return React.cloneElement(child, props);
  },

  render: function render() {
    var classes = this.getClassSet();
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    return React.createElement(
      'div',
      _extends({}, restProps, {
        className: classNames(classes, this.props.className)
      }),
      React.Children.map(this.props.children, this.renderPanel)
    );
  }
});

module.exports = PanelGroup;
},{"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],98:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');

var Popover = React.createClass({
  displayName: 'Popover',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string.isRequired,
    placement: React.PropTypes.oneOf(['top', 'right', 'bottom', 'left']),
    positionLeft: React.PropTypes.number,
    positionTop: React.PropTypes.number,
    amSize: React.PropTypes.oneOf(['sm', 'lg']),
    amStyle: React.PropTypes.string,
    onRequestHide: React.PropTypes.func
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'popover'
    };
  },

  render: function render() {
    var classSet = this.getClassSet();
    var style = {
      left: this.props.positionLeft,
      top: this.props.positionTop,
      display: 'block'
    };
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    classSet[this.setClassNamespace('active')] = true;
    classSet[this.prefixClass(this.props.placement)] = true;

    return React.createElement(
      'div',
      _extends({}, restProps, {
        style: style,
        className: classNames(classSet, this.props.className)
      }),
      React.createElement(
        'div',
        { className: this.prefixClass('inner') },
        this.props.children
      ),
      React.createElement('div', { className: this.prefixClass('caret') })
    );
  }
});

module.exports = Popover;
},{"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],99:[function(require,module,exports){
'use strict';

var React = require('react');
var ReactDOM = require('react-dom');
var cloneElement = React.cloneElement;
var OverlayMixin = require('./mixins/OverlayMixin');
var assign = require('object-assign');
var dom = require('./utils/domUtils');
var createChainedFunction = require('./utils/createChainedFunction');

function isOneOf(one, of) {
  if (Array.isArray(of)) {
    return of.indexOf(one) >= 0;
  }
  return one === of;
}

var PopoverTrigger = React.createClass({
  displayName: 'PopoverTrigger',

  mixins: [OverlayMixin],

  propTypes: {
    trigger: React.PropTypes.oneOfType([React.PropTypes.oneOf(['click', 'hover', 'focus']), React.PropTypes.arrayOf(React.PropTypes.oneOf(['click', 'hover', 'focus']))]),
    placement: React.PropTypes.oneOf(['top', 'right', 'bottom', 'left']),
    delay: React.PropTypes.number,
    delayOpen: React.PropTypes.number,
    delayClose: React.PropTypes.number,
    defaultPopoverActive: React.PropTypes.bool,
    popover: React.PropTypes.node.isRequired
  },

  getDefaultProps: function getDefaultProps() {
    return {
      placement: 'right',
      trigger: ['hover', 'focus']
    };
  },

  getInitialState: function getInitialState() {
    return {
      isPopoverActive: this.props.defaultPopoverActive == null ? false : this.props.defaultPopoverActive,
      popoverLeft: null,
      popoverTop: null
    };
  },

  componentDidMount: function componentDidMount() {
    if (this.props.defaultPopoverActive) {
      this.updatePopoverPosition();
    }
  },

  componentWillUnmount: function componentWillUnmount() {
    clearTimeout(this._hoverDelay);
  },

  open: function open() {
    this.setState({
      isPopoverActive: true
    }, function () {
      this.updatePopoverPosition();
    });
  },

  close: function close() {
    this.setState({
      isPopoverActive: false
    });
  },

  toggle: function toggle() {
    this.state.isPopoverActive ? this.close() : this.open();
  },

  handleDelayedOpen: function handleDelayedOpen() {
    if (this._hoverDelay != null) {
      clearTimeout(this._hoverDelay);
      this._hoverDelay = null;
      return;
    }

    var delay = this.props.delayOpen != null ? this.props.delayOpen : this.props.delay;

    if (!delay) {
      this.open();
      return;
    }

    this._hoverDelay = setTimeout(function () {
      this._hoverDelay = null;
      this.open();
    }.bind(this), delay);
  },

  handleDelayedClose: function handleDelayedClose() {
    if (this._hoverDelay != null) {
      clearTimeout(this._hoverDelay);
      this._hoverDelay = null;
      return;
    }

    var delay = this.props.delayClose != null ? this.props.delayClose : this.props.delay;

    if (!delay) {
      this.close();
      return;
    }

    this._hoverDelay = setTimeout(function () {
      this._hoverDelay = null;
      this.close();
    }.bind(this), delay);
  },

  updatePopoverPosition: function updatePopoverPosition() {
    if (!this.isMounted()) {
      return;
    }

    var position = this.calcPopoverPosition();

    this.setState({
      popoverLeft: position.left,
      popoverTop: position.top
    });
  },

  calcPopoverPosition: function calcPopoverPosition() {
    var childOffset = this.getPosition();
    var popoverNode = this.getOverlayDOMNode();
    var popoverHeight = popoverNode.offsetHeight;
    var popoverWidth = popoverNode.offsetWidth;
    var caretSize = 8;

    switch (this.props.placement) {
      case 'right':
        return {
          top: childOffset.top + childOffset.height / 2 - popoverHeight / 2,
          left: childOffset.left + childOffset.width + caretSize
        };
      case 'left':
        return {
          top: childOffset.top + childOffset.height / 2 - popoverHeight / 2,
          left: childOffset.left - popoverWidth - caretSize
        };
      case 'top':
        return {
          top: childOffset.top - popoverHeight - caretSize,
          left: childOffset.left + childOffset.width / 2 - popoverWidth / 2
        };
      case 'bottom':
        return {
          top: childOffset.top + childOffset.height + caretSize,
          left: childOffset.left + childOffset.width / 2 - popoverWidth / 2
        };
      default:
        throw new Error('calcPopoverPosition(): No such placement of [' + this.props.placement + '] found.');
    }
  },

  getPosition: function getPosition() {
    var node = ReactDOM.findDOMNode(this);
    var container = this.getContainerDOMNode();

    var offset = container.tagName === 'BODY' ? dom.offset(node) : dom.position(node, container);

    return assign({}, offset, {
      height: node.offsetHeight,
      width: node.offsetWidth
    });
  },

  // used by Mixin
  renderOverlay: function renderOverlay() {
    if (!this.state.isPopoverActive) {
      return React.createElement('span', null);
    }

    var popover = this.props.popover;

    return cloneElement(this.props.popover, {
      onRequestHide: this.close,
      placement: this.props.placement,
      positionLeft: this.state.popoverLeft,
      positionTop: this.state.popoverTop,
      amStyle: popover.props.amStyle || this.props.amStyle,
      amSize: popover.props.amSize || this.props.amSize
    });
  },

  render: function render() {
    var child = React.Children.only(this.props.children);

    var props = {};

    props.onClick = createChainedFunction(child.props.onClick, this.props.onClick);

    if (isOneOf('click', this.props.trigger)) {
      props.onClick = createChainedFunction(this.toggle, props.onClick);
    }

    if (isOneOf('hover', this.props.trigger)) {
      props.onMouseOver = createChainedFunction(this.handleDelayedOpen, this.props.onMouseOver);
      props.onMouseOut = createChainedFunction(this.handleDelayedClose, this.props.onMouseOut);
    }

    if (isOneOf('focus', this.props.trigger)) {
      props.onFocus = createChainedFunction(this.handleDelayedOpen, this.props.onFocus);
      props.onBlur = createChainedFunction(this.handleDelayedClose, this.props.onBlur);
    }

    return cloneElement(child, props);
  }
});

module.exports = PopoverTrigger;
},{"./mixins/OverlayMixin":117,"./utils/createChainedFunction":124,"./utils/domUtils":127,"object-assign":185,"react":356,"react-dom":189}],100:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');

var Progress = React.createClass({
  displayName: 'Progress',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    now: React.PropTypes.number,
    label: React.PropTypes.string,
    active: React.PropTypes.bool,
    striped: React.PropTypes.bool,
    amSize: React.PropTypes.string,
    amStyle: React.PropTypes.string
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'progress'
    };
  },

  renderProgressBar: function renderProgressBar() {
    var styleSheet = {
      width: this.props.now + '%'
    };
    var classes = {};
    var prefix = this.prefixClass('bar');
    var amStyle = this.props.amStyle;

    // set am-progress-bar
    classes[prefix] = true;

    if (amStyle) {
      classes[prefix + '-' + amStyle] = true;
    }

    return React.createElement(
      'div',
      {
        className: classNames(classes),
        style: styleSheet,
        role: 'progressbar'
      },
      this.props.label
    );
  },

  renderChildBar: function renderChildBar(child, index) {
    return React.cloneElement(child, {
      isChild: true,
      key: child.key ? child.key : index
    });
  },

  render: function render() {
    var classes = this.getClassSet();
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    // set class
    classes[this.prefixClass('striped')] = this.props.striped;

    if (this.props.active) {
      classes[this.prefixClass('striped')] = true;
    }

    if (!this.props.children) {
      if (!this.props.isChild) {
        return React.createElement(
          'div',
          _extends({}, restProps, {
            className: classNames(classes, this.props.className)
          }),
          this.renderProgressBar()
        );
      } else {
        return this.renderProgressBar();
      }
    } else {
      return React.createElement(
        'div',
        _extends({}, restProps, {
          className: classNames(classes, this.props.className)
        }),
        React.Children.map(this.props.children, this.renderChildBar)
      );
    }
  }
});

module.exports = Progress;

// Todo: 删除无用 class
//     : key ref 处理问题
},{"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],101:[function(require,module,exports){
'use strict';

var React = require('react');
var ReactDOM = require('react-dom');
var cloneElement = React.cloneElement;
var assign = require('object-assign');
var classNames = require('classnames');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var isInViewport = require('./utils/isInViewport');
var Events = require('./utils/Events');
var TransitionEvents = require('./utils/TransitionEvents');
var requestAnimationFrame = require('./utils/requestAnimationFrame');
var debounce = require('./utils/debounce');
var canUseDOM = require('./utils/canUseDOM');
var domUtils = require('./utils/domUtils');

var ScrollSpy = React.createClass({
  displayName: 'ScrollSpy',

  mixins: [ClassNameMixin],

  propTypes: {
    animation: React.PropTypes.string,
    delay: React.PropTypes.number,
    repeat: React.PropTypes.bool,
    // container which has scrollbar
    container: React.PropTypes.any
  },

  getDefaultProps: function getDefaultProps() {
    return {
      animation: 'fade',
      delay: 0,
      repeat: false
    };
  },

  getInitialState: function getInitialState() {
    return {
      inViewport: false
    };
  },

  componentDidMount: function componentDidMount() {
    if (canUseDOM) {
      this.checkRAF();

      var node = ReactDOM.findDOMNode(this);
      var doc = domUtils.ownerDocument(node);
      // var scrollContainer = ReactDOM.findDOMNode(this.props.container || doc.body);
      var debounced = debounce(this.checkRAF, 100).bind(this);

      this._scrollListener = Events.on(doc, 'scroll', debounced);
      this._resizeListener = Events.on(window, 'resize', debounced);
      this._orientationListener = Events.on(window, 'orientationchange', debounced);
    }
  },

  componentWillUnmount: function componentWillUnmount() {
    this._removeEventLister();
  },

  _removeEventLister: function _removeEventLister() {
    this._scrollListener && this._scrollListener.off();
    this._resizeListener && this._resizeListener.off();
    this._orientationListener && this._orientationListener.off();
    clearTimeout(this._timer);
  },

  checkIsInView: function checkIsInView() {
    if (!TransitionEvents.support.animationend) {
      return;
    }

    if (this.isMounted()) {
      var isInView = isInViewport(ReactDOM.findDOMNode(this));

      if (isInView && !this.state.inViewport) {
        if (this._timer) {
          clearTimeout(this._timer);
        }

        this._timer = setTimeout(function () {
          this.setState({
            inViewport: true
          });
        }.bind(this), this.props.delay);
      }

      if (this.props.repeat && !isInView) {
        this.setState({
          inViewport: false
        });
      }
    }
  },

  checkRAF: function checkRAF() {
    requestAnimationFrame(this.checkIsInView);
  },

  render: function render() {
    var animation = this.state.inViewport ? this.setClassNamespace('animation-' + this.props.animation) : null;
    var child = React.Children.only(this.props.children);

    // transfer child's props to cloned element
    return cloneElement(child, assign({}, child.props, {
      className: classNames(child.props.className, animation),
      'data-am-scrollspy': 'animation' // style helper
    }));
  }
});

module.exports = ScrollSpy;
},{"./mixins/ClassNameMixin":114,"./utils/Events":121,"./utils/TransitionEvents":122,"./utils/canUseDOM":123,"./utils/debounce":126,"./utils/domUtils":127,"./utils/isInViewport":129,"./utils/requestAnimationFrame":131,"classnames":132,"object-assign":185,"react":356,"react-dom":189}],102:[function(require,module,exports){
'use strict';

var React = require('react');
var ReactDOM = require('react-dom');
var cloneElement = React.cloneElement;
var assign = require('object-assign');
var omit = require('object.omit');
var classNames = require('classnames');
var SmoothScrollMixin = require('./mixins/SmoothScrollMixin');
var isInViewport = require('./utils/isInViewport');
var Events = require('./utils/Events');
var requestAnimationFrame = require('./utils/requestAnimationFrame');
var debounce = require('./utils/debounce');
var CSSCore = require('./utils/CSSCore');
var domUtils = require('./utils/domUtils');
var createChainedFunction = require('./utils/createChainedFunction');
var canUseDOM = require('./utils/canUseDOM');
var constants = require('./constants');

var ScrollSpyNav = React.createClass({
  displayName: 'ScrollSpyNav',

  mixins: [SmoothScrollMixin],

  propTypes: {
    activeClass: React.PropTypes.string,
    offsetTop: React.PropTypes.number,
    container: React.PropTypes.any
  },

  getDefaultProps: function getDefaultProps() {
    return {
      activeClass: constants.CLASSES.active
    };
  },

  componentDidMount: function componentDidMount() {
    if (canUseDOM) {
      this._init();
      this.checkRAF();

      var debounced = debounce(this.checkRAF, 100).bind(this);

      this._scrollListener = Events.on(window, 'scroll', this.checkRAF);
      this._resizeListener = Events.on(window, 'resize', debounced);
      this._orientationListener = Events.on(window, 'orientationchange', debounced);
    }
  },

  componentWillUnmount: function componentWillUnmount() {
    this._scrollListener && this._scrollListener.off();
    this._resizeListener && this._resizeListener.off();
    this._orientationListener && this._orientationListener.off();
  },

  _init: function _init() {
    this._linkNodes = ReactDOM.findDOMNode(this).querySelectorAll('a[href^="#"]');
    this._anchorNodes = [];

    Array.prototype.forEach.call(this._linkNodes, function (link) {
      var anchor = document.getElementById(link.getAttribute('href').substr(1));

      if (anchor) {
        this._anchorNodes.push(anchor);
      }
    }.bind(this));
  },

  checkIsInView: function checkIsInView() {
    if (this.isMounted()) {
      var inViewsNodes = [];

      this._anchorNodes.forEach(function (anchor) {
        if (isInViewport(anchor)) {
          inViewsNodes.push(anchor);
        }
      });

      if (inViewsNodes.length) {
        var targetNode;

        inViewsNodes.every(function (node) {
          if (domUtils.offset(node).top >= domUtils.scrollTop(window)) {
            targetNode = node;
            return false; // break loop
          }
          return true;
        });

        if (!targetNode) {
          return;
        }

        Array.prototype.forEach.call(this._linkNodes, function (link) {
          CSSCore.removeClass(link, this.props.activeClass);
        }.bind(this));

        var targetLink = ReactDOM.findDOMNode(this).querySelector('a[href="#' + targetNode.id + '"]');

        targetLink && CSSCore.addClass(targetLink, this.props.activeClass);
      }
    }
  },

  checkRAF: function checkRAF() {
    requestAnimationFrame(this.checkIsInView);
  },

  // Smooth scroll
  handleClick: function handleClick(e) {
    e.preventDefault();

    if (e.target && e.target.nodeName === 'A') {
      var targetNode = document.getElementById(e.target.getAttribute('href').substr(1));

      // TODO: set scroll element if `container` prop set
      targetNode && this.smoothScroll(window, {
        position: domUtils.offset(targetNode).top - this.props.offsetTop || 0
      });
    }
  },

  render: function render() {
    var child = React.Children.only(this.props.children);

    // transfer child's props to cloned element
    return cloneElement(child, assign({}, omit(this.props, Object.keys(this.constructor.propTypes)), child.props, {
      onClick: createChainedFunction(this.handleClick, child.props.onClick),
      className: classNames(this.props.className, child.props.className)
    }));
  }
});

module.exports = ScrollSpyNav;

// TODO: improve in view logic
},{"./constants":113,"./mixins/SmoothScrollMixin":118,"./utils/CSSCore":120,"./utils/Events":121,"./utils/canUseDOM":123,"./utils/createChainedFunction":124,"./utils/debounce":126,"./utils/domUtils":127,"./utils/isInViewport":129,"./utils/requestAnimationFrame":131,"classnames":132,"object-assign":185,"object.omit":186,"react":356,"react-dom":189}],103:[function(require,module,exports){
'use strict';

var React = require('react');
var ReactDOM = require('react-dom');
var classNames = require('classnames');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var Dropdown = require('./Dropdown');
var Icon = require('./Icon');
var Input = require('./Input');

var Selected = React.createClass({
  displayName: 'Selected',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    data: React.PropTypes.array.isRequired,
    placeholder: React.PropTypes.string,
    value: React.PropTypes.string,
    multiple: React.PropTypes.bool,
    searchBox: React.PropTypes.bool,
    name: React.PropTypes.string,
    onChange: React.PropTypes.func,
    optionFilter: React.PropTypes.func,
    dropup: React.PropTypes.bool,
    btnWidth: React.PropTypes.number,
    btnStyle: React.PropTypes.string,
    btnSize: React.PropTypes.string,
    maxHeight: React.PropTypes.number,

    // delimiter to use to join multiple values
    delimiter: React.PropTypes.string
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'selected',
      placeholder: '点击选择...',
      onChange: function onChange() {},
      value: '',
      delimiter: ',',
      optionFilter: function optionFilter(filterText, option) {
        return option.label.toLowerCase().indexOf(filterText.toLowerCase()) > -1;
      }
    };
  },

  getInitialState: function getInitialState() {
    return {
      value: this.props.value,
      dropdownWidth: null,
      filterText: null
    };
  },

  componentDidMount: function componentDidMount() {
    this.setDropdownWidth();
  },

  componentWillReceiveProps: function componentWillReceiveProps(nextProps) {
    if (this.props.value !== nextProps.value) {
      this.setState({
        value: nextProps.value
      });
    }
  },

  setDropdownWidth: function setDropdownWidth() {
    if (this.isMounted()) {
      var toggleButton = ReactDOM.findDOMNode(this.refs.dropdown.refs.dropdownToggle);

      toggleButton && this.setState({ dropdownWidth: toggleButton.offsetWidth });
    }
  },

  getValueArray: function getValueArray() {
    return this.state.value ? this.state.value.split(this.props.delimiter) : [];
  },

  hasValue: function hasValue(value) {
    return this.getValueArray().indexOf(value) > -1;
  },

  setValue: function setValue(value, callback) {
    this.setState({
      value: value
    }, function () {
      this.props.onChange(value);
      callback && callback();
    });
  },

  handleCheck: function handleCheck(option, e) {
    e.preventDefault();

    var clickedValue = option.value;

    // multiple select
    if (this.props.multiple) {
      var values = this.getValueArray();

      if (this.hasValue(clickedValue)) {
        values.splice(values.indexOf(clickedValue), 1);
      } else {
        values.push(clickedValue);
      }

      this.setValue(values.join(this.props.delimiter));
    } else {
      this.setValue(clickedValue);
      this.refs.dropdown.setDropdown(false);
    }
  },

  handleUserInput: function handleUserInput(e) {
    e.preventDefault();

    this.setState({
      filterText: ReactDOM.findDOMNode(this.refs.filterInput).value
    });
  },

  // clear filter
  clearFilterInput: function clearFilterInput() {
    if (this.props.multiple && this.props.searchBox) {
      this.setState({
        filterText: null
      });
      ReactDOM.findDOMNode(this.refs.filterInput).value = null;
    }
  },

  // API for getting component value
  getValue: function getValue() {
    return this.state.value;
  },

  render: function render() {
    var classSet = this.getClassSet();
    var selectedLabel = [];
    var items = [];
    var filterText = this.state.filterText;
    var groupHeader;

    this.props.data.forEach(function (option, i) {
      var checked = this.hasValue(option.value);
      var checkedClass = checked ? this.setClassNamespace('checked') : null;
      var checkedIcon = checked ? React.createElement(Icon, { icon: 'check' }) : null;

      checked && selectedLabel.push(option.label);

      // add group header
      if (option.group && groupHeader !== option.group) {
        groupHeader = option.group;
        items.push(React.createElement(
          'li',
          {
            className: this.prefixClass('list-header'),
            key: 'header' + i
          },
          groupHeader
        ));
      }

      if (filterText && !this.props.optionFilter(filterText, option)) {
        return;
      }

      items.push(React.createElement(
        'li',
        {
          className: checkedClass,
          onClick: this.handleCheck.bind(this, option),
          key: i
        },
        React.createElement(
          'span',
          { className: this.prefixClass('text') },
          option.label
        ),
        checkedIcon
      ));
    }.bind(this));

    var status = React.createElement(
      'span',
      {
        className: classNames(this.prefixClass('status'), this.setClassNamespace('fl'))
      },
      selectedLabel.length ? selectedLabel.join(', ') : React.createElement(
        'span',
        { className: this.prefixClass('placeholder ') },
        this.props.placeholder
      )
    );
    var optionsStyle = {};

    if (this.props.maxHeight) {
      optionsStyle = {
        maxHeight: this.props.maxHeight,
        overflowY: 'scroll'
      };
    }

    return React.createElement(
      Dropdown,
      {
        className: classNames(this.props.className, classSet),
        title: status,
        onClose: this.clearFilterInput,
        btnStyle: this.props.btnStyle,
        btnSize: this.props.btnSize,
        btnInlineStyle: { width: this.props.btnWidth },
        contentInlineStyle: { minWidth: this.state.dropdownWidth },
        toggleClassName: this.prefixClass('btn'),
        caretClassName: this.prefixClass('icon'),
        contentClassName: this.prefixClass('content'),
        contentTag: 'div',
        dropup: this.props.dropup,
        ref: 'dropdown'
      },
      this.props.searchBox ? React.createElement(
        'div',
        { className: this.prefixClass('search') },
        React.createElement(Input, {
          onChange: this.handleUserInput,
          autoComplete: 'off',
          standalone: true,
          ref: 'filterInput'
        })
      ) : null,
      React.createElement(
        'ul',
        {
          style: optionsStyle,
          className: this.prefixClass('list')
        },
        items
      ),
      React.createElement('input', {
        name: this.props.name,
        type: 'hidden',
        ref: 'selectedField',
        value: this.state.value
      })
    );
  }
});

module.exports = Selected;
},{"./Dropdown":73,"./Icon":82,"./Input":84,"./mixins/ClassNameMixin":114,"classnames":132,"react":356,"react-dom":189}],104:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var ReactDOM = require('react-dom');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var TransitionEvents = require('./utils/TransitionEvents');

//React.initializeTouchEvents(true);

var Slider = React.createClass({
  displayName: 'Slider',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    theme: React.PropTypes.oneOf(['default', 'a1', 'a2', 'a3', 'a4', 'a5', 'b1', 'b2', 'b3', 'b4', 'c1', 'c2', 'c3', 'c4', 'd1', 'd2', 'd3']),
    directionNav: React.PropTypes.bool, // prev/next icon
    controlNav: React.PropTypes.bool,

    animation: React.PropTypes.string, // not working
    slide: React.PropTypes.bool,
    autoPlay: React.PropTypes.bool,
    slideSpeed: React.PropTypes.number, // interval
    loop: React.PropTypes.bool, // loop slide

    pauseOnHover: React.PropTypes.bool,
    touch: React.PropTypes.bool, // TODO: add touch support

    onSelect: React.PropTypes.func,
    onSlideEnd: React.PropTypes.func,
    activeIndex: React.PropTypes.number,
    defaultActiveIndex: React.PropTypes.number,
    direction: React.PropTypes.oneOf(['prev', 'next'])
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'slider',
      theme: 'default',
      directionNav: true,
      controlNav: true,
      slide: true,
      autoPlay: true,
      loop: true,
      slideSpeed: 5000,
      pauseOnHover: true
    };
  },

  getInitialState: function getInitialState() {
    return {
      activeIndex: this.props.defaultActiveIndex == null ? 0 : this.props.defaultActiveIndex,
      previousActiveIndex: null,
      direction: null
    };
  },

  componentWillReceiveProps: function componentWillReceiveProps(nextProps) {
    var activeIndex = this.getActiveIndex();

    if (nextProps.activeIndex != null && nextProps.activeIndex !== activeIndex) {
      clearTimeout(this.timeout);
      this.setState({
        previousActiveIndex: activeIndex,
        direction: nextProps.direction != null ? nextProps.direction : this.getDirection(activeIndex, nextProps.activeIndex)
      });
    }
  },

  componentDidMount: function componentDidMount() {
    this.props.autoPlay && this.waitForNext();
  },

  componentWillUnmount: function componentWillUnmount() {
    clearTimeout(this.timeout);
  },

  getDirection: function getDirection(prevIndex, index) {
    if (prevIndex === index) {
      return null;
    }

    return prevIndex > index ? 'prev' : 'next';
  },

  next: function next(e) {
    e && e.preventDefault();

    var index = this.getActiveIndex() + 1;
    var count = React.Children.count(this.props.children);

    if (index > count - 1) {
      if (!this.props.loop) {
        return;
      }
      index = 0;
    }

    this.handleSelect(index, 'next');
  },

  prev: function prev(e) {
    e && e.preventDefault();

    var index = this.getActiveIndex() - 1;

    if (index < 0) {
      if (!this.props.loop) {
        return;
      }
      index = React.Children.count(this.props.children) - 1;
    }

    this.handleSelect(index, 'prev');
  },

  pause: function pause() {
    this.isPaused = true;
    clearTimeout(this.timeout);
  },

  play: function play() {
    this.isPaused = false;
    this.waitForNext();
  },

  waitForNext: function waitForNext() {
    if (!this.isPaused && this.props.slide && this.props.slideSpeed && this.props.activeIndex == null) {
      this.timeout = setTimeout(this.next, this.props.slideSpeed);
    }
  },

  handleMouseOver: function handleMouseOver() {
    if (this.props.pauseOnHover) {
      this.pause();
    }
  },

  handleMouseOut: function handleMouseOut() {
    if (this.isPaused) {
      this.play();
    }
  },

  getActiveIndex: function getActiveIndex() {
    return this.props.activeIndex != null ? this.props.activeIndex : this.state.activeIndex;
  },

  handleItemAnimateOutEnd: function handleItemAnimateOutEnd() {
    this.setState({
      previousActiveIndex: null,
      direction: null
    }, function () {
      this.waitForNext();

      if (this.props.onSlideEnd) {
        this.props.onSlideEnd();
      }
    });
  },

  handleSelect: function handleSelect(index, direction, e) {
    e && e.preventDefault();
    clearTimeout(this.timeout);

    var previousActiveIndex = this.getActiveIndex();

    direction = direction || this.getDirection(previousActiveIndex, index);

    if (this.props.onSelect) {
      this.props.onSelect(index, direction);
    }

    if (this.props.activeIndex == null && index !== previousActiveIndex) {
      if (this.state.previousActiveIndex != null) {
        // If currently animating don't activate the new index.
        // TODO: look into queuing this canceled call and
        // animating after the current animation has ended.
        return;
      }

      this.setState({
        activeIndex: index,
        previousActiveIndex: previousActiveIndex,
        direction: direction
      });
    }
  },

  renderDirectionNav: function renderDirectionNav() {
    return this.props.directionNav ? React.createElement(
      'ul',
      { className: this.setClassNamespace('direction-nav') },
      React.createElement(
        'li',
        null,
        React.createElement(
          'a',
          {
            onClick: this.prev,
            className: this.setClassNamespace('prev'),
            href: '#prev' },
          'Previous'
        )
      ),
      React.createElement(
        'li',
        null,
        React.createElement(
          'a',
          {
            onClick: this.next,
            className: this.setClassNamespace('next'),
            href: '#next' },
          'Next'
        )
      )
    ) : null;
  },

  renderControlNav: function renderControlNav() {
    if (this.props.controlNav) {
      var isThumbnailNav = false;
      var children = React.Children.map(this.props.children, function (child, i) {
        var className = i === this.getActiveIndex() ? this.setClassNamespace('active') : null;

        if (!isThumbnailNav) {
          isThumbnailNav = !!child.props.thumbnail;
        }

        var thumb = child.props.thumbnail;

        return React.createElement(
          'li',
          {
            onClick: this.handleSelect.bind(this, i, null),
            key: i
          },
          thumb ? React.createElement('img', { className: className, src: thumb }) : React.createElement('a', { href: '#' + i, className: className }),
          React.createElement('i', null)
        );
      }.bind(this));
      var controlClass = this.setClassNamespace('control-' + (isThumbnailNav ? 'thumbs' : 'paging'));

      return React.createElement(
        'ol',
        {
          className: classNames(this.setClassNamespace('control-nav'), controlClass) },
        children
      );
    }

    return null;
  },

  renderItem: function renderItem(child, index) {
    var activeIndex = this.getActiveIndex();
    var isActive = index === activeIndex;
    var isPreviousActive = this.state.previousActiveIndex != null && this.state.previousActiveIndex === index && this.props.slide;

    return React.cloneElement(child, {
      active: isActive,
      ref: child.ref,
      key: child.key ? child.key : index,
      index: index,
      animateOut: isPreviousActive,
      animateIn: isActive && this.state.previousActiveIndex != null && this.props.slide,
      direction: this.state.direction,
      onAnimateOutEnd: isPreviousActive ? this.handleItemAnimateOutEnd : null
    });
  },

  render: function render() {
    var classSet = this.getClassSet();
    var viewportStyle = {
      overflow: 'hidden',
      position: 'relative',
      width: '100%'
    };
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    // React version slider style
    classSet[this.prefixClass('slide')] = true;

    return React.createElement(
      'div',
      _extends({}, restProps, {
        className: classNames(classSet, this.props.className),
        onMouseOver: this.handleMouseOver,
        onMouseOut: this.handleMouseOut
      }),
      React.createElement(
        'div',
        {
          className: this.setClassNamespace('viewport'),
          style: viewportStyle
        },
        React.createElement(
          'ul',
          { className: this.setClassNamespace('slides') },
          React.Children.map(this.props.children, this.renderItem)
        )
      ),
      this.renderDirectionNav(),
      this.renderControlNav()
    );
  }
});

Slider.Item = React.createClass({
  displayName: 'Item',

  propTypes: {
    direction: React.PropTypes.oneOf(['prev', 'next']),
    onAnimateOutEnd: React.PropTypes.func,
    active: React.PropTypes.bool,
    animateIn: React.PropTypes.bool,
    animateOut: React.PropTypes.bool,
    caption: React.PropTypes.node,
    index: React.PropTypes.number,
    thumbnail: React.PropTypes.string
  },

  getInitialState: function getInitialState() {
    return {
      direction: null
    };
  },

  getDefaultProps: function getDefaultProps() {
    return {
      animation: true
    };
  },

  componentWillReceiveProps: function componentWillReceiveProps(nextProps) {
    if (this.props.active !== nextProps.active) {
      this.setState({
        direction: null
      });
    }
  },

  componentDidUpdate: function componentDidUpdate(prevProps) {
    if (!this.props.active && prevProps.active) {
      TransitionEvents.on(ReactDOM.findDOMNode(this), this.handleAnimateOutEnd);
    }

    if (this.props.active !== prevProps.active) {
      setTimeout(this.startAnimation, 20);
    }
  },

  handleAnimateOutEnd: function handleAnimateOutEnd() {
    if (this.props.onAnimateOutEnd && this.isMounted()) {
      this.props.onAnimateOutEnd(this.props.index);
    }
  },

  startAnimation: function startAnimation() {
    if (!this.isMounted()) {
      return;
    }

    this.setState({
      direction: this.props.direction === 'prev' ? 'right' : 'left'
    });
  },

  render: function render() {
    var classSet = {
      active: this.props.active && !this.props.animateIn || this.props.animateOut,
      next: this.props.active && this.props.animateIn && this.props.direction === 'next',
      prev: this.props.active && this.props.animateIn && this.props.direction === 'prev'
    };

    if (this.state.direction && (this.props.animateIn || this.props.animateOut)) {
      classSet[this.state.direction] = true;
    }

    return React.createElement(
      'li',
      {
        className: classNames(this.props.className, classSet)
      },
      this.props.children
    );
  }
});

module.exports = Slider;
},{"./mixins/ClassNameMixin":114,"./utils/TransitionEvents":122,"classnames":132,"object.omit":186,"react":356,"react-dom":189}],105:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol ? "symbol" : typeof obj; };

var React = require('react');
var ReactDOM = require('react-dom');
var assign = require('object-assign');
var omit = require('object.omit');
var classNames = require('classnames');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var Events = require('./utils/Events');
var debounce = require('./utils/debounce');
var domUtils = require('./utils/domUtils');
var canUseDOM = require('./utils/canUseDOM');

var Sticky = React.createClass({
  displayName: 'Sticky',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    media: React.PropTypes.oneOfType([React.PropTypes.string, React.PropTypes.number]),
    top: React.PropTypes.number,
    animation: React.PropTypes.string,
    bottom: React.PropTypes.oneOfType([React.PropTypes.number, React.PropTypes.func])
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'sticky',
      top: 0
    };
  },

  getInitialState: function getInitialState() {
    return {
      sticked: false,
      holderStyle: null,
      initialized: false,
      stickerStyle: null
    };
  },

  componentDidMount: function componentDidMount() {
    if (canUseDOM) {
      this._init();
      this.checkPosition();
      var ownerWindow = domUtils.ownerWindow(ReactDOM.findDOMNode(this.refs.sticker));

      this._scrollListener = Events.on(ownerWindow, 'scroll', debounce(this.checkPosition, 10).bind(this));
      this._resizeListener = Events.on(ownerWindow, 'resize', debounce(this.checkPosition, 50).bind(this));
    }
  },

  componentWillUnmount: function componentWillUnmount() {
    this._scrollListener && this._scrollListener.off();
    this._resizeListener && this._resizeListener.off();
  },

  _init: function _init() {
    if (this.state.initialized || !this.isMounted() || !this.checkMedia()) {
      return;
    }

    var sticker = ReactDOM.findDOMNode(this.refs.sticker);
    var elStyle = getComputedStyle(sticker);
    var outerHeight = parseInt(elStyle.height, 10) + parseInt(elStyle.marginTop, 10) + parseInt(elStyle.marginBottom, 10);
    var style = {
      height: elStyle.position !== 'absolute' ? outerHeight : '',
      float: elStyle.float !== 'none' ? elStyle.float : '',
      margin: elStyle.margin
    };

    this.setState({
      initialized: true,
      holderStyle: style,
      stickerStyle: {
        margin: 0
      }
    });
  },

  checkPosition: function checkPosition() {
    if (this.isMounted) {
      var scrollTop = domUtils.scrollTop(window);
      var offsetTop = this.props.top;
      var offsetBottom = this.props.bottom;
      var holder = ReactDOM.findDOMNode(this);

      if (typeof offsetBottom === 'function') {
        offsetBottom = offsetBottom();
      }

      var checkResult = scrollTop > domUtils.offset(holder).top;

      if (checkResult && !this.state.sticked) {
        this.setState({
          stickerStyle: {
            top: offsetTop,
            left: domUtils.offset(holder).left,
            width: holder.offsetWidth
          }
        });
      }

      if (this.state.sticked && !checkResult) {
        this.resetSticker();
      }

      this.setState({
        sticked: checkResult
      });
    }
  },

  checkMedia: function checkMedia() {
    // TODO: add element visible detector
    /*if (!this.$element.is(':visible')) {
     return false;
     }*/

    var media = this.props.media;

    if (media) {
      switch (typeof media === 'undefined' ? 'undefined' : _typeof(media)) {
        case 'number':
          if (window.innerWidth < media) {
            return false;
          }
          break;

        case 'string':
          if (window.matchMedia && !window.matchMedia(media).matches) {
            return false;
          }
          break;
      }
    }

    return true;
  },

  resetSticker: function resetSticker() {
    this.setState({
      stickerStyle: {
        position: '',
        top: '',
        width: '',
        left: '',
        margin: 0
      }
    });
  },

  // Smooth scroll
  handleClick: function handleClick(e) {
    e.preventDefault();

    if (e.target && e.target.nodeName === 'A') {
      var targetNode = document.getElementById(e.target.getAttribute('href').substr(1));

      targetNode && this.smoothScroll(window, {
        position: domUtils.offset(targetNode).top - this.props.offsetTop || 0
      });
    }
  },

  render: function render() {
    var stickyClass = this.getClassSet();
    var child = React.Children.only(this.props.children);
    var animation = this.props.animation && this.state.sticked ? this.setClassNamespace('animation-' + this.props.animation) : null;
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    // transfer child's props to cloned element
    return React.createElement(
      'div',
      _extends({}, restProps, {
        style: this.state.holderStyle,
        className: classNames(this.props.className, this.prefixClass('placeholder'))
      }),
      React.cloneElement(child, assign({}, child.props, {
        style: this.state.stickerStyle,
        ref: 'sticker',
        className: classNames(child.props.className, this.state.sticked ? stickyClass : null, animation)
      }))
    );
  }
});

module.exports = Sticky;
},{"./mixins/ClassNameMixin":114,"./utils/Events":121,"./utils/canUseDOM":123,"./utils/debounce":126,"./utils/domUtils":127,"classnames":132,"object-assign":185,"object.omit":186,"react":356,"react-dom":189}],106:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');

var Table = React.createClass({
  displayName: 'Table',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string.isRequired,
    bordered: React.PropTypes.bool,
    compact: React.PropTypes.bool,
    hover: React.PropTypes.bool,
    striped: React.PropTypes.bool,
    radius: React.PropTypes.bool,
    responsive: React.PropTypes.bool
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'table'
    };
  },

  render: function render() {
    var classSet = this.getClassSet();
    var responsive = this.props.responsive;
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    classSet[this.prefixClass('bordered')] = this.props.bordered;
    classSet[this.prefixClass('compact')] = this.props.compact;
    classSet[this.prefixClass('hover')] = this.props.hover;
    classSet[this.prefixClass('striped')] = this.props.striped;
    classSet[this.prefixClass('radius')] = this.props.radius;

    // add `.am-text-nowrap` to responsive table
    classSet[this.setClassNamespace('text-nowrap')] = responsive;

    var table = React.createElement(
      'table',
      _extends({}, restProps, {
        className: classNames(this.props.className, classSet)
      }),
      this.props.children
    );

    return responsive ? React.createElement(
      'div',
      { className: this.setClassNamespace('scrollable-horizontal') },
      table
    ) : table;
  }
});

module.exports = Table;
},{"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],107:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var Nav = require('./Nav');
var NavItem = require('./NavItem');

var Tabs = React.createClass({
  displayName: 'Tabs',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    theme: React.PropTypes.oneOf(['default', 'd2']),
    onSelect: React.PropTypes.func,
    animation: React.PropTypes.oneOf(['slide', 'fade']),
    defaultActiveKey: React.PropTypes.any,
    justify: React.PropTypes.bool,
    data: React.PropTypes.array
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'tabs',
      animation: 'fade'
    };
  },

  getInitialState: function getInitialState() {
    var defaultActiveKey = this.props.defaultActiveKey != null ? this.props.defaultActiveKey : this.getDefaultActiveKey(this.props.children);

    return {
      activeKey: defaultActiveKey,
      previousActiveKey: null
    };
  },

  componentWillReceiveProps: function componentWillReceiveProps(nextProps) {
    if (nextProps.activeKey != null && nextProps.activeKey !== this.props.activeKey) {
      this.setState({
        previousActiveKey: this.props.activeKey
      });
    }
  },

  getDefaultActiveKey: function getDefaultActiveKey(children) {
    var defaultActiveKey = null;

    if (this.props.data) {
      this.props.data.every(function (item, i) {
        if (item.active) {
          defaultActiveKey = i;
          return false;
        }

        return true;
      });

      return defaultActiveKey == null ? 0 : defaultActiveKey;
    }

    React.Children.forEach(children, function (child) {
      if (defaultActiveKey == null) {
        defaultActiveKey = child.props.eventKey;
      }
    });

    return defaultActiveKey;
  },

  handleClick: function handleClick(key, disabled, e) {
    e.preventDefault();
    var activeKey = this.state.activeKey;

    if (disabled) {
      return null;
    }

    if (this.props.onSelect) {
      this.props.onSelect(key);
    }

    if (activeKey !== key) {
      this.setState({
        activeKey: key,
        previousActiveKey: activeKey
      });
    }
  },

  renderNav: function renderNav() {
    var activeKey = this.state.activeKey;

    return React.Children.map(this.props.children, function (child, index) {
      var key = child.props.eventKey || index;
      var disabled = child.props.disabled;

      return React.createElement(
        NavItem,
        {
          href: '#',
          ref: 'ref' + key,
          key: key,
          onClick: this.handleClick.bind(this, key, disabled),
          active: child.props.eventKey === activeKey,
          disabled: disabled
        },
        child.props.title
      );
    }.bind(this));
  },

  renderTabPanels: function renderTabPanels() {
    var activeKey = this.state.activeKey;

    return React.Children.map(this.props.children, function (child, index) {
      return React.createElement(
        Tabs.Item,
        {
          active: child.props.eventKey === activeKey,
          key: index
        },
        child.props.children
      );
    });
  },

  // for Amaze UI tabs widget
  renderData: function renderData() {
    var activeKey = this.state.activeKey;
    var navs = [];
    var panels = [];

    this.props.data.forEach(function (item, key) {
      navs.push(React.createElement(
        NavItem,
        {
          href: '#',
          ref: 'ref' + key,
          key: key,
          onClick: this.handleClick.bind(this, key, item.disabled),
          active: key === activeKey,
          disabled: item.disabled
        },
        item.title
      ));

      panels.push(React.createElement(
        Tabs.Item,
        {
          eventKey: key
          // active={item.active}
          , active: key === activeKey,
          key: key },
        item.content
      ));
    }.bind(this));

    return {
      navs: navs,
      panels: panels
    };
  },

  renderWrapper: function renderWrapper(children) {
    var classSet = this.getClassSet();
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    return React.createElement(
      'div',
      _extends({}, restProps, {
        'data-am-widget': this.props.theme ? this.props.classPrefix : null,
        className: classNames(classSet, this.props.className)
      }),
      children
    );
  },

  renderNavWrapper: function renderNavWrapper(children) {
    var TabsNav = this.props.theme ? 'ul' : Nav;
    var props = {
      key: "tabsNav",
      tabs: true,
      className: classNames(this.prefixClass('nav'), this.setClassNamespace('cf')),
      justify: this.props.justify
    };
    if (TabsNav === 'ul') {
      delete props.tabs;
      delete props.justify;
    }

    return React.createElement(
      TabsNav,
      props,
      children
    );
  },

  renderBodyWrapper: function renderBodyWrapper(children) {
    var animationClass = this.prefixClass(this.props.animation);

    return React.createElement(
      'div',
      {
        key: 'tabsBody',
        className: classNames(this.prefixClass('bd'), animationClass)
      },
      children
    );
  },

  render: function render() {
    var children = this.props.data ? this.renderData() : {};

    return this.renderWrapper([this.renderNavWrapper(children.navs || this.renderNav()), this.renderBodyWrapper(children.panels || this.renderTabPanels())]);
  }
});

Tabs.Item = React.createClass({
  displayName: 'Item',

  mixins: [ClassNameMixin],

  propTypes: {
    title: React.PropTypes.string,
    disabled: React.PropTypes.bool,
    eventKey: React.PropTypes.any,
    active: React.PropTypes.bool
  },

  render: function render() {
    var classSet = {};

    classSet[this.setClassNamespace('tab-panel')] = true;
    classSet[this.setClassNamespace('fade')] = true;
    classSet[this.setClassNamespace('active')] = this.props.active;
    classSet[this.setClassNamespace('in')] = this.props.active;

    return React.createElement(
      'div',
      { className: classNames(classSet) },
      this.props.children
    );
  }
});

module.exports = Tabs;
},{"./Nav":92,"./NavItem":93,"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],108:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var omit = require('object.omit');

var Thumbnail = React.createClass({
  displayName: 'Thumbnail',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    standalone: React.PropTypes.bool,
    caption: React.PropTypes.node,
    component: React.PropTypes.node
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'thumbnail',
      component: 'figure'
    };
  },

  renderImg: function renderImg(classes, props) {
    props = props || {};

    return props.src ? React.createElement('img', _extends({}, props, {
      className: classes
    })) : null;
  },

  render: function render() {
    var classes = classNames(this.getClassSet(), this.props.className);
    var restProps = omit(this.props, ['classPrefix', 'standalone', 'caption', 'component']);

    if (this.props.standalone) {
      return this.renderImg(classes, restProps);
    }

    var Component = this.props.href ? 'a' : this.props.component;
    var imgProps = {
      alt: this.props.alt,
      src: this.props.src,
      width: this.props.width,
      height: this.props.height
    };
    var props = omit(restProps, ['alt', 'src', 'width', 'height']);
    var caption = this.props.caption;

    return React.createElement(
      Component,
      _extends({}, props, {
        className: classes
      }),
      this.renderImg(null, imgProps),
      caption || this.props.children ? React.createElement(
        Thumbnail.Caption,
        {
          component: typeof caption === 'string' ? 'figcaption' : 'div'
        },
        this.props.caption || this.props.children
      ) : null
    );
  }
});

Thumbnail.Caption = React.createClass({
  displayName: 'Caption',

  mixins: [ClassNameMixin],

  propTypes: {
    component: React.PropTypes.node
  },

  getDefaultProps: function getDefaultProps() {
    return {
      component: 'div'
    };
  },

  render: function render() {
    var Component = this.props.component;
    var classes = classNames(this.props.className, this.setClassNamespace('thumbnail-caption'));
    var restProps = omit(this.props, ['component']);

    return React.createElement(
      Component,
      _extends({}, restProps, {
        className: classes
      }),
      this.props.children
    );
  }
});

module.exports = Thumbnail;
},{"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],109:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var AvgGrid = require('./AvgGrid');
var omit = require('object.omit');

var Thumbnails = React.createClass({
  displayName: 'Thumbnails',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'thumbnails'
    };
  },

  render: function render() {
    var classes = classNames(this.getClassSet(), this.props.className);
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    return React.createElement(
      AvgGrid,
      _extends({}, restProps, {
        className: classes
      }),
      React.Children.map(this.props.children, function (child, i) {
        return React.createElement(
          'li',
          { key: i },
          child
        );
      })
    );
  }
});

module.exports = Thumbnails;
},{"./AvgGrid":56,"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],110:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var AvgGrid = require('./AvgGrid');
var omit = require('object.omit');

var Titlebar = React.createClass({
  displayName: 'Titlebar',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    theme: React.PropTypes.oneOf(['default', 'multi', 'cols']),
    nav: React.PropTypes.array,
    title: React.PropTypes.node
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'titlebar',
      theme: 'default',
      data: []
    };
  },

  render: function render() {
    var classSet = this.getClassSet();
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    return React.createElement(
      'div',
      _extends({}, restProps, {
        'data-am-widget': this.props.classPrefix,
        className: classNames(this.props.className, classSet)
      }),
      React.createElement(
        'h2',
        { className: this.prefixClass('title') },
        this.props.href ? React.createElement(
          'a',
          { href: this.props.href },
          this.props.title
        ) : this.props.title
      ),
      this.props.nav ? React.createElement(
        'nav',
        { className: this.prefixClass('nav') },
        this.props.nav.map(function (item, i) {
          return React.createElement(
            'a',
            { href: item.link, key: i },
            item.title
          );
        })
      ) : null
    );
  }
});

module.exports = Titlebar;
},{"./AvgGrid":56,"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],111:[function(require,module,exports){
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var assign = require('object-assign');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var createChainedFunction = require('./utils/createChainedFunction');
var Icon = require('./Icon');
var Button = require('./Button');

var Topbar = React.createClass({
  displayName: 'Topbar',

  mixins: [ClassNameMixin],

  propTypes: {
    classPrefix: React.PropTypes.string,
    component: React.PropTypes.node,
    brand: React.PropTypes.node,
    brandLink: React.PropTypes.string,
    inverse: React.PropTypes.bool,
    fixedTop: React.PropTypes.bool,
    fixedBottom: React.PropTypes.bool,
    toggleBtn: React.PropTypes.node,
    toggleNavKey: React.PropTypes.oneOfType([React.PropTypes.string, React.PropTypes.number]),
    onToggle: React.PropTypes.func,
    navExpanded: React.PropTypes.bool,
    defaultNavExpanded: React.PropTypes.bool,
    fluid: React.PropTypes.bool
  },

  getDefaultProps: function getDefaultProps() {
    return {
      classPrefix: 'topbar',
      component: 'header'
    };
  },

  getInitialState: function getInitialState() {
    return {
      navExpanded: this.props.defaultNavExpanded
    };
  },

  shouldComponentUpdate: function shouldComponentUpdate() {
    // Defer any updates to this component during the `onSelect` handler.
    return !this._isChanging;
  },

  handleToggle: function handleToggle() {
    if (this.props.onToggle) {
      this._isChanging = true;
      this.props.onToggle();
      this._isChanging = false;
    }

    this.setState({
      navExpanded: !this.state.navExpanded
    });
  },

  isNavExpanded: function isNavExpanded() {
    return this.props.navExpanded != null ? this.props.navExpanded : this.state.navExpanded;
  },

  renderBrand: function renderBrand() {
    var brand = this.props.brand;
    var brandClassName = this.prefixClass('brand');

    if (React.isValidElement(brand)) {
      return React.cloneElement(brand, assign({}, brand.props, {
        className: classNames(brand.props.className, brandClassName),
        onClick: createChainedFunction(this.handleToggle, brand.props.onClick)
      }));
    }

    return brand ? React.createElement(
      'h1',
      { className: brandClassName },
      this.props.brandLink ? React.createElement(
        'a',
        { href: this.props.brandLink },
        this.props.brand
      ) : this.props.brand
    ) : null;
  },

  renderToggleButton: function renderToggleButton() {
    var toggleBtn = this.props.toggleBtn;
    var toggleBtnClassName = this.prefixClass('toggle');

    if (React.isValidElement(toggleBtn)) {
      return React.cloneElement(toggleBtn, assign({}, toggleBtn.props, {
        className: classNames(toggleBtn.props.className, toggleBtnClassName),
        onClick: createChainedFunction(this.handleToggle, toggleBtn.props.onClick)
      }));
    }

    return React.createElement(
      Button,
      {
        amSize: 'sm',
        onClick: this.handleToggle,
        className: classNames(this.prefixClass('btn'), this.prefixClass('toggle'), this.setClassNamespace('show-sm-only'))
      },
      React.createElement(
        'span',
        { className: this.setClassNamespace('sr-only') },
        '导航切换'
      ),
      React.createElement(Icon, { icon: 'bars' })
    );
  },

  renderChild: function renderChild(child, i) {
    return React.cloneElement(child, assign({}, child.props, {
      topbar: true,
      collapsible: this.props.toggleNavKey != null && this.props.toggleNavKey === child.props.eventKey,
      expanded: this.props.toggleNavKey != null && this.props.toggleNavKey === child.props.eventKey && this.isNavExpanded(),
      key: child.key ? child.key : i,
      className: classNames(child.props.className, child.props.right ? this.prefixClass('right') : null)
    }));
  },

  render: function render() {
    var classes = this.getClassSet();
    var Component = this.props.component;
    var restProps = omit(this.props, Object.keys(this.constructor.propTypes));

    // set classes
    classes[this.prefixClass('inverse')] = this.props.inverse;
    classes[this.prefixClass('fixed-top')] = this.props.fixedTop;
    classes[this.prefixClass('fixed-bottom')] = this.props.fixedBottom;

    return React.createElement(
      Component,
      _extends({}, restProps, {
        className: classNames(classes, this.props.className)
      }),
      React.createElement(
        'div',
        {
          className: !this.props.fluid ? this.setClassNamespace('container') : null
        },
        this.renderBrand(),
        this.renderToggleButton(),
        React.Children.map(this.props.children, this.renderChild)
      )
    );
  }
});

module.exports = Topbar;
},{"./Button":59,"./Icon":82,"./mixins/ClassNameMixin":114,"./utils/createChainedFunction":124,"classnames":132,"object-assign":185,"object.omit":186,"react":356}],112:[function(require,module,exports){
'use strict';

/**
 * Custom radio/checkbox style
 */

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var classNames = require('classnames');
var omit = require('object.omit');
var ClassNameMixin = require('./mixins/ClassNameMixin');
var Input = require('./Input');
var Icon = require('./Icon');
var constants = require('./constants');

var UCheck = React.createClass({
  displayName: 'UCheck',

  mixins: [ClassNameMixin],

  propTypes: {
    type: React.PropTypes.oneOf(['radio', 'checkbox']),
    disabled: React.PropTypes.bool,
    amStyle: React.PropTypes.oneOf(['secondary', 'success', 'warning', 'danger']),
    inline: React.PropTypes.bool,
    hasFeedback: React.PropTypes.bool
  },

  getDefaultProps: function getDefaultProps() {
    return {
      type: 'checkbox'
    };
  },

  render: function render() {
    var classSet = {};
    var restProps = omit(this.props, ['inline', 'amStyle', 'hasFeedback']);

    classSet[this.setClassNamespace(this.props.type)] = !this.props.inline;
    classSet[this.setClassNamespace(this.props.type + '-inline')] = this.props.inline;

    if (this.props.amStyle) {
      classSet[this.setClassNamespace(this.props.amStyle)] = true;
    }

    return React.createElement(
      'label',
      { className: classNames(this.props.className, classSet) },
      React.createElement(Input, _extends({}, restProps, {
        ref: 'field',
        className: this.setClassNamespace('ucheck-checkbox'),
        standalone: true
      })),
      React.createElement(
        'span',
        { className: this.setClassNamespace('ucheck-icons') },
        React.createElement(Icon, { icon: 'unchecked' }),
        React.createElement(Icon, { icon: 'checked' })
      ),
      this.props.label
    );
  }
});

module.exports = UCheck;
},{"./Icon":82,"./Input":84,"./constants":113,"./mixins/ClassNameMixin":114,"classnames":132,"object.omit":186,"react":356}],113:[function(require,module,exports){
'use strict';

var NAMESPACE = 'am';
var setNamespace = function setNamespace(className) {
  return (NAMESPACE ? NAMESPACE + '-' : '') + className;
};

module.exports = {
  NAMESPACE: NAMESPACE,

  CLASSES: {
    active: setNamespace('active'),
    disabled: setNamespace('disabled'),
    round: setNamespace('round'),
    radius: setNamespace('radius'),
    square: setNamespace('square'),
    circle: setNamespace('circle'),
    divider: setNamespace('divider'),
    cf: setNamespace('cf'),
    fl: setNamespace('fl'),
    fr: setNamespace('fr')
  },

  STYLES: {
    default: 'default',
    primary: 'primary',
    secondary: 'secondary',
    success: 'success',
    warning: 'warning',
    danger: 'danger'
  },

  SIZES: {}
};
},{}],114:[function(require,module,exports){
'use strict';

var React = require('react');
var constants = require('../constants');
var nsPrefix = constants.NAMESPACE ? constants.NAMESPACE + '-' : '';

module.exports = {
  getClassSet: function getClassSet(ignorePrefix) {
    var classNames = {};
    // uses `.am-` as prefix if `classPrefix` is not defined
    var prefix = nsPrefix;

    if (this.props.classPrefix) {
      var classPrefix = this.setClassNamespace();

      prefix = classPrefix + '-';

      // don't return prefix if flag set
      !ignorePrefix && (classNames[classPrefix] = true);
    }

    var amSize = this.props.amSize;
    var amStyle = this.props.amStyle;

    if (amSize) {
      classNames[prefix + amSize] = true;
    }

    if (amStyle) {
      classNames[prefix + amStyle] = true;
    }

    // add theme className for widgets
    if (this.props.theme) {
      classNames[prefix + this.props.theme] = true;
    }

    // states
    classNames[constants.CLASSES.active] = this.props.active;
    classNames[constants.CLASSES.disabled] = this.props.disabled;

    // shape
    classNames[constants.CLASSES.radius] = this.props.radius;
    classNames[constants.CLASSES.round] = this.props.round;

    // clearfix
    classNames[constants.CLASSES.cf] = this.props.cf;

    // am-divider
    if (this.props.classPrefix !== 'divider') {
      classNames[constants.CLASSES.divider] = this.props.divider;
    }

    return classNames;
  },

  // add namespace to classPrefix
  setClassNamespace: function setClassNamespace(classPrefix) {
    var prefix = classPrefix || this.props.classPrefix || '';

    return nsPrefix + prefix;
  },

  prefixClass: function prefixClass(subClass) {
    return this.setClassNamespace() + '-' + subClass;
  }
};
},{"../constants":113,"react":356}],115:[function(require,module,exports){
/**
 * modified version of:
 * https://github.com/react-bootstrap/react-bootstrap/blob/master/src/CollapsibleMixin.js
 */

'use strict';

var React = require('react');
var TransitionEvents = require('../utils/TransitionEvents');

var CollapseMixin = {
  propTypes: {
    defaultExpanded: React.PropTypes.bool,
    expanded: React.PropTypes.bool
  },

  getInitialState: function getInitialState() {
    var defaultExpanded = this.props.defaultExpanded != null ? this.props.defaultExpanded : this.props.expanded != null ? this.props.expanded : false;

    return {
      expanded: defaultExpanded,
      collapsing: false
    };
  },

  componentWillUpdate: function componentWillUpdate(nextProps, nextState) {
    var willExpanded = nextProps.expanded != null ? nextProps.expanded : nextState.expanded;

    if (willExpanded === this.isExpanded()) {
      return;
    }

    // if the expanded state is being toggled, ensure node has a dimension value
    // this is needed for the animation to work and needs to be set before
    // the collapsing class is applied (after collapsing is applied the in class
    // is removed and the node's dimension will be wrong)

    var node = this.getCollapsibleDOMNode();
    var dimension = this.dimension();
    var value = '0';

    if (!willExpanded) {
      // get height
      value = this.getCollapsibleDimensionValue();
    }

    node.style[dimension] = value + 'px';

    this._afterWillUpdate();
  },

  componentDidUpdate: function componentDidUpdate(prevProps, prevState) {
    // check if expanded is being toggled; if so, set collapsing
    this._checkToggleCollapsing(prevProps, prevState);

    // check if collapsing was turned on; if so, start animation
    this._checkStartAnimation();
  },

  // helps enable test stubs
  _afterWillUpdate: function _afterWillUpdate() {},

  _checkStartAnimation: function _checkStartAnimation() {
    if (!this.state.collapsing) {
      return;
    }

    var node = this.getCollapsibleDOMNode();
    var dimension = this.dimension();
    var value = this.getCollapsibleDimensionValue();

    // setting the dimension here starts the transition animation
    var result;

    if (this.isExpanded()) {
      result = value + 'px';
    } else {
      result = '0px';
    }
    node.style[dimension] = result;
  },

  _checkToggleCollapsing: function _checkToggleCollapsing(prevProps, prevState) {
    var wasExpanded = prevProps.expanded != null ? prevProps.expanded : prevState.expanded;
    var isExpanded = this.isExpanded();

    if (wasExpanded !== isExpanded) {
      if (wasExpanded) {
        this._handleCollapse();
      } else {
        this._handleExpand();
      }
    }
  },

  _handleExpand: function _handleExpand() {
    var node = this.getCollapsibleDOMNode();
    var dimension = this.dimension();

    var complete = function () {
      this._removeEndEventListener(node, complete);
      // remove dimension value - this ensures the collapsible item can grow
      // in dimension after initial display (such as an image loading)
      node.style[dimension] = '';
      this.setState({
        collapsing: false
      });
    }.bind(this);

    this._addEndEventListener(node, complete);

    this.setState({
      collapsing: true
    });
  },

  _handleCollapse: function _handleCollapse() {
    var node = this.getCollapsibleDOMNode();
    var _this = this;
    var complete = function complete() {
      _this._removeEndEventListener(node, complete);
      _this.setState({
        collapsing: false
      });
    };

    this._addEndEventListener(node, complete);

    this.setState({
      collapsing: true
    });
  },

  // helps enable test stubs
  _addEndEventListener: function _addEndEventListener(node, complete) {
    TransitionEvents.on(node, complete);
  },

  // helps enable test stubs
  _removeEndEventListener: function _removeEndEventListener(node, complete) {
    TransitionEvents.off(node, complete);
  },

  dimension: function dimension() {
    return typeof this.getCollapsibleDimension === 'function' ? this.getCollapsibleDimension() : 'height';
  },

  isExpanded: function isExpanded() {
    return this.props.expanded != null ? this.props.expanded : this.state.expanded;
  },

  getCollapsibleClassSet: function getCollapsibleClassSet(className) {
    var classSet = {};

    if (typeof className === 'string') {
      className.split(' ').forEach(function (subClass) {
        if (subClass) {
          classSet[subClass] = true;
        }
      });
    }

    classSet[this.setClassNamespace('collapsing')] = this.state.collapsing;
    classSet[this.setClassNamespace('collapse')] = !this.state.collapsing;
    classSet[this.setClassNamespace('in')] = this.isExpanded() && !this.state.collapsing;

    return classSet;
  }
};

module.exports = CollapseMixin;
},{"../utils/TransitionEvents":122,"react":356}],116:[function(require,module,exports){
'use strict';

var React = require('react');
var ReactDOM = require('react-dom');
var classNames = require('classnames');
var getScrollbarWidth = require('../utils/getScrollbarWidth');
var ownerDocument = require('../utils/domUtils').ownerDocument;
var CSSCore = require('../utils/CSSCore');

module.exports = {
  propTypes: {
    container: React.PropTypes.node
  },

  _getContainer: function _getContainer() {
    var node = this.refs.modal;
    var doc = ownerDocument(node);

    return this.props.container && ReactDOM.findDOMNode(this.props.container) || doc.body;
  },

  _getDimmerActiveClassName: function _getDimmerActiveClassName() {
    return this.setClassNamespace('dimmer-active');
  },

  setDimmerContainer: function setDimmerContainer() {
    var container = this._getContainer();
    var bodyPaddingRight = parseInt(container.style.paddingRight || 0, 10);
    var barWidth = getScrollbarWidth();

    if (barWidth) {
      container.style.paddingRight = bodyPaddingRight + barWidth + 'px';
    }

    CSSCore.addClass(container, this._getDimmerActiveClassName());
  },

  resetDimmerContainer: function resetDimmerContainer(nextProps, nextState) {
    var container = this._getContainer();

    CSSCore.removeClass(container, this._getDimmerActiveClassName());

    container.style.paddingRight = '';
  },

  renderDimmer: function renderDimmer(children) {
    var onClick = this.handleDimmerClick || null;
    var classSet = {};

    classSet[this.setClassNamespace('dimmer')] = true;
    classSet[this.setClassNamespace('active')] = true;

    return React.createElement(
      'div',
      null,
      React.createElement('div', {
        onClick: onClick,
        ref: 'dimmer',
        style: { display: 'block' },
        className: classNames(classSet)
      }),
      children
    );
  }
};
},{"../utils/CSSCore":120,"../utils/domUtils":127,"../utils/getScrollbarWidth":128,"classnames":132,"react":356,"react-dom":189}],117:[function(require,module,exports){
'use strict';

var React = require('react');
var ReactDOM = require('react-dom');

/**
 * Overlay Mixin
 *
 * @desc `overlay` is something like Popover, Modal, etc.
 * */

module.exports = {
  propTypes: {
    container: React.PropTypes.node
  },

  componentDidMount: function componentDidMount() {
    this._renderOverlay();
  },

  componentDidUpdate: function componentDidUpdate() {
    this._renderOverlay();
  },

  // Remove Overlay related DOM node
  componentWillUnmount: function componentWillUnmount() {
    this._unmountOverlay();

    if (this._overlayWrapper) {
      this.getContainerDOMNode().removeChild(this._overlayWrapper);
      this._overlayWrapper = null;
    }
  },

  // Create Overlay wrapper
  _mountOverlayWrapper: function _mountOverlayWrapper() {
    this._overlayWrapper = document.createElement('div');
    this.getContainerDOMNode().appendChild(this._overlayWrapper);
  },

  // Render Overlay to wrapper
  _renderOverlay: function _renderOverlay() {
    if (!this._overlayWrapper) {
      this._mountOverlayWrapper();
    }

    var overlay = this.renderOverlay();

    if (overlay !== null) {
      this._overlayInstance = ReactDOM.render(overlay, this._overlayWrapper);
    } else {
      // Unmount if the component is null for transitions to null
      this._unmountOverlay();
    }
  },

  // Remove a mounted Overlay from wrapper
  _unmountOverlay: function _unmountOverlay() {
    ReactDOM.unmountComponentAtNode(this._overlayWrapper);
    this._overlayInstance = null;
  },

  getOverlayDOMNode: function getOverlayDOMNode() {
    if (!this.isMounted()) {
      throw new Error('getOverlayDOMNode(): A component must be mounted to' + ' have a DOM node.');
    }

    if (this._overlayInstance) {
      return ReactDOM.findDOMNode(this._overlayInstance);
    }

    return null;
  },

  getContainerDOMNode: function getContainerDOMNode() {
    return ReactDOM.findDOMNode(this.props.container) || document.body;
  }
};
},{"react":356,"react-dom":189}],118:[function(require,module,exports){
/**
 * modified version of:
 * http://mir.aculo.us/2014/01/19/scrolling-dom-elements-to-the-top-a-zepto-plugin/
 */

'use strict';

var React = require('react');
var Events = require('../utils/Events');
var dom = require('../utils/domUtils');
var rAF = require('../utils/requestAnimationFrame');
var scrollInProgress = false;

var SmoothScrollMixin = {
  smoothScroll: function smoothScroll(element, options) {
    options = options || {};
    var scrollTarget = element || window;
    var targetY = options.position && parseInt(options.position, 10) || 0;
    var initialY = dom.scrollTop(scrollTarget);
    var lastY = initialY;
    var delta = targetY - initialY;
    // duration in ms, make it a bit shorter for short distances
    // this is not scientific and you might want to adjust this for
    // your preferences
    var speed = options.speed || Math.min(750, Math.min(1500, Math.abs(initialY - targetY)));
    // temp variables (t will be a position between 0 and 1, y is the calculated scrollTop)
    var start;
    var t;
    var y;
    var cancelScroll = function cancelScroll() {
      abort();
    };

    // abort if already in progress or nothing to scroll
    if (scrollInProgress) {
      // console.log(scrollInProgress);
      return;
    }

    if (delta === 0) {
      return;
    }

    // quint ease-in-out smoothing, from
    // https://github.com/madrobby/scripty2/blob/master/src/effects/transitions/penner.js#L127-L136
    function smooth(pos) {
      if ((pos /= 0.5) < 1) {
        return 0.5 * Math.pow(pos, 5);
      }

      return 0.5 * (Math.pow(pos - 2, 5) + 2);
    }

    function abort() {
      Events.off(scrollTarget, 'touchstart', cancelScroll);
      scrollInProgress = false;
    }

    // when there's a touch detected while scrolling is in progress, abort
    // the scrolling (emulates native scrolling behavior)
    Events.on(scrollTarget, 'touchstart', cancelScroll);
    scrollInProgress = true;

    // start rendering away! note the function given to frame
    // is named "render" so we can reference it again further down
    rAF(function render(now) {
      if (!scrollInProgress) {
        return;
      }

      if (!start) {
        start = now;
      }

      // calculate t, position of animation in [0..1]
      t = Math.min(1, Math.max((now - start) / speed, 0));
      // calculate the new scrollTop position (don't forget to smooth)
      y = Math.round(initialY + delta * smooth(t));
      // bracket scrollTop so we're never over-scrolling
      if (delta > 0 && y > targetY) {
        y = targetY;
      }

      if (delta < 0 && y < targetY) {
        y = targetY;
      }
      // only actually set scrollTop if there was a change front he last frame
      if (lastY !== y) {
        dom.scrollTop(scrollTarget, y);
      }

      lastY = y;
      // if we're not done yet, queue up an other frame to render,
      // or clean up
      if (y !== targetY) {
        rAF(render);
      } else {
        abort();
      }
    });
  }
};

module.exports = SmoothScrollMixin;
},{"../utils/Events":121,"../utils/domUtils":127,"../utils/requestAnimationFrame":131,"react":356}],119:[function(require,module,exports){
'use strict';

module.exports = {
  ClassNameMixin: require('./ClassNameMixin'),
  CollapseMixin: require('./CollapseMixin'),
  DimmerMixin: require('./DimmerMixin'),
  OverlayMixin: require('./OverlayMixin'),
  SmoothScrollMixin: require('./SmoothScrollMixin.js')
};
},{"./ClassNameMixin":114,"./CollapseMixin":115,"./DimmerMixin":116,"./OverlayMixin":117,"./SmoothScrollMixin.js":118}],120:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @via https://github.com/facebook/react/blob/master/src/vendor/core/CSSCore.js
 */

'use strict';

var CSSCore = {

  /**
   * Adds the class passed in to the element if it doesn't already have it.
   *
   * @param {DOMElement} element the element to set the class on
   * @param {string} className the CSS className
   * @return {DOMElement} the element passed in
   */
  addClass: function addClass(element, className) {
    if (className) {
      if (element.classList) {
        element.classList.add(className);
      } else if (!CSSCore.hasClass(element, className)) {
        element.className = element.className + ' ' + className;
      }
    }
    return element;
  },

  /**
   * Removes the class passed in from the element
   *
   * @param {DOMElement} element the element to set the class on
   * @param {string} className the CSS className
   * @return {DOMElement} the element passed in
   */
  removeClass: function removeClass(element, className) {
    if (className) {
      if (element.classList) {
        element.classList.remove(className);
      } else if (CSSCore.hasClass(element, className)) {
        element.className = element.className.replace(new RegExp('(^|\\s)' + className + '(?:\\s|$)', 'g'), '$1').replace(/\s+/g, ' ') // multiple spaces to one
        .replace(/^\s*|\s*$/g, ''); // trim the ends
      }
    }
    return element;
  },

  /**
   * Helper to add or remove a class from an element based on a condition.
   *
   * @param {DOMElement} element the element to set the class on
   * @param {string} className the CSS className
   * @param {*} bool condition to whether to add or remove the class
   * @return {DOMElement} the element passed in
   */
  conditionClass: function conditionClass(element, className, bool) {
    return (bool ? CSSCore.addClass : CSSCore.removeClass)(element, className);
  },

  /**
   * Tests whether the element has the class specified.
   *
   * @param {DOMNode|DOMWindow} element the element to set the class on
   * @param {string} className the CSS className
   * @return {boolean} true if the element has the class, false if not
   */
  hasClass: function hasClass(element, className) {
    if (element.classList) {
      return !!className && element.classList.contains(className);
    }
    return (' ' + element.className + ' ').indexOf(' ' + className + ' ') > -1;
  },

  toggleClass: function toggleClass(element, className) {
    return CSSCore.hasClass(element, className) ? CSSCore.removeClass(element, className) : CSSCore.addClass(element, className);
  }
};

module.exports = CSSCore;
},{}],121:[function(require,module,exports){
'use strict';

var canUseDOM = require('./canUseDOM');
var one = function one() {};
var on = function on() {};
var off = function off() {};

if (canUseDOM) {
  var bind = window.addEventListener ? 'addEventListener' : 'attachEvent';
  var unbind = window.removeEventListener ? 'removeEventListener' : 'detachEvent';
  var prefix = bind !== 'addEventListener' ? 'on' : '';

  one = function one(node, eventNames, eventListener) {
    var typeArray = eventNames.split(' ');
    var recursiveFunction = function recursiveFunction(e) {
      e.target.removeEventListener(e.type, recursiveFunction);
      return eventListener(e);
    };

    for (var i = typeArray.length - 1; i >= 0; i--) {
      this.on(node, typeArray[i], recursiveFunction);
    }
  };

  /**
   * Bind `node` event `eventName` to `eventListener`.
   *
   * @param {Element} node
   * @param {String} eventName
   * @param {Function} eventListener
   * @param {Boolean} capture
   * @return {Obejct}
   * @api public
   */

  on = function on(node, eventName, eventListener, capture) {
    node[bind](prefix + eventName, eventListener, capture || false);

    return {
      off: function off() {
        node[unbind](prefix + eventName, eventListener, capture || false);
      }
    };
  };

  /**
   * Unbind `node` event `eventName`'s callback `eventListener`.
   *
   * @param {Element} node
   * @param {String} eventName
   * @param {Function} eventListener
   * @param {Boolean} capture
   * @return {Function}
   * @api public
   */

  off = function off(node, eventName, eventListener, capture) {
    node[unbind](prefix + eventName, eventListener, capture || false);
    return eventListener;
  };
}

module.exports = {
  one: one,
  on: on,
  off: off
};
},{"./canUseDOM":123}],122:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * modified version of:
 * https://github.com/facebook/react/blob/0.13-stable/src/addons/transitions/ReactTransitionEvents.js
 * https://github.com/facebook/react/blob/5696ccfcd72189f4fea13d8b0f084a0e3c9b8147/src/renderers/dom/client/utils/getVendorPrefixedEventName.js
 */

'use strict';

var CSSCore = require('./CSSCore');
var canUseDOM = require('./canUseDOM');

/**
 * EVENT_NAME_MAP is used to determine which event fired when a
 * transition/animation ends, based on the style property used to
 * define that event.
 */
var EVENT_NAME_MAP = {
  transitionend: {
    'transition': 'transitionend',
    'WebkitTransition': 'webkitTransitionEnd',
    'MozTransition': 'mozTransitionEnd',
    'OTransition': 'oTransitionEnd',
    'msTransition': 'MSTransitionEnd'
  },

  animationend: {
    'animation': 'animationend',
    'WebkitAnimation': 'webkitAnimationEnd',
    'MozAnimation': 'mozAnimationEnd',
    'OAnimation': 'oAnimationEnd',
    'msAnimation': 'MSAnimationEnd'
  }
};

var endEvents = [];
var support = {};

function detectEvents() {
  var testEl = document.createElement('div');
  var style = testEl.style;

  // On some platforms, in particular some releases of Android 4.x,
  // the un-prefixed "animation" and "transition" properties are defined on the
  // style object but the events that fire will still be prefixed, so we need
  // to check if the un-prefixed events are useable, and if not remove them
  // from the map
  if (!('AnimationEvent' in window)) {
    delete EVENT_NAME_MAP.animationend.animation;
  }

  if (!('TransitionEvent' in window)) {
    delete EVENT_NAME_MAP.transitionend.transition;
  }

  for (var baseEventName in EVENT_NAME_MAP) {
    var baseEvents = EVENT_NAME_MAP[baseEventName];
    support[baseEventName] = false;

    for (var styleName in baseEvents) {
      if (styleName in style) {
        support[baseEventName] = baseEvents[styleName];
        endEvents.push(baseEvents[styleName]);
        break;
      }
    }
  }
}

if (canUseDOM) {
  detectEvents();
}

if (support.animationend) {
  CSSCore.addClass(document.documentElement, 'cssanimations');
}

// We use the raw {add|remove}EventListener() call because EventListener
// does not know how to remove event listeners and we really should
// clean up. Also, these events are not triggered in older browsers
// so we should be A-OK here.

function addEventListener(node, eventName, eventListener) {
  node.addEventListener(eventName, eventListener, false);
}

function removeEventListener(node, eventName, eventListener) {
  node.removeEventListener(eventName, eventListener, false);
}

var TransitionEvents = {
  on: function on(node, eventListener) {
    if (endEvents.length === 0) {
      // If CSS transitions are not supported, trigger an "end animation"
      // event immediately.
      window.setTimeout(eventListener, 0);
      return;
    }
    endEvents.forEach(function (endEvent) {
      addEventListener(node, endEvent, eventListener);
    });
  },

  off: function off(node, eventListener) {
    if (endEvents.length === 0) {
      return;
    }
    endEvents.forEach(function (endEvent) {
      removeEventListener(node, endEvent, eventListener);
    });
  },

  support: support
};

module.exports = TransitionEvents;
},{"./CSSCore":120,"./canUseDOM":123}],123:[function(require,module,exports){
'use strict';

module.exports = !!(typeof window !== 'undefined' && window.document && window.document.createElement);
},{}],124:[function(require,module,exports){
/**
 * modified version of:
 * https://github.com/react-bootstrap/react-bootstrap/blob/master/src/utils/createChainedFunction.js
 */

'use strict';

/**
 * Safe chained function
 *
 * Will only create a new function if needed,
 * otherwise will pass back existing functions or null.
 *
 * @param {function} one
 * @param {function} two
 * @returns {function|null}
 */

function createChainedFunction(one, two) {
  var hasOne = typeof one === 'function';
  var hasTwo = typeof two === 'function';

  if (!hasOne && !hasTwo) {
    return null;
  }

  if (!hasOne) {
    return two;
  }

  if (!hasTwo) {
    return one;
  }

  return function chainedFunction() {
    one.apply(this, arguments) === false || two.apply(this, arguments);
  };
}

module.exports = createChainedFunction;
},{}],125:[function(require,module,exports){
'use strict';

var locales = {
  'en_US': {
    days: ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday'],
    daysShort: ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'],
    daysMin: ['Su', 'Mo', 'Tu', 'We', 'Th', 'Fr', 'Sa', 'Su'],
    months: ['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'],
    monthsShort: ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'],
    today: 'Today',
    weekStart: 0
  },
  'zh_CN': {
    days: ['星期日', '星期一', '星期二', '星期三', '星期四', '星期五', '星期六'],
    daysShort: ['周日', '周一', '周二', '周三', '周四', '周五', '周六'],
    daysMin: ['日', '一', '二', '三', '四', '五', '六'],
    months: ['一月', '二月', '三月', '四月', '五月', '六月', '七月', '八月', '九月', '十月', '十一月', '十二月'],
    monthsShort: ['一月', '二月', '三月', '四月', '五月', '六月', '七月', '八月', '九月', '十月', '十一月', '十二月'],
    today: '今天',
    weekStart: 0
  }
};

var dateUtils = {
  isLeapYear: function isLeapYear(year) {
    return year % 4 === 0 && year % 100 !== 0 || year % 400 === 0;
  },

  getDaysInMonth: function getDaysInMonth(year, month) {
    return [31, this.isLeapYear(year) ? 29 : 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31][month];
  },

  getLocale: function getLocale(locale) {
    if (!locale && navigator && navigator.language) {
      locale = navigator.language.split('-');
      locale[1] = locale[1].toUpperCase();
      locale = locale.join('_');
    }

    return locales[locale] || locales['zh_CN'];
  }
};

module.exports = dateUtils;
},{}],126:[function(require,module,exports){
'use strict';

/**
 * Debounce function
 * @param {function} fn  Function to be debounced
 * @param {number} wait Function execution threshold in milliseconds
 * @param {bool} immediate  Whether the function should be called at
 *                          the beginning of the delay instead of the
 *                          end. Default is false.
 * @desc Executes a function when it stops being invoked for n seconds
 * @via  _.debounce() http://underscorejs.org
 */

module.exports = function (fn, wait, immediate) {
  var timeout;

  return function () {
    var context = this;
    var args = arguments;
    var later = function later() {
      timeout = null;
      if (!immediate) {
        fn.apply(context, args);
      }
    };
    var callNow = immediate && !timeout;

    clearTimeout(timeout);
    timeout = setTimeout(later, wait);

    if (callNow) {
      fn.apply(context, args);
    }
  };
};
},{}],127:[function(require,module,exports){
'use strict';

var React = require('react');
var ReactDOM = require('react-dom');

/**
 * Get ownerDocument
 * @param {ReactComponent|HTMLElement} componentOrElement
 * @returns {HTMLDocument}
 */
function ownerDocument(componentOrElement) {
  var element = ReactDOM.findDOMNode(componentOrElement);

  return element && element.ownerDocument || document;
}

/**
 * Get ownerWindow
 * @param {HTMLElement} element
 * @returns {DocumentView|Window}
 * @refer https://github.com/jquery/jquery/blob/6df669f0fb87cd9975a18bf6bbe3c3548afa4fee/src/event.js#L294-L297
 */
function ownerWindow(element) {
  var doc = ownerDocument(element);

  return doc.defaultView || doc.parentWindow || window;
}

module.exports = {
  ownerDocument: ownerDocument,

  ownerWindow: ownerWindow,

  scrollTop: function scrollTop(element, value) {
    if (!element) {
      return;
    }

    var hasScrollTop = 'scrollTop' in element;

    if (value === undefined) {
      return hasScrollTop ? element.scrollTop : element.pageYOffset;
    }

    hasScrollTop ? element.scrollTop = value : element.scrollTo(element.scrollX, value);
  },

  offset: function offset(element) {
    if (element) {
      var rect = element.getBoundingClientRect();
      var body = document.body;
      var clientTop = element.clientTop || body.clientTop || 0;
      var clientLeft = element.clientLeft || body.clientLeft || 0;
      var scrollTop = window.pageYOffset || element.scrollTop;
      var scrollLeft = window.pageXOffset || element.scrollLeft;

      return {
        top: rect.top + scrollTop - clientTop,
        left: rect.left + scrollLeft - clientLeft
      };
    }

    return null;
  },

  position: function position(element) {
    return {
      left: element.offsetLeft,
      top: element.offsetTop
    };
  }
};
},{"react":356,"react-dom":189}],128:[function(require,module,exports){
'use strict';

/**
 * getScrollbarWidth
 *
 * @desc via http://davidwalsh.name/detect-scrollbar-width
 * @returns {number}
 */

function getScrollbarWidth() {
  if (document.body.clientWidth >= window.innerWidth) {
    return 0;
  }

  // Create the measurement node
  var measure = document.createElement('div');

  measure.className = 'am-scrollbar-measure';
  document.body.appendChild(measure);

  // Get the scrollbar width
  var scrollbarWidth = measure.offsetWidth - measure.clientWidth;

  // Delete the DIV
  document.body.removeChild(measure);

  return scrollbarWidth;
}

module.exports = getScrollbarWidth;
},{}],129:[function(require,module,exports){
'use strict';

/**
 * isInViewport
 *
 * @desc determine if any part of the element is visible in the viewport
 * @reference https://github.com/Josh-Miller/isInViewport
 * @see http://stackoverflow.com/questions/123999/how-to-tell-if-a-dom-element-is-visible-in-the-current-viewport
 * @param {HTMLElement} element
 * @returns {boolean}
 */

function isInViewport(element) {
  var top = element.offsetTop;
  var left = element.offsetLeft;
  var width = element.offsetWidth;
  var height = element.offsetHeight;

  while (element.offsetParent) {
    element = element.offsetParent;
    top += element.offsetTop;
    left += element.offsetLeft;
  }

  return top < window.pageYOffset + window.innerHeight && left < window.pageXOffset + window.innerWidth && top + height > window.pageYOffset && left + width > window.pageXOffset;
}

module.exports = isInViewport;

// TODO: 考虑滚动条不在窗口上的情形
},{}],130:[function(require,module,exports){
'use strict';

module.exports = function (node, tree) {
  while (node) {
    if (node === tree) {
      return true;
    }
    node = node.parentNode;
  }

  return false;
};
},{}],131:[function(require,module,exports){
(function (global){
/**
 * Copyright 2014-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * modified version of:
 * https://github.com/facebook/react/blob/0.13-stable/src/vendor/core/requestAnimationFrame.js
 */

'use strict';

var nativeRAF = global.requestAnimationFrame || global.webkitRequestAnimationFrame || global.mozRequestAnimationFrame;

var lastTime = 0;

var requestAnimationFrame = nativeRAF || function (callback) {
  var currTime = Date.now();
  var timeDelay = Math.max(0, 16 - (currTime - lastTime));

  lastTime = currTime + timeDelay;
  return global.setTimeout(function () {
    callback(Date.now());
  }, timeDelay);
};

// Works around a rare bug in Safari 6 where the first request is never invoked.
requestAnimationFrame(function () {});

module.exports = requestAnimationFrame;
}).call(this,typeof global !== "undefined" ? global : typeof self !== "undefined" ? self : typeof window !== "undefined" ? window : {})
},{}],132:[function(require,module,exports){
/*!
  Copyright (c) 2016 Jed Watson.
  Licensed under the MIT License (MIT), see
  http://jedwatson.github.io/classnames
*/
/* global define */

(function () {
	'use strict';

	var hasOwn = {}.hasOwnProperty;

	function classNames () {
		var classes = [];

		for (var i = 0; i < arguments.length; i++) {
			var arg = arguments[i];
			if (!arg) continue;

			var argType = typeof arg;

			if (argType === 'string' || argType === 'number') {
				classes.push(arg);
			} else if (Array.isArray(arg)) {
				classes.push(classNames.apply(null, arg));
			} else if (argType === 'object') {
				for (var key in arg) {
					if (hasOwn.call(arg, key) && arg[key]) {
						classes.push(key);
					}
				}
			}
		}

		return classes.join(' ');
	}

	if (typeof module !== 'undefined' && module.exports) {
		module.exports = classNames;
	} else if (typeof define === 'function' && typeof define.amd === 'object' && define.amd) {
		// register as 'classnames', consistent with npm package name
		define('classnames', [], function () {
			return classNames;
		});
	} else {
		window.classNames = classNames;
	}
}());

},{}],133:[function(require,module,exports){
var pSlice = Array.prototype.slice;
var objectKeys = require('./lib/keys.js');
var isArguments = require('./lib/is_arguments.js');

var deepEqual = module.exports = function (actual, expected, opts) {
  if (!opts) opts = {};
  // 7.1. All identical values are equivalent, as determined by ===.
  if (actual === expected) {
    return true;

  } else if (actual instanceof Date && expected instanceof Date) {
    return actual.getTime() === expected.getTime();

  // 7.3. Other pairs that do not both pass typeof value == 'object',
  // equivalence is determined by ==.
  } else if (!actual || !expected || typeof actual != 'object' && typeof expected != 'object') {
    return opts.strict ? actual === expected : actual == expected;

  // 7.4. For all other Object pairs, including Array objects, equivalence is
  // determined by having the same number of owned properties (as verified
  // with Object.prototype.hasOwnProperty.call), the same set of keys
  // (although not necessarily the same order), equivalent values for every
  // corresponding key, and an identical 'prototype' property. Note: this
  // accounts for both named and indexed properties on Arrays.
  } else {
    return objEquiv(actual, expected, opts);
  }
}

function isUndefinedOrNull(value) {
  return value === null || value === undefined;
}

function isBuffer (x) {
  if (!x || typeof x !== 'object' || typeof x.length !== 'number') return false;
  if (typeof x.copy !== 'function' || typeof x.slice !== 'function') {
    return false;
  }
  if (x.length > 0 && typeof x[0] !== 'number') return false;
  return true;
}

function objEquiv(a, b, opts) {
  var i, key;
  if (isUndefinedOrNull(a) || isUndefinedOrNull(b))
    return false;
  // an identical 'prototype' property.
  if (a.prototype !== b.prototype) return false;
  //~~~I've managed to break Object.keys through screwy arguments passing.
  //   Converting to array solves the problem.
  if (isArguments(a)) {
    if (!isArguments(b)) {
      return false;
    }
    a = pSlice.call(a);
    b = pSlice.call(b);
    return deepEqual(a, b, opts);
  }
  if (isBuffer(a)) {
    if (!isBuffer(b)) {
      return false;
    }
    if (a.length !== b.length) return false;
    for (i = 0; i < a.length; i++) {
      if (a[i] !== b[i]) return false;
    }
    return true;
  }
  try {
    var ka = objectKeys(a),
        kb = objectKeys(b);
  } catch (e) {//happens when one is a string literal and the other isn't
    return false;
  }
  // having the same number of owned properties (keys incorporates
  // hasOwnProperty)
  if (ka.length != kb.length)
    return false;
  //the same set of keys (although not necessarily the same order),
  ka.sort();
  kb.sort();
  //~~~cheap key test
  for (i = ka.length - 1; i >= 0; i--) {
    if (ka[i] != kb[i])
      return false;
  }
  //equivalent values for every corresponding key, and
  //~~~possibly expensive deep test
  for (i = ka.length - 1; i >= 0; i--) {
    key = ka[i];
    if (!deepEqual(a[key], b[key], opts)) return false;
  }
  return typeof a === typeof b;
}

},{"./lib/is_arguments.js":134,"./lib/keys.js":135}],134:[function(require,module,exports){
var supportsArgumentsClass = (function(){
  return Object.prototype.toString.call(arguments)
})() == '[object Arguments]';

exports = module.exports = supportsArgumentsClass ? supported : unsupported;

exports.supported = supported;
function supported(object) {
  return Object.prototype.toString.call(object) == '[object Arguments]';
};

exports.unsupported = unsupported;
function unsupported(object){
  return object &&
    typeof object == 'object' &&
    typeof object.length == 'number' &&
    Object.prototype.hasOwnProperty.call(object, 'callee') &&
    !Object.prototype.propertyIsEnumerable.call(object, 'callee') ||
    false;
};

},{}],135:[function(require,module,exports){
exports = module.exports = typeof Object.keys === 'function'
  ? Object.keys : shim;

exports.shim = shim;
function shim (obj) {
  var keys = [];
  for (var key in obj) keys.push(key);
  return keys;
}

},{}],136:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule EventListener
 * @typechecks
 */

'use strict';

var emptyFunction = require('./emptyFunction');

/**
 * Upstream version of event listener. Does not take into account specific
 * nature of platform.
 */
var EventListener = {
  /**
   * Listen to DOM events during the bubble phase.
   *
   * @param {DOMEventTarget} target DOM element to register listener on.
   * @param {string} eventType Event type, e.g. 'click' or 'mouseover'.
   * @param {function} callback Callback function.
   * @return {object} Object with a `remove` method.
   */
  listen: function (target, eventType, callback) {
    if (target.addEventListener) {
      target.addEventListener(eventType, callback, false);
      return {
        remove: function () {
          target.removeEventListener(eventType, callback, false);
        }
      };
    } else if (target.attachEvent) {
      target.attachEvent('on' + eventType, callback);
      return {
        remove: function () {
          target.detachEvent('on' + eventType, callback);
        }
      };
    }
  },

  /**
   * Listen to DOM events during the capture phase.
   *
   * @param {DOMEventTarget} target DOM element to register listener on.
   * @param {string} eventType Event type, e.g. 'click' or 'mouseover'.
   * @param {function} callback Callback function.
   * @return {object} Object with a `remove` method.
   */
  capture: function (target, eventType, callback) {
    if (target.addEventListener) {
      target.addEventListener(eventType, callback, true);
      return {
        remove: function () {
          target.removeEventListener(eventType, callback, true);
        }
      };
    } else {
      if ("production" !== 'production') {
        console.error('Attempted to listen to events during the capture phase on a ' + 'browser that does not support the capture phase. Your application ' + 'will not receive some events.');
      }
      return {
        remove: emptyFunction
      };
    }
  },

  registerDefault: function () {}
};

module.exports = EventListener;
},{"./emptyFunction":143}],137:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ExecutionEnvironment
 */

'use strict';

var canUseDOM = !!(typeof window !== 'undefined' && window.document && window.document.createElement);

/**
 * Simple, lightweight module assisting with the detection and context of
 * Worker. Helps avoid circular dependencies and allows code to reason about
 * whether or not they are in a Worker, even if they never include the main
 * `ReactWorker` dependency.
 */
var ExecutionEnvironment = {

  canUseDOM: canUseDOM,

  canUseWorkers: typeof Worker !== 'undefined',

  canUseEventListeners: canUseDOM && !!(window.addEventListener || window.attachEvent),

  canUseViewport: canUseDOM && !!window.screen,

  isInWorker: !canUseDOM // For now, this is true - might change in the future.

};

module.exports = ExecutionEnvironment;
},{}],138:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule camelize
 * @typechecks
 */

"use strict";

var _hyphenPattern = /-(.)/g;

/**
 * Camelcases a hyphenated string, for example:
 *
 *   > camelize('background-color')
 *   < "backgroundColor"
 *
 * @param {string} string
 * @return {string}
 */
function camelize(string) {
  return string.replace(_hyphenPattern, function (_, character) {
    return character.toUpperCase();
  });
}

module.exports = camelize;
},{}],139:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule camelizeStyleName
 * @typechecks
 */

'use strict';

var camelize = require('./camelize');

var msPattern = /^-ms-/;

/**
 * Camelcases a hyphenated CSS property name, for example:
 *
 *   > camelizeStyleName('background-color')
 *   < "backgroundColor"
 *   > camelizeStyleName('-moz-transition')
 *   < "MozTransition"
 *   > camelizeStyleName('-ms-transition')
 *   < "msTransition"
 *
 * As Andi Smith suggests
 * (http://www.andismith.com/blog/2012/02/modernizr-prefixed/), an `-ms` prefix
 * is converted to lowercase `ms`.
 *
 * @param {string} string
 * @return {string}
 */
function camelizeStyleName(string) {
  return camelize(string.replace(msPattern, 'ms-'));
}

module.exports = camelizeStyleName;
},{"./camelize":138}],140:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule containsNode
 * @typechecks
 */

'use strict';

var isTextNode = require('./isTextNode');

/*eslint-disable no-bitwise */

/**
 * Checks if a given DOM node contains or is another DOM node.
 *
 * @param {?DOMNode} outerNode Outer DOM node.
 * @param {?DOMNode} innerNode Inner DOM node.
 * @return {boolean} True if `outerNode` contains or is `innerNode`.
 */
function containsNode(_x, _x2) {
  var _again = true;

  _function: while (_again) {
    var outerNode = _x,
        innerNode = _x2;
    _again = false;

    if (!outerNode || !innerNode) {
      return false;
    } else if (outerNode === innerNode) {
      return true;
    } else if (isTextNode(outerNode)) {
      return false;
    } else if (isTextNode(innerNode)) {
      _x = outerNode;
      _x2 = innerNode.parentNode;
      _again = true;
      continue _function;
    } else if (outerNode.contains) {
      return outerNode.contains(innerNode);
    } else if (outerNode.compareDocumentPosition) {
      return !!(outerNode.compareDocumentPosition(innerNode) & 16);
    } else {
      return false;
    }
  }
}

module.exports = containsNode;
},{"./isTextNode":153}],141:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule createArrayFromMixed
 * @typechecks
 */

'use strict';

var toArray = require('./toArray');

/**
 * Perform a heuristic test to determine if an object is "array-like".
 *
 *   A monk asked Joshu, a Zen master, "Has a dog Buddha nature?"
 *   Joshu replied: "Mu."
 *
 * This function determines if its argument has "array nature": it returns
 * true if the argument is an actual array, an `arguments' object, or an
 * HTMLCollection (e.g. node.childNodes or node.getElementsByTagName()).
 *
 * It will return false for other array-like objects like Filelist.
 *
 * @param {*} obj
 * @return {boolean}
 */
function hasArrayNature(obj) {
  return(
    // not null/false
    !!obj && (
    // arrays are objects, NodeLists are functions in Safari
    typeof obj == 'object' || typeof obj == 'function') &&
    // quacks like an array
    'length' in obj &&
    // not window
    !('setInterval' in obj) &&
    // no DOM node should be considered an array-like
    // a 'select' element has 'length' and 'item' properties on IE8
    typeof obj.nodeType != 'number' && (
    // a real array
    Array.isArray(obj) ||
    // arguments
    'callee' in obj ||
    // HTMLCollection/NodeList
    'item' in obj)
  );
}

/**
 * Ensure that the argument is an array by wrapping it in an array if it is not.
 * Creates a copy of the argument if it is already an array.
 *
 * This is mostly useful idiomatically:
 *
 *   var createArrayFromMixed = require('createArrayFromMixed');
 *
 *   function takesOneOrMoreThings(things) {
 *     things = createArrayFromMixed(things);
 *     ...
 *   }
 *
 * This allows you to treat `things' as an array, but accept scalars in the API.
 *
 * If you need to convert an array-like object, like `arguments`, into an array
 * use toArray instead.
 *
 * @param {*} obj
 * @return {array}
 */
function createArrayFromMixed(obj) {
  if (!hasArrayNature(obj)) {
    return [obj];
  } else if (Array.isArray(obj)) {
    return obj.slice();
  } else {
    return toArray(obj);
  }
}

module.exports = createArrayFromMixed;
},{"./toArray":161}],142:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule createNodesFromMarkup
 * @typechecks
 */

/*eslint-disable fb-www/unsafe-html*/

'use strict';

var ExecutionEnvironment = require('./ExecutionEnvironment');

var createArrayFromMixed = require('./createArrayFromMixed');
var getMarkupWrap = require('./getMarkupWrap');
var invariant = require('./invariant');

/**
 * Dummy container used to render all markup.
 */
var dummyNode = ExecutionEnvironment.canUseDOM ? document.createElement('div') : null;

/**
 * Pattern used by `getNodeName`.
 */
var nodeNamePattern = /^\s*<(\w+)/;

/**
 * Extracts the `nodeName` of the first element in a string of markup.
 *
 * @param {string} markup String of markup.
 * @return {?string} Node name of the supplied markup.
 */
function getNodeName(markup) {
  var nodeNameMatch = markup.match(nodeNamePattern);
  return nodeNameMatch && nodeNameMatch[1].toLowerCase();
}

/**
 * Creates an array containing the nodes rendered from the supplied markup. The
 * optionally supplied `handleScript` function will be invoked once for each
 * <script> element that is rendered. If no `handleScript` function is supplied,
 * an exception is thrown if any <script> elements are rendered.
 *
 * @param {string} markup A string of valid HTML markup.
 * @param {?function} handleScript Invoked once for each rendered <script>.
 * @return {array<DOMElement|DOMTextNode>} An array of rendered nodes.
 */
function createNodesFromMarkup(markup, handleScript) {
  var node = dummyNode;
  !!!dummyNode ? "production" !== 'production' ? invariant(false, 'createNodesFromMarkup dummy not initialized') : invariant(false) : undefined;
  var nodeName = getNodeName(markup);

  var wrap = nodeName && getMarkupWrap(nodeName);
  if (wrap) {
    node.innerHTML = wrap[1] + markup + wrap[2];

    var wrapDepth = wrap[0];
    while (wrapDepth--) {
      node = node.lastChild;
    }
  } else {
    node.innerHTML = markup;
  }

  var scripts = node.getElementsByTagName('script');
  if (scripts.length) {
    !handleScript ? "production" !== 'production' ? invariant(false, 'createNodesFromMarkup(...): Unexpected <script> element rendered.') : invariant(false) : undefined;
    createArrayFromMixed(scripts).forEach(handleScript);
  }

  var nodes = createArrayFromMixed(node.childNodes);
  while (node.lastChild) {
    node.removeChild(node.lastChild);
  }
  return nodes;
}

module.exports = createNodesFromMarkup;
},{"./ExecutionEnvironment":137,"./createArrayFromMixed":141,"./getMarkupWrap":147,"./invariant":151}],143:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule emptyFunction
 */

"use strict";

function makeEmptyFunction(arg) {
  return function () {
    return arg;
  };
}

/**
 * This function accepts and discards inputs; it has no side effects. This is
 * primarily useful idiomatically for overridable function endpoints which
 * always need to be callable, since JS lacks a null-call idiom ala Cocoa.
 */
function emptyFunction() {}

emptyFunction.thatReturns = makeEmptyFunction;
emptyFunction.thatReturnsFalse = makeEmptyFunction(false);
emptyFunction.thatReturnsTrue = makeEmptyFunction(true);
emptyFunction.thatReturnsNull = makeEmptyFunction(null);
emptyFunction.thatReturnsThis = function () {
  return this;
};
emptyFunction.thatReturnsArgument = function (arg) {
  return arg;
};

module.exports = emptyFunction;
},{}],144:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule emptyObject
 */

'use strict';

var emptyObject = {};

if ("production" !== 'production') {
  Object.freeze(emptyObject);
}

module.exports = emptyObject;
},{}],145:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule focusNode
 */

'use strict';

/**
 * @param {DOMElement} node input/textarea to focus
 */
function focusNode(node) {
  // IE8 can throw "Can't move focus to the control because it is invisible,
  // not enabled, or of a type that does not accept the focus." for all kinds of
  // reasons that are too expensive and fragile to test.
  try {
    node.focus();
  } catch (e) {}
}

module.exports = focusNode;
},{}],146:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule getActiveElement
 * @typechecks
 */

/* eslint-disable fb-www/typeof-undefined */

/**
 * Same as document.activeElement but wraps in a try-catch block. In IE it is
 * not safe to call document.activeElement if there is nothing focused.
 *
 * The activeElement will be null only if the document or document body is not
 * yet defined.
 */
'use strict';

function getActiveElement() /*?DOMElement*/{
  if (typeof document === 'undefined') {
    return null;
  }
  try {
    return document.activeElement || document.body;
  } catch (e) {
    return document.body;
  }
}

module.exports = getActiveElement;
},{}],147:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule getMarkupWrap
 */

/*eslint-disable fb-www/unsafe-html */

'use strict';

var ExecutionEnvironment = require('./ExecutionEnvironment');

var invariant = require('./invariant');

/**
 * Dummy container used to detect which wraps are necessary.
 */
var dummyNode = ExecutionEnvironment.canUseDOM ? document.createElement('div') : null;

/**
 * Some browsers cannot use `innerHTML` to render certain elements standalone,
 * so we wrap them, render the wrapped nodes, then extract the desired node.
 *
 * In IE8, certain elements cannot render alone, so wrap all elements ('*').
 */

var shouldWrap = {};

var selectWrap = [1, '<select multiple="true">', '</select>'];
var tableWrap = [1, '<table>', '</table>'];
var trWrap = [3, '<table><tbody><tr>', '</tr></tbody></table>'];

var svgWrap = [1, '<svg xmlns="http://www.w3.org/2000/svg">', '</svg>'];

var markupWrap = {
  '*': [1, '?<div>', '</div>'],

  'area': [1, '<map>', '</map>'],
  'col': [2, '<table><tbody></tbody><colgroup>', '</colgroup></table>'],
  'legend': [1, '<fieldset>', '</fieldset>'],
  'param': [1, '<object>', '</object>'],
  'tr': [2, '<table><tbody>', '</tbody></table>'],

  'optgroup': selectWrap,
  'option': selectWrap,

  'caption': tableWrap,
  'colgroup': tableWrap,
  'tbody': tableWrap,
  'tfoot': tableWrap,
  'thead': tableWrap,

  'td': trWrap,
  'th': trWrap
};

// Initialize the SVG elements since we know they'll always need to be wrapped
// consistently. If they are created inside a <div> they will be initialized in
// the wrong namespace (and will not display).
var svgElements = ['circle', 'clipPath', 'defs', 'ellipse', 'g', 'image', 'line', 'linearGradient', 'mask', 'path', 'pattern', 'polygon', 'polyline', 'radialGradient', 'rect', 'stop', 'text', 'tspan'];
svgElements.forEach(function (nodeName) {
  markupWrap[nodeName] = svgWrap;
  shouldWrap[nodeName] = true;
});

/**
 * Gets the markup wrap configuration for the supplied `nodeName`.
 *
 * NOTE: This lazily detects which wraps are necessary for the current browser.
 *
 * @param {string} nodeName Lowercase `nodeName`.
 * @return {?array} Markup wrap configuration, if applicable.
 */
function getMarkupWrap(nodeName) {
  !!!dummyNode ? "production" !== 'production' ? invariant(false, 'Markup wrapping node not initialized') : invariant(false) : undefined;
  if (!markupWrap.hasOwnProperty(nodeName)) {
    nodeName = '*';
  }
  if (!shouldWrap.hasOwnProperty(nodeName)) {
    if (nodeName === '*') {
      dummyNode.innerHTML = '<link />';
    } else {
      dummyNode.innerHTML = '<' + nodeName + '></' + nodeName + '>';
    }
    shouldWrap[nodeName] = !dummyNode.firstChild;
  }
  return shouldWrap[nodeName] ? markupWrap[nodeName] : null;
}

module.exports = getMarkupWrap;
},{"./ExecutionEnvironment":137,"./invariant":151}],148:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule getUnboundedScrollPosition
 * @typechecks
 */

'use strict';

/**
 * Gets the scroll position of the supplied element or window.
 *
 * The return values are unbounded, unlike `getScrollPosition`. This means they
 * may be negative or exceed the element boundaries (which is possible using
 * inertial scrolling).
 *
 * @param {DOMWindow|DOMElement} scrollable
 * @return {object} Map with `x` and `y` keys.
 */
function getUnboundedScrollPosition(scrollable) {
  if (scrollable === window) {
    return {
      x: window.pageXOffset || document.documentElement.scrollLeft,
      y: window.pageYOffset || document.documentElement.scrollTop
    };
  }
  return {
    x: scrollable.scrollLeft,
    y: scrollable.scrollTop
  };
}

module.exports = getUnboundedScrollPosition;
},{}],149:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule hyphenate
 * @typechecks
 */

'use strict';

var _uppercasePattern = /([A-Z])/g;

/**
 * Hyphenates a camelcased string, for example:
 *
 *   > hyphenate('backgroundColor')
 *   < "background-color"
 *
 * For CSS style names, use `hyphenateStyleName` instead which works properly
 * with all vendor prefixes, including `ms`.
 *
 * @param {string} string
 * @return {string}
 */
function hyphenate(string) {
  return string.replace(_uppercasePattern, '-$1').toLowerCase();
}

module.exports = hyphenate;
},{}],150:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule hyphenateStyleName
 * @typechecks
 */

'use strict';

var hyphenate = require('./hyphenate');

var msPattern = /^ms-/;

/**
 * Hyphenates a camelcased CSS property name, for example:
 *
 *   > hyphenateStyleName('backgroundColor')
 *   < "background-color"
 *   > hyphenateStyleName('MozTransition')
 *   < "-moz-transition"
 *   > hyphenateStyleName('msTransition')
 *   < "-ms-transition"
 *
 * As Modernizr suggests (http://modernizr.com/docs/#prefixed), an `ms` prefix
 * is converted to `-ms-`.
 *
 * @param {string} string
 * @return {string}
 */
function hyphenateStyleName(string) {
  return hyphenate(string).replace(msPattern, '-ms-');
}

module.exports = hyphenateStyleName;
},{"./hyphenate":149}],151:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule invariant
 */

'use strict';

/**
 * Use invariant() to assert state which your program assumes to be true.
 *
 * Provide sprintf-style format (only %s is supported) and arguments
 * to provide information about what broke and what you were
 * expecting.
 *
 * The invariant message will be stripped in production, but the invariant
 * will remain to ensure logic does not differ in production.
 */

function invariant(condition, format, a, b, c, d, e, f) {
  if ("production" !== 'production') {
    if (format === undefined) {
      throw new Error('invariant requires an error message argument');
    }
  }

  if (!condition) {
    var error;
    if (format === undefined) {
      error = new Error('Minified exception occurred; use the non-minified dev environment ' + 'for the full error message and additional helpful warnings.');
    } else {
      var args = [a, b, c, d, e, f];
      var argIndex = 0;
      error = new Error(format.replace(/%s/g, function () {
        return args[argIndex++];
      }));
      error.name = 'Invariant Violation';
    }

    error.framesToPop = 1; // we don't care about invariant's own frame
    throw error;
  }
}

module.exports = invariant;
},{}],152:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule isNode
 * @typechecks
 */

/**
 * @param {*} object The object to check.
 * @return {boolean} Whether or not the object is a DOM node.
 */
'use strict';

function isNode(object) {
  return !!(object && (typeof Node === 'function' ? object instanceof Node : typeof object === 'object' && typeof object.nodeType === 'number' && typeof object.nodeName === 'string'));
}

module.exports = isNode;
},{}],153:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule isTextNode
 * @typechecks
 */

'use strict';

var isNode = require('./isNode');

/**
 * @param {*} object The object to check.
 * @return {boolean} Whether or not the object is a DOM text node.
 */
function isTextNode(object) {
  return isNode(object) && object.nodeType == 3;
}

module.exports = isTextNode;
},{"./isNode":152}],154:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule keyMirror
 * @typechecks static-only
 */

'use strict';

var invariant = require('./invariant');

/**
 * Constructs an enumeration with keys equal to their value.
 *
 * For example:
 *
 *   var COLORS = keyMirror({blue: null, red: null});
 *   var myColor = COLORS.blue;
 *   var isColorValid = !!COLORS[myColor];
 *
 * The last line could not be performed if the values of the generated enum were
 * not equal to their keys.
 *
 *   Input:  {key1: val1, key2: val2}
 *   Output: {key1: key1, key2: key2}
 *
 * @param {object} obj
 * @return {object}
 */
var keyMirror = function (obj) {
  var ret = {};
  var key;
  !(obj instanceof Object && !Array.isArray(obj)) ? "production" !== 'production' ? invariant(false, 'keyMirror(...): Argument must be an object.') : invariant(false) : undefined;
  for (key in obj) {
    if (!obj.hasOwnProperty(key)) {
      continue;
    }
    ret[key] = key;
  }
  return ret;
};

module.exports = keyMirror;
},{"./invariant":151}],155:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule keyOf
 */

/**
 * Allows extraction of a minified key. Let's the build system minify keys
 * without losing the ability to dynamically use key strings as values
 * themselves. Pass in an object with a single key/val pair and it will return
 * you the string key of that single record. Suppose you want to grab the
 * value for a key 'className' inside of an object. Key/val minification may
 * have aliased that key to be 'xa12'. keyOf({className: null}) will return
 * 'xa12' in that case. Resolve keys you want to use once at startup time, then
 * reuse those resolutions.
 */
"use strict";

var keyOf = function (oneKeyObj) {
  var key;
  for (key in oneKeyObj) {
    if (!oneKeyObj.hasOwnProperty(key)) {
      continue;
    }
    return key;
  }
  return null;
};

module.exports = keyOf;
},{}],156:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule mapObject
 */

'use strict';

var hasOwnProperty = Object.prototype.hasOwnProperty;

/**
 * Executes the provided `callback` once for each enumerable own property in the
 * object and constructs a new object from the results. The `callback` is
 * invoked with three arguments:
 *
 *  - the property value
 *  - the property name
 *  - the object being traversed
 *
 * Properties that are added after the call to `mapObject` will not be visited
 * by `callback`. If the values of existing properties are changed, the value
 * passed to `callback` will be the value at the time `mapObject` visits them.
 * Properties that are deleted before being visited are not visited.
 *
 * @grep function objectMap()
 * @grep function objMap()
 *
 * @param {?object} object
 * @param {function} callback
 * @param {*} context
 * @return {?object}
 */
function mapObject(object, callback, context) {
  if (!object) {
    return null;
  }
  var result = {};
  for (var name in object) {
    if (hasOwnProperty.call(object, name)) {
      result[name] = callback.call(context, object[name], name, object);
    }
  }
  return result;
}

module.exports = mapObject;
},{}],157:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule memoizeStringOnly
 * @typechecks static-only
 */

'use strict';

/**
 * Memoizes the return value of a function that accepts one string argument.
 *
 * @param {function} callback
 * @return {function}
 */
function memoizeStringOnly(callback) {
  var cache = {};
  return function (string) {
    if (!cache.hasOwnProperty(string)) {
      cache[string] = callback.call(this, string);
    }
    return cache[string];
  };
}

module.exports = memoizeStringOnly;
},{}],158:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule performance
 * @typechecks
 */

'use strict';

var ExecutionEnvironment = require('./ExecutionEnvironment');

var performance;

if (ExecutionEnvironment.canUseDOM) {
  performance = window.performance || window.msPerformance || window.webkitPerformance;
}

module.exports = performance || {};
},{"./ExecutionEnvironment":137}],159:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule performanceNow
 * @typechecks
 */

'use strict';

var performance = require('./performance');

var performanceNow;

/**
 * Detect if we can use `window.performance.now()` and gracefully fallback to
 * `Date.now()` if it doesn't exist. We need to support Firefox < 15 for now
 * because of Facebook's testing infrastructure.
 */
if (performance.now) {
  performanceNow = function () {
    return performance.now();
  };
} else {
  performanceNow = function () {
    return Date.now();
  };
}

module.exports = performanceNow;
},{"./performance":158}],160:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule shallowEqual
 * @typechecks
 * 
 */

'use strict';

var hasOwnProperty = Object.prototype.hasOwnProperty;

/**
 * Performs equality by iterating through keys on an object and returning false
 * when any key has values which are not strictly equal between the arguments.
 * Returns true when the values of all keys are strictly equal.
 */
function shallowEqual(objA, objB) {
  if (objA === objB) {
    return true;
  }

  if (typeof objA !== 'object' || objA === null || typeof objB !== 'object' || objB === null) {
    return false;
  }

  var keysA = Object.keys(objA);
  var keysB = Object.keys(objB);

  if (keysA.length !== keysB.length) {
    return false;
  }

  // Test for A's keys different from B.
  var bHasOwnProperty = hasOwnProperty.bind(objB);
  for (var i = 0; i < keysA.length; i++) {
    if (!bHasOwnProperty(keysA[i]) || objA[keysA[i]] !== objB[keysA[i]]) {
      return false;
    }
  }

  return true;
}

module.exports = shallowEqual;
},{}],161:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule toArray
 * @typechecks
 */

'use strict';

var invariant = require('./invariant');

/**
 * Convert array-like objects to arrays.
 *
 * This API assumes the caller knows the contents of the data type. For less
 * well defined inputs use createArrayFromMixed.
 *
 * @param {object|function|filelist} obj
 * @return {array}
 */
function toArray(obj) {
  var length = obj.length;

  // Some browse builtin objects can report typeof 'function' (e.g. NodeList in
  // old versions of Safari).
  !(!Array.isArray(obj) && (typeof obj === 'object' || typeof obj === 'function')) ? "production" !== 'production' ? invariant(false, 'toArray: Array-like object expected') : invariant(false) : undefined;

  !(typeof length === 'number') ? "production" !== 'production' ? invariant(false, 'toArray: Object needs a length property') : invariant(false) : undefined;

  !(length === 0 || length - 1 in obj) ? "production" !== 'production' ? invariant(false, 'toArray: Object should have keys for indices') : invariant(false) : undefined;

  // Old IE doesn't give collections access to hasOwnProperty. Assume inputs
  // without method will throw during the slice call and skip straight to the
  // fallback.
  if (obj.hasOwnProperty) {
    try {
      return Array.prototype.slice.call(obj);
    } catch (e) {
      // IE < 9 does not support Array#slice on collections objects
    }
  }

  // Fall back to copying key by key. This assumes all keys have a value,
  // so will not preserve sparsely populated inputs.
  var ret = Array(length);
  for (var ii = 0; ii < length; ii++) {
    ret[ii] = obj[ii];
  }
  return ret;
}

module.exports = toArray;
},{"./invariant":151}],162:[function(require,module,exports){
/**
 * Copyright 2014-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule warning
 */

'use strict';

var emptyFunction = require('./emptyFunction');

/**
 * Similar to invariant but only logs a warning if the condition is not met.
 * This can be used to log issues in development environments in critical
 * paths. Removing the logging code for production environments will keep the
 * same logic and follow the same code paths.
 */

var warning = emptyFunction;

if ("production" !== 'production') {
  warning = function (condition, format) {
    for (var _len = arguments.length, args = Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
      args[_key - 2] = arguments[_key];
    }

    if (format === undefined) {
      throw new Error('`warning(condition, format, ...args)` requires a warning ' + 'message argument');
    }

    if (format.indexOf('Failed Composite propType: ') === 0) {
      return; // Ignore CompositeComponent proptype check.
    }

    if (!condition) {
      var argIndex = 0;
      var message = 'Warning: ' + format.replace(/%s/g, function () {
        return args[argIndex++];
      });
      if (typeof console !== 'undefined') {
        console.error(message);
      }
      try {
        // --- Welcome to debugging React ---
        // This error was thrown as a convenience so that you can use this stack
        // to find the callsite that caused this warning to fire.
        throw new Error(message);
      } catch (x) {}
    }
  };
}

module.exports = warning;
},{"./emptyFunction":143}],163:[function(require,module,exports){
(function (main) {
  'use strict';

  /**
   * Parse or format dates
   * @class fecha
   */
  var fecha = {};
  var token = /d{1,4}|M{1,4}|YY(?:YY)?|S{1,3}|Do|ZZ|([HhMsDm])\1?|[aA]|"[^"]*"|'[^']*'/g;
  var twoDigits = /\d\d?/;
  var threeDigits = /\d{3}/;
  var fourDigits = /\d{4}/;
  var word = /[0-9]*['a-z\u00A0-\u05FF\u0700-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF]+|[\u0600-\u06FF\/]+(\s*?[\u0600-\u06FF]+){1,2}/i;
  var noop = function () {
  };

  function shorten(arr, sLen) {
    var newArr = [];
    for (var i = 0, len = arr.length; i < len; i++) {
      newArr.push(arr[i].substr(0, sLen));
    }
    return newArr;
  }

  function monthUpdate(arrName) {
    return function (d, v, i18n) {
      var index = i18n[arrName].indexOf(v.charAt(0).toUpperCase() + v.substr(1).toLowerCase());
      if (~index) {
        d.month = index;
      }
    };
  }

  function pad(val, len) {
    val = String(val);
    len = len || 2;
    while (val.length < len) {
      val = '0' + val;
    }
    return val;
  }

  var dayNames = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];
  var monthNames = ['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'];
  var monthNamesShort = shorten(monthNames, 3);
  var dayNamesShort = shorten(dayNames, 3);
  fecha.i18n = {
    dayNamesShort: dayNamesShort,
    dayNames: dayNames,
    monthNamesShort: monthNamesShort,
    monthNames: monthNames,
    amPm: ['am', 'pm'],
    DoFn: function DoFn(D) {
      return D + ['th', 'st', 'nd', 'rd'][D % 10 > 3 ? 0 : (D - D % 10 !== 10) * D % 10];
    }
  };

  var formatFlags = {
    D: function(dateObj) {
      return dateObj.getDate();
    },
    DD: function(dateObj) {
      return pad(dateObj.getDate());
    },
    Do: function(dateObj, i18n) {
      return i18n.DoFn(dateObj.getDate());
    },
    d: function(dateObj) {
      return dateObj.getDay();
    },
    dd: function(dateObj) {
      return pad(dateObj.getDay());
    },
    ddd: function(dateObj, i18n) {
      return i18n.dayNamesShort[dateObj.getDay()];
    },
    dddd: function(dateObj, i18n) {
      return i18n.dayNames[dateObj.getDay()];
    },
    M: function(dateObj) {
      return dateObj.getMonth() + 1;
    },
    MM: function(dateObj) {
      return pad(dateObj.getMonth() + 1);
    },
    MMM: function(dateObj, i18n) {
      return i18n.monthNamesShort[dateObj.getMonth()];
    },
    MMMM: function(dateObj, i18n) {
      return i18n.monthNames[dateObj.getMonth()];
    },
    YY: function(dateObj) {
      return String(dateObj.getFullYear()).substr(2);
    },
    YYYY: function(dateObj) {
      return dateObj.getFullYear();
    },
    h: function(dateObj) {
      return dateObj.getHours() % 12 || 12;
    },
    hh: function(dateObj) {
      return pad(dateObj.getHours() % 12 || 12);
    },
    H: function(dateObj) {
      return dateObj.getHours();
    },
    HH: function(dateObj) {
      return pad(dateObj.getHours());
    },
    m: function(dateObj) {
      return dateObj.getMinutes();
    },
    mm: function(dateObj) {
      return pad(dateObj.getMinutes());
    },
    s: function(dateObj) {
      return dateObj.getSeconds();
    },
    ss: function(dateObj) {
      return pad(dateObj.getSeconds());
    },
    S: function(dateObj) {
      return Math.round(dateObj.getMilliseconds() / 100);
    },
    SS: function(dateObj) {
      return pad(Math.round(dateObj.getMilliseconds() / 10), 2);
    },
    SSS: function(dateObj) {
      return pad(dateObj.getMilliseconds(), 3);
    },
    a: function(dateObj, i18n) {
      return dateObj.getHours() < 12 ? i18n.amPm[0] : i18n.amPm[1];
    },
    A: function(dateObj, i18n) {
      return dateObj.getHours() < 12 ? i18n.amPm[0].toUpperCase() : i18n.amPm[1].toUpperCase();
    },
    ZZ: function(dateObj) {
      var o = dateObj.getTimezoneOffset();
      return (o > 0 ? '-' : '+') + pad(Math.floor(Math.abs(o) / 60) * 100 + Math.abs(o) % 60, 4);
    }
  };

  var parseFlags = {
    D: [twoDigits, function (d, v) {
      d.day = v;
    }],
    Do: [new RegExp(twoDigits.source + word.source), function (d, v) {
      d.day = parseInt(v, 10);
    }],
    M: [twoDigits, function (d, v) {
      d.month = v - 1;
    }],
    YY: [twoDigits, function (d, v) {
      var da = new Date(), cent = +('' + da.getFullYear()).substr(0, 2);
      d.year = '' + (v > 68 ? cent - 1 : cent) + v;
    }],
    h: [twoDigits, function (d, v) {
      d.hour = v;
    }],
    m: [twoDigits, function (d, v) {
      d.minute = v;
    }],
    s: [twoDigits, function (d, v) {
      d.second = v;
    }],
    YYYY: [fourDigits, function (d, v) {
      d.year = v;
    }],
    S: [/\d/, function (d, v) {
      d.millisecond = v * 100;
    }],
    SS: [/\d{2}/, function (d, v) {
      d.millisecond = v * 10;
    }],
    SSS: [threeDigits, function (d, v) {
      d.millisecond = v;
    }],
    d: [twoDigits, noop],
    ddd: [word, noop],
    MMM: [word, monthUpdate('monthNamesShort')],
    MMMM: [word, monthUpdate('monthNames')],
    a: [word, function (d, v, i18n) {
      var val = v.toLowerCase();
      if (val === i18n.amPm[0]) {
        d.isPm = false;
      } else if (val === i18n.amPm[1]) {
        d.isPm = true;
      }
    }],
    ZZ: [/[\+\-]\d\d:?\d\d/, function (d, v) {
      var parts = (v + '').match(/([\+\-]|\d\d)/gi), minutes;

      if (parts) {
        minutes = +(parts[1] * 60) + parseInt(parts[2], 10);
        d.timezoneOffset = parts[0] === '+' ? minutes : -minutes;
      }
    }]
  };
  parseFlags.dd = parseFlags.d;
  parseFlags.dddd = parseFlags.ddd;
  parseFlags.DD = parseFlags.D;
  parseFlags.mm = parseFlags.m;
  parseFlags.hh = parseFlags.H = parseFlags.HH = parseFlags.h;
  parseFlags.MM = parseFlags.M;
  parseFlags.ss = parseFlags.s;
  parseFlags.A = parseFlags.a;


  // Some common format strings
  fecha.masks = {
    'default': 'ddd MMM DD YYYY HH:mm:ss',
    shortDate: 'M/D/YY',
    mediumDate: 'MMM D, YYYY',
    longDate: 'MMMM D, YYYY',
    fullDate: 'dddd, MMMM D, YYYY',
    shortTime: 'HH:mm',
    mediumTime: 'HH:mm:ss',
    longTime: 'HH:mm:ss.SSS'
  };

  /***
   * Format a date
   * @method format
   * @param {Date|number} dateObj
   * @param {string} mask Format of the date, i.e. 'mm-dd-yy' or 'shortDate'
   */
  fecha.format = function (dateObj, mask, i18nSettings) {
    var i18n = i18nSettings || fecha.i18n;

    if (typeof dateObj === 'number') {
      dateObj = new Date(dateObj);
    }

    if (Object.prototype.toString.call(dateObj) !== '[object Date]' || isNaN(dateObj.getTime())) {
      throw new Error('Invalid Date in fecha.format');
    }

    mask = fecha.masks[mask] || mask || fecha.masks['default'];

    return mask.replace(token, function ($0) {
      return $0 in formatFlags ? formatFlags[$0](dateObj, i18n) : $0.slice(1, $0.length - 1);
    });
  };

  /**
   * Parse a date string into an object, changes - into /
   * @method parse
   * @param {string} dateStr Date string
   * @param {string} format Date parse format
   * @returns {Date|boolean}
   */
  fecha.parse = function (dateStr, format, i18nSettings) {
    var i18n = i18nSettings || fecha.i18n;

    if (typeof format !== 'string') {
      throw new Error('Invalid format in fecha.parse');
    }

    format = fecha.masks[format] || format;

    // Avoid regular expression denial of service, fail early for really long strings
    // https://www.owasp.org/index.php/Regular_expression_Denial_of_Service_-_ReDoS
    if (dateStr.length > 1000) {
      return false;
    }

    var isValid = true;
    var dateInfo = {};
    format.replace(token, function ($0) {
      if (parseFlags[$0]) {
        var info = parseFlags[$0];
        var index = dateStr.search(info[0]);
        if (!~index) {
          isValid = false;
        } else {
          dateStr.replace(info[0], function (result) {
            info[1](dateInfo, result, i18n);
            dateStr = dateStr.substr(index + result.length);
            return result;
          });
        }
      }

      return parseFlags[$0] ? '' : $0.slice(1, $0.length - 1);
    });

    if (!isValid) {
      return false;
    }

    var today = new Date();
    if (dateInfo.isPm === true && dateInfo.hour != null && +dateInfo.hour !== 12) {
      dateInfo.hour = +dateInfo.hour + 12;
    } else if (dateInfo.isPm === false && +dateInfo.hour === 12) {
      dateInfo.hour = 0;
    }

    var date;
    if (dateInfo.timezoneOffset != null) {
      dateInfo.minute = +(dateInfo.minute || 0) - +dateInfo.timezoneOffset;
      date = new Date(Date.UTC(dateInfo.year || today.getFullYear(), dateInfo.month || 0, dateInfo.day || 1,
        dateInfo.hour || 0, dateInfo.minute || 0, dateInfo.second || 0, dateInfo.millisecond || 0));
    } else {
      date = new Date(dateInfo.year || today.getFullYear(), dateInfo.month || 0, dateInfo.day || 1,
        dateInfo.hour || 0, dateInfo.minute || 0, dateInfo.second || 0, dateInfo.millisecond || 0);
    }
    return date;
  };

  /* istanbul ignore next */
  if (typeof module !== 'undefined' && module.exports) {
    module.exports = fecha;
  } else if (typeof define === 'function' && define.amd) {
    define(function () {
      return fecha;
    });
  } else {
    main.fecha = fecha;
  }
})(this);

},{}],164:[function(require,module,exports){
/*!
 * for-in <https://github.com/jonschlinkert/for-in>
 *
 * Copyright (c) 2014-2016, Jon Schlinkert.
 * Licensed under the MIT License.
 */

'use strict';

module.exports = function forIn(o, fn, thisArg) {
  for (var key in o) {
    if (fn.call(thisArg, o[key], key, o) === false) {
      break;
    }
  }
};

},{}],165:[function(require,module,exports){
/*!
 * for-own <https://github.com/jonschlinkert/for-own>
 *
 * Copyright (c) 2014-2016, Jon Schlinkert.
 * Licensed under the MIT License.
 */

'use strict';

var forIn = require('for-in');
var hasOwn = Object.prototype.hasOwnProperty;

module.exports = function forOwn(o, fn, thisArg) {
  forIn(o, function(val, key) {
    if (hasOwn.call(o, key)) {
      return fn.call(thisArg, o[key], key, o);
    }
  });
};

},{"for-in":164}],166:[function(require,module,exports){
/**
 * Indicates that navigation was caused by a call to history.push.
 */
'use strict';

exports.__esModule = true;
var PUSH = 'PUSH';

exports.PUSH = PUSH;
/**
 * Indicates that navigation was caused by a call to history.replace.
 */
var REPLACE = 'REPLACE';

exports.REPLACE = REPLACE;
/**
 * Indicates that navigation was caused by some other action such
 * as using a browser's back/forward buttons and/or manually manipulating
 * the URL in a browser's location bar. This is the default.
 *
 * See https://developer.mozilla.org/en-US/docs/Web/API/WindowEventHandlers/onpopstate
 * for more information.
 */
var POP = 'POP';

exports.POP = POP;
exports['default'] = {
  PUSH: PUSH,
  REPLACE: REPLACE,
  POP: POP
};
},{}],167:[function(require,module,exports){
"use strict";

exports.__esModule = true;
var _slice = Array.prototype.slice;
exports.loopAsync = loopAsync;

function loopAsync(turns, work, callback) {
  var currentTurn = 0,
      isDone = false;
  var sync = false,
      hasNext = false,
      doneArgs = undefined;

  function done() {
    isDone = true;
    if (sync) {
      // Iterate instead of recursing if possible.
      doneArgs = [].concat(_slice.call(arguments));
      return;
    }

    callback.apply(this, arguments);
  }

  function next() {
    if (isDone) {
      return;
    }

    hasNext = true;
    if (sync) {
      // Iterate instead of recursing if possible.
      return;
    }

    sync = true;

    while (!isDone && currentTurn < turns && hasNext) {
      hasNext = false;
      work.call(this, currentTurn++, next, done);
    }

    sync = false;

    if (isDone) {
      // This means the loop finished synchronously.
      callback.apply(this, doneArgs);
      return;
    }

    if (currentTurn >= turns && hasNext) {
      isDone = true;
      callback();
    }
  }

  next();
}
},{}],168:[function(require,module,exports){
(function (process){
/*eslint-disable no-empty */
'use strict';

exports.__esModule = true;
exports.saveState = saveState;
exports.readState = readState;

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

var _warning = require('warning');

var _warning2 = _interopRequireDefault(_warning);

var KeyPrefix = '@@History/';
var QuotaExceededErrors = ['QuotaExceededError', 'QUOTA_EXCEEDED_ERR'];

var SecurityError = 'SecurityError';

function createKey(key) {
  return KeyPrefix + key;
}

function saveState(key, state) {
  try {
    if (state == null) {
      window.sessionStorage.removeItem(createKey(key));
    } else {
      window.sessionStorage.setItem(createKey(key), JSON.stringify(state));
    }
  } catch (error) {
    if (error.name === SecurityError) {
      // Blocking cookies in Chrome/Firefox/Safari throws SecurityError on any
      // attempt to access window.sessionStorage.
      process.env.NODE_ENV !== 'production' ? _warning2['default'](false, '[history] Unable to save state; sessionStorage is not available due to security settings') : undefined;

      return;
    }

    if (QuotaExceededErrors.indexOf(error.name) >= 0 && window.sessionStorage.length === 0) {
      // Safari "private mode" throws QuotaExceededError.
      process.env.NODE_ENV !== 'production' ? _warning2['default'](false, '[history] Unable to save state; sessionStorage is not available in Safari private mode') : undefined;

      return;
    }

    throw error;
  }
}

function readState(key) {
  var json = undefined;
  try {
    json = window.sessionStorage.getItem(createKey(key));
  } catch (error) {
    if (error.name === SecurityError) {
      // Blocking cookies in Chrome/Firefox/Safari throws SecurityError on any
      // attempt to access window.sessionStorage.
      process.env.NODE_ENV !== 'production' ? _warning2['default'](false, '[history] Unable to read state; sessionStorage is not available due to security settings') : undefined;

      return null;
    }
  }

  if (json) {
    try {
      return JSON.parse(json);
    } catch (error) {
      // Ignore invalid JSON.
    }
  }

  return null;
}
}).call(this,require('_process'))
},{"_process":187,"warning":358}],169:[function(require,module,exports){
'use strict';

exports.__esModule = true;
exports.addEventListener = addEventListener;
exports.removeEventListener = removeEventListener;
exports.getHashPath = getHashPath;
exports.replaceHashPath = replaceHashPath;
exports.getWindowPath = getWindowPath;
exports.go = go;
exports.getUserConfirmation = getUserConfirmation;
exports.supportsHistory = supportsHistory;
exports.supportsGoWithoutReloadUsingHash = supportsGoWithoutReloadUsingHash;

function addEventListener(node, event, listener) {
  if (node.addEventListener) {
    node.addEventListener(event, listener, false);
  } else {
    node.attachEvent('on' + event, listener);
  }
}

function removeEventListener(node, event, listener) {
  if (node.removeEventListener) {
    node.removeEventListener(event, listener, false);
  } else {
    node.detachEvent('on' + event, listener);
  }
}

function getHashPath() {
  // We can't use window.location.hash here because it's not
  // consistent across browsers - Firefox will pre-decode it!
  return window.location.href.split('#')[1] || '';
}

function replaceHashPath(path) {
  window.location.replace(window.location.pathname + window.location.search + '#' + path);
}

function getWindowPath() {
  return window.location.pathname + window.location.search + window.location.hash;
}

function go(n) {
  if (n) window.history.go(n);
}

function getUserConfirmation(message, callback) {
  callback(window.confirm(message));
}

/**
 * Returns true if the HTML5 history API is supported. Taken from Modernizr.
 *
 * https://github.com/Modernizr/Modernizr/blob/master/LICENSE
 * https://github.com/Modernizr/Modernizr/blob/master/feature-detects/history.js
 * changed to avoid false negatives for Windows Phones: https://github.com/rackt/react-router/issues/586
 */

function supportsHistory() {
  var ua = navigator.userAgent;
  if ((ua.indexOf('Android 2.') !== -1 || ua.indexOf('Android 4.0') !== -1) && ua.indexOf('Mobile Safari') !== -1 && ua.indexOf('Chrome') === -1 && ua.indexOf('Windows Phone') === -1) {
    return false;
  }
  return window.history && 'pushState' in window.history;
}

/**
 * Returns false if using go(n) with hash history causes a full page reload.
 */

function supportsGoWithoutReloadUsingHash() {
  var ua = navigator.userAgent;
  return ua.indexOf('Firefox') === -1;
}
},{}],170:[function(require,module,exports){
'use strict';

exports.__esModule = true;
var canUseDOM = !!(typeof window !== 'undefined' && window.document && window.document.createElement);
exports.canUseDOM = canUseDOM;
},{}],171:[function(require,module,exports){
(function (process){
'use strict';

exports.__esModule = true;
exports.extractPath = extractPath;
exports.parsePath = parsePath;

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

var _warning = require('warning');

var _warning2 = _interopRequireDefault(_warning);

function extractPath(string) {
  var match = string.match(/^https?:\/\/[^\/]*/);

  if (match == null) return string;

  return string.substring(match[0].length);
}

function parsePath(path) {
  var pathname = extractPath(path);
  var search = '';
  var hash = '';

  process.env.NODE_ENV !== 'production' ? _warning2['default'](path === pathname, 'A path must be pathname + search + hash only, not a fully qualified URL like "%s"', path) : undefined;

  var hashIndex = pathname.indexOf('#');
  if (hashIndex !== -1) {
    hash = pathname.substring(hashIndex);
    pathname = pathname.substring(0, hashIndex);
  }

  var searchIndex = pathname.indexOf('?');
  if (searchIndex !== -1) {
    search = pathname.substring(searchIndex);
    pathname = pathname.substring(0, searchIndex);
  }

  if (pathname === '') pathname = '/';

  return {
    pathname: pathname,
    search: search,
    hash: hash
  };
}
}).call(this,require('_process'))
},{"_process":187,"warning":358}],172:[function(require,module,exports){
(function (process){
'use strict';

exports.__esModule = true;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

var _invariant = require('invariant');

var _invariant2 = _interopRequireDefault(_invariant);

var _Actions = require('./Actions');

var _PathUtils = require('./PathUtils');

var _ExecutionEnvironment = require('./ExecutionEnvironment');

var _DOMUtils = require('./DOMUtils');

var _DOMStateStorage = require('./DOMStateStorage');

var _createDOMHistory = require('./createDOMHistory');

var _createDOMHistory2 = _interopRequireDefault(_createDOMHistory);

/**
 * Creates and returns a history object that uses HTML5's history API
 * (pushState, replaceState, and the popstate event) to manage history.
 * This is the recommended method of managing history in browsers because
 * it provides the cleanest URLs.
 *
 * Note: In browsers that do not support the HTML5 history API full
 * page reloads will be used to preserve URLs.
 */
function createBrowserHistory() {
  var options = arguments.length <= 0 || arguments[0] === undefined ? {} : arguments[0];

  !_ExecutionEnvironment.canUseDOM ? process.env.NODE_ENV !== 'production' ? _invariant2['default'](false, 'Browser history needs a DOM') : _invariant2['default'](false) : undefined;

  var forceRefresh = options.forceRefresh;

  var isSupported = _DOMUtils.supportsHistory();
  var useRefresh = !isSupported || forceRefresh;

  function getCurrentLocation(historyState) {
    try {
      historyState = historyState || window.history.state || {};
    } catch (e) {
      historyState = {};
    }

    var path = _DOMUtils.getWindowPath();
    var _historyState = historyState;
    var key = _historyState.key;

    var state = undefined;
    if (key) {
      state = _DOMStateStorage.readState(key);
    } else {
      state = null;
      key = history.createKey();

      if (isSupported) window.history.replaceState(_extends({}, historyState, { key: key }), null);
    }

    var location = _PathUtils.parsePath(path);

    return history.createLocation(_extends({}, location, { state: state }), undefined, key);
  }

  function startPopStateListener(_ref) {
    var transitionTo = _ref.transitionTo;

    function popStateListener(event) {
      if (event.state === undefined) return; // Ignore extraneous popstate events in WebKit.

      transitionTo(getCurrentLocation(event.state));
    }

    _DOMUtils.addEventListener(window, 'popstate', popStateListener);

    return function () {
      _DOMUtils.removeEventListener(window, 'popstate', popStateListener);
    };
  }

  function finishTransition(location) {
    var basename = location.basename;
    var pathname = location.pathname;
    var search = location.search;
    var hash = location.hash;
    var state = location.state;
    var action = location.action;
    var key = location.key;

    if (action === _Actions.POP) return; // Nothing to do.

    _DOMStateStorage.saveState(key, state);

    var path = (basename || '') + pathname + search + hash;
    var historyState = {
      key: key
    };

    if (action === _Actions.PUSH) {
      if (useRefresh) {
        window.location.href = path;
        return false; // Prevent location update.
      } else {
          window.history.pushState(historyState, null, path);
        }
    } else {
      // REPLACE
      if (useRefresh) {
        window.location.replace(path);
        return false; // Prevent location update.
      } else {
          window.history.replaceState(historyState, null, path);
        }
    }
  }

  var history = _createDOMHistory2['default'](_extends({}, options, {
    getCurrentLocation: getCurrentLocation,
    finishTransition: finishTransition,
    saveState: _DOMStateStorage.saveState
  }));

  var listenerCount = 0,
      stopPopStateListener = undefined;

  function listenBefore(listener) {
    if (++listenerCount === 1) stopPopStateListener = startPopStateListener(history);

    var unlisten = history.listenBefore(listener);

    return function () {
      unlisten();

      if (--listenerCount === 0) stopPopStateListener();
    };
  }

  function listen(listener) {
    if (++listenerCount === 1) stopPopStateListener = startPopStateListener(history);

    var unlisten = history.listen(listener);

    return function () {
      unlisten();

      if (--listenerCount === 0) stopPopStateListener();
    };
  }

  // deprecated
  function registerTransitionHook(hook) {
    if (++listenerCount === 1) stopPopStateListener = startPopStateListener(history);

    history.registerTransitionHook(hook);
  }

  // deprecated
  function unregisterTransitionHook(hook) {
    history.unregisterTransitionHook(hook);

    if (--listenerCount === 0) stopPopStateListener();
  }

  return _extends({}, history, {
    listenBefore: listenBefore,
    listen: listen,
    registerTransitionHook: registerTransitionHook,
    unregisterTransitionHook: unregisterTransitionHook
  });
}

exports['default'] = createBrowserHistory;
module.exports = exports['default'];
}).call(this,require('_process'))
},{"./Actions":166,"./DOMStateStorage":168,"./DOMUtils":169,"./ExecutionEnvironment":170,"./PathUtils":171,"./createDOMHistory":173,"_process":187,"invariant":183}],173:[function(require,module,exports){
(function (process){
'use strict';

exports.__esModule = true;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

var _invariant = require('invariant');

var _invariant2 = _interopRequireDefault(_invariant);

var _ExecutionEnvironment = require('./ExecutionEnvironment');

var _DOMUtils = require('./DOMUtils');

var _createHistory = require('./createHistory');

var _createHistory2 = _interopRequireDefault(_createHistory);

function createDOMHistory(options) {
  var history = _createHistory2['default'](_extends({
    getUserConfirmation: _DOMUtils.getUserConfirmation
  }, options, {
    go: _DOMUtils.go
  }));

  function listen(listener) {
    !_ExecutionEnvironment.canUseDOM ? process.env.NODE_ENV !== 'production' ? _invariant2['default'](false, 'DOM history needs a DOM') : _invariant2['default'](false) : undefined;

    return history.listen(listener);
  }

  return _extends({}, history, {
    listen: listen
  });
}

exports['default'] = createDOMHistory;
module.exports = exports['default'];
}).call(this,require('_process'))
},{"./DOMUtils":169,"./ExecutionEnvironment":170,"./createHistory":175,"_process":187,"invariant":183}],174:[function(require,module,exports){
(function (process){
'use strict';

exports.__esModule = true;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

var _warning = require('warning');

var _warning2 = _interopRequireDefault(_warning);

var _invariant = require('invariant');

var _invariant2 = _interopRequireDefault(_invariant);

var _Actions = require('./Actions');

var _PathUtils = require('./PathUtils');

var _ExecutionEnvironment = require('./ExecutionEnvironment');

var _DOMUtils = require('./DOMUtils');

var _DOMStateStorage = require('./DOMStateStorage');

var _createDOMHistory = require('./createDOMHistory');

var _createDOMHistory2 = _interopRequireDefault(_createDOMHistory);

function isAbsolutePath(path) {
  return typeof path === 'string' && path.charAt(0) === '/';
}

function ensureSlash() {
  var path = _DOMUtils.getHashPath();

  if (isAbsolutePath(path)) return true;

  _DOMUtils.replaceHashPath('/' + path);

  return false;
}

function addQueryStringValueToPath(path, key, value) {
  return path + (path.indexOf('?') === -1 ? '?' : '&') + (key + '=' + value);
}

function stripQueryStringValueFromPath(path, key) {
  return path.replace(new RegExp('[?&]?' + key + '=[a-zA-Z0-9]+'), '');
}

function getQueryStringValueFromPath(path, key) {
  var match = path.match(new RegExp('\\?.*?\\b' + key + '=(.+?)\\b'));
  return match && match[1];
}

var DefaultQueryKey = '_k';

function createHashHistory() {
  var options = arguments.length <= 0 || arguments[0] === undefined ? {} : arguments[0];

  !_ExecutionEnvironment.canUseDOM ? process.env.NODE_ENV !== 'production' ? _invariant2['default'](false, 'Hash history needs a DOM') : _invariant2['default'](false) : undefined;

  var queryKey = options.queryKey;

  if (queryKey === undefined || !!queryKey) queryKey = typeof queryKey === 'string' ? queryKey : DefaultQueryKey;

  function getCurrentLocation() {
    var path = _DOMUtils.getHashPath();

    var key = undefined,
        state = undefined;
    if (queryKey) {
      key = getQueryStringValueFromPath(path, queryKey);
      path = stripQueryStringValueFromPath(path, queryKey);

      if (key) {
        state = _DOMStateStorage.readState(key);
      } else {
        state = null;
        key = history.createKey();
        _DOMUtils.replaceHashPath(addQueryStringValueToPath(path, queryKey, key));
      }
    } else {
      key = state = null;
    }

    var location = _PathUtils.parsePath(path);

    return history.createLocation(_extends({}, location, { state: state }), undefined, key);
  }

  function startHashChangeListener(_ref) {
    var transitionTo = _ref.transitionTo;

    function hashChangeListener() {
      if (!ensureSlash()) return; // Always make sure hashes are preceeded with a /.

      transitionTo(getCurrentLocation());
    }

    ensureSlash();
    _DOMUtils.addEventListener(window, 'hashchange', hashChangeListener);

    return function () {
      _DOMUtils.removeEventListener(window, 'hashchange', hashChangeListener);
    };
  }

  function finishTransition(location) {
    var basename = location.basename;
    var pathname = location.pathname;
    var search = location.search;
    var state = location.state;
    var action = location.action;
    var key = location.key;

    if (action === _Actions.POP) return; // Nothing to do.

    var path = (basename || '') + pathname + search;

    if (queryKey) {
      path = addQueryStringValueToPath(path, queryKey, key);
      _DOMStateStorage.saveState(key, state);
    } else {
      // Drop key and state.
      location.key = location.state = null;
    }

    var currentHash = _DOMUtils.getHashPath();

    if (action === _Actions.PUSH) {
      if (currentHash !== path) {
        window.location.hash = path;
      } else {
        process.env.NODE_ENV !== 'production' ? _warning2['default'](false, 'You cannot PUSH the same path using hash history') : undefined;
      }
    } else if (currentHash !== path) {
      // REPLACE
      _DOMUtils.replaceHashPath(path);
    }
  }

  var history = _createDOMHistory2['default'](_extends({}, options, {
    getCurrentLocation: getCurrentLocation,
    finishTransition: finishTransition,
    saveState: _DOMStateStorage.saveState
  }));

  var listenerCount = 0,
      stopHashChangeListener = undefined;

  function listenBefore(listener) {
    if (++listenerCount === 1) stopHashChangeListener = startHashChangeListener(history);

    var unlisten = history.listenBefore(listener);

    return function () {
      unlisten();

      if (--listenerCount === 0) stopHashChangeListener();
    };
  }

  function listen(listener) {
    if (++listenerCount === 1) stopHashChangeListener = startHashChangeListener(history);

    var unlisten = history.listen(listener);

    return function () {
      unlisten();

      if (--listenerCount === 0) stopHashChangeListener();
    };
  }

  function push(location) {
    process.env.NODE_ENV !== 'production' ? _warning2['default'](queryKey || location.state == null, 'You cannot use state without a queryKey it will be dropped') : undefined;

    history.push(location);
  }

  function replace(location) {
    process.env.NODE_ENV !== 'production' ? _warning2['default'](queryKey || location.state == null, 'You cannot use state without a queryKey it will be dropped') : undefined;

    history.replace(location);
  }

  var goIsSupportedWithoutReload = _DOMUtils.supportsGoWithoutReloadUsingHash();

  function go(n) {
    process.env.NODE_ENV !== 'production' ? _warning2['default'](goIsSupportedWithoutReload, 'Hash history go(n) causes a full page reload in this browser') : undefined;

    history.go(n);
  }

  function createHref(path) {
    return '#' + history.createHref(path);
  }

  // deprecated
  function registerTransitionHook(hook) {
    if (++listenerCount === 1) stopHashChangeListener = startHashChangeListener(history);

    history.registerTransitionHook(hook);
  }

  // deprecated
  function unregisterTransitionHook(hook) {
    history.unregisterTransitionHook(hook);

    if (--listenerCount === 0) stopHashChangeListener();
  }

  // deprecated
  function pushState(state, path) {
    process.env.NODE_ENV !== 'production' ? _warning2['default'](queryKey || state == null, 'You cannot use state without a queryKey it will be dropped') : undefined;

    history.pushState(state, path);
  }

  // deprecated
  function replaceState(state, path) {
    process.env.NODE_ENV !== 'production' ? _warning2['default'](queryKey || state == null, 'You cannot use state without a queryKey it will be dropped') : undefined;

    history.replaceState(state, path);
  }

  return _extends({}, history, {
    listenBefore: listenBefore,
    listen: listen,
    push: push,
    replace: replace,
    go: go,
    createHref: createHref,

    registerTransitionHook: registerTransitionHook, // deprecated - warning is in createHistory
    unregisterTransitionHook: unregisterTransitionHook, // deprecated - warning is in createHistory
    pushState: pushState, // deprecated - warning is in createHistory
    replaceState: replaceState // deprecated - warning is in createHistory
  });
}

exports['default'] = createHashHistory;
module.exports = exports['default'];
}).call(this,require('_process'))
},{"./Actions":166,"./DOMStateStorage":168,"./DOMUtils":169,"./ExecutionEnvironment":170,"./PathUtils":171,"./createDOMHistory":173,"_process":187,"invariant":183,"warning":358}],175:[function(require,module,exports){
(function (process){
'use strict';

exports.__esModule = true;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

var _warning = require('warning');

var _warning2 = _interopRequireDefault(_warning);

var _deepEqual = require('deep-equal');

var _deepEqual2 = _interopRequireDefault(_deepEqual);

var _PathUtils = require('./PathUtils');

var _AsyncUtils = require('./AsyncUtils');

var _Actions = require('./Actions');

var _createLocation2 = require('./createLocation');

var _createLocation3 = _interopRequireDefault(_createLocation2);

var _runTransitionHook = require('./runTransitionHook');

var _runTransitionHook2 = _interopRequireDefault(_runTransitionHook);

var _deprecate = require('./deprecate');

var _deprecate2 = _interopRequireDefault(_deprecate);

function createRandomKey(length) {
  return Math.random().toString(36).substr(2, length);
}

function locationsAreEqual(a, b) {
  return a.pathname === b.pathname && a.search === b.search &&
  //a.action === b.action && // Different action !== location change.
  a.key === b.key && _deepEqual2['default'](a.state, b.state);
}

var DefaultKeyLength = 6;

function createHistory() {
  var options = arguments.length <= 0 || arguments[0] === undefined ? {} : arguments[0];
  var getCurrentLocation = options.getCurrentLocation;
  var finishTransition = options.finishTransition;
  var saveState = options.saveState;
  var go = options.go;
  var getUserConfirmation = options.getUserConfirmation;
  var keyLength = options.keyLength;

  if (typeof keyLength !== 'number') keyLength = DefaultKeyLength;

  var transitionHooks = [];

  function listenBefore(hook) {
    transitionHooks.push(hook);

    return function () {
      transitionHooks = transitionHooks.filter(function (item) {
        return item !== hook;
      });
    };
  }

  var allKeys = [];
  var changeListeners = [];
  var location = undefined;

  function getCurrent() {
    if (pendingLocation && pendingLocation.action === _Actions.POP) {
      return allKeys.indexOf(pendingLocation.key);
    } else if (location) {
      return allKeys.indexOf(location.key);
    } else {
      return -1;
    }
  }

  function updateLocation(newLocation) {
    var current = getCurrent();

    location = newLocation;

    if (location.action === _Actions.PUSH) {
      allKeys = [].concat(allKeys.slice(0, current + 1), [location.key]);
    } else if (location.action === _Actions.REPLACE) {
      allKeys[current] = location.key;
    }

    changeListeners.forEach(function (listener) {
      listener(location);
    });
  }

  function listen(listener) {
    changeListeners.push(listener);

    if (location) {
      listener(location);
    } else {
      var _location = getCurrentLocation();
      allKeys = [_location.key];
      updateLocation(_location);
    }

    return function () {
      changeListeners = changeListeners.filter(function (item) {
        return item !== listener;
      });
    };
  }

  function confirmTransitionTo(location, callback) {
    _AsyncUtils.loopAsync(transitionHooks.length, function (index, next, done) {
      _runTransitionHook2['default'](transitionHooks[index], location, function (result) {
        if (result != null) {
          done(result);
        } else {
          next();
        }
      });
    }, function (message) {
      if (getUserConfirmation && typeof message === 'string') {
        getUserConfirmation(message, function (ok) {
          callback(ok !== false);
        });
      } else {
        callback(message !== false);
      }
    });
  }

  var pendingLocation = undefined;

  function transitionTo(nextLocation) {
    if (location && locationsAreEqual(location, nextLocation)) return; // Nothing to do.

    pendingLocation = nextLocation;

    confirmTransitionTo(nextLocation, function (ok) {
      if (pendingLocation !== nextLocation) return; // Transition was interrupted.

      if (ok) {
        // treat PUSH to current path like REPLACE to be consistent with browsers
        if (nextLocation.action === _Actions.PUSH) {
          var prevPath = createPath(location);
          var nextPath = createPath(nextLocation);

          if (nextPath === prevPath && _deepEqual2['default'](location.state, nextLocation.state)) nextLocation.action = _Actions.REPLACE;
        }

        if (finishTransition(nextLocation) !== false) updateLocation(nextLocation);
      } else if (location && nextLocation.action === _Actions.POP) {
        var prevIndex = allKeys.indexOf(location.key);
        var nextIndex = allKeys.indexOf(nextLocation.key);

        if (prevIndex !== -1 && nextIndex !== -1) go(prevIndex - nextIndex); // Restore the URL.
      }
    });
  }

  function push(location) {
    transitionTo(createLocation(location, _Actions.PUSH, createKey()));
  }

  function replace(location) {
    transitionTo(createLocation(location, _Actions.REPLACE, createKey()));
  }

  function goBack() {
    go(-1);
  }

  function goForward() {
    go(1);
  }

  function createKey() {
    return createRandomKey(keyLength);
  }

  function createPath(location) {
    if (location == null || typeof location === 'string') return location;

    var pathname = location.pathname;
    var search = location.search;
    var hash = location.hash;

    var result = pathname;

    if (search) result += search;

    if (hash) result += hash;

    return result;
  }

  function createHref(location) {
    return createPath(location);
  }

  function createLocation(location, action) {
    var key = arguments.length <= 2 || arguments[2] === undefined ? createKey() : arguments[2];

    if (typeof action === 'object') {
      process.env.NODE_ENV !== 'production' ? _warning2['default'](false, 'The state (2nd) argument to history.createLocation is deprecated; use a ' + 'location descriptor instead') : undefined;

      if (typeof location === 'string') location = _PathUtils.parsePath(location);

      location = _extends({}, location, { state: action });

      action = key;
      key = arguments[3] || createKey();
    }

    return _createLocation3['default'](location, action, key);
  }

  // deprecated
  function setState(state) {
    if (location) {
      updateLocationState(location, state);
      updateLocation(location);
    } else {
      updateLocationState(getCurrentLocation(), state);
    }
  }

  function updateLocationState(location, state) {
    location.state = _extends({}, location.state, state);
    saveState(location.key, location.state);
  }

  // deprecated
  function registerTransitionHook(hook) {
    if (transitionHooks.indexOf(hook) === -1) transitionHooks.push(hook);
  }

  // deprecated
  function unregisterTransitionHook(hook) {
    transitionHooks = transitionHooks.filter(function (item) {
      return item !== hook;
    });
  }

  // deprecated
  function pushState(state, path) {
    if (typeof path === 'string') path = _PathUtils.parsePath(path);

    push(_extends({ state: state }, path));
  }

  // deprecated
  function replaceState(state, path) {
    if (typeof path === 'string') path = _PathUtils.parsePath(path);

    replace(_extends({ state: state }, path));
  }

  return {
    listenBefore: listenBefore,
    listen: listen,
    transitionTo: transitionTo,
    push: push,
    replace: replace,
    go: go,
    goBack: goBack,
    goForward: goForward,
    createKey: createKey,
    createPath: createPath,
    createHref: createHref,
    createLocation: createLocation,

    setState: _deprecate2['default'](setState, 'setState is deprecated; use location.key to save state instead'),
    registerTransitionHook: _deprecate2['default'](registerTransitionHook, 'registerTransitionHook is deprecated; use listenBefore instead'),
    unregisterTransitionHook: _deprecate2['default'](unregisterTransitionHook, 'unregisterTransitionHook is deprecated; use the callback returned from listenBefore instead'),
    pushState: _deprecate2['default'](pushState, 'pushState is deprecated; use push instead'),
    replaceState: _deprecate2['default'](replaceState, 'replaceState is deprecated; use replace instead')
  };
}

exports['default'] = createHistory;
module.exports = exports['default'];
}).call(this,require('_process'))
},{"./Actions":166,"./AsyncUtils":167,"./PathUtils":171,"./createLocation":176,"./deprecate":178,"./runTransitionHook":179,"_process":187,"deep-equal":133,"warning":358}],176:[function(require,module,exports){
(function (process){
'use strict';

exports.__esModule = true;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

var _warning = require('warning');

var _warning2 = _interopRequireDefault(_warning);

var _Actions = require('./Actions');

var _PathUtils = require('./PathUtils');

function createLocation() {
  var location = arguments.length <= 0 || arguments[0] === undefined ? '/' : arguments[0];
  var action = arguments.length <= 1 || arguments[1] === undefined ? _Actions.POP : arguments[1];
  var key = arguments.length <= 2 || arguments[2] === undefined ? null : arguments[2];

  var _fourthArg = arguments.length <= 3 || arguments[3] === undefined ? null : arguments[3];

  if (typeof location === 'string') location = _PathUtils.parsePath(location);

  if (typeof action === 'object') {
    process.env.NODE_ENV !== 'production' ? _warning2['default'](false, 'The state (2nd) argument to createLocation is deprecated; use a ' + 'location descriptor instead') : undefined;

    location = _extends({}, location, { state: action });

    action = key || _Actions.POP;
    key = _fourthArg;
  }

  var pathname = location.pathname || '/';
  var search = location.search || '';
  var hash = location.hash || '';
  var state = location.state || null;

  return {
    pathname: pathname,
    search: search,
    hash: hash,
    state: state,
    action: action,
    key: key
  };
}

exports['default'] = createLocation;
module.exports = exports['default'];
}).call(this,require('_process'))
},{"./Actions":166,"./PathUtils":171,"_process":187,"warning":358}],177:[function(require,module,exports){
(function (process){
'use strict';

exports.__esModule = true;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

var _warning = require('warning');

var _warning2 = _interopRequireDefault(_warning);

var _invariant = require('invariant');

var _invariant2 = _interopRequireDefault(_invariant);

var _PathUtils = require('./PathUtils');

var _Actions = require('./Actions');

var _createHistory = require('./createHistory');

var _createHistory2 = _interopRequireDefault(_createHistory);

function createStateStorage(entries) {
  return entries.filter(function (entry) {
    return entry.state;
  }).reduce(function (memo, entry) {
    memo[entry.key] = entry.state;
    return memo;
  }, {});
}

function createMemoryHistory() {
  var options = arguments.length <= 0 || arguments[0] === undefined ? {} : arguments[0];

  if (Array.isArray(options)) {
    options = { entries: options };
  } else if (typeof options === 'string') {
    options = { entries: [options] };
  }

  var history = _createHistory2['default'](_extends({}, options, {
    getCurrentLocation: getCurrentLocation,
    finishTransition: finishTransition,
    saveState: saveState,
    go: go
  }));

  var _options = options;
  var entries = _options.entries;
  var current = _options.current;

  if (typeof entries === 'string') {
    entries = [entries];
  } else if (!Array.isArray(entries)) {
    entries = ['/'];
  }

  entries = entries.map(function (entry) {
    var key = history.createKey();

    if (typeof entry === 'string') return { pathname: entry, key: key };

    if (typeof entry === 'object' && entry) return _extends({}, entry, { key: key });

    !false ? process.env.NODE_ENV !== 'production' ? _invariant2['default'](false, 'Unable to create history entry from %s', entry) : _invariant2['default'](false) : undefined;
  });

  if (current == null) {
    current = entries.length - 1;
  } else {
    !(current >= 0 && current < entries.length) ? process.env.NODE_ENV !== 'production' ? _invariant2['default'](false, 'Current index must be >= 0 and < %s, was %s', entries.length, current) : _invariant2['default'](false) : undefined;
  }

  var storage = createStateStorage(entries);

  function saveState(key, state) {
    storage[key] = state;
  }

  function readState(key) {
    return storage[key];
  }

  function getCurrentLocation() {
    var entry = entries[current];
    var basename = entry.basename;
    var pathname = entry.pathname;
    var search = entry.search;

    var path = (basename || '') + pathname + (search || '');

    var key = undefined,
        state = undefined;
    if (entry.key) {
      key = entry.key;
      state = readState(key);
    } else {
      key = history.createKey();
      state = null;
      entry.key = key;
    }

    var location = _PathUtils.parsePath(path);

    return history.createLocation(_extends({}, location, { state: state }), undefined, key);
  }

  function canGo(n) {
    var index = current + n;
    return index >= 0 && index < entries.length;
  }

  function go(n) {
    if (n) {
      if (!canGo(n)) {
        process.env.NODE_ENV !== 'production' ? _warning2['default'](false, 'Cannot go(%s) there is not enough history', n) : undefined;
        return;
      }

      current += n;

      var currentLocation = getCurrentLocation();

      // change action to POP
      history.transitionTo(_extends({}, currentLocation, { action: _Actions.POP }));
    }
  }

  function finishTransition(location) {
    switch (location.action) {
      case _Actions.PUSH:
        current += 1;

        // if we are not on the top of stack
        // remove rest and push new
        if (current < entries.length) entries.splice(current);

        entries.push(location);
        saveState(location.key, location.state);
        break;
      case _Actions.REPLACE:
        entries[current] = location;
        saveState(location.key, location.state);
        break;
    }
  }

  return history;
}

exports['default'] = createMemoryHistory;
module.exports = exports['default'];
}).call(this,require('_process'))
},{"./Actions":166,"./PathUtils":171,"./createHistory":175,"_process":187,"invariant":183,"warning":358}],178:[function(require,module,exports){
(function (process){
'use strict';

exports.__esModule = true;

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

var _warning = require('warning');

var _warning2 = _interopRequireDefault(_warning);

function deprecate(fn, message) {
  return function () {
    process.env.NODE_ENV !== 'production' ? _warning2['default'](false, '[history] ' + message) : undefined;
    return fn.apply(this, arguments);
  };
}

exports['default'] = deprecate;
module.exports = exports['default'];
}).call(this,require('_process'))
},{"_process":187,"warning":358}],179:[function(require,module,exports){
(function (process){
'use strict';

exports.__esModule = true;

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

var _warning = require('warning');

var _warning2 = _interopRequireDefault(_warning);

function runTransitionHook(hook, location, callback) {
  var result = hook(location, callback);

  if (hook.length < 2) {
    // Assume the hook runs synchronously and automatically
    // call the callback with the return value.
    callback(result);
  } else {
    process.env.NODE_ENV !== 'production' ? _warning2['default'](result === undefined, 'You should not "return" in a transition hook with a callback argument; call the callback instead') : undefined;
  }
}

exports['default'] = runTransitionHook;
module.exports = exports['default'];
}).call(this,require('_process'))
},{"_process":187,"warning":358}],180:[function(require,module,exports){
(function (process){
'use strict';

exports.__esModule = true;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

var _warning = require('warning');

var _warning2 = _interopRequireDefault(_warning);

var _ExecutionEnvironment = require('./ExecutionEnvironment');

var _PathUtils = require('./PathUtils');

var _runTransitionHook = require('./runTransitionHook');

var _runTransitionHook2 = _interopRequireDefault(_runTransitionHook);

var _deprecate = require('./deprecate');

var _deprecate2 = _interopRequireDefault(_deprecate);

function useBasename(createHistory) {
  return function () {
    var options = arguments.length <= 0 || arguments[0] === undefined ? {} : arguments[0];

    var history = createHistory(options);

    var basename = options.basename;

    var checkedBaseHref = false;

    function checkBaseHref() {
      if (checkedBaseHref) {
        return;
      }

      // Automatically use the value of <base href> in HTML
      // documents as basename if it's not explicitly given.
      if (basename == null && _ExecutionEnvironment.canUseDOM) {
        var base = document.getElementsByTagName('base')[0];
        var baseHref = base && base.getAttribute('href');

        if (baseHref != null) {
          basename = baseHref;

          process.env.NODE_ENV !== 'production' ? _warning2['default'](false, 'Automatically setting basename using <base href> is deprecated and will ' + 'be removed in the next major release. The semantics of <base href> are ' + 'subtly different from basename. Please pass the basename explicitly in ' + 'the options to createHistory') : undefined;
        }
      }

      checkedBaseHref = true;
    }

    function addBasename(location) {
      checkBaseHref();

      if (basename && location.basename == null) {
        if (location.pathname.indexOf(basename) === 0) {
          location.pathname = location.pathname.substring(basename.length);
          location.basename = basename;

          if (location.pathname === '') location.pathname = '/';
        } else {
          location.basename = '';
        }
      }

      return location;
    }

    function prependBasename(location) {
      checkBaseHref();

      if (!basename) return location;

      if (typeof location === 'string') location = _PathUtils.parsePath(location);

      var pname = location.pathname;
      var normalizedBasename = basename.slice(-1) === '/' ? basename : basename + '/';
      var normalizedPathname = pname.charAt(0) === '/' ? pname.slice(1) : pname;
      var pathname = normalizedBasename + normalizedPathname;

      return _extends({}, location, {
        pathname: pathname
      });
    }

    // Override all read methods with basename-aware versions.
    function listenBefore(hook) {
      return history.listenBefore(function (location, callback) {
        _runTransitionHook2['default'](hook, addBasename(location), callback);
      });
    }

    function listen(listener) {
      return history.listen(function (location) {
        listener(addBasename(location));
      });
    }

    // Override all write methods with basename-aware versions.
    function push(location) {
      history.push(prependBasename(location));
    }

    function replace(location) {
      history.replace(prependBasename(location));
    }

    function createPath(location) {
      return history.createPath(prependBasename(location));
    }

    function createHref(location) {
      return history.createHref(prependBasename(location));
    }

    function createLocation(location) {
      for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
        args[_key - 1] = arguments[_key];
      }

      return addBasename(history.createLocation.apply(history, [prependBasename(location)].concat(args)));
    }

    // deprecated
    function pushState(state, path) {
      if (typeof path === 'string') path = _PathUtils.parsePath(path);

      push(_extends({ state: state }, path));
    }

    // deprecated
    function replaceState(state, path) {
      if (typeof path === 'string') path = _PathUtils.parsePath(path);

      replace(_extends({ state: state }, path));
    }

    return _extends({}, history, {
      listenBefore: listenBefore,
      listen: listen,
      push: push,
      replace: replace,
      createPath: createPath,
      createHref: createHref,
      createLocation: createLocation,

      pushState: _deprecate2['default'](pushState, 'pushState is deprecated; use push instead'),
      replaceState: _deprecate2['default'](replaceState, 'replaceState is deprecated; use replace instead')
    });
  };
}

exports['default'] = useBasename;
module.exports = exports['default'];
}).call(this,require('_process'))
},{"./ExecutionEnvironment":170,"./PathUtils":171,"./deprecate":178,"./runTransitionHook":179,"_process":187,"warning":358}],181:[function(require,module,exports){
(function (process){
'use strict';

exports.__esModule = true;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

var _warning = require('warning');

var _warning2 = _interopRequireDefault(_warning);

var _queryString = require('query-string');

var _runTransitionHook = require('./runTransitionHook');

var _runTransitionHook2 = _interopRequireDefault(_runTransitionHook);

var _PathUtils = require('./PathUtils');

var _deprecate = require('./deprecate');

var _deprecate2 = _interopRequireDefault(_deprecate);

var SEARCH_BASE_KEY = '$searchBase';

function defaultStringifyQuery(query) {
  return _queryString.stringify(query).replace(/%20/g, '+');
}

var defaultParseQueryString = _queryString.parse;

function isNestedObject(object) {
  for (var p in object) {
    if (Object.prototype.hasOwnProperty.call(object, p) && typeof object[p] === 'object' && !Array.isArray(object[p]) && object[p] !== null) return true;
  }return false;
}

/**
 * Returns a new createHistory function that may be used to create
 * history objects that know how to handle URL queries.
 */
function useQueries(createHistory) {
  return function () {
    var options = arguments.length <= 0 || arguments[0] === undefined ? {} : arguments[0];

    var history = createHistory(options);

    var stringifyQuery = options.stringifyQuery;
    var parseQueryString = options.parseQueryString;

    if (typeof stringifyQuery !== 'function') stringifyQuery = defaultStringifyQuery;

    if (typeof parseQueryString !== 'function') parseQueryString = defaultParseQueryString;

    function addQuery(location) {
      if (location.query == null) {
        var search = location.search;

        location.query = parseQueryString(search.substring(1));
        location[SEARCH_BASE_KEY] = { search: search, searchBase: '' };
      }

      // TODO: Instead of all the book-keeping here, this should just strip the
      // stringified query from the search.

      return location;
    }

    function appendQuery(location, query) {
      var _extends2;

      var searchBaseSpec = location[SEARCH_BASE_KEY];
      var queryString = query ? stringifyQuery(query) : '';
      if (!searchBaseSpec && !queryString) {
        return location;
      }

      process.env.NODE_ENV !== 'production' ? _warning2['default'](stringifyQuery !== defaultStringifyQuery || !isNestedObject(query), 'useQueries does not stringify nested query objects by default; ' + 'use a custom stringifyQuery function') : undefined;

      if (typeof location === 'string') location = _PathUtils.parsePath(location);

      var searchBase = undefined;
      if (searchBaseSpec && location.search === searchBaseSpec.search) {
        searchBase = searchBaseSpec.searchBase;
      } else {
        searchBase = location.search || '';
      }

      var search = searchBase;
      if (queryString) {
        search += (search ? '&' : '?') + queryString;
      }

      return _extends({}, location, (_extends2 = {
        search: search
      }, _extends2[SEARCH_BASE_KEY] = { search: search, searchBase: searchBase }, _extends2));
    }

    // Override all read methods with query-aware versions.
    function listenBefore(hook) {
      return history.listenBefore(function (location, callback) {
        _runTransitionHook2['default'](hook, addQuery(location), callback);
      });
    }

    function listen(listener) {
      return history.listen(function (location) {
        listener(addQuery(location));
      });
    }

    // Override all write methods with query-aware versions.
    function push(location) {
      history.push(appendQuery(location, location.query));
    }

    function replace(location) {
      history.replace(appendQuery(location, location.query));
    }

    function createPath(location, query) {
      process.env.NODE_ENV !== 'production' ? _warning2['default'](!query, 'the query argument to createPath is deprecated; use a location descriptor instead') : undefined;

      return history.createPath(appendQuery(location, query || location.query));
    }

    function createHref(location, query) {
      process.env.NODE_ENV !== 'production' ? _warning2['default'](!query, 'the query argument to createHref is deprecated; use a location descriptor instead') : undefined;

      return history.createHref(appendQuery(location, query || location.query));
    }

    function createLocation(location) {
      for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
        args[_key - 1] = arguments[_key];
      }

      var fullLocation = history.createLocation.apply(history, [appendQuery(location, location.query)].concat(args));
      if (location.query) {
        fullLocation.query = location.query;
      }
      return addQuery(fullLocation);
    }

    // deprecated
    function pushState(state, path, query) {
      if (typeof path === 'string') path = _PathUtils.parsePath(path);

      push(_extends({ state: state }, path, { query: query }));
    }

    // deprecated
    function replaceState(state, path, query) {
      if (typeof path === 'string') path = _PathUtils.parsePath(path);

      replace(_extends({ state: state }, path, { query: query }));
    }

    return _extends({}, history, {
      listenBefore: listenBefore,
      listen: listen,
      push: push,
      replace: replace,
      createPath: createPath,
      createHref: createHref,
      createLocation: createLocation,

      pushState: _deprecate2['default'](pushState, 'pushState is deprecated; use push instead'),
      replaceState: _deprecate2['default'](replaceState, 'replaceState is deprecated; use replace instead')
    });
  };
}

exports['default'] = useQueries;
module.exports = exports['default'];
}).call(this,require('_process'))
},{"./PathUtils":171,"./deprecate":178,"./runTransitionHook":179,"_process":187,"query-string":188,"warning":358}],182:[function(require,module,exports){
/**
 * Copyright 2015, Yahoo! Inc.
 * Copyrights licensed under the New BSD License. See the accompanying LICENSE file for terms.
 */
'use strict';

var REACT_STATICS = {
    childContextTypes: true,
    contextTypes: true,
    defaultProps: true,
    displayName: true,
    getDefaultProps: true,
    mixins: true,
    propTypes: true,
    type: true
};

var KNOWN_STATICS = {
    name: true,
    length: true,
    prototype: true,
    caller: true,
    arguments: true,
    arity: true
};

var isGetOwnPropertySymbolsAvailable = typeof Object.getOwnPropertySymbols === 'function';

module.exports = function hoistNonReactStatics(targetComponent, sourceComponent, customStatics) {
    if (typeof sourceComponent !== 'string') { // don't hoist over string (html) components
        var keys = Object.getOwnPropertyNames(sourceComponent);

        /* istanbul ignore else */
        if (isGetOwnPropertySymbolsAvailable) {
            keys = keys.concat(Object.getOwnPropertySymbols(sourceComponent));
        }

        for (var i = 0; i < keys.length; ++i) {
            if (!REACT_STATICS[keys[i]] && !KNOWN_STATICS[keys[i]] && (!customStatics || !customStatics[keys[i]])) {
                try {
                    targetComponent[keys[i]] = sourceComponent[keys[i]];
                } catch (error) {

                }
            }
        }
    }

    return targetComponent;
};

},{}],183:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 */

'use strict';

/**
 * Use invariant() to assert state which your program assumes to be true.
 *
 * Provide sprintf-style format (only %s is supported) and arguments
 * to provide information about what broke and what you were
 * expecting.
 *
 * The invariant message will be stripped in production, but the invariant
 * will remain to ensure logic does not differ in production.
 */

var invariant = function(condition, format, a, b, c, d, e, f) {
  if ("production" !== 'production') {
    if (format === undefined) {
      throw new Error('invariant requires an error message argument');
    }
  }

  if (!condition) {
    var error;
    if (format === undefined) {
      error = new Error(
        'Minified exception occurred; use the non-minified dev environment ' +
        'for the full error message and additional helpful warnings.'
      );
    } else {
      var args = [a, b, c, d, e, f];
      var argIndex = 0;
      error = new Error(
        format.replace(/%s/g, function() { return args[argIndex++]; })
      );
      error.name = 'Invariant Violation';
    }

    error.framesToPop = 1; // we don't care about invariant's own frame
    throw error;
  }
};

module.exports = invariant;

},{}],184:[function(require,module,exports){
/*!
 * is-extendable <https://github.com/jonschlinkert/is-extendable>
 *
 * Copyright (c) 2015, Jon Schlinkert.
 * Licensed under the MIT License.
 */

'use strict';

module.exports = function isExtendable(val) {
  return typeof val !== 'undefined' && val !== null
    && (typeof val === 'object' || typeof val === 'function');
};

},{}],185:[function(require,module,exports){
'use strict';
/* eslint-disable no-unused-vars */
var hasOwnProperty = Object.prototype.hasOwnProperty;
var propIsEnumerable = Object.prototype.propertyIsEnumerable;

function toObject(val) {
	if (val === null || val === undefined) {
		throw new TypeError('Object.assign cannot be called with null or undefined');
	}

	return Object(val);
}

function shouldUseNative() {
	try {
		if (!Object.assign) {
			return false;
		}

		// Detect buggy property enumeration order in older V8 versions.

		// https://bugs.chromium.org/p/v8/issues/detail?id=4118
		var test1 = new String('abc');  // eslint-disable-line
		test1[5] = 'de';
		if (Object.getOwnPropertyNames(test1)[0] === '5') {
			return false;
		}

		// https://bugs.chromium.org/p/v8/issues/detail?id=3056
		var test2 = {};
		for (var i = 0; i < 10; i++) {
			test2['_' + String.fromCharCode(i)] = i;
		}
		var order2 = Object.getOwnPropertyNames(test2).map(function (n) {
			return test2[n];
		});
		if (order2.join('') !== '0123456789') {
			return false;
		}

		// https://bugs.chromium.org/p/v8/issues/detail?id=3056
		var test3 = {};
		'abcdefghijklmnopqrst'.split('').forEach(function (letter) {
			test3[letter] = letter;
		});
		if (Object.keys(Object.assign({}, test3)).join('') !==
				'abcdefghijklmnopqrst') {
			return false;
		}

		return true;
	} catch (e) {
		// We don't expect any of the above to throw, but better to be safe.
		return false;
	}
}

module.exports = shouldUseNative() ? Object.assign : function (target, source) {
	var from;
	var to = toObject(target);
	var symbols;

	for (var s = 1; s < arguments.length; s++) {
		from = Object(arguments[s]);

		for (var key in from) {
			if (hasOwnProperty.call(from, key)) {
				to[key] = from[key];
			}
		}

		if (Object.getOwnPropertySymbols) {
			symbols = Object.getOwnPropertySymbols(from);
			for (var i = 0; i < symbols.length; i++) {
				if (propIsEnumerable.call(from, symbols[i])) {
					to[symbols[i]] = from[symbols[i]];
				}
			}
		}
	}

	return to;
};

},{}],186:[function(require,module,exports){
/*!
 * object.omit <https://github.com/jonschlinkert/object.omit>
 *
 * Copyright (c) 2014-2015, Jon Schlinkert.
 * Licensed under the MIT License.
 */

'use strict';

var isObject = require('is-extendable');
var forOwn = require('for-own');

module.exports = function omit(obj, keys) {
  if (!isObject(obj)) return {};

  var keys = [].concat.apply([], [].slice.call(arguments, 1));
  var last = keys[keys.length - 1];
  var res = {}, fn;

  if (typeof last === 'function') {
    fn = keys.pop();
  }

  var isFunction = typeof fn === 'function';
  if (!keys.length && !isFunction) {
    return obj;
  }

  forOwn(obj, function (value, key) {
    if (keys.indexOf(key) === -1) {

      if (!isFunction) {
        res[key] = value;
      } else if (fn(value, key, obj)) {
        res[key] = value;
      }
    }
  });
  return res;
};

},{"for-own":165,"is-extendable":184}],187:[function(require,module,exports){
// shim for using process in browser
var process = module.exports = {};

// cached from whatever global is present so that test runners that stub it
// don't break things.  But we need to wrap it in a try catch in case it is
// wrapped in strict mode code which doesn't define any globals.  It's inside a
// function because try/catches deoptimize in certain engines.

var cachedSetTimeout;
var cachedClearTimeout;

(function () {
    try {
        cachedSetTimeout = setTimeout;
    } catch (e) {
        cachedSetTimeout = function () {
            throw new Error('setTimeout is not defined');
        }
    }
    try {
        cachedClearTimeout = clearTimeout;
    } catch (e) {
        cachedClearTimeout = function () {
            throw new Error('clearTimeout is not defined');
        }
    }
} ())
function runTimeout(fun) {
    if (cachedSetTimeout === setTimeout) {
        //normal enviroments in sane situations
        return setTimeout(fun, 0);
    }
    try {
        // when when somebody has screwed with setTimeout but no I.E. maddness
        return cachedSetTimeout(fun, 0);
    } catch(e){
        try {
            // When we are in I.E. but the script has been evaled so I.E. doesn't trust the global object when called normally
            return cachedSetTimeout.call(null, fun, 0);
        } catch(e){
            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error
            return cachedSetTimeout.call(this, fun, 0);
        }
    }


}
function runClearTimeout(marker) {
    if (cachedClearTimeout === clearTimeout) {
        //normal enviroments in sane situations
        return clearTimeout(marker);
    }
    try {
        // when when somebody has screwed with setTimeout but no I.E. maddness
        return cachedClearTimeout(marker);
    } catch (e){
        try {
            // When we are in I.E. but the script has been evaled so I.E. doesn't  trust the global object when called normally
            return cachedClearTimeout.call(null, marker);
        } catch (e){
            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error.
            // Some versions of I.E. have different rules for clearTimeout vs setTimeout
            return cachedClearTimeout.call(this, marker);
        }
    }



}
var queue = [];
var draining = false;
var currentQueue;
var queueIndex = -1;

function cleanUpNextTick() {
    if (!draining || !currentQueue) {
        return;
    }
    draining = false;
    if (currentQueue.length) {
        queue = currentQueue.concat(queue);
    } else {
        queueIndex = -1;
    }
    if (queue.length) {
        drainQueue();
    }
}

function drainQueue() {
    if (draining) {
        return;
    }
    var timeout = runTimeout(cleanUpNextTick);
    draining = true;

    var len = queue.length;
    while(len) {
        currentQueue = queue;
        queue = [];
        while (++queueIndex < len) {
            if (currentQueue) {
                currentQueue[queueIndex].run();
            }
        }
        queueIndex = -1;
        len = queue.length;
    }
    currentQueue = null;
    draining = false;
    runClearTimeout(timeout);
}

process.nextTick = function (fun) {
    var args = new Array(arguments.length - 1);
    if (arguments.length > 1) {
        for (var i = 1; i < arguments.length; i++) {
            args[i - 1] = arguments[i];
        }
    }
    queue.push(new Item(fun, args));
    if (queue.length === 1 && !draining) {
        runTimeout(drainQueue);
    }
};

// v8 likes predictible objects
function Item(fun, array) {
    this.fun = fun;
    this.array = array;
}
Item.prototype.run = function () {
    this.fun.apply(null, this.array);
};
process.title = 'browser';
process.browser = true;
process.env = {};
process.argv = [];
process.version = ''; // empty string to avoid regexp issues
process.versions = {};

function noop() {}

process.on = noop;
process.addListener = noop;
process.once = noop;
process.off = noop;
process.removeListener = noop;
process.removeAllListeners = noop;
process.emit = noop;

process.binding = function (name) {
    throw new Error('process.binding is not supported');
};

process.cwd = function () { return '/' };
process.chdir = function (dir) {
    throw new Error('process.chdir is not supported');
};
process.umask = function() { return 0; };

},{}],188:[function(require,module,exports){
'use strict';
var strictUriEncode = require('strict-uri-encode');

exports.extract = function (str) {
	return str.split('?')[1] || '';
};

exports.parse = function (str) {
	if (typeof str !== 'string') {
		return {};
	}

	str = str.trim().replace(/^(\?|#|&)/, '');

	if (!str) {
		return {};
	}

	return str.split('&').reduce(function (ret, param) {
		var parts = param.replace(/\+/g, ' ').split('=');
		// Firefox (pre 40) decodes `%3D` to `=`
		// https://github.com/sindresorhus/query-string/pull/37
		var key = parts.shift();
		var val = parts.length > 0 ? parts.join('=') : undefined;

		key = decodeURIComponent(key);

		// missing `=` should be `null`:
		// http://w3.org/TR/2012/WD-url-20120524/#collect-url-parameters
		val = val === undefined ? null : decodeURIComponent(val);

		if (!ret.hasOwnProperty(key)) {
			ret[key] = val;
		} else if (Array.isArray(ret[key])) {
			ret[key].push(val);
		} else {
			ret[key] = [ret[key], val];
		}

		return ret;
	}, {});
};

exports.stringify = function (obj) {
	return obj ? Object.keys(obj).sort().map(function (key) {
		var val = obj[key];

		if (val === undefined) {
			return '';
		}

		if (val === null) {
			return key;
		}

		if (Array.isArray(val)) {
			return val.slice().sort().map(function (val2) {
				return strictUriEncode(key) + '=' + strictUriEncode(val2);
			}).join('&');
		}

		return strictUriEncode(key) + '=' + strictUriEncode(val);
	}).filter(function (x) {
		return x.length > 0;
	}).join('&') : '';
};

},{"strict-uri-encode":357}],189:[function(require,module,exports){
'use strict';

module.exports = require('react/lib/ReactDOM');

},{"react/lib/ReactDOM":262}],190:[function(require,module,exports){
"use strict";

exports.__esModule = true;
exports.loopAsync = loopAsync;
exports.mapAsync = mapAsync;
function loopAsync(turns, work, callback) {
  var currentTurn = 0,
      isDone = false;
  var sync = false,
      hasNext = false,
      doneArgs = void 0;

  function done() {
    isDone = true;
    if (sync) {
      // Iterate instead of recursing if possible.
      doneArgs = [].concat(Array.prototype.slice.call(arguments));
      return;
    }

    callback.apply(this, arguments);
  }

  function next() {
    if (isDone) {
      return;
    }

    hasNext = true;
    if (sync) {
      // Iterate instead of recursing if possible.
      return;
    }

    sync = true;

    while (!isDone && currentTurn < turns && hasNext) {
      hasNext = false;
      work.call(this, currentTurn++, next, done);
    }

    sync = false;

    if (isDone) {
      // This means the loop finished synchronously.
      callback.apply(this, doneArgs);
      return;
    }

    if (currentTurn >= turns && hasNext) {
      isDone = true;
      callback();
    }
  }

  next();
}

function mapAsync(array, work, callback) {
  var length = array.length;
  var values = [];

  if (length === 0) return callback(null, values);

  var isDone = false,
      doneCount = 0;

  function done(index, error, value) {
    if (isDone) return;

    if (error) {
      isDone = true;
      callback(error);
    } else {
      values[index] = value;

      isDone = ++doneCount === length;

      if (isDone) callback(null, values);
    }
  }

  array.forEach(function (item, index) {
    work(item, index, function (error, value) {
      done(index, error, value);
    });
  });
}
},{}],191:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _routerWarning = require('./routerWarning');

var _routerWarning2 = _interopRequireDefault(_routerWarning);

var _InternalPropTypes = require('./InternalPropTypes');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * A mixin that adds the "history" instance variable to components.
 */
var History = {

  contextTypes: {
    history: _InternalPropTypes.history
  },

  componentWillMount: function componentWillMount() {
    "production" !== 'production' ? (0, _routerWarning2.default)(false, 'the `History` mixin is deprecated, please access `context.router` with your own `contextTypes`. http://tiny.cc/router-historymixin') : void 0;
    this.history = this.context.history;
  }
};

exports.default = History;
module.exports = exports['default'];
},{"./InternalPropTypes":195,"./routerWarning":224}],192:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _Link = require('./Link');

var _Link2 = _interopRequireDefault(_Link);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * An <IndexLink> is used to link to an <IndexRoute>.
 */
var IndexLink = _react2.default.createClass({
  displayName: 'IndexLink',
  render: function render() {
    return _react2.default.createElement(_Link2.default, _extends({}, this.props, { onlyActiveOnIndex: true }));
  }
});

exports.default = IndexLink;
module.exports = exports['default'];
},{"./Link":197,"react":356}],193:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _routerWarning = require('./routerWarning');

var _routerWarning2 = _interopRequireDefault(_routerWarning);

var _invariant = require('invariant');

var _invariant2 = _interopRequireDefault(_invariant);

var _Redirect = require('./Redirect');

var _Redirect2 = _interopRequireDefault(_Redirect);

var _InternalPropTypes = require('./InternalPropTypes');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var _React$PropTypes = _react2.default.PropTypes;
var string = _React$PropTypes.string;
var object = _React$PropTypes.object;

/**
 * An <IndexRedirect> is used to redirect from an indexRoute.
 */

var IndexRedirect = _react2.default.createClass({
  displayName: 'IndexRedirect',


  statics: {
    createRouteFromReactElement: function createRouteFromReactElement(element, parentRoute) {
      /* istanbul ignore else: sanity check */
      if (parentRoute) {
        parentRoute.indexRoute = _Redirect2.default.createRouteFromReactElement(element);
      } else {
        "production" !== 'production' ? (0, _routerWarning2.default)(false, 'An <IndexRedirect> does not make sense at the root of your route config') : void 0;
      }
    }
  },

  propTypes: {
    to: string.isRequired,
    query: object,
    state: object,
    onEnter: _InternalPropTypes.falsy,
    children: _InternalPropTypes.falsy
  },

  /* istanbul ignore next: sanity check */
  render: function render() {
    !false ? "production" !== 'production' ? (0, _invariant2.default)(false, '<IndexRedirect> elements are for router configuration only and should not be rendered') : (0, _invariant2.default)(false) : void 0;
  }
});

exports.default = IndexRedirect;
module.exports = exports['default'];
},{"./InternalPropTypes":195,"./Redirect":200,"./routerWarning":224,"invariant":183,"react":356}],194:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _routerWarning = require('./routerWarning');

var _routerWarning2 = _interopRequireDefault(_routerWarning);

var _invariant = require('invariant');

var _invariant2 = _interopRequireDefault(_invariant);

var _RouteUtils = require('./RouteUtils');

var _InternalPropTypes = require('./InternalPropTypes');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var func = _react2.default.PropTypes.func;

/**
 * An <IndexRoute> is used to specify its parent's <Route indexRoute> in
 * a JSX route config.
 */

var IndexRoute = _react2.default.createClass({
  displayName: 'IndexRoute',


  statics: {
    createRouteFromReactElement: function createRouteFromReactElement(element, parentRoute) {
      /* istanbul ignore else: sanity check */
      if (parentRoute) {
        parentRoute.indexRoute = (0, _RouteUtils.createRouteFromReactElement)(element);
      } else {
        "production" !== 'production' ? (0, _routerWarning2.default)(false, 'An <IndexRoute> does not make sense at the root of your route config') : void 0;
      }
    }
  },

  propTypes: {
    path: _InternalPropTypes.falsy,
    component: _InternalPropTypes.component,
    components: _InternalPropTypes.components,
    getComponent: func,
    getComponents: func
  },

  /* istanbul ignore next: sanity check */
  render: function render() {
    !false ? "production" !== 'production' ? (0, _invariant2.default)(false, '<IndexRoute> elements are for router configuration only and should not be rendered') : (0, _invariant2.default)(false) : void 0;
  }
});

exports.default = IndexRoute;
module.exports = exports['default'];
},{"./InternalPropTypes":195,"./RouteUtils":203,"./routerWarning":224,"invariant":183,"react":356}],195:[function(require,module,exports){
'use strict';

exports.__esModule = true;
exports.routes = exports.route = exports.components = exports.component = exports.history = undefined;
exports.falsy = falsy;

var _react = require('react');

var func = _react.PropTypes.func;
var object = _react.PropTypes.object;
var arrayOf = _react.PropTypes.arrayOf;
var oneOfType = _react.PropTypes.oneOfType;
var element = _react.PropTypes.element;
var shape = _react.PropTypes.shape;
var string = _react.PropTypes.string;
function falsy(props, propName, componentName) {
  if (props[propName]) return new Error('<' + componentName + '> should not have a "' + propName + '" prop');
}

var history = exports.history = shape({
  listen: func.isRequired,
  push: func.isRequired,
  replace: func.isRequired,
  go: func.isRequired,
  goBack: func.isRequired,
  goForward: func.isRequired
});

var component = exports.component = oneOfType([func, string]);
var components = exports.components = oneOfType([component, object]);
var route = exports.route = oneOfType([object, element]);
var routes = exports.routes = oneOfType([route, arrayOf(route)]);
},{"react":356}],196:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _routerWarning = require('./routerWarning');

var _routerWarning2 = _interopRequireDefault(_routerWarning);

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _invariant = require('invariant');

var _invariant2 = _interopRequireDefault(_invariant);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var object = _react2.default.PropTypes.object;

/**
 * The Lifecycle mixin adds the routerWillLeave lifecycle method to a
 * component that may be used to cancel a transition or prompt the user
 * for confirmation.
 *
 * On standard transitions, routerWillLeave receives a single argument: the
 * location we're transitioning to. To cancel the transition, return false.
 * To prompt the user for confirmation, return a prompt message (string).
 *
 * During the beforeunload event (assuming you're using the useBeforeUnload
 * history enhancer), routerWillLeave does not receive a location object
 * because it isn't possible for us to know the location we're transitioning
 * to. In this case routerWillLeave must return a prompt message to prevent
 * the user from closing the window/tab.
 */

var Lifecycle = {

  contextTypes: {
    history: object.isRequired,
    // Nested children receive the route as context, either
    // set by the route component using the RouteContext mixin
    // or by some other ancestor.
    route: object
  },

  propTypes: {
    // Route components receive the route object as a prop.
    route: object
  },

  componentDidMount: function componentDidMount() {
    "production" !== 'production' ? (0, _routerWarning2.default)(false, 'the `Lifecycle` mixin is deprecated, please use `context.router.setRouteLeaveHook(route, hook)`. http://tiny.cc/router-lifecyclemixin') : void 0;
    !this.routerWillLeave ? "production" !== 'production' ? (0, _invariant2.default)(false, 'The Lifecycle mixin requires you to define a routerWillLeave method') : (0, _invariant2.default)(false) : void 0;

    var route = this.props.route || this.context.route;

    !route ? "production" !== 'production' ? (0, _invariant2.default)(false, 'The Lifecycle mixin must be used on either a) a <Route component> or ' + 'b) a descendant of a <Route component> that uses the RouteContext mixin') : (0, _invariant2.default)(false) : void 0;

    this._unlistenBeforeLeavingRoute = this.context.history.listenBeforeLeavingRoute(route, this.routerWillLeave);
  },
  componentWillUnmount: function componentWillUnmount() {
    if (this._unlistenBeforeLeavingRoute) this._unlistenBeforeLeavingRoute();
  }
};

exports.default = Lifecycle;
module.exports = exports['default'];
},{"./routerWarning":224,"invariant":183,"react":356}],197:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _routerWarning = require('./routerWarning');

var _routerWarning2 = _interopRequireDefault(_routerWarning);

var _invariant = require('invariant');

var _invariant2 = _interopRequireDefault(_invariant);

var _PropTypes = require('./PropTypes');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _objectWithoutProperties(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }

var _React$PropTypes = _react2.default.PropTypes;
var bool = _React$PropTypes.bool;
var object = _React$PropTypes.object;
var string = _React$PropTypes.string;
var func = _React$PropTypes.func;
var oneOfType = _React$PropTypes.oneOfType;


function isLeftClickEvent(event) {
  return event.button === 0;
}

function isModifiedEvent(event) {
  return !!(event.metaKey || event.altKey || event.ctrlKey || event.shiftKey);
}

// TODO: De-duplicate against hasAnyProperties in createTransitionManager.
function isEmptyObject(object) {
  for (var p in object) {
    if (Object.prototype.hasOwnProperty.call(object, p)) return false;
  }return true;
}

function createLocationDescriptor(to, _ref) {
  var query = _ref.query;
  var hash = _ref.hash;
  var state = _ref.state;

  if (query || hash || state) {
    return { pathname: to, query: query, hash: hash, state: state };
  }

  return to;
}

/**
 * A <Link> is used to create an <a> element that links to a route.
 * When that route is active, the link gets the value of its
 * activeClassName prop.
 *
 * For example, assuming you have the following route:
 *
 *   <Route path="/posts/:postID" component={Post} />
 *
 * You could use the following component to link to that route:
 *
 *   <Link to={`/posts/${post.id}`} />
 *
 * Links may pass along location state and/or query string parameters
 * in the state/query props, respectively.
 *
 *   <Link ... query={{ show: true }} state={{ the: 'state' }} />
 */
var Link = _react2.default.createClass({
  displayName: 'Link',


  contextTypes: {
    router: _PropTypes.routerShape
  },

  propTypes: {
    to: oneOfType([string, object]).isRequired,
    query: object,
    hash: string,
    state: object,
    activeStyle: object,
    activeClassName: string,
    onlyActiveOnIndex: bool.isRequired,
    onClick: func,
    target: string
  },

  getDefaultProps: function getDefaultProps() {
    return {
      onlyActiveOnIndex: false,
      style: {}
    };
  },
  handleClick: function handleClick(event) {
    if (this.props.onClick) this.props.onClick(event);

    if (event.defaultPrevented) return;

    !this.context.router ? "production" !== 'production' ? (0, _invariant2.default)(false, '<Link>s rendered outside of a router context cannot navigate.') : (0, _invariant2.default)(false) : void 0;

    if (isModifiedEvent(event) || !isLeftClickEvent(event)) return;

    // If target prop is set (e.g. to "_blank"), let browser handle link.
    /* istanbul ignore if: untestable with Karma */
    if (this.props.target) return;

    event.preventDefault();

    var _props = this.props;
    var to = _props.to;
    var query = _props.query;
    var hash = _props.hash;
    var state = _props.state;

    var location = createLocationDescriptor(to, { query: query, hash: hash, state: state });

    this.context.router.push(location);
  },
  render: function render() {
    var _props2 = this.props;
    var to = _props2.to;
    var query = _props2.query;
    var hash = _props2.hash;
    var state = _props2.state;
    var activeClassName = _props2.activeClassName;
    var activeStyle = _props2.activeStyle;
    var onlyActiveOnIndex = _props2.onlyActiveOnIndex;

    var props = _objectWithoutProperties(_props2, ['to', 'query', 'hash', 'state', 'activeClassName', 'activeStyle', 'onlyActiveOnIndex']);

    "production" !== 'production' ? (0, _routerWarning2.default)(!(query || hash || state), 'the `query`, `hash`, and `state` props on `<Link>` are deprecated, use `<Link to={{ pathname, query, hash, state }}/>. http://tiny.cc/router-isActivedeprecated') : void 0;

    // Ignore if rendered outside the context of router, simplifies unit testing.
    var router = this.context.router;


    if (router) {
      var location = createLocationDescriptor(to, { query: query, hash: hash, state: state });
      props.href = router.createHref(location);

      if (activeClassName || activeStyle != null && !isEmptyObject(activeStyle)) {
        if (router.isActive(location, onlyActiveOnIndex)) {
          if (activeClassName) {
            if (props.className) {
              props.className += ' ' + activeClassName;
            } else {
              props.className = activeClassName;
            }
          }

          if (activeStyle) props.style = _extends({}, props.style, activeStyle);
        }
      }
    }

    return _react2.default.createElement('a', _extends({}, props, { onClick: this.handleClick }));
  }
});

exports.default = Link;
module.exports = exports['default'];
},{"./PropTypes":199,"./routerWarning":224,"invariant":183,"react":356}],198:[function(require,module,exports){
'use strict';

exports.__esModule = true;
exports.compilePattern = compilePattern;
exports.matchPattern = matchPattern;
exports.getParamNames = getParamNames;
exports.getParams = getParams;
exports.formatPattern = formatPattern;

var _invariant = require('invariant');

var _invariant2 = _interopRequireDefault(_invariant);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function escapeRegExp(string) {
  return string.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function _compilePattern(pattern) {
  var regexpSource = '';
  var paramNames = [];
  var tokens = [];

  var match = void 0,
      lastIndex = 0,
      matcher = /:([a-zA-Z_$][a-zA-Z0-9_$]*)|\*\*|\*|\(|\)/g;
  while (match = matcher.exec(pattern)) {
    if (match.index !== lastIndex) {
      tokens.push(pattern.slice(lastIndex, match.index));
      regexpSource += escapeRegExp(pattern.slice(lastIndex, match.index));
    }

    if (match[1]) {
      regexpSource += '([^/]+)';
      paramNames.push(match[1]);
    } else if (match[0] === '**') {
      regexpSource += '(.*)';
      paramNames.push('splat');
    } else if (match[0] === '*') {
      regexpSource += '(.*?)';
      paramNames.push('splat');
    } else if (match[0] === '(') {
      regexpSource += '(?:';
    } else if (match[0] === ')') {
      regexpSource += ')?';
    }

    tokens.push(match[0]);

    lastIndex = matcher.lastIndex;
  }

  if (lastIndex !== pattern.length) {
    tokens.push(pattern.slice(lastIndex, pattern.length));
    regexpSource += escapeRegExp(pattern.slice(lastIndex, pattern.length));
  }

  return {
    pattern: pattern,
    regexpSource: regexpSource,
    paramNames: paramNames,
    tokens: tokens
  };
}

var CompiledPatternsCache = Object.create(null);

function compilePattern(pattern) {
  if (!CompiledPatternsCache[pattern]) CompiledPatternsCache[pattern] = _compilePattern(pattern);

  return CompiledPatternsCache[pattern];
}

/**
 * Attempts to match a pattern on the given pathname. Patterns may use
 * the following special characters:
 *
 * - :paramName     Matches a URL segment up to the next /, ?, or #. The
 *                  captured string is considered a "param"
 * - ()             Wraps a segment of the URL that is optional
 * - *              Consumes (non-greedy) all characters up to the next
 *                  character in the pattern, or to the end of the URL if
 *                  there is none
 * - **             Consumes (greedy) all characters up to the next character
 *                  in the pattern, or to the end of the URL if there is none
 *
 *  The function calls callback(error, matched) when finished.
 * The return value is an object with the following properties:
 *
 * - remainingPathname
 * - paramNames
 * - paramValues
 */
function matchPattern(pattern, pathname) {
  // Ensure pattern starts with leading slash for consistency with pathname.
  if (pattern.charAt(0) !== '/') {
    pattern = '/' + pattern;
  }

  var _compilePattern2 = compilePattern(pattern);

  var regexpSource = _compilePattern2.regexpSource;
  var paramNames = _compilePattern2.paramNames;
  var tokens = _compilePattern2.tokens;


  if (pattern.charAt(pattern.length - 1) !== '/') {
    regexpSource += '/?'; // Allow optional path separator at end.
  }

  // Special-case patterns like '*' for catch-all routes.
  if (tokens[tokens.length - 1] === '*') {
    regexpSource += '$';
  }

  var match = pathname.match(new RegExp('^' + regexpSource, 'i'));
  if (match == null) {
    return null;
  }

  var matchedPath = match[0];
  var remainingPathname = pathname.substr(matchedPath.length);

  if (remainingPathname) {
    // Require that the match ends at a path separator, if we didn't match
    // the full path, so any remaining pathname is a new path segment.
    if (matchedPath.charAt(matchedPath.length - 1) !== '/') {
      return null;
    }

    // If there is a remaining pathname, treat the path separator as part of
    // the remaining pathname for properly continuing the match.
    remainingPathname = '/' + remainingPathname;
  }

  return {
    remainingPathname: remainingPathname,
    paramNames: paramNames,
    paramValues: match.slice(1).map(function (v) {
      return v && decodeURIComponent(v);
    })
  };
}

function getParamNames(pattern) {
  return compilePattern(pattern).paramNames;
}

function getParams(pattern, pathname) {
  var match = matchPattern(pattern, pathname);
  if (!match) {
    return null;
  }

  var paramNames = match.paramNames;
  var paramValues = match.paramValues;

  var params = {};

  paramNames.forEach(function (paramName, index) {
    params[paramName] = paramValues[index];
  });

  return params;
}

/**
 * Returns a version of the given pattern with params interpolated. Throws
 * if there is a dynamic segment of the pattern for which there is no param.
 */
function formatPattern(pattern, params) {
  params = params || {};

  var _compilePattern3 = compilePattern(pattern);

  var tokens = _compilePattern3.tokens;

  var parenCount = 0,
      pathname = '',
      splatIndex = 0;

  var token = void 0,
      paramName = void 0,
      paramValue = void 0;
  for (var i = 0, len = tokens.length; i < len; ++i) {
    token = tokens[i];

    if (token === '*' || token === '**') {
      paramValue = Array.isArray(params.splat) ? params.splat[splatIndex++] : params.splat;

      !(paramValue != null || parenCount > 0) ? "production" !== 'production' ? (0, _invariant2.default)(false, 'Missing splat #%s for path "%s"', splatIndex, pattern) : (0, _invariant2.default)(false) : void 0;

      if (paramValue != null) pathname += encodeURI(paramValue);
    } else if (token === '(') {
      parenCount += 1;
    } else if (token === ')') {
      parenCount -= 1;
    } else if (token.charAt(0) === ':') {
      paramName = token.substring(1);
      paramValue = params[paramName];

      !(paramValue != null || parenCount > 0) ? "production" !== 'production' ? (0, _invariant2.default)(false, 'Missing "%s" parameter for path "%s"', paramName, pattern) : (0, _invariant2.default)(false) : void 0;

      if (paramValue != null) pathname += encodeURIComponent(paramValue);
    } else {
      pathname += token;
    }
  }

  return pathname.replace(/\/+/g, '/');
}
},{"invariant":183}],199:[function(require,module,exports){
'use strict';

exports.__esModule = true;
exports.router = exports.routes = exports.route = exports.components = exports.component = exports.location = exports.history = exports.falsy = exports.locationShape = exports.routerShape = undefined;

var _react = require('react');

var _deprecateObjectProperties = require('./deprecateObjectProperties');

var _deprecateObjectProperties2 = _interopRequireDefault(_deprecateObjectProperties);

var _InternalPropTypes = require('./InternalPropTypes');

var InternalPropTypes = _interopRequireWildcard(_InternalPropTypes);

var _routerWarning = require('./routerWarning');

var _routerWarning2 = _interopRequireDefault(_routerWarning);

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var func = _react.PropTypes.func;
var object = _react.PropTypes.object;
var shape = _react.PropTypes.shape;
var string = _react.PropTypes.string;
var routerShape = exports.routerShape = shape({
  push: func.isRequired,
  replace: func.isRequired,
  go: func.isRequired,
  goBack: func.isRequired,
  goForward: func.isRequired,
  setRouteLeaveHook: func.isRequired,
  isActive: func.isRequired
});

var locationShape = exports.locationShape = shape({
  pathname: string.isRequired,
  search: string.isRequired,
  state: object,
  action: string.isRequired,
  key: string
});

// Deprecated stuff below:

var falsy = exports.falsy = InternalPropTypes.falsy;
var history = exports.history = InternalPropTypes.history;
var location = exports.location = locationShape;
var component = exports.component = InternalPropTypes.component;
var components = exports.components = InternalPropTypes.components;
var route = exports.route = InternalPropTypes.route;
var routes = exports.routes = InternalPropTypes.routes;
var router = exports.router = routerShape;

if ("production" !== 'production') {
  (function () {
    var deprecatePropType = function deprecatePropType(propType, message) {
      return function () {
        "production" !== 'production' ? (0, _routerWarning2.default)(false, message) : void 0;
        return propType.apply(undefined, arguments);
      };
    };

    var deprecateInternalPropType = function deprecateInternalPropType(propType) {
      return deprecatePropType(propType, 'This prop type is not intended for external use, and was previously exported by mistake. These internal prop types are deprecated for external use, and will be removed in a later version.');
    };

    var deprecateRenamedPropType = function deprecateRenamedPropType(propType, name) {
      return deprecatePropType(propType, 'The `' + name + '` prop type is now exported as `' + name + 'Shape` to avoid name conflicts. This export is deprecated and will be removed in a later version.');
    };

    exports.falsy = falsy = deprecateInternalPropType(falsy);
    exports.history = history = deprecateInternalPropType(history);
    exports.component = component = deprecateInternalPropType(component);
    exports.components = components = deprecateInternalPropType(components);
    exports.route = route = deprecateInternalPropType(route);
    exports.routes = routes = deprecateInternalPropType(routes);

    exports.location = location = deprecateRenamedPropType(location, 'location');
    exports.router = router = deprecateRenamedPropType(router, 'router');
  })();
}

var defaultExport = {
  falsy: falsy,
  history: history,
  location: location,
  component: component,
  components: components,
  route: route,
  // For some reason, routes was never here.
  router: router
};

if ("production" !== 'production') {
  defaultExport = (0, _deprecateObjectProperties2.default)(defaultExport, 'The default export from `react-router/lib/PropTypes` is deprecated. Please use the named exports instead.');
}

exports.default = defaultExport;
},{"./InternalPropTypes":195,"./deprecateObjectProperties":215,"./routerWarning":224,"react":356}],200:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _invariant = require('invariant');

var _invariant2 = _interopRequireDefault(_invariant);

var _RouteUtils = require('./RouteUtils');

var _PatternUtils = require('./PatternUtils');

var _InternalPropTypes = require('./InternalPropTypes');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var _React$PropTypes = _react2.default.PropTypes;
var string = _React$PropTypes.string;
var object = _React$PropTypes.object;

/**
 * A <Redirect> is used to declare another URL path a client should
 * be sent to when they request a given URL.
 *
 * Redirects are placed alongside routes in the route configuration
 * and are traversed in the same manner.
 */

var Redirect = _react2.default.createClass({
  displayName: 'Redirect',


  statics: {
    createRouteFromReactElement: function createRouteFromReactElement(element) {
      var route = (0, _RouteUtils.createRouteFromReactElement)(element);

      if (route.from) route.path = route.from;

      route.onEnter = function (nextState, replace) {
        var location = nextState.location;
        var params = nextState.params;


        var pathname = void 0;
        if (route.to.charAt(0) === '/') {
          pathname = (0, _PatternUtils.formatPattern)(route.to, params);
        } else if (!route.to) {
          pathname = location.pathname;
        } else {
          var routeIndex = nextState.routes.indexOf(route);
          var parentPattern = Redirect.getRoutePattern(nextState.routes, routeIndex - 1);
          var pattern = parentPattern.replace(/\/*$/, '/') + route.to;
          pathname = (0, _PatternUtils.formatPattern)(pattern, params);
        }

        replace({
          pathname: pathname,
          query: route.query || location.query,
          state: route.state || location.state
        });
      };

      return route;
    },
    getRoutePattern: function getRoutePattern(routes, routeIndex) {
      var parentPattern = '';

      for (var i = routeIndex; i >= 0; i--) {
        var route = routes[i];
        var pattern = route.path || '';

        parentPattern = pattern.replace(/\/*$/, '/') + parentPattern;

        if (pattern.indexOf('/') === 0) break;
      }

      return '/' + parentPattern;
    }
  },

  propTypes: {
    path: string,
    from: string, // Alias for path
    to: string.isRequired,
    query: object,
    state: object,
    onEnter: _InternalPropTypes.falsy,
    children: _InternalPropTypes.falsy
  },

  /* istanbul ignore next: sanity check */
  render: function render() {
    !false ? "production" !== 'production' ? (0, _invariant2.default)(false, '<Redirect> elements are for router configuration only and should not be rendered') : (0, _invariant2.default)(false) : void 0;
  }
});

exports.default = Redirect;
module.exports = exports['default'];
},{"./InternalPropTypes":195,"./PatternUtils":198,"./RouteUtils":203,"invariant":183,"react":356}],201:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _invariant = require('invariant');

var _invariant2 = _interopRequireDefault(_invariant);

var _RouteUtils = require('./RouteUtils');

var _InternalPropTypes = require('./InternalPropTypes');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var _React$PropTypes = _react2.default.PropTypes;
var string = _React$PropTypes.string;
var func = _React$PropTypes.func;

/**
 * A <Route> is used to declare which components are rendered to the
 * page when the URL matches a given pattern.
 *
 * Routes are arranged in a nested tree structure. When a new URL is
 * requested, the tree is searched depth-first to find a route whose
 * path matches the URL.  When one is found, all routes in the tree
 * that lead to it are considered "active" and their components are
 * rendered into the DOM, nested in the same order as in the tree.
 */

var Route = _react2.default.createClass({
  displayName: 'Route',


  statics: {
    createRouteFromReactElement: _RouteUtils.createRouteFromReactElement
  },

  propTypes: {
    path: string,
    component: _InternalPropTypes.component,
    components: _InternalPropTypes.components,
    getComponent: func,
    getComponents: func
  },

  /* istanbul ignore next: sanity check */
  render: function render() {
    !false ? "production" !== 'production' ? (0, _invariant2.default)(false, '<Route> elements are for router configuration only and should not be rendered') : (0, _invariant2.default)(false) : void 0;
  }
});

exports.default = Route;
module.exports = exports['default'];
},{"./InternalPropTypes":195,"./RouteUtils":203,"invariant":183,"react":356}],202:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _routerWarning = require('./routerWarning');

var _routerWarning2 = _interopRequireDefault(_routerWarning);

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var object = _react2.default.PropTypes.object;

/**
 * The RouteContext mixin provides a convenient way for route
 * components to set the route in context. This is needed for
 * routes that render elements that want to use the Lifecycle
 * mixin to prevent transitions.
 */

var RouteContext = {

  propTypes: {
    route: object.isRequired
  },

  childContextTypes: {
    route: object.isRequired
  },

  getChildContext: function getChildContext() {
    return {
      route: this.props.route
    };
  },
  componentWillMount: function componentWillMount() {
    "production" !== 'production' ? (0, _routerWarning2.default)(false, 'The `RouteContext` mixin is deprecated. You can provide `this.props.route` on context with your own `contextTypes`. http://tiny.cc/router-routecontextmixin') : void 0;
  }
};

exports.default = RouteContext;
module.exports = exports['default'];
},{"./routerWarning":224,"react":356}],203:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

exports.isReactChildren = isReactChildren;
exports.createRouteFromReactElement = createRouteFromReactElement;
exports.createRoutesFromReactChildren = createRoutesFromReactChildren;
exports.createRoutes = createRoutes;

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function isValidChild(object) {
  return object == null || _react2.default.isValidElement(object);
}

function isReactChildren(object) {
  return isValidChild(object) || Array.isArray(object) && object.every(isValidChild);
}

function createRoute(defaultProps, props) {
  return _extends({}, defaultProps, props);
}

function createRouteFromReactElement(element) {
  var type = element.type;
  var route = createRoute(type.defaultProps, element.props);

  if (route.children) {
    var childRoutes = createRoutesFromReactChildren(route.children, route);

    if (childRoutes.length) route.childRoutes = childRoutes;

    delete route.children;
  }

  return route;
}

/**
 * Creates and returns a routes object from the given ReactChildren. JSX
 * provides a convenient way to visualize how routes in the hierarchy are
 * nested.
 *
 *   import { Route, createRoutesFromReactChildren } from 'react-router'
 *
 *   const routes = createRoutesFromReactChildren(
 *     <Route component={App}>
 *       <Route path="home" component={Dashboard}/>
 *       <Route path="news" component={NewsFeed}/>
 *     </Route>
 *   )
 *
 * Note: This method is automatically used when you provide <Route> children
 * to a <Router> component.
 */
function createRoutesFromReactChildren(children, parentRoute) {
  var routes = [];

  _react2.default.Children.forEach(children, function (element) {
    if (_react2.default.isValidElement(element)) {
      // Component classes may have a static create* method.
      if (element.type.createRouteFromReactElement) {
        var route = element.type.createRouteFromReactElement(element, parentRoute);

        if (route) routes.push(route);
      } else {
        routes.push(createRouteFromReactElement(element));
      }
    }
  });

  return routes;
}

/**
 * Creates and returns an array of routes from the given object which
 * may be a JSX route, a plain object route, or an array of either.
 */
function createRoutes(routes) {
  if (isReactChildren(routes)) {
    routes = createRoutesFromReactChildren(routes);
  } else if (routes && !Array.isArray(routes)) {
    routes = [routes];
  }

  return routes;
}
},{"react":356}],204:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _createHashHistory = require('history/lib/createHashHistory');

var _createHashHistory2 = _interopRequireDefault(_createHashHistory);

var _useQueries = require('history/lib/useQueries');

var _useQueries2 = _interopRequireDefault(_useQueries);

var _invariant = require('invariant');

var _invariant2 = _interopRequireDefault(_invariant);

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _createTransitionManager = require('./createTransitionManager');

var _createTransitionManager2 = _interopRequireDefault(_createTransitionManager);

var _InternalPropTypes = require('./InternalPropTypes');

var _RouterContext = require('./RouterContext');

var _RouterContext2 = _interopRequireDefault(_RouterContext);

var _RouteUtils = require('./RouteUtils');

var _RouterUtils = require('./RouterUtils');

var _routerWarning = require('./routerWarning');

var _routerWarning2 = _interopRequireDefault(_routerWarning);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _objectWithoutProperties(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }

function isDeprecatedHistory(history) {
  return !history || !history.__v2_compatible__;
}

/* istanbul ignore next: sanity check */
function isUnsupportedHistory(history) {
  // v3 histories expose getCurrentLocation, but aren't currently supported.
  return history && history.getCurrentLocation;
}

var _React$PropTypes = _react2.default.PropTypes;
var func = _React$PropTypes.func;
var object = _React$PropTypes.object;

/**
 * A <Router> is a high-level API for automatically setting up
 * a router that renders a <RouterContext> with all the props
 * it needs each time the URL changes.
 */

var Router = _react2.default.createClass({
  displayName: 'Router',


  propTypes: {
    history: object,
    children: _InternalPropTypes.routes,
    routes: _InternalPropTypes.routes, // alias for children
    render: func,
    createElement: func,
    onError: func,
    onUpdate: func,

    // Deprecated:
    parseQueryString: func,
    stringifyQuery: func,

    // PRIVATE: For client-side rehydration of server match.
    matchContext: object
  },

  getDefaultProps: function getDefaultProps() {
    return {
      render: function render(props) {
        return _react2.default.createElement(_RouterContext2.default, props);
      }
    };
  },
  getInitialState: function getInitialState() {
    return {
      location: null,
      routes: null,
      params: null,
      components: null
    };
  },
  handleError: function handleError(error) {
    if (this.props.onError) {
      this.props.onError.call(this, error);
    } else {
      // Throw errors by default so we don't silently swallow them!
      throw error; // This error probably occurred in getChildRoutes or getComponents.
    }
  },
  componentWillMount: function componentWillMount() {
    var _this = this;

    var _props = this.props;
    var parseQueryString = _props.parseQueryString;
    var stringifyQuery = _props.stringifyQuery;

    "production" !== 'production' ? (0, _routerWarning2.default)(!(parseQueryString || stringifyQuery), '`parseQueryString` and `stringifyQuery` are deprecated. Please create a custom history. http://tiny.cc/router-customquerystring') : void 0;

    var _createRouterObjects = this.createRouterObjects();

    var history = _createRouterObjects.history;
    var transitionManager = _createRouterObjects.transitionManager;
    var router = _createRouterObjects.router;


    this._unlisten = transitionManager.listen(function (error, state) {
      if (error) {
        _this.handleError(error);
      } else {
        _this.setState(state, _this.props.onUpdate);
      }
    });

    this.history = history;
    this.router = router;
  },
  createRouterObjects: function createRouterObjects() {
    var matchContext = this.props.matchContext;

    if (matchContext) {
      return matchContext;
    }

    var history = this.props.history;
    var _props2 = this.props;
    var routes = _props2.routes;
    var children = _props2.children;


    !!isUnsupportedHistory(history) ? "production" !== 'production' ? (0, _invariant2.default)(false, 'You have provided a history object created with history v3.x. ' + 'This version of React Router is not compatible with v3 history ' + 'objects. Please use history v2.x instead.') : (0, _invariant2.default)(false) : void 0;

    if (isDeprecatedHistory(history)) {
      history = this.wrapDeprecatedHistory(history);
    }

    var transitionManager = (0, _createTransitionManager2.default)(history, (0, _RouteUtils.createRoutes)(routes || children));
    var router = (0, _RouterUtils.createRouterObject)(history, transitionManager);
    var routingHistory = (0, _RouterUtils.createRoutingHistory)(history, transitionManager);

    return { history: routingHistory, transitionManager: transitionManager, router: router };
  },
  wrapDeprecatedHistory: function wrapDeprecatedHistory(history) {
    var _props3 = this.props;
    var parseQueryString = _props3.parseQueryString;
    var stringifyQuery = _props3.stringifyQuery;


    var createHistory = void 0;
    if (history) {
      "production" !== 'production' ? (0, _routerWarning2.default)(false, 'It appears you have provided a deprecated history object to `<Router/>`, please use a history provided by ' + 'React Router with `import { browserHistory } from \'react-router\'` or `import { hashHistory } from \'react-router\'`. ' + 'If you are using a custom history please create it with `useRouterHistory`, see http://tiny.cc/router-usinghistory for details.') : void 0;
      createHistory = function createHistory() {
        return history;
      };
    } else {
      "production" !== 'production' ? (0, _routerWarning2.default)(false, '`Router` no longer defaults the history prop to hash history. Please use the `hashHistory` singleton instead. http://tiny.cc/router-defaulthistory') : void 0;
      createHistory = _createHashHistory2.default;
    }

    return (0, _useQueries2.default)(createHistory)({ parseQueryString: parseQueryString, stringifyQuery: stringifyQuery });
  },


  /* istanbul ignore next: sanity check */
  componentWillReceiveProps: function componentWillReceiveProps(nextProps) {
    "production" !== 'production' ? (0, _routerWarning2.default)(nextProps.history === this.props.history, 'You cannot change <Router history>; it will be ignored') : void 0;

    "production" !== 'production' ? (0, _routerWarning2.default)((nextProps.routes || nextProps.children) === (this.props.routes || this.props.children), 'You cannot change <Router routes>; it will be ignored') : void 0;
  },
  componentWillUnmount: function componentWillUnmount() {
    if (this._unlisten) this._unlisten();
  },
  render: function render() {
    var _state = this.state;
    var location = _state.location;
    var routes = _state.routes;
    var params = _state.params;
    var components = _state.components;
    var _props4 = this.props;
    var createElement = _props4.createElement;
    var render = _props4.render;

    var props = _objectWithoutProperties(_props4, ['createElement', 'render']);

    if (location == null) return null; // Async match

    // Only forward non-Router-specific props to routing context, as those are
    // the only ones that might be custom routing context props.
    Object.keys(Router.propTypes).forEach(function (propType) {
      return delete props[propType];
    });

    return render(_extends({}, props, {
      history: this.history,
      router: this.router,
      location: location,
      routes: routes,
      params: params,
      components: components,
      createElement: createElement
    }));
  }
});

exports.default = Router;
module.exports = exports['default'];
},{"./InternalPropTypes":195,"./RouteUtils":203,"./RouterContext":205,"./RouterUtils":206,"./createTransitionManager":214,"./routerWarning":224,"history/lib/createHashHistory":174,"history/lib/useQueries":181,"invariant":183,"react":356}],205:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol ? "symbol" : typeof obj; };

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _invariant = require('invariant');

var _invariant2 = _interopRequireDefault(_invariant);

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _deprecateObjectProperties = require('./deprecateObjectProperties');

var _deprecateObjectProperties2 = _interopRequireDefault(_deprecateObjectProperties);

var _getRouteParams = require('./getRouteParams');

var _getRouteParams2 = _interopRequireDefault(_getRouteParams);

var _RouteUtils = require('./RouteUtils');

var _routerWarning = require('./routerWarning');

var _routerWarning2 = _interopRequireDefault(_routerWarning);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var _React$PropTypes = _react2.default.PropTypes;
var array = _React$PropTypes.array;
var func = _React$PropTypes.func;
var object = _React$PropTypes.object;

/**
 * A <RouterContext> renders the component tree for a given router state
 * and sets the history object and the current location in context.
 */

var RouterContext = _react2.default.createClass({
  displayName: 'RouterContext',


  propTypes: {
    history: object,
    router: object.isRequired,
    location: object.isRequired,
    routes: array.isRequired,
    params: object.isRequired,
    components: array.isRequired,
    createElement: func.isRequired
  },

  getDefaultProps: function getDefaultProps() {
    return {
      createElement: _react2.default.createElement
    };
  },


  childContextTypes: {
    history: object,
    location: object.isRequired,
    router: object.isRequired
  },

  getChildContext: function getChildContext() {
    var _props = this.props;
    var router = _props.router;
    var history = _props.history;
    var location = _props.location;

    if (!router) {
      "production" !== 'production' ? (0, _routerWarning2.default)(false, '`<RouterContext>` expects a `router` rather than a `history`') : void 0;

      router = _extends({}, history, {
        setRouteLeaveHook: history.listenBeforeLeavingRoute
      });
      delete router.listenBeforeLeavingRoute;
    }

    if ("production" !== 'production') {
      location = (0, _deprecateObjectProperties2.default)(location, '`context.location` is deprecated, please use a route component\'s `props.location` instead. http://tiny.cc/router-accessinglocation');
    }

    return { history: history, location: location, router: router };
  },
  createElement: function createElement(component, props) {
    return component == null ? null : this.props.createElement(component, props);
  },
  render: function render() {
    var _this = this;

    var _props2 = this.props;
    var history = _props2.history;
    var location = _props2.location;
    var routes = _props2.routes;
    var params = _props2.params;
    var components = _props2.components;

    var element = null;

    if (components) {
      element = components.reduceRight(function (element, components, index) {
        if (components == null) return element; // Don't create new children; use the grandchildren.

        var route = routes[index];
        var routeParams = (0, _getRouteParams2.default)(route, params);
        var props = {
          history: history,
          location: location,
          params: params,
          route: route,
          routeParams: routeParams,
          routes: routes
        };

        if ((0, _RouteUtils.isReactChildren)(element)) {
          props.children = element;
        } else if (element) {
          for (var prop in element) {
            if (Object.prototype.hasOwnProperty.call(element, prop)) props[prop] = element[prop];
          }
        }

        if ((typeof components === 'undefined' ? 'undefined' : _typeof(components)) === 'object') {
          var elements = {};

          for (var key in components) {
            if (Object.prototype.hasOwnProperty.call(components, key)) {
              // Pass through the key as a prop to createElement to allow
              // custom createElement functions to know which named component
              // they're rendering, for e.g. matching up to fetched data.
              elements[key] = _this.createElement(components[key], _extends({
                key: key }, props));
            }
          }

          return elements;
        }

        return _this.createElement(components, props);
      }, element);
    }

    !(element === null || element === false || _react2.default.isValidElement(element)) ? "production" !== 'production' ? (0, _invariant2.default)(false, 'The root route must render a single element') : (0, _invariant2.default)(false) : void 0;

    return element;
  }
});

exports.default = RouterContext;
module.exports = exports['default'];
},{"./RouteUtils":203,"./deprecateObjectProperties":215,"./getRouteParams":217,"./routerWarning":224,"invariant":183,"react":356}],206:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

exports.createRouterObject = createRouterObject;
exports.createRoutingHistory = createRoutingHistory;

var _deprecateObjectProperties = require('./deprecateObjectProperties');

var _deprecateObjectProperties2 = _interopRequireDefault(_deprecateObjectProperties);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function createRouterObject(history, transitionManager) {
  return _extends({}, history, {
    setRouteLeaveHook: transitionManager.listenBeforeLeavingRoute,
    isActive: transitionManager.isActive
  });
}

// deprecated
function createRoutingHistory(history, transitionManager) {
  history = _extends({}, history, transitionManager);

  if ("production" !== 'production') {
    history = (0, _deprecateObjectProperties2.default)(history, '`props.history` and `context.history` are deprecated. Please use `context.router`. http://tiny.cc/router-contextchanges');
  }

  return history;
}
},{"./deprecateObjectProperties":215}],207:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _RouterContext = require('./RouterContext');

var _RouterContext2 = _interopRequireDefault(_RouterContext);

var _routerWarning = require('./routerWarning');

var _routerWarning2 = _interopRequireDefault(_routerWarning);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var RoutingContext = _react2.default.createClass({
  displayName: 'RoutingContext',
  componentWillMount: function componentWillMount() {
    "production" !== 'production' ? (0, _routerWarning2.default)(false, '`RoutingContext` has been renamed to `RouterContext`. Please use `import { RouterContext } from \'react-router\'`. http://tiny.cc/router-routercontext') : void 0;
  },
  render: function render() {
    return _react2.default.createElement(_RouterContext2.default, this.props);
  }
});

exports.default = RoutingContext;
module.exports = exports['default'];
},{"./RouterContext":205,"./routerWarning":224,"react":356}],208:[function(require,module,exports){
'use strict';

exports.__esModule = true;
exports.runEnterHooks = runEnterHooks;
exports.runChangeHooks = runChangeHooks;
exports.runLeaveHooks = runLeaveHooks;

var _AsyncUtils = require('./AsyncUtils');

var _routerWarning = require('./routerWarning');

var _routerWarning2 = _interopRequireDefault(_routerWarning);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function createTransitionHook(hook, route, asyncArity) {
  return function () {
    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    hook.apply(route, args);

    if (hook.length < asyncArity) {
      var callback = args[args.length - 1];
      // Assume hook executes synchronously and
      // automatically call the callback.
      callback();
    }
  };
}

function getEnterHooks(routes) {
  return routes.reduce(function (hooks, route) {
    if (route.onEnter) hooks.push(createTransitionHook(route.onEnter, route, 3));

    return hooks;
  }, []);
}

function getChangeHooks(routes) {
  return routes.reduce(function (hooks, route) {
    if (route.onChange) hooks.push(createTransitionHook(route.onChange, route, 4));
    return hooks;
  }, []);
}

function runTransitionHooks(length, iter, callback) {
  if (!length) {
    callback();
    return;
  }

  var redirectInfo = void 0;
  function replace(location, deprecatedPathname, deprecatedQuery) {
    if (deprecatedPathname) {
      "production" !== 'production' ? (0, _routerWarning2.default)(false, '`replaceState(state, pathname, query) is deprecated; use `replace(location)` with a location descriptor instead. http://tiny.cc/router-isActivedeprecated') : void 0;
      redirectInfo = {
        pathname: deprecatedPathname,
        query: deprecatedQuery,
        state: location
      };

      return;
    }

    redirectInfo = location;
  }

  (0, _AsyncUtils.loopAsync)(length, function (index, next, done) {
    iter(index, replace, function (error) {
      if (error || redirectInfo) {
        done(error, redirectInfo); // No need to continue.
      } else {
        next();
      }
    });
  }, callback);
}

/**
 * Runs all onEnter hooks in the given array of routes in order
 * with onEnter(nextState, replace, callback) and calls
 * callback(error, redirectInfo) when finished. The first hook
 * to use replace short-circuits the loop.
 *
 * If a hook needs to run asynchronously, it may use the callback
 * function. However, doing so will cause the transition to pause,
 * which could lead to a non-responsive UI if the hook is slow.
 */
function runEnterHooks(routes, nextState, callback) {
  var hooks = getEnterHooks(routes);
  return runTransitionHooks(hooks.length, function (index, replace, next) {
    hooks[index](nextState, replace, next);
  }, callback);
}

/**
 * Runs all onChange hooks in the given array of routes in order
 * with onChange(prevState, nextState, replace, callback) and calls
 * callback(error, redirectInfo) when finished. The first hook
 * to use replace short-circuits the loop.
 *
 * If a hook needs to run asynchronously, it may use the callback
 * function. However, doing so will cause the transition to pause,
 * which could lead to a non-responsive UI if the hook is slow.
 */
function runChangeHooks(routes, state, nextState, callback) {
  var hooks = getChangeHooks(routes);
  return runTransitionHooks(hooks.length, function (index, replace, next) {
    hooks[index](state, nextState, replace, next);
  }, callback);
}

/**
 * Runs all onLeave hooks in the given array of routes in order.
 */
function runLeaveHooks(routes, prevState) {
  for (var i = 0, len = routes.length; i < len; ++i) {
    if (routes[i].onLeave) routes[i].onLeave.call(routes[i], prevState);
  }
}
},{"./AsyncUtils":190,"./routerWarning":224}],209:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _RouterContext = require('./RouterContext');

var _RouterContext2 = _interopRequireDefault(_RouterContext);

var _routerWarning = require('./routerWarning');

var _routerWarning2 = _interopRequireDefault(_routerWarning);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = function () {
  for (var _len = arguments.length, middlewares = Array(_len), _key = 0; _key < _len; _key++) {
    middlewares[_key] = arguments[_key];
  }

  if ("production" !== 'production') {
    middlewares.forEach(function (middleware, index) {
      "production" !== 'production' ? (0, _routerWarning2.default)(middleware.renderRouterContext || middleware.renderRouteComponent, 'The middleware specified at index ' + index + ' does not appear to be ' + 'a valid React Router middleware.') : void 0;
    });
  }

  var withContext = middlewares.map(function (middleware) {
    return middleware.renderRouterContext;
  }).filter(Boolean);
  var withComponent = middlewares.map(function (middleware) {
    return middleware.renderRouteComponent;
  }).filter(Boolean);

  var makeCreateElement = function makeCreateElement() {
    var baseCreateElement = arguments.length <= 0 || arguments[0] === undefined ? _react.createElement : arguments[0];
    return function (Component, props) {
      return withComponent.reduceRight(function (previous, renderRouteComponent) {
        return renderRouteComponent(previous, props);
      }, baseCreateElement(Component, props));
    };
  };

  return function (renderProps) {
    return withContext.reduceRight(function (previous, renderRouterContext) {
      return renderRouterContext(previous, renderProps);
    }, _react2.default.createElement(_RouterContext2.default, _extends({}, renderProps, {
      createElement: makeCreateElement(renderProps.createElement)
    })));
  };
};

module.exports = exports['default'];
},{"./RouterContext":205,"./routerWarning":224,"react":356}],210:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _createBrowserHistory = require('history/lib/createBrowserHistory');

var _createBrowserHistory2 = _interopRequireDefault(_createBrowserHistory);

var _createRouterHistory = require('./createRouterHistory');

var _createRouterHistory2 = _interopRequireDefault(_createRouterHistory);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = (0, _createRouterHistory2.default)(_createBrowserHistory2.default);
module.exports = exports['default'];
},{"./createRouterHistory":213,"history/lib/createBrowserHistory":172}],211:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _PatternUtils = require('./PatternUtils');

function routeParamsChanged(route, prevState, nextState) {
  if (!route.path) return false;

  var paramNames = (0, _PatternUtils.getParamNames)(route.path);

  return paramNames.some(function (paramName) {
    return prevState.params[paramName] !== nextState.params[paramName];
  });
}

/**
 * Returns an object of { leaveRoutes, changeRoutes, enterRoutes } determined by
 * the change from prevState to nextState. We leave routes if either
 * 1) they are not in the next state or 2) they are in the next state
 * but their params have changed (i.e. /users/123 => /users/456).
 *
 * leaveRoutes are ordered starting at the leaf route of the tree
 * we're leaving up to the common parent route. enterRoutes are ordered
 * from the top of the tree we're entering down to the leaf route.
 *
 * changeRoutes are any routes that didn't leave or enter during
 * the transition.
 */
function computeChangedRoutes(prevState, nextState) {
  var prevRoutes = prevState && prevState.routes;
  var nextRoutes = nextState.routes;

  var leaveRoutes = void 0,
      changeRoutes = void 0,
      enterRoutes = void 0;
  if (prevRoutes) {
    (function () {
      var parentIsLeaving = false;
      leaveRoutes = prevRoutes.filter(function (route) {
        if (parentIsLeaving) {
          return true;
        } else {
          var isLeaving = nextRoutes.indexOf(route) === -1 || routeParamsChanged(route, prevState, nextState);
          if (isLeaving) parentIsLeaving = true;
          return isLeaving;
        }
      });

      // onLeave hooks start at the leaf route.
      leaveRoutes.reverse();

      enterRoutes = [];
      changeRoutes = [];

      nextRoutes.forEach(function (route) {
        var isNew = prevRoutes.indexOf(route) === -1;
        var paramsChanged = leaveRoutes.indexOf(route) !== -1;

        if (isNew || paramsChanged) enterRoutes.push(route);else changeRoutes.push(route);
      });
    })();
  } else {
    leaveRoutes = [];
    changeRoutes = [];
    enterRoutes = nextRoutes;
  }

  return {
    leaveRoutes: leaveRoutes,
    changeRoutes: changeRoutes,
    enterRoutes: enterRoutes
  };
}

exports.default = computeChangedRoutes;
module.exports = exports['default'];
},{"./PatternUtils":198}],212:[function(require,module,exports){
'use strict';

exports.__esModule = true;
exports.default = createMemoryHistory;

var _useQueries = require('history/lib/useQueries');

var _useQueries2 = _interopRequireDefault(_useQueries);

var _useBasename = require('history/lib/useBasename');

var _useBasename2 = _interopRequireDefault(_useBasename);

var _createMemoryHistory = require('history/lib/createMemoryHistory');

var _createMemoryHistory2 = _interopRequireDefault(_createMemoryHistory);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function createMemoryHistory(options) {
  // signatures and type checking differ between `useRoutes` and
  // `createMemoryHistory`, have to create `memoryHistory` first because
  // `useQueries` doesn't understand the signature
  var memoryHistory = (0, _createMemoryHistory2.default)(options);
  var createHistory = function createHistory() {
    return memoryHistory;
  };
  var history = (0, _useQueries2.default)((0, _useBasename2.default)(createHistory))(options);
  history.__v2_compatible__ = true;
  return history;
}
module.exports = exports['default'];
},{"history/lib/createMemoryHistory":177,"history/lib/useBasename":180,"history/lib/useQueries":181}],213:[function(require,module,exports){
'use strict';

exports.__esModule = true;

exports.default = function (createHistory) {
  var history = void 0;
  if (canUseDOM) history = (0, _useRouterHistory2.default)(createHistory)();
  return history;
};

var _useRouterHistory = require('./useRouterHistory');

var _useRouterHistory2 = _interopRequireDefault(_useRouterHistory);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var canUseDOM = !!(typeof window !== 'undefined' && window.document && window.document.createElement);

module.exports = exports['default'];
},{"./useRouterHistory":225}],214:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

exports.default = createTransitionManager;

var _routerWarning = require('./routerWarning');

var _routerWarning2 = _interopRequireDefault(_routerWarning);

var _Actions = require('history/lib/Actions');

var _computeChangedRoutes2 = require('./computeChangedRoutes');

var _computeChangedRoutes3 = _interopRequireDefault(_computeChangedRoutes2);

var _TransitionUtils = require('./TransitionUtils');

var _isActive2 = require('./isActive');

var _isActive3 = _interopRequireDefault(_isActive2);

var _getComponents = require('./getComponents');

var _getComponents2 = _interopRequireDefault(_getComponents);

var _matchRoutes = require('./matchRoutes');

var _matchRoutes2 = _interopRequireDefault(_matchRoutes);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function hasAnyProperties(object) {
  for (var p in object) {
    if (Object.prototype.hasOwnProperty.call(object, p)) return true;
  }return false;
}

function createTransitionManager(history, routes) {
  var state = {};

  // Signature should be (location, indexOnly), but needs to support (path,
  // query, indexOnly)
  function isActive(location) {
    var indexOnlyOrDeprecatedQuery = arguments.length <= 1 || arguments[1] === undefined ? false : arguments[1];
    var deprecatedIndexOnly = arguments.length <= 2 || arguments[2] === undefined ? null : arguments[2];

    var indexOnly = void 0;
    if (indexOnlyOrDeprecatedQuery && indexOnlyOrDeprecatedQuery !== true || deprecatedIndexOnly !== null) {
      "production" !== 'production' ? (0, _routerWarning2.default)(false, '`isActive(pathname, query, indexOnly) is deprecated; use `isActive(location, indexOnly)` with a location descriptor instead. http://tiny.cc/router-isActivedeprecated') : void 0;
      location = { pathname: location, query: indexOnlyOrDeprecatedQuery };
      indexOnly = deprecatedIndexOnly || false;
    } else {
      location = history.createLocation(location);
      indexOnly = indexOnlyOrDeprecatedQuery;
    }

    return (0, _isActive3.default)(location, indexOnly, state.location, state.routes, state.params);
  }

  function createLocationFromRedirectInfo(location) {
    return history.createLocation(location, _Actions.REPLACE);
  }

  var partialNextState = void 0;

  function match(location, callback) {
    if (partialNextState && partialNextState.location === location) {
      // Continue from where we left off.
      finishMatch(partialNextState, callback);
    } else {
      (0, _matchRoutes2.default)(routes, location, function (error, nextState) {
        if (error) {
          callback(error);
        } else if (nextState) {
          finishMatch(_extends({}, nextState, { location: location }), callback);
        } else {
          callback();
        }
      });
    }
  }

  function finishMatch(nextState, callback) {
    var _computeChangedRoutes = (0, _computeChangedRoutes3.default)(state, nextState);

    var leaveRoutes = _computeChangedRoutes.leaveRoutes;
    var changeRoutes = _computeChangedRoutes.changeRoutes;
    var enterRoutes = _computeChangedRoutes.enterRoutes;


    (0, _TransitionUtils.runLeaveHooks)(leaveRoutes, state);

    // Tear down confirmation hooks for left routes
    leaveRoutes.filter(function (route) {
      return enterRoutes.indexOf(route) === -1;
    }).forEach(removeListenBeforeHooksForRoute);

    // change and enter hooks are run in series
    (0, _TransitionUtils.runChangeHooks)(changeRoutes, state, nextState, function (error, redirectInfo) {
      if (error || redirectInfo) return handleErrorOrRedirect(error, redirectInfo);

      (0, _TransitionUtils.runEnterHooks)(enterRoutes, nextState, finishEnterHooks);
    });

    function finishEnterHooks(error, redirectInfo) {
      if (error || redirectInfo) return handleErrorOrRedirect(error, redirectInfo);

      // TODO: Fetch components after state is updated.
      (0, _getComponents2.default)(nextState, function (error, components) {
        if (error) {
          callback(error);
        } else {
          // TODO: Make match a pure function and have some other API
          // for "match and update state".
          callback(null, null, state = _extends({}, nextState, { components: components }));
        }
      });
    }

    function handleErrorOrRedirect(error, redirectInfo) {
      if (error) callback(error);else callback(null, createLocationFromRedirectInfo(redirectInfo));
    }
  }

  var RouteGuid = 1;

  function getRouteID(route) {
    var create = arguments.length <= 1 || arguments[1] === undefined ? true : arguments[1];

    return route.__id__ || create && (route.__id__ = RouteGuid++);
  }

  var RouteHooks = Object.create(null);

  function getRouteHooksForRoutes(routes) {
    return routes.reduce(function (hooks, route) {
      hooks.push.apply(hooks, RouteHooks[getRouteID(route)]);
      return hooks;
    }, []);
  }

  function transitionHook(location, callback) {
    (0, _matchRoutes2.default)(routes, location, function (error, nextState) {
      if (nextState == null) {
        // TODO: We didn't actually match anything, but hang
        // onto error/nextState so we don't have to matchRoutes
        // again in the listen callback.
        callback();
        return;
      }

      // Cache some state here so we don't have to
      // matchRoutes() again in the listen callback.
      partialNextState = _extends({}, nextState, { location: location });

      var hooks = getRouteHooksForRoutes((0, _computeChangedRoutes3.default)(state, partialNextState).leaveRoutes);

      var result = void 0;
      for (var i = 0, len = hooks.length; result == null && i < len; ++i) {
        // Passing the location arg here indicates to
        // the user that this is a transition hook.
        result = hooks[i](location);
      }

      callback(result);
    });
  }

  /* istanbul ignore next: untestable with Karma */
  function beforeUnloadHook() {
    // Synchronously check to see if any route hooks want
    // to prevent the current window/tab from closing.
    if (state.routes) {
      var hooks = getRouteHooksForRoutes(state.routes);

      var message = void 0;
      for (var i = 0, len = hooks.length; typeof message !== 'string' && i < len; ++i) {
        // Passing no args indicates to the user that this is a
        // beforeunload hook. We don't know the next location.
        message = hooks[i]();
      }

      return message;
    }
  }

  var unlistenBefore = void 0,
      unlistenBeforeUnload = void 0;

  function removeListenBeforeHooksForRoute(route) {
    var routeID = getRouteID(route, false);
    if (!routeID) {
      return;
    }

    delete RouteHooks[routeID];

    if (!hasAnyProperties(RouteHooks)) {
      // teardown transition & beforeunload hooks
      if (unlistenBefore) {
        unlistenBefore();
        unlistenBefore = null;
      }

      if (unlistenBeforeUnload) {
        unlistenBeforeUnload();
        unlistenBeforeUnload = null;
      }
    }
  }

  /**
   * Registers the given hook function to run before leaving the given route.
   *
   * During a normal transition, the hook function receives the next location
   * as its only argument and can return either a prompt message (string) to show the user,
   * to make sure they want to leave the page; or `false`, to prevent the transition.
   * Any other return value will have no effect.
   *
   * During the beforeunload event (in browsers) the hook receives no arguments.
   * In this case it must return a prompt message to prevent the transition.
   *
   * Returns a function that may be used to unbind the listener.
   */
  function listenBeforeLeavingRoute(route, hook) {
    // TODO: Warn if they register for a route that isn't currently
    // active. They're probably doing something wrong, like re-creating
    // route objects on every location change.
    var routeID = getRouteID(route);
    var hooks = RouteHooks[routeID];

    if (!hooks) {
      var thereWereNoRouteHooks = !hasAnyProperties(RouteHooks);

      RouteHooks[routeID] = [hook];

      if (thereWereNoRouteHooks) {
        // setup transition & beforeunload hooks
        unlistenBefore = history.listenBefore(transitionHook);

        if (history.listenBeforeUnload) unlistenBeforeUnload = history.listenBeforeUnload(beforeUnloadHook);
      }
    } else {
      if (hooks.indexOf(hook) === -1) {
        "production" !== 'production' ? (0, _routerWarning2.default)(false, 'adding multiple leave hooks for the same route is deprecated; manage multiple confirmations in your own code instead') : void 0;

        hooks.push(hook);
      }
    }

    return function () {
      var hooks = RouteHooks[routeID];

      if (hooks) {
        var newHooks = hooks.filter(function (item) {
          return item !== hook;
        });

        if (newHooks.length === 0) {
          removeListenBeforeHooksForRoute(route);
        } else {
          RouteHooks[routeID] = newHooks;
        }
      }
    };
  }

  /**
   * This is the API for stateful environments. As the location
   * changes, we update state and call the listener. We can also
   * gracefully handle errors and redirects.
   */
  function listen(listener) {
    // TODO: Only use a single history listener. Otherwise we'll
    // end up with multiple concurrent calls to match.
    return history.listen(function (location) {
      if (state.location === location) {
        listener(null, state);
      } else {
        match(location, function (error, redirectLocation, nextState) {
          if (error) {
            listener(error);
          } else if (redirectLocation) {
            history.transitionTo(redirectLocation);
          } else if (nextState) {
            listener(null, nextState);
          } else {
            "production" !== 'production' ? (0, _routerWarning2.default)(false, 'Location "%s" did not match any routes', location.pathname + location.search + location.hash) : void 0;
          }
        });
      }
    });
  }

  return {
    isActive: isActive,
    match: match,
    listenBeforeLeavingRoute: listenBeforeLeavingRoute,
    listen: listen
  };
}

//export default useRoutes

module.exports = exports['default'];
},{"./TransitionUtils":208,"./computeChangedRoutes":211,"./getComponents":216,"./isActive":220,"./matchRoutes":223,"./routerWarning":224,"history/lib/Actions":166}],215:[function(require,module,exports){
'use strict';

exports.__esModule = true;
exports.canUseMembrane = undefined;

var _routerWarning = require('./routerWarning');

var _routerWarning2 = _interopRequireDefault(_routerWarning);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var canUseMembrane = exports.canUseMembrane = false;

// No-op by default.
var deprecateObjectProperties = function deprecateObjectProperties(object) {
  return object;
};

if ("production" !== 'production') {
  try {
    if (Object.defineProperty({}, 'x', {
      get: function get() {
        return true;
      }
    }).x) {
      exports.canUseMembrane = canUseMembrane = true;
    }
    /* eslint-disable no-empty */
  } catch (e) {}
  /* eslint-enable no-empty */

  if (canUseMembrane) {
    deprecateObjectProperties = function deprecateObjectProperties(object, message) {
      // Wrap the deprecated object in a membrane to warn on property access.
      var membrane = {};

      var _loop = function _loop(prop) {
        if (!Object.prototype.hasOwnProperty.call(object, prop)) {
          return 'continue';
        }

        if (typeof object[prop] === 'function') {
          // Can't use fat arrow here because of use of arguments below.
          membrane[prop] = function () {
            "production" !== 'production' ? (0, _routerWarning2.default)(false, message) : void 0;
            return object[prop].apply(object, arguments);
          };
          return 'continue';
        }

        // These properties are non-enumerable to prevent React dev tools from
        // seeing them and causing spurious warnings when accessing them. In
        // principle this could be done with a proxy, but support for the
        // ownKeys trap on proxies is not universal, even among browsers that
        // otherwise support proxies.
        Object.defineProperty(membrane, prop, {
          get: function get() {
            "production" !== 'production' ? (0, _routerWarning2.default)(false, message) : void 0;
            return object[prop];
          }
        });
      };

      for (var prop in object) {
        var _ret = _loop(prop);

        if (_ret === 'continue') continue;
      }

      return membrane;
    };
  }
}

exports.default = deprecateObjectProperties;
},{"./routerWarning":224}],216:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _AsyncUtils = require('./AsyncUtils');

var _makeStateWithLocation = require('./makeStateWithLocation');

var _makeStateWithLocation2 = _interopRequireDefault(_makeStateWithLocation);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function getComponentsForRoute(nextState, route, callback) {
  if (route.component || route.components) {
    callback(null, route.component || route.components);
    return;
  }

  var getComponent = route.getComponent || route.getComponents;
  if (!getComponent) {
    callback();
    return;
  }

  var location = nextState.location;

  var nextStateWithLocation = (0, _makeStateWithLocation2.default)(nextState, location);

  getComponent.call(route, nextStateWithLocation, callback);
}

/**
 * Asynchronously fetches all components needed for the given router
 * state and calls callback(error, components) when finished.
 *
 * Note: This operation may finish synchronously if no routes have an
 * asynchronous getComponents method.
 */
function getComponents(nextState, callback) {
  (0, _AsyncUtils.mapAsync)(nextState.routes, function (route, index, callback) {
    getComponentsForRoute(nextState, route, callback);
  }, callback);
}

exports.default = getComponents;
module.exports = exports['default'];
},{"./AsyncUtils":190,"./makeStateWithLocation":221}],217:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _PatternUtils = require('./PatternUtils');

/**
 * Extracts an object of params the given route cares about from
 * the given params object.
 */
function getRouteParams(route, params) {
  var routeParams = {};

  if (!route.path) return routeParams;

  (0, _PatternUtils.getParamNames)(route.path).forEach(function (p) {
    if (Object.prototype.hasOwnProperty.call(params, p)) {
      routeParams[p] = params[p];
    }
  });

  return routeParams;
}

exports.default = getRouteParams;
module.exports = exports['default'];
},{"./PatternUtils":198}],218:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _createHashHistory = require('history/lib/createHashHistory');

var _createHashHistory2 = _interopRequireDefault(_createHashHistory);

var _createRouterHistory = require('./createRouterHistory');

var _createRouterHistory2 = _interopRequireDefault(_createRouterHistory);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = (0, _createRouterHistory2.default)(_createHashHistory2.default);
module.exports = exports['default'];
},{"./createRouterHistory":213,"history/lib/createHashHistory":174}],219:[function(require,module,exports){
'use strict';

exports.__esModule = true;
exports.createMemoryHistory = exports.hashHistory = exports.browserHistory = exports.applyRouterMiddleware = exports.formatPattern = exports.useRouterHistory = exports.match = exports.routerShape = exports.locationShape = exports.PropTypes = exports.RoutingContext = exports.RouterContext = exports.createRoutes = exports.useRoutes = exports.RouteContext = exports.Lifecycle = exports.History = exports.Route = exports.Redirect = exports.IndexRoute = exports.IndexRedirect = exports.withRouter = exports.IndexLink = exports.Link = exports.Router = undefined;

var _RouteUtils = require('./RouteUtils');

Object.defineProperty(exports, 'createRoutes', {
  enumerable: true,
  get: function get() {
    return _RouteUtils.createRoutes;
  }
});

var _PropTypes2 = require('./PropTypes');

Object.defineProperty(exports, 'locationShape', {
  enumerable: true,
  get: function get() {
    return _PropTypes2.locationShape;
  }
});
Object.defineProperty(exports, 'routerShape', {
  enumerable: true,
  get: function get() {
    return _PropTypes2.routerShape;
  }
});

var _PatternUtils = require('./PatternUtils');

Object.defineProperty(exports, 'formatPattern', {
  enumerable: true,
  get: function get() {
    return _PatternUtils.formatPattern;
  }
});

var _Router2 = require('./Router');

var _Router3 = _interopRequireDefault(_Router2);

var _Link2 = require('./Link');

var _Link3 = _interopRequireDefault(_Link2);

var _IndexLink2 = require('./IndexLink');

var _IndexLink3 = _interopRequireDefault(_IndexLink2);

var _withRouter2 = require('./withRouter');

var _withRouter3 = _interopRequireDefault(_withRouter2);

var _IndexRedirect2 = require('./IndexRedirect');

var _IndexRedirect3 = _interopRequireDefault(_IndexRedirect2);

var _IndexRoute2 = require('./IndexRoute');

var _IndexRoute3 = _interopRequireDefault(_IndexRoute2);

var _Redirect2 = require('./Redirect');

var _Redirect3 = _interopRequireDefault(_Redirect2);

var _Route2 = require('./Route');

var _Route3 = _interopRequireDefault(_Route2);

var _History2 = require('./History');

var _History3 = _interopRequireDefault(_History2);

var _Lifecycle2 = require('./Lifecycle');

var _Lifecycle3 = _interopRequireDefault(_Lifecycle2);

var _RouteContext2 = require('./RouteContext');

var _RouteContext3 = _interopRequireDefault(_RouteContext2);

var _useRoutes2 = require('./useRoutes');

var _useRoutes3 = _interopRequireDefault(_useRoutes2);

var _RouterContext2 = require('./RouterContext');

var _RouterContext3 = _interopRequireDefault(_RouterContext2);

var _RoutingContext2 = require('./RoutingContext');

var _RoutingContext3 = _interopRequireDefault(_RoutingContext2);

var _PropTypes3 = _interopRequireDefault(_PropTypes2);

var _match2 = require('./match');

var _match3 = _interopRequireDefault(_match2);

var _useRouterHistory2 = require('./useRouterHistory');

var _useRouterHistory3 = _interopRequireDefault(_useRouterHistory2);

var _applyRouterMiddleware2 = require('./applyRouterMiddleware');

var _applyRouterMiddleware3 = _interopRequireDefault(_applyRouterMiddleware2);

var _browserHistory2 = require('./browserHistory');

var _browserHistory3 = _interopRequireDefault(_browserHistory2);

var _hashHistory2 = require('./hashHistory');

var _hashHistory3 = _interopRequireDefault(_hashHistory2);

var _createMemoryHistory2 = require('./createMemoryHistory');

var _createMemoryHistory3 = _interopRequireDefault(_createMemoryHistory2);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.Router = _Router3.default; /* components */

exports.Link = _Link3.default;
exports.IndexLink = _IndexLink3.default;
exports.withRouter = _withRouter3.default;

/* components (configuration) */

exports.IndexRedirect = _IndexRedirect3.default;
exports.IndexRoute = _IndexRoute3.default;
exports.Redirect = _Redirect3.default;
exports.Route = _Route3.default;

/* mixins */

exports.History = _History3.default;
exports.Lifecycle = _Lifecycle3.default;
exports.RouteContext = _RouteContext3.default;

/* utils */

exports.useRoutes = _useRoutes3.default;
exports.RouterContext = _RouterContext3.default;
exports.RoutingContext = _RoutingContext3.default;
exports.PropTypes = _PropTypes3.default;
exports.match = _match3.default;
exports.useRouterHistory = _useRouterHistory3.default;
exports.applyRouterMiddleware = _applyRouterMiddleware3.default;

/* histories */

exports.browserHistory = _browserHistory3.default;
exports.hashHistory = _hashHistory3.default;
exports.createMemoryHistory = _createMemoryHistory3.default;
},{"./History":191,"./IndexLink":192,"./IndexRedirect":193,"./IndexRoute":194,"./Lifecycle":196,"./Link":197,"./PatternUtils":198,"./PropTypes":199,"./Redirect":200,"./Route":201,"./RouteContext":202,"./RouteUtils":203,"./Router":204,"./RouterContext":205,"./RoutingContext":207,"./applyRouterMiddleware":209,"./browserHistory":210,"./createMemoryHistory":212,"./hashHistory":218,"./match":222,"./useRouterHistory":225,"./useRoutes":226,"./withRouter":227}],220:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol ? "symbol" : typeof obj; };

exports.default = isActive;

var _PatternUtils = require('./PatternUtils');

function deepEqual(a, b) {
  if (a == b) return true;

  if (a == null || b == null) return false;

  if (Array.isArray(a)) {
    return Array.isArray(b) && a.length === b.length && a.every(function (item, index) {
      return deepEqual(item, b[index]);
    });
  }

  if ((typeof a === 'undefined' ? 'undefined' : _typeof(a)) === 'object') {
    for (var p in a) {
      if (!Object.prototype.hasOwnProperty.call(a, p)) {
        continue;
      }

      if (a[p] === undefined) {
        if (b[p] !== undefined) {
          return false;
        }
      } else if (!Object.prototype.hasOwnProperty.call(b, p)) {
        return false;
      } else if (!deepEqual(a[p], b[p])) {
        return false;
      }
    }

    return true;
  }

  return String(a) === String(b);
}

/**
 * Returns true if the current pathname matches the supplied one, net of
 * leading and trailing slash normalization. This is sufficient for an
 * indexOnly route match.
 */
function pathIsActive(pathname, currentPathname) {
  // Normalize leading slash for consistency. Leading slash on pathname has
  // already been normalized in isActive. See caveat there.
  if (currentPathname.charAt(0) !== '/') {
    currentPathname = '/' + currentPathname;
  }

  // Normalize the end of both path names too. Maybe `/foo/` shouldn't show
  // `/foo` as active, but in this case, we would already have failed the
  // match.
  if (pathname.charAt(pathname.length - 1) !== '/') {
    pathname += '/';
  }
  if (currentPathname.charAt(currentPathname.length - 1) !== '/') {
    currentPathname += '/';
  }

  return currentPathname === pathname;
}

/**
 * Returns true if the given pathname matches the active routes and params.
 */
function routeIsActive(pathname, routes, params) {
  var remainingPathname = pathname,
      paramNames = [],
      paramValues = [];

  // for...of would work here but it's probably slower post-transpilation.
  for (var i = 0, len = routes.length; i < len; ++i) {
    var route = routes[i];
    var pattern = route.path || '';

    if (pattern.charAt(0) === '/') {
      remainingPathname = pathname;
      paramNames = [];
      paramValues = [];
    }

    if (remainingPathname !== null && pattern) {
      var matched = (0, _PatternUtils.matchPattern)(pattern, remainingPathname);
      if (matched) {
        remainingPathname = matched.remainingPathname;
        paramNames = [].concat(paramNames, matched.paramNames);
        paramValues = [].concat(paramValues, matched.paramValues);
      } else {
        remainingPathname = null;
      }

      if (remainingPathname === '') {
        // We have an exact match on the route. Just check that all the params
        // match.
        // FIXME: This doesn't work on repeated params.
        return paramNames.every(function (paramName, index) {
          return String(paramValues[index]) === String(params[paramName]);
        });
      }
    }
  }

  return false;
}

/**
 * Returns true if all key/value pairs in the given query are
 * currently active.
 */
function queryIsActive(query, activeQuery) {
  if (activeQuery == null) return query == null;

  if (query == null) return true;

  return deepEqual(query, activeQuery);
}

/**
 * Returns true if a <Link> to the given pathname/query combination is
 * currently active.
 */
function isActive(_ref, indexOnly, currentLocation, routes, params) {
  var pathname = _ref.pathname;
  var query = _ref.query;

  if (currentLocation == null) return false;

  // TODO: This is a bit ugly. It keeps around support for treating pathnames
  // without preceding slashes as absolute paths, but possibly also works
  // around the same quirks with basenames as in matchRoutes.
  if (pathname.charAt(0) !== '/') {
    pathname = '/' + pathname;
  }

  if (!pathIsActive(pathname, currentLocation.pathname)) {
    // The path check is necessary and sufficient for indexOnly, but otherwise
    // we still need to check the routes.
    if (indexOnly || !routeIsActive(pathname, routes, params)) {
      return false;
    }
  }

  return queryIsActive(query, currentLocation.query);
}
module.exports = exports['default'];
},{"./PatternUtils":198}],221:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

exports.default = makeStateWithLocation;

var _deprecateObjectProperties = require('./deprecateObjectProperties');

var _routerWarning = require('./routerWarning');

var _routerWarning2 = _interopRequireDefault(_routerWarning);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function makeStateWithLocation(state, location) {
  if ("production" !== 'production' && _deprecateObjectProperties.canUseMembrane) {
    var stateWithLocation = _extends({}, state);

    // I don't use deprecateObjectProperties here because I want to keep the
    // same code path between development and production, in that we just
    // assign extra properties to the copy of the state object in both cases.

    var _loop = function _loop(prop) {
      if (!Object.prototype.hasOwnProperty.call(location, prop)) {
        return 'continue';
      }

      Object.defineProperty(stateWithLocation, prop, {
        get: function get() {
          "production" !== 'production' ? (0, _routerWarning2.default)(false, 'Accessing location properties directly from the first argument to `getComponent`, `getComponents`, `getChildRoutes`, and `getIndexRoute` is deprecated. That argument is now the router state (`nextState` or `partialNextState`) rather than the location. To access the location, use `nextState.location` or `partialNextState.location`.') : void 0;
          return location[prop];
        }
      });
    };

    for (var prop in location) {
      var _ret = _loop(prop);

      if (_ret === 'continue') continue;
    }

    return stateWithLocation;
  }

  return _extends({}, state, location);
}
module.exports = exports['default'];
},{"./deprecateObjectProperties":215,"./routerWarning":224}],222:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _invariant = require('invariant');

var _invariant2 = _interopRequireDefault(_invariant);

var _createMemoryHistory = require('./createMemoryHistory');

var _createMemoryHistory2 = _interopRequireDefault(_createMemoryHistory);

var _createTransitionManager = require('./createTransitionManager');

var _createTransitionManager2 = _interopRequireDefault(_createTransitionManager);

var _RouteUtils = require('./RouteUtils');

var _RouterUtils = require('./RouterUtils');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _objectWithoutProperties(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }

/**
 * A high-level API to be used for server-side rendering.
 *
 * This function matches a location to a set of routes and calls
 * callback(error, redirectLocation, renderProps) when finished.
 *
 * Note: You probably don't want to use this in a browser unless you're using
 * server-side rendering with async routes.
 */
function match(_ref, callback) {
  var history = _ref.history;
  var routes = _ref.routes;
  var location = _ref.location;

  var options = _objectWithoutProperties(_ref, ['history', 'routes', 'location']);

  !(history || location) ? "production" !== 'production' ? (0, _invariant2.default)(false, 'match needs a history or a location') : (0, _invariant2.default)(false) : void 0;

  history = history ? history : (0, _createMemoryHistory2.default)(options);
  var transitionManager = (0, _createTransitionManager2.default)(history, (0, _RouteUtils.createRoutes)(routes));

  var unlisten = void 0;

  if (location) {
    // Allow match({ location: '/the/path', ... })
    location = history.createLocation(location);
  } else {
    // Pick up the location from the history via synchronous history.listen
    // call if needed.
    unlisten = history.listen(function (historyLocation) {
      location = historyLocation;
    });
  }

  var router = (0, _RouterUtils.createRouterObject)(history, transitionManager);
  history = (0, _RouterUtils.createRoutingHistory)(history, transitionManager);

  transitionManager.match(location, function (error, redirectLocation, nextState) {
    callback(error, redirectLocation, nextState && _extends({}, nextState, {
      history: history,
      router: router,
      matchContext: { history: history, transitionManager: transitionManager, router: router }
    }));

    // Defer removing the listener to here to prevent DOM histories from having
    // to unwind DOM event listeners unnecessarily, in case callback renders a
    // <Router> and attaches another history listener.
    if (unlisten) {
      unlisten();
    }
  });
}

exports.default = match;
module.exports = exports['default'];
},{"./RouteUtils":203,"./RouterUtils":206,"./createMemoryHistory":212,"./createTransitionManager":214,"invariant":183}],223:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol ? "symbol" : typeof obj; };

exports.default = matchRoutes;

var _AsyncUtils = require('./AsyncUtils');

var _makeStateWithLocation = require('./makeStateWithLocation');

var _makeStateWithLocation2 = _interopRequireDefault(_makeStateWithLocation);

var _PatternUtils = require('./PatternUtils');

var _routerWarning = require('./routerWarning');

var _routerWarning2 = _interopRequireDefault(_routerWarning);

var _RouteUtils = require('./RouteUtils');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function getChildRoutes(route, location, paramNames, paramValues, callback) {
  if (route.childRoutes) {
    return [null, route.childRoutes];
  }
  if (!route.getChildRoutes) {
    return [];
  }

  var sync = true,
      result = void 0;

  var partialNextState = {
    location: location,
    params: createParams(paramNames, paramValues)
  };

  var partialNextStateWithLocation = (0, _makeStateWithLocation2.default)(partialNextState, location);

  route.getChildRoutes(partialNextStateWithLocation, function (error, childRoutes) {
    childRoutes = !error && (0, _RouteUtils.createRoutes)(childRoutes);
    if (sync) {
      result = [error, childRoutes];
      return;
    }

    callback(error, childRoutes);
  });

  sync = false;
  return result; // Might be undefined.
}

function getIndexRoute(route, location, paramNames, paramValues, callback) {
  if (route.indexRoute) {
    callback(null, route.indexRoute);
  } else if (route.getIndexRoute) {
    var partialNextState = {
      location: location,
      params: createParams(paramNames, paramValues)
    };

    var partialNextStateWithLocation = (0, _makeStateWithLocation2.default)(partialNextState, location);

    route.getIndexRoute(partialNextStateWithLocation, function (error, indexRoute) {
      callback(error, !error && (0, _RouteUtils.createRoutes)(indexRoute)[0]);
    });
  } else if (route.childRoutes) {
    (function () {
      var pathless = route.childRoutes.filter(function (childRoute) {
        return !childRoute.path;
      });

      (0, _AsyncUtils.loopAsync)(pathless.length, function (index, next, done) {
        getIndexRoute(pathless[index], location, paramNames, paramValues, function (error, indexRoute) {
          if (error || indexRoute) {
            var routes = [pathless[index]].concat(Array.isArray(indexRoute) ? indexRoute : [indexRoute]);
            done(error, routes);
          } else {
            next();
          }
        });
      }, function (err, routes) {
        callback(null, routes);
      });
    })();
  } else {
    callback();
  }
}

function assignParams(params, paramNames, paramValues) {
  return paramNames.reduce(function (params, paramName, index) {
    var paramValue = paramValues && paramValues[index];

    if (Array.isArray(params[paramName])) {
      params[paramName].push(paramValue);
    } else if (paramName in params) {
      params[paramName] = [params[paramName], paramValue];
    } else {
      params[paramName] = paramValue;
    }

    return params;
  }, params);
}

function createParams(paramNames, paramValues) {
  return assignParams({}, paramNames, paramValues);
}

function matchRouteDeep(route, location, remainingPathname, paramNames, paramValues, callback) {
  var pattern = route.path || '';

  if (pattern.charAt(0) === '/') {
    remainingPathname = location.pathname;
    paramNames = [];
    paramValues = [];
  }

  // Only try to match the path if the route actually has a pattern, and if
  // we're not just searching for potential nested absolute paths.
  if (remainingPathname !== null && pattern) {
    try {
      var matched = (0, _PatternUtils.matchPattern)(pattern, remainingPathname);
      if (matched) {
        remainingPathname = matched.remainingPathname;
        paramNames = [].concat(paramNames, matched.paramNames);
        paramValues = [].concat(paramValues, matched.paramValues);
      } else {
        remainingPathname = null;
      }
    } catch (error) {
      callback(error);
    }

    // By assumption, pattern is non-empty here, which is the prerequisite for
    // actually terminating a match.
    if (remainingPathname === '') {
      var _ret2 = function () {
        var match = {
          routes: [route],
          params: createParams(paramNames, paramValues)
        };

        getIndexRoute(route, location, paramNames, paramValues, function (error, indexRoute) {
          if (error) {
            callback(error);
          } else {
            if (Array.isArray(indexRoute)) {
              var _match$routes;

              "production" !== 'production' ? (0, _routerWarning2.default)(indexRoute.every(function (route) {
                return !route.path;
              }), 'Index routes should not have paths') : void 0;
              (_match$routes = match.routes).push.apply(_match$routes, indexRoute);
            } else if (indexRoute) {
              "production" !== 'production' ? (0, _routerWarning2.default)(!indexRoute.path, 'Index routes should not have paths') : void 0;
              match.routes.push(indexRoute);
            }

            callback(null, match);
          }
        });

        return {
          v: void 0
        };
      }();

      if ((typeof _ret2 === 'undefined' ? 'undefined' : _typeof(_ret2)) === "object") return _ret2.v;
    }
  }

  if (remainingPathname != null || route.childRoutes) {
    // Either a) this route matched at least some of the path or b)
    // we don't have to load this route's children asynchronously. In
    // either case continue checking for matches in the subtree.
    var onChildRoutes = function onChildRoutes(error, childRoutes) {
      if (error) {
        callback(error);
      } else if (childRoutes) {
        // Check the child routes to see if any of them match.
        matchRoutes(childRoutes, location, function (error, match) {
          if (error) {
            callback(error);
          } else if (match) {
            // A child route matched! Augment the match and pass it up the stack.
            match.routes.unshift(route);
            callback(null, match);
          } else {
            callback();
          }
        }, remainingPathname, paramNames, paramValues);
      } else {
        callback();
      }
    };

    var result = getChildRoutes(route, location, paramNames, paramValues, onChildRoutes);
    if (result) {
      onChildRoutes.apply(undefined, result);
    }
  } else {
    callback();
  }
}

/**
 * Asynchronously matches the given location to a set of routes and calls
 * callback(error, state) when finished. The state object will have the
 * following properties:
 *
 * - routes       An array of routes that matched, in hierarchical order
 * - params       An object of URL parameters
 *
 * Note: This operation may finish synchronously if no routes have an
 * asynchronous getChildRoutes method.
 */
function matchRoutes(routes, location, callback, remainingPathname) {
  var paramNames = arguments.length <= 4 || arguments[4] === undefined ? [] : arguments[4];
  var paramValues = arguments.length <= 5 || arguments[5] === undefined ? [] : arguments[5];

  if (remainingPathname === undefined) {
    // TODO: This is a little bit ugly, but it works around a quirk in history
    // that strips the leading slash from pathnames when using basenames with
    // trailing slashes.
    if (location.pathname.charAt(0) !== '/') {
      location = _extends({}, location, {
        pathname: '/' + location.pathname
      });
    }
    remainingPathname = location.pathname;
  }

  (0, _AsyncUtils.loopAsync)(routes.length, function (index, next, done) {
    matchRouteDeep(routes[index], location, remainingPathname, paramNames, paramValues, function (error, match) {
      if (error || match) {
        done(error, match);
      } else {
        next();
      }
    });
  }, callback);
}
module.exports = exports['default'];
},{"./AsyncUtils":190,"./PatternUtils":198,"./RouteUtils":203,"./makeStateWithLocation":221,"./routerWarning":224}],224:[function(require,module,exports){
'use strict';

exports.__esModule = true;
exports.default = routerWarning;
exports._resetWarned = _resetWarned;

var _warning = require('warning');

var _warning2 = _interopRequireDefault(_warning);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var warned = {};

function routerWarning(falseToWarn, message) {
  // Only issue deprecation warnings once.
  if (message.indexOf('deprecated') !== -1) {
    if (warned[message]) {
      return;
    }

    warned[message] = true;
  }

  message = '[react-router] ' + message;

  for (var _len = arguments.length, args = Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
    args[_key - 2] = arguments[_key];
  }

  _warning2.default.apply(undefined, [falseToWarn, message].concat(args));
}

function _resetWarned() {
  warned = {};
}
},{"warning":359}],225:[function(require,module,exports){
'use strict';

exports.__esModule = true;
exports.default = useRouterHistory;

var _useQueries = require('history/lib/useQueries');

var _useQueries2 = _interopRequireDefault(_useQueries);

var _useBasename = require('history/lib/useBasename');

var _useBasename2 = _interopRequireDefault(_useBasename);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function useRouterHistory(createHistory) {
  return function (options) {
    var history = (0, _useQueries2.default)((0, _useBasename2.default)(createHistory))(options);
    history.__v2_compatible__ = true;
    return history;
  };
}
module.exports = exports['default'];
},{"history/lib/useBasename":180,"history/lib/useQueries":181}],226:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _useQueries = require('history/lib/useQueries');

var _useQueries2 = _interopRequireDefault(_useQueries);

var _createTransitionManager = require('./createTransitionManager');

var _createTransitionManager2 = _interopRequireDefault(_createTransitionManager);

var _routerWarning = require('./routerWarning');

var _routerWarning2 = _interopRequireDefault(_routerWarning);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _objectWithoutProperties(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }

/**
 * Returns a new createHistory function that may be used to create
 * history objects that know about routing.
 *
 * Enhances history objects with the following methods:
 *
 * - listen((error, nextState) => {})
 * - listenBeforeLeavingRoute(route, (nextLocation) => {})
 * - match(location, (error, redirectLocation, nextState) => {})
 * - isActive(pathname, query, indexOnly=false)
 */
function useRoutes(createHistory) {
  "production" !== 'production' ? (0, _routerWarning2.default)(false, '`useRoutes` is deprecated. Please use `createTransitionManager` instead.') : void 0;

  return function () {
    var _ref = arguments.length <= 0 || arguments[0] === undefined ? {} : arguments[0];

    var routes = _ref.routes;

    var options = _objectWithoutProperties(_ref, ['routes']);

    var history = (0, _useQueries2.default)(createHistory)(options);
    var transitionManager = (0, _createTransitionManager2.default)(history, routes);
    return _extends({}, history, transitionManager);
  };
}

exports.default = useRoutes;
module.exports = exports['default'];
},{"./createTransitionManager":214,"./routerWarning":224,"history/lib/useQueries":181}],227:[function(require,module,exports){
'use strict';

exports.__esModule = true;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

exports.default = withRouter;

var _invariant = require('invariant');

var _invariant2 = _interopRequireDefault(_invariant);

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _hoistNonReactStatics = require('hoist-non-react-statics');

var _hoistNonReactStatics2 = _interopRequireDefault(_hoistNonReactStatics);

var _PropTypes = require('./PropTypes');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function getDisplayName(WrappedComponent) {
  return WrappedComponent.displayName || WrappedComponent.name || 'Component';
}

function withRouter(WrappedComponent, options) {
  var withRef = options && options.withRef;

  var WithRouter = _react2.default.createClass({
    displayName: 'WithRouter',

    contextTypes: { router: _PropTypes.routerShape },
    propTypes: { router: _PropTypes.routerShape },

    getWrappedInstance: function getWrappedInstance() {
      !withRef ? "production" !== 'production' ? (0, _invariant2.default)(false, 'To access the wrapped instance, you need to specify ' + '`{ withRef: true }` as the second argument of the withRouter() call.') : (0, _invariant2.default)(false) : void 0;

      return this.wrappedInstance;
    },
    render: function render() {
      var _this = this;

      var router = this.props.router || this.context.router;
      var props = _extends({}, this.props, { router: router });

      if (withRef) {
        props.ref = function (c) {
          _this.wrappedInstance = c;
        };
      }

      return _react2.default.createElement(WrappedComponent, props);
    }
  });

  WithRouter.displayName = 'withRouter(' + getDisplayName(WrappedComponent) + ')';
  WithRouter.WrappedComponent = WrappedComponent;

  return (0, _hoistNonReactStatics2.default)(WithRouter, WrappedComponent);
}
module.exports = exports['default'];
},{"./PropTypes":199,"hoist-non-react-statics":182,"invariant":183,"react":356}],228:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule AutoFocusUtils
 * @typechecks static-only
 */

'use strict';

var ReactMount = require('./ReactMount');

var findDOMNode = require('./findDOMNode');
var focusNode = require('fbjs/lib/focusNode');

var Mixin = {
  componentDidMount: function () {
    if (this.props.autoFocus) {
      focusNode(findDOMNode(this));
    }
  }
};

var AutoFocusUtils = {
  Mixin: Mixin,

  focusDOMComponent: function () {
    focusNode(ReactMount.getNode(this._rootNodeID));
  }
};

module.exports = AutoFocusUtils;
},{"./ReactMount":292,"./findDOMNode":335,"fbjs/lib/focusNode":145}],229:[function(require,module,exports){
/**
 * Copyright 2013-2015 Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule BeforeInputEventPlugin
 * @typechecks static-only
 */

'use strict';

var EventConstants = require('./EventConstants');
var EventPropagators = require('./EventPropagators');
var ExecutionEnvironment = require('fbjs/lib/ExecutionEnvironment');
var FallbackCompositionState = require('./FallbackCompositionState');
var SyntheticCompositionEvent = require('./SyntheticCompositionEvent');
var SyntheticInputEvent = require('./SyntheticInputEvent');

var keyOf = require('fbjs/lib/keyOf');

var END_KEYCODES = [9, 13, 27, 32]; // Tab, Return, Esc, Space
var START_KEYCODE = 229;

var canUseCompositionEvent = ExecutionEnvironment.canUseDOM && 'CompositionEvent' in window;

var documentMode = null;
if (ExecutionEnvironment.canUseDOM && 'documentMode' in document) {
  documentMode = document.documentMode;
}

// Webkit offers a very useful `textInput` event that can be used to
// directly represent `beforeInput`. The IE `textinput` event is not as
// useful, so we don't use it.
var canUseTextInputEvent = ExecutionEnvironment.canUseDOM && 'TextEvent' in window && !documentMode && !isPresto();

// In IE9+, we have access to composition events, but the data supplied
// by the native compositionend event may be incorrect. Japanese ideographic
// spaces, for instance (\u3000) are not recorded correctly.
var useFallbackCompositionData = ExecutionEnvironment.canUseDOM && (!canUseCompositionEvent || documentMode && documentMode > 8 && documentMode <= 11);

/**
 * Opera <= 12 includes TextEvent in window, but does not fire
 * text input events. Rely on keypress instead.
 */
function isPresto() {
  var opera = window.opera;
  return typeof opera === 'object' && typeof opera.version === 'function' && parseInt(opera.version(), 10) <= 12;
}

var SPACEBAR_CODE = 32;
var SPACEBAR_CHAR = String.fromCharCode(SPACEBAR_CODE);

var topLevelTypes = EventConstants.topLevelTypes;

// Events and their corresponding property names.
var eventTypes = {
  beforeInput: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onBeforeInput: null }),
      captured: keyOf({ onBeforeInputCapture: null })
    },
    dependencies: [topLevelTypes.topCompositionEnd, topLevelTypes.topKeyPress, topLevelTypes.topTextInput, topLevelTypes.topPaste]
  },
  compositionEnd: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onCompositionEnd: null }),
      captured: keyOf({ onCompositionEndCapture: null })
    },
    dependencies: [topLevelTypes.topBlur, topLevelTypes.topCompositionEnd, topLevelTypes.topKeyDown, topLevelTypes.topKeyPress, topLevelTypes.topKeyUp, topLevelTypes.topMouseDown]
  },
  compositionStart: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onCompositionStart: null }),
      captured: keyOf({ onCompositionStartCapture: null })
    },
    dependencies: [topLevelTypes.topBlur, topLevelTypes.topCompositionStart, topLevelTypes.topKeyDown, topLevelTypes.topKeyPress, topLevelTypes.topKeyUp, topLevelTypes.topMouseDown]
  },
  compositionUpdate: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onCompositionUpdate: null }),
      captured: keyOf({ onCompositionUpdateCapture: null })
    },
    dependencies: [topLevelTypes.topBlur, topLevelTypes.topCompositionUpdate, topLevelTypes.topKeyDown, topLevelTypes.topKeyPress, topLevelTypes.topKeyUp, topLevelTypes.topMouseDown]
  }
};

// Track whether we've ever handled a keypress on the space key.
var hasSpaceKeypress = false;

/**
 * Return whether a native keypress event is assumed to be a command.
 * This is required because Firefox fires `keypress` events for key commands
 * (cut, copy, select-all, etc.) even though no character is inserted.
 */
function isKeypressCommand(nativeEvent) {
  return (nativeEvent.ctrlKey || nativeEvent.altKey || nativeEvent.metaKey) &&
  // ctrlKey && altKey is equivalent to AltGr, and is not a command.
  !(nativeEvent.ctrlKey && nativeEvent.altKey);
}

/**
 * Translate native top level events into event types.
 *
 * @param {string} topLevelType
 * @return {object}
 */
function getCompositionEventType(topLevelType) {
  switch (topLevelType) {
    case topLevelTypes.topCompositionStart:
      return eventTypes.compositionStart;
    case topLevelTypes.topCompositionEnd:
      return eventTypes.compositionEnd;
    case topLevelTypes.topCompositionUpdate:
      return eventTypes.compositionUpdate;
  }
}

/**
 * Does our fallback best-guess model think this event signifies that
 * composition has begun?
 *
 * @param {string} topLevelType
 * @param {object} nativeEvent
 * @return {boolean}
 */
function isFallbackCompositionStart(topLevelType, nativeEvent) {
  return topLevelType === topLevelTypes.topKeyDown && nativeEvent.keyCode === START_KEYCODE;
}

/**
 * Does our fallback mode think that this event is the end of composition?
 *
 * @param {string} topLevelType
 * @param {object} nativeEvent
 * @return {boolean}
 */
function isFallbackCompositionEnd(topLevelType, nativeEvent) {
  switch (topLevelType) {
    case topLevelTypes.topKeyUp:
      // Command keys insert or clear IME input.
      return END_KEYCODES.indexOf(nativeEvent.keyCode) !== -1;
    case topLevelTypes.topKeyDown:
      // Expect IME keyCode on each keydown. If we get any other
      // code we must have exited earlier.
      return nativeEvent.keyCode !== START_KEYCODE;
    case topLevelTypes.topKeyPress:
    case topLevelTypes.topMouseDown:
    case topLevelTypes.topBlur:
      // Events are not possible without cancelling IME.
      return true;
    default:
      return false;
  }
}

/**
 * Google Input Tools provides composition data via a CustomEvent,
 * with the `data` property populated in the `detail` object. If this
 * is available on the event object, use it. If not, this is a plain
 * composition event and we have nothing special to extract.
 *
 * @param {object} nativeEvent
 * @return {?string}
 */
function getDataFromCustomEvent(nativeEvent) {
  var detail = nativeEvent.detail;
  if (typeof detail === 'object' && 'data' in detail) {
    return detail.data;
  }
  return null;
}

// Track the current IME composition fallback object, if any.
var currentComposition = null;

/**
 * @param {string} topLevelType Record from `EventConstants`.
 * @param {DOMEventTarget} topLevelTarget The listening component root node.
 * @param {string} topLevelTargetID ID of `topLevelTarget`.
 * @param {object} nativeEvent Native browser event.
 * @return {?object} A SyntheticCompositionEvent.
 */
function extractCompositionEvent(topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget) {
  var eventType;
  var fallbackData;

  if (canUseCompositionEvent) {
    eventType = getCompositionEventType(topLevelType);
  } else if (!currentComposition) {
    if (isFallbackCompositionStart(topLevelType, nativeEvent)) {
      eventType = eventTypes.compositionStart;
    }
  } else if (isFallbackCompositionEnd(topLevelType, nativeEvent)) {
    eventType = eventTypes.compositionEnd;
  }

  if (!eventType) {
    return null;
  }

  if (useFallbackCompositionData) {
    // The current composition is stored statically and must not be
    // overwritten while composition continues.
    if (!currentComposition && eventType === eventTypes.compositionStart) {
      currentComposition = FallbackCompositionState.getPooled(topLevelTarget);
    } else if (eventType === eventTypes.compositionEnd) {
      if (currentComposition) {
        fallbackData = currentComposition.getData();
      }
    }
  }

  var event = SyntheticCompositionEvent.getPooled(eventType, topLevelTargetID, nativeEvent, nativeEventTarget);

  if (fallbackData) {
    // Inject data generated from fallback path into the synthetic event.
    // This matches the property of native CompositionEventInterface.
    event.data = fallbackData;
  } else {
    var customData = getDataFromCustomEvent(nativeEvent);
    if (customData !== null) {
      event.data = customData;
    }
  }

  EventPropagators.accumulateTwoPhaseDispatches(event);
  return event;
}

/**
 * @param {string} topLevelType Record from `EventConstants`.
 * @param {object} nativeEvent Native browser event.
 * @return {?string} The string corresponding to this `beforeInput` event.
 */
function getNativeBeforeInputChars(topLevelType, nativeEvent) {
  switch (topLevelType) {
    case topLevelTypes.topCompositionEnd:
      return getDataFromCustomEvent(nativeEvent);
    case topLevelTypes.topKeyPress:
      /**
       * If native `textInput` events are available, our goal is to make
       * use of them. However, there is a special case: the spacebar key.
       * In Webkit, preventing default on a spacebar `textInput` event
       * cancels character insertion, but it *also* causes the browser
       * to fall back to its default spacebar behavior of scrolling the
       * page.
       *
       * Tracking at:
       * https://code.google.com/p/chromium/issues/detail?id=355103
       *
       * To avoid this issue, use the keypress event as if no `textInput`
       * event is available.
       */
      var which = nativeEvent.which;
      if (which !== SPACEBAR_CODE) {
        return null;
      }

      hasSpaceKeypress = true;
      return SPACEBAR_CHAR;

    case topLevelTypes.topTextInput:
      // Record the characters to be added to the DOM.
      var chars = nativeEvent.data;

      // If it's a spacebar character, assume that we have already handled
      // it at the keypress level and bail immediately. Android Chrome
      // doesn't give us keycodes, so we need to blacklist it.
      if (chars === SPACEBAR_CHAR && hasSpaceKeypress) {
        return null;
      }

      return chars;

    default:
      // For other native event types, do nothing.
      return null;
  }
}

/**
 * For browsers that do not provide the `textInput` event, extract the
 * appropriate string to use for SyntheticInputEvent.
 *
 * @param {string} topLevelType Record from `EventConstants`.
 * @param {object} nativeEvent Native browser event.
 * @return {?string} The fallback string for this `beforeInput` event.
 */
function getFallbackBeforeInputChars(topLevelType, nativeEvent) {
  // If we are currently composing (IME) and using a fallback to do so,
  // try to extract the composed characters from the fallback object.
  if (currentComposition) {
    if (topLevelType === topLevelTypes.topCompositionEnd || isFallbackCompositionEnd(topLevelType, nativeEvent)) {
      var chars = currentComposition.getData();
      FallbackCompositionState.release(currentComposition);
      currentComposition = null;
      return chars;
    }
    return null;
  }

  switch (topLevelType) {
    case topLevelTypes.topPaste:
      // If a paste event occurs after a keypress, throw out the input
      // chars. Paste events should not lead to BeforeInput events.
      return null;
    case topLevelTypes.topKeyPress:
      /**
       * As of v27, Firefox may fire keypress events even when no character
       * will be inserted. A few possibilities:
       *
       * - `which` is `0`. Arrow keys, Esc key, etc.
       *
       * - `which` is the pressed key code, but no char is available.
       *   Ex: 'AltGr + d` in Polish. There is no modified character for
       *   this key combination and no character is inserted into the
       *   document, but FF fires the keypress for char code `100` anyway.
       *   No `input` event will occur.
       *
       * - `which` is the pressed key code, but a command combination is
       *   being used. Ex: `Cmd+C`. No character is inserted, and no
       *   `input` event will occur.
       */
      if (nativeEvent.which && !isKeypressCommand(nativeEvent)) {
        return String.fromCharCode(nativeEvent.which);
      }
      return null;
    case topLevelTypes.topCompositionEnd:
      return useFallbackCompositionData ? null : nativeEvent.data;
    default:
      return null;
  }
}

/**
 * Extract a SyntheticInputEvent for `beforeInput`, based on either native
 * `textInput` or fallback behavior.
 *
 * @param {string} topLevelType Record from `EventConstants`.
 * @param {DOMEventTarget} topLevelTarget The listening component root node.
 * @param {string} topLevelTargetID ID of `topLevelTarget`.
 * @param {object} nativeEvent Native browser event.
 * @return {?object} A SyntheticInputEvent.
 */
function extractBeforeInputEvent(topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget) {
  var chars;

  if (canUseTextInputEvent) {
    chars = getNativeBeforeInputChars(topLevelType, nativeEvent);
  } else {
    chars = getFallbackBeforeInputChars(topLevelType, nativeEvent);
  }

  // If no characters are being inserted, no BeforeInput event should
  // be fired.
  if (!chars) {
    return null;
  }

  var event = SyntheticInputEvent.getPooled(eventTypes.beforeInput, topLevelTargetID, nativeEvent, nativeEventTarget);

  event.data = chars;
  EventPropagators.accumulateTwoPhaseDispatches(event);
  return event;
}

/**
 * Create an `onBeforeInput` event to match
 * http://www.w3.org/TR/2013/WD-DOM-Level-3-Events-20131105/#events-inputevents.
 *
 * This event plugin is based on the native `textInput` event
 * available in Chrome, Safari, Opera, and IE. This event fires after
 * `onKeyPress` and `onCompositionEnd`, but before `onInput`.
 *
 * `beforeInput` is spec'd but not implemented in any browsers, and
 * the `input` event does not provide any useful information about what has
 * actually been added, contrary to the spec. Thus, `textInput` is the best
 * available event to identify the characters that have actually been inserted
 * into the target node.
 *
 * This plugin is also responsible for emitting `composition` events, thus
 * allowing us to share composition fallback code for both `beforeInput` and
 * `composition` event types.
 */
var BeforeInputEventPlugin = {

  eventTypes: eventTypes,

  /**
   * @param {string} topLevelType Record from `EventConstants`.
   * @param {DOMEventTarget} topLevelTarget The listening component root node.
   * @param {string} topLevelTargetID ID of `topLevelTarget`.
   * @param {object} nativeEvent Native browser event.
   * @return {*} An accumulation of synthetic events.
   * @see {EventPluginHub.extractEvents}
   */
  extractEvents: function (topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget) {
    return [extractCompositionEvent(topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget), extractBeforeInputEvent(topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget)];
  }
};

module.exports = BeforeInputEventPlugin;
},{"./EventConstants":241,"./EventPropagators":245,"./FallbackCompositionState":246,"./SyntheticCompositionEvent":317,"./SyntheticInputEvent":321,"fbjs/lib/ExecutionEnvironment":137,"fbjs/lib/keyOf":155}],230:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule CSSProperty
 */

'use strict';

/**
 * CSS properties which accept numbers but are not in units of "px".
 */
var isUnitlessNumber = {
  animationIterationCount: true,
  boxFlex: true,
  boxFlexGroup: true,
  boxOrdinalGroup: true,
  columnCount: true,
  flex: true,
  flexGrow: true,
  flexPositive: true,
  flexShrink: true,
  flexNegative: true,
  flexOrder: true,
  fontWeight: true,
  lineClamp: true,
  lineHeight: true,
  opacity: true,
  order: true,
  orphans: true,
  tabSize: true,
  widows: true,
  zIndex: true,
  zoom: true,

  // SVG-related properties
  fillOpacity: true,
  stopOpacity: true,
  strokeDashoffset: true,
  strokeOpacity: true,
  strokeWidth: true
};

/**
 * @param {string} prefix vendor-specific prefix, eg: Webkit
 * @param {string} key style name, eg: transitionDuration
 * @return {string} style name prefixed with `prefix`, properly camelCased, eg:
 * WebkitTransitionDuration
 */
function prefixKey(prefix, key) {
  return prefix + key.charAt(0).toUpperCase() + key.substring(1);
}

/**
 * Support style names that may come passed in prefixed by adding permutations
 * of vendor prefixes.
 */
var prefixes = ['Webkit', 'ms', 'Moz', 'O'];

// Using Object.keys here, or else the vanilla for-in loop makes IE8 go into an
// infinite loop, because it iterates over the newly added props too.
Object.keys(isUnitlessNumber).forEach(function (prop) {
  prefixes.forEach(function (prefix) {
    isUnitlessNumber[prefixKey(prefix, prop)] = isUnitlessNumber[prop];
  });
});

/**
 * Most style properties can be unset by doing .style[prop] = '' but IE8
 * doesn't like doing that with shorthand properties so for the properties that
 * IE8 breaks on, which are listed here, we instead unset each of the
 * individual properties. See http://bugs.jquery.com/ticket/12385.
 * The 4-value 'clock' properties like margin, padding, border-width seem to
 * behave without any problems. Curiously, list-style works too without any
 * special prodding.
 */
var shorthandPropertyExpansions = {
  background: {
    backgroundAttachment: true,
    backgroundColor: true,
    backgroundImage: true,
    backgroundPositionX: true,
    backgroundPositionY: true,
    backgroundRepeat: true
  },
  backgroundPosition: {
    backgroundPositionX: true,
    backgroundPositionY: true
  },
  border: {
    borderWidth: true,
    borderStyle: true,
    borderColor: true
  },
  borderBottom: {
    borderBottomWidth: true,
    borderBottomStyle: true,
    borderBottomColor: true
  },
  borderLeft: {
    borderLeftWidth: true,
    borderLeftStyle: true,
    borderLeftColor: true
  },
  borderRight: {
    borderRightWidth: true,
    borderRightStyle: true,
    borderRightColor: true
  },
  borderTop: {
    borderTopWidth: true,
    borderTopStyle: true,
    borderTopColor: true
  },
  font: {
    fontStyle: true,
    fontVariant: true,
    fontWeight: true,
    fontSize: true,
    lineHeight: true,
    fontFamily: true
  },
  outline: {
    outlineWidth: true,
    outlineStyle: true,
    outlineColor: true
  }
};

var CSSProperty = {
  isUnitlessNumber: isUnitlessNumber,
  shorthandPropertyExpansions: shorthandPropertyExpansions
};

module.exports = CSSProperty;
},{}],231:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule CSSPropertyOperations
 * @typechecks static-only
 */

'use strict';

var CSSProperty = require('./CSSProperty');
var ExecutionEnvironment = require('fbjs/lib/ExecutionEnvironment');
var ReactPerf = require('./ReactPerf');

var camelizeStyleName = require('fbjs/lib/camelizeStyleName');
var dangerousStyleValue = require('./dangerousStyleValue');
var hyphenateStyleName = require('fbjs/lib/hyphenateStyleName');
var memoizeStringOnly = require('fbjs/lib/memoizeStringOnly');
var warning = require('fbjs/lib/warning');

var processStyleName = memoizeStringOnly(function (styleName) {
  return hyphenateStyleName(styleName);
});

var hasShorthandPropertyBug = false;
var styleFloatAccessor = 'cssFloat';
if (ExecutionEnvironment.canUseDOM) {
  var tempStyle = document.createElement('div').style;
  try {
    // IE8 throws "Invalid argument." if resetting shorthand style properties.
    tempStyle.font = '';
  } catch (e) {
    hasShorthandPropertyBug = true;
  }
  // IE8 only supports accessing cssFloat (standard) as styleFloat
  if (document.documentElement.style.cssFloat === undefined) {
    styleFloatAccessor = 'styleFloat';
  }
}

if ("production" !== 'production') {
  // 'msTransform' is correct, but the other prefixes should be capitalized
  var badVendoredStyleNamePattern = /^(?:webkit|moz|o)[A-Z]/;

  // style values shouldn't contain a semicolon
  var badStyleValueWithSemicolonPattern = /;\s*$/;

  var warnedStyleNames = {};
  var warnedStyleValues = {};

  var warnHyphenatedStyleName = function (name) {
    if (warnedStyleNames.hasOwnProperty(name) && warnedStyleNames[name]) {
      return;
    }

    warnedStyleNames[name] = true;
    "production" !== 'production' ? warning(false, 'Unsupported style property %s. Did you mean %s?', name, camelizeStyleName(name)) : undefined;
  };

  var warnBadVendoredStyleName = function (name) {
    if (warnedStyleNames.hasOwnProperty(name) && warnedStyleNames[name]) {
      return;
    }

    warnedStyleNames[name] = true;
    "production" !== 'production' ? warning(false, 'Unsupported vendor-prefixed style property %s. Did you mean %s?', name, name.charAt(0).toUpperCase() + name.slice(1)) : undefined;
  };

  var warnStyleValueWithSemicolon = function (name, value) {
    if (warnedStyleValues.hasOwnProperty(value) && warnedStyleValues[value]) {
      return;
    }

    warnedStyleValues[value] = true;
    "production" !== 'production' ? warning(false, 'Style property values shouldn\'t contain a semicolon. ' + 'Try "%s: %s" instead.', name, value.replace(badStyleValueWithSemicolonPattern, '')) : undefined;
  };

  /**
   * @param {string} name
   * @param {*} value
   */
  var warnValidStyle = function (name, value) {
    if (name.indexOf('-') > -1) {
      warnHyphenatedStyleName(name);
    } else if (badVendoredStyleNamePattern.test(name)) {
      warnBadVendoredStyleName(name);
    } else if (badStyleValueWithSemicolonPattern.test(value)) {
      warnStyleValueWithSemicolon(name, value);
    }
  };
}

/**
 * Operations for dealing with CSS properties.
 */
var CSSPropertyOperations = {

  /**
   * Serializes a mapping of style properties for use as inline styles:
   *
   *   > createMarkupForStyles({width: '200px', height: 0})
   *   "width:200px;height:0;"
   *
   * Undefined values are ignored so that declarative programming is easier.
   * The result should be HTML-escaped before insertion into the DOM.
   *
   * @param {object} styles
   * @return {?string}
   */
  createMarkupForStyles: function (styles) {
    var serialized = '';
    for (var styleName in styles) {
      if (!styles.hasOwnProperty(styleName)) {
        continue;
      }
      var styleValue = styles[styleName];
      if ("production" !== 'production') {
        warnValidStyle(styleName, styleValue);
      }
      if (styleValue != null) {
        serialized += processStyleName(styleName) + ':';
        serialized += dangerousStyleValue(styleName, styleValue) + ';';
      }
    }
    return serialized || null;
  },

  /**
   * Sets the value for multiple styles on a node.  If a value is specified as
   * '' (empty string), the corresponding style property will be unset.
   *
   * @param {DOMElement} node
   * @param {object} styles
   */
  setValueForStyles: function (node, styles) {
    var style = node.style;
    for (var styleName in styles) {
      if (!styles.hasOwnProperty(styleName)) {
        continue;
      }
      if ("production" !== 'production') {
        warnValidStyle(styleName, styles[styleName]);
      }
      var styleValue = dangerousStyleValue(styleName, styles[styleName]);
      if (styleName === 'float') {
        styleName = styleFloatAccessor;
      }
      if (styleValue) {
        style[styleName] = styleValue;
      } else {
        var expansion = hasShorthandPropertyBug && CSSProperty.shorthandPropertyExpansions[styleName];
        if (expansion) {
          // Shorthand property that IE8 won't like unsetting, so unset each
          // component to placate it
          for (var individualStyleName in expansion) {
            style[individualStyleName] = '';
          }
        } else {
          style[styleName] = '';
        }
      }
    }
  }

};

ReactPerf.measureMethods(CSSPropertyOperations, 'CSSPropertyOperations', {
  setValueForStyles: 'setValueForStyles'
});

module.exports = CSSPropertyOperations;
},{"./CSSProperty":230,"./ReactPerf":298,"./dangerousStyleValue":332,"fbjs/lib/ExecutionEnvironment":137,"fbjs/lib/camelizeStyleName":139,"fbjs/lib/hyphenateStyleName":150,"fbjs/lib/memoizeStringOnly":157,"fbjs/lib/warning":162}],232:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule CallbackQueue
 */

'use strict';

var PooledClass = require('./PooledClass');

var assign = require('./Object.assign');
var invariant = require('fbjs/lib/invariant');

/**
 * A specialized pseudo-event module to help keep track of components waiting to
 * be notified when their DOM representations are available for use.
 *
 * This implements `PooledClass`, so you should never need to instantiate this.
 * Instead, use `CallbackQueue.getPooled()`.
 *
 * @class ReactMountReady
 * @implements PooledClass
 * @internal
 */
function CallbackQueue() {
  this._callbacks = null;
  this._contexts = null;
}

assign(CallbackQueue.prototype, {

  /**
   * Enqueues a callback to be invoked when `notifyAll` is invoked.
   *
   * @param {function} callback Invoked when `notifyAll` is invoked.
   * @param {?object} context Context to call `callback` with.
   * @internal
   */
  enqueue: function (callback, context) {
    this._callbacks = this._callbacks || [];
    this._contexts = this._contexts || [];
    this._callbacks.push(callback);
    this._contexts.push(context);
  },

  /**
   * Invokes all enqueued callbacks and clears the queue. This is invoked after
   * the DOM representation of a component has been created or updated.
   *
   * @internal
   */
  notifyAll: function () {
    var callbacks = this._callbacks;
    var contexts = this._contexts;
    if (callbacks) {
      !(callbacks.length === contexts.length) ? "production" !== 'production' ? invariant(false, 'Mismatched list of contexts in callback queue') : invariant(false) : undefined;
      this._callbacks = null;
      this._contexts = null;
      for (var i = 0; i < callbacks.length; i++) {
        callbacks[i].call(contexts[i]);
      }
      callbacks.length = 0;
      contexts.length = 0;
    }
  },

  /**
   * Resets the internal queue.
   *
   * @internal
   */
  reset: function () {
    this._callbacks = null;
    this._contexts = null;
  },

  /**
   * `PooledClass` looks for this.
   */
  destructor: function () {
    this.reset();
  }

});

PooledClass.addPoolingTo(CallbackQueue);

module.exports = CallbackQueue;
},{"./Object.assign":249,"./PooledClass":250,"fbjs/lib/invariant":151}],233:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ChangeEventPlugin
 */

'use strict';

var EventConstants = require('./EventConstants');
var EventPluginHub = require('./EventPluginHub');
var EventPropagators = require('./EventPropagators');
var ExecutionEnvironment = require('fbjs/lib/ExecutionEnvironment');
var ReactUpdates = require('./ReactUpdates');
var SyntheticEvent = require('./SyntheticEvent');

var getEventTarget = require('./getEventTarget');
var isEventSupported = require('./isEventSupported');
var isTextInputElement = require('./isTextInputElement');
var keyOf = require('fbjs/lib/keyOf');

var topLevelTypes = EventConstants.topLevelTypes;

var eventTypes = {
  change: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onChange: null }),
      captured: keyOf({ onChangeCapture: null })
    },
    dependencies: [topLevelTypes.topBlur, topLevelTypes.topChange, topLevelTypes.topClick, topLevelTypes.topFocus, topLevelTypes.topInput, topLevelTypes.topKeyDown, topLevelTypes.topKeyUp, topLevelTypes.topSelectionChange]
  }
};

/**
 * For IE shims
 */
var activeElement = null;
var activeElementID = null;
var activeElementValue = null;
var activeElementValueProp = null;

/**
 * SECTION: handle `change` event
 */
function shouldUseChangeEvent(elem) {
  var nodeName = elem.nodeName && elem.nodeName.toLowerCase();
  return nodeName === 'select' || nodeName === 'input' && elem.type === 'file';
}

var doesChangeEventBubble = false;
if (ExecutionEnvironment.canUseDOM) {
  // See `handleChange` comment below
  doesChangeEventBubble = isEventSupported('change') && (!('documentMode' in document) || document.documentMode > 8);
}

function manualDispatchChangeEvent(nativeEvent) {
  var event = SyntheticEvent.getPooled(eventTypes.change, activeElementID, nativeEvent, getEventTarget(nativeEvent));
  EventPropagators.accumulateTwoPhaseDispatches(event);

  // If change and propertychange bubbled, we'd just bind to it like all the
  // other events and have it go through ReactBrowserEventEmitter. Since it
  // doesn't, we manually listen for the events and so we have to enqueue and
  // process the abstract event manually.
  //
  // Batching is necessary here in order to ensure that all event handlers run
  // before the next rerender (including event handlers attached to ancestor
  // elements instead of directly on the input). Without this, controlled
  // components don't work properly in conjunction with event bubbling because
  // the component is rerendered and the value reverted before all the event
  // handlers can run. See https://github.com/facebook/react/issues/708.
  ReactUpdates.batchedUpdates(runEventInBatch, event);
}

function runEventInBatch(event) {
  EventPluginHub.enqueueEvents(event);
  EventPluginHub.processEventQueue(false);
}

function startWatchingForChangeEventIE8(target, targetID) {
  activeElement = target;
  activeElementID = targetID;
  activeElement.attachEvent('onchange', manualDispatchChangeEvent);
}

function stopWatchingForChangeEventIE8() {
  if (!activeElement) {
    return;
  }
  activeElement.detachEvent('onchange', manualDispatchChangeEvent);
  activeElement = null;
  activeElementID = null;
}

function getTargetIDForChangeEvent(topLevelType, topLevelTarget, topLevelTargetID) {
  if (topLevelType === topLevelTypes.topChange) {
    return topLevelTargetID;
  }
}
function handleEventsForChangeEventIE8(topLevelType, topLevelTarget, topLevelTargetID) {
  if (topLevelType === topLevelTypes.topFocus) {
    // stopWatching() should be a noop here but we call it just in case we
    // missed a blur event somehow.
    stopWatchingForChangeEventIE8();
    startWatchingForChangeEventIE8(topLevelTarget, topLevelTargetID);
  } else if (topLevelType === topLevelTypes.topBlur) {
    stopWatchingForChangeEventIE8();
  }
}

/**
 * SECTION: handle `input` event
 */
var isInputEventSupported = false;
if (ExecutionEnvironment.canUseDOM) {
  // IE9 claims to support the input event but fails to trigger it when
  // deleting text, so we ignore its input events
  isInputEventSupported = isEventSupported('input') && (!('documentMode' in document) || document.documentMode > 9);
}

/**
 * (For old IE.) Replacement getter/setter for the `value` property that gets
 * set on the active element.
 */
var newValueProp = {
  get: function () {
    return activeElementValueProp.get.call(this);
  },
  set: function (val) {
    // Cast to a string so we can do equality checks.
    activeElementValue = '' + val;
    activeElementValueProp.set.call(this, val);
  }
};

/**
 * (For old IE.) Starts tracking propertychange events on the passed-in element
 * and override the value property so that we can distinguish user events from
 * value changes in JS.
 */
function startWatchingForValueChange(target, targetID) {
  activeElement = target;
  activeElementID = targetID;
  activeElementValue = target.value;
  activeElementValueProp = Object.getOwnPropertyDescriptor(target.constructor.prototype, 'value');

  // Not guarded in a canDefineProperty check: IE8 supports defineProperty only
  // on DOM elements
  Object.defineProperty(activeElement, 'value', newValueProp);
  activeElement.attachEvent('onpropertychange', handlePropertyChange);
}

/**
 * (For old IE.) Removes the event listeners from the currently-tracked element,
 * if any exists.
 */
function stopWatchingForValueChange() {
  if (!activeElement) {
    return;
  }

  // delete restores the original property definition
  delete activeElement.value;
  activeElement.detachEvent('onpropertychange', handlePropertyChange);

  activeElement = null;
  activeElementID = null;
  activeElementValue = null;
  activeElementValueProp = null;
}

/**
 * (For old IE.) Handles a propertychange event, sending a `change` event if
 * the value of the active element has changed.
 */
function handlePropertyChange(nativeEvent) {
  if (nativeEvent.propertyName !== 'value') {
    return;
  }
  var value = nativeEvent.srcElement.value;
  if (value === activeElementValue) {
    return;
  }
  activeElementValue = value;

  manualDispatchChangeEvent(nativeEvent);
}

/**
 * If a `change` event should be fired, returns the target's ID.
 */
function getTargetIDForInputEvent(topLevelType, topLevelTarget, topLevelTargetID) {
  if (topLevelType === topLevelTypes.topInput) {
    // In modern browsers (i.e., not IE8 or IE9), the input event is exactly
    // what we want so fall through here and trigger an abstract event
    return topLevelTargetID;
  }
}

// For IE8 and IE9.
function handleEventsForInputEventIE(topLevelType, topLevelTarget, topLevelTargetID) {
  if (topLevelType === topLevelTypes.topFocus) {
    // In IE8, we can capture almost all .value changes by adding a
    // propertychange handler and looking for events with propertyName
    // equal to 'value'
    // In IE9, propertychange fires for most input events but is buggy and
    // doesn't fire when text is deleted, but conveniently, selectionchange
    // appears to fire in all of the remaining cases so we catch those and
    // forward the event if the value has changed
    // In either case, we don't want to call the event handler if the value
    // is changed from JS so we redefine a setter for `.value` that updates
    // our activeElementValue variable, allowing us to ignore those changes
    //
    // stopWatching() should be a noop here but we call it just in case we
    // missed a blur event somehow.
    stopWatchingForValueChange();
    startWatchingForValueChange(topLevelTarget, topLevelTargetID);
  } else if (topLevelType === topLevelTypes.topBlur) {
    stopWatchingForValueChange();
  }
}

// For IE8 and IE9.
function getTargetIDForInputEventIE(topLevelType, topLevelTarget, topLevelTargetID) {
  if (topLevelType === topLevelTypes.topSelectionChange || topLevelType === topLevelTypes.topKeyUp || topLevelType === topLevelTypes.topKeyDown) {
    // On the selectionchange event, the target is just document which isn't
    // helpful for us so just check activeElement instead.
    //
    // 99% of the time, keydown and keyup aren't necessary. IE8 fails to fire
    // propertychange on the first input event after setting `value` from a
    // script and fires only keydown, keypress, keyup. Catching keyup usually
    // gets it and catching keydown lets us fire an event for the first
    // keystroke if user does a key repeat (it'll be a little delayed: right
    // before the second keystroke). Other input methods (e.g., paste) seem to
    // fire selectionchange normally.
    if (activeElement && activeElement.value !== activeElementValue) {
      activeElementValue = activeElement.value;
      return activeElementID;
    }
  }
}

/**
 * SECTION: handle `click` event
 */
function shouldUseClickEvent(elem) {
  // Use the `click` event to detect changes to checkbox and radio inputs.
  // This approach works across all browsers, whereas `change` does not fire
  // until `blur` in IE8.
  return elem.nodeName && elem.nodeName.toLowerCase() === 'input' && (elem.type === 'checkbox' || elem.type === 'radio');
}

function getTargetIDForClickEvent(topLevelType, topLevelTarget, topLevelTargetID) {
  if (topLevelType === topLevelTypes.topClick) {
    return topLevelTargetID;
  }
}

/**
 * This plugin creates an `onChange` event that normalizes change events
 * across form elements. This event fires at a time when it's possible to
 * change the element's value without seeing a flicker.
 *
 * Supported elements are:
 * - input (see `isTextInputElement`)
 * - textarea
 * - select
 */
var ChangeEventPlugin = {

  eventTypes: eventTypes,

  /**
   * @param {string} topLevelType Record from `EventConstants`.
   * @param {DOMEventTarget} topLevelTarget The listening component root node.
   * @param {string} topLevelTargetID ID of `topLevelTarget`.
   * @param {object} nativeEvent Native browser event.
   * @return {*} An accumulation of synthetic events.
   * @see {EventPluginHub.extractEvents}
   */
  extractEvents: function (topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget) {

    var getTargetIDFunc, handleEventFunc;
    if (shouldUseChangeEvent(topLevelTarget)) {
      if (doesChangeEventBubble) {
        getTargetIDFunc = getTargetIDForChangeEvent;
      } else {
        handleEventFunc = handleEventsForChangeEventIE8;
      }
    } else if (isTextInputElement(topLevelTarget)) {
      if (isInputEventSupported) {
        getTargetIDFunc = getTargetIDForInputEvent;
      } else {
        getTargetIDFunc = getTargetIDForInputEventIE;
        handleEventFunc = handleEventsForInputEventIE;
      }
    } else if (shouldUseClickEvent(topLevelTarget)) {
      getTargetIDFunc = getTargetIDForClickEvent;
    }

    if (getTargetIDFunc) {
      var targetID = getTargetIDFunc(topLevelType, topLevelTarget, topLevelTargetID);
      if (targetID) {
        var event = SyntheticEvent.getPooled(eventTypes.change, targetID, nativeEvent, nativeEventTarget);
        event.type = 'change';
        EventPropagators.accumulateTwoPhaseDispatches(event);
        return event;
      }
    }

    if (handleEventFunc) {
      handleEventFunc(topLevelType, topLevelTarget, topLevelTargetID);
    }
  }

};

module.exports = ChangeEventPlugin;
},{"./EventConstants":241,"./EventPluginHub":242,"./EventPropagators":245,"./ReactUpdates":310,"./SyntheticEvent":319,"./getEventTarget":341,"./isEventSupported":346,"./isTextInputElement":347,"fbjs/lib/ExecutionEnvironment":137,"fbjs/lib/keyOf":155}],234:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ClientReactRootIndex
 * @typechecks
 */

'use strict';

var nextReactRootIndex = 0;

var ClientReactRootIndex = {
  createReactRootIndex: function () {
    return nextReactRootIndex++;
  }
};

module.exports = ClientReactRootIndex;
},{}],235:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule DOMChildrenOperations
 * @typechecks static-only
 */

'use strict';

var Danger = require('./Danger');
var ReactMultiChildUpdateTypes = require('./ReactMultiChildUpdateTypes');
var ReactPerf = require('./ReactPerf');

var setInnerHTML = require('./setInnerHTML');
var setTextContent = require('./setTextContent');
var invariant = require('fbjs/lib/invariant');

/**
 * Inserts `childNode` as a child of `parentNode` at the `index`.
 *
 * @param {DOMElement} parentNode Parent node in which to insert.
 * @param {DOMElement} childNode Child node to insert.
 * @param {number} index Index at which to insert the child.
 * @internal
 */
function insertChildAt(parentNode, childNode, index) {
  // By exploiting arrays returning `undefined` for an undefined index, we can
  // rely exclusively on `insertBefore(node, null)` instead of also using
  // `appendChild(node)`. However, using `undefined` is not allowed by all
  // browsers so we must replace it with `null`.

  // fix render order error in safari
  // IE8 will throw error when index out of list size.
  var beforeChild = index >= parentNode.childNodes.length ? null : parentNode.childNodes.item(index);

  parentNode.insertBefore(childNode, beforeChild);
}

/**
 * Operations for updating with DOM children.
 */
var DOMChildrenOperations = {

  dangerouslyReplaceNodeWithMarkup: Danger.dangerouslyReplaceNodeWithMarkup,

  updateTextContent: setTextContent,

  /**
   * Updates a component's children by processing a series of updates. The
   * update configurations are each expected to have a `parentNode` property.
   *
   * @param {array<object>} updates List of update configurations.
   * @param {array<string>} markupList List of markup strings.
   * @internal
   */
  processUpdates: function (updates, markupList) {
    var update;
    // Mapping from parent IDs to initial child orderings.
    var initialChildren = null;
    // List of children that will be moved or removed.
    var updatedChildren = null;

    for (var i = 0; i < updates.length; i++) {
      update = updates[i];
      if (update.type === ReactMultiChildUpdateTypes.MOVE_EXISTING || update.type === ReactMultiChildUpdateTypes.REMOVE_NODE) {
        var updatedIndex = update.fromIndex;
        var updatedChild = update.parentNode.childNodes[updatedIndex];
        var parentID = update.parentID;

        !updatedChild ? "production" !== 'production' ? invariant(false, 'processUpdates(): Unable to find child %s of element. This ' + 'probably means the DOM was unexpectedly mutated (e.g., by the ' + 'browser), usually due to forgetting a <tbody> when using tables, ' + 'nesting tags like <form>, <p>, or <a>, or using non-SVG elements ' + 'in an <svg> parent. Try inspecting the child nodes of the element ' + 'with React ID `%s`.', updatedIndex, parentID) : invariant(false) : undefined;

        initialChildren = initialChildren || {};
        initialChildren[parentID] = initialChildren[parentID] || [];
        initialChildren[parentID][updatedIndex] = updatedChild;

        updatedChildren = updatedChildren || [];
        updatedChildren.push(updatedChild);
      }
    }

    var renderedMarkup;
    // markupList is either a list of markup or just a list of elements
    if (markupList.length && typeof markupList[0] === 'string') {
      renderedMarkup = Danger.dangerouslyRenderMarkup(markupList);
    } else {
      renderedMarkup = markupList;
    }

    // Remove updated children first so that `toIndex` is consistent.
    if (updatedChildren) {
      for (var j = 0; j < updatedChildren.length; j++) {
        updatedChildren[j].parentNode.removeChild(updatedChildren[j]);
      }
    }

    for (var k = 0; k < updates.length; k++) {
      update = updates[k];
      switch (update.type) {
        case ReactMultiChildUpdateTypes.INSERT_MARKUP:
          insertChildAt(update.parentNode, renderedMarkup[update.markupIndex], update.toIndex);
          break;
        case ReactMultiChildUpdateTypes.MOVE_EXISTING:
          insertChildAt(update.parentNode, initialChildren[update.parentID][update.fromIndex], update.toIndex);
          break;
        case ReactMultiChildUpdateTypes.SET_MARKUP:
          setInnerHTML(update.parentNode, update.content);
          break;
        case ReactMultiChildUpdateTypes.TEXT_CONTENT:
          setTextContent(update.parentNode, update.content);
          break;
        case ReactMultiChildUpdateTypes.REMOVE_NODE:
          // Already removed by the for-loop above.
          break;
      }
    }
  }

};

ReactPerf.measureMethods(DOMChildrenOperations, 'DOMChildrenOperations', {
  updateTextContent: 'updateTextContent'
});

module.exports = DOMChildrenOperations;
},{"./Danger":238,"./ReactMultiChildUpdateTypes":294,"./ReactPerf":298,"./setInnerHTML":351,"./setTextContent":352,"fbjs/lib/invariant":151}],236:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule DOMProperty
 * @typechecks static-only
 */

'use strict';

var invariant = require('fbjs/lib/invariant');

function checkMask(value, bitmask) {
  return (value & bitmask) === bitmask;
}

var DOMPropertyInjection = {
  /**
   * Mapping from normalized, camelcased property names to a configuration that
   * specifies how the associated DOM property should be accessed or rendered.
   */
  MUST_USE_ATTRIBUTE: 0x1,
  MUST_USE_PROPERTY: 0x2,
  HAS_SIDE_EFFECTS: 0x4,
  HAS_BOOLEAN_VALUE: 0x8,
  HAS_NUMERIC_VALUE: 0x10,
  HAS_POSITIVE_NUMERIC_VALUE: 0x20 | 0x10,
  HAS_OVERLOADED_BOOLEAN_VALUE: 0x40,

  /**
   * Inject some specialized knowledge about the DOM. This takes a config object
   * with the following properties:
   *
   * isCustomAttribute: function that given an attribute name will return true
   * if it can be inserted into the DOM verbatim. Useful for data-* or aria-*
   * attributes where it's impossible to enumerate all of the possible
   * attribute names,
   *
   * Properties: object mapping DOM property name to one of the
   * DOMPropertyInjection constants or null. If your attribute isn't in here,
   * it won't get written to the DOM.
   *
   * DOMAttributeNames: object mapping React attribute name to the DOM
   * attribute name. Attribute names not specified use the **lowercase**
   * normalized name.
   *
   * DOMAttributeNamespaces: object mapping React attribute name to the DOM
   * attribute namespace URL. (Attribute names not specified use no namespace.)
   *
   * DOMPropertyNames: similar to DOMAttributeNames but for DOM properties.
   * Property names not specified use the normalized name.
   *
   * DOMMutationMethods: Properties that require special mutation methods. If
   * `value` is undefined, the mutation method should unset the property.
   *
   * @param {object} domPropertyConfig the config as described above.
   */
  injectDOMPropertyConfig: function (domPropertyConfig) {
    var Injection = DOMPropertyInjection;
    var Properties = domPropertyConfig.Properties || {};
    var DOMAttributeNamespaces = domPropertyConfig.DOMAttributeNamespaces || {};
    var DOMAttributeNames = domPropertyConfig.DOMAttributeNames || {};
    var DOMPropertyNames = domPropertyConfig.DOMPropertyNames || {};
    var DOMMutationMethods = domPropertyConfig.DOMMutationMethods || {};

    if (domPropertyConfig.isCustomAttribute) {
      DOMProperty._isCustomAttributeFunctions.push(domPropertyConfig.isCustomAttribute);
    }

    for (var propName in Properties) {
      !!DOMProperty.properties.hasOwnProperty(propName) ? "production" !== 'production' ? invariant(false, 'injectDOMPropertyConfig(...): You\'re trying to inject DOM property ' + '\'%s\' which has already been injected. You may be accidentally ' + 'injecting the same DOM property config twice, or you may be ' + 'injecting two configs that have conflicting property names.', propName) : invariant(false) : undefined;

      var lowerCased = propName.toLowerCase();
      var propConfig = Properties[propName];

      var propertyInfo = {
        attributeName: lowerCased,
        attributeNamespace: null,
        propertyName: propName,
        mutationMethod: null,

        mustUseAttribute: checkMask(propConfig, Injection.MUST_USE_ATTRIBUTE),
        mustUseProperty: checkMask(propConfig, Injection.MUST_USE_PROPERTY),
        hasSideEffects: checkMask(propConfig, Injection.HAS_SIDE_EFFECTS),
        hasBooleanValue: checkMask(propConfig, Injection.HAS_BOOLEAN_VALUE),
        hasNumericValue: checkMask(propConfig, Injection.HAS_NUMERIC_VALUE),
        hasPositiveNumericValue: checkMask(propConfig, Injection.HAS_POSITIVE_NUMERIC_VALUE),
        hasOverloadedBooleanValue: checkMask(propConfig, Injection.HAS_OVERLOADED_BOOLEAN_VALUE)
      };

      !(!propertyInfo.mustUseAttribute || !propertyInfo.mustUseProperty) ? "production" !== 'production' ? invariant(false, 'DOMProperty: Cannot require using both attribute and property: %s', propName) : invariant(false) : undefined;
      !(propertyInfo.mustUseProperty || !propertyInfo.hasSideEffects) ? "production" !== 'production' ? invariant(false, 'DOMProperty: Properties that have side effects must use property: %s', propName) : invariant(false) : undefined;
      !(propertyInfo.hasBooleanValue + propertyInfo.hasNumericValue + propertyInfo.hasOverloadedBooleanValue <= 1) ? "production" !== 'production' ? invariant(false, 'DOMProperty: Value can be one of boolean, overloaded boolean, or ' + 'numeric value, but not a combination: %s', propName) : invariant(false) : undefined;

      if ("production" !== 'production') {
        DOMProperty.getPossibleStandardName[lowerCased] = propName;
      }

      if (DOMAttributeNames.hasOwnProperty(propName)) {
        var attributeName = DOMAttributeNames[propName];
        propertyInfo.attributeName = attributeName;
        if ("production" !== 'production') {
          DOMProperty.getPossibleStandardName[attributeName] = propName;
        }
      }

      if (DOMAttributeNamespaces.hasOwnProperty(propName)) {
        propertyInfo.attributeNamespace = DOMAttributeNamespaces[propName];
      }

      if (DOMPropertyNames.hasOwnProperty(propName)) {
        propertyInfo.propertyName = DOMPropertyNames[propName];
      }

      if (DOMMutationMethods.hasOwnProperty(propName)) {
        propertyInfo.mutationMethod = DOMMutationMethods[propName];
      }

      DOMProperty.properties[propName] = propertyInfo;
    }
  }
};
var defaultValueCache = {};

/**
 * DOMProperty exports lookup objects that can be used like functions:
 *
 *   > DOMProperty.isValid['id']
 *   true
 *   > DOMProperty.isValid['foobar']
 *   undefined
 *
 * Although this may be confusing, it performs better in general.
 *
 * @see http://jsperf.com/key-exists
 * @see http://jsperf.com/key-missing
 */
var DOMProperty = {

  ID_ATTRIBUTE_NAME: 'data-reactid',

  /**
   * Map from property "standard name" to an object with info about how to set
   * the property in the DOM. Each object contains:
   *
   * attributeName:
   *   Used when rendering markup or with `*Attribute()`.
   * attributeNamespace
   * propertyName:
   *   Used on DOM node instances. (This includes properties that mutate due to
   *   external factors.)
   * mutationMethod:
   *   If non-null, used instead of the property or `setAttribute()` after
   *   initial render.
   * mustUseAttribute:
   *   Whether the property must be accessed and mutated using `*Attribute()`.
   *   (This includes anything that fails `<propName> in <element>`.)
   * mustUseProperty:
   *   Whether the property must be accessed and mutated as an object property.
   * hasSideEffects:
   *   Whether or not setting a value causes side effects such as triggering
   *   resources to be loaded or text selection changes. If true, we read from
   *   the DOM before updating to ensure that the value is only set if it has
   *   changed.
   * hasBooleanValue:
   *   Whether the property should be removed when set to a falsey value.
   * hasNumericValue:
   *   Whether the property must be numeric or parse as a numeric and should be
   *   removed when set to a falsey value.
   * hasPositiveNumericValue:
   *   Whether the property must be positive numeric or parse as a positive
   *   numeric and should be removed when set to a falsey value.
   * hasOverloadedBooleanValue:
   *   Whether the property can be used as a flag as well as with a value.
   *   Removed when strictly equal to false; present without a value when
   *   strictly equal to true; present with a value otherwise.
   */
  properties: {},

  /**
   * Mapping from lowercase property names to the properly cased version, used
   * to warn in the case of missing properties. Available only in __DEV__.
   * @type {Object}
   */
  getPossibleStandardName: "production" !== 'production' ? {} : null,

  /**
   * All of the isCustomAttribute() functions that have been injected.
   */
  _isCustomAttributeFunctions: [],

  /**
   * Checks whether a property name is a custom attribute.
   * @method
   */
  isCustomAttribute: function (attributeName) {
    for (var i = 0; i < DOMProperty._isCustomAttributeFunctions.length; i++) {
      var isCustomAttributeFn = DOMProperty._isCustomAttributeFunctions[i];
      if (isCustomAttributeFn(attributeName)) {
        return true;
      }
    }
    return false;
  },

  /**
   * Returns the default property value for a DOM property (i.e., not an
   * attribute). Most default values are '' or false, but not all. Worse yet,
   * some (in particular, `type`) vary depending on the type of element.
   *
   * TODO: Is it better to grab all the possible properties when creating an
   * element to avoid having to create the same element twice?
   */
  getDefaultValueForProperty: function (nodeName, prop) {
    var nodeDefaults = defaultValueCache[nodeName];
    var testElement;
    if (!nodeDefaults) {
      defaultValueCache[nodeName] = nodeDefaults = {};
    }
    if (!(prop in nodeDefaults)) {
      testElement = document.createElement(nodeName);
      nodeDefaults[prop] = testElement[prop];
    }
    return nodeDefaults[prop];
  },

  injection: DOMPropertyInjection
};

module.exports = DOMProperty;
},{"fbjs/lib/invariant":151}],237:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule DOMPropertyOperations
 * @typechecks static-only
 */

'use strict';

var DOMProperty = require('./DOMProperty');
var ReactPerf = require('./ReactPerf');

var quoteAttributeValueForBrowser = require('./quoteAttributeValueForBrowser');
var warning = require('fbjs/lib/warning');

// Simplified subset
var VALID_ATTRIBUTE_NAME_REGEX = /^[a-zA-Z_][\w\.\-]*$/;
var illegalAttributeNameCache = {};
var validatedAttributeNameCache = {};

function isAttributeNameSafe(attributeName) {
  if (validatedAttributeNameCache.hasOwnProperty(attributeName)) {
    return true;
  }
  if (illegalAttributeNameCache.hasOwnProperty(attributeName)) {
    return false;
  }
  if (VALID_ATTRIBUTE_NAME_REGEX.test(attributeName)) {
    validatedAttributeNameCache[attributeName] = true;
    return true;
  }
  illegalAttributeNameCache[attributeName] = true;
  "production" !== 'production' ? warning(false, 'Invalid attribute name: `%s`', attributeName) : undefined;
  return false;
}

function shouldIgnoreValue(propertyInfo, value) {
  return value == null || propertyInfo.hasBooleanValue && !value || propertyInfo.hasNumericValue && isNaN(value) || propertyInfo.hasPositiveNumericValue && value < 1 || propertyInfo.hasOverloadedBooleanValue && value === false;
}

if ("production" !== 'production') {
  var reactProps = {
    children: true,
    dangerouslySetInnerHTML: true,
    key: true,
    ref: true
  };
  var warnedProperties = {};

  var warnUnknownProperty = function (name) {
    if (reactProps.hasOwnProperty(name) && reactProps[name] || warnedProperties.hasOwnProperty(name) && warnedProperties[name]) {
      return;
    }

    warnedProperties[name] = true;
    var lowerCasedName = name.toLowerCase();

    // data-* attributes should be lowercase; suggest the lowercase version
    var standardName = DOMProperty.isCustomAttribute(lowerCasedName) ? lowerCasedName : DOMProperty.getPossibleStandardName.hasOwnProperty(lowerCasedName) ? DOMProperty.getPossibleStandardName[lowerCasedName] : null;

    // For now, only warn when we have a suggested correction. This prevents
    // logging too much when using transferPropsTo.
    "production" !== 'production' ? warning(standardName == null, 'Unknown DOM property %s. Did you mean %s?', name, standardName) : undefined;
  };
}

/**
 * Operations for dealing with DOM properties.
 */
var DOMPropertyOperations = {

  /**
   * Creates markup for the ID property.
   *
   * @param {string} id Unescaped ID.
   * @return {string} Markup string.
   */
  createMarkupForID: function (id) {
    return DOMProperty.ID_ATTRIBUTE_NAME + '=' + quoteAttributeValueForBrowser(id);
  },

  setAttributeForID: function (node, id) {
    node.setAttribute(DOMProperty.ID_ATTRIBUTE_NAME, id);
  },

  /**
   * Creates markup for a property.
   *
   * @param {string} name
   * @param {*} value
   * @return {?string} Markup string, or null if the property was invalid.
   */
  createMarkupForProperty: function (name, value) {
    var propertyInfo = DOMProperty.properties.hasOwnProperty(name) ? DOMProperty.properties[name] : null;
    if (propertyInfo) {
      if (shouldIgnoreValue(propertyInfo, value)) {
        return '';
      }
      var attributeName = propertyInfo.attributeName;
      if (propertyInfo.hasBooleanValue || propertyInfo.hasOverloadedBooleanValue && value === true) {
        return attributeName + '=""';
      }
      return attributeName + '=' + quoteAttributeValueForBrowser(value);
    } else if (DOMProperty.isCustomAttribute(name)) {
      if (value == null) {
        return '';
      }
      return name + '=' + quoteAttributeValueForBrowser(value);
    } else if ("production" !== 'production') {
      warnUnknownProperty(name);
    }
    return null;
  },

  /**
   * Creates markup for a custom property.
   *
   * @param {string} name
   * @param {*} value
   * @return {string} Markup string, or empty string if the property was invalid.
   */
  createMarkupForCustomAttribute: function (name, value) {
    if (!isAttributeNameSafe(name) || value == null) {
      return '';
    }
    return name + '=' + quoteAttributeValueForBrowser(value);
  },

  /**
   * Sets the value for a property on a node.
   *
   * @param {DOMElement} node
   * @param {string} name
   * @param {*} value
   */
  setValueForProperty: function (node, name, value) {
    var propertyInfo = DOMProperty.properties.hasOwnProperty(name) ? DOMProperty.properties[name] : null;
    if (propertyInfo) {
      var mutationMethod = propertyInfo.mutationMethod;
      if (mutationMethod) {
        mutationMethod(node, value);
      } else if (shouldIgnoreValue(propertyInfo, value)) {
        this.deleteValueForProperty(node, name);
      } else if (propertyInfo.mustUseAttribute) {
        var attributeName = propertyInfo.attributeName;
        var namespace = propertyInfo.attributeNamespace;
        // `setAttribute` with objects becomes only `[object]` in IE8/9,
        // ('' + value) makes it output the correct toString()-value.
        if (namespace) {
          node.setAttributeNS(namespace, attributeName, '' + value);
        } else if (propertyInfo.hasBooleanValue || propertyInfo.hasOverloadedBooleanValue && value === true) {
          node.setAttribute(attributeName, '');
        } else {
          node.setAttribute(attributeName, '' + value);
        }
      } else {
        var propName = propertyInfo.propertyName;
        // Must explicitly cast values for HAS_SIDE_EFFECTS-properties to the
        // property type before comparing; only `value` does and is string.
        if (!propertyInfo.hasSideEffects || '' + node[propName] !== '' + value) {
          // Contrary to `setAttribute`, object properties are properly
          // `toString`ed by IE8/9.
          node[propName] = value;
        }
      }
    } else if (DOMProperty.isCustomAttribute(name)) {
      DOMPropertyOperations.setValueForAttribute(node, name, value);
    } else if ("production" !== 'production') {
      warnUnknownProperty(name);
    }
  },

  setValueForAttribute: function (node, name, value) {
    if (!isAttributeNameSafe(name)) {
      return;
    }
    if (value == null) {
      node.removeAttribute(name);
    } else {
      node.setAttribute(name, '' + value);
    }
  },

  /**
   * Deletes the value for a property on a node.
   *
   * @param {DOMElement} node
   * @param {string} name
   */
  deleteValueForProperty: function (node, name) {
    var propertyInfo = DOMProperty.properties.hasOwnProperty(name) ? DOMProperty.properties[name] : null;
    if (propertyInfo) {
      var mutationMethod = propertyInfo.mutationMethod;
      if (mutationMethod) {
        mutationMethod(node, undefined);
      } else if (propertyInfo.mustUseAttribute) {
        node.removeAttribute(propertyInfo.attributeName);
      } else {
        var propName = propertyInfo.propertyName;
        var defaultValue = DOMProperty.getDefaultValueForProperty(node.nodeName, propName);
        if (!propertyInfo.hasSideEffects || '' + node[propName] !== defaultValue) {
          node[propName] = defaultValue;
        }
      }
    } else if (DOMProperty.isCustomAttribute(name)) {
      node.removeAttribute(name);
    } else if ("production" !== 'production') {
      warnUnknownProperty(name);
    }
  }

};

ReactPerf.measureMethods(DOMPropertyOperations, 'DOMPropertyOperations', {
  setValueForProperty: 'setValueForProperty',
  setValueForAttribute: 'setValueForAttribute',
  deleteValueForProperty: 'deleteValueForProperty'
});

module.exports = DOMPropertyOperations;
},{"./DOMProperty":236,"./ReactPerf":298,"./quoteAttributeValueForBrowser":349,"fbjs/lib/warning":162}],238:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule Danger
 * @typechecks static-only
 */

'use strict';

var ExecutionEnvironment = require('fbjs/lib/ExecutionEnvironment');

var createNodesFromMarkup = require('fbjs/lib/createNodesFromMarkup');
var emptyFunction = require('fbjs/lib/emptyFunction');
var getMarkupWrap = require('fbjs/lib/getMarkupWrap');
var invariant = require('fbjs/lib/invariant');

var OPEN_TAG_NAME_EXP = /^(<[^ \/>]+)/;
var RESULT_INDEX_ATTR = 'data-danger-index';

/**
 * Extracts the `nodeName` from a string of markup.
 *
 * NOTE: Extracting the `nodeName` does not require a regular expression match
 * because we make assumptions about React-generated markup (i.e. there are no
 * spaces surrounding the opening tag and there is at least one attribute).
 *
 * @param {string} markup String of markup.
 * @return {string} Node name of the supplied markup.
 * @see http://jsperf.com/extract-nodename
 */
function getNodeName(markup) {
  return markup.substring(1, markup.indexOf(' '));
}

var Danger = {

  /**
   * Renders markup into an array of nodes. The markup is expected to render
   * into a list of root nodes. Also, the length of `resultList` and
   * `markupList` should be the same.
   *
   * @param {array<string>} markupList List of markup strings to render.
   * @return {array<DOMElement>} List of rendered nodes.
   * @internal
   */
  dangerouslyRenderMarkup: function (markupList) {
    !ExecutionEnvironment.canUseDOM ? "production" !== 'production' ? invariant(false, 'dangerouslyRenderMarkup(...): Cannot render markup in a worker ' + 'thread. Make sure `window` and `document` are available globally ' + 'before requiring React when unit testing or use ' + 'ReactDOMServer.renderToString for server rendering.') : invariant(false) : undefined;
    var nodeName;
    var markupByNodeName = {};
    // Group markup by `nodeName` if a wrap is necessary, else by '*'.
    for (var i = 0; i < markupList.length; i++) {
      !markupList[i] ? "production" !== 'production' ? invariant(false, 'dangerouslyRenderMarkup(...): Missing markup.') : invariant(false) : undefined;
      nodeName = getNodeName(markupList[i]);
      nodeName = getMarkupWrap(nodeName) ? nodeName : '*';
      markupByNodeName[nodeName] = markupByNodeName[nodeName] || [];
      markupByNodeName[nodeName][i] = markupList[i];
    }
    var resultList = [];
    var resultListAssignmentCount = 0;
    for (nodeName in markupByNodeName) {
      if (!markupByNodeName.hasOwnProperty(nodeName)) {
        continue;
      }
      var markupListByNodeName = markupByNodeName[nodeName];

      // This for-in loop skips the holes of the sparse array. The order of
      // iteration should follow the order of assignment, which happens to match
      // numerical index order, but we don't rely on that.
      var resultIndex;
      for (resultIndex in markupListByNodeName) {
        if (markupListByNodeName.hasOwnProperty(resultIndex)) {
          var markup = markupListByNodeName[resultIndex];

          // Push the requested markup with an additional RESULT_INDEX_ATTR
          // attribute.  If the markup does not start with a < character, it
          // will be discarded below (with an appropriate console.error).
          markupListByNodeName[resultIndex] = markup.replace(OPEN_TAG_NAME_EXP,
          // This index will be parsed back out below.
          '$1 ' + RESULT_INDEX_ATTR + '="' + resultIndex + '" ');
        }
      }

      // Render each group of markup with similar wrapping `nodeName`.
      var renderNodes = createNodesFromMarkup(markupListByNodeName.join(''), emptyFunction // Do nothing special with <script> tags.
      );

      for (var j = 0; j < renderNodes.length; ++j) {
        var renderNode = renderNodes[j];
        if (renderNode.hasAttribute && renderNode.hasAttribute(RESULT_INDEX_ATTR)) {

          resultIndex = +renderNode.getAttribute(RESULT_INDEX_ATTR);
          renderNode.removeAttribute(RESULT_INDEX_ATTR);

          !!resultList.hasOwnProperty(resultIndex) ? "production" !== 'production' ? invariant(false, 'Danger: Assigning to an already-occupied result index.') : invariant(false) : undefined;

          resultList[resultIndex] = renderNode;

          // This should match resultList.length and markupList.length when
          // we're done.
          resultListAssignmentCount += 1;
        } else if ("production" !== 'production') {
          console.error('Danger: Discarding unexpected node:', renderNode);
        }
      }
    }

    // Although resultList was populated out of order, it should now be a dense
    // array.
    !(resultListAssignmentCount === resultList.length) ? "production" !== 'production' ? invariant(false, 'Danger: Did not assign to every index of resultList.') : invariant(false) : undefined;

    !(resultList.length === markupList.length) ? "production" !== 'production' ? invariant(false, 'Danger: Expected markup to render %s nodes, but rendered %s.', markupList.length, resultList.length) : invariant(false) : undefined;

    return resultList;
  },

  /**
   * Replaces a node with a string of markup at its current position within its
   * parent. The markup must render into a single root node.
   *
   * @param {DOMElement} oldChild Child node to replace.
   * @param {string} markup Markup to render in place of the child node.
   * @internal
   */
  dangerouslyReplaceNodeWithMarkup: function (oldChild, markup) {
    !ExecutionEnvironment.canUseDOM ? "production" !== 'production' ? invariant(false, 'dangerouslyReplaceNodeWithMarkup(...): Cannot render markup in a ' + 'worker thread. Make sure `window` and `document` are available ' + 'globally before requiring React when unit testing or use ' + 'ReactDOMServer.renderToString() for server rendering.') : invariant(false) : undefined;
    !markup ? "production" !== 'production' ? invariant(false, 'dangerouslyReplaceNodeWithMarkup(...): Missing markup.') : invariant(false) : undefined;
    !(oldChild.tagName.toLowerCase() !== 'html') ? "production" !== 'production' ? invariant(false, 'dangerouslyReplaceNodeWithMarkup(...): Cannot replace markup of the ' + '<html> node. This is because browser quirks make this unreliable ' + 'and/or slow. If you want to render to the root you must use ' + 'server rendering. See ReactDOMServer.renderToString().') : invariant(false) : undefined;

    var newChild;
    if (typeof markup === 'string') {
      newChild = createNodesFromMarkup(markup, emptyFunction)[0];
    } else {
      newChild = markup;
    }
    oldChild.parentNode.replaceChild(newChild, oldChild);
  }

};

module.exports = Danger;
},{"fbjs/lib/ExecutionEnvironment":137,"fbjs/lib/createNodesFromMarkup":142,"fbjs/lib/emptyFunction":143,"fbjs/lib/getMarkupWrap":147,"fbjs/lib/invariant":151}],239:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule DefaultEventPluginOrder
 */

'use strict';

var keyOf = require('fbjs/lib/keyOf');

/**
 * Module that is injectable into `EventPluginHub`, that specifies a
 * deterministic ordering of `EventPlugin`s. A convenient way to reason about
 * plugins, without having to package every one of them. This is better than
 * having plugins be ordered in the same order that they are injected because
 * that ordering would be influenced by the packaging order.
 * `ResponderEventPlugin` must occur before `SimpleEventPlugin` so that
 * preventing default on events is convenient in `SimpleEventPlugin` handlers.
 */
var DefaultEventPluginOrder = [keyOf({ ResponderEventPlugin: null }), keyOf({ SimpleEventPlugin: null }), keyOf({ TapEventPlugin: null }), keyOf({ EnterLeaveEventPlugin: null }), keyOf({ ChangeEventPlugin: null }), keyOf({ SelectEventPlugin: null }), keyOf({ BeforeInputEventPlugin: null })];

module.exports = DefaultEventPluginOrder;
},{"fbjs/lib/keyOf":155}],240:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule EnterLeaveEventPlugin
 * @typechecks static-only
 */

'use strict';

var EventConstants = require('./EventConstants');
var EventPropagators = require('./EventPropagators');
var SyntheticMouseEvent = require('./SyntheticMouseEvent');

var ReactMount = require('./ReactMount');
var keyOf = require('fbjs/lib/keyOf');

var topLevelTypes = EventConstants.topLevelTypes;
var getFirstReactDOM = ReactMount.getFirstReactDOM;

var eventTypes = {
  mouseEnter: {
    registrationName: keyOf({ onMouseEnter: null }),
    dependencies: [topLevelTypes.topMouseOut, topLevelTypes.topMouseOver]
  },
  mouseLeave: {
    registrationName: keyOf({ onMouseLeave: null }),
    dependencies: [topLevelTypes.topMouseOut, topLevelTypes.topMouseOver]
  }
};

var extractedEvents = [null, null];

var EnterLeaveEventPlugin = {

  eventTypes: eventTypes,

  /**
   * For almost every interaction we care about, there will be both a top-level
   * `mouseover` and `mouseout` event that occurs. Only use `mouseout` so that
   * we do not extract duplicate events. However, moving the mouse into the
   * browser from outside will not fire a `mouseout` event. In this case, we use
   * the `mouseover` top-level event.
   *
   * @param {string} topLevelType Record from `EventConstants`.
   * @param {DOMEventTarget} topLevelTarget The listening component root node.
   * @param {string} topLevelTargetID ID of `topLevelTarget`.
   * @param {object} nativeEvent Native browser event.
   * @return {*} An accumulation of synthetic events.
   * @see {EventPluginHub.extractEvents}
   */
  extractEvents: function (topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget) {
    if (topLevelType === topLevelTypes.topMouseOver && (nativeEvent.relatedTarget || nativeEvent.fromElement)) {
      return null;
    }
    if (topLevelType !== topLevelTypes.topMouseOut && topLevelType !== topLevelTypes.topMouseOver) {
      // Must not be a mouse in or mouse out - ignoring.
      return null;
    }

    var win;
    if (topLevelTarget.window === topLevelTarget) {
      // `topLevelTarget` is probably a window object.
      win = topLevelTarget;
    } else {
      // TODO: Figure out why `ownerDocument` is sometimes undefined in IE8.
      var doc = topLevelTarget.ownerDocument;
      if (doc) {
        win = doc.defaultView || doc.parentWindow;
      } else {
        win = window;
      }
    }

    var from;
    var to;
    var fromID = '';
    var toID = '';
    if (topLevelType === topLevelTypes.topMouseOut) {
      from = topLevelTarget;
      fromID = topLevelTargetID;
      to = getFirstReactDOM(nativeEvent.relatedTarget || nativeEvent.toElement);
      if (to) {
        toID = ReactMount.getID(to);
      } else {
        to = win;
      }
      to = to || win;
    } else {
      from = win;
      to = topLevelTarget;
      toID = topLevelTargetID;
    }

    if (from === to) {
      // Nothing pertains to our managed components.
      return null;
    }

    var leave = SyntheticMouseEvent.getPooled(eventTypes.mouseLeave, fromID, nativeEvent, nativeEventTarget);
    leave.type = 'mouseleave';
    leave.target = from;
    leave.relatedTarget = to;

    var enter = SyntheticMouseEvent.getPooled(eventTypes.mouseEnter, toID, nativeEvent, nativeEventTarget);
    enter.type = 'mouseenter';
    enter.target = to;
    enter.relatedTarget = from;

    EventPropagators.accumulateEnterLeaveDispatches(leave, enter, fromID, toID);

    extractedEvents[0] = leave;
    extractedEvents[1] = enter;

    return extractedEvents;
  }

};

module.exports = EnterLeaveEventPlugin;
},{"./EventConstants":241,"./EventPropagators":245,"./ReactMount":292,"./SyntheticMouseEvent":323,"fbjs/lib/keyOf":155}],241:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule EventConstants
 */

'use strict';

var keyMirror = require('fbjs/lib/keyMirror');

var PropagationPhases = keyMirror({ bubbled: null, captured: null });

/**
 * Types of raw signals from the browser caught at the top level.
 */
var topLevelTypes = keyMirror({
  topAbort: null,
  topBlur: null,
  topCanPlay: null,
  topCanPlayThrough: null,
  topChange: null,
  topClick: null,
  topCompositionEnd: null,
  topCompositionStart: null,
  topCompositionUpdate: null,
  topContextMenu: null,
  topCopy: null,
  topCut: null,
  topDoubleClick: null,
  topDrag: null,
  topDragEnd: null,
  topDragEnter: null,
  topDragExit: null,
  topDragLeave: null,
  topDragOver: null,
  topDragStart: null,
  topDrop: null,
  topDurationChange: null,
  topEmptied: null,
  topEncrypted: null,
  topEnded: null,
  topError: null,
  topFocus: null,
  topInput: null,
  topKeyDown: null,
  topKeyPress: null,
  topKeyUp: null,
  topLoad: null,
  topLoadedData: null,
  topLoadedMetadata: null,
  topLoadStart: null,
  topMouseDown: null,
  topMouseMove: null,
  topMouseOut: null,
  topMouseOver: null,
  topMouseUp: null,
  topPaste: null,
  topPause: null,
  topPlay: null,
  topPlaying: null,
  topProgress: null,
  topRateChange: null,
  topReset: null,
  topScroll: null,
  topSeeked: null,
  topSeeking: null,
  topSelectionChange: null,
  topStalled: null,
  topSubmit: null,
  topSuspend: null,
  topTextInput: null,
  topTimeUpdate: null,
  topTouchCancel: null,
  topTouchEnd: null,
  topTouchMove: null,
  topTouchStart: null,
  topVolumeChange: null,
  topWaiting: null,
  topWheel: null
});

var EventConstants = {
  topLevelTypes: topLevelTypes,
  PropagationPhases: PropagationPhases
};

module.exports = EventConstants;
},{"fbjs/lib/keyMirror":154}],242:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule EventPluginHub
 */

'use strict';

var EventPluginRegistry = require('./EventPluginRegistry');
var EventPluginUtils = require('./EventPluginUtils');
var ReactErrorUtils = require('./ReactErrorUtils');

var accumulateInto = require('./accumulateInto');
var forEachAccumulated = require('./forEachAccumulated');
var invariant = require('fbjs/lib/invariant');
var warning = require('fbjs/lib/warning');

/**
 * Internal store for event listeners
 */
var listenerBank = {};

/**
 * Internal queue of events that have accumulated their dispatches and are
 * waiting to have their dispatches executed.
 */
var eventQueue = null;

/**
 * Dispatches an event and releases it back into the pool, unless persistent.
 *
 * @param {?object} event Synthetic event to be dispatched.
 * @param {boolean} simulated If the event is simulated (changes exn behavior)
 * @private
 */
var executeDispatchesAndRelease = function (event, simulated) {
  if (event) {
    EventPluginUtils.executeDispatchesInOrder(event, simulated);

    if (!event.isPersistent()) {
      event.constructor.release(event);
    }
  }
};
var executeDispatchesAndReleaseSimulated = function (e) {
  return executeDispatchesAndRelease(e, true);
};
var executeDispatchesAndReleaseTopLevel = function (e) {
  return executeDispatchesAndRelease(e, false);
};

/**
 * - `InstanceHandle`: [required] Module that performs logical traversals of DOM
 *   hierarchy given ids of the logical DOM elements involved.
 */
var InstanceHandle = null;

function validateInstanceHandle() {
  var valid = InstanceHandle && InstanceHandle.traverseTwoPhase && InstanceHandle.traverseEnterLeave;
  "production" !== 'production' ? warning(valid, 'InstanceHandle not injected before use!') : undefined;
}

/**
 * This is a unified interface for event plugins to be installed and configured.
 *
 * Event plugins can implement the following properties:
 *
 *   `extractEvents` {function(string, DOMEventTarget, string, object): *}
 *     Required. When a top-level event is fired, this method is expected to
 *     extract synthetic events that will in turn be queued and dispatched.
 *
 *   `eventTypes` {object}
 *     Optional, plugins that fire events must publish a mapping of registration
 *     names that are used to register listeners. Values of this mapping must
 *     be objects that contain `registrationName` or `phasedRegistrationNames`.
 *
 *   `executeDispatch` {function(object, function, string)}
 *     Optional, allows plugins to override how an event gets dispatched. By
 *     default, the listener is simply invoked.
 *
 * Each plugin that is injected into `EventsPluginHub` is immediately operable.
 *
 * @public
 */
var EventPluginHub = {

  /**
   * Methods for injecting dependencies.
   */
  injection: {

    /**
     * @param {object} InjectedMount
     * @public
     */
    injectMount: EventPluginUtils.injection.injectMount,

    /**
     * @param {object} InjectedInstanceHandle
     * @public
     */
    injectInstanceHandle: function (InjectedInstanceHandle) {
      InstanceHandle = InjectedInstanceHandle;
      if ("production" !== 'production') {
        validateInstanceHandle();
      }
    },

    getInstanceHandle: function () {
      if ("production" !== 'production') {
        validateInstanceHandle();
      }
      return InstanceHandle;
    },

    /**
     * @param {array} InjectedEventPluginOrder
     * @public
     */
    injectEventPluginOrder: EventPluginRegistry.injectEventPluginOrder,

    /**
     * @param {object} injectedNamesToPlugins Map from names to plugin modules.
     */
    injectEventPluginsByName: EventPluginRegistry.injectEventPluginsByName

  },

  eventNameDispatchConfigs: EventPluginRegistry.eventNameDispatchConfigs,

  registrationNameModules: EventPluginRegistry.registrationNameModules,

  /**
   * Stores `listener` at `listenerBank[registrationName][id]`. Is idempotent.
   *
   * @param {string} id ID of the DOM element.
   * @param {string} registrationName Name of listener (e.g. `onClick`).
   * @param {?function} listener The callback to store.
   */
  putListener: function (id, registrationName, listener) {
    !(typeof listener === 'function') ? "production" !== 'production' ? invariant(false, 'Expected %s listener to be a function, instead got type %s', registrationName, typeof listener) : invariant(false) : undefined;

    var bankForRegistrationName = listenerBank[registrationName] || (listenerBank[registrationName] = {});
    bankForRegistrationName[id] = listener;

    var PluginModule = EventPluginRegistry.registrationNameModules[registrationName];
    if (PluginModule && PluginModule.didPutListener) {
      PluginModule.didPutListener(id, registrationName, listener);
    }
  },

  /**
   * @param {string} id ID of the DOM element.
   * @param {string} registrationName Name of listener (e.g. `onClick`).
   * @return {?function} The stored callback.
   */
  getListener: function (id, registrationName) {
    var bankForRegistrationName = listenerBank[registrationName];
    return bankForRegistrationName && bankForRegistrationName[id];
  },

  /**
   * Deletes a listener from the registration bank.
   *
   * @param {string} id ID of the DOM element.
   * @param {string} registrationName Name of listener (e.g. `onClick`).
   */
  deleteListener: function (id, registrationName) {
    var PluginModule = EventPluginRegistry.registrationNameModules[registrationName];
    if (PluginModule && PluginModule.willDeleteListener) {
      PluginModule.willDeleteListener(id, registrationName);
    }

    var bankForRegistrationName = listenerBank[registrationName];
    // TODO: This should never be null -- when is it?
    if (bankForRegistrationName) {
      delete bankForRegistrationName[id];
    }
  },

  /**
   * Deletes all listeners for the DOM element with the supplied ID.
   *
   * @param {string} id ID of the DOM element.
   */
  deleteAllListeners: function (id) {
    for (var registrationName in listenerBank) {
      if (!listenerBank[registrationName][id]) {
        continue;
      }

      var PluginModule = EventPluginRegistry.registrationNameModules[registrationName];
      if (PluginModule && PluginModule.willDeleteListener) {
        PluginModule.willDeleteListener(id, registrationName);
      }

      delete listenerBank[registrationName][id];
    }
  },

  /**
   * Allows registered plugins an opportunity to extract events from top-level
   * native browser events.
   *
   * @param {string} topLevelType Record from `EventConstants`.
   * @param {DOMEventTarget} topLevelTarget The listening component root node.
   * @param {string} topLevelTargetID ID of `topLevelTarget`.
   * @param {object} nativeEvent Native browser event.
   * @return {*} An accumulation of synthetic events.
   * @internal
   */
  extractEvents: function (topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget) {
    var events;
    var plugins = EventPluginRegistry.plugins;
    for (var i = 0; i < plugins.length; i++) {
      // Not every plugin in the ordering may be loaded at runtime.
      var possiblePlugin = plugins[i];
      if (possiblePlugin) {
        var extractedEvents = possiblePlugin.extractEvents(topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget);
        if (extractedEvents) {
          events = accumulateInto(events, extractedEvents);
        }
      }
    }
    return events;
  },

  /**
   * Enqueues a synthetic event that should be dispatched when
   * `processEventQueue` is invoked.
   *
   * @param {*} events An accumulation of synthetic events.
   * @internal
   */
  enqueueEvents: function (events) {
    if (events) {
      eventQueue = accumulateInto(eventQueue, events);
    }
  },

  /**
   * Dispatches all synthetic events on the event queue.
   *
   * @internal
   */
  processEventQueue: function (simulated) {
    // Set `eventQueue` to null before processing it so that we can tell if more
    // events get enqueued while processing.
    var processingEventQueue = eventQueue;
    eventQueue = null;
    if (simulated) {
      forEachAccumulated(processingEventQueue, executeDispatchesAndReleaseSimulated);
    } else {
      forEachAccumulated(processingEventQueue, executeDispatchesAndReleaseTopLevel);
    }
    !!eventQueue ? "production" !== 'production' ? invariant(false, 'processEventQueue(): Additional events were enqueued while processing ' + 'an event queue. Support for this has not yet been implemented.') : invariant(false) : undefined;
    // This would be a good time to rethrow if any of the event handlers threw.
    ReactErrorUtils.rethrowCaughtError();
  },

  /**
   * These are needed for tests only. Do not use!
   */
  __purge: function () {
    listenerBank = {};
  },

  __getListenerBank: function () {
    return listenerBank;
  }

};

module.exports = EventPluginHub;
},{"./EventPluginRegistry":243,"./EventPluginUtils":244,"./ReactErrorUtils":283,"./accumulateInto":329,"./forEachAccumulated":337,"fbjs/lib/invariant":151,"fbjs/lib/warning":162}],243:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule EventPluginRegistry
 * @typechecks static-only
 */

'use strict';

var invariant = require('fbjs/lib/invariant');

/**
 * Injectable ordering of event plugins.
 */
var EventPluginOrder = null;

/**
 * Injectable mapping from names to event plugin modules.
 */
var namesToPlugins = {};

/**
 * Recomputes the plugin list using the injected plugins and plugin ordering.
 *
 * @private
 */
function recomputePluginOrdering() {
  if (!EventPluginOrder) {
    // Wait until an `EventPluginOrder` is injected.
    return;
  }
  for (var pluginName in namesToPlugins) {
    var PluginModule = namesToPlugins[pluginName];
    var pluginIndex = EventPluginOrder.indexOf(pluginName);
    !(pluginIndex > -1) ? "production" !== 'production' ? invariant(false, 'EventPluginRegistry: Cannot inject event plugins that do not exist in ' + 'the plugin ordering, `%s`.', pluginName) : invariant(false) : undefined;
    if (EventPluginRegistry.plugins[pluginIndex]) {
      continue;
    }
    !PluginModule.extractEvents ? "production" !== 'production' ? invariant(false, 'EventPluginRegistry: Event plugins must implement an `extractEvents` ' + 'method, but `%s` does not.', pluginName) : invariant(false) : undefined;
    EventPluginRegistry.plugins[pluginIndex] = PluginModule;
    var publishedEvents = PluginModule.eventTypes;
    for (var eventName in publishedEvents) {
      !publishEventForPlugin(publishedEvents[eventName], PluginModule, eventName) ? "production" !== 'production' ? invariant(false, 'EventPluginRegistry: Failed to publish event `%s` for plugin `%s`.', eventName, pluginName) : invariant(false) : undefined;
    }
  }
}

/**
 * Publishes an event so that it can be dispatched by the supplied plugin.
 *
 * @param {object} dispatchConfig Dispatch configuration for the event.
 * @param {object} PluginModule Plugin publishing the event.
 * @return {boolean} True if the event was successfully published.
 * @private
 */
function publishEventForPlugin(dispatchConfig, PluginModule, eventName) {
  !!EventPluginRegistry.eventNameDispatchConfigs.hasOwnProperty(eventName) ? "production" !== 'production' ? invariant(false, 'EventPluginHub: More than one plugin attempted to publish the same ' + 'event name, `%s`.', eventName) : invariant(false) : undefined;
  EventPluginRegistry.eventNameDispatchConfigs[eventName] = dispatchConfig;

  var phasedRegistrationNames = dispatchConfig.phasedRegistrationNames;
  if (phasedRegistrationNames) {
    for (var phaseName in phasedRegistrationNames) {
      if (phasedRegistrationNames.hasOwnProperty(phaseName)) {
        var phasedRegistrationName = phasedRegistrationNames[phaseName];
        publishRegistrationName(phasedRegistrationName, PluginModule, eventName);
      }
    }
    return true;
  } else if (dispatchConfig.registrationName) {
    publishRegistrationName(dispatchConfig.registrationName, PluginModule, eventName);
    return true;
  }
  return false;
}

/**
 * Publishes a registration name that is used to identify dispatched events and
 * can be used with `EventPluginHub.putListener` to register listeners.
 *
 * @param {string} registrationName Registration name to add.
 * @param {object} PluginModule Plugin publishing the event.
 * @private
 */
function publishRegistrationName(registrationName, PluginModule, eventName) {
  !!EventPluginRegistry.registrationNameModules[registrationName] ? "production" !== 'production' ? invariant(false, 'EventPluginHub: More than one plugin attempted to publish the same ' + 'registration name, `%s`.', registrationName) : invariant(false) : undefined;
  EventPluginRegistry.registrationNameModules[registrationName] = PluginModule;
  EventPluginRegistry.registrationNameDependencies[registrationName] = PluginModule.eventTypes[eventName].dependencies;
}

/**
 * Registers plugins so that they can extract and dispatch events.
 *
 * @see {EventPluginHub}
 */
var EventPluginRegistry = {

  /**
   * Ordered list of injected plugins.
   */
  plugins: [],

  /**
   * Mapping from event name to dispatch config
   */
  eventNameDispatchConfigs: {},

  /**
   * Mapping from registration name to plugin module
   */
  registrationNameModules: {},

  /**
   * Mapping from registration name to event name
   */
  registrationNameDependencies: {},

  /**
   * Injects an ordering of plugins (by plugin name). This allows the ordering
   * to be decoupled from injection of the actual plugins so that ordering is
   * always deterministic regardless of packaging, on-the-fly injection, etc.
   *
   * @param {array} InjectedEventPluginOrder
   * @internal
   * @see {EventPluginHub.injection.injectEventPluginOrder}
   */
  injectEventPluginOrder: function (InjectedEventPluginOrder) {
    !!EventPluginOrder ? "production" !== 'production' ? invariant(false, 'EventPluginRegistry: Cannot inject event plugin ordering more than ' + 'once. You are likely trying to load more than one copy of React.') : invariant(false) : undefined;
    // Clone the ordering so it cannot be dynamically mutated.
    EventPluginOrder = Array.prototype.slice.call(InjectedEventPluginOrder);
    recomputePluginOrdering();
  },

  /**
   * Injects plugins to be used by `EventPluginHub`. The plugin names must be
   * in the ordering injected by `injectEventPluginOrder`.
   *
   * Plugins can be injected as part of page initialization or on-the-fly.
   *
   * @param {object} injectedNamesToPlugins Map from names to plugin modules.
   * @internal
   * @see {EventPluginHub.injection.injectEventPluginsByName}
   */
  injectEventPluginsByName: function (injectedNamesToPlugins) {
    var isOrderingDirty = false;
    for (var pluginName in injectedNamesToPlugins) {
      if (!injectedNamesToPlugins.hasOwnProperty(pluginName)) {
        continue;
      }
      var PluginModule = injectedNamesToPlugins[pluginName];
      if (!namesToPlugins.hasOwnProperty(pluginName) || namesToPlugins[pluginName] !== PluginModule) {
        !!namesToPlugins[pluginName] ? "production" !== 'production' ? invariant(false, 'EventPluginRegistry: Cannot inject two different event plugins ' + 'using the same name, `%s`.', pluginName) : invariant(false) : undefined;
        namesToPlugins[pluginName] = PluginModule;
        isOrderingDirty = true;
      }
    }
    if (isOrderingDirty) {
      recomputePluginOrdering();
    }
  },

  /**
   * Looks up the plugin for the supplied event.
   *
   * @param {object} event A synthetic event.
   * @return {?object} The plugin that created the supplied event.
   * @internal
   */
  getPluginModuleForEvent: function (event) {
    var dispatchConfig = event.dispatchConfig;
    if (dispatchConfig.registrationName) {
      return EventPluginRegistry.registrationNameModules[dispatchConfig.registrationName] || null;
    }
    for (var phase in dispatchConfig.phasedRegistrationNames) {
      if (!dispatchConfig.phasedRegistrationNames.hasOwnProperty(phase)) {
        continue;
      }
      var PluginModule = EventPluginRegistry.registrationNameModules[dispatchConfig.phasedRegistrationNames[phase]];
      if (PluginModule) {
        return PluginModule;
      }
    }
    return null;
  },

  /**
   * Exposed for unit testing.
   * @private
   */
  _resetEventPlugins: function () {
    EventPluginOrder = null;
    for (var pluginName in namesToPlugins) {
      if (namesToPlugins.hasOwnProperty(pluginName)) {
        delete namesToPlugins[pluginName];
      }
    }
    EventPluginRegistry.plugins.length = 0;

    var eventNameDispatchConfigs = EventPluginRegistry.eventNameDispatchConfigs;
    for (var eventName in eventNameDispatchConfigs) {
      if (eventNameDispatchConfigs.hasOwnProperty(eventName)) {
        delete eventNameDispatchConfigs[eventName];
      }
    }

    var registrationNameModules = EventPluginRegistry.registrationNameModules;
    for (var registrationName in registrationNameModules) {
      if (registrationNameModules.hasOwnProperty(registrationName)) {
        delete registrationNameModules[registrationName];
      }
    }
  }

};

module.exports = EventPluginRegistry;
},{"fbjs/lib/invariant":151}],244:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule EventPluginUtils
 */

'use strict';

var EventConstants = require('./EventConstants');
var ReactErrorUtils = require('./ReactErrorUtils');

var invariant = require('fbjs/lib/invariant');
var warning = require('fbjs/lib/warning');

/**
 * Injected dependencies:
 */

/**
 * - `Mount`: [required] Module that can convert between React dom IDs and
 *   actual node references.
 */
var injection = {
  Mount: null,
  injectMount: function (InjectedMount) {
    injection.Mount = InjectedMount;
    if ("production" !== 'production') {
      "production" !== 'production' ? warning(InjectedMount && InjectedMount.getNode && InjectedMount.getID, 'EventPluginUtils.injection.injectMount(...): Injected Mount ' + 'module is missing getNode or getID.') : undefined;
    }
  }
};

var topLevelTypes = EventConstants.topLevelTypes;

function isEndish(topLevelType) {
  return topLevelType === topLevelTypes.topMouseUp || topLevelType === topLevelTypes.topTouchEnd || topLevelType === topLevelTypes.topTouchCancel;
}

function isMoveish(topLevelType) {
  return topLevelType === topLevelTypes.topMouseMove || topLevelType === topLevelTypes.topTouchMove;
}
function isStartish(topLevelType) {
  return topLevelType === topLevelTypes.topMouseDown || topLevelType === topLevelTypes.topTouchStart;
}

var validateEventDispatches;
if ("production" !== 'production') {
  validateEventDispatches = function (event) {
    var dispatchListeners = event._dispatchListeners;
    var dispatchIDs = event._dispatchIDs;

    var listenersIsArr = Array.isArray(dispatchListeners);
    var idsIsArr = Array.isArray(dispatchIDs);
    var IDsLen = idsIsArr ? dispatchIDs.length : dispatchIDs ? 1 : 0;
    var listenersLen = listenersIsArr ? dispatchListeners.length : dispatchListeners ? 1 : 0;

    "production" !== 'production' ? warning(idsIsArr === listenersIsArr && IDsLen === listenersLen, 'EventPluginUtils: Invalid `event`.') : undefined;
  };
}

/**
 * Dispatch the event to the listener.
 * @param {SyntheticEvent} event SyntheticEvent to handle
 * @param {boolean} simulated If the event is simulated (changes exn behavior)
 * @param {function} listener Application-level callback
 * @param {string} domID DOM id to pass to the callback.
 */
function executeDispatch(event, simulated, listener, domID) {
  var type = event.type || 'unknown-event';
  event.currentTarget = injection.Mount.getNode(domID);
  if (simulated) {
    ReactErrorUtils.invokeGuardedCallbackWithCatch(type, listener, event, domID);
  } else {
    ReactErrorUtils.invokeGuardedCallback(type, listener, event, domID);
  }
  event.currentTarget = null;
}

/**
 * Standard/simple iteration through an event's collected dispatches.
 */
function executeDispatchesInOrder(event, simulated) {
  var dispatchListeners = event._dispatchListeners;
  var dispatchIDs = event._dispatchIDs;
  if ("production" !== 'production') {
    validateEventDispatches(event);
  }
  if (Array.isArray(dispatchListeners)) {
    for (var i = 0; i < dispatchListeners.length; i++) {
      if (event.isPropagationStopped()) {
        break;
      }
      // Listeners and IDs are two parallel arrays that are always in sync.
      executeDispatch(event, simulated, dispatchListeners[i], dispatchIDs[i]);
    }
  } else if (dispatchListeners) {
    executeDispatch(event, simulated, dispatchListeners, dispatchIDs);
  }
  event._dispatchListeners = null;
  event._dispatchIDs = null;
}

/**
 * Standard/simple iteration through an event's collected dispatches, but stops
 * at the first dispatch execution returning true, and returns that id.
 *
 * @return {?string} id of the first dispatch execution who's listener returns
 * true, or null if no listener returned true.
 */
function executeDispatchesInOrderStopAtTrueImpl(event) {
  var dispatchListeners = event._dispatchListeners;
  var dispatchIDs = event._dispatchIDs;
  if ("production" !== 'production') {
    validateEventDispatches(event);
  }
  if (Array.isArray(dispatchListeners)) {
    for (var i = 0; i < dispatchListeners.length; i++) {
      if (event.isPropagationStopped()) {
        break;
      }
      // Listeners and IDs are two parallel arrays that are always in sync.
      if (dispatchListeners[i](event, dispatchIDs[i])) {
        return dispatchIDs[i];
      }
    }
  } else if (dispatchListeners) {
    if (dispatchListeners(event, dispatchIDs)) {
      return dispatchIDs;
    }
  }
  return null;
}

/**
 * @see executeDispatchesInOrderStopAtTrueImpl
 */
function executeDispatchesInOrderStopAtTrue(event) {
  var ret = executeDispatchesInOrderStopAtTrueImpl(event);
  event._dispatchIDs = null;
  event._dispatchListeners = null;
  return ret;
}

/**
 * Execution of a "direct" dispatch - there must be at most one dispatch
 * accumulated on the event or it is considered an error. It doesn't really make
 * sense for an event with multiple dispatches (bubbled) to keep track of the
 * return values at each dispatch execution, but it does tend to make sense when
 * dealing with "direct" dispatches.
 *
 * @return {*} The return value of executing the single dispatch.
 */
function executeDirectDispatch(event) {
  if ("production" !== 'production') {
    validateEventDispatches(event);
  }
  var dispatchListener = event._dispatchListeners;
  var dispatchID = event._dispatchIDs;
  !!Array.isArray(dispatchListener) ? "production" !== 'production' ? invariant(false, 'executeDirectDispatch(...): Invalid `event`.') : invariant(false) : undefined;
  var res = dispatchListener ? dispatchListener(event, dispatchID) : null;
  event._dispatchListeners = null;
  event._dispatchIDs = null;
  return res;
}

/**
 * @param {SyntheticEvent} event
 * @return {boolean} True iff number of dispatches accumulated is greater than 0.
 */
function hasDispatches(event) {
  return !!event._dispatchListeners;
}

/**
 * General utilities that are useful in creating custom Event Plugins.
 */
var EventPluginUtils = {
  isEndish: isEndish,
  isMoveish: isMoveish,
  isStartish: isStartish,

  executeDirectDispatch: executeDirectDispatch,
  executeDispatchesInOrder: executeDispatchesInOrder,
  executeDispatchesInOrderStopAtTrue: executeDispatchesInOrderStopAtTrue,
  hasDispatches: hasDispatches,

  getNode: function (id) {
    return injection.Mount.getNode(id);
  },
  getID: function (node) {
    return injection.Mount.getID(node);
  },

  injection: injection
};

module.exports = EventPluginUtils;
},{"./EventConstants":241,"./ReactErrorUtils":283,"fbjs/lib/invariant":151,"fbjs/lib/warning":162}],245:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule EventPropagators
 */

'use strict';

var EventConstants = require('./EventConstants');
var EventPluginHub = require('./EventPluginHub');

var warning = require('fbjs/lib/warning');

var accumulateInto = require('./accumulateInto');
var forEachAccumulated = require('./forEachAccumulated');

var PropagationPhases = EventConstants.PropagationPhases;
var getListener = EventPluginHub.getListener;

/**
 * Some event types have a notion of different registration names for different
 * "phases" of propagation. This finds listeners by a given phase.
 */
function listenerAtPhase(id, event, propagationPhase) {
  var registrationName = event.dispatchConfig.phasedRegistrationNames[propagationPhase];
  return getListener(id, registrationName);
}

/**
 * Tags a `SyntheticEvent` with dispatched listeners. Creating this function
 * here, allows us to not have to bind or create functions for each event.
 * Mutating the event's members allows us to not have to create a wrapping
 * "dispatch" object that pairs the event with the listener.
 */
function accumulateDirectionalDispatches(domID, upwards, event) {
  if ("production" !== 'production') {
    "production" !== 'production' ? warning(domID, 'Dispatching id must not be null') : undefined;
  }
  var phase = upwards ? PropagationPhases.bubbled : PropagationPhases.captured;
  var listener = listenerAtPhase(domID, event, phase);
  if (listener) {
    event._dispatchListeners = accumulateInto(event._dispatchListeners, listener);
    event._dispatchIDs = accumulateInto(event._dispatchIDs, domID);
  }
}

/**
 * Collect dispatches (must be entirely collected before dispatching - see unit
 * tests). Lazily allocate the array to conserve memory.  We must loop through
 * each event and perform the traversal for each one. We cannot perform a
 * single traversal for the entire collection of events because each event may
 * have a different target.
 */
function accumulateTwoPhaseDispatchesSingle(event) {
  if (event && event.dispatchConfig.phasedRegistrationNames) {
    EventPluginHub.injection.getInstanceHandle().traverseTwoPhase(event.dispatchMarker, accumulateDirectionalDispatches, event);
  }
}

/**
 * Same as `accumulateTwoPhaseDispatchesSingle`, but skips over the targetID.
 */
function accumulateTwoPhaseDispatchesSingleSkipTarget(event) {
  if (event && event.dispatchConfig.phasedRegistrationNames) {
    EventPluginHub.injection.getInstanceHandle().traverseTwoPhaseSkipTarget(event.dispatchMarker, accumulateDirectionalDispatches, event);
  }
}

/**
 * Accumulates without regard to direction, does not look for phased
 * registration names. Same as `accumulateDirectDispatchesSingle` but without
 * requiring that the `dispatchMarker` be the same as the dispatched ID.
 */
function accumulateDispatches(id, ignoredDirection, event) {
  if (event && event.dispatchConfig.registrationName) {
    var registrationName = event.dispatchConfig.registrationName;
    var listener = getListener(id, registrationName);
    if (listener) {
      event._dispatchListeners = accumulateInto(event._dispatchListeners, listener);
      event._dispatchIDs = accumulateInto(event._dispatchIDs, id);
    }
  }
}

/**
 * Accumulates dispatches on an `SyntheticEvent`, but only for the
 * `dispatchMarker`.
 * @param {SyntheticEvent} event
 */
function accumulateDirectDispatchesSingle(event) {
  if (event && event.dispatchConfig.registrationName) {
    accumulateDispatches(event.dispatchMarker, null, event);
  }
}

function accumulateTwoPhaseDispatches(events) {
  forEachAccumulated(events, accumulateTwoPhaseDispatchesSingle);
}

function accumulateTwoPhaseDispatchesSkipTarget(events) {
  forEachAccumulated(events, accumulateTwoPhaseDispatchesSingleSkipTarget);
}

function accumulateEnterLeaveDispatches(leave, enter, fromID, toID) {
  EventPluginHub.injection.getInstanceHandle().traverseEnterLeave(fromID, toID, accumulateDispatches, leave, enter);
}

function accumulateDirectDispatches(events) {
  forEachAccumulated(events, accumulateDirectDispatchesSingle);
}

/**
 * A small set of propagation patterns, each of which will accept a small amount
 * of information, and generate a set of "dispatch ready event objects" - which
 * are sets of events that have already been annotated with a set of dispatched
 * listener functions/ids. The API is designed this way to discourage these
 * propagation strategies from actually executing the dispatches, since we
 * always want to collect the entire set of dispatches before executing event a
 * single one.
 *
 * @constructor EventPropagators
 */
var EventPropagators = {
  accumulateTwoPhaseDispatches: accumulateTwoPhaseDispatches,
  accumulateTwoPhaseDispatchesSkipTarget: accumulateTwoPhaseDispatchesSkipTarget,
  accumulateDirectDispatches: accumulateDirectDispatches,
  accumulateEnterLeaveDispatches: accumulateEnterLeaveDispatches
};

module.exports = EventPropagators;
},{"./EventConstants":241,"./EventPluginHub":242,"./accumulateInto":329,"./forEachAccumulated":337,"fbjs/lib/warning":162}],246:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule FallbackCompositionState
 * @typechecks static-only
 */

'use strict';

var PooledClass = require('./PooledClass');

var assign = require('./Object.assign');
var getTextContentAccessor = require('./getTextContentAccessor');

/**
 * This helper class stores information about text content of a target node,
 * allowing comparison of content before and after a given event.
 *
 * Identify the node where selection currently begins, then observe
 * both its text content and its current position in the DOM. Since the
 * browser may natively replace the target node during composition, we can
 * use its position to find its replacement.
 *
 * @param {DOMEventTarget} root
 */
function FallbackCompositionState(root) {
  this._root = root;
  this._startText = this.getText();
  this._fallbackText = null;
}

assign(FallbackCompositionState.prototype, {
  destructor: function () {
    this._root = null;
    this._startText = null;
    this._fallbackText = null;
  },

  /**
   * Get current text of input.
   *
   * @return {string}
   */
  getText: function () {
    if ('value' in this._root) {
      return this._root.value;
    }
    return this._root[getTextContentAccessor()];
  },

  /**
   * Determine the differing substring between the initially stored
   * text content and the current content.
   *
   * @return {string}
   */
  getData: function () {
    if (this._fallbackText) {
      return this._fallbackText;
    }

    var start;
    var startValue = this._startText;
    var startLength = startValue.length;
    var end;
    var endValue = this.getText();
    var endLength = endValue.length;

    for (start = 0; start < startLength; start++) {
      if (startValue[start] !== endValue[start]) {
        break;
      }
    }

    var minEnd = startLength - start;
    for (end = 1; end <= minEnd; end++) {
      if (startValue[startLength - end] !== endValue[endLength - end]) {
        break;
      }
    }

    var sliceTail = end > 1 ? 1 - end : undefined;
    this._fallbackText = endValue.slice(start, sliceTail);
    return this._fallbackText;
  }
});

PooledClass.addPoolingTo(FallbackCompositionState);

module.exports = FallbackCompositionState;
},{"./Object.assign":249,"./PooledClass":250,"./getTextContentAccessor":344}],247:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule HTMLDOMPropertyConfig
 */

'use strict';

var DOMProperty = require('./DOMProperty');
var ExecutionEnvironment = require('fbjs/lib/ExecutionEnvironment');

var MUST_USE_ATTRIBUTE = DOMProperty.injection.MUST_USE_ATTRIBUTE;
var MUST_USE_PROPERTY = DOMProperty.injection.MUST_USE_PROPERTY;
var HAS_BOOLEAN_VALUE = DOMProperty.injection.HAS_BOOLEAN_VALUE;
var HAS_SIDE_EFFECTS = DOMProperty.injection.HAS_SIDE_EFFECTS;
var HAS_NUMERIC_VALUE = DOMProperty.injection.HAS_NUMERIC_VALUE;
var HAS_POSITIVE_NUMERIC_VALUE = DOMProperty.injection.HAS_POSITIVE_NUMERIC_VALUE;
var HAS_OVERLOADED_BOOLEAN_VALUE = DOMProperty.injection.HAS_OVERLOADED_BOOLEAN_VALUE;

var hasSVG;
if (ExecutionEnvironment.canUseDOM) {
  var implementation = document.implementation;
  hasSVG = implementation && implementation.hasFeature && implementation.hasFeature('http://www.w3.org/TR/SVG11/feature#BasicStructure', '1.1');
}

var HTMLDOMPropertyConfig = {
  isCustomAttribute: RegExp.prototype.test.bind(/^(data|aria)-[a-z_][a-z\d_.\-]*$/),
  Properties: {
    /**
     * Standard Properties
     */
    accept: null,
    acceptCharset: null,
    accessKey: null,
    action: null,
    allowFullScreen: MUST_USE_ATTRIBUTE | HAS_BOOLEAN_VALUE,
    allowTransparency: MUST_USE_ATTRIBUTE,
    alt: null,
    async: HAS_BOOLEAN_VALUE,
    autoComplete: null,
    // autoFocus is polyfilled/normalized by AutoFocusUtils
    // autoFocus: HAS_BOOLEAN_VALUE,
    autoPlay: HAS_BOOLEAN_VALUE,
    capture: MUST_USE_ATTRIBUTE | HAS_BOOLEAN_VALUE,
    cellPadding: null,
    cellSpacing: null,
    charSet: MUST_USE_ATTRIBUTE,
    challenge: MUST_USE_ATTRIBUTE,
    checked: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
    classID: MUST_USE_ATTRIBUTE,
    // To set className on SVG elements, it's necessary to use .setAttribute;
    // this works on HTML elements too in all browsers except IE8. Conveniently,
    // IE8 doesn't support SVG and so we can simply use the attribute in
    // browsers that support SVG and the property in browsers that don't,
    // regardless of whether the element is HTML or SVG.
    className: hasSVG ? MUST_USE_ATTRIBUTE : MUST_USE_PROPERTY,
    cols: MUST_USE_ATTRIBUTE | HAS_POSITIVE_NUMERIC_VALUE,
    colSpan: null,
    content: null,
    contentEditable: null,
    contextMenu: MUST_USE_ATTRIBUTE,
    controls: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
    coords: null,
    crossOrigin: null,
    data: null, // For `<object />` acts as `src`.
    dateTime: MUST_USE_ATTRIBUTE,
    'default': HAS_BOOLEAN_VALUE,
    defer: HAS_BOOLEAN_VALUE,
    dir: null,
    disabled: MUST_USE_ATTRIBUTE | HAS_BOOLEAN_VALUE,
    download: HAS_OVERLOADED_BOOLEAN_VALUE,
    draggable: null,
    encType: null,
    form: MUST_USE_ATTRIBUTE,
    formAction: MUST_USE_ATTRIBUTE,
    formEncType: MUST_USE_ATTRIBUTE,
    formMethod: MUST_USE_ATTRIBUTE,
    formNoValidate: HAS_BOOLEAN_VALUE,
    formTarget: MUST_USE_ATTRIBUTE,
    frameBorder: MUST_USE_ATTRIBUTE,
    headers: null,
    height: MUST_USE_ATTRIBUTE,
    hidden: MUST_USE_ATTRIBUTE | HAS_BOOLEAN_VALUE,
    high: null,
    href: null,
    hrefLang: null,
    htmlFor: null,
    httpEquiv: null,
    icon: null,
    id: MUST_USE_PROPERTY,
    inputMode: MUST_USE_ATTRIBUTE,
    integrity: null,
    is: MUST_USE_ATTRIBUTE,
    keyParams: MUST_USE_ATTRIBUTE,
    keyType: MUST_USE_ATTRIBUTE,
    kind: null,
    label: null,
    lang: null,
    list: MUST_USE_ATTRIBUTE,
    loop: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
    low: null,
    manifest: MUST_USE_ATTRIBUTE,
    marginHeight: null,
    marginWidth: null,
    max: null,
    maxLength: MUST_USE_ATTRIBUTE,
    media: MUST_USE_ATTRIBUTE,
    mediaGroup: null,
    method: null,
    min: null,
    minLength: MUST_USE_ATTRIBUTE,
    multiple: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
    muted: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
    name: null,
    nonce: MUST_USE_ATTRIBUTE,
    noValidate: HAS_BOOLEAN_VALUE,
    open: HAS_BOOLEAN_VALUE,
    optimum: null,
    pattern: null,
    placeholder: null,
    poster: null,
    preload: null,
    radioGroup: null,
    readOnly: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
    rel: null,
    required: HAS_BOOLEAN_VALUE,
    reversed: HAS_BOOLEAN_VALUE,
    role: MUST_USE_ATTRIBUTE,
    rows: MUST_USE_ATTRIBUTE | HAS_POSITIVE_NUMERIC_VALUE,
    rowSpan: null,
    sandbox: null,
    scope: null,
    scoped: HAS_BOOLEAN_VALUE,
    scrolling: null,
    seamless: MUST_USE_ATTRIBUTE | HAS_BOOLEAN_VALUE,
    selected: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
    shape: null,
    size: MUST_USE_ATTRIBUTE | HAS_POSITIVE_NUMERIC_VALUE,
    sizes: MUST_USE_ATTRIBUTE,
    span: HAS_POSITIVE_NUMERIC_VALUE,
    spellCheck: null,
    src: null,
    srcDoc: MUST_USE_PROPERTY,
    srcLang: null,
    srcSet: MUST_USE_ATTRIBUTE,
    start: HAS_NUMERIC_VALUE,
    step: null,
    style: null,
    summary: null,
    tabIndex: null,
    target: null,
    title: null,
    type: null,
    useMap: null,
    value: MUST_USE_PROPERTY | HAS_SIDE_EFFECTS,
    width: MUST_USE_ATTRIBUTE,
    wmode: MUST_USE_ATTRIBUTE,
    wrap: null,

    /**
     * RDFa Properties
     */
    about: MUST_USE_ATTRIBUTE,
    datatype: MUST_USE_ATTRIBUTE,
    inlist: MUST_USE_ATTRIBUTE,
    prefix: MUST_USE_ATTRIBUTE,
    // property is also supported for OpenGraph in meta tags.
    property: MUST_USE_ATTRIBUTE,
    resource: MUST_USE_ATTRIBUTE,
    'typeof': MUST_USE_ATTRIBUTE,
    vocab: MUST_USE_ATTRIBUTE,

    /**
     * Non-standard Properties
     */
    // autoCapitalize and autoCorrect are supported in Mobile Safari for
    // keyboard hints.
    autoCapitalize: MUST_USE_ATTRIBUTE,
    autoCorrect: MUST_USE_ATTRIBUTE,
    // autoSave allows WebKit/Blink to persist values of input fields on page reloads
    autoSave: null,
    // color is for Safari mask-icon link
    color: null,
    // itemProp, itemScope, itemType are for
    // Microdata support. See http://schema.org/docs/gs.html
    itemProp: MUST_USE_ATTRIBUTE,
    itemScope: MUST_USE_ATTRIBUTE | HAS_BOOLEAN_VALUE,
    itemType: MUST_USE_ATTRIBUTE,
    // itemID and itemRef are for Microdata support as well but
    // only specified in the the WHATWG spec document. See
    // https://html.spec.whatwg.org/multipage/microdata.html#microdata-dom-api
    itemID: MUST_USE_ATTRIBUTE,
    itemRef: MUST_USE_ATTRIBUTE,
    // results show looking glass icon and recent searches on input
    // search fields in WebKit/Blink
    results: null,
    // IE-only attribute that specifies security restrictions on an iframe
    // as an alternative to the sandbox attribute on IE<10
    security: MUST_USE_ATTRIBUTE,
    // IE-only attribute that controls focus behavior
    unselectable: MUST_USE_ATTRIBUTE
  },
  DOMAttributeNames: {
    acceptCharset: 'accept-charset',
    className: 'class',
    htmlFor: 'for',
    httpEquiv: 'http-equiv'
  },
  DOMPropertyNames: {
    autoComplete: 'autocomplete',
    autoFocus: 'autofocus',
    autoPlay: 'autoplay',
    autoSave: 'autosave',
    // `encoding` is equivalent to `enctype`, IE8 lacks an `enctype` setter.
    // http://www.w3.org/TR/html5/forms.html#dom-fs-encoding
    encType: 'encoding',
    hrefLang: 'hreflang',
    radioGroup: 'radiogroup',
    spellCheck: 'spellcheck',
    srcDoc: 'srcdoc',
    srcSet: 'srcset'
  }
};

module.exports = HTMLDOMPropertyConfig;
},{"./DOMProperty":236,"fbjs/lib/ExecutionEnvironment":137}],248:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule LinkedValueUtils
 * @typechecks static-only
 */

'use strict';

var ReactPropTypes = require('./ReactPropTypes');
var ReactPropTypeLocations = require('./ReactPropTypeLocations');

var invariant = require('fbjs/lib/invariant');
var warning = require('fbjs/lib/warning');

var hasReadOnlyValue = {
  'button': true,
  'checkbox': true,
  'image': true,
  'hidden': true,
  'radio': true,
  'reset': true,
  'submit': true
};

function _assertSingleLink(inputProps) {
  !(inputProps.checkedLink == null || inputProps.valueLink == null) ? "production" !== 'production' ? invariant(false, 'Cannot provide a checkedLink and a valueLink. If you want to use ' + 'checkedLink, you probably don\'t want to use valueLink and vice versa.') : invariant(false) : undefined;
}
function _assertValueLink(inputProps) {
  _assertSingleLink(inputProps);
  !(inputProps.value == null && inputProps.onChange == null) ? "production" !== 'production' ? invariant(false, 'Cannot provide a valueLink and a value or onChange event. If you want ' + 'to use value or onChange, you probably don\'t want to use valueLink.') : invariant(false) : undefined;
}

function _assertCheckedLink(inputProps) {
  _assertSingleLink(inputProps);
  !(inputProps.checked == null && inputProps.onChange == null) ? "production" !== 'production' ? invariant(false, 'Cannot provide a checkedLink and a checked property or onChange event. ' + 'If you want to use checked or onChange, you probably don\'t want to ' + 'use checkedLink') : invariant(false) : undefined;
}

var propTypes = {
  value: function (props, propName, componentName) {
    if (!props[propName] || hasReadOnlyValue[props.type] || props.onChange || props.readOnly || props.disabled) {
      return null;
    }
    return new Error('You provided a `value` prop to a form field without an ' + '`onChange` handler. This will render a read-only field. If ' + 'the field should be mutable use `defaultValue`. Otherwise, ' + 'set either `onChange` or `readOnly`.');
  },
  checked: function (props, propName, componentName) {
    if (!props[propName] || props.onChange || props.readOnly || props.disabled) {
      return null;
    }
    return new Error('You provided a `checked` prop to a form field without an ' + '`onChange` handler. This will render a read-only field. If ' + 'the field should be mutable use `defaultChecked`. Otherwise, ' + 'set either `onChange` or `readOnly`.');
  },
  onChange: ReactPropTypes.func
};

var loggedTypeFailures = {};
function getDeclarationErrorAddendum(owner) {
  if (owner) {
    var name = owner.getName();
    if (name) {
      return ' Check the render method of `' + name + '`.';
    }
  }
  return '';
}

/**
 * Provide a linked `value` attribute for controlled forms. You should not use
 * this outside of the ReactDOM controlled form components.
 */
var LinkedValueUtils = {
  checkPropTypes: function (tagName, props, owner) {
    for (var propName in propTypes) {
      if (propTypes.hasOwnProperty(propName)) {
        var error = propTypes[propName](props, propName, tagName, ReactPropTypeLocations.prop);
      }
      if (error instanceof Error && !(error.message in loggedTypeFailures)) {
        // Only monitor this failure once because there tends to be a lot of the
        // same error.
        loggedTypeFailures[error.message] = true;

        var addendum = getDeclarationErrorAddendum(owner);
        "production" !== 'production' ? warning(false, 'Failed form propType: %s%s', error.message, addendum) : undefined;
      }
    }
  },

  /**
   * @param {object} inputProps Props for form component
   * @return {*} current value of the input either from value prop or link.
   */
  getValue: function (inputProps) {
    if (inputProps.valueLink) {
      _assertValueLink(inputProps);
      return inputProps.valueLink.value;
    }
    return inputProps.value;
  },

  /**
   * @param {object} inputProps Props for form component
   * @return {*} current checked status of the input either from checked prop
   *             or link.
   */
  getChecked: function (inputProps) {
    if (inputProps.checkedLink) {
      _assertCheckedLink(inputProps);
      return inputProps.checkedLink.value;
    }
    return inputProps.checked;
  },

  /**
   * @param {object} inputProps Props for form component
   * @param {SyntheticEvent} event change event to handle
   */
  executeOnChange: function (inputProps, event) {
    if (inputProps.valueLink) {
      _assertValueLink(inputProps);
      return inputProps.valueLink.requestChange(event.target.value);
    } else if (inputProps.checkedLink) {
      _assertCheckedLink(inputProps);
      return inputProps.checkedLink.requestChange(event.target.checked);
    } else if (inputProps.onChange) {
      return inputProps.onChange.call(undefined, event);
    }
  }
};

module.exports = LinkedValueUtils;
},{"./ReactPropTypeLocations":300,"./ReactPropTypes":301,"fbjs/lib/invariant":151,"fbjs/lib/warning":162}],249:[function(require,module,exports){
/**
 * Copyright 2014-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule Object.assign
 */

// https://people.mozilla.org/~jorendorff/es6-draft.html#sec-object.assign

'use strict';

function assign(target, sources) {
  if (target == null) {
    throw new TypeError('Object.assign target cannot be null or undefined');
  }

  var to = Object(target);
  var hasOwnProperty = Object.prototype.hasOwnProperty;

  for (var nextIndex = 1; nextIndex < arguments.length; nextIndex++) {
    var nextSource = arguments[nextIndex];
    if (nextSource == null) {
      continue;
    }

    var from = Object(nextSource);

    // We don't currently support accessors nor proxies. Therefore this
    // copy cannot throw. If we ever supported this then we must handle
    // exceptions and side-effects. We don't support symbols so they won't
    // be transferred.

    for (var key in from) {
      if (hasOwnProperty.call(from, key)) {
        to[key] = from[key];
      }
    }
  }

  return to;
}

module.exports = assign;
},{}],250:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule PooledClass
 */

'use strict';

var invariant = require('fbjs/lib/invariant');

/**
 * Static poolers. Several custom versions for each potential number of
 * arguments. A completely generic pooler is easy to implement, but would
 * require accessing the `arguments` object. In each of these, `this` refers to
 * the Class itself, not an instance. If any others are needed, simply add them
 * here, or in their own files.
 */
var oneArgumentPooler = function (copyFieldsFrom) {
  var Klass = this;
  if (Klass.instancePool.length) {
    var instance = Klass.instancePool.pop();
    Klass.call(instance, copyFieldsFrom);
    return instance;
  } else {
    return new Klass(copyFieldsFrom);
  }
};

var twoArgumentPooler = function (a1, a2) {
  var Klass = this;
  if (Klass.instancePool.length) {
    var instance = Klass.instancePool.pop();
    Klass.call(instance, a1, a2);
    return instance;
  } else {
    return new Klass(a1, a2);
  }
};

var threeArgumentPooler = function (a1, a2, a3) {
  var Klass = this;
  if (Klass.instancePool.length) {
    var instance = Klass.instancePool.pop();
    Klass.call(instance, a1, a2, a3);
    return instance;
  } else {
    return new Klass(a1, a2, a3);
  }
};

var fourArgumentPooler = function (a1, a2, a3, a4) {
  var Klass = this;
  if (Klass.instancePool.length) {
    var instance = Klass.instancePool.pop();
    Klass.call(instance, a1, a2, a3, a4);
    return instance;
  } else {
    return new Klass(a1, a2, a3, a4);
  }
};

var fiveArgumentPooler = function (a1, a2, a3, a4, a5) {
  var Klass = this;
  if (Klass.instancePool.length) {
    var instance = Klass.instancePool.pop();
    Klass.call(instance, a1, a2, a3, a4, a5);
    return instance;
  } else {
    return new Klass(a1, a2, a3, a4, a5);
  }
};

var standardReleaser = function (instance) {
  var Klass = this;
  !(instance instanceof Klass) ? "production" !== 'production' ? invariant(false, 'Trying to release an instance into a pool of a different type.') : invariant(false) : undefined;
  instance.destructor();
  if (Klass.instancePool.length < Klass.poolSize) {
    Klass.instancePool.push(instance);
  }
};

var DEFAULT_POOL_SIZE = 10;
var DEFAULT_POOLER = oneArgumentPooler;

/**
 * Augments `CopyConstructor` to be a poolable class, augmenting only the class
 * itself (statically) not adding any prototypical fields. Any CopyConstructor
 * you give this may have a `poolSize` property, and will look for a
 * prototypical `destructor` on instances (optional).
 *
 * @param {Function} CopyConstructor Constructor that can be used to reset.
 * @param {Function} pooler Customizable pooler.
 */
var addPoolingTo = function (CopyConstructor, pooler) {
  var NewKlass = CopyConstructor;
  NewKlass.instancePool = [];
  NewKlass.getPooled = pooler || DEFAULT_POOLER;
  if (!NewKlass.poolSize) {
    NewKlass.poolSize = DEFAULT_POOL_SIZE;
  }
  NewKlass.release = standardReleaser;
  return NewKlass;
};

var PooledClass = {
  addPoolingTo: addPoolingTo,
  oneArgumentPooler: oneArgumentPooler,
  twoArgumentPooler: twoArgumentPooler,
  threeArgumentPooler: threeArgumentPooler,
  fourArgumentPooler: fourArgumentPooler,
  fiveArgumentPooler: fiveArgumentPooler
};

module.exports = PooledClass;
},{"fbjs/lib/invariant":151}],251:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule React
 */

'use strict';

var ReactDOM = require('./ReactDOM');
var ReactDOMServer = require('./ReactDOMServer');
var ReactIsomorphic = require('./ReactIsomorphic');

var assign = require('./Object.assign');
var deprecated = require('./deprecated');

// `version` will be added here by ReactIsomorphic.
var React = {};

assign(React, ReactIsomorphic);

assign(React, {
  // ReactDOM
  findDOMNode: deprecated('findDOMNode', 'ReactDOM', 'react-dom', ReactDOM, ReactDOM.findDOMNode),
  render: deprecated('render', 'ReactDOM', 'react-dom', ReactDOM, ReactDOM.render),
  unmountComponentAtNode: deprecated('unmountComponentAtNode', 'ReactDOM', 'react-dom', ReactDOM, ReactDOM.unmountComponentAtNode),

  // ReactDOMServer
  renderToString: deprecated('renderToString', 'ReactDOMServer', 'react-dom/server', ReactDOMServer, ReactDOMServer.renderToString),
  renderToStaticMarkup: deprecated('renderToStaticMarkup', 'ReactDOMServer', 'react-dom/server', ReactDOMServer, ReactDOMServer.renderToStaticMarkup)
});

React.__SECRET_DOM_DO_NOT_USE_OR_YOU_WILL_BE_FIRED = ReactDOM;
React.__SECRET_DOM_SERVER_DO_NOT_USE_OR_YOU_WILL_BE_FIRED = ReactDOMServer;

module.exports = React;
},{"./Object.assign":249,"./ReactDOM":262,"./ReactDOMServer":272,"./ReactIsomorphic":290,"./deprecated":333}],252:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactBrowserComponentMixin
 */

'use strict';

var ReactInstanceMap = require('./ReactInstanceMap');

var findDOMNode = require('./findDOMNode');
var warning = require('fbjs/lib/warning');

var didWarnKey = '_getDOMNodeDidWarn';

var ReactBrowserComponentMixin = {
  /**
   * Returns the DOM node rendered by this component.
   *
   * @return {DOMElement} The root node of this component.
   * @final
   * @protected
   */
  getDOMNode: function () {
    "production" !== 'production' ? warning(this.constructor[didWarnKey], '%s.getDOMNode(...) is deprecated. Please use ' + 'ReactDOM.findDOMNode(instance) instead.', ReactInstanceMap.get(this).getName() || this.tagName || 'Unknown') : undefined;
    this.constructor[didWarnKey] = true;
    return findDOMNode(this);
  }
};

module.exports = ReactBrowserComponentMixin;
},{"./ReactInstanceMap":289,"./findDOMNode":335,"fbjs/lib/warning":162}],253:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactBrowserEventEmitter
 * @typechecks static-only
 */

'use strict';

var EventConstants = require('./EventConstants');
var EventPluginHub = require('./EventPluginHub');
var EventPluginRegistry = require('./EventPluginRegistry');
var ReactEventEmitterMixin = require('./ReactEventEmitterMixin');
var ReactPerf = require('./ReactPerf');
var ViewportMetrics = require('./ViewportMetrics');

var assign = require('./Object.assign');
var isEventSupported = require('./isEventSupported');

/**
 * Summary of `ReactBrowserEventEmitter` event handling:
 *
 *  - Top-level delegation is used to trap most native browser events. This
 *    may only occur in the main thread and is the responsibility of
 *    ReactEventListener, which is injected and can therefore support pluggable
 *    event sources. This is the only work that occurs in the main thread.
 *
 *  - We normalize and de-duplicate events to account for browser quirks. This
 *    may be done in the worker thread.
 *
 *  - Forward these native events (with the associated top-level type used to
 *    trap it) to `EventPluginHub`, which in turn will ask plugins if they want
 *    to extract any synthetic events.
 *
 *  - The `EventPluginHub` will then process each event by annotating them with
 *    "dispatches", a sequence of listeners and IDs that care about that event.
 *
 *  - The `EventPluginHub` then dispatches the events.
 *
 * Overview of React and the event system:
 *
 * +------------+    .
 * |    DOM     |    .
 * +------------+    .
 *       |           .
 *       v           .
 * +------------+    .
 * | ReactEvent |    .
 * |  Listener  |    .
 * +------------+    .                         +-----------+
 *       |           .               +--------+|SimpleEvent|
 *       |           .               |         |Plugin     |
 * +-----|------+    .               v         +-----------+
 * |     |      |    .    +--------------+                    +------------+
 * |     +-----------.--->|EventPluginHub|                    |    Event   |
 * |            |    .    |              |     +-----------+  | Propagators|
 * | ReactEvent |    .    |              |     |TapEvent   |  |------------|
 * |  Emitter   |    .    |              |<---+|Plugin     |  |other plugin|
 * |            |    .    |              |     +-----------+  |  utilities |
 * |     +-----------.--->|              |                    +------------+
 * |     |      |    .    +--------------+
 * +-----|------+    .                ^        +-----------+
 *       |           .                |        |Enter/Leave|
 *       +           .                +-------+|Plugin     |
 * +-------------+   .                         +-----------+
 * | application |   .
 * |-------------|   .
 * |             |   .
 * |             |   .
 * +-------------+   .
 *                   .
 *    React Core     .  General Purpose Event Plugin System
 */

var alreadyListeningTo = {};
var isMonitoringScrollValue = false;
var reactTopListenersCounter = 0;

// For events like 'submit' which don't consistently bubble (which we trap at a
// lower node than `document`), binding at `document` would cause duplicate
// events so we don't include them here
var topEventMapping = {
  topAbort: 'abort',
  topBlur: 'blur',
  topCanPlay: 'canplay',
  topCanPlayThrough: 'canplaythrough',
  topChange: 'change',
  topClick: 'click',
  topCompositionEnd: 'compositionend',
  topCompositionStart: 'compositionstart',
  topCompositionUpdate: 'compositionupdate',
  topContextMenu: 'contextmenu',
  topCopy: 'copy',
  topCut: 'cut',
  topDoubleClick: 'dblclick',
  topDrag: 'drag',
  topDragEnd: 'dragend',
  topDragEnter: 'dragenter',
  topDragExit: 'dragexit',
  topDragLeave: 'dragleave',
  topDragOver: 'dragover',
  topDragStart: 'dragstart',
  topDrop: 'drop',
  topDurationChange: 'durationchange',
  topEmptied: 'emptied',
  topEncrypted: 'encrypted',
  topEnded: 'ended',
  topError: 'error',
  topFocus: 'focus',
  topInput: 'input',
  topKeyDown: 'keydown',
  topKeyPress: 'keypress',
  topKeyUp: 'keyup',
  topLoadedData: 'loadeddata',
  topLoadedMetadata: 'loadedmetadata',
  topLoadStart: 'loadstart',
  topMouseDown: 'mousedown',
  topMouseMove: 'mousemove',
  topMouseOut: 'mouseout',
  topMouseOver: 'mouseover',
  topMouseUp: 'mouseup',
  topPaste: 'paste',
  topPause: 'pause',
  topPlay: 'play',
  topPlaying: 'playing',
  topProgress: 'progress',
  topRateChange: 'ratechange',
  topScroll: 'scroll',
  topSeeked: 'seeked',
  topSeeking: 'seeking',
  topSelectionChange: 'selectionchange',
  topStalled: 'stalled',
  topSuspend: 'suspend',
  topTextInput: 'textInput',
  topTimeUpdate: 'timeupdate',
  topTouchCancel: 'touchcancel',
  topTouchEnd: 'touchend',
  topTouchMove: 'touchmove',
  topTouchStart: 'touchstart',
  topVolumeChange: 'volumechange',
  topWaiting: 'waiting',
  topWheel: 'wheel'
};

/**
 * To ensure no conflicts with other potential React instances on the page
 */
var topListenersIDKey = '_reactListenersID' + String(Math.random()).slice(2);

function getListeningForDocument(mountAt) {
  // In IE8, `mountAt` is a host object and doesn't have `hasOwnProperty`
  // directly.
  if (!Object.prototype.hasOwnProperty.call(mountAt, topListenersIDKey)) {
    mountAt[topListenersIDKey] = reactTopListenersCounter++;
    alreadyListeningTo[mountAt[topListenersIDKey]] = {};
  }
  return alreadyListeningTo[mountAt[topListenersIDKey]];
}

/**
 * `ReactBrowserEventEmitter` is used to attach top-level event listeners. For
 * example:
 *
 *   ReactBrowserEventEmitter.putListener('myID', 'onClick', myFunction);
 *
 * This would allocate a "registration" of `('onClick', myFunction)` on 'myID'.
 *
 * @internal
 */
var ReactBrowserEventEmitter = assign({}, ReactEventEmitterMixin, {

  /**
   * Injectable event backend
   */
  ReactEventListener: null,

  injection: {
    /**
     * @param {object} ReactEventListener
     */
    injectReactEventListener: function (ReactEventListener) {
      ReactEventListener.setHandleTopLevel(ReactBrowserEventEmitter.handleTopLevel);
      ReactBrowserEventEmitter.ReactEventListener = ReactEventListener;
    }
  },

  /**
   * Sets whether or not any created callbacks should be enabled.
   *
   * @param {boolean} enabled True if callbacks should be enabled.
   */
  setEnabled: function (enabled) {
    if (ReactBrowserEventEmitter.ReactEventListener) {
      ReactBrowserEventEmitter.ReactEventListener.setEnabled(enabled);
    }
  },

  /**
   * @return {boolean} True if callbacks are enabled.
   */
  isEnabled: function () {
    return !!(ReactBrowserEventEmitter.ReactEventListener && ReactBrowserEventEmitter.ReactEventListener.isEnabled());
  },

  /**
   * We listen for bubbled touch events on the document object.
   *
   * Firefox v8.01 (and possibly others) exhibited strange behavior when
   * mounting `onmousemove` events at some node that was not the document
   * element. The symptoms were that if your mouse is not moving over something
   * contained within that mount point (for example on the background) the
   * top-level listeners for `onmousemove` won't be called. However, if you
   * register the `mousemove` on the document object, then it will of course
   * catch all `mousemove`s. This along with iOS quirks, justifies restricting
   * top-level listeners to the document object only, at least for these
   * movement types of events and possibly all events.
   *
   * @see http://www.quirksmode.org/blog/archives/2010/09/click_event_del.html
   *
   * Also, `keyup`/`keypress`/`keydown` do not bubble to the window on IE, but
   * they bubble to document.
   *
   * @param {string} registrationName Name of listener (e.g. `onClick`).
   * @param {object} contentDocumentHandle Document which owns the container
   */
  listenTo: function (registrationName, contentDocumentHandle) {
    var mountAt = contentDocumentHandle;
    var isListening = getListeningForDocument(mountAt);
    var dependencies = EventPluginRegistry.registrationNameDependencies[registrationName];

    var topLevelTypes = EventConstants.topLevelTypes;
    for (var i = 0; i < dependencies.length; i++) {
      var dependency = dependencies[i];
      if (!(isListening.hasOwnProperty(dependency) && isListening[dependency])) {
        if (dependency === topLevelTypes.topWheel) {
          if (isEventSupported('wheel')) {
            ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(topLevelTypes.topWheel, 'wheel', mountAt);
          } else if (isEventSupported('mousewheel')) {
            ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(topLevelTypes.topWheel, 'mousewheel', mountAt);
          } else {
            // Firefox needs to capture a different mouse scroll event.
            // @see http://www.quirksmode.org/dom/events/tests/scroll.html
            ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(topLevelTypes.topWheel, 'DOMMouseScroll', mountAt);
          }
        } else if (dependency === topLevelTypes.topScroll) {

          if (isEventSupported('scroll', true)) {
            ReactBrowserEventEmitter.ReactEventListener.trapCapturedEvent(topLevelTypes.topScroll, 'scroll', mountAt);
          } else {
            ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(topLevelTypes.topScroll, 'scroll', ReactBrowserEventEmitter.ReactEventListener.WINDOW_HANDLE);
          }
        } else if (dependency === topLevelTypes.topFocus || dependency === topLevelTypes.topBlur) {

          if (isEventSupported('focus', true)) {
            ReactBrowserEventEmitter.ReactEventListener.trapCapturedEvent(topLevelTypes.topFocus, 'focus', mountAt);
            ReactBrowserEventEmitter.ReactEventListener.trapCapturedEvent(topLevelTypes.topBlur, 'blur', mountAt);
          } else if (isEventSupported('focusin')) {
            // IE has `focusin` and `focusout` events which bubble.
            // @see http://www.quirksmode.org/blog/archives/2008/04/delegating_the.html
            ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(topLevelTypes.topFocus, 'focusin', mountAt);
            ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(topLevelTypes.topBlur, 'focusout', mountAt);
          }

          // to make sure blur and focus event listeners are only attached once
          isListening[topLevelTypes.topBlur] = true;
          isListening[topLevelTypes.topFocus] = true;
        } else if (topEventMapping.hasOwnProperty(dependency)) {
          ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(dependency, topEventMapping[dependency], mountAt);
        }

        isListening[dependency] = true;
      }
    }
  },

  trapBubbledEvent: function (topLevelType, handlerBaseName, handle) {
    return ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(topLevelType, handlerBaseName, handle);
  },

  trapCapturedEvent: function (topLevelType, handlerBaseName, handle) {
    return ReactBrowserEventEmitter.ReactEventListener.trapCapturedEvent(topLevelType, handlerBaseName, handle);
  },

  /**
   * Listens to window scroll and resize events. We cache scroll values so that
   * application code can access them without triggering reflows.
   *
   * NOTE: Scroll events do not bubble.
   *
   * @see http://www.quirksmode.org/dom/events/scroll.html
   */
  ensureScrollValueMonitoring: function () {
    if (!isMonitoringScrollValue) {
      var refresh = ViewportMetrics.refreshScrollValues;
      ReactBrowserEventEmitter.ReactEventListener.monitorScrollValue(refresh);
      isMonitoringScrollValue = true;
    }
  },

  eventNameDispatchConfigs: EventPluginHub.eventNameDispatchConfigs,

  registrationNameModules: EventPluginHub.registrationNameModules,

  putListener: EventPluginHub.putListener,

  getListener: EventPluginHub.getListener,

  deleteListener: EventPluginHub.deleteListener,

  deleteAllListeners: EventPluginHub.deleteAllListeners

});

ReactPerf.measureMethods(ReactBrowserEventEmitter, 'ReactBrowserEventEmitter', {
  putListener: 'putListener',
  deleteListener: 'deleteListener'
});

module.exports = ReactBrowserEventEmitter;
},{"./EventConstants":241,"./EventPluginHub":242,"./EventPluginRegistry":243,"./Object.assign":249,"./ReactEventEmitterMixin":284,"./ReactPerf":298,"./ViewportMetrics":328,"./isEventSupported":346}],254:[function(require,module,exports){
/**
 * Copyright 2014-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactChildReconciler
 * @typechecks static-only
 */

'use strict';

var ReactReconciler = require('./ReactReconciler');

var instantiateReactComponent = require('./instantiateReactComponent');
var shouldUpdateReactComponent = require('./shouldUpdateReactComponent');
var traverseAllChildren = require('./traverseAllChildren');
var warning = require('fbjs/lib/warning');

function instantiateChild(childInstances, child, name) {
  // We found a component instance.
  var keyUnique = childInstances[name] === undefined;
  if ("production" !== 'production') {
    "production" !== 'production' ? warning(keyUnique, 'flattenChildren(...): Encountered two children with the same key, ' + '`%s`. Child keys must be unique; when two children share a key, only ' + 'the first child will be used.', name) : undefined;
  }
  if (child != null && keyUnique) {
    childInstances[name] = instantiateReactComponent(child, null);
  }
}

/**
 * ReactChildReconciler provides helpers for initializing or updating a set of
 * children. Its output is suitable for passing it onto ReactMultiChild which
 * does diffed reordering and insertion.
 */
var ReactChildReconciler = {
  /**
   * Generates a "mount image" for each of the supplied children. In the case
   * of `ReactDOMComponent`, a mount image is a string of markup.
   *
   * @param {?object} nestedChildNodes Nested child maps.
   * @return {?object} A set of child instances.
   * @internal
   */
  instantiateChildren: function (nestedChildNodes, transaction, context) {
    if (nestedChildNodes == null) {
      return null;
    }
    var childInstances = {};
    traverseAllChildren(nestedChildNodes, instantiateChild, childInstances);
    return childInstances;
  },

  /**
   * Updates the rendered children and returns a new set of children.
   *
   * @param {?object} prevChildren Previously initialized set of children.
   * @param {?object} nextChildren Flat child element maps.
   * @param {ReactReconcileTransaction} transaction
   * @param {object} context
   * @return {?object} A new set of child instances.
   * @internal
   */
  updateChildren: function (prevChildren, nextChildren, transaction, context) {
    // We currently don't have a way to track moves here but if we use iterators
    // instead of for..in we can zip the iterators and check if an item has
    // moved.
    // TODO: If nothing has changed, return the prevChildren object so that we
    // can quickly bailout if nothing has changed.
    if (!nextChildren && !prevChildren) {
      return null;
    }
    var name;
    for (name in nextChildren) {
      if (!nextChildren.hasOwnProperty(name)) {
        continue;
      }
      var prevChild = prevChildren && prevChildren[name];
      var prevElement = prevChild && prevChild._currentElement;
      var nextElement = nextChildren[name];
      if (prevChild != null && shouldUpdateReactComponent(prevElement, nextElement)) {
        ReactReconciler.receiveComponent(prevChild, nextElement, transaction, context);
        nextChildren[name] = prevChild;
      } else {
        if (prevChild) {
          ReactReconciler.unmountComponent(prevChild, name);
        }
        // The child must be instantiated before it's mounted.
        var nextChildInstance = instantiateReactComponent(nextElement, null);
        nextChildren[name] = nextChildInstance;
      }
    }
    // Unmount children that are no longer present.
    for (name in prevChildren) {
      if (prevChildren.hasOwnProperty(name) && !(nextChildren && nextChildren.hasOwnProperty(name))) {
        ReactReconciler.unmountComponent(prevChildren[name]);
      }
    }
    return nextChildren;
  },

  /**
   * Unmounts all rendered children. This should be used to clean up children
   * when this component is unmounted.
   *
   * @param {?object} renderedChildren Previously initialized set of children.
   * @internal
   */
  unmountChildren: function (renderedChildren) {
    for (var name in renderedChildren) {
      if (renderedChildren.hasOwnProperty(name)) {
        var renderedChild = renderedChildren[name];
        ReactReconciler.unmountComponent(renderedChild);
      }
    }
  }

};

module.exports = ReactChildReconciler;
},{"./ReactReconciler":303,"./instantiateReactComponent":345,"./shouldUpdateReactComponent":353,"./traverseAllChildren":354,"fbjs/lib/warning":162}],255:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactChildren
 */

'use strict';

var PooledClass = require('./PooledClass');
var ReactElement = require('./ReactElement');

var emptyFunction = require('fbjs/lib/emptyFunction');
var traverseAllChildren = require('./traverseAllChildren');

var twoArgumentPooler = PooledClass.twoArgumentPooler;
var fourArgumentPooler = PooledClass.fourArgumentPooler;

var userProvidedKeyEscapeRegex = /\/(?!\/)/g;
function escapeUserProvidedKey(text) {
  return ('' + text).replace(userProvidedKeyEscapeRegex, '//');
}

/**
 * PooledClass representing the bookkeeping associated with performing a child
 * traversal. Allows avoiding binding callbacks.
 *
 * @constructor ForEachBookKeeping
 * @param {!function} forEachFunction Function to perform traversal with.
 * @param {?*} forEachContext Context to perform context with.
 */
function ForEachBookKeeping(forEachFunction, forEachContext) {
  this.func = forEachFunction;
  this.context = forEachContext;
  this.count = 0;
}
ForEachBookKeeping.prototype.destructor = function () {
  this.func = null;
  this.context = null;
  this.count = 0;
};
PooledClass.addPoolingTo(ForEachBookKeeping, twoArgumentPooler);

function forEachSingleChild(bookKeeping, child, name) {
  var func = bookKeeping.func;
  var context = bookKeeping.context;

  func.call(context, child, bookKeeping.count++);
}

/**
 * Iterates through children that are typically specified as `props.children`.
 *
 * The provided forEachFunc(child, index) will be called for each
 * leaf child.
 *
 * @param {?*} children Children tree container.
 * @param {function(*, int)} forEachFunc
 * @param {*} forEachContext Context for forEachContext.
 */
function forEachChildren(children, forEachFunc, forEachContext) {
  if (children == null) {
    return children;
  }
  var traverseContext = ForEachBookKeeping.getPooled(forEachFunc, forEachContext);
  traverseAllChildren(children, forEachSingleChild, traverseContext);
  ForEachBookKeeping.release(traverseContext);
}

/**
 * PooledClass representing the bookkeeping associated with performing a child
 * mapping. Allows avoiding binding callbacks.
 *
 * @constructor MapBookKeeping
 * @param {!*} mapResult Object containing the ordered map of results.
 * @param {!function} mapFunction Function to perform mapping with.
 * @param {?*} mapContext Context to perform mapping with.
 */
function MapBookKeeping(mapResult, keyPrefix, mapFunction, mapContext) {
  this.result = mapResult;
  this.keyPrefix = keyPrefix;
  this.func = mapFunction;
  this.context = mapContext;
  this.count = 0;
}
MapBookKeeping.prototype.destructor = function () {
  this.result = null;
  this.keyPrefix = null;
  this.func = null;
  this.context = null;
  this.count = 0;
};
PooledClass.addPoolingTo(MapBookKeeping, fourArgumentPooler);

function mapSingleChildIntoContext(bookKeeping, child, childKey) {
  var result = bookKeeping.result;
  var keyPrefix = bookKeeping.keyPrefix;
  var func = bookKeeping.func;
  var context = bookKeeping.context;

  var mappedChild = func.call(context, child, bookKeeping.count++);
  if (Array.isArray(mappedChild)) {
    mapIntoWithKeyPrefixInternal(mappedChild, result, childKey, emptyFunction.thatReturnsArgument);
  } else if (mappedChild != null) {
    if (ReactElement.isValidElement(mappedChild)) {
      mappedChild = ReactElement.cloneAndReplaceKey(mappedChild,
      // Keep both the (mapped) and old keys if they differ, just as
      // traverseAllChildren used to do for objects as children
      keyPrefix + (mappedChild !== child ? escapeUserProvidedKey(mappedChild.key || '') + '/' : '') + childKey);
    }
    result.push(mappedChild);
  }
}

function mapIntoWithKeyPrefixInternal(children, array, prefix, func, context) {
  var escapedPrefix = '';
  if (prefix != null) {
    escapedPrefix = escapeUserProvidedKey(prefix) + '/';
  }
  var traverseContext = MapBookKeeping.getPooled(array, escapedPrefix, func, context);
  traverseAllChildren(children, mapSingleChildIntoContext, traverseContext);
  MapBookKeeping.release(traverseContext);
}

/**
 * Maps children that are typically specified as `props.children`.
 *
 * The provided mapFunction(child, key, index) will be called for each
 * leaf child.
 *
 * @param {?*} children Children tree container.
 * @param {function(*, int)} func The map function.
 * @param {*} context Context for mapFunction.
 * @return {object} Object containing the ordered map of results.
 */
function mapChildren(children, func, context) {
  if (children == null) {
    return children;
  }
  var result = [];
  mapIntoWithKeyPrefixInternal(children, result, null, func, context);
  return result;
}

function forEachSingleChildDummy(traverseContext, child, name) {
  return null;
}

/**
 * Count the number of children that are typically specified as
 * `props.children`.
 *
 * @param {?*} children Children tree container.
 * @return {number} The number of children.
 */
function countChildren(children, context) {
  return traverseAllChildren(children, forEachSingleChildDummy, null);
}

/**
 * Flatten a children object (typically specified as `props.children`) and
 * return an array with appropriately re-keyed children.
 */
function toArray(children) {
  var result = [];
  mapIntoWithKeyPrefixInternal(children, result, null, emptyFunction.thatReturnsArgument);
  return result;
}

var ReactChildren = {
  forEach: forEachChildren,
  map: mapChildren,
  mapIntoWithKeyPrefixInternal: mapIntoWithKeyPrefixInternal,
  count: countChildren,
  toArray: toArray
};

module.exports = ReactChildren;
},{"./PooledClass":250,"./ReactElement":279,"./traverseAllChildren":354,"fbjs/lib/emptyFunction":143}],256:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactClass
 */

'use strict';

var ReactComponent = require('./ReactComponent');
var ReactElement = require('./ReactElement');
var ReactPropTypeLocations = require('./ReactPropTypeLocations');
var ReactPropTypeLocationNames = require('./ReactPropTypeLocationNames');
var ReactNoopUpdateQueue = require('./ReactNoopUpdateQueue');

var assign = require('./Object.assign');
var emptyObject = require('fbjs/lib/emptyObject');
var invariant = require('fbjs/lib/invariant');
var keyMirror = require('fbjs/lib/keyMirror');
var keyOf = require('fbjs/lib/keyOf');
var warning = require('fbjs/lib/warning');

var MIXINS_KEY = keyOf({ mixins: null });

/**
 * Policies that describe methods in `ReactClassInterface`.
 */
var SpecPolicy = keyMirror({
  /**
   * These methods may be defined only once by the class specification or mixin.
   */
  DEFINE_ONCE: null,
  /**
   * These methods may be defined by both the class specification and mixins.
   * Subsequent definitions will be chained. These methods must return void.
   */
  DEFINE_MANY: null,
  /**
   * These methods are overriding the base class.
   */
  OVERRIDE_BASE: null,
  /**
   * These methods are similar to DEFINE_MANY, except we assume they return
   * objects. We try to merge the keys of the return values of all the mixed in
   * functions. If there is a key conflict we throw.
   */
  DEFINE_MANY_MERGED: null
});

var injectedMixins = [];

var warnedSetProps = false;
function warnSetProps() {
  if (!warnedSetProps) {
    warnedSetProps = true;
    "production" !== 'production' ? warning(false, 'setProps(...) and replaceProps(...) are deprecated. ' + 'Instead, call render again at the top level.') : undefined;
  }
}

/**
 * Composite components are higher-level components that compose other composite
 * or native components.
 *
 * To create a new type of `ReactClass`, pass a specification of
 * your new class to `React.createClass`. The only requirement of your class
 * specification is that you implement a `render` method.
 *
 *   var MyComponent = React.createClass({
 *     render: function() {
 *       return <div>Hello World</div>;
 *     }
 *   });
 *
 * The class specification supports a specific protocol of methods that have
 * special meaning (e.g. `render`). See `ReactClassInterface` for
 * more the comprehensive protocol. Any other properties and methods in the
 * class specification will be available on the prototype.
 *
 * @interface ReactClassInterface
 * @internal
 */
var ReactClassInterface = {

  /**
   * An array of Mixin objects to include when defining your component.
   *
   * @type {array}
   * @optional
   */
  mixins: SpecPolicy.DEFINE_MANY,

  /**
   * An object containing properties and methods that should be defined on
   * the component's constructor instead of its prototype (static methods).
   *
   * @type {object}
   * @optional
   */
  statics: SpecPolicy.DEFINE_MANY,

  /**
   * Definition of prop types for this component.
   *
   * @type {object}
   * @optional
   */
  propTypes: SpecPolicy.DEFINE_MANY,

  /**
   * Definition of context types for this component.
   *
   * @type {object}
   * @optional
   */
  contextTypes: SpecPolicy.DEFINE_MANY,

  /**
   * Definition of context types this component sets for its children.
   *
   * @type {object}
   * @optional
   */
  childContextTypes: SpecPolicy.DEFINE_MANY,

  // ==== Definition methods ====

  /**
   * Invoked when the component is mounted. Values in the mapping will be set on
   * `this.props` if that prop is not specified (i.e. using an `in` check).
   *
   * This method is invoked before `getInitialState` and therefore cannot rely
   * on `this.state` or use `this.setState`.
   *
   * @return {object}
   * @optional
   */
  getDefaultProps: SpecPolicy.DEFINE_MANY_MERGED,

  /**
   * Invoked once before the component is mounted. The return value will be used
   * as the initial value of `this.state`.
   *
   *   getInitialState: function() {
   *     return {
   *       isOn: false,
   *       fooBaz: new BazFoo()
   *     }
   *   }
   *
   * @return {object}
   * @optional
   */
  getInitialState: SpecPolicy.DEFINE_MANY_MERGED,

  /**
   * @return {object}
   * @optional
   */
  getChildContext: SpecPolicy.DEFINE_MANY_MERGED,

  /**
   * Uses props from `this.props` and state from `this.state` to render the
   * structure of the component.
   *
   * No guarantees are made about when or how often this method is invoked, so
   * it must not have side effects.
   *
   *   render: function() {
   *     var name = this.props.name;
   *     return <div>Hello, {name}!</div>;
   *   }
   *
   * @return {ReactComponent}
   * @nosideeffects
   * @required
   */
  render: SpecPolicy.DEFINE_ONCE,

  // ==== Delegate methods ====

  /**
   * Invoked when the component is initially created and about to be mounted.
   * This may have side effects, but any external subscriptions or data created
   * by this method must be cleaned up in `componentWillUnmount`.
   *
   * @optional
   */
  componentWillMount: SpecPolicy.DEFINE_MANY,

  /**
   * Invoked when the component has been mounted and has a DOM representation.
   * However, there is no guarantee that the DOM node is in the document.
   *
   * Use this as an opportunity to operate on the DOM when the component has
   * been mounted (initialized and rendered) for the first time.
   *
   * @param {DOMElement} rootNode DOM element representing the component.
   * @optional
   */
  componentDidMount: SpecPolicy.DEFINE_MANY,

  /**
   * Invoked before the component receives new props.
   *
   * Use this as an opportunity to react to a prop transition by updating the
   * state using `this.setState`. Current props are accessed via `this.props`.
   *
   *   componentWillReceiveProps: function(nextProps, nextContext) {
   *     this.setState({
   *       likesIncreasing: nextProps.likeCount > this.props.likeCount
   *     });
   *   }
   *
   * NOTE: There is no equivalent `componentWillReceiveState`. An incoming prop
   * transition may cause a state change, but the opposite is not true. If you
   * need it, you are probably looking for `componentWillUpdate`.
   *
   * @param {object} nextProps
   * @optional
   */
  componentWillReceiveProps: SpecPolicy.DEFINE_MANY,

  /**
   * Invoked while deciding if the component should be updated as a result of
   * receiving new props, state and/or context.
   *
   * Use this as an opportunity to `return false` when you're certain that the
   * transition to the new props/state/context will not require a component
   * update.
   *
   *   shouldComponentUpdate: function(nextProps, nextState, nextContext) {
   *     return !equal(nextProps, this.props) ||
   *       !equal(nextState, this.state) ||
   *       !equal(nextContext, this.context);
   *   }
   *
   * @param {object} nextProps
   * @param {?object} nextState
   * @param {?object} nextContext
   * @return {boolean} True if the component should update.
   * @optional
   */
  shouldComponentUpdate: SpecPolicy.DEFINE_ONCE,

  /**
   * Invoked when the component is about to update due to a transition from
   * `this.props`, `this.state` and `this.context` to `nextProps`, `nextState`
   * and `nextContext`.
   *
   * Use this as an opportunity to perform preparation before an update occurs.
   *
   * NOTE: You **cannot** use `this.setState()` in this method.
   *
   * @param {object} nextProps
   * @param {?object} nextState
   * @param {?object} nextContext
   * @param {ReactReconcileTransaction} transaction
   * @optional
   */
  componentWillUpdate: SpecPolicy.DEFINE_MANY,

  /**
   * Invoked when the component's DOM representation has been updated.
   *
   * Use this as an opportunity to operate on the DOM when the component has
   * been updated.
   *
   * @param {object} prevProps
   * @param {?object} prevState
   * @param {?object} prevContext
   * @param {DOMElement} rootNode DOM element representing the component.
   * @optional
   */
  componentDidUpdate: SpecPolicy.DEFINE_MANY,

  /**
   * Invoked when the component is about to be removed from its parent and have
   * its DOM representation destroyed.
   *
   * Use this as an opportunity to deallocate any external resources.
   *
   * NOTE: There is no `componentDidUnmount` since your component will have been
   * destroyed by that point.
   *
   * @optional
   */
  componentWillUnmount: SpecPolicy.DEFINE_MANY,

  // ==== Advanced methods ====

  /**
   * Updates the component's currently mounted DOM representation.
   *
   * By default, this implements React's rendering and reconciliation algorithm.
   * Sophisticated clients may wish to override this.
   *
   * @param {ReactReconcileTransaction} transaction
   * @internal
   * @overridable
   */
  updateComponent: SpecPolicy.OVERRIDE_BASE

};

/**
 * Mapping from class specification keys to special processing functions.
 *
 * Although these are declared like instance properties in the specification
 * when defining classes using `React.createClass`, they are actually static
 * and are accessible on the constructor instead of the prototype. Despite
 * being static, they must be defined outside of the "statics" key under
 * which all other static methods are defined.
 */
var RESERVED_SPEC_KEYS = {
  displayName: function (Constructor, displayName) {
    Constructor.displayName = displayName;
  },
  mixins: function (Constructor, mixins) {
    if (mixins) {
      for (var i = 0; i < mixins.length; i++) {
        mixSpecIntoComponent(Constructor, mixins[i]);
      }
    }
  },
  childContextTypes: function (Constructor, childContextTypes) {
    if ("production" !== 'production') {
      validateTypeDef(Constructor, childContextTypes, ReactPropTypeLocations.childContext);
    }
    Constructor.childContextTypes = assign({}, Constructor.childContextTypes, childContextTypes);
  },
  contextTypes: function (Constructor, contextTypes) {
    if ("production" !== 'production') {
      validateTypeDef(Constructor, contextTypes, ReactPropTypeLocations.context);
    }
    Constructor.contextTypes = assign({}, Constructor.contextTypes, contextTypes);
  },
  /**
   * Special case getDefaultProps which should move into statics but requires
   * automatic merging.
   */
  getDefaultProps: function (Constructor, getDefaultProps) {
    if (Constructor.getDefaultProps) {
      Constructor.getDefaultProps = createMergedResultFunction(Constructor.getDefaultProps, getDefaultProps);
    } else {
      Constructor.getDefaultProps = getDefaultProps;
    }
  },
  propTypes: function (Constructor, propTypes) {
    if ("production" !== 'production') {
      validateTypeDef(Constructor, propTypes, ReactPropTypeLocations.prop);
    }
    Constructor.propTypes = assign({}, Constructor.propTypes, propTypes);
  },
  statics: function (Constructor, statics) {
    mixStaticSpecIntoComponent(Constructor, statics);
  },
  autobind: function () {} };

// noop
function validateTypeDef(Constructor, typeDef, location) {
  for (var propName in typeDef) {
    if (typeDef.hasOwnProperty(propName)) {
      // use a warning instead of an invariant so components
      // don't show up in prod but not in __DEV__
      "production" !== 'production' ? warning(typeof typeDef[propName] === 'function', '%s: %s type `%s` is invalid; it must be a function, usually from ' + 'React.PropTypes.', Constructor.displayName || 'ReactClass', ReactPropTypeLocationNames[location], propName) : undefined;
    }
  }
}

function validateMethodOverride(proto, name) {
  var specPolicy = ReactClassInterface.hasOwnProperty(name) ? ReactClassInterface[name] : null;

  // Disallow overriding of base class methods unless explicitly allowed.
  if (ReactClassMixin.hasOwnProperty(name)) {
    !(specPolicy === SpecPolicy.OVERRIDE_BASE) ? "production" !== 'production' ? invariant(false, 'ReactClassInterface: You are attempting to override ' + '`%s` from your class specification. Ensure that your method names ' + 'do not overlap with React methods.', name) : invariant(false) : undefined;
  }

  // Disallow defining methods more than once unless explicitly allowed.
  if (proto.hasOwnProperty(name)) {
    !(specPolicy === SpecPolicy.DEFINE_MANY || specPolicy === SpecPolicy.DEFINE_MANY_MERGED) ? "production" !== 'production' ? invariant(false, 'ReactClassInterface: You are attempting to define ' + '`%s` on your component more than once. This conflict may be due ' + 'to a mixin.', name) : invariant(false) : undefined;
  }
}

/**
 * Mixin helper which handles policy validation and reserved
 * specification keys when building React classses.
 */
function mixSpecIntoComponent(Constructor, spec) {
  if (!spec) {
    return;
  }

  !(typeof spec !== 'function') ? "production" !== 'production' ? invariant(false, 'ReactClass: You\'re attempting to ' + 'use a component class as a mixin. Instead, just use a regular object.') : invariant(false) : undefined;
  !!ReactElement.isValidElement(spec) ? "production" !== 'production' ? invariant(false, 'ReactClass: You\'re attempting to ' + 'use a component as a mixin. Instead, just use a regular object.') : invariant(false) : undefined;

  var proto = Constructor.prototype;

  // By handling mixins before any other properties, we ensure the same
  // chaining order is applied to methods with DEFINE_MANY policy, whether
  // mixins are listed before or after these methods in the spec.
  if (spec.hasOwnProperty(MIXINS_KEY)) {
    RESERVED_SPEC_KEYS.mixins(Constructor, spec.mixins);
  }

  for (var name in spec) {
    if (!spec.hasOwnProperty(name)) {
      continue;
    }

    if (name === MIXINS_KEY) {
      // We have already handled mixins in a special case above.
      continue;
    }

    var property = spec[name];
    validateMethodOverride(proto, name);

    if (RESERVED_SPEC_KEYS.hasOwnProperty(name)) {
      RESERVED_SPEC_KEYS[name](Constructor, property);
    } else {
      // Setup methods on prototype:
      // The following member methods should not be automatically bound:
      // 1. Expected ReactClass methods (in the "interface").
      // 2. Overridden methods (that were mixed in).
      var isReactClassMethod = ReactClassInterface.hasOwnProperty(name);
      var isAlreadyDefined = proto.hasOwnProperty(name);
      var isFunction = typeof property === 'function';
      var shouldAutoBind = isFunction && !isReactClassMethod && !isAlreadyDefined && spec.autobind !== false;

      if (shouldAutoBind) {
        if (!proto.__reactAutoBindMap) {
          proto.__reactAutoBindMap = {};
        }
        proto.__reactAutoBindMap[name] = property;
        proto[name] = property;
      } else {
        if (isAlreadyDefined) {
          var specPolicy = ReactClassInterface[name];

          // These cases should already be caught by validateMethodOverride.
          !(isReactClassMethod && (specPolicy === SpecPolicy.DEFINE_MANY_MERGED || specPolicy === SpecPolicy.DEFINE_MANY)) ? "production" !== 'production' ? invariant(false, 'ReactClass: Unexpected spec policy %s for key %s ' + 'when mixing in component specs.', specPolicy, name) : invariant(false) : undefined;

          // For methods which are defined more than once, call the existing
          // methods before calling the new property, merging if appropriate.
          if (specPolicy === SpecPolicy.DEFINE_MANY_MERGED) {
            proto[name] = createMergedResultFunction(proto[name], property);
          } else if (specPolicy === SpecPolicy.DEFINE_MANY) {
            proto[name] = createChainedFunction(proto[name], property);
          }
        } else {
          proto[name] = property;
          if ("production" !== 'production') {
            // Add verbose displayName to the function, which helps when looking
            // at profiling tools.
            if (typeof property === 'function' && spec.displayName) {
              proto[name].displayName = spec.displayName + '_' + name;
            }
          }
        }
      }
    }
  }
}

function mixStaticSpecIntoComponent(Constructor, statics) {
  if (!statics) {
    return;
  }
  for (var name in statics) {
    var property = statics[name];
    if (!statics.hasOwnProperty(name)) {
      continue;
    }

    var isReserved = (name in RESERVED_SPEC_KEYS);
    !!isReserved ? "production" !== 'production' ? invariant(false, 'ReactClass: You are attempting to define a reserved ' + 'property, `%s`, that shouldn\'t be on the "statics" key. Define it ' + 'as an instance property instead; it will still be accessible on the ' + 'constructor.', name) : invariant(false) : undefined;

    var isInherited = (name in Constructor);
    !!isInherited ? "production" !== 'production' ? invariant(false, 'ReactClass: You are attempting to define ' + '`%s` on your component more than once. This conflict may be ' + 'due to a mixin.', name) : invariant(false) : undefined;
    Constructor[name] = property;
  }
}

/**
 * Merge two objects, but throw if both contain the same key.
 *
 * @param {object} one The first object, which is mutated.
 * @param {object} two The second object
 * @return {object} one after it has been mutated to contain everything in two.
 */
function mergeIntoWithNoDuplicateKeys(one, two) {
  !(one && two && typeof one === 'object' && typeof two === 'object') ? "production" !== 'production' ? invariant(false, 'mergeIntoWithNoDuplicateKeys(): Cannot merge non-objects.') : invariant(false) : undefined;

  for (var key in two) {
    if (two.hasOwnProperty(key)) {
      !(one[key] === undefined) ? "production" !== 'production' ? invariant(false, 'mergeIntoWithNoDuplicateKeys(): ' + 'Tried to merge two objects with the same key: `%s`. This conflict ' + 'may be due to a mixin; in particular, this may be caused by two ' + 'getInitialState() or getDefaultProps() methods returning objects ' + 'with clashing keys.', key) : invariant(false) : undefined;
      one[key] = two[key];
    }
  }
  return one;
}

/**
 * Creates a function that invokes two functions and merges their return values.
 *
 * @param {function} one Function to invoke first.
 * @param {function} two Function to invoke second.
 * @return {function} Function that invokes the two argument functions.
 * @private
 */
function createMergedResultFunction(one, two) {
  return function mergedResult() {
    var a = one.apply(this, arguments);
    var b = two.apply(this, arguments);
    if (a == null) {
      return b;
    } else if (b == null) {
      return a;
    }
    var c = {};
    mergeIntoWithNoDuplicateKeys(c, a);
    mergeIntoWithNoDuplicateKeys(c, b);
    return c;
  };
}

/**
 * Creates a function that invokes two functions and ignores their return vales.
 *
 * @param {function} one Function to invoke first.
 * @param {function} two Function to invoke second.
 * @return {function} Function that invokes the two argument functions.
 * @private
 */
function createChainedFunction(one, two) {
  return function chainedFunction() {
    one.apply(this, arguments);
    two.apply(this, arguments);
  };
}

/**
 * Binds a method to the component.
 *
 * @param {object} component Component whose method is going to be bound.
 * @param {function} method Method to be bound.
 * @return {function} The bound method.
 */
function bindAutoBindMethod(component, method) {
  var boundMethod = method.bind(component);
  if ("production" !== 'production') {
    boundMethod.__reactBoundContext = component;
    boundMethod.__reactBoundMethod = method;
    boundMethod.__reactBoundArguments = null;
    var componentName = component.constructor.displayName;
    var _bind = boundMethod.bind;
    /* eslint-disable block-scoped-var, no-undef */
    boundMethod.bind = function (newThis) {
      for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
        args[_key - 1] = arguments[_key];
      }

      // User is trying to bind() an autobound method; we effectively will
      // ignore the value of "this" that the user is trying to use, so
      // let's warn.
      if (newThis !== component && newThis !== null) {
        "production" !== 'production' ? warning(false, 'bind(): React component methods may only be bound to the ' + 'component instance. See %s', componentName) : undefined;
      } else if (!args.length) {
        "production" !== 'production' ? warning(false, 'bind(): You are binding a component method to the component. ' + 'React does this for you automatically in a high-performance ' + 'way, so you can safely remove this call. See %s', componentName) : undefined;
        return boundMethod;
      }
      var reboundMethod = _bind.apply(boundMethod, arguments);
      reboundMethod.__reactBoundContext = component;
      reboundMethod.__reactBoundMethod = method;
      reboundMethod.__reactBoundArguments = args;
      return reboundMethod;
      /* eslint-enable */
    };
  }
  return boundMethod;
}

/**
 * Binds all auto-bound methods in a component.
 *
 * @param {object} component Component whose method is going to be bound.
 */
function bindAutoBindMethods(component) {
  for (var autoBindKey in component.__reactAutoBindMap) {
    if (component.__reactAutoBindMap.hasOwnProperty(autoBindKey)) {
      var method = component.__reactAutoBindMap[autoBindKey];
      component[autoBindKey] = bindAutoBindMethod(component, method);
    }
  }
}

/**
 * Add more to the ReactClass base class. These are all legacy features and
 * therefore not already part of the modern ReactComponent.
 */
var ReactClassMixin = {

  /**
   * TODO: This will be deprecated because state should always keep a consistent
   * type signature and the only use case for this, is to avoid that.
   */
  replaceState: function (newState, callback) {
    this.updater.enqueueReplaceState(this, newState);
    if (callback) {
      this.updater.enqueueCallback(this, callback);
    }
  },

  /**
   * Checks whether or not this composite component is mounted.
   * @return {boolean} True if mounted, false otherwise.
   * @protected
   * @final
   */
  isMounted: function () {
    return this.updater.isMounted(this);
  },

  /**
   * Sets a subset of the props.
   *
   * @param {object} partialProps Subset of the next props.
   * @param {?function} callback Called after props are updated.
   * @final
   * @public
   * @deprecated
   */
  setProps: function (partialProps, callback) {
    if ("production" !== 'production') {
      warnSetProps();
    }
    this.updater.enqueueSetProps(this, partialProps);
    if (callback) {
      this.updater.enqueueCallback(this, callback);
    }
  },

  /**
   * Replace all the props.
   *
   * @param {object} newProps Subset of the next props.
   * @param {?function} callback Called after props are updated.
   * @final
   * @public
   * @deprecated
   */
  replaceProps: function (newProps, callback) {
    if ("production" !== 'production') {
      warnSetProps();
    }
    this.updater.enqueueReplaceProps(this, newProps);
    if (callback) {
      this.updater.enqueueCallback(this, callback);
    }
  }
};

var ReactClassComponent = function () {};
assign(ReactClassComponent.prototype, ReactComponent.prototype, ReactClassMixin);

/**
 * Module for creating composite components.
 *
 * @class ReactClass
 */
var ReactClass = {

  /**
   * Creates a composite component class given a class specification.
   *
   * @param {object} spec Class specification (which must define `render`).
   * @return {function} Component constructor function.
   * @public
   */
  createClass: function (spec) {
    var Constructor = function (props, context, updater) {
      // This constructor is overridden by mocks. The argument is used
      // by mocks to assert on what gets mounted.

      if ("production" !== 'production') {
        "production" !== 'production' ? warning(this instanceof Constructor, 'Something is calling a React component directly. Use a factory or ' + 'JSX instead. See: https://fb.me/react-legacyfactory') : undefined;
      }

      // Wire up auto-binding
      if (this.__reactAutoBindMap) {
        bindAutoBindMethods(this);
      }

      this.props = props;
      this.context = context;
      this.refs = emptyObject;
      this.updater = updater || ReactNoopUpdateQueue;

      this.state = null;

      // ReactClasses doesn't have constructors. Instead, they use the
      // getInitialState and componentWillMount methods for initialization.

      var initialState = this.getInitialState ? this.getInitialState() : null;
      if ("production" !== 'production') {
        // We allow auto-mocks to proceed as if they're returning null.
        if (typeof initialState === 'undefined' && this.getInitialState._isMockFunction) {
          // This is probably bad practice. Consider warning here and
          // deprecating this convenience.
          initialState = null;
        }
      }
      !(typeof initialState === 'object' && !Array.isArray(initialState)) ? "production" !== 'production' ? invariant(false, '%s.getInitialState(): must return an object or null', Constructor.displayName || 'ReactCompositeComponent') : invariant(false) : undefined;

      this.state = initialState;
    };
    Constructor.prototype = new ReactClassComponent();
    Constructor.prototype.constructor = Constructor;

    injectedMixins.forEach(mixSpecIntoComponent.bind(null, Constructor));

    mixSpecIntoComponent(Constructor, spec);

    // Initialize the defaultProps property after all mixins have been merged.
    if (Constructor.getDefaultProps) {
      Constructor.defaultProps = Constructor.getDefaultProps();
    }

    if ("production" !== 'production') {
      // This is a tag to indicate that the use of these method names is ok,
      // since it's used with createClass. If it's not, then it's likely a
      // mistake so we'll warn you to use the static property, property
      // initializer or constructor respectively.
      if (Constructor.getDefaultProps) {
        Constructor.getDefaultProps.isReactClassApproved = {};
      }
      if (Constructor.prototype.getInitialState) {
        Constructor.prototype.getInitialState.isReactClassApproved = {};
      }
    }

    !Constructor.prototype.render ? "production" !== 'production' ? invariant(false, 'createClass(...): Class specification must implement a `render` method.') : invariant(false) : undefined;

    if ("production" !== 'production') {
      "production" !== 'production' ? warning(!Constructor.prototype.componentShouldUpdate, '%s has a method called ' + 'componentShouldUpdate(). Did you mean shouldComponentUpdate()? ' + 'The name is phrased as a question because the function is ' + 'expected to return a value.', spec.displayName || 'A component') : undefined;
      "production" !== 'production' ? warning(!Constructor.prototype.componentWillRecieveProps, '%s has a method called ' + 'componentWillRecieveProps(). Did you mean componentWillReceiveProps()?', spec.displayName || 'A component') : undefined;
    }

    // Reduce time spent doing lookups by setting these on the prototype.
    for (var methodName in ReactClassInterface) {
      if (!Constructor.prototype[methodName]) {
        Constructor.prototype[methodName] = null;
      }
    }

    return Constructor;
  },

  injection: {
    injectMixin: function (mixin) {
      injectedMixins.push(mixin);
    }
  }

};

module.exports = ReactClass;
},{"./Object.assign":249,"./ReactComponent":257,"./ReactElement":279,"./ReactNoopUpdateQueue":296,"./ReactPropTypeLocationNames":299,"./ReactPropTypeLocations":300,"fbjs/lib/emptyObject":144,"fbjs/lib/invariant":151,"fbjs/lib/keyMirror":154,"fbjs/lib/keyOf":155,"fbjs/lib/warning":162}],257:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactComponent
 */

'use strict';

var ReactNoopUpdateQueue = require('./ReactNoopUpdateQueue');

var canDefineProperty = require('./canDefineProperty');
var emptyObject = require('fbjs/lib/emptyObject');
var invariant = require('fbjs/lib/invariant');
var warning = require('fbjs/lib/warning');

/**
 * Base class helpers for the updating state of a component.
 */
function ReactComponent(props, context, updater) {
  this.props = props;
  this.context = context;
  this.refs = emptyObject;
  // We initialize the default updater but the real one gets injected by the
  // renderer.
  this.updater = updater || ReactNoopUpdateQueue;
}

ReactComponent.prototype.isReactComponent = {};

/**
 * Sets a subset of the state. Always use this to mutate
 * state. You should treat `this.state` as immutable.
 *
 * There is no guarantee that `this.state` will be immediately updated, so
 * accessing `this.state` after calling this method may return the old value.
 *
 * There is no guarantee that calls to `setState` will run synchronously,
 * as they may eventually be batched together.  You can provide an optional
 * callback that will be executed when the call to setState is actually
 * completed.
 *
 * When a function is provided to setState, it will be called at some point in
 * the future (not synchronously). It will be called with the up to date
 * component arguments (state, props, context). These values can be different
 * from this.* because your function may be called after receiveProps but before
 * shouldComponentUpdate, and this new state, props, and context will not yet be
 * assigned to this.
 *
 * @param {object|function} partialState Next partial state or function to
 *        produce next partial state to be merged with current state.
 * @param {?function} callback Called after state is updated.
 * @final
 * @protected
 */
ReactComponent.prototype.setState = function (partialState, callback) {
  !(typeof partialState === 'object' || typeof partialState === 'function' || partialState == null) ? "production" !== 'production' ? invariant(false, 'setState(...): takes an object of state variables to update or a ' + 'function which returns an object of state variables.') : invariant(false) : undefined;
  if ("production" !== 'production') {
    "production" !== 'production' ? warning(partialState != null, 'setState(...): You passed an undefined or null state object; ' + 'instead, use forceUpdate().') : undefined;
  }
  this.updater.enqueueSetState(this, partialState);
  if (callback) {
    this.updater.enqueueCallback(this, callback);
  }
};

/**
 * Forces an update. This should only be invoked when it is known with
 * certainty that we are **not** in a DOM transaction.
 *
 * You may want to call this when you know that some deeper aspect of the
 * component's state has changed but `setState` was not called.
 *
 * This will not invoke `shouldComponentUpdate`, but it will invoke
 * `componentWillUpdate` and `componentDidUpdate`.
 *
 * @param {?function} callback Called after update is complete.
 * @final
 * @protected
 */
ReactComponent.prototype.forceUpdate = function (callback) {
  this.updater.enqueueForceUpdate(this);
  if (callback) {
    this.updater.enqueueCallback(this, callback);
  }
};

/**
 * Deprecated APIs. These APIs used to exist on classic React classes but since
 * we would like to deprecate them, we're not going to move them over to this
 * modern base class. Instead, we define a getter that warns if it's accessed.
 */
if ("production" !== 'production') {
  var deprecatedAPIs = {
    getDOMNode: ['getDOMNode', 'Use ReactDOM.findDOMNode(component) instead.'],
    isMounted: ['isMounted', 'Instead, make sure to clean up subscriptions and pending requests in ' + 'componentWillUnmount to prevent memory leaks.'],
    replaceProps: ['replaceProps', 'Instead, call render again at the top level.'],
    replaceState: ['replaceState', 'Refactor your code to use setState instead (see ' + 'https://github.com/facebook/react/issues/3236).'],
    setProps: ['setProps', 'Instead, call render again at the top level.']
  };
  var defineDeprecationWarning = function (methodName, info) {
    if (canDefineProperty) {
      Object.defineProperty(ReactComponent.prototype, methodName, {
        get: function () {
          "production" !== 'production' ? warning(false, '%s(...) is deprecated in plain JavaScript React classes. %s', info[0], info[1]) : undefined;
          return undefined;
        }
      });
    }
  };
  for (var fnName in deprecatedAPIs) {
    if (deprecatedAPIs.hasOwnProperty(fnName)) {
      defineDeprecationWarning(fnName, deprecatedAPIs[fnName]);
    }
  }
}

module.exports = ReactComponent;
},{"./ReactNoopUpdateQueue":296,"./canDefineProperty":331,"fbjs/lib/emptyObject":144,"fbjs/lib/invariant":151,"fbjs/lib/warning":162}],258:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactComponentBrowserEnvironment
 */

'use strict';

var ReactDOMIDOperations = require('./ReactDOMIDOperations');
var ReactMount = require('./ReactMount');

/**
 * Abstracts away all functionality of the reconciler that requires knowledge of
 * the browser context. TODO: These callers should be refactored to avoid the
 * need for this injection.
 */
var ReactComponentBrowserEnvironment = {

  processChildrenUpdates: ReactDOMIDOperations.dangerouslyProcessChildrenUpdates,

  replaceNodeWithMarkupByID: ReactDOMIDOperations.dangerouslyReplaceNodeWithMarkupByID,

  /**
   * If a particular environment requires that some resources be cleaned up,
   * specify this in the injected Mixin. In the DOM, we would likely want to
   * purge any cached node ID lookups.
   *
   * @private
   */
  unmountIDFromEnvironment: function (rootNodeID) {
    ReactMount.purgeID(rootNodeID);
  }

};

module.exports = ReactComponentBrowserEnvironment;
},{"./ReactDOMIDOperations":267,"./ReactMount":292}],259:[function(require,module,exports){
/**
 * Copyright 2014-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactComponentEnvironment
 */

'use strict';

var invariant = require('fbjs/lib/invariant');

var injected = false;

var ReactComponentEnvironment = {

  /**
   * Optionally injectable environment dependent cleanup hook. (server vs.
   * browser etc). Example: A browser system caches DOM nodes based on component
   * ID and must remove that cache entry when this instance is unmounted.
   */
  unmountIDFromEnvironment: null,

  /**
   * Optionally injectable hook for swapping out mount images in the middle of
   * the tree.
   */
  replaceNodeWithMarkupByID: null,

  /**
   * Optionally injectable hook for processing a queue of child updates. Will
   * later move into MultiChildComponents.
   */
  processChildrenUpdates: null,

  injection: {
    injectEnvironment: function (environment) {
      !!injected ? "production" !== 'production' ? invariant(false, 'ReactCompositeComponent: injectEnvironment() can only be called once.') : invariant(false) : undefined;
      ReactComponentEnvironment.unmountIDFromEnvironment = environment.unmountIDFromEnvironment;
      ReactComponentEnvironment.replaceNodeWithMarkupByID = environment.replaceNodeWithMarkupByID;
      ReactComponentEnvironment.processChildrenUpdates = environment.processChildrenUpdates;
      injected = true;
    }
  }

};

module.exports = ReactComponentEnvironment;
},{"fbjs/lib/invariant":151}],260:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactCompositeComponent
 */

'use strict';

var ReactComponentEnvironment = require('./ReactComponentEnvironment');
var ReactCurrentOwner = require('./ReactCurrentOwner');
var ReactElement = require('./ReactElement');
var ReactInstanceMap = require('./ReactInstanceMap');
var ReactPerf = require('./ReactPerf');
var ReactPropTypeLocations = require('./ReactPropTypeLocations');
var ReactPropTypeLocationNames = require('./ReactPropTypeLocationNames');
var ReactReconciler = require('./ReactReconciler');
var ReactUpdateQueue = require('./ReactUpdateQueue');

var assign = require('./Object.assign');
var emptyObject = require('fbjs/lib/emptyObject');
var invariant = require('fbjs/lib/invariant');
var shouldUpdateReactComponent = require('./shouldUpdateReactComponent');
var warning = require('fbjs/lib/warning');

function getDeclarationErrorAddendum(component) {
  var owner = component._currentElement._owner || null;
  if (owner) {
    var name = owner.getName();
    if (name) {
      return ' Check the render method of `' + name + '`.';
    }
  }
  return '';
}

function StatelessComponent(Component) {}
StatelessComponent.prototype.render = function () {
  var Component = ReactInstanceMap.get(this)._currentElement.type;
  return Component(this.props, this.context, this.updater);
};

/**
 * ------------------ The Life-Cycle of a Composite Component ------------------
 *
 * - constructor: Initialization of state. The instance is now retained.
 *   - componentWillMount
 *   - render
 *   - [children's constructors]
 *     - [children's componentWillMount and render]
 *     - [children's componentDidMount]
 *     - componentDidMount
 *
 *       Update Phases:
 *       - componentWillReceiveProps (only called if parent updated)
 *       - shouldComponentUpdate
 *         - componentWillUpdate
 *           - render
 *           - [children's constructors or receive props phases]
 *         - componentDidUpdate
 *
 *     - componentWillUnmount
 *     - [children's componentWillUnmount]
 *   - [children destroyed]
 * - (destroyed): The instance is now blank, released by React and ready for GC.
 *
 * -----------------------------------------------------------------------------
 */

/**
 * An incrementing ID assigned to each component when it is mounted. This is
 * used to enforce the order in which `ReactUpdates` updates dirty components.
 *
 * @private
 */
var nextMountID = 1;

/**
 * @lends {ReactCompositeComponent.prototype}
 */
var ReactCompositeComponentMixin = {

  /**
   * Base constructor for all composite component.
   *
   * @param {ReactElement} element
   * @final
   * @internal
   */
  construct: function (element) {
    this._currentElement = element;
    this._rootNodeID = null;
    this._instance = null;

    // See ReactUpdateQueue
    this._pendingElement = null;
    this._pendingStateQueue = null;
    this._pendingReplaceState = false;
    this._pendingForceUpdate = false;

    this._renderedComponent = null;

    this._context = null;
    this._mountOrder = 0;
    this._topLevelWrapper = null;

    // See ReactUpdates and ReactUpdateQueue.
    this._pendingCallbacks = null;
  },

  /**
   * Initializes the component, renders markup, and registers event listeners.
   *
   * @param {string} rootID DOM ID of the root node.
   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
   * @return {?string} Rendered markup to be inserted into the DOM.
   * @final
   * @internal
   */
  mountComponent: function (rootID, transaction, context) {
    this._context = context;
    this._mountOrder = nextMountID++;
    this._rootNodeID = rootID;

    var publicProps = this._processProps(this._currentElement.props);
    var publicContext = this._processContext(context);

    var Component = this._currentElement.type;

    // Initialize the public class
    var inst;
    var renderedElement;

    // This is a way to detect if Component is a stateless arrow function
    // component, which is not newable. It might not be 100% reliable but is
    // something we can do until we start detecting that Component extends
    // React.Component. We already assume that typeof Component === 'function'.
    var canInstantiate = ('prototype' in Component);

    if (canInstantiate) {
      if ("production" !== 'production') {
        ReactCurrentOwner.current = this;
        try {
          inst = new Component(publicProps, publicContext, ReactUpdateQueue);
        } finally {
          ReactCurrentOwner.current = null;
        }
      } else {
        inst = new Component(publicProps, publicContext, ReactUpdateQueue);
      }
    }

    if (!canInstantiate || inst === null || inst === false || ReactElement.isValidElement(inst)) {
      renderedElement = inst;
      inst = new StatelessComponent(Component);
    }

    if ("production" !== 'production') {
      // This will throw later in _renderValidatedComponent, but add an early
      // warning now to help debugging
      if (inst.render == null) {
        "production" !== 'production' ? warning(false, '%s(...): No `render` method found on the returned component ' + 'instance: you may have forgotten to define `render`, returned ' + 'null/false from a stateless component, or tried to render an ' + 'element whose type is a function that isn\'t a React component.', Component.displayName || Component.name || 'Component') : undefined;
      } else {
        // We support ES6 inheriting from React.Component, the module pattern,
        // and stateless components, but not ES6 classes that don't extend
        "production" !== 'production' ? warning(Component.prototype && Component.prototype.isReactComponent || !canInstantiate || !(inst instanceof Component), '%s(...): React component classes must extend React.Component.', Component.displayName || Component.name || 'Component') : undefined;
      }
    }

    // These should be set up in the constructor, but as a convenience for
    // simpler class abstractions, we set them up after the fact.
    inst.props = publicProps;
    inst.context = publicContext;
    inst.refs = emptyObject;
    inst.updater = ReactUpdateQueue;

    this._instance = inst;

    // Store a reference from the instance back to the internal representation
    ReactInstanceMap.set(inst, this);

    if ("production" !== 'production') {
      // Since plain JS classes are defined without any special initialization
      // logic, we can not catch common errors early. Therefore, we have to
      // catch them here, at initialization time, instead.
      "production" !== 'production' ? warning(!inst.getInitialState || inst.getInitialState.isReactClassApproved, 'getInitialState was defined on %s, a plain JavaScript class. ' + 'This is only supported for classes created using React.createClass. ' + 'Did you mean to define a state property instead?', this.getName() || 'a component') : undefined;
      "production" !== 'production' ? warning(!inst.getDefaultProps || inst.getDefaultProps.isReactClassApproved, 'getDefaultProps was defined on %s, a plain JavaScript class. ' + 'This is only supported for classes created using React.createClass. ' + 'Use a static property to define defaultProps instead.', this.getName() || 'a component') : undefined;
      "production" !== 'production' ? warning(!inst.propTypes, 'propTypes was defined as an instance property on %s. Use a static ' + 'property to define propTypes instead.', this.getName() || 'a component') : undefined;
      "production" !== 'production' ? warning(!inst.contextTypes, 'contextTypes was defined as an instance property on %s. Use a ' + 'static property to define contextTypes instead.', this.getName() || 'a component') : undefined;
      "production" !== 'production' ? warning(typeof inst.componentShouldUpdate !== 'function', '%s has a method called ' + 'componentShouldUpdate(). Did you mean shouldComponentUpdate()? ' + 'The name is phrased as a question because the function is ' + 'expected to return a value.', this.getName() || 'A component') : undefined;
      "production" !== 'production' ? warning(typeof inst.componentDidUnmount !== 'function', '%s has a method called ' + 'componentDidUnmount(). But there is no such lifecycle method. ' + 'Did you mean componentWillUnmount()?', this.getName() || 'A component') : undefined;
      "production" !== 'production' ? warning(typeof inst.componentWillRecieveProps !== 'function', '%s has a method called ' + 'componentWillRecieveProps(). Did you mean componentWillReceiveProps()?', this.getName() || 'A component') : undefined;
    }

    var initialState = inst.state;
    if (initialState === undefined) {
      inst.state = initialState = null;
    }
    !(typeof initialState === 'object' && !Array.isArray(initialState)) ? "production" !== 'production' ? invariant(false, '%s.state: must be set to an object or null', this.getName() || 'ReactCompositeComponent') : invariant(false) : undefined;

    this._pendingStateQueue = null;
    this._pendingReplaceState = false;
    this._pendingForceUpdate = false;

    if (inst.componentWillMount) {
      inst.componentWillMount();
      // When mounting, calls to `setState` by `componentWillMount` will set
      // `this._pendingStateQueue` without triggering a re-render.
      if (this._pendingStateQueue) {
        inst.state = this._processPendingState(inst.props, inst.context);
      }
    }

    // If not a stateless component, we now render
    if (renderedElement === undefined) {
      renderedElement = this._renderValidatedComponent();
    }

    this._renderedComponent = this._instantiateReactComponent(renderedElement);

    var markup = ReactReconciler.mountComponent(this._renderedComponent, rootID, transaction, this._processChildContext(context));
    if (inst.componentDidMount) {
      transaction.getReactMountReady().enqueue(inst.componentDidMount, inst);
    }

    return markup;
  },

  /**
   * Releases any resources allocated by `mountComponent`.
   *
   * @final
   * @internal
   */
  unmountComponent: function () {
    var inst = this._instance;

    if (inst.componentWillUnmount) {
      inst.componentWillUnmount();
    }

    ReactReconciler.unmountComponent(this._renderedComponent);
    this._renderedComponent = null;
    this._instance = null;

    // Reset pending fields
    // Even if this component is scheduled for another update in ReactUpdates,
    // it would still be ignored because these fields are reset.
    this._pendingStateQueue = null;
    this._pendingReplaceState = false;
    this._pendingForceUpdate = false;
    this._pendingCallbacks = null;
    this._pendingElement = null;

    // These fields do not really need to be reset since this object is no
    // longer accessible.
    this._context = null;
    this._rootNodeID = null;
    this._topLevelWrapper = null;

    // Delete the reference from the instance to this internal representation
    // which allow the internals to be properly cleaned up even if the user
    // leaks a reference to the public instance.
    ReactInstanceMap.remove(inst);

    // Some existing components rely on inst.props even after they've been
    // destroyed (in event handlers).
    // TODO: inst.props = null;
    // TODO: inst.state = null;
    // TODO: inst.context = null;
  },

  /**
   * Filters the context object to only contain keys specified in
   * `contextTypes`
   *
   * @param {object} context
   * @return {?object}
   * @private
   */
  _maskContext: function (context) {
    var maskedContext = null;
    var Component = this._currentElement.type;
    var contextTypes = Component.contextTypes;
    if (!contextTypes) {
      return emptyObject;
    }
    maskedContext = {};
    for (var contextName in contextTypes) {
      maskedContext[contextName] = context[contextName];
    }
    return maskedContext;
  },

  /**
   * Filters the context object to only contain keys specified in
   * `contextTypes`, and asserts that they are valid.
   *
   * @param {object} context
   * @return {?object}
   * @private
   */
  _processContext: function (context) {
    var maskedContext = this._maskContext(context);
    if ("production" !== 'production') {
      var Component = this._currentElement.type;
      if (Component.contextTypes) {
        this._checkPropTypes(Component.contextTypes, maskedContext, ReactPropTypeLocations.context);
      }
    }
    return maskedContext;
  },

  /**
   * @param {object} currentContext
   * @return {object}
   * @private
   */
  _processChildContext: function (currentContext) {
    var Component = this._currentElement.type;
    var inst = this._instance;
    var childContext = inst.getChildContext && inst.getChildContext();
    if (childContext) {
      !(typeof Component.childContextTypes === 'object') ? "production" !== 'production' ? invariant(false, '%s.getChildContext(): childContextTypes must be defined in order to ' + 'use getChildContext().', this.getName() || 'ReactCompositeComponent') : invariant(false) : undefined;
      if ("production" !== 'production') {
        this._checkPropTypes(Component.childContextTypes, childContext, ReactPropTypeLocations.childContext);
      }
      for (var name in childContext) {
        !(name in Component.childContextTypes) ? "production" !== 'production' ? invariant(false, '%s.getChildContext(): key "%s" is not defined in childContextTypes.', this.getName() || 'ReactCompositeComponent', name) : invariant(false) : undefined;
      }
      return assign({}, currentContext, childContext);
    }
    return currentContext;
  },

  /**
   * Processes props by setting default values for unspecified props and
   * asserting that the props are valid. Does not mutate its argument; returns
   * a new props object with defaults merged in.
   *
   * @param {object} newProps
   * @return {object}
   * @private
   */
  _processProps: function (newProps) {
    if ("production" !== 'production') {
      var Component = this._currentElement.type;
      if (Component.propTypes) {
        this._checkPropTypes(Component.propTypes, newProps, ReactPropTypeLocations.prop);
      }
    }
    return newProps;
  },

  /**
   * Assert that the props are valid
   *
   * @param {object} propTypes Map of prop name to a ReactPropType
   * @param {object} props
   * @param {string} location e.g. "prop", "context", "child context"
   * @private
   */
  _checkPropTypes: function (propTypes, props, location) {
    // TODO: Stop validating prop types here and only use the element
    // validation.
    var componentName = this.getName();
    for (var propName in propTypes) {
      if (propTypes.hasOwnProperty(propName)) {
        var error;
        try {
          // This is intentionally an invariant that gets caught. It's the same
          // behavior as without this statement except with a better message.
          !(typeof propTypes[propName] === 'function') ? "production" !== 'production' ? invariant(false, '%s: %s type `%s` is invalid; it must be a function, usually ' + 'from React.PropTypes.', componentName || 'React class', ReactPropTypeLocationNames[location], propName) : invariant(false) : undefined;
          error = propTypes[propName](props, propName, componentName, location);
        } catch (ex) {
          error = ex;
        }
        if (error instanceof Error) {
          // We may want to extend this logic for similar errors in
          // top-level render calls, so I'm abstracting it away into
          // a function to minimize refactoring in the future
          var addendum = getDeclarationErrorAddendum(this);

          if (location === ReactPropTypeLocations.prop) {
            // Preface gives us something to blacklist in warning module
            "production" !== 'production' ? warning(false, 'Failed Composite propType: %s%s', error.message, addendum) : undefined;
          } else {
            "production" !== 'production' ? warning(false, 'Failed Context Types: %s%s', error.message, addendum) : undefined;
          }
        }
      }
    }
  },

  receiveComponent: function (nextElement, transaction, nextContext) {
    var prevElement = this._currentElement;
    var prevContext = this._context;

    this._pendingElement = null;

    this.updateComponent(transaction, prevElement, nextElement, prevContext, nextContext);
  },

  /**
   * If any of `_pendingElement`, `_pendingStateQueue`, or `_pendingForceUpdate`
   * is set, update the component.
   *
   * @param {ReactReconcileTransaction} transaction
   * @internal
   */
  performUpdateIfNecessary: function (transaction) {
    if (this._pendingElement != null) {
      ReactReconciler.receiveComponent(this, this._pendingElement || this._currentElement, transaction, this._context);
    }

    if (this._pendingStateQueue !== null || this._pendingForceUpdate) {
      this.updateComponent(transaction, this._currentElement, this._currentElement, this._context, this._context);
    }
  },

  /**
   * Perform an update to a mounted component. The componentWillReceiveProps and
   * shouldComponentUpdate methods are called, then (assuming the update isn't
   * skipped) the remaining update lifecycle methods are called and the DOM
   * representation is updated.
   *
   * By default, this implements React's rendering and reconciliation algorithm.
   * Sophisticated clients may wish to override this.
   *
   * @param {ReactReconcileTransaction} transaction
   * @param {ReactElement} prevParentElement
   * @param {ReactElement} nextParentElement
   * @internal
   * @overridable
   */
  updateComponent: function (transaction, prevParentElement, nextParentElement, prevUnmaskedContext, nextUnmaskedContext) {
    var inst = this._instance;

    var nextContext = this._context === nextUnmaskedContext ? inst.context : this._processContext(nextUnmaskedContext);
    var nextProps;

    // Distinguish between a props update versus a simple state update
    if (prevParentElement === nextParentElement) {
      // Skip checking prop types again -- we don't read inst.props to avoid
      // warning for DOM component props in this upgrade
      nextProps = nextParentElement.props;
    } else {
      nextProps = this._processProps(nextParentElement.props);
      // An update here will schedule an update but immediately set
      // _pendingStateQueue which will ensure that any state updates gets
      // immediately reconciled instead of waiting for the next batch.

      if (inst.componentWillReceiveProps) {
        inst.componentWillReceiveProps(nextProps, nextContext);
      }
    }

    var nextState = this._processPendingState(nextProps, nextContext);

    var shouldUpdate = this._pendingForceUpdate || !inst.shouldComponentUpdate || inst.shouldComponentUpdate(nextProps, nextState, nextContext);

    if ("production" !== 'production') {
      "production" !== 'production' ? warning(typeof shouldUpdate !== 'undefined', '%s.shouldComponentUpdate(): Returned undefined instead of a ' + 'boolean value. Make sure to return true or false.', this.getName() || 'ReactCompositeComponent') : undefined;
    }

    if (shouldUpdate) {
      this._pendingForceUpdate = false;
      // Will set `this.props`, `this.state` and `this.context`.
      this._performComponentUpdate(nextParentElement, nextProps, nextState, nextContext, transaction, nextUnmaskedContext);
    } else {
      // If it's determined that a component should not update, we still want
      // to set props and state but we shortcut the rest of the update.
      this._currentElement = nextParentElement;
      this._context = nextUnmaskedContext;
      inst.props = nextProps;
      inst.state = nextState;
      inst.context = nextContext;
    }
  },

  _processPendingState: function (props, context) {
    var inst = this._instance;
    var queue = this._pendingStateQueue;
    var replace = this._pendingReplaceState;
    this._pendingReplaceState = false;
    this._pendingStateQueue = null;

    if (!queue) {
      return inst.state;
    }

    if (replace && queue.length === 1) {
      return queue[0];
    }

    var nextState = assign({}, replace ? queue[0] : inst.state);
    for (var i = replace ? 1 : 0; i < queue.length; i++) {
      var partial = queue[i];
      assign(nextState, typeof partial === 'function' ? partial.call(inst, nextState, props, context) : partial);
    }

    return nextState;
  },

  /**
   * Merges new props and state, notifies delegate methods of update and
   * performs update.
   *
   * @param {ReactElement} nextElement Next element
   * @param {object} nextProps Next public object to set as properties.
   * @param {?object} nextState Next object to set as state.
   * @param {?object} nextContext Next public object to set as context.
   * @param {ReactReconcileTransaction} transaction
   * @param {?object} unmaskedContext
   * @private
   */
  _performComponentUpdate: function (nextElement, nextProps, nextState, nextContext, transaction, unmaskedContext) {
    var inst = this._instance;

    var hasComponentDidUpdate = Boolean(inst.componentDidUpdate);
    var prevProps;
    var prevState;
    var prevContext;
    if (hasComponentDidUpdate) {
      prevProps = inst.props;
      prevState = inst.state;
      prevContext = inst.context;
    }

    if (inst.componentWillUpdate) {
      inst.componentWillUpdate(nextProps, nextState, nextContext);
    }

    this._currentElement = nextElement;
    this._context = unmaskedContext;
    inst.props = nextProps;
    inst.state = nextState;
    inst.context = nextContext;

    this._updateRenderedComponent(transaction, unmaskedContext);

    if (hasComponentDidUpdate) {
      transaction.getReactMountReady().enqueue(inst.componentDidUpdate.bind(inst, prevProps, prevState, prevContext), inst);
    }
  },

  /**
   * Call the component's `render` method and update the DOM accordingly.
   *
   * @param {ReactReconcileTransaction} transaction
   * @internal
   */
  _updateRenderedComponent: function (transaction, context) {
    var prevComponentInstance = this._renderedComponent;
    var prevRenderedElement = prevComponentInstance._currentElement;
    var nextRenderedElement = this._renderValidatedComponent();
    if (shouldUpdateReactComponent(prevRenderedElement, nextRenderedElement)) {
      ReactReconciler.receiveComponent(prevComponentInstance, nextRenderedElement, transaction, this._processChildContext(context));
    } else {
      // These two IDs are actually the same! But nothing should rely on that.
      var thisID = this._rootNodeID;
      var prevComponentID = prevComponentInstance._rootNodeID;
      ReactReconciler.unmountComponent(prevComponentInstance);

      this._renderedComponent = this._instantiateReactComponent(nextRenderedElement);
      var nextMarkup = ReactReconciler.mountComponent(this._renderedComponent, thisID, transaction, this._processChildContext(context));
      this._replaceNodeWithMarkupByID(prevComponentID, nextMarkup);
    }
  },

  /**
   * @protected
   */
  _replaceNodeWithMarkupByID: function (prevComponentID, nextMarkup) {
    ReactComponentEnvironment.replaceNodeWithMarkupByID(prevComponentID, nextMarkup);
  },

  /**
   * @protected
   */
  _renderValidatedComponentWithoutOwnerOrContext: function () {
    var inst = this._instance;
    var renderedComponent = inst.render();
    if ("production" !== 'production') {
      // We allow auto-mocks to proceed as if they're returning null.
      if (typeof renderedComponent === 'undefined' && inst.render._isMockFunction) {
        // This is probably bad practice. Consider warning here and
        // deprecating this convenience.
        renderedComponent = null;
      }
    }

    return renderedComponent;
  },

  /**
   * @private
   */
  _renderValidatedComponent: function () {
    var renderedComponent;
    ReactCurrentOwner.current = this;
    try {
      renderedComponent = this._renderValidatedComponentWithoutOwnerOrContext();
    } finally {
      ReactCurrentOwner.current = null;
    }
    !(
    // TODO: An `isValidNode` function would probably be more appropriate
    renderedComponent === null || renderedComponent === false || ReactElement.isValidElement(renderedComponent)) ? "production" !== 'production' ? invariant(false, '%s.render(): A valid ReactComponent must be returned. You may have ' + 'returned undefined, an array or some other invalid object.', this.getName() || 'ReactCompositeComponent') : invariant(false) : undefined;
    return renderedComponent;
  },

  /**
   * Lazily allocates the refs object and stores `component` as `ref`.
   *
   * @param {string} ref Reference name.
   * @param {component} component Component to store as `ref`.
   * @final
   * @private
   */
  attachRef: function (ref, component) {
    var inst = this.getPublicInstance();
    !(inst != null) ? "production" !== 'production' ? invariant(false, 'Stateless function components cannot have refs.') : invariant(false) : undefined;
    var publicComponentInstance = component.getPublicInstance();
    if ("production" !== 'production') {
      var componentName = component && component.getName ? component.getName() : 'a component';
      "production" !== 'production' ? warning(publicComponentInstance != null, 'Stateless function components cannot be given refs ' + '(See ref "%s" in %s created by %s). ' + 'Attempts to access this ref will fail.', ref, componentName, this.getName()) : undefined;
    }
    var refs = inst.refs === emptyObject ? inst.refs = {} : inst.refs;
    refs[ref] = publicComponentInstance;
  },

  /**
   * Detaches a reference name.
   *
   * @param {string} ref Name to dereference.
   * @final
   * @private
   */
  detachRef: function (ref) {
    var refs = this.getPublicInstance().refs;
    delete refs[ref];
  },

  /**
   * Get a text description of the component that can be used to identify it
   * in error messages.
   * @return {string} The name or null.
   * @internal
   */
  getName: function () {
    var type = this._currentElement.type;
    var constructor = this._instance && this._instance.constructor;
    return type.displayName || constructor && constructor.displayName || type.name || constructor && constructor.name || null;
  },

  /**
   * Get the publicly accessible representation of this component - i.e. what
   * is exposed by refs and returned by render. Can be null for stateless
   * components.
   *
   * @return {ReactComponent} the public component instance.
   * @internal
   */
  getPublicInstance: function () {
    var inst = this._instance;
    if (inst instanceof StatelessComponent) {
      return null;
    }
    return inst;
  },

  // Stub
  _instantiateReactComponent: null

};

ReactPerf.measureMethods(ReactCompositeComponentMixin, 'ReactCompositeComponent', {
  mountComponent: 'mountComponent',
  updateComponent: 'updateComponent',
  _renderValidatedComponent: '_renderValidatedComponent'
});

var ReactCompositeComponent = {

  Mixin: ReactCompositeComponentMixin

};

module.exports = ReactCompositeComponent;
},{"./Object.assign":249,"./ReactComponentEnvironment":259,"./ReactCurrentOwner":261,"./ReactElement":279,"./ReactInstanceMap":289,"./ReactPerf":298,"./ReactPropTypeLocationNames":299,"./ReactPropTypeLocations":300,"./ReactReconciler":303,"./ReactUpdateQueue":309,"./shouldUpdateReactComponent":353,"fbjs/lib/emptyObject":144,"fbjs/lib/invariant":151,"fbjs/lib/warning":162}],261:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactCurrentOwner
 */

'use strict';

/**
 * Keeps track of the current owner.
 *
 * The current owner is the component who should own any components that are
 * currently being constructed.
 */
var ReactCurrentOwner = {

  /**
   * @internal
   * @type {ReactComponent}
   */
  current: null

};

module.exports = ReactCurrentOwner;
},{}],262:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactDOM
 */

/* globals __REACT_DEVTOOLS_GLOBAL_HOOK__*/

'use strict';

var ReactCurrentOwner = require('./ReactCurrentOwner');
var ReactDOMTextComponent = require('./ReactDOMTextComponent');
var ReactDefaultInjection = require('./ReactDefaultInjection');
var ReactInstanceHandles = require('./ReactInstanceHandles');
var ReactMount = require('./ReactMount');
var ReactPerf = require('./ReactPerf');
var ReactReconciler = require('./ReactReconciler');
var ReactUpdates = require('./ReactUpdates');
var ReactVersion = require('./ReactVersion');

var findDOMNode = require('./findDOMNode');
var renderSubtreeIntoContainer = require('./renderSubtreeIntoContainer');
var warning = require('fbjs/lib/warning');

ReactDefaultInjection.inject();

var render = ReactPerf.measure('React', 'render', ReactMount.render);

var React = {
  findDOMNode: findDOMNode,
  render: render,
  unmountComponentAtNode: ReactMount.unmountComponentAtNode,
  version: ReactVersion,

  /* eslint-disable camelcase */
  unstable_batchedUpdates: ReactUpdates.batchedUpdates,
  unstable_renderSubtreeIntoContainer: renderSubtreeIntoContainer
};

// Inject the runtime into a devtools global hook regardless of browser.
// Allows for debugging when the hook is injected on the page.
/* eslint-enable camelcase */
if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== 'undefined' && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.inject === 'function') {
  __REACT_DEVTOOLS_GLOBAL_HOOK__.inject({
    CurrentOwner: ReactCurrentOwner,
    InstanceHandles: ReactInstanceHandles,
    Mount: ReactMount,
    Reconciler: ReactReconciler,
    TextComponent: ReactDOMTextComponent
  });
}

if ("production" !== 'production') {
  var ExecutionEnvironment = require('fbjs/lib/ExecutionEnvironment');
  if (ExecutionEnvironment.canUseDOM && window.top === window.self) {

    // First check if devtools is not installed
    if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === 'undefined') {
      // If we're in Chrome or Firefox, provide a download link if not installed.
      if (navigator.userAgent.indexOf('Chrome') > -1 && navigator.userAgent.indexOf('Edge') === -1 || navigator.userAgent.indexOf('Firefox') > -1) {
        console.debug('Download the React DevTools for a better development experience: ' + 'https://fb.me/react-devtools');
      }
    }

    // If we're in IE8, check to see if we are in compatibility mode and provide
    // information on preventing compatibility mode
    var ieCompatibilityMode = document.documentMode && document.documentMode < 8;

    "production" !== 'production' ? warning(!ieCompatibilityMode, 'Internet Explorer is running in compatibility mode; please add the ' + 'following tag to your HTML to prevent this from happening: ' + '<meta http-equiv="X-UA-Compatible" content="IE=edge" />') : undefined;

    var expectedFeatures = [
    // shims
    Array.isArray, Array.prototype.every, Array.prototype.forEach, Array.prototype.indexOf, Array.prototype.map, Date.now, Function.prototype.bind, Object.keys, String.prototype.split, String.prototype.trim,

    // shams
    Object.create, Object.freeze];

    for (var i = 0; i < expectedFeatures.length; i++) {
      if (!expectedFeatures[i]) {
        console.error('One or more ES5 shim/shams expected by React are not available: ' + 'https://fb.me/react-warning-polyfills');
        break;
      }
    }
  }
}

module.exports = React;
},{"./ReactCurrentOwner":261,"./ReactDOMTextComponent":273,"./ReactDefaultInjection":276,"./ReactInstanceHandles":288,"./ReactMount":292,"./ReactPerf":298,"./ReactReconciler":303,"./ReactUpdates":310,"./ReactVersion":311,"./findDOMNode":335,"./renderSubtreeIntoContainer":350,"fbjs/lib/ExecutionEnvironment":137,"fbjs/lib/warning":162}],263:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactDOMButton
 */

'use strict';

var mouseListenerNames = {
  onClick: true,
  onDoubleClick: true,
  onMouseDown: true,
  onMouseMove: true,
  onMouseUp: true,

  onClickCapture: true,
  onDoubleClickCapture: true,
  onMouseDownCapture: true,
  onMouseMoveCapture: true,
  onMouseUpCapture: true
};

/**
 * Implements a <button> native component that does not receive mouse events
 * when `disabled` is set.
 */
var ReactDOMButton = {
  getNativeProps: function (inst, props, context) {
    if (!props.disabled) {
      return props;
    }

    // Copy the props, except the mouse listeners
    var nativeProps = {};
    for (var key in props) {
      if (props.hasOwnProperty(key) && !mouseListenerNames[key]) {
        nativeProps[key] = props[key];
      }
    }

    return nativeProps;
  }
};

module.exports = ReactDOMButton;
},{}],264:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactDOMComponent
 * @typechecks static-only
 */

/* global hasOwnProperty:true */

'use strict';

var AutoFocusUtils = require('./AutoFocusUtils');
var CSSPropertyOperations = require('./CSSPropertyOperations');
var DOMProperty = require('./DOMProperty');
var DOMPropertyOperations = require('./DOMPropertyOperations');
var EventConstants = require('./EventConstants');
var ReactBrowserEventEmitter = require('./ReactBrowserEventEmitter');
var ReactComponentBrowserEnvironment = require('./ReactComponentBrowserEnvironment');
var ReactDOMButton = require('./ReactDOMButton');
var ReactDOMInput = require('./ReactDOMInput');
var ReactDOMOption = require('./ReactDOMOption');
var ReactDOMSelect = require('./ReactDOMSelect');
var ReactDOMTextarea = require('./ReactDOMTextarea');
var ReactMount = require('./ReactMount');
var ReactMultiChild = require('./ReactMultiChild');
var ReactPerf = require('./ReactPerf');
var ReactUpdateQueue = require('./ReactUpdateQueue');

var assign = require('./Object.assign');
var canDefineProperty = require('./canDefineProperty');
var escapeTextContentForBrowser = require('./escapeTextContentForBrowser');
var invariant = require('fbjs/lib/invariant');
var isEventSupported = require('./isEventSupported');
var keyOf = require('fbjs/lib/keyOf');
var setInnerHTML = require('./setInnerHTML');
var setTextContent = require('./setTextContent');
var shallowEqual = require('fbjs/lib/shallowEqual');
var validateDOMNesting = require('./validateDOMNesting');
var warning = require('fbjs/lib/warning');

var deleteListener = ReactBrowserEventEmitter.deleteListener;
var listenTo = ReactBrowserEventEmitter.listenTo;
var registrationNameModules = ReactBrowserEventEmitter.registrationNameModules;

// For quickly matching children type, to test if can be treated as content.
var CONTENT_TYPES = { 'string': true, 'number': true };

var CHILDREN = keyOf({ children: null });
var STYLE = keyOf({ style: null });
var HTML = keyOf({ __html: null });

var ELEMENT_NODE_TYPE = 1;

function getDeclarationErrorAddendum(internalInstance) {
  if (internalInstance) {
    var owner = internalInstance._currentElement._owner || null;
    if (owner) {
      var name = owner.getName();
      if (name) {
        return ' This DOM node was rendered by `' + name + '`.';
      }
    }
  }
  return '';
}

var legacyPropsDescriptor;
if ("production" !== 'production') {
  legacyPropsDescriptor = {
    props: {
      enumerable: false,
      get: function () {
        var component = this._reactInternalComponent;
        "production" !== 'production' ? warning(false, 'ReactDOMComponent: Do not access .props of a DOM node; instead, ' + 'recreate the props as `render` did originally or read the DOM ' + 'properties/attributes directly from this node (e.g., ' + 'this.refs.box.className).%s', getDeclarationErrorAddendum(component)) : undefined;
        return component._currentElement.props;
      }
    }
  };
}

function legacyGetDOMNode() {
  if ("production" !== 'production') {
    var component = this._reactInternalComponent;
    "production" !== 'production' ? warning(false, 'ReactDOMComponent: Do not access .getDOMNode() of a DOM node; ' + 'instead, use the node directly.%s', getDeclarationErrorAddendum(component)) : undefined;
  }
  return this;
}

function legacyIsMounted() {
  var component = this._reactInternalComponent;
  if ("production" !== 'production') {
    "production" !== 'production' ? warning(false, 'ReactDOMComponent: Do not access .isMounted() of a DOM node.%s', getDeclarationErrorAddendum(component)) : undefined;
  }
  return !!component;
}

function legacySetStateEtc() {
  if ("production" !== 'production') {
    var component = this._reactInternalComponent;
    "production" !== 'production' ? warning(false, 'ReactDOMComponent: Do not access .setState(), .replaceState(), or ' + '.forceUpdate() of a DOM node. This is a no-op.%s', getDeclarationErrorAddendum(component)) : undefined;
  }
}

function legacySetProps(partialProps, callback) {
  var component = this._reactInternalComponent;
  if ("production" !== 'production') {
    "production" !== 'production' ? warning(false, 'ReactDOMComponent: Do not access .setProps() of a DOM node. ' + 'Instead, call ReactDOM.render again at the top level.%s', getDeclarationErrorAddendum(component)) : undefined;
  }
  if (!component) {
    return;
  }
  ReactUpdateQueue.enqueueSetPropsInternal(component, partialProps);
  if (callback) {
    ReactUpdateQueue.enqueueCallbackInternal(component, callback);
  }
}

function legacyReplaceProps(partialProps, callback) {
  var component = this._reactInternalComponent;
  if ("production" !== 'production') {
    "production" !== 'production' ? warning(false, 'ReactDOMComponent: Do not access .replaceProps() of a DOM node. ' + 'Instead, call ReactDOM.render again at the top level.%s', getDeclarationErrorAddendum(component)) : undefined;
  }
  if (!component) {
    return;
  }
  ReactUpdateQueue.enqueueReplacePropsInternal(component, partialProps);
  if (callback) {
    ReactUpdateQueue.enqueueCallbackInternal(component, callback);
  }
}

function friendlyStringify(obj) {
  if (typeof obj === 'object') {
    if (Array.isArray(obj)) {
      return '[' + obj.map(friendlyStringify).join(', ') + ']';
    } else {
      var pairs = [];
      for (var key in obj) {
        if (Object.prototype.hasOwnProperty.call(obj, key)) {
          var keyEscaped = /^[a-z$_][\w$_]*$/i.test(key) ? key : JSON.stringify(key);
          pairs.push(keyEscaped + ': ' + friendlyStringify(obj[key]));
        }
      }
      return '{' + pairs.join(', ') + '}';
    }
  } else if (typeof obj === 'string') {
    return JSON.stringify(obj);
  } else if (typeof obj === 'function') {
    return '[function object]';
  }
  // Differs from JSON.stringify in that undefined becauses undefined and that
  // inf and nan don't become null
  return String(obj);
}

var styleMutationWarning = {};

function checkAndWarnForMutatedStyle(style1, style2, component) {
  if (style1 == null || style2 == null) {
    return;
  }
  if (shallowEqual(style1, style2)) {
    return;
  }

  var componentName = component._tag;
  var owner = component._currentElement._owner;
  var ownerName;
  if (owner) {
    ownerName = owner.getName();
  }

  var hash = ownerName + '|' + componentName;

  if (styleMutationWarning.hasOwnProperty(hash)) {
    return;
  }

  styleMutationWarning[hash] = true;

  "production" !== 'production' ? warning(false, '`%s` was passed a style object that has previously been mutated. ' + 'Mutating `style` is deprecated. Consider cloning it beforehand. Check ' + 'the `render` %s. Previous style: %s. Mutated style: %s.', componentName, owner ? 'of `' + ownerName + '`' : 'using <' + componentName + '>', friendlyStringify(style1), friendlyStringify(style2)) : undefined;
}

/**
 * @param {object} component
 * @param {?object} props
 */
function assertValidProps(component, props) {
  if (!props) {
    return;
  }
  // Note the use of `==` which checks for null or undefined.
  if ("production" !== 'production') {
    if (voidElementTags[component._tag]) {
      "production" !== 'production' ? warning(props.children == null && props.dangerouslySetInnerHTML == null, '%s is a void element tag and must not have `children` or ' + 'use `props.dangerouslySetInnerHTML`.%s', component._tag, component._currentElement._owner ? ' Check the render method of ' + component._currentElement._owner.getName() + '.' : '') : undefined;
    }
  }
  if (props.dangerouslySetInnerHTML != null) {
    !(props.children == null) ? "production" !== 'production' ? invariant(false, 'Can only set one of `children` or `props.dangerouslySetInnerHTML`.') : invariant(false) : undefined;
    !(typeof props.dangerouslySetInnerHTML === 'object' && HTML in props.dangerouslySetInnerHTML) ? "production" !== 'production' ? invariant(false, '`props.dangerouslySetInnerHTML` must be in the form `{__html: ...}`. ' + 'Please visit https://fb.me/react-invariant-dangerously-set-inner-html ' + 'for more information.') : invariant(false) : undefined;
  }
  if ("production" !== 'production') {
    "production" !== 'production' ? warning(props.innerHTML == null, 'Directly setting property `innerHTML` is not permitted. ' + 'For more information, lookup documentation on `dangerouslySetInnerHTML`.') : undefined;
    "production" !== 'production' ? warning(!props.contentEditable || props.children == null, 'A component is `contentEditable` and contains `children` managed by ' + 'React. It is now your responsibility to guarantee that none of ' + 'those nodes are unexpectedly modified or duplicated. This is ' + 'probably not intentional.') : undefined;
  }
  !(props.style == null || typeof props.style === 'object') ? "production" !== 'production' ? invariant(false, 'The `style` prop expects a mapping from style properties to values, ' + 'not a string. For example, style={{marginRight: spacing + \'em\'}} when ' + 'using JSX.%s', getDeclarationErrorAddendum(component)) : invariant(false) : undefined;
}

function enqueuePutListener(id, registrationName, listener, transaction) {
  if ("production" !== 'production') {
    // IE8 has no API for event capturing and the `onScroll` event doesn't
    // bubble.
    "production" !== 'production' ? warning(registrationName !== 'onScroll' || isEventSupported('scroll', true), 'This browser doesn\'t support the `onScroll` event') : undefined;
  }
  var container = ReactMount.findReactContainerForID(id);
  if (container) {
    var doc = container.nodeType === ELEMENT_NODE_TYPE ? container.ownerDocument : container;
    listenTo(registrationName, doc);
  }
  transaction.getReactMountReady().enqueue(putListener, {
    id: id,
    registrationName: registrationName,
    listener: listener
  });
}

function putListener() {
  var listenerToPut = this;
  ReactBrowserEventEmitter.putListener(listenerToPut.id, listenerToPut.registrationName, listenerToPut.listener);
}

// There are so many media events, it makes sense to just
// maintain a list rather than create a `trapBubbledEvent` for each
var mediaEvents = {
  topAbort: 'abort',
  topCanPlay: 'canplay',
  topCanPlayThrough: 'canplaythrough',
  topDurationChange: 'durationchange',
  topEmptied: 'emptied',
  topEncrypted: 'encrypted',
  topEnded: 'ended',
  topError: 'error',
  topLoadedData: 'loadeddata',
  topLoadedMetadata: 'loadedmetadata',
  topLoadStart: 'loadstart',
  topPause: 'pause',
  topPlay: 'play',
  topPlaying: 'playing',
  topProgress: 'progress',
  topRateChange: 'ratechange',
  topSeeked: 'seeked',
  topSeeking: 'seeking',
  topStalled: 'stalled',
  topSuspend: 'suspend',
  topTimeUpdate: 'timeupdate',
  topVolumeChange: 'volumechange',
  topWaiting: 'waiting'
};

function trapBubbledEventsLocal() {
  var inst = this;
  // If a component renders to null or if another component fatals and causes
  // the state of the tree to be corrupted, `node` here can be null.
  !inst._rootNodeID ? "production" !== 'production' ? invariant(false, 'Must be mounted to trap events') : invariant(false) : undefined;
  var node = ReactMount.getNode(inst._rootNodeID);
  !node ? "production" !== 'production' ? invariant(false, 'trapBubbledEvent(...): Requires node to be rendered.') : invariant(false) : undefined;

  switch (inst._tag) {
    case 'iframe':
      inst._wrapperState.listeners = [ReactBrowserEventEmitter.trapBubbledEvent(EventConstants.topLevelTypes.topLoad, 'load', node)];
      break;
    case 'video':
    case 'audio':

      inst._wrapperState.listeners = [];
      // create listener for each media event
      for (var event in mediaEvents) {
        if (mediaEvents.hasOwnProperty(event)) {
          inst._wrapperState.listeners.push(ReactBrowserEventEmitter.trapBubbledEvent(EventConstants.topLevelTypes[event], mediaEvents[event], node));
        }
      }

      break;
    case 'img':
      inst._wrapperState.listeners = [ReactBrowserEventEmitter.trapBubbledEvent(EventConstants.topLevelTypes.topError, 'error', node), ReactBrowserEventEmitter.trapBubbledEvent(EventConstants.topLevelTypes.topLoad, 'load', node)];
      break;
    case 'form':
      inst._wrapperState.listeners = [ReactBrowserEventEmitter.trapBubbledEvent(EventConstants.topLevelTypes.topReset, 'reset', node), ReactBrowserEventEmitter.trapBubbledEvent(EventConstants.topLevelTypes.topSubmit, 'submit', node)];
      break;
  }
}

function mountReadyInputWrapper() {
  ReactDOMInput.mountReadyWrapper(this);
}

function postUpdateSelectWrapper() {
  ReactDOMSelect.postUpdateWrapper(this);
}

// For HTML, certain tags should omit their close tag. We keep a whitelist for
// those special cased tags.

var omittedCloseTags = {
  'area': true,
  'base': true,
  'br': true,
  'col': true,
  'embed': true,
  'hr': true,
  'img': true,
  'input': true,
  'keygen': true,
  'link': true,
  'meta': true,
  'param': true,
  'source': true,
  'track': true,
  'wbr': true
};

// NOTE: menuitem's close tag should be omitted, but that causes problems.
var newlineEatingTags = {
  'listing': true,
  'pre': true,
  'textarea': true
};

// For HTML, certain tags cannot have children. This has the same purpose as
// `omittedCloseTags` except that `menuitem` should still have its closing tag.

var voidElementTags = assign({
  'menuitem': true
}, omittedCloseTags);

// We accept any tag to be rendered but since this gets injected into arbitrary
// HTML, we want to make sure that it's a safe tag.
// http://www.w3.org/TR/REC-xml/#NT-Name

var VALID_TAG_REGEX = /^[a-zA-Z][a-zA-Z:_\.\-\d]*$/; // Simplified subset
var validatedTagCache = {};
var hasOwnProperty = ({}).hasOwnProperty;

function validateDangerousTag(tag) {
  if (!hasOwnProperty.call(validatedTagCache, tag)) {
    !VALID_TAG_REGEX.test(tag) ? "production" !== 'production' ? invariant(false, 'Invalid tag: %s', tag) : invariant(false) : undefined;
    validatedTagCache[tag] = true;
  }
}

function processChildContextDev(context, inst) {
  // Pass down our tag name to child components for validation purposes
  context = assign({}, context);
  var info = context[validateDOMNesting.ancestorInfoContextKey];
  context[validateDOMNesting.ancestorInfoContextKey] = validateDOMNesting.updatedAncestorInfo(info, inst._tag, inst);
  return context;
}

function isCustomComponent(tagName, props) {
  return tagName.indexOf('-') >= 0 || props.is != null;
}

/**
 * Creates a new React class that is idempotent and capable of containing other
 * React components. It accepts event listeners and DOM properties that are
 * valid according to `DOMProperty`.
 *
 *  - Event listeners: `onClick`, `onMouseDown`, etc.
 *  - DOM properties: `className`, `name`, `title`, etc.
 *
 * The `style` property functions differently from the DOM API. It accepts an
 * object mapping of style properties to values.
 *
 * @constructor ReactDOMComponent
 * @extends ReactMultiChild
 */
function ReactDOMComponent(tag) {
  validateDangerousTag(tag);
  this._tag = tag.toLowerCase();
  this._renderedChildren = null;
  this._previousStyle = null;
  this._previousStyleCopy = null;
  this._rootNodeID = null;
  this._wrapperState = null;
  this._topLevelWrapper = null;
  this._nodeWithLegacyProperties = null;
  if ("production" !== 'production') {
    this._unprocessedContextDev = null;
    this._processedContextDev = null;
  }
}

ReactDOMComponent.displayName = 'ReactDOMComponent';

ReactDOMComponent.Mixin = {

  construct: function (element) {
    this._currentElement = element;
  },

  /**
   * Generates root tag markup then recurses. This method has side effects and
   * is not idempotent.
   *
   * @internal
   * @param {string} rootID The root DOM ID for this node.
   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
   * @param {object} context
   * @return {string} The computed markup.
   */
  mountComponent: function (rootID, transaction, context) {
    this._rootNodeID = rootID;

    var props = this._currentElement.props;

    switch (this._tag) {
      case 'iframe':
      case 'img':
      case 'form':
      case 'video':
      case 'audio':
        this._wrapperState = {
          listeners: null
        };
        transaction.getReactMountReady().enqueue(trapBubbledEventsLocal, this);
        break;
      case 'button':
        props = ReactDOMButton.getNativeProps(this, props, context);
        break;
      case 'input':
        ReactDOMInput.mountWrapper(this, props, context);
        props = ReactDOMInput.getNativeProps(this, props, context);
        break;
      case 'option':
        ReactDOMOption.mountWrapper(this, props, context);
        props = ReactDOMOption.getNativeProps(this, props, context);
        break;
      case 'select':
        ReactDOMSelect.mountWrapper(this, props, context);
        props = ReactDOMSelect.getNativeProps(this, props, context);
        context = ReactDOMSelect.processChildContext(this, props, context);
        break;
      case 'textarea':
        ReactDOMTextarea.mountWrapper(this, props, context);
        props = ReactDOMTextarea.getNativeProps(this, props, context);
        break;
    }

    assertValidProps(this, props);
    if ("production" !== 'production') {
      if (context[validateDOMNesting.ancestorInfoContextKey]) {
        validateDOMNesting(this._tag, this, context[validateDOMNesting.ancestorInfoContextKey]);
      }
    }

    if ("production" !== 'production') {
      this._unprocessedContextDev = context;
      this._processedContextDev = processChildContextDev(context, this);
      context = this._processedContextDev;
    }

    var mountImage;
    if (transaction.useCreateElement) {
      var ownerDocument = context[ReactMount.ownerDocumentContextKey];
      var el = ownerDocument.createElement(this._currentElement.type);
      DOMPropertyOperations.setAttributeForID(el, this._rootNodeID);
      // Populate node cache
      ReactMount.getID(el);
      this._updateDOMProperties({}, props, transaction, el);
      this._createInitialChildren(transaction, props, context, el);
      mountImage = el;
    } else {
      var tagOpen = this._createOpenTagMarkupAndPutListeners(transaction, props);
      var tagContent = this._createContentMarkup(transaction, props, context);
      if (!tagContent && omittedCloseTags[this._tag]) {
        mountImage = tagOpen + '/>';
      } else {
        mountImage = tagOpen + '>' + tagContent + '</' + this._currentElement.type + '>';
      }
    }

    switch (this._tag) {
      case 'input':
        transaction.getReactMountReady().enqueue(mountReadyInputWrapper, this);
      // falls through
      case 'button':
      case 'select':
      case 'textarea':
        if (props.autoFocus) {
          transaction.getReactMountReady().enqueue(AutoFocusUtils.focusDOMComponent, this);
        }
        break;
    }

    return mountImage;
  },

  /**
   * Creates markup for the open tag and all attributes.
   *
   * This method has side effects because events get registered.
   *
   * Iterating over object properties is faster than iterating over arrays.
   * @see http://jsperf.com/obj-vs-arr-iteration
   *
   * @private
   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
   * @param {object} props
   * @return {string} Markup of opening tag.
   */
  _createOpenTagMarkupAndPutListeners: function (transaction, props) {
    var ret = '<' + this._currentElement.type;

    for (var propKey in props) {
      if (!props.hasOwnProperty(propKey)) {
        continue;
      }
      var propValue = props[propKey];
      if (propValue == null) {
        continue;
      }
      if (registrationNameModules.hasOwnProperty(propKey)) {
        if (propValue) {
          enqueuePutListener(this._rootNodeID, propKey, propValue, transaction);
        }
      } else {
        if (propKey === STYLE) {
          if (propValue) {
            if ("production" !== 'production') {
              // See `_updateDOMProperties`. style block
              this._previousStyle = propValue;
            }
            propValue = this._previousStyleCopy = assign({}, props.style);
          }
          propValue = CSSPropertyOperations.createMarkupForStyles(propValue);
        }
        var markup = null;
        if (this._tag != null && isCustomComponent(this._tag, props)) {
          if (propKey !== CHILDREN) {
            markup = DOMPropertyOperations.createMarkupForCustomAttribute(propKey, propValue);
          }
        } else {
          markup = DOMPropertyOperations.createMarkupForProperty(propKey, propValue);
        }
        if (markup) {
          ret += ' ' + markup;
        }
      }
    }

    // For static pages, no need to put React ID and checksum. Saves lots of
    // bytes.
    if (transaction.renderToStaticMarkup) {
      return ret;
    }

    var markupForID = DOMPropertyOperations.createMarkupForID(this._rootNodeID);
    return ret + ' ' + markupForID;
  },

  /**
   * Creates markup for the content between the tags.
   *
   * @private
   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
   * @param {object} props
   * @param {object} context
   * @return {string} Content markup.
   */
  _createContentMarkup: function (transaction, props, context) {
    var ret = '';

    // Intentional use of != to avoid catching zero/false.
    var innerHTML = props.dangerouslySetInnerHTML;
    if (innerHTML != null) {
      if (innerHTML.__html != null) {
        ret = innerHTML.__html;
      }
    } else {
      var contentToUse = CONTENT_TYPES[typeof props.children] ? props.children : null;
      var childrenToUse = contentToUse != null ? null : props.children;
      if (contentToUse != null) {
        // TODO: Validate that text is allowed as a child of this node
        ret = escapeTextContentForBrowser(contentToUse);
      } else if (childrenToUse != null) {
        var mountImages = this.mountChildren(childrenToUse, transaction, context);
        ret = mountImages.join('');
      }
    }
    if (newlineEatingTags[this._tag] && ret.charAt(0) === '\n') {
      // text/html ignores the first character in these tags if it's a newline
      // Prefer to break application/xml over text/html (for now) by adding
      // a newline specifically to get eaten by the parser. (Alternately for
      // textareas, replacing "^\n" with "\r\n" doesn't get eaten, and the first
      // \r is normalized out by HTMLTextAreaElement#value.)
      // See: <http://www.w3.org/TR/html-polyglot/#newlines-in-textarea-and-pre>
      // See: <http://www.w3.org/TR/html5/syntax.html#element-restrictions>
      // See: <http://www.w3.org/TR/html5/syntax.html#newlines>
      // See: Parsing of "textarea" "listing" and "pre" elements
      //  from <http://www.w3.org/TR/html5/syntax.html#parsing-main-inbody>
      return '\n' + ret;
    } else {
      return ret;
    }
  },

  _createInitialChildren: function (transaction, props, context, el) {
    // Intentional use of != to avoid catching zero/false.
    var innerHTML = props.dangerouslySetInnerHTML;
    if (innerHTML != null) {
      if (innerHTML.__html != null) {
        setInnerHTML(el, innerHTML.__html);
      }
    } else {
      var contentToUse = CONTENT_TYPES[typeof props.children] ? props.children : null;
      var childrenToUse = contentToUse != null ? null : props.children;
      if (contentToUse != null) {
        // TODO: Validate that text is allowed as a child of this node
        setTextContent(el, contentToUse);
      } else if (childrenToUse != null) {
        var mountImages = this.mountChildren(childrenToUse, transaction, context);
        for (var i = 0; i < mountImages.length; i++) {
          el.appendChild(mountImages[i]);
        }
      }
    }
  },

  /**
   * Receives a next element and updates the component.
   *
   * @internal
   * @param {ReactElement} nextElement
   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
   * @param {object} context
   */
  receiveComponent: function (nextElement, transaction, context) {
    var prevElement = this._currentElement;
    this._currentElement = nextElement;
    this.updateComponent(transaction, prevElement, nextElement, context);
  },

  /**
   * Updates a native DOM component after it has already been allocated and
   * attached to the DOM. Reconciles the root DOM node, then recurses.
   *
   * @param {ReactReconcileTransaction} transaction
   * @param {ReactElement} prevElement
   * @param {ReactElement} nextElement
   * @internal
   * @overridable
   */
  updateComponent: function (transaction, prevElement, nextElement, context) {
    var lastProps = prevElement.props;
    var nextProps = this._currentElement.props;

    switch (this._tag) {
      case 'button':
        lastProps = ReactDOMButton.getNativeProps(this, lastProps);
        nextProps = ReactDOMButton.getNativeProps(this, nextProps);
        break;
      case 'input':
        ReactDOMInput.updateWrapper(this);
        lastProps = ReactDOMInput.getNativeProps(this, lastProps);
        nextProps = ReactDOMInput.getNativeProps(this, nextProps);
        break;
      case 'option':
        lastProps = ReactDOMOption.getNativeProps(this, lastProps);
        nextProps = ReactDOMOption.getNativeProps(this, nextProps);
        break;
      case 'select':
        lastProps = ReactDOMSelect.getNativeProps(this, lastProps);
        nextProps = ReactDOMSelect.getNativeProps(this, nextProps);
        break;
      case 'textarea':
        ReactDOMTextarea.updateWrapper(this);
        lastProps = ReactDOMTextarea.getNativeProps(this, lastProps);
        nextProps = ReactDOMTextarea.getNativeProps(this, nextProps);
        break;
    }

    if ("production" !== 'production') {
      // If the context is reference-equal to the old one, pass down the same
      // processed object so the update bailout in ReactReconciler behaves
      // correctly (and identically in dev and prod). See #5005.
      if (this._unprocessedContextDev !== context) {
        this._unprocessedContextDev = context;
        this._processedContextDev = processChildContextDev(context, this);
      }
      context = this._processedContextDev;
    }

    assertValidProps(this, nextProps);
    this._updateDOMProperties(lastProps, nextProps, transaction, null);
    this._updateDOMChildren(lastProps, nextProps, transaction, context);

    if (!canDefineProperty && this._nodeWithLegacyProperties) {
      this._nodeWithLegacyProperties.props = nextProps;
    }

    if (this._tag === 'select') {
      // <select> value update needs to occur after <option> children
      // reconciliation
      transaction.getReactMountReady().enqueue(postUpdateSelectWrapper, this);
    }
  },

  /**
   * Reconciles the properties by detecting differences in property values and
   * updating the DOM as necessary. This function is probably the single most
   * critical path for performance optimization.
   *
   * TODO: Benchmark whether checking for changed values in memory actually
   *       improves performance (especially statically positioned elements).
   * TODO: Benchmark the effects of putting this at the top since 99% of props
   *       do not change for a given reconciliation.
   * TODO: Benchmark areas that can be improved with caching.
   *
   * @private
   * @param {object} lastProps
   * @param {object} nextProps
   * @param {ReactReconcileTransaction} transaction
   * @param {?DOMElement} node
   */
  _updateDOMProperties: function (lastProps, nextProps, transaction, node) {
    var propKey;
    var styleName;
    var styleUpdates;
    for (propKey in lastProps) {
      if (nextProps.hasOwnProperty(propKey) || !lastProps.hasOwnProperty(propKey)) {
        continue;
      }
      if (propKey === STYLE) {
        var lastStyle = this._previousStyleCopy;
        for (styleName in lastStyle) {
          if (lastStyle.hasOwnProperty(styleName)) {
            styleUpdates = styleUpdates || {};
            styleUpdates[styleName] = '';
          }
        }
        this._previousStyleCopy = null;
      } else if (registrationNameModules.hasOwnProperty(propKey)) {
        if (lastProps[propKey]) {
          // Only call deleteListener if there was a listener previously or
          // else willDeleteListener gets called when there wasn't actually a
          // listener (e.g., onClick={null})
          deleteListener(this._rootNodeID, propKey);
        }
      } else if (DOMProperty.properties[propKey] || DOMProperty.isCustomAttribute(propKey)) {
        if (!node) {
          node = ReactMount.getNode(this._rootNodeID);
        }
        DOMPropertyOperations.deleteValueForProperty(node, propKey);
      }
    }
    for (propKey in nextProps) {
      var nextProp = nextProps[propKey];
      var lastProp = propKey === STYLE ? this._previousStyleCopy : lastProps[propKey];
      if (!nextProps.hasOwnProperty(propKey) || nextProp === lastProp) {
        continue;
      }
      if (propKey === STYLE) {
        if (nextProp) {
          if ("production" !== 'production') {
            checkAndWarnForMutatedStyle(this._previousStyleCopy, this._previousStyle, this);
            this._previousStyle = nextProp;
          }
          nextProp = this._previousStyleCopy = assign({}, nextProp);
        } else {
          this._previousStyleCopy = null;
        }
        if (lastProp) {
          // Unset styles on `lastProp` but not on `nextProp`.
          for (styleName in lastProp) {
            if (lastProp.hasOwnProperty(styleName) && (!nextProp || !nextProp.hasOwnProperty(styleName))) {
              styleUpdates = styleUpdates || {};
              styleUpdates[styleName] = '';
            }
          }
          // Update styles that changed since `lastProp`.
          for (styleName in nextProp) {
            if (nextProp.hasOwnProperty(styleName) && lastProp[styleName] !== nextProp[styleName]) {
              styleUpdates = styleUpdates || {};
              styleUpdates[styleName] = nextProp[styleName];
            }
          }
        } else {
          // Relies on `updateStylesByID` not mutating `styleUpdates`.
          styleUpdates = nextProp;
        }
      } else if (registrationNameModules.hasOwnProperty(propKey)) {
        if (nextProp) {
          enqueuePutListener(this._rootNodeID, propKey, nextProp, transaction);
        } else if (lastProp) {
          deleteListener(this._rootNodeID, propKey);
        }
      } else if (isCustomComponent(this._tag, nextProps)) {
        if (!node) {
          node = ReactMount.getNode(this._rootNodeID);
        }
        if (propKey === CHILDREN) {
          nextProp = null;
        }
        DOMPropertyOperations.setValueForAttribute(node, propKey, nextProp);
      } else if (DOMProperty.properties[propKey] || DOMProperty.isCustomAttribute(propKey)) {
        if (!node) {
          node = ReactMount.getNode(this._rootNodeID);
        }
        // If we're updating to null or undefined, we should remove the property
        // from the DOM node instead of inadvertantly setting to a string. This
        // brings us in line with the same behavior we have on initial render.
        if (nextProp != null) {
          DOMPropertyOperations.setValueForProperty(node, propKey, nextProp);
        } else {
          DOMPropertyOperations.deleteValueForProperty(node, propKey);
        }
      }
    }
    if (styleUpdates) {
      if (!node) {
        node = ReactMount.getNode(this._rootNodeID);
      }
      CSSPropertyOperations.setValueForStyles(node, styleUpdates);
    }
  },

  /**
   * Reconciles the children with the various properties that affect the
   * children content.
   *
   * @param {object} lastProps
   * @param {object} nextProps
   * @param {ReactReconcileTransaction} transaction
   * @param {object} context
   */
  _updateDOMChildren: function (lastProps, nextProps, transaction, context) {
    var lastContent = CONTENT_TYPES[typeof lastProps.children] ? lastProps.children : null;
    var nextContent = CONTENT_TYPES[typeof nextProps.children] ? nextProps.children : null;

    var lastHtml = lastProps.dangerouslySetInnerHTML && lastProps.dangerouslySetInnerHTML.__html;
    var nextHtml = nextProps.dangerouslySetInnerHTML && nextProps.dangerouslySetInnerHTML.__html;

    // Note the use of `!=` which checks for null or undefined.
    var lastChildren = lastContent != null ? null : lastProps.children;
    var nextChildren = nextContent != null ? null : nextProps.children;

    // If we're switching from children to content/html or vice versa, remove
    // the old content
    var lastHasContentOrHtml = lastContent != null || lastHtml != null;
    var nextHasContentOrHtml = nextContent != null || nextHtml != null;
    if (lastChildren != null && nextChildren == null) {
      this.updateChildren(null, transaction, context);
    } else if (lastHasContentOrHtml && !nextHasContentOrHtml) {
      this.updateTextContent('');
    }

    if (nextContent != null) {
      if (lastContent !== nextContent) {
        this.updateTextContent('' + nextContent);
      }
    } else if (nextHtml != null) {
      if (lastHtml !== nextHtml) {
        this.updateMarkup('' + nextHtml);
      }
    } else if (nextChildren != null) {
      this.updateChildren(nextChildren, transaction, context);
    }
  },

  /**
   * Destroys all event registrations for this instance. Does not remove from
   * the DOM. That must be done by the parent.
   *
   * @internal
   */
  unmountComponent: function () {
    switch (this._tag) {
      case 'iframe':
      case 'img':
      case 'form':
      case 'video':
      case 'audio':
        var listeners = this._wrapperState.listeners;
        if (listeners) {
          for (var i = 0; i < listeners.length; i++) {
            listeners[i].remove();
          }
        }
        break;
      case 'input':
        ReactDOMInput.unmountWrapper(this);
        break;
      case 'html':
      case 'head':
      case 'body':
        /**
         * Components like <html> <head> and <body> can't be removed or added
         * easily in a cross-browser way, however it's valuable to be able to
         * take advantage of React's reconciliation for styling and <title>
         * management. So we just document it and throw in dangerous cases.
         */
        !false ? "production" !== 'production' ? invariant(false, '<%s> tried to unmount. Because of cross-browser quirks it is ' + 'impossible to unmount some top-level components (eg <html>, ' + '<head>, and <body>) reliably and efficiently. To fix this, have a ' + 'single top-level component that never unmounts render these ' + 'elements.', this._tag) : invariant(false) : undefined;
        break;
    }

    this.unmountChildren();
    ReactBrowserEventEmitter.deleteAllListeners(this._rootNodeID);
    ReactComponentBrowserEnvironment.unmountIDFromEnvironment(this._rootNodeID);
    this._rootNodeID = null;
    this._wrapperState = null;
    if (this._nodeWithLegacyProperties) {
      var node = this._nodeWithLegacyProperties;
      node._reactInternalComponent = null;
      this._nodeWithLegacyProperties = null;
    }
  },

  getPublicInstance: function () {
    if (!this._nodeWithLegacyProperties) {
      var node = ReactMount.getNode(this._rootNodeID);

      node._reactInternalComponent = this;
      node.getDOMNode = legacyGetDOMNode;
      node.isMounted = legacyIsMounted;
      node.setState = legacySetStateEtc;
      node.replaceState = legacySetStateEtc;
      node.forceUpdate = legacySetStateEtc;
      node.setProps = legacySetProps;
      node.replaceProps = legacyReplaceProps;

      if ("production" !== 'production') {
        if (canDefineProperty) {
          Object.defineProperties(node, legacyPropsDescriptor);
        } else {
          // updateComponent will update this property on subsequent renders
          node.props = this._currentElement.props;
        }
      } else {
        // updateComponent will update this property on subsequent renders
        node.props = this._currentElement.props;
      }

      this._nodeWithLegacyProperties = node;
    }
    return this._nodeWithLegacyProperties;
  }

};

ReactPerf.measureMethods(ReactDOMComponent, 'ReactDOMComponent', {
  mountComponent: 'mountComponent',
  updateComponent: 'updateComponent'
});

assign(ReactDOMComponent.prototype, ReactDOMComponent.Mixin, ReactMultiChild.Mixin);

module.exports = ReactDOMComponent;
},{"./AutoFocusUtils":228,"./CSSPropertyOperations":231,"./DOMProperty":236,"./DOMPropertyOperations":237,"./EventConstants":241,"./Object.assign":249,"./ReactBrowserEventEmitter":253,"./ReactComponentBrowserEnvironment":258,"./ReactDOMButton":263,"./ReactDOMInput":268,"./ReactDOMOption":269,"./ReactDOMSelect":270,"./ReactDOMTextarea":274,"./ReactMount":292,"./ReactMultiChild":293,"./ReactPerf":298,"./ReactUpdateQueue":309,"./canDefineProperty":331,"./escapeTextContentForBrowser":334,"./isEventSupported":346,"./setInnerHTML":351,"./setTextContent":352,"./validateDOMNesting":355,"fbjs/lib/invariant":151,"fbjs/lib/keyOf":155,"fbjs/lib/shallowEqual":160,"fbjs/lib/warning":162}],265:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactDOMFactories
 * @typechecks static-only
 */

'use strict';

var ReactElement = require('./ReactElement');
var ReactElementValidator = require('./ReactElementValidator');

var mapObject = require('fbjs/lib/mapObject');

/**
 * Create a factory that creates HTML tag elements.
 *
 * @param {string} tag Tag name (e.g. `div`).
 * @private
 */
function createDOMFactory(tag) {
  if ("production" !== 'production') {
    return ReactElementValidator.createFactory(tag);
  }
  return ReactElement.createFactory(tag);
}

/**
 * Creates a mapping from supported HTML tags to `ReactDOMComponent` classes.
 * This is also accessible via `React.DOM`.
 *
 * @public
 */
var ReactDOMFactories = mapObject({
  a: 'a',
  abbr: 'abbr',
  address: 'address',
  area: 'area',
  article: 'article',
  aside: 'aside',
  audio: 'audio',
  b: 'b',
  base: 'base',
  bdi: 'bdi',
  bdo: 'bdo',
  big: 'big',
  blockquote: 'blockquote',
  body: 'body',
  br: 'br',
  button: 'button',
  canvas: 'canvas',
  caption: 'caption',
  cite: 'cite',
  code: 'code',
  col: 'col',
  colgroup: 'colgroup',
  data: 'data',
  datalist: 'datalist',
  dd: 'dd',
  del: 'del',
  details: 'details',
  dfn: 'dfn',
  dialog: 'dialog',
  div: 'div',
  dl: 'dl',
  dt: 'dt',
  em: 'em',
  embed: 'embed',
  fieldset: 'fieldset',
  figcaption: 'figcaption',
  figure: 'figure',
  footer: 'footer',
  form: 'form',
  h1: 'h1',
  h2: 'h2',
  h3: 'h3',
  h4: 'h4',
  h5: 'h5',
  h6: 'h6',
  head: 'head',
  header: 'header',
  hgroup: 'hgroup',
  hr: 'hr',
  html: 'html',
  i: 'i',
  iframe: 'iframe',
  img: 'img',
  input: 'input',
  ins: 'ins',
  kbd: 'kbd',
  keygen: 'keygen',
  label: 'label',
  legend: 'legend',
  li: 'li',
  link: 'link',
  main: 'main',
  map: 'map',
  mark: 'mark',
  menu: 'menu',
  menuitem: 'menuitem',
  meta: 'meta',
  meter: 'meter',
  nav: 'nav',
  noscript: 'noscript',
  object: 'object',
  ol: 'ol',
  optgroup: 'optgroup',
  option: 'option',
  output: 'output',
  p: 'p',
  param: 'param',
  picture: 'picture',
  pre: 'pre',
  progress: 'progress',
  q: 'q',
  rp: 'rp',
  rt: 'rt',
  ruby: 'ruby',
  s: 's',
  samp: 'samp',
  script: 'script',
  section: 'section',
  select: 'select',
  small: 'small',
  source: 'source',
  span: 'span',
  strong: 'strong',
  style: 'style',
  sub: 'sub',
  summary: 'summary',
  sup: 'sup',
  table: 'table',
  tbody: 'tbody',
  td: 'td',
  textarea: 'textarea',
  tfoot: 'tfoot',
  th: 'th',
  thead: 'thead',
  time: 'time',
  title: 'title',
  tr: 'tr',
  track: 'track',
  u: 'u',
  ul: 'ul',
  'var': 'var',
  video: 'video',
  wbr: 'wbr',

  // SVG
  circle: 'circle',
  clipPath: 'clipPath',
  defs: 'defs',
  ellipse: 'ellipse',
  g: 'g',
  image: 'image',
  line: 'line',
  linearGradient: 'linearGradient',
  mask: 'mask',
  path: 'path',
  pattern: 'pattern',
  polygon: 'polygon',
  polyline: 'polyline',
  radialGradient: 'radialGradient',
  rect: 'rect',
  stop: 'stop',
  svg: 'svg',
  text: 'text',
  tspan: 'tspan'

}, createDOMFactory);

module.exports = ReactDOMFactories;
},{"./ReactElement":279,"./ReactElementValidator":280,"fbjs/lib/mapObject":156}],266:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactDOMFeatureFlags
 */

'use strict';

var ReactDOMFeatureFlags = {
  useCreateElement: false
};

module.exports = ReactDOMFeatureFlags;
},{}],267:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactDOMIDOperations
 * @typechecks static-only
 */

'use strict';

var DOMChildrenOperations = require('./DOMChildrenOperations');
var DOMPropertyOperations = require('./DOMPropertyOperations');
var ReactMount = require('./ReactMount');
var ReactPerf = require('./ReactPerf');

var invariant = require('fbjs/lib/invariant');

/**
 * Errors for properties that should not be updated with `updatePropertyByID()`.
 *
 * @type {object}
 * @private
 */
var INVALID_PROPERTY_ERRORS = {
  dangerouslySetInnerHTML: '`dangerouslySetInnerHTML` must be set using `updateInnerHTMLByID()`.',
  style: '`style` must be set using `updateStylesByID()`.'
};

/**
 * Operations used to process updates to DOM nodes.
 */
var ReactDOMIDOperations = {

  /**
   * Updates a DOM node with new property values. This should only be used to
   * update DOM properties in `DOMProperty`.
   *
   * @param {string} id ID of the node to update.
   * @param {string} name A valid property name, see `DOMProperty`.
   * @param {*} value New value of the property.
   * @internal
   */
  updatePropertyByID: function (id, name, value) {
    var node = ReactMount.getNode(id);
    !!INVALID_PROPERTY_ERRORS.hasOwnProperty(name) ? "production" !== 'production' ? invariant(false, 'updatePropertyByID(...): %s', INVALID_PROPERTY_ERRORS[name]) : invariant(false) : undefined;

    // If we're updating to null or undefined, we should remove the property
    // from the DOM node instead of inadvertantly setting to a string. This
    // brings us in line with the same behavior we have on initial render.
    if (value != null) {
      DOMPropertyOperations.setValueForProperty(node, name, value);
    } else {
      DOMPropertyOperations.deleteValueForProperty(node, name);
    }
  },

  /**
   * Replaces a DOM node that exists in the document with markup.
   *
   * @param {string} id ID of child to be replaced.
   * @param {string} markup Dangerous markup to inject in place of child.
   * @internal
   * @see {Danger.dangerouslyReplaceNodeWithMarkup}
   */
  dangerouslyReplaceNodeWithMarkupByID: function (id, markup) {
    var node = ReactMount.getNode(id);
    DOMChildrenOperations.dangerouslyReplaceNodeWithMarkup(node, markup);
  },

  /**
   * Updates a component's children by processing a series of updates.
   *
   * @param {array<object>} updates List of update configurations.
   * @param {array<string>} markup List of markup strings.
   * @internal
   */
  dangerouslyProcessChildrenUpdates: function (updates, markup) {
    for (var i = 0; i < updates.length; i++) {
      updates[i].parentNode = ReactMount.getNode(updates[i].parentID);
    }
    DOMChildrenOperations.processUpdates(updates, markup);
  }
};

ReactPerf.measureMethods(ReactDOMIDOperations, 'ReactDOMIDOperations', {
  dangerouslyReplaceNodeWithMarkupByID: 'dangerouslyReplaceNodeWithMarkupByID',
  dangerouslyProcessChildrenUpdates: 'dangerouslyProcessChildrenUpdates'
});

module.exports = ReactDOMIDOperations;
},{"./DOMChildrenOperations":235,"./DOMPropertyOperations":237,"./ReactMount":292,"./ReactPerf":298,"fbjs/lib/invariant":151}],268:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactDOMInput
 */

'use strict';

var ReactDOMIDOperations = require('./ReactDOMIDOperations');
var LinkedValueUtils = require('./LinkedValueUtils');
var ReactMount = require('./ReactMount');
var ReactUpdates = require('./ReactUpdates');

var assign = require('./Object.assign');
var invariant = require('fbjs/lib/invariant');

var instancesByReactID = {};

function forceUpdateIfMounted() {
  if (this._rootNodeID) {
    // DOM component is still mounted; update
    ReactDOMInput.updateWrapper(this);
  }
}

/**
 * Implements an <input> native component that allows setting these optional
 * props: `checked`, `value`, `defaultChecked`, and `defaultValue`.
 *
 * If `checked` or `value` are not supplied (or null/undefined), user actions
 * that affect the checked state or value will trigger updates to the element.
 *
 * If they are supplied (and not null/undefined), the rendered element will not
 * trigger updates to the element. Instead, the props must change in order for
 * the rendered element to be updated.
 *
 * The rendered element will be initialized as unchecked (or `defaultChecked`)
 * with an empty value (or `defaultValue`).
 *
 * @see http://www.w3.org/TR/2012/WD-html5-20121025/the-input-element.html
 */
var ReactDOMInput = {
  getNativeProps: function (inst, props, context) {
    var value = LinkedValueUtils.getValue(props);
    var checked = LinkedValueUtils.getChecked(props);

    var nativeProps = assign({}, props, {
      defaultChecked: undefined,
      defaultValue: undefined,
      value: value != null ? value : inst._wrapperState.initialValue,
      checked: checked != null ? checked : inst._wrapperState.initialChecked,
      onChange: inst._wrapperState.onChange
    });

    return nativeProps;
  },

  mountWrapper: function (inst, props) {
    if ("production" !== 'production') {
      LinkedValueUtils.checkPropTypes('input', props, inst._currentElement._owner);
    }

    var defaultValue = props.defaultValue;
    inst._wrapperState = {
      initialChecked: props.defaultChecked || false,
      initialValue: defaultValue != null ? defaultValue : null,
      onChange: _handleChange.bind(inst)
    };
  },

  mountReadyWrapper: function (inst) {
    // Can't be in mountWrapper or else server rendering leaks.
    instancesByReactID[inst._rootNodeID] = inst;
  },

  unmountWrapper: function (inst) {
    delete instancesByReactID[inst._rootNodeID];
  },

  updateWrapper: function (inst) {
    var props = inst._currentElement.props;

    // TODO: Shouldn't this be getChecked(props)?
    var checked = props.checked;
    if (checked != null) {
      ReactDOMIDOperations.updatePropertyByID(inst._rootNodeID, 'checked', checked || false);
    }

    var value = LinkedValueUtils.getValue(props);
    if (value != null) {
      // Cast `value` to a string to ensure the value is set correctly. While
      // browsers typically do this as necessary, jsdom doesn't.
      ReactDOMIDOperations.updatePropertyByID(inst._rootNodeID, 'value', '' + value);
    }
  }
};

function _handleChange(event) {
  var props = this._currentElement.props;

  var returnValue = LinkedValueUtils.executeOnChange(props, event);

  // Here we use asap to wait until all updates have propagated, which
  // is important when using controlled components within layers:
  // https://github.com/facebook/react/issues/1698
  ReactUpdates.asap(forceUpdateIfMounted, this);

  var name = props.name;
  if (props.type === 'radio' && name != null) {
    var rootNode = ReactMount.getNode(this._rootNodeID);
    var queryRoot = rootNode;

    while (queryRoot.parentNode) {
      queryRoot = queryRoot.parentNode;
    }

    // If `rootNode.form` was non-null, then we could try `form.elements`,
    // but that sometimes behaves strangely in IE8. We could also try using
    // `form.getElementsByName`, but that will only return direct children
    // and won't include inputs that use the HTML5 `form=` attribute. Since
    // the input might not even be in a form, let's just use the global
    // `querySelectorAll` to ensure we don't miss anything.
    var group = queryRoot.querySelectorAll('input[name=' + JSON.stringify('' + name) + '][type="radio"]');

    for (var i = 0; i < group.length; i++) {
      var otherNode = group[i];
      if (otherNode === rootNode || otherNode.form !== rootNode.form) {
        continue;
      }
      // This will throw if radio buttons rendered by different copies of React
      // and the same name are rendered into the same form (same as #1939).
      // That's probably okay; we don't support it just as we don't support
      // mixing React with non-React.
      var otherID = ReactMount.getID(otherNode);
      !otherID ? "production" !== 'production' ? invariant(false, 'ReactDOMInput: Mixing React and non-React radio inputs with the ' + 'same `name` is not supported.') : invariant(false) : undefined;
      var otherInstance = instancesByReactID[otherID];
      !otherInstance ? "production" !== 'production' ? invariant(false, 'ReactDOMInput: Unknown radio button ID %s.', otherID) : invariant(false) : undefined;
      // If this is a controlled radio button group, forcing the input that
      // was previously checked to update will cause it to be come re-checked
      // as appropriate.
      ReactUpdates.asap(forceUpdateIfMounted, otherInstance);
    }
  }

  return returnValue;
}

module.exports = ReactDOMInput;
},{"./LinkedValueUtils":248,"./Object.assign":249,"./ReactDOMIDOperations":267,"./ReactMount":292,"./ReactUpdates":310,"fbjs/lib/invariant":151}],269:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactDOMOption
 */

'use strict';

var ReactChildren = require('./ReactChildren');
var ReactDOMSelect = require('./ReactDOMSelect');

var assign = require('./Object.assign');
var warning = require('fbjs/lib/warning');

var valueContextKey = ReactDOMSelect.valueContextKey;

/**
 * Implements an <option> native component that warns when `selected` is set.
 */
var ReactDOMOption = {
  mountWrapper: function (inst, props, context) {
    // TODO (yungsters): Remove support for `selected` in <option>.
    if ("production" !== 'production') {
      "production" !== 'production' ? warning(props.selected == null, 'Use the `defaultValue` or `value` props on <select> instead of ' + 'setting `selected` on <option>.') : undefined;
    }

    // Look up whether this option is 'selected' via context
    var selectValue = context[valueContextKey];

    // If context key is null (e.g., no specified value or after initial mount)
    // or missing (e.g., for <datalist>), we don't change props.selected
    var selected = null;
    if (selectValue != null) {
      selected = false;
      if (Array.isArray(selectValue)) {
        // multiple
        for (var i = 0; i < selectValue.length; i++) {
          if ('' + selectValue[i] === '' + props.value) {
            selected = true;
            break;
          }
        }
      } else {
        selected = '' + selectValue === '' + props.value;
      }
    }

    inst._wrapperState = { selected: selected };
  },

  getNativeProps: function (inst, props, context) {
    var nativeProps = assign({ selected: undefined, children: undefined }, props);

    // Read state only from initial mount because <select> updates value
    // manually; we need the initial state only for server rendering
    if (inst._wrapperState.selected != null) {
      nativeProps.selected = inst._wrapperState.selected;
    }

    var content = '';

    // Flatten children and warn if they aren't strings or numbers;
    // invalid types are ignored.
    ReactChildren.forEach(props.children, function (child) {
      if (child == null) {
        return;
      }
      if (typeof child === 'string' || typeof child === 'number') {
        content += child;
      } else {
        "production" !== 'production' ? warning(false, 'Only strings and numbers are supported as <option> children.') : undefined;
      }
    });

    if (content) {
      nativeProps.children = content;
    }

    return nativeProps;
  }

};

module.exports = ReactDOMOption;
},{"./Object.assign":249,"./ReactChildren":255,"./ReactDOMSelect":270,"fbjs/lib/warning":162}],270:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactDOMSelect
 */

'use strict';

var LinkedValueUtils = require('./LinkedValueUtils');
var ReactMount = require('./ReactMount');
var ReactUpdates = require('./ReactUpdates');

var assign = require('./Object.assign');
var warning = require('fbjs/lib/warning');

var valueContextKey = '__ReactDOMSelect_value$' + Math.random().toString(36).slice(2);

function updateOptionsIfPendingUpdateAndMounted() {
  if (this._rootNodeID && this._wrapperState.pendingUpdate) {
    this._wrapperState.pendingUpdate = false;

    var props = this._currentElement.props;
    var value = LinkedValueUtils.getValue(props);

    if (value != null) {
      updateOptions(this, Boolean(props.multiple), value);
    }
  }
}

function getDeclarationErrorAddendum(owner) {
  if (owner) {
    var name = owner.getName();
    if (name) {
      return ' Check the render method of `' + name + '`.';
    }
  }
  return '';
}

var valuePropNames = ['value', 'defaultValue'];

/**
 * Validation function for `value` and `defaultValue`.
 * @private
 */
function checkSelectPropTypes(inst, props) {
  var owner = inst._currentElement._owner;
  LinkedValueUtils.checkPropTypes('select', props, owner);

  for (var i = 0; i < valuePropNames.length; i++) {
    var propName = valuePropNames[i];
    if (props[propName] == null) {
      continue;
    }
    if (props.multiple) {
      "production" !== 'production' ? warning(Array.isArray(props[propName]), 'The `%s` prop supplied to <select> must be an array if ' + '`multiple` is true.%s', propName, getDeclarationErrorAddendum(owner)) : undefined;
    } else {
      "production" !== 'production' ? warning(!Array.isArray(props[propName]), 'The `%s` prop supplied to <select> must be a scalar ' + 'value if `multiple` is false.%s', propName, getDeclarationErrorAddendum(owner)) : undefined;
    }
  }
}

/**
 * @param {ReactDOMComponent} inst
 * @param {boolean} multiple
 * @param {*} propValue A stringable (with `multiple`, a list of stringables).
 * @private
 */
function updateOptions(inst, multiple, propValue) {
  var selectedValue, i;
  var options = ReactMount.getNode(inst._rootNodeID).options;

  if (multiple) {
    selectedValue = {};
    for (i = 0; i < propValue.length; i++) {
      selectedValue['' + propValue[i]] = true;
    }
    for (i = 0; i < options.length; i++) {
      var selected = selectedValue.hasOwnProperty(options[i].value);
      if (options[i].selected !== selected) {
        options[i].selected = selected;
      }
    }
  } else {
    // Do not set `select.value` as exact behavior isn't consistent across all
    // browsers for all cases.
    selectedValue = '' + propValue;
    for (i = 0; i < options.length; i++) {
      if (options[i].value === selectedValue) {
        options[i].selected = true;
        return;
      }
    }
    if (options.length) {
      options[0].selected = true;
    }
  }
}

/**
 * Implements a <select> native component that allows optionally setting the
 * props `value` and `defaultValue`. If `multiple` is false, the prop must be a
 * stringable. If `multiple` is true, the prop must be an array of stringables.
 *
 * If `value` is not supplied (or null/undefined), user actions that change the
 * selected option will trigger updates to the rendered options.
 *
 * If it is supplied (and not null/undefined), the rendered options will not
 * update in response to user actions. Instead, the `value` prop must change in
 * order for the rendered options to update.
 *
 * If `defaultValue` is provided, any options with the supplied values will be
 * selected.
 */
var ReactDOMSelect = {
  valueContextKey: valueContextKey,

  getNativeProps: function (inst, props, context) {
    return assign({}, props, {
      onChange: inst._wrapperState.onChange,
      value: undefined
    });
  },

  mountWrapper: function (inst, props) {
    if ("production" !== 'production') {
      checkSelectPropTypes(inst, props);
    }

    var value = LinkedValueUtils.getValue(props);
    inst._wrapperState = {
      pendingUpdate: false,
      initialValue: value != null ? value : props.defaultValue,
      onChange: _handleChange.bind(inst),
      wasMultiple: Boolean(props.multiple)
    };
  },

  processChildContext: function (inst, props, context) {
    // Pass down initial value so initial generated markup has correct
    // `selected` attributes
    var childContext = assign({}, context);
    childContext[valueContextKey] = inst._wrapperState.initialValue;
    return childContext;
  },

  postUpdateWrapper: function (inst) {
    var props = inst._currentElement.props;

    // After the initial mount, we control selected-ness manually so don't pass
    // the context value down
    inst._wrapperState.initialValue = undefined;

    var wasMultiple = inst._wrapperState.wasMultiple;
    inst._wrapperState.wasMultiple = Boolean(props.multiple);

    var value = LinkedValueUtils.getValue(props);
    if (value != null) {
      inst._wrapperState.pendingUpdate = false;
      updateOptions(inst, Boolean(props.multiple), value);
    } else if (wasMultiple !== Boolean(props.multiple)) {
      // For simplicity, reapply `defaultValue` if `multiple` is toggled.
      if (props.defaultValue != null) {
        updateOptions(inst, Boolean(props.multiple), props.defaultValue);
      } else {
        // Revert the select back to its default unselected state.
        updateOptions(inst, Boolean(props.multiple), props.multiple ? [] : '');
      }
    }
  }
};

function _handleChange(event) {
  var props = this._currentElement.props;
  var returnValue = LinkedValueUtils.executeOnChange(props, event);

  this._wrapperState.pendingUpdate = true;
  ReactUpdates.asap(updateOptionsIfPendingUpdateAndMounted, this);
  return returnValue;
}

module.exports = ReactDOMSelect;
},{"./LinkedValueUtils":248,"./Object.assign":249,"./ReactMount":292,"./ReactUpdates":310,"fbjs/lib/warning":162}],271:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactDOMSelection
 */

'use strict';

var ExecutionEnvironment = require('fbjs/lib/ExecutionEnvironment');

var getNodeForCharacterOffset = require('./getNodeForCharacterOffset');
var getTextContentAccessor = require('./getTextContentAccessor');

/**
 * While `isCollapsed` is available on the Selection object and `collapsed`
 * is available on the Range object, IE11 sometimes gets them wrong.
 * If the anchor/focus nodes and offsets are the same, the range is collapsed.
 */
function isCollapsed(anchorNode, anchorOffset, focusNode, focusOffset) {
  return anchorNode === focusNode && anchorOffset === focusOffset;
}

/**
 * Get the appropriate anchor and focus node/offset pairs for IE.
 *
 * The catch here is that IE's selection API doesn't provide information
 * about whether the selection is forward or backward, so we have to
 * behave as though it's always forward.
 *
 * IE text differs from modern selection in that it behaves as though
 * block elements end with a new line. This means character offsets will
 * differ between the two APIs.
 *
 * @param {DOMElement} node
 * @return {object}
 */
function getIEOffsets(node) {
  var selection = document.selection;
  var selectedRange = selection.createRange();
  var selectedLength = selectedRange.text.length;

  // Duplicate selection so we can move range without breaking user selection.
  var fromStart = selectedRange.duplicate();
  fromStart.moveToElementText(node);
  fromStart.setEndPoint('EndToStart', selectedRange);

  var startOffset = fromStart.text.length;
  var endOffset = startOffset + selectedLength;

  return {
    start: startOffset,
    end: endOffset
  };
}

/**
 * @param {DOMElement} node
 * @return {?object}
 */
function getModernOffsets(node) {
  var selection = window.getSelection && window.getSelection();

  if (!selection || selection.rangeCount === 0) {
    return null;
  }

  var anchorNode = selection.anchorNode;
  var anchorOffset = selection.anchorOffset;
  var focusNode = selection.focusNode;
  var focusOffset = selection.focusOffset;

  var currentRange = selection.getRangeAt(0);

  // In Firefox, range.startContainer and range.endContainer can be "anonymous
  // divs", e.g. the up/down buttons on an <input type="number">. Anonymous
  // divs do not seem to expose properties, triggering a "Permission denied
  // error" if any of its properties are accessed. The only seemingly possible
  // way to avoid erroring is to access a property that typically works for
  // non-anonymous divs and catch any error that may otherwise arise. See
  // https://bugzilla.mozilla.org/show_bug.cgi?id=208427
  try {
    /* eslint-disable no-unused-expressions */
    currentRange.startContainer.nodeType;
    currentRange.endContainer.nodeType;
    /* eslint-enable no-unused-expressions */
  } catch (e) {
    return null;
  }

  // If the node and offset values are the same, the selection is collapsed.
  // `Selection.isCollapsed` is available natively, but IE sometimes gets
  // this value wrong.
  var isSelectionCollapsed = isCollapsed(selection.anchorNode, selection.anchorOffset, selection.focusNode, selection.focusOffset);

  var rangeLength = isSelectionCollapsed ? 0 : currentRange.toString().length;

  var tempRange = currentRange.cloneRange();
  tempRange.selectNodeContents(node);
  tempRange.setEnd(currentRange.startContainer, currentRange.startOffset);

  var isTempRangeCollapsed = isCollapsed(tempRange.startContainer, tempRange.startOffset, tempRange.endContainer, tempRange.endOffset);

  var start = isTempRangeCollapsed ? 0 : tempRange.toString().length;
  var end = start + rangeLength;

  // Detect whether the selection is backward.
  var detectionRange = document.createRange();
  detectionRange.setStart(anchorNode, anchorOffset);
  detectionRange.setEnd(focusNode, focusOffset);
  var isBackward = detectionRange.collapsed;

  return {
    start: isBackward ? end : start,
    end: isBackward ? start : end
  };
}

/**
 * @param {DOMElement|DOMTextNode} node
 * @param {object} offsets
 */
function setIEOffsets(node, offsets) {
  var range = document.selection.createRange().duplicate();
  var start, end;

  if (typeof offsets.end === 'undefined') {
    start = offsets.start;
    end = start;
  } else if (offsets.start > offsets.end) {
    start = offsets.end;
    end = offsets.start;
  } else {
    start = offsets.start;
    end = offsets.end;
  }

  range.moveToElementText(node);
  range.moveStart('character', start);
  range.setEndPoint('EndToStart', range);
  range.moveEnd('character', end - start);
  range.select();
}

/**
 * In modern non-IE browsers, we can support both forward and backward
 * selections.
 *
 * Note: IE10+ supports the Selection object, but it does not support
 * the `extend` method, which means that even in modern IE, it's not possible
 * to programatically create a backward selection. Thus, for all IE
 * versions, we use the old IE API to create our selections.
 *
 * @param {DOMElement|DOMTextNode} node
 * @param {object} offsets
 */
function setModernOffsets(node, offsets) {
  if (!window.getSelection) {
    return;
  }

  var selection = window.getSelection();
  var length = node[getTextContentAccessor()].length;
  var start = Math.min(offsets.start, length);
  var end = typeof offsets.end === 'undefined' ? start : Math.min(offsets.end, length);

  // IE 11 uses modern selection, but doesn't support the extend method.
  // Flip backward selections, so we can set with a single range.
  if (!selection.extend && start > end) {
    var temp = end;
    end = start;
    start = temp;
  }

  var startMarker = getNodeForCharacterOffset(node, start);
  var endMarker = getNodeForCharacterOffset(node, end);

  if (startMarker && endMarker) {
    var range = document.createRange();
    range.setStart(startMarker.node, startMarker.offset);
    selection.removeAllRanges();

    if (start > end) {
      selection.addRange(range);
      selection.extend(endMarker.node, endMarker.offset);
    } else {
      range.setEnd(endMarker.node, endMarker.offset);
      selection.addRange(range);
    }
  }
}

var useIEOffsets = ExecutionEnvironment.canUseDOM && 'selection' in document && !('getSelection' in window);

var ReactDOMSelection = {
  /**
   * @param {DOMElement} node
   */
  getOffsets: useIEOffsets ? getIEOffsets : getModernOffsets,

  /**
   * @param {DOMElement|DOMTextNode} node
   * @param {object} offsets
   */
  setOffsets: useIEOffsets ? setIEOffsets : setModernOffsets
};

module.exports = ReactDOMSelection;
},{"./getNodeForCharacterOffset":343,"./getTextContentAccessor":344,"fbjs/lib/ExecutionEnvironment":137}],272:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactDOMServer
 */

'use strict';

var ReactDefaultInjection = require('./ReactDefaultInjection');
var ReactServerRendering = require('./ReactServerRendering');
var ReactVersion = require('./ReactVersion');

ReactDefaultInjection.inject();

var ReactDOMServer = {
  renderToString: ReactServerRendering.renderToString,
  renderToStaticMarkup: ReactServerRendering.renderToStaticMarkup,
  version: ReactVersion
};

module.exports = ReactDOMServer;
},{"./ReactDefaultInjection":276,"./ReactServerRendering":307,"./ReactVersion":311}],273:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactDOMTextComponent
 * @typechecks static-only
 */

'use strict';

var DOMChildrenOperations = require('./DOMChildrenOperations');
var DOMPropertyOperations = require('./DOMPropertyOperations');
var ReactComponentBrowserEnvironment = require('./ReactComponentBrowserEnvironment');
var ReactMount = require('./ReactMount');

var assign = require('./Object.assign');
var escapeTextContentForBrowser = require('./escapeTextContentForBrowser');
var setTextContent = require('./setTextContent');
var validateDOMNesting = require('./validateDOMNesting');

/**
 * Text nodes violate a couple assumptions that React makes about components:
 *
 *  - When mounting text into the DOM, adjacent text nodes are merged.
 *  - Text nodes cannot be assigned a React root ID.
 *
 * This component is used to wrap strings in elements so that they can undergo
 * the same reconciliation that is applied to elements.
 *
 * TODO: Investigate representing React components in the DOM with text nodes.
 *
 * @class ReactDOMTextComponent
 * @extends ReactComponent
 * @internal
 */
var ReactDOMTextComponent = function (props) {
  // This constructor and its argument is currently used by mocks.
};

assign(ReactDOMTextComponent.prototype, {

  /**
   * @param {ReactText} text
   * @internal
   */
  construct: function (text) {
    // TODO: This is really a ReactText (ReactNode), not a ReactElement
    this._currentElement = text;
    this._stringText = '' + text;

    // Properties
    this._rootNodeID = null;
    this._mountIndex = 0;
  },

  /**
   * Creates the markup for this text node. This node is not intended to have
   * any features besides containing text content.
   *
   * @param {string} rootID DOM ID of the root node.
   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
   * @return {string} Markup for this text node.
   * @internal
   */
  mountComponent: function (rootID, transaction, context) {
    if ("production" !== 'production') {
      if (context[validateDOMNesting.ancestorInfoContextKey]) {
        validateDOMNesting('span', null, context[validateDOMNesting.ancestorInfoContextKey]);
      }
    }

    this._rootNodeID = rootID;
    if (transaction.useCreateElement) {
      var ownerDocument = context[ReactMount.ownerDocumentContextKey];
      var el = ownerDocument.createElement('span');
      DOMPropertyOperations.setAttributeForID(el, rootID);
      // Populate node cache
      ReactMount.getID(el);
      setTextContent(el, this._stringText);
      return el;
    } else {
      var escapedText = escapeTextContentForBrowser(this._stringText);

      if (transaction.renderToStaticMarkup) {
        // Normally we'd wrap this in a `span` for the reasons stated above, but
        // since this is a situation where React won't take over (static pages),
        // we can simply return the text as it is.
        return escapedText;
      }

      return '<span ' + DOMPropertyOperations.createMarkupForID(rootID) + '>' + escapedText + '</span>';
    }
  },

  /**
   * Updates this component by updating the text content.
   *
   * @param {ReactText} nextText The next text content
   * @param {ReactReconcileTransaction} transaction
   * @internal
   */
  receiveComponent: function (nextText, transaction) {
    if (nextText !== this._currentElement) {
      this._currentElement = nextText;
      var nextStringText = '' + nextText;
      if (nextStringText !== this._stringText) {
        // TODO: Save this as pending props and use performUpdateIfNecessary
        // and/or updateComponent to do the actual update for consistency with
        // other component types?
        this._stringText = nextStringText;
        var node = ReactMount.getNode(this._rootNodeID);
        DOMChildrenOperations.updateTextContent(node, nextStringText);
      }
    }
  },

  unmountComponent: function () {
    ReactComponentBrowserEnvironment.unmountIDFromEnvironment(this._rootNodeID);
  }

});

module.exports = ReactDOMTextComponent;
},{"./DOMChildrenOperations":235,"./DOMPropertyOperations":237,"./Object.assign":249,"./ReactComponentBrowserEnvironment":258,"./ReactMount":292,"./escapeTextContentForBrowser":334,"./setTextContent":352,"./validateDOMNesting":355}],274:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactDOMTextarea
 */

'use strict';

var LinkedValueUtils = require('./LinkedValueUtils');
var ReactDOMIDOperations = require('./ReactDOMIDOperations');
var ReactUpdates = require('./ReactUpdates');

var assign = require('./Object.assign');
var invariant = require('fbjs/lib/invariant');
var warning = require('fbjs/lib/warning');

function forceUpdateIfMounted() {
  if (this._rootNodeID) {
    // DOM component is still mounted; update
    ReactDOMTextarea.updateWrapper(this);
  }
}

/**
 * Implements a <textarea> native component that allows setting `value`, and
 * `defaultValue`. This differs from the traditional DOM API because value is
 * usually set as PCDATA children.
 *
 * If `value` is not supplied (or null/undefined), user actions that affect the
 * value will trigger updates to the element.
 *
 * If `value` is supplied (and not null/undefined), the rendered element will
 * not trigger updates to the element. Instead, the `value` prop must change in
 * order for the rendered element to be updated.
 *
 * The rendered element will be initialized with an empty value, the prop
 * `defaultValue` if specified, or the children content (deprecated).
 */
var ReactDOMTextarea = {
  getNativeProps: function (inst, props, context) {
    !(props.dangerouslySetInnerHTML == null) ? "production" !== 'production' ? invariant(false, '`dangerouslySetInnerHTML` does not make sense on <textarea>.') : invariant(false) : undefined;

    // Always set children to the same thing. In IE9, the selection range will
    // get reset if `textContent` is mutated.
    var nativeProps = assign({}, props, {
      defaultValue: undefined,
      value: undefined,
      children: inst._wrapperState.initialValue,
      onChange: inst._wrapperState.onChange
    });

    return nativeProps;
  },

  mountWrapper: function (inst, props) {
    if ("production" !== 'production') {
      LinkedValueUtils.checkPropTypes('textarea', props, inst._currentElement._owner);
    }

    var defaultValue = props.defaultValue;
    // TODO (yungsters): Remove support for children content in <textarea>.
    var children = props.children;
    if (children != null) {
      if ("production" !== 'production') {
        "production" !== 'production' ? warning(false, 'Use the `defaultValue` or `value` props instead of setting ' + 'children on <textarea>.') : undefined;
      }
      !(defaultValue == null) ? "production" !== 'production' ? invariant(false, 'If you supply `defaultValue` on a <textarea>, do not pass children.') : invariant(false) : undefined;
      if (Array.isArray(children)) {
        !(children.length <= 1) ? "production" !== 'production' ? invariant(false, '<textarea> can only have at most one child.') : invariant(false) : undefined;
        children = children[0];
      }

      defaultValue = '' + children;
    }
    if (defaultValue == null) {
      defaultValue = '';
    }
    var value = LinkedValueUtils.getValue(props);

    inst._wrapperState = {
      // We save the initial value so that `ReactDOMComponent` doesn't update
      // `textContent` (unnecessary since we update value).
      // The initial value can be a boolean or object so that's why it's
      // forced to be a string.
      initialValue: '' + (value != null ? value : defaultValue),
      onChange: _handleChange.bind(inst)
    };
  },

  updateWrapper: function (inst) {
    var props = inst._currentElement.props;
    var value = LinkedValueUtils.getValue(props);
    if (value != null) {
      // Cast `value` to a string to ensure the value is set correctly. While
      // browsers typically do this as necessary, jsdom doesn't.
      ReactDOMIDOperations.updatePropertyByID(inst._rootNodeID, 'value', '' + value);
    }
  }
};

function _handleChange(event) {
  var props = this._currentElement.props;
  var returnValue = LinkedValueUtils.executeOnChange(props, event);
  ReactUpdates.asap(forceUpdateIfMounted, this);
  return returnValue;
}

module.exports = ReactDOMTextarea;
},{"./LinkedValueUtils":248,"./Object.assign":249,"./ReactDOMIDOperations":267,"./ReactUpdates":310,"fbjs/lib/invariant":151,"fbjs/lib/warning":162}],275:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactDefaultBatchingStrategy
 */

'use strict';

var ReactUpdates = require('./ReactUpdates');
var Transaction = require('./Transaction');

var assign = require('./Object.assign');
var emptyFunction = require('fbjs/lib/emptyFunction');

var RESET_BATCHED_UPDATES = {
  initialize: emptyFunction,
  close: function () {
    ReactDefaultBatchingStrategy.isBatchingUpdates = false;
  }
};

var FLUSH_BATCHED_UPDATES = {
  initialize: emptyFunction,
  close: ReactUpdates.flushBatchedUpdates.bind(ReactUpdates)
};

var TRANSACTION_WRAPPERS = [FLUSH_BATCHED_UPDATES, RESET_BATCHED_UPDATES];

function ReactDefaultBatchingStrategyTransaction() {
  this.reinitializeTransaction();
}

assign(ReactDefaultBatchingStrategyTransaction.prototype, Transaction.Mixin, {
  getTransactionWrappers: function () {
    return TRANSACTION_WRAPPERS;
  }
});

var transaction = new ReactDefaultBatchingStrategyTransaction();

var ReactDefaultBatchingStrategy = {
  isBatchingUpdates: false,

  /**
   * Call the provided function in a context within which calls to `setState`
   * and friends are batched such that components aren't updated unnecessarily.
   */
  batchedUpdates: function (callback, a, b, c, d, e) {
    var alreadyBatchingUpdates = ReactDefaultBatchingStrategy.isBatchingUpdates;

    ReactDefaultBatchingStrategy.isBatchingUpdates = true;

    // The code is written this way to avoid extra allocations
    if (alreadyBatchingUpdates) {
      callback(a, b, c, d, e);
    } else {
      transaction.perform(callback, null, a, b, c, d, e);
    }
  }
};

module.exports = ReactDefaultBatchingStrategy;
},{"./Object.assign":249,"./ReactUpdates":310,"./Transaction":327,"fbjs/lib/emptyFunction":143}],276:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactDefaultInjection
 */

'use strict';

var BeforeInputEventPlugin = require('./BeforeInputEventPlugin');
var ChangeEventPlugin = require('./ChangeEventPlugin');
var ClientReactRootIndex = require('./ClientReactRootIndex');
var DefaultEventPluginOrder = require('./DefaultEventPluginOrder');
var EnterLeaveEventPlugin = require('./EnterLeaveEventPlugin');
var ExecutionEnvironment = require('fbjs/lib/ExecutionEnvironment');
var HTMLDOMPropertyConfig = require('./HTMLDOMPropertyConfig');
var ReactBrowserComponentMixin = require('./ReactBrowserComponentMixin');
var ReactComponentBrowserEnvironment = require('./ReactComponentBrowserEnvironment');
var ReactDefaultBatchingStrategy = require('./ReactDefaultBatchingStrategy');
var ReactDOMComponent = require('./ReactDOMComponent');
var ReactDOMTextComponent = require('./ReactDOMTextComponent');
var ReactEventListener = require('./ReactEventListener');
var ReactInjection = require('./ReactInjection');
var ReactInstanceHandles = require('./ReactInstanceHandles');
var ReactMount = require('./ReactMount');
var ReactReconcileTransaction = require('./ReactReconcileTransaction');
var SelectEventPlugin = require('./SelectEventPlugin');
var ServerReactRootIndex = require('./ServerReactRootIndex');
var SimpleEventPlugin = require('./SimpleEventPlugin');
var SVGDOMPropertyConfig = require('./SVGDOMPropertyConfig');

var alreadyInjected = false;

function inject() {
  if (alreadyInjected) {
    // TODO: This is currently true because these injections are shared between
    // the client and the server package. They should be built independently
    // and not share any injection state. Then this problem will be solved.
    return;
  }
  alreadyInjected = true;

  ReactInjection.EventEmitter.injectReactEventListener(ReactEventListener);

  /**
   * Inject modules for resolving DOM hierarchy and plugin ordering.
   */
  ReactInjection.EventPluginHub.injectEventPluginOrder(DefaultEventPluginOrder);
  ReactInjection.EventPluginHub.injectInstanceHandle(ReactInstanceHandles);
  ReactInjection.EventPluginHub.injectMount(ReactMount);

  /**
   * Some important event plugins included by default (without having to require
   * them).
   */
  ReactInjection.EventPluginHub.injectEventPluginsByName({
    SimpleEventPlugin: SimpleEventPlugin,
    EnterLeaveEventPlugin: EnterLeaveEventPlugin,
    ChangeEventPlugin: ChangeEventPlugin,
    SelectEventPlugin: SelectEventPlugin,
    BeforeInputEventPlugin: BeforeInputEventPlugin
  });

  ReactInjection.NativeComponent.injectGenericComponentClass(ReactDOMComponent);

  ReactInjection.NativeComponent.injectTextComponentClass(ReactDOMTextComponent);

  ReactInjection.Class.injectMixin(ReactBrowserComponentMixin);

  ReactInjection.DOMProperty.injectDOMPropertyConfig(HTMLDOMPropertyConfig);
  ReactInjection.DOMProperty.injectDOMPropertyConfig(SVGDOMPropertyConfig);

  ReactInjection.EmptyComponent.injectEmptyComponent('noscript');

  ReactInjection.Updates.injectReconcileTransaction(ReactReconcileTransaction);
  ReactInjection.Updates.injectBatchingStrategy(ReactDefaultBatchingStrategy);

  ReactInjection.RootIndex.injectCreateReactRootIndex(ExecutionEnvironment.canUseDOM ? ClientReactRootIndex.createReactRootIndex : ServerReactRootIndex.createReactRootIndex);

  ReactInjection.Component.injectEnvironment(ReactComponentBrowserEnvironment);

  if ("production" !== 'production') {
    var url = ExecutionEnvironment.canUseDOM && window.location.href || '';
    if (/[?&]react_perf\b/.test(url)) {
      var ReactDefaultPerf = require('./ReactDefaultPerf');
      ReactDefaultPerf.start();
    }
  }
}

module.exports = {
  inject: inject
};
},{"./BeforeInputEventPlugin":229,"./ChangeEventPlugin":233,"./ClientReactRootIndex":234,"./DefaultEventPluginOrder":239,"./EnterLeaveEventPlugin":240,"./HTMLDOMPropertyConfig":247,"./ReactBrowserComponentMixin":252,"./ReactComponentBrowserEnvironment":258,"./ReactDOMComponent":264,"./ReactDOMTextComponent":273,"./ReactDefaultBatchingStrategy":275,"./ReactDefaultPerf":277,"./ReactEventListener":285,"./ReactInjection":286,"./ReactInstanceHandles":288,"./ReactMount":292,"./ReactReconcileTransaction":302,"./SVGDOMPropertyConfig":312,"./SelectEventPlugin":313,"./ServerReactRootIndex":314,"./SimpleEventPlugin":315,"fbjs/lib/ExecutionEnvironment":137}],277:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactDefaultPerf
 * @typechecks static-only
 */

'use strict';

var DOMProperty = require('./DOMProperty');
var ReactDefaultPerfAnalysis = require('./ReactDefaultPerfAnalysis');
var ReactMount = require('./ReactMount');
var ReactPerf = require('./ReactPerf');

var performanceNow = require('fbjs/lib/performanceNow');

function roundFloat(val) {
  return Math.floor(val * 100) / 100;
}

function addValue(obj, key, val) {
  obj[key] = (obj[key] || 0) + val;
}

var ReactDefaultPerf = {
  _allMeasurements: [], // last item in the list is the current one
  _mountStack: [0],
  _injected: false,

  start: function () {
    if (!ReactDefaultPerf._injected) {
      ReactPerf.injection.injectMeasure(ReactDefaultPerf.measure);
    }

    ReactDefaultPerf._allMeasurements.length = 0;
    ReactPerf.enableMeasure = true;
  },

  stop: function () {
    ReactPerf.enableMeasure = false;
  },

  getLastMeasurements: function () {
    return ReactDefaultPerf._allMeasurements;
  },

  printExclusive: function (measurements) {
    measurements = measurements || ReactDefaultPerf._allMeasurements;
    var summary = ReactDefaultPerfAnalysis.getExclusiveSummary(measurements);
    console.table(summary.map(function (item) {
      return {
        'Component class name': item.componentName,
        'Total inclusive time (ms)': roundFloat(item.inclusive),
        'Exclusive mount time (ms)': roundFloat(item.exclusive),
        'Exclusive render time (ms)': roundFloat(item.render),
        'Mount time per instance (ms)': roundFloat(item.exclusive / item.count),
        'Render time per instance (ms)': roundFloat(item.render / item.count),
        'Instances': item.count
      };
    }));
    // TODO: ReactDefaultPerfAnalysis.getTotalTime() does not return the correct
    // number.
  },

  printInclusive: function (measurements) {
    measurements = measurements || ReactDefaultPerf._allMeasurements;
    var summary = ReactDefaultPerfAnalysis.getInclusiveSummary(measurements);
    console.table(summary.map(function (item) {
      return {
        'Owner > component': item.componentName,
        'Inclusive time (ms)': roundFloat(item.time),
        'Instances': item.count
      };
    }));
    console.log('Total time:', ReactDefaultPerfAnalysis.getTotalTime(measurements).toFixed(2) + ' ms');
  },

  getMeasurementsSummaryMap: function (measurements) {
    var summary = ReactDefaultPerfAnalysis.getInclusiveSummary(measurements, true);
    return summary.map(function (item) {
      return {
        'Owner > component': item.componentName,
        'Wasted time (ms)': item.time,
        'Instances': item.count
      };
    });
  },

  printWasted: function (measurements) {
    measurements = measurements || ReactDefaultPerf._allMeasurements;
    console.table(ReactDefaultPerf.getMeasurementsSummaryMap(measurements));
    console.log('Total time:', ReactDefaultPerfAnalysis.getTotalTime(measurements).toFixed(2) + ' ms');
  },

  printDOM: function (measurements) {
    measurements = measurements || ReactDefaultPerf._allMeasurements;
    var summary = ReactDefaultPerfAnalysis.getDOMSummary(measurements);
    console.table(summary.map(function (item) {
      var result = {};
      result[DOMProperty.ID_ATTRIBUTE_NAME] = item.id;
      result.type = item.type;
      result.args = JSON.stringify(item.args);
      return result;
    }));
    console.log('Total time:', ReactDefaultPerfAnalysis.getTotalTime(measurements).toFixed(2) + ' ms');
  },

  _recordWrite: function (id, fnName, totalTime, args) {
    // TODO: totalTime isn't that useful since it doesn't count paints/reflows
    var writes = ReactDefaultPerf._allMeasurements[ReactDefaultPerf._allMeasurements.length - 1].writes;
    writes[id] = writes[id] || [];
    writes[id].push({
      type: fnName,
      time: totalTime,
      args: args
    });
  },

  measure: function (moduleName, fnName, func) {
    return function () {
      for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
        args[_key] = arguments[_key];
      }

      var totalTime;
      var rv;
      var start;

      if (fnName === '_renderNewRootComponent' || fnName === 'flushBatchedUpdates') {
        // A "measurement" is a set of metrics recorded for each flush. We want
        // to group the metrics for a given flush together so we can look at the
        // components that rendered and the DOM operations that actually
        // happened to determine the amount of "wasted work" performed.
        ReactDefaultPerf._allMeasurements.push({
          exclusive: {},
          inclusive: {},
          render: {},
          counts: {},
          writes: {},
          displayNames: {},
          totalTime: 0,
          created: {}
        });
        start = performanceNow();
        rv = func.apply(this, args);
        ReactDefaultPerf._allMeasurements[ReactDefaultPerf._allMeasurements.length - 1].totalTime = performanceNow() - start;
        return rv;
      } else if (fnName === '_mountImageIntoNode' || moduleName === 'ReactBrowserEventEmitter' || moduleName === 'ReactDOMIDOperations' || moduleName === 'CSSPropertyOperations' || moduleName === 'DOMChildrenOperations' || moduleName === 'DOMPropertyOperations') {
        start = performanceNow();
        rv = func.apply(this, args);
        totalTime = performanceNow() - start;

        if (fnName === '_mountImageIntoNode') {
          var mountID = ReactMount.getID(args[1]);
          ReactDefaultPerf._recordWrite(mountID, fnName, totalTime, args[0]);
        } else if (fnName === 'dangerouslyProcessChildrenUpdates') {
          // special format
          args[0].forEach(function (update) {
            var writeArgs = {};
            if (update.fromIndex !== null) {
              writeArgs.fromIndex = update.fromIndex;
            }
            if (update.toIndex !== null) {
              writeArgs.toIndex = update.toIndex;
            }
            if (update.textContent !== null) {
              writeArgs.textContent = update.textContent;
            }
            if (update.markupIndex !== null) {
              writeArgs.markup = args[1][update.markupIndex];
            }
            ReactDefaultPerf._recordWrite(update.parentID, update.type, totalTime, writeArgs);
          });
        } else {
          // basic format
          var id = args[0];
          if (typeof id === 'object') {
            id = ReactMount.getID(args[0]);
          }
          ReactDefaultPerf._recordWrite(id, fnName, totalTime, Array.prototype.slice.call(args, 1));
        }
        return rv;
      } else if (moduleName === 'ReactCompositeComponent' && (fnName === 'mountComponent' || fnName === 'updateComponent' || // TODO: receiveComponent()?
      fnName === '_renderValidatedComponent')) {

        if (this._currentElement.type === ReactMount.TopLevelWrapper) {
          return func.apply(this, args);
        }

        var rootNodeID = fnName === 'mountComponent' ? args[0] : this._rootNodeID;
        var isRender = fnName === '_renderValidatedComponent';
        var isMount = fnName === 'mountComponent';

        var mountStack = ReactDefaultPerf._mountStack;
        var entry = ReactDefaultPerf._allMeasurements[ReactDefaultPerf._allMeasurements.length - 1];

        if (isRender) {
          addValue(entry.counts, rootNodeID, 1);
        } else if (isMount) {
          entry.created[rootNodeID] = true;
          mountStack.push(0);
        }

        start = performanceNow();
        rv = func.apply(this, args);
        totalTime = performanceNow() - start;

        if (isRender) {
          addValue(entry.render, rootNodeID, totalTime);
        } else if (isMount) {
          var subMountTime = mountStack.pop();
          mountStack[mountStack.length - 1] += totalTime;
          addValue(entry.exclusive, rootNodeID, totalTime - subMountTime);
          addValue(entry.inclusive, rootNodeID, totalTime);
        } else {
          addValue(entry.inclusive, rootNodeID, totalTime);
        }

        entry.displayNames[rootNodeID] = {
          current: this.getName(),
          owner: this._currentElement._owner ? this._currentElement._owner.getName() : '<root>'
        };

        return rv;
      } else {
        return func.apply(this, args);
      }
    };
  }
};

module.exports = ReactDefaultPerf;
},{"./DOMProperty":236,"./ReactDefaultPerfAnalysis":278,"./ReactMount":292,"./ReactPerf":298,"fbjs/lib/performanceNow":159}],278:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactDefaultPerfAnalysis
 */

'use strict';

var assign = require('./Object.assign');

// Don't try to save users less than 1.2ms (a number I made up)
var DONT_CARE_THRESHOLD = 1.2;
var DOM_OPERATION_TYPES = {
  '_mountImageIntoNode': 'set innerHTML',
  INSERT_MARKUP: 'set innerHTML',
  MOVE_EXISTING: 'move',
  REMOVE_NODE: 'remove',
  SET_MARKUP: 'set innerHTML',
  TEXT_CONTENT: 'set textContent',
  'setValueForProperty': 'update attribute',
  'setValueForAttribute': 'update attribute',
  'deleteValueForProperty': 'remove attribute',
  'setValueForStyles': 'update styles',
  'replaceNodeWithMarkup': 'replace',
  'updateTextContent': 'set textContent'
};

function getTotalTime(measurements) {
  // TODO: return number of DOM ops? could be misleading.
  // TODO: measure dropped frames after reconcile?
  // TODO: log total time of each reconcile and the top-level component
  // class that triggered it.
  var totalTime = 0;
  for (var i = 0; i < measurements.length; i++) {
    var measurement = measurements[i];
    totalTime += measurement.totalTime;
  }
  return totalTime;
}

function getDOMSummary(measurements) {
  var items = [];
  measurements.forEach(function (measurement) {
    Object.keys(measurement.writes).forEach(function (id) {
      measurement.writes[id].forEach(function (write) {
        items.push({
          id: id,
          type: DOM_OPERATION_TYPES[write.type] || write.type,
          args: write.args
        });
      });
    });
  });
  return items;
}

function getExclusiveSummary(measurements) {
  var candidates = {};
  var displayName;

  for (var i = 0; i < measurements.length; i++) {
    var measurement = measurements[i];
    var allIDs = assign({}, measurement.exclusive, measurement.inclusive);

    for (var id in allIDs) {
      displayName = measurement.displayNames[id].current;

      candidates[displayName] = candidates[displayName] || {
        componentName: displayName,
        inclusive: 0,
        exclusive: 0,
        render: 0,
        count: 0
      };
      if (measurement.render[id]) {
        candidates[displayName].render += measurement.render[id];
      }
      if (measurement.exclusive[id]) {
        candidates[displayName].exclusive += measurement.exclusive[id];
      }
      if (measurement.inclusive[id]) {
        candidates[displayName].inclusive += measurement.inclusive[id];
      }
      if (measurement.counts[id]) {
        candidates[displayName].count += measurement.counts[id];
      }
    }
  }

  // Now make a sorted array with the results.
  var arr = [];
  for (displayName in candidates) {
    if (candidates[displayName].exclusive >= DONT_CARE_THRESHOLD) {
      arr.push(candidates[displayName]);
    }
  }

  arr.sort(function (a, b) {
    return b.exclusive - a.exclusive;
  });

  return arr;
}

function getInclusiveSummary(measurements, onlyClean) {
  var candidates = {};
  var inclusiveKey;

  for (var i = 0; i < measurements.length; i++) {
    var measurement = measurements[i];
    var allIDs = assign({}, measurement.exclusive, measurement.inclusive);
    var cleanComponents;

    if (onlyClean) {
      cleanComponents = getUnchangedComponents(measurement);
    }

    for (var id in allIDs) {
      if (onlyClean && !cleanComponents[id]) {
        continue;
      }

      var displayName = measurement.displayNames[id];

      // Inclusive time is not useful for many components without knowing where
      // they are instantiated. So we aggregate inclusive time with both the
      // owner and current displayName as the key.
      inclusiveKey = displayName.owner + ' > ' + displayName.current;

      candidates[inclusiveKey] = candidates[inclusiveKey] || {
        componentName: inclusiveKey,
        time: 0,
        count: 0
      };

      if (measurement.inclusive[id]) {
        candidates[inclusiveKey].time += measurement.inclusive[id];
      }
      if (measurement.counts[id]) {
        candidates[inclusiveKey].count += measurement.counts[id];
      }
    }
  }

  // Now make a sorted array with the results.
  var arr = [];
  for (inclusiveKey in candidates) {
    if (candidates[inclusiveKey].time >= DONT_CARE_THRESHOLD) {
      arr.push(candidates[inclusiveKey]);
    }
  }

  arr.sort(function (a, b) {
    return b.time - a.time;
  });

  return arr;
}

function getUnchangedComponents(measurement) {
  // For a given reconcile, look at which components did not actually
  // render anything to the DOM and return a mapping of their ID to
  // the amount of time it took to render the entire subtree.
  var cleanComponents = {};
  var dirtyLeafIDs = Object.keys(measurement.writes);
  var allIDs = assign({}, measurement.exclusive, measurement.inclusive);

  for (var id in allIDs) {
    var isDirty = false;
    // For each component that rendered, see if a component that triggered
    // a DOM op is in its subtree.
    for (var i = 0; i < dirtyLeafIDs.length; i++) {
      if (dirtyLeafIDs[i].indexOf(id) === 0) {
        isDirty = true;
        break;
      }
    }
    // check if component newly created
    if (measurement.created[id]) {
      isDirty = true;
    }
    if (!isDirty && measurement.counts[id] > 0) {
      cleanComponents[id] = true;
    }
  }
  return cleanComponents;
}

var ReactDefaultPerfAnalysis = {
  getExclusiveSummary: getExclusiveSummary,
  getInclusiveSummary: getInclusiveSummary,
  getDOMSummary: getDOMSummary,
  getTotalTime: getTotalTime
};

module.exports = ReactDefaultPerfAnalysis;
},{"./Object.assign":249}],279:[function(require,module,exports){
/**
 * Copyright 2014-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactElement
 */

'use strict';

var ReactCurrentOwner = require('./ReactCurrentOwner');

var assign = require('./Object.assign');
var canDefineProperty = require('./canDefineProperty');

// The Symbol used to tag the ReactElement type. If there is no native Symbol
// nor polyfill, then a plain number is used for performance.
var REACT_ELEMENT_TYPE = typeof Symbol === 'function' && Symbol['for'] && Symbol['for']('react.element') || 0xeac7;

var RESERVED_PROPS = {
  key: true,
  ref: true,
  __self: true,
  __source: true
};

/**
 * Base constructor for all React elements. This is only used to make this
 * work with a dynamic instanceof check. Nothing should live on this prototype.
 *
 * @param {*} type
 * @param {*} key
 * @param {string|object} ref
 * @param {*} self A *temporary* helper to detect places where `this` is
 * different from the `owner` when React.createElement is called, so that we
 * can warn. We want to get rid of owner and replace string `ref`s with arrow
 * functions, and as long as `this` and owner are the same, there will be no
 * change in behavior.
 * @param {*} source An annotation object (added by a transpiler or otherwise)
 * indicating filename, line number, and/or other information.
 * @param {*} owner
 * @param {*} props
 * @internal
 */
var ReactElement = function (type, key, ref, self, source, owner, props) {
  var element = {
    // This tag allow us to uniquely identify this as a React Element
    $$typeof: REACT_ELEMENT_TYPE,

    // Built-in properties that belong on the element
    type: type,
    key: key,
    ref: ref,
    props: props,

    // Record the component responsible for creating this element.
    _owner: owner
  };

  if ("production" !== 'production') {
    // The validation flag is currently mutative. We put it on
    // an external backing store so that we can freeze the whole object.
    // This can be replaced with a WeakMap once they are implemented in
    // commonly used development environments.
    element._store = {};

    // To make comparing ReactElements easier for testing purposes, we make
    // the validation flag non-enumerable (where possible, which should
    // include every environment we run tests in), so the test framework
    // ignores it.
    if (canDefineProperty) {
      Object.defineProperty(element._store, 'validated', {
        configurable: false,
        enumerable: false,
        writable: true,
        value: false
      });
      // self and source are DEV only properties.
      Object.defineProperty(element, '_self', {
        configurable: false,
        enumerable: false,
        writable: false,
        value: self
      });
      // Two elements created in two different places should be considered
      // equal for testing purposes and therefore we hide it from enumeration.
      Object.defineProperty(element, '_source', {
        configurable: false,
        enumerable: false,
        writable: false,
        value: source
      });
    } else {
      element._store.validated = false;
      element._self = self;
      element._source = source;
    }
    Object.freeze(element.props);
    Object.freeze(element);
  }

  return element;
};

ReactElement.createElement = function (type, config, children) {
  var propName;

  // Reserved names are extracted
  var props = {};

  var key = null;
  var ref = null;
  var self = null;
  var source = null;

  if (config != null) {
    ref = config.ref === undefined ? null : config.ref;
    key = config.key === undefined ? null : '' + config.key;
    self = config.__self === undefined ? null : config.__self;
    source = config.__source === undefined ? null : config.__source;
    // Remaining properties are added to a new props object
    for (propName in config) {
      if (config.hasOwnProperty(propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
        props[propName] = config[propName];
      }
    }
  }

  // Children can be more than one argument, and those are transferred onto
  // the newly allocated props object.
  var childrenLength = arguments.length - 2;
  if (childrenLength === 1) {
    props.children = children;
  } else if (childrenLength > 1) {
    var childArray = Array(childrenLength);
    for (var i = 0; i < childrenLength; i++) {
      childArray[i] = arguments[i + 2];
    }
    props.children = childArray;
  }

  // Resolve default props
  if (type && type.defaultProps) {
    var defaultProps = type.defaultProps;
    for (propName in defaultProps) {
      if (typeof props[propName] === 'undefined') {
        props[propName] = defaultProps[propName];
      }
    }
  }

  return ReactElement(type, key, ref, self, source, ReactCurrentOwner.current, props);
};

ReactElement.createFactory = function (type) {
  var factory = ReactElement.createElement.bind(null, type);
  // Expose the type on the factory and the prototype so that it can be
  // easily accessed on elements. E.g. `<Foo />.type === Foo`.
  // This should not be named `constructor` since this may not be the function
  // that created the element, and it may not even be a constructor.
  // Legacy hook TODO: Warn if this is accessed
  factory.type = type;
  return factory;
};

ReactElement.cloneAndReplaceKey = function (oldElement, newKey) {
  var newElement = ReactElement(oldElement.type, newKey, oldElement.ref, oldElement._self, oldElement._source, oldElement._owner, oldElement.props);

  return newElement;
};

ReactElement.cloneAndReplaceProps = function (oldElement, newProps) {
  var newElement = ReactElement(oldElement.type, oldElement.key, oldElement.ref, oldElement._self, oldElement._source, oldElement._owner, newProps);

  if ("production" !== 'production') {
    // If the key on the original is valid, then the clone is valid
    newElement._store.validated = oldElement._store.validated;
  }

  return newElement;
};

ReactElement.cloneElement = function (element, config, children) {
  var propName;

  // Original props are copied
  var props = assign({}, element.props);

  // Reserved names are extracted
  var key = element.key;
  var ref = element.ref;
  // Self is preserved since the owner is preserved.
  var self = element._self;
  // Source is preserved since cloneElement is unlikely to be targeted by a
  // transpiler, and the original source is probably a better indicator of the
  // true owner.
  var source = element._source;

  // Owner will be preserved, unless ref is overridden
  var owner = element._owner;

  if (config != null) {
    if (config.ref !== undefined) {
      // Silently steal the ref from the parent.
      ref = config.ref;
      owner = ReactCurrentOwner.current;
    }
    if (config.key !== undefined) {
      key = '' + config.key;
    }
    // Remaining properties override existing props
    for (propName in config) {
      if (config.hasOwnProperty(propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
        props[propName] = config[propName];
      }
    }
  }

  // Children can be more than one argument, and those are transferred onto
  // the newly allocated props object.
  var childrenLength = arguments.length - 2;
  if (childrenLength === 1) {
    props.children = children;
  } else if (childrenLength > 1) {
    var childArray = Array(childrenLength);
    for (var i = 0; i < childrenLength; i++) {
      childArray[i] = arguments[i + 2];
    }
    props.children = childArray;
  }

  return ReactElement(element.type, key, ref, self, source, owner, props);
};

/**
 * @param {?object} object
 * @return {boolean} True if `object` is a valid component.
 * @final
 */
ReactElement.isValidElement = function (object) {
  return typeof object === 'object' && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
};

module.exports = ReactElement;
},{"./Object.assign":249,"./ReactCurrentOwner":261,"./canDefineProperty":331}],280:[function(require,module,exports){
/**
 * Copyright 2014-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactElementValidator
 */

/**
 * ReactElementValidator provides a wrapper around a element factory
 * which validates the props passed to the element. This is intended to be
 * used only in DEV and could be replaced by a static type checker for languages
 * that support it.
 */

'use strict';

var ReactElement = require('./ReactElement');
var ReactPropTypeLocations = require('./ReactPropTypeLocations');
var ReactPropTypeLocationNames = require('./ReactPropTypeLocationNames');
var ReactCurrentOwner = require('./ReactCurrentOwner');

var canDefineProperty = require('./canDefineProperty');
var getIteratorFn = require('./getIteratorFn');
var invariant = require('fbjs/lib/invariant');
var warning = require('fbjs/lib/warning');

function getDeclarationErrorAddendum() {
  if (ReactCurrentOwner.current) {
    var name = ReactCurrentOwner.current.getName();
    if (name) {
      return ' Check the render method of `' + name + '`.';
    }
  }
  return '';
}

/**
 * Warn if there's no key explicitly set on dynamic arrays of children or
 * object keys are not valid. This allows us to keep track of children between
 * updates.
 */
var ownerHasKeyUseWarning = {};

var loggedTypeFailures = {};

/**
 * Warn if the element doesn't have an explicit key assigned to it.
 * This element is in an array. The array could grow and shrink or be
 * reordered. All children that haven't already been validated are required to
 * have a "key" property assigned to it.
 *
 * @internal
 * @param {ReactElement} element Element that requires a key.
 * @param {*} parentType element's parent's type.
 */
function validateExplicitKey(element, parentType) {
  if (!element._store || element._store.validated || element.key != null) {
    return;
  }
  element._store.validated = true;

  var addenda = getAddendaForKeyUse('uniqueKey', element, parentType);
  if (addenda === null) {
    // we already showed the warning
    return;
  }
  "production" !== 'production' ? warning(false, 'Each child in an array or iterator should have a unique "key" prop.' + '%s%s%s', addenda.parentOrOwner || '', addenda.childOwner || '', addenda.url || '') : undefined;
}

/**
 * Shared warning and monitoring code for the key warnings.
 *
 * @internal
 * @param {string} messageType A key used for de-duping warnings.
 * @param {ReactElement} element Component that requires a key.
 * @param {*} parentType element's parent's type.
 * @returns {?object} A set of addenda to use in the warning message, or null
 * if the warning has already been shown before (and shouldn't be shown again).
 */
function getAddendaForKeyUse(messageType, element, parentType) {
  var addendum = getDeclarationErrorAddendum();
  if (!addendum) {
    var parentName = typeof parentType === 'string' ? parentType : parentType.displayName || parentType.name;
    if (parentName) {
      addendum = ' Check the top-level render call using <' + parentName + '>.';
    }
  }

  var memoizer = ownerHasKeyUseWarning[messageType] || (ownerHasKeyUseWarning[messageType] = {});
  if (memoizer[addendum]) {
    return null;
  }
  memoizer[addendum] = true;

  var addenda = {
    parentOrOwner: addendum,
    url: ' See https://fb.me/react-warning-keys for more information.',
    childOwner: null
  };

  // Usually the current owner is the offender, but if it accepts children as a
  // property, it may be the creator of the child that's responsible for
  // assigning it a key.
  if (element && element._owner && element._owner !== ReactCurrentOwner.current) {
    // Give the component that originally created this child.
    addenda.childOwner = ' It was passed a child from ' + element._owner.getName() + '.';
  }

  return addenda;
}

/**
 * Ensure that every element either is passed in a static location, in an
 * array with an explicit keys property defined, or in an object literal
 * with valid key property.
 *
 * @internal
 * @param {ReactNode} node Statically passed child of any type.
 * @param {*} parentType node's parent's type.
 */
function validateChildKeys(node, parentType) {
  if (typeof node !== 'object') {
    return;
  }
  if (Array.isArray(node)) {
    for (var i = 0; i < node.length; i++) {
      var child = node[i];
      if (ReactElement.isValidElement(child)) {
        validateExplicitKey(child, parentType);
      }
    }
  } else if (ReactElement.isValidElement(node)) {
    // This element was passed in a valid location.
    if (node._store) {
      node._store.validated = true;
    }
  } else if (node) {
    var iteratorFn = getIteratorFn(node);
    // Entry iterators provide implicit keys.
    if (iteratorFn) {
      if (iteratorFn !== node.entries) {
        var iterator = iteratorFn.call(node);
        var step;
        while (!(step = iterator.next()).done) {
          if (ReactElement.isValidElement(step.value)) {
            validateExplicitKey(step.value, parentType);
          }
        }
      }
    }
  }
}

/**
 * Assert that the props are valid
 *
 * @param {string} componentName Name of the component for error messages.
 * @param {object} propTypes Map of prop name to a ReactPropType
 * @param {object} props
 * @param {string} location e.g. "prop", "context", "child context"
 * @private
 */
function checkPropTypes(componentName, propTypes, props, location) {
  for (var propName in propTypes) {
    if (propTypes.hasOwnProperty(propName)) {
      var error;
      // Prop type validation may throw. In case they do, we don't want to
      // fail the render phase where it didn't fail before. So we log it.
      // After these have been cleaned up, we'll let them throw.
      try {
        // This is intentionally an invariant that gets caught. It's the same
        // behavior as without this statement except with a better message.
        !(typeof propTypes[propName] === 'function') ? "production" !== 'production' ? invariant(false, '%s: %s type `%s` is invalid; it must be a function, usually from ' + 'React.PropTypes.', componentName || 'React class', ReactPropTypeLocationNames[location], propName) : invariant(false) : undefined;
        error = propTypes[propName](props, propName, componentName, location);
      } catch (ex) {
        error = ex;
      }
      "production" !== 'production' ? warning(!error || error instanceof Error, '%s: type specification of %s `%s` is invalid; the type checker ' + 'function must return `null` or an `Error` but returned a %s. ' + 'You may have forgotten to pass an argument to the type checker ' + 'creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and ' + 'shape all require an argument).', componentName || 'React class', ReactPropTypeLocationNames[location], propName, typeof error) : undefined;
      if (error instanceof Error && !(error.message in loggedTypeFailures)) {
        // Only monitor this failure once because there tends to be a lot of the
        // same error.
        loggedTypeFailures[error.message] = true;

        var addendum = getDeclarationErrorAddendum();
        "production" !== 'production' ? warning(false, 'Failed propType: %s%s', error.message, addendum) : undefined;
      }
    }
  }
}

/**
 * Given an element, validate that its props follow the propTypes definition,
 * provided by the type.
 *
 * @param {ReactElement} element
 */
function validatePropTypes(element) {
  var componentClass = element.type;
  if (typeof componentClass !== 'function') {
    return;
  }
  var name = componentClass.displayName || componentClass.name;
  if (componentClass.propTypes) {
    checkPropTypes(name, componentClass.propTypes, element.props, ReactPropTypeLocations.prop);
  }
  if (typeof componentClass.getDefaultProps === 'function') {
    "production" !== 'production' ? warning(componentClass.getDefaultProps.isReactClassApproved, 'getDefaultProps is only used on classic React.createClass ' + 'definitions. Use a static property named `defaultProps` instead.') : undefined;
  }
}

var ReactElementValidator = {

  createElement: function (type, props, children) {
    var validType = typeof type === 'string' || typeof type === 'function';
    // We warn in this case but don't throw. We expect the element creation to
    // succeed and there will likely be errors in render.
    "production" !== 'production' ? warning(validType, 'React.createElement: type should not be null, undefined, boolean, or ' + 'number. It should be a string (for DOM elements) or a ReactClass ' + '(for composite components).%s', getDeclarationErrorAddendum()) : undefined;

    var element = ReactElement.createElement.apply(this, arguments);

    // The result can be nullish if a mock or a custom function is used.
    // TODO: Drop this when these are no longer allowed as the type argument.
    if (element == null) {
      return element;
    }

    // Skip key warning if the type isn't valid since our key validation logic
    // doesn't expect a non-string/function type and can throw confusing errors.
    // We don't want exception behavior to differ between dev and prod.
    // (Rendering will throw with a helpful message and as soon as the type is
    // fixed, the key warnings will appear.)
    if (validType) {
      for (var i = 2; i < arguments.length; i++) {
        validateChildKeys(arguments[i], type);
      }
    }

    validatePropTypes(element);

    return element;
  },

  createFactory: function (type) {
    var validatedFactory = ReactElementValidator.createElement.bind(null, type);
    // Legacy hook TODO: Warn if this is accessed
    validatedFactory.type = type;

    if ("production" !== 'production') {
      if (canDefineProperty) {
        Object.defineProperty(validatedFactory, 'type', {
          enumerable: false,
          get: function () {
            "production" !== 'production' ? warning(false, 'Factory.type is deprecated. Access the class directly ' + 'before passing it to createFactory.') : undefined;
            Object.defineProperty(this, 'type', {
              value: type
            });
            return type;
          }
        });
      }
    }

    return validatedFactory;
  },

  cloneElement: function (element, props, children) {
    var newElement = ReactElement.cloneElement.apply(this, arguments);
    for (var i = 2; i < arguments.length; i++) {
      validateChildKeys(arguments[i], newElement.type);
    }
    validatePropTypes(newElement);
    return newElement;
  }

};

module.exports = ReactElementValidator;
},{"./ReactCurrentOwner":261,"./ReactElement":279,"./ReactPropTypeLocationNames":299,"./ReactPropTypeLocations":300,"./canDefineProperty":331,"./getIteratorFn":342,"fbjs/lib/invariant":151,"fbjs/lib/warning":162}],281:[function(require,module,exports){
/**
 * Copyright 2014-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactEmptyComponent
 */

'use strict';

var ReactElement = require('./ReactElement');
var ReactEmptyComponentRegistry = require('./ReactEmptyComponentRegistry');
var ReactReconciler = require('./ReactReconciler');

var assign = require('./Object.assign');

var placeholderElement;

var ReactEmptyComponentInjection = {
  injectEmptyComponent: function (component) {
    placeholderElement = ReactElement.createElement(component);
  }
};

function registerNullComponentID() {
  ReactEmptyComponentRegistry.registerNullComponentID(this._rootNodeID);
}

var ReactEmptyComponent = function (instantiate) {
  this._currentElement = null;
  this._rootNodeID = null;
  this._renderedComponent = instantiate(placeholderElement);
};
assign(ReactEmptyComponent.prototype, {
  construct: function (element) {},
  mountComponent: function (rootID, transaction, context) {
    transaction.getReactMountReady().enqueue(registerNullComponentID, this);
    this._rootNodeID = rootID;
    return ReactReconciler.mountComponent(this._renderedComponent, rootID, transaction, context);
  },
  receiveComponent: function () {},
  unmountComponent: function (rootID, transaction, context) {
    ReactReconciler.unmountComponent(this._renderedComponent);
    ReactEmptyComponentRegistry.deregisterNullComponentID(this._rootNodeID);
    this._rootNodeID = null;
    this._renderedComponent = null;
  }
});

ReactEmptyComponent.injection = ReactEmptyComponentInjection;

module.exports = ReactEmptyComponent;
},{"./Object.assign":249,"./ReactElement":279,"./ReactEmptyComponentRegistry":282,"./ReactReconciler":303}],282:[function(require,module,exports){
/**
 * Copyright 2014-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactEmptyComponentRegistry
 */

'use strict';

// This registry keeps track of the React IDs of the components that rendered to
// `null` (in reality a placeholder such as `noscript`)
var nullComponentIDsRegistry = {};

/**
 * @param {string} id Component's `_rootNodeID`.
 * @return {boolean} True if the component is rendered to null.
 */
function isNullComponentID(id) {
  return !!nullComponentIDsRegistry[id];
}

/**
 * Mark the component as having rendered to null.
 * @param {string} id Component's `_rootNodeID`.
 */
function registerNullComponentID(id) {
  nullComponentIDsRegistry[id] = true;
}

/**
 * Unmark the component as having rendered to null: it renders to something now.
 * @param {string} id Component's `_rootNodeID`.
 */
function deregisterNullComponentID(id) {
  delete nullComponentIDsRegistry[id];
}

var ReactEmptyComponentRegistry = {
  isNullComponentID: isNullComponentID,
  registerNullComponentID: registerNullComponentID,
  deregisterNullComponentID: deregisterNullComponentID
};

module.exports = ReactEmptyComponentRegistry;
},{}],283:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactErrorUtils
 * @typechecks
 */

'use strict';

var caughtError = null;

/**
 * Call a function while guarding against errors that happens within it.
 *
 * @param {?String} name of the guard to use for logging or debugging
 * @param {Function} func The function to invoke
 * @param {*} a First argument
 * @param {*} b Second argument
 */
function invokeGuardedCallback(name, func, a, b) {
  try {
    return func(a, b);
  } catch (x) {
    if (caughtError === null) {
      caughtError = x;
    }
    return undefined;
  }
}

var ReactErrorUtils = {
  invokeGuardedCallback: invokeGuardedCallback,

  /**
   * Invoked by ReactTestUtils.Simulate so that any errors thrown by the event
   * handler are sure to be rethrown by rethrowCaughtError.
   */
  invokeGuardedCallbackWithCatch: invokeGuardedCallback,

  /**
   * During execution of guarded functions we will capture the first error which
   * we will rethrow to be handled by the top level error handler.
   */
  rethrowCaughtError: function () {
    if (caughtError) {
      var error = caughtError;
      caughtError = null;
      throw error;
    }
  }
};

if ("production" !== 'production') {
  /**
   * To help development we can get better devtools integration by simulating a
   * real browser event.
   */
  if (typeof window !== 'undefined' && typeof window.dispatchEvent === 'function' && typeof document !== 'undefined' && typeof document.createEvent === 'function') {
    var fakeNode = document.createElement('react');
    ReactErrorUtils.invokeGuardedCallback = function (name, func, a, b) {
      var boundFunc = func.bind(null, a, b);
      var evtType = 'react-' + name;
      fakeNode.addEventListener(evtType, boundFunc, false);
      var evt = document.createEvent('Event');
      evt.initEvent(evtType, false, false);
      fakeNode.dispatchEvent(evt);
      fakeNode.removeEventListener(evtType, boundFunc, false);
    };
  }
}

module.exports = ReactErrorUtils;
},{}],284:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactEventEmitterMixin
 */

'use strict';

var EventPluginHub = require('./EventPluginHub');

function runEventQueueInBatch(events) {
  EventPluginHub.enqueueEvents(events);
  EventPluginHub.processEventQueue(false);
}

var ReactEventEmitterMixin = {

  /**
   * Streams a fired top-level event to `EventPluginHub` where plugins have the
   * opportunity to create `ReactEvent`s to be dispatched.
   *
   * @param {string} topLevelType Record from `EventConstants`.
   * @param {object} topLevelTarget The listening component root node.
   * @param {string} topLevelTargetID ID of `topLevelTarget`.
   * @param {object} nativeEvent Native environment event.
   */
  handleTopLevel: function (topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget) {
    var events = EventPluginHub.extractEvents(topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget);
    runEventQueueInBatch(events);
  }
};

module.exports = ReactEventEmitterMixin;
},{"./EventPluginHub":242}],285:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactEventListener
 * @typechecks static-only
 */

'use strict';

var EventListener = require('fbjs/lib/EventListener');
var ExecutionEnvironment = require('fbjs/lib/ExecutionEnvironment');
var PooledClass = require('./PooledClass');
var ReactInstanceHandles = require('./ReactInstanceHandles');
var ReactMount = require('./ReactMount');
var ReactUpdates = require('./ReactUpdates');

var assign = require('./Object.assign');
var getEventTarget = require('./getEventTarget');
var getUnboundedScrollPosition = require('fbjs/lib/getUnboundedScrollPosition');

var DOCUMENT_FRAGMENT_NODE_TYPE = 11;

/**
 * Finds the parent React component of `node`.
 *
 * @param {*} node
 * @return {?DOMEventTarget} Parent container, or `null` if the specified node
 *                           is not nested.
 */
function findParent(node) {
  // TODO: It may be a good idea to cache this to prevent unnecessary DOM
  // traversal, but caching is difficult to do correctly without using a
  // mutation observer to listen for all DOM changes.
  var nodeID = ReactMount.getID(node);
  var rootID = ReactInstanceHandles.getReactRootIDFromNodeID(nodeID);
  var container = ReactMount.findReactContainerForID(rootID);
  var parent = ReactMount.getFirstReactDOM(container);
  return parent;
}

// Used to store ancestor hierarchy in top level callback
function TopLevelCallbackBookKeeping(topLevelType, nativeEvent) {
  this.topLevelType = topLevelType;
  this.nativeEvent = nativeEvent;
  this.ancestors = [];
}
assign(TopLevelCallbackBookKeeping.prototype, {
  destructor: function () {
    this.topLevelType = null;
    this.nativeEvent = null;
    this.ancestors.length = 0;
  }
});
PooledClass.addPoolingTo(TopLevelCallbackBookKeeping, PooledClass.twoArgumentPooler);

function handleTopLevelImpl(bookKeeping) {
  // TODO: Re-enable event.path handling
  //
  // if (bookKeeping.nativeEvent.path && bookKeeping.nativeEvent.path.length > 1) {
  //   // New browsers have a path attribute on native events
  //   handleTopLevelWithPath(bookKeeping);
  // } else {
  //   // Legacy browsers don't have a path attribute on native events
  //   handleTopLevelWithoutPath(bookKeeping);
  // }

  void handleTopLevelWithPath; // temporarily unused
  handleTopLevelWithoutPath(bookKeeping);
}

// Legacy browsers don't have a path attribute on native events
function handleTopLevelWithoutPath(bookKeeping) {
  var topLevelTarget = ReactMount.getFirstReactDOM(getEventTarget(bookKeeping.nativeEvent)) || window;

  // Loop through the hierarchy, in case there's any nested components.
  // It's important that we build the array of ancestors before calling any
  // event handlers, because event handlers can modify the DOM, leading to
  // inconsistencies with ReactMount's node cache. See #1105.
  var ancestor = topLevelTarget;
  while (ancestor) {
    bookKeeping.ancestors.push(ancestor);
    ancestor = findParent(ancestor);
  }

  for (var i = 0; i < bookKeeping.ancestors.length; i++) {
    topLevelTarget = bookKeeping.ancestors[i];
    var topLevelTargetID = ReactMount.getID(topLevelTarget) || '';
    ReactEventListener._handleTopLevel(bookKeeping.topLevelType, topLevelTarget, topLevelTargetID, bookKeeping.nativeEvent, getEventTarget(bookKeeping.nativeEvent));
  }
}

// New browsers have a path attribute on native events
function handleTopLevelWithPath(bookKeeping) {
  var path = bookKeeping.nativeEvent.path;
  var currentNativeTarget = path[0];
  var eventsFired = 0;
  for (var i = 0; i < path.length; i++) {
    var currentPathElement = path[i];
    if (currentPathElement.nodeType === DOCUMENT_FRAGMENT_NODE_TYPE) {
      currentNativeTarget = path[i + 1];
    }
    // TODO: slow
    var reactParent = ReactMount.getFirstReactDOM(currentPathElement);
    if (reactParent === currentPathElement) {
      var currentPathElementID = ReactMount.getID(currentPathElement);
      var newRootID = ReactInstanceHandles.getReactRootIDFromNodeID(currentPathElementID);
      bookKeeping.ancestors.push(currentPathElement);

      var topLevelTargetID = ReactMount.getID(currentPathElement) || '';
      eventsFired++;
      ReactEventListener._handleTopLevel(bookKeeping.topLevelType, currentPathElement, topLevelTargetID, bookKeeping.nativeEvent, currentNativeTarget);

      // Jump to the root of this React render tree
      while (currentPathElementID !== newRootID) {
        i++;
        currentPathElement = path[i];
        currentPathElementID = ReactMount.getID(currentPathElement);
      }
    }
  }
  if (eventsFired === 0) {
    ReactEventListener._handleTopLevel(bookKeeping.topLevelType, window, '', bookKeeping.nativeEvent, getEventTarget(bookKeeping.nativeEvent));
  }
}

function scrollValueMonitor(cb) {
  var scrollPosition = getUnboundedScrollPosition(window);
  cb(scrollPosition);
}

var ReactEventListener = {
  _enabled: true,
  _handleTopLevel: null,

  WINDOW_HANDLE: ExecutionEnvironment.canUseDOM ? window : null,

  setHandleTopLevel: function (handleTopLevel) {
    ReactEventListener._handleTopLevel = handleTopLevel;
  },

  setEnabled: function (enabled) {
    ReactEventListener._enabled = !!enabled;
  },

  isEnabled: function () {
    return ReactEventListener._enabled;
  },

  /**
   * Traps top-level events by using event bubbling.
   *
   * @param {string} topLevelType Record from `EventConstants`.
   * @param {string} handlerBaseName Event name (e.g. "click").
   * @param {object} handle Element on which to attach listener.
   * @return {?object} An object with a remove function which will forcefully
   *                  remove the listener.
   * @internal
   */
  trapBubbledEvent: function (topLevelType, handlerBaseName, handle) {
    var element = handle;
    if (!element) {
      return null;
    }
    return EventListener.listen(element, handlerBaseName, ReactEventListener.dispatchEvent.bind(null, topLevelType));
  },

  /**
   * Traps a top-level event by using event capturing.
   *
   * @param {string} topLevelType Record from `EventConstants`.
   * @param {string} handlerBaseName Event name (e.g. "click").
   * @param {object} handle Element on which to attach listener.
   * @return {?object} An object with a remove function which will forcefully
   *                  remove the listener.
   * @internal
   */
  trapCapturedEvent: function (topLevelType, handlerBaseName, handle) {
    var element = handle;
    if (!element) {
      return null;
    }
    return EventListener.capture(element, handlerBaseName, ReactEventListener.dispatchEvent.bind(null, topLevelType));
  },

  monitorScrollValue: function (refresh) {
    var callback = scrollValueMonitor.bind(null, refresh);
    EventListener.listen(window, 'scroll', callback);
  },

  dispatchEvent: function (topLevelType, nativeEvent) {
    if (!ReactEventListener._enabled) {
      return;
    }

    var bookKeeping = TopLevelCallbackBookKeeping.getPooled(topLevelType, nativeEvent);
    try {
      // Event queue being processed in the same cycle allows
      // `preventDefault`.
      ReactUpdates.batchedUpdates(handleTopLevelImpl, bookKeeping);
    } finally {
      TopLevelCallbackBookKeeping.release(bookKeeping);
    }
  }
};

module.exports = ReactEventListener;
},{"./Object.assign":249,"./PooledClass":250,"./ReactInstanceHandles":288,"./ReactMount":292,"./ReactUpdates":310,"./getEventTarget":341,"fbjs/lib/EventListener":136,"fbjs/lib/ExecutionEnvironment":137,"fbjs/lib/getUnboundedScrollPosition":148}],286:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactInjection
 */

'use strict';

var DOMProperty = require('./DOMProperty');
var EventPluginHub = require('./EventPluginHub');
var ReactComponentEnvironment = require('./ReactComponentEnvironment');
var ReactClass = require('./ReactClass');
var ReactEmptyComponent = require('./ReactEmptyComponent');
var ReactBrowserEventEmitter = require('./ReactBrowserEventEmitter');
var ReactNativeComponent = require('./ReactNativeComponent');
var ReactPerf = require('./ReactPerf');
var ReactRootIndex = require('./ReactRootIndex');
var ReactUpdates = require('./ReactUpdates');

var ReactInjection = {
  Component: ReactComponentEnvironment.injection,
  Class: ReactClass.injection,
  DOMProperty: DOMProperty.injection,
  EmptyComponent: ReactEmptyComponent.injection,
  EventPluginHub: EventPluginHub.injection,
  EventEmitter: ReactBrowserEventEmitter.injection,
  NativeComponent: ReactNativeComponent.injection,
  Perf: ReactPerf.injection,
  RootIndex: ReactRootIndex.injection,
  Updates: ReactUpdates.injection
};

module.exports = ReactInjection;
},{"./DOMProperty":236,"./EventPluginHub":242,"./ReactBrowserEventEmitter":253,"./ReactClass":256,"./ReactComponentEnvironment":259,"./ReactEmptyComponent":281,"./ReactNativeComponent":295,"./ReactPerf":298,"./ReactRootIndex":305,"./ReactUpdates":310}],287:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactInputSelection
 */

'use strict';

var ReactDOMSelection = require('./ReactDOMSelection');

var containsNode = require('fbjs/lib/containsNode');
var focusNode = require('fbjs/lib/focusNode');
var getActiveElement = require('fbjs/lib/getActiveElement');

function isInDocument(node) {
  return containsNode(document.documentElement, node);
}

/**
 * @ReactInputSelection: React input selection module. Based on Selection.js,
 * but modified to be suitable for react and has a couple of bug fixes (doesn't
 * assume buttons have range selections allowed).
 * Input selection module for React.
 */
var ReactInputSelection = {

  hasSelectionCapabilities: function (elem) {
    var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();
    return nodeName && (nodeName === 'input' && elem.type === 'text' || nodeName === 'textarea' || elem.contentEditable === 'true');
  },

  getSelectionInformation: function () {
    var focusedElem = getActiveElement();
    return {
      focusedElem: focusedElem,
      selectionRange: ReactInputSelection.hasSelectionCapabilities(focusedElem) ? ReactInputSelection.getSelection(focusedElem) : null
    };
  },

  /**
   * @restoreSelection: If any selection information was potentially lost,
   * restore it. This is useful when performing operations that could remove dom
   * nodes and place them back in, resulting in focus being lost.
   */
  restoreSelection: function (priorSelectionInformation) {
    var curFocusedElem = getActiveElement();
    var priorFocusedElem = priorSelectionInformation.focusedElem;
    var priorSelectionRange = priorSelectionInformation.selectionRange;
    if (curFocusedElem !== priorFocusedElem && isInDocument(priorFocusedElem)) {
      if (ReactInputSelection.hasSelectionCapabilities(priorFocusedElem)) {
        ReactInputSelection.setSelection(priorFocusedElem, priorSelectionRange);
      }
      focusNode(priorFocusedElem);
    }
  },

  /**
   * @getSelection: Gets the selection bounds of a focused textarea, input or
   * contentEditable node.
   * -@input: Look up selection bounds of this input
   * -@return {start: selectionStart, end: selectionEnd}
   */
  getSelection: function (input) {
    var selection;

    if ('selectionStart' in input) {
      // Modern browser with input or textarea.
      selection = {
        start: input.selectionStart,
        end: input.selectionEnd
      };
    } else if (document.selection && (input.nodeName && input.nodeName.toLowerCase() === 'input')) {
      // IE8 input.
      var range = document.selection.createRange();
      // There can only be one selection per document in IE, so it must
      // be in our element.
      if (range.parentElement() === input) {
        selection = {
          start: -range.moveStart('character', -input.value.length),
          end: -range.moveEnd('character', -input.value.length)
        };
      }
    } else {
      // Content editable or old IE textarea.
      selection = ReactDOMSelection.getOffsets(input);
    }

    return selection || { start: 0, end: 0 };
  },

  /**
   * @setSelection: Sets the selection bounds of a textarea or input and focuses
   * the input.
   * -@input     Set selection bounds of this input or textarea
   * -@offsets   Object of same form that is returned from get*
   */
  setSelection: function (input, offsets) {
    var start = offsets.start;
    var end = offsets.end;
    if (typeof end === 'undefined') {
      end = start;
    }

    if ('selectionStart' in input) {
      input.selectionStart = start;
      input.selectionEnd = Math.min(end, input.value.length);
    } else if (document.selection && (input.nodeName && input.nodeName.toLowerCase() === 'input')) {
      var range = input.createTextRange();
      range.collapse(true);
      range.moveStart('character', start);
      range.moveEnd('character', end - start);
      range.select();
    } else {
      ReactDOMSelection.setOffsets(input, offsets);
    }
  }
};

module.exports = ReactInputSelection;
},{"./ReactDOMSelection":271,"fbjs/lib/containsNode":140,"fbjs/lib/focusNode":145,"fbjs/lib/getActiveElement":146}],288:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactInstanceHandles
 * @typechecks static-only
 */

'use strict';

var ReactRootIndex = require('./ReactRootIndex');

var invariant = require('fbjs/lib/invariant');

var SEPARATOR = '.';
var SEPARATOR_LENGTH = SEPARATOR.length;

/**
 * Maximum depth of traversals before we consider the possibility of a bad ID.
 */
var MAX_TREE_DEPTH = 10000;

/**
 * Creates a DOM ID prefix to use when mounting React components.
 *
 * @param {number} index A unique integer
 * @return {string} React root ID.
 * @internal
 */
function getReactRootIDString(index) {
  return SEPARATOR + index.toString(36);
}

/**
 * Checks if a character in the supplied ID is a separator or the end.
 *
 * @param {string} id A React DOM ID.
 * @param {number} index Index of the character to check.
 * @return {boolean} True if the character is a separator or end of the ID.
 * @private
 */
function isBoundary(id, index) {
  return id.charAt(index) === SEPARATOR || index === id.length;
}

/**
 * Checks if the supplied string is a valid React DOM ID.
 *
 * @param {string} id A React DOM ID, maybe.
 * @return {boolean} True if the string is a valid React DOM ID.
 * @private
 */
function isValidID(id) {
  return id === '' || id.charAt(0) === SEPARATOR && id.charAt(id.length - 1) !== SEPARATOR;
}

/**
 * Checks if the first ID is an ancestor of or equal to the second ID.
 *
 * @param {string} ancestorID
 * @param {string} descendantID
 * @return {boolean} True if `ancestorID` is an ancestor of `descendantID`.
 * @internal
 */
function isAncestorIDOf(ancestorID, descendantID) {
  return descendantID.indexOf(ancestorID) === 0 && isBoundary(descendantID, ancestorID.length);
}

/**
 * Gets the parent ID of the supplied React DOM ID, `id`.
 *
 * @param {string} id ID of a component.
 * @return {string} ID of the parent, or an empty string.
 * @private
 */
function getParentID(id) {
  return id ? id.substr(0, id.lastIndexOf(SEPARATOR)) : '';
}

/**
 * Gets the next DOM ID on the tree path from the supplied `ancestorID` to the
 * supplied `destinationID`. If they are equal, the ID is returned.
 *
 * @param {string} ancestorID ID of an ancestor node of `destinationID`.
 * @param {string} destinationID ID of the destination node.
 * @return {string} Next ID on the path from `ancestorID` to `destinationID`.
 * @private
 */
function getNextDescendantID(ancestorID, destinationID) {
  !(isValidID(ancestorID) && isValidID(destinationID)) ? "production" !== 'production' ? invariant(false, 'getNextDescendantID(%s, %s): Received an invalid React DOM ID.', ancestorID, destinationID) : invariant(false) : undefined;
  !isAncestorIDOf(ancestorID, destinationID) ? "production" !== 'production' ? invariant(false, 'getNextDescendantID(...): React has made an invalid assumption about ' + 'the DOM hierarchy. Expected `%s` to be an ancestor of `%s`.', ancestorID, destinationID) : invariant(false) : undefined;
  if (ancestorID === destinationID) {
    return ancestorID;
  }
  // Skip over the ancestor and the immediate separator. Traverse until we hit
  // another separator or we reach the end of `destinationID`.
  var start = ancestorID.length + SEPARATOR_LENGTH;
  var i;
  for (i = start; i < destinationID.length; i++) {
    if (isBoundary(destinationID, i)) {
      break;
    }
  }
  return destinationID.substr(0, i);
}

/**
 * Gets the nearest common ancestor ID of two IDs.
 *
 * Using this ID scheme, the nearest common ancestor ID is the longest common
 * prefix of the two IDs that immediately preceded a "marker" in both strings.
 *
 * @param {string} oneID
 * @param {string} twoID
 * @return {string} Nearest common ancestor ID, or the empty string if none.
 * @private
 */
function getFirstCommonAncestorID(oneID, twoID) {
  var minLength = Math.min(oneID.length, twoID.length);
  if (minLength === 0) {
    return '';
  }
  var lastCommonMarkerIndex = 0;
  // Use `<=` to traverse until the "EOL" of the shorter string.
  for (var i = 0; i <= minLength; i++) {
    if (isBoundary(oneID, i) && isBoundary(twoID, i)) {
      lastCommonMarkerIndex = i;
    } else if (oneID.charAt(i) !== twoID.charAt(i)) {
      break;
    }
  }
  var longestCommonID = oneID.substr(0, lastCommonMarkerIndex);
  !isValidID(longestCommonID) ? "production" !== 'production' ? invariant(false, 'getFirstCommonAncestorID(%s, %s): Expected a valid React DOM ID: %s', oneID, twoID, longestCommonID) : invariant(false) : undefined;
  return longestCommonID;
}

/**
 * Traverses the parent path between two IDs (either up or down). The IDs must
 * not be the same, and there must exist a parent path between them. If the
 * callback returns `false`, traversal is stopped.
 *
 * @param {?string} start ID at which to start traversal.
 * @param {?string} stop ID at which to end traversal.
 * @param {function} cb Callback to invoke each ID with.
 * @param {*} arg Argument to invoke the callback with.
 * @param {?boolean} skipFirst Whether or not to skip the first node.
 * @param {?boolean} skipLast Whether or not to skip the last node.
 * @private
 */
function traverseParentPath(start, stop, cb, arg, skipFirst, skipLast) {
  start = start || '';
  stop = stop || '';
  !(start !== stop) ? "production" !== 'production' ? invariant(false, 'traverseParentPath(...): Cannot traverse from and to the same ID, `%s`.', start) : invariant(false) : undefined;
  var traverseUp = isAncestorIDOf(stop, start);
  !(traverseUp || isAncestorIDOf(start, stop)) ? "production" !== 'production' ? invariant(false, 'traverseParentPath(%s, %s, ...): Cannot traverse from two IDs that do ' + 'not have a parent path.', start, stop) : invariant(false) : undefined;
  // Traverse from `start` to `stop` one depth at a time.
  var depth = 0;
  var traverse = traverseUp ? getParentID : getNextDescendantID;
  for (var id = start;; /* until break */id = traverse(id, stop)) {
    var ret;
    if ((!skipFirst || id !== start) && (!skipLast || id !== stop)) {
      ret = cb(id, traverseUp, arg);
    }
    if (ret === false || id === stop) {
      // Only break //after// visiting `stop`.
      break;
    }
    !(depth++ < MAX_TREE_DEPTH) ? "production" !== 'production' ? invariant(false, 'traverseParentPath(%s, %s, ...): Detected an infinite loop while ' + 'traversing the React DOM ID tree. This may be due to malformed IDs: %s', start, stop, id) : invariant(false) : undefined;
  }
}

/**
 * Manages the IDs assigned to DOM representations of React components. This
 * uses a specific scheme in order to traverse the DOM efficiently (e.g. in
 * order to simulate events).
 *
 * @internal
 */
var ReactInstanceHandles = {

  /**
   * Constructs a React root ID
   * @return {string} A React root ID.
   */
  createReactRootID: function () {
    return getReactRootIDString(ReactRootIndex.createReactRootIndex());
  },

  /**
   * Constructs a React ID by joining a root ID with a name.
   *
   * @param {string} rootID Root ID of a parent component.
   * @param {string} name A component's name (as flattened children).
   * @return {string} A React ID.
   * @internal
   */
  createReactID: function (rootID, name) {
    return rootID + name;
  },

  /**
   * Gets the DOM ID of the React component that is the root of the tree that
   * contains the React component with the supplied DOM ID.
   *
   * @param {string} id DOM ID of a React component.
   * @return {?string} DOM ID of the React component that is the root.
   * @internal
   */
  getReactRootIDFromNodeID: function (id) {
    if (id && id.charAt(0) === SEPARATOR && id.length > 1) {
      var index = id.indexOf(SEPARATOR, 1);
      return index > -1 ? id.substr(0, index) : id;
    }
    return null;
  },

  /**
   * Traverses the ID hierarchy and invokes the supplied `cb` on any IDs that
   * should would receive a `mouseEnter` or `mouseLeave` event.
   *
   * NOTE: Does not invoke the callback on the nearest common ancestor because
   * nothing "entered" or "left" that element.
   *
   * @param {string} leaveID ID being left.
   * @param {string} enterID ID being entered.
   * @param {function} cb Callback to invoke on each entered/left ID.
   * @param {*} upArg Argument to invoke the callback with on left IDs.
   * @param {*} downArg Argument to invoke the callback with on entered IDs.
   * @internal
   */
  traverseEnterLeave: function (leaveID, enterID, cb, upArg, downArg) {
    var ancestorID = getFirstCommonAncestorID(leaveID, enterID);
    if (ancestorID !== leaveID) {
      traverseParentPath(leaveID, ancestorID, cb, upArg, false, true);
    }
    if (ancestorID !== enterID) {
      traverseParentPath(ancestorID, enterID, cb, downArg, true, false);
    }
  },

  /**
   * Simulates the traversal of a two-phase, capture/bubble event dispatch.
   *
   * NOTE: This traversal happens on IDs without touching the DOM.
   *
   * @param {string} targetID ID of the target node.
   * @param {function} cb Callback to invoke.
   * @param {*} arg Argument to invoke the callback with.
   * @internal
   */
  traverseTwoPhase: function (targetID, cb, arg) {
    if (targetID) {
      traverseParentPath('', targetID, cb, arg, true, false);
      traverseParentPath(targetID, '', cb, arg, false, true);
    }
  },

  /**
   * Same as `traverseTwoPhase` but skips the `targetID`.
   */
  traverseTwoPhaseSkipTarget: function (targetID, cb, arg) {
    if (targetID) {
      traverseParentPath('', targetID, cb, arg, true, true);
      traverseParentPath(targetID, '', cb, arg, true, true);
    }
  },

  /**
   * Traverse a node ID, calling the supplied `cb` for each ancestor ID. For
   * example, passing `.0.$row-0.1` would result in `cb` getting called
   * with `.0`, `.0.$row-0`, and `.0.$row-0.1`.
   *
   * NOTE: This traversal happens on IDs without touching the DOM.
   *
   * @param {string} targetID ID of the target node.
   * @param {function} cb Callback to invoke.
   * @param {*} arg Argument to invoke the callback with.
   * @internal
   */
  traverseAncestors: function (targetID, cb, arg) {
    traverseParentPath('', targetID, cb, arg, true, false);
  },

  getFirstCommonAncestorID: getFirstCommonAncestorID,

  /**
   * Exposed for unit testing.
   * @private
   */
  _getNextDescendantID: getNextDescendantID,

  isAncestorIDOf: isAncestorIDOf,

  SEPARATOR: SEPARATOR

};

module.exports = ReactInstanceHandles;
},{"./ReactRootIndex":305,"fbjs/lib/invariant":151}],289:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactInstanceMap
 */

'use strict';

/**
 * `ReactInstanceMap` maintains a mapping from a public facing stateful
 * instance (key) and the internal representation (value). This allows public
 * methods to accept the user facing instance as an argument and map them back
 * to internal methods.
 */

// TODO: Replace this with ES6: var ReactInstanceMap = new Map();
var ReactInstanceMap = {

  /**
   * This API should be called `delete` but we'd have to make sure to always
   * transform these to strings for IE support. When this transform is fully
   * supported we can rename it.
   */
  remove: function (key) {
    key._reactInternalInstance = undefined;
  },

  get: function (key) {
    return key._reactInternalInstance;
  },

  has: function (key) {
    return key._reactInternalInstance !== undefined;
  },

  set: function (key, value) {
    key._reactInternalInstance = value;
  }

};

module.exports = ReactInstanceMap;
},{}],290:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactIsomorphic
 */

'use strict';

var ReactChildren = require('./ReactChildren');
var ReactComponent = require('./ReactComponent');
var ReactClass = require('./ReactClass');
var ReactDOMFactories = require('./ReactDOMFactories');
var ReactElement = require('./ReactElement');
var ReactElementValidator = require('./ReactElementValidator');
var ReactPropTypes = require('./ReactPropTypes');
var ReactVersion = require('./ReactVersion');

var assign = require('./Object.assign');
var onlyChild = require('./onlyChild');

var createElement = ReactElement.createElement;
var createFactory = ReactElement.createFactory;
var cloneElement = ReactElement.cloneElement;

if ("production" !== 'production') {
  createElement = ReactElementValidator.createElement;
  createFactory = ReactElementValidator.createFactory;
  cloneElement = ReactElementValidator.cloneElement;
}

var React = {

  // Modern

  Children: {
    map: ReactChildren.map,
    forEach: ReactChildren.forEach,
    count: ReactChildren.count,
    toArray: ReactChildren.toArray,
    only: onlyChild
  },

  Component: ReactComponent,

  createElement: createElement,
  cloneElement: cloneElement,
  isValidElement: ReactElement.isValidElement,

  // Classic

  PropTypes: ReactPropTypes,
  createClass: ReactClass.createClass,
  createFactory: createFactory,
  createMixin: function (mixin) {
    // Currently a noop. Will be used to validate and trace mixins.
    return mixin;
  },

  // This looks DOM specific but these are actually isomorphic helpers
  // since they are just generating DOM strings.
  DOM: ReactDOMFactories,

  version: ReactVersion,

  // Hook for JSX spread, don't use this for anything else.
  __spread: assign
};

module.exports = React;
},{"./Object.assign":249,"./ReactChildren":255,"./ReactClass":256,"./ReactComponent":257,"./ReactDOMFactories":265,"./ReactElement":279,"./ReactElementValidator":280,"./ReactPropTypes":301,"./ReactVersion":311,"./onlyChild":348}],291:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactMarkupChecksum
 */

'use strict';

var adler32 = require('./adler32');

var TAG_END = /\/?>/;

var ReactMarkupChecksum = {
  CHECKSUM_ATTR_NAME: 'data-react-checksum',

  /**
   * @param {string} markup Markup string
   * @return {string} Markup string with checksum attribute attached
   */
  addChecksumToMarkup: function (markup) {
    var checksum = adler32(markup);

    // Add checksum (handle both parent tags and self-closing tags)
    return markup.replace(TAG_END, ' ' + ReactMarkupChecksum.CHECKSUM_ATTR_NAME + '="' + checksum + '"$&');
  },

  /**
   * @param {string} markup to use
   * @param {DOMElement} element root React element
   * @returns {boolean} whether or not the markup is the same
   */
  canReuseMarkup: function (markup, element) {
    var existingChecksum = element.getAttribute(ReactMarkupChecksum.CHECKSUM_ATTR_NAME);
    existingChecksum = existingChecksum && parseInt(existingChecksum, 10);
    var markupChecksum = adler32(markup);
    return markupChecksum === existingChecksum;
  }
};

module.exports = ReactMarkupChecksum;
},{"./adler32":330}],292:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactMount
 */

'use strict';

var DOMProperty = require('./DOMProperty');
var ReactBrowserEventEmitter = require('./ReactBrowserEventEmitter');
var ReactCurrentOwner = require('./ReactCurrentOwner');
var ReactDOMFeatureFlags = require('./ReactDOMFeatureFlags');
var ReactElement = require('./ReactElement');
var ReactEmptyComponentRegistry = require('./ReactEmptyComponentRegistry');
var ReactInstanceHandles = require('./ReactInstanceHandles');
var ReactInstanceMap = require('./ReactInstanceMap');
var ReactMarkupChecksum = require('./ReactMarkupChecksum');
var ReactPerf = require('./ReactPerf');
var ReactReconciler = require('./ReactReconciler');
var ReactUpdateQueue = require('./ReactUpdateQueue');
var ReactUpdates = require('./ReactUpdates');

var assign = require('./Object.assign');
var emptyObject = require('fbjs/lib/emptyObject');
var containsNode = require('fbjs/lib/containsNode');
var instantiateReactComponent = require('./instantiateReactComponent');
var invariant = require('fbjs/lib/invariant');
var setInnerHTML = require('./setInnerHTML');
var shouldUpdateReactComponent = require('./shouldUpdateReactComponent');
var validateDOMNesting = require('./validateDOMNesting');
var warning = require('fbjs/lib/warning');

var ATTR_NAME = DOMProperty.ID_ATTRIBUTE_NAME;
var nodeCache = {};

var ELEMENT_NODE_TYPE = 1;
var DOC_NODE_TYPE = 9;
var DOCUMENT_FRAGMENT_NODE_TYPE = 11;

var ownerDocumentContextKey = '__ReactMount_ownerDocument$' + Math.random().toString(36).slice(2);

/** Mapping from reactRootID to React component instance. */
var instancesByReactRootID = {};

/** Mapping from reactRootID to `container` nodes. */
var containersByReactRootID = {};

if ("production" !== 'production') {
  /** __DEV__-only mapping from reactRootID to root elements. */
  var rootElementsByReactRootID = {};
}

// Used to store breadth-first search state in findComponentRoot.
var findComponentRootReusableArray = [];

/**
 * Finds the index of the first character
 * that's not common between the two given strings.
 *
 * @return {number} the index of the character where the strings diverge
 */
function firstDifferenceIndex(string1, string2) {
  var minLen = Math.min(string1.length, string2.length);
  for (var i = 0; i < minLen; i++) {
    if (string1.charAt(i) !== string2.charAt(i)) {
      return i;
    }
  }
  return string1.length === string2.length ? -1 : minLen;
}

/**
 * @param {DOMElement|DOMDocument} container DOM element that may contain
 * a React component
 * @return {?*} DOM element that may have the reactRoot ID, or null.
 */
function getReactRootElementInContainer(container) {
  if (!container) {
    return null;
  }

  if (container.nodeType === DOC_NODE_TYPE) {
    return container.documentElement;
  } else {
    return container.firstChild;
  }
}

/**
 * @param {DOMElement} container DOM element that may contain a React component.
 * @return {?string} A "reactRoot" ID, if a React component is rendered.
 */
function getReactRootID(container) {
  var rootElement = getReactRootElementInContainer(container);
  return rootElement && ReactMount.getID(rootElement);
}

/**
 * Accessing node[ATTR_NAME] or calling getAttribute(ATTR_NAME) on a form
 * element can return its control whose name or ID equals ATTR_NAME. All
 * DOM nodes support `getAttributeNode` but this can also get called on
 * other objects so just return '' if we're given something other than a
 * DOM node (such as window).
 *
 * @param {?DOMElement|DOMWindow|DOMDocument|DOMTextNode} node DOM node.
 * @return {string} ID of the supplied `domNode`.
 */
function getID(node) {
  var id = internalGetID(node);
  if (id) {
    if (nodeCache.hasOwnProperty(id)) {
      var cached = nodeCache[id];
      if (cached !== node) {
        !!isValid(cached, id) ? "production" !== 'production' ? invariant(false, 'ReactMount: Two valid but unequal nodes with the same `%s`: %s', ATTR_NAME, id) : invariant(false) : undefined;

        nodeCache[id] = node;
      }
    } else {
      nodeCache[id] = node;
    }
  }

  return id;
}

function internalGetID(node) {
  // If node is something like a window, document, or text node, none of
  // which support attributes or a .getAttribute method, gracefully return
  // the empty string, as if the attribute were missing.
  return node && node.getAttribute && node.getAttribute(ATTR_NAME) || '';
}

/**
 * Sets the React-specific ID of the given node.
 *
 * @param {DOMElement} node The DOM node whose ID will be set.
 * @param {string} id The value of the ID attribute.
 */
function setID(node, id) {
  var oldID = internalGetID(node);
  if (oldID !== id) {
    delete nodeCache[oldID];
  }
  node.setAttribute(ATTR_NAME, id);
  nodeCache[id] = node;
}

/**
 * Finds the node with the supplied React-generated DOM ID.
 *
 * @param {string} id A React-generated DOM ID.
 * @return {DOMElement} DOM node with the suppled `id`.
 * @internal
 */
function getNode(id) {
  if (!nodeCache.hasOwnProperty(id) || !isValid(nodeCache[id], id)) {
    nodeCache[id] = ReactMount.findReactNodeByID(id);
  }
  return nodeCache[id];
}

/**
 * Finds the node with the supplied public React instance.
 *
 * @param {*} instance A public React instance.
 * @return {?DOMElement} DOM node with the suppled `id`.
 * @internal
 */
function getNodeFromInstance(instance) {
  var id = ReactInstanceMap.get(instance)._rootNodeID;
  if (ReactEmptyComponentRegistry.isNullComponentID(id)) {
    return null;
  }
  if (!nodeCache.hasOwnProperty(id) || !isValid(nodeCache[id], id)) {
    nodeCache[id] = ReactMount.findReactNodeByID(id);
  }
  return nodeCache[id];
}

/**
 * A node is "valid" if it is contained by a currently mounted container.
 *
 * This means that the node does not have to be contained by a document in
 * order to be considered valid.
 *
 * @param {?DOMElement} node The candidate DOM node.
 * @param {string} id The expected ID of the node.
 * @return {boolean} Whether the node is contained by a mounted container.
 */
function isValid(node, id) {
  if (node) {
    !(internalGetID(node) === id) ? "production" !== 'production' ? invariant(false, 'ReactMount: Unexpected modification of `%s`', ATTR_NAME) : invariant(false) : undefined;

    var container = ReactMount.findReactContainerForID(id);
    if (container && containsNode(container, node)) {
      return true;
    }
  }

  return false;
}

/**
 * Causes the cache to forget about one React-specific ID.
 *
 * @param {string} id The ID to forget.
 */
function purgeID(id) {
  delete nodeCache[id];
}

var deepestNodeSoFar = null;
function findDeepestCachedAncestorImpl(ancestorID) {
  var ancestor = nodeCache[ancestorID];
  if (ancestor && isValid(ancestor, ancestorID)) {
    deepestNodeSoFar = ancestor;
  } else {
    // This node isn't populated in the cache, so presumably none of its
    // descendants are. Break out of the loop.
    return false;
  }
}

/**
 * Return the deepest cached node whose ID is a prefix of `targetID`.
 */
function findDeepestCachedAncestor(targetID) {
  deepestNodeSoFar = null;
  ReactInstanceHandles.traverseAncestors(targetID, findDeepestCachedAncestorImpl);

  var foundNode = deepestNodeSoFar;
  deepestNodeSoFar = null;
  return foundNode;
}

/**
 * Mounts this component and inserts it into the DOM.
 *
 * @param {ReactComponent} componentInstance The instance to mount.
 * @param {string} rootID DOM ID of the root node.
 * @param {DOMElement} container DOM element to mount into.
 * @param {ReactReconcileTransaction} transaction
 * @param {boolean} shouldReuseMarkup If true, do not insert markup
 */
function mountComponentIntoNode(componentInstance, rootID, container, transaction, shouldReuseMarkup, context) {
  if (ReactDOMFeatureFlags.useCreateElement) {
    context = assign({}, context);
    if (container.nodeType === DOC_NODE_TYPE) {
      context[ownerDocumentContextKey] = container;
    } else {
      context[ownerDocumentContextKey] = container.ownerDocument;
    }
  }
  if ("production" !== 'production') {
    if (context === emptyObject) {
      context = {};
    }
    var tag = container.nodeName.toLowerCase();
    context[validateDOMNesting.ancestorInfoContextKey] = validateDOMNesting.updatedAncestorInfo(null, tag, null);
  }
  var markup = ReactReconciler.mountComponent(componentInstance, rootID, transaction, context);
  componentInstance._renderedComponent._topLevelWrapper = componentInstance;
  ReactMount._mountImageIntoNode(markup, container, shouldReuseMarkup, transaction);
}

/**
 * Batched mount.
 *
 * @param {ReactComponent} componentInstance The instance to mount.
 * @param {string} rootID DOM ID of the root node.
 * @param {DOMElement} container DOM element to mount into.
 * @param {boolean} shouldReuseMarkup If true, do not insert markup
 */
function batchedMountComponentIntoNode(componentInstance, rootID, container, shouldReuseMarkup, context) {
  var transaction = ReactUpdates.ReactReconcileTransaction.getPooled(
  /* forceHTML */shouldReuseMarkup);
  transaction.perform(mountComponentIntoNode, null, componentInstance, rootID, container, transaction, shouldReuseMarkup, context);
  ReactUpdates.ReactReconcileTransaction.release(transaction);
}

/**
 * Unmounts a component and removes it from the DOM.
 *
 * @param {ReactComponent} instance React component instance.
 * @param {DOMElement} container DOM element to unmount from.
 * @final
 * @internal
 * @see {ReactMount.unmountComponentAtNode}
 */
function unmountComponentFromNode(instance, container) {
  ReactReconciler.unmountComponent(instance);

  if (container.nodeType === DOC_NODE_TYPE) {
    container = container.documentElement;
  }

  // http://jsperf.com/emptying-a-node
  while (container.lastChild) {
    container.removeChild(container.lastChild);
  }
}

/**
 * True if the supplied DOM node has a direct React-rendered child that is
 * not a React root element. Useful for warning in `render`,
 * `unmountComponentAtNode`, etc.
 *
 * @param {?DOMElement} node The candidate DOM node.
 * @return {boolean} True if the DOM element contains a direct child that was
 * rendered by React but is not a root element.
 * @internal
 */
function hasNonRootReactChild(node) {
  var reactRootID = getReactRootID(node);
  return reactRootID ? reactRootID !== ReactInstanceHandles.getReactRootIDFromNodeID(reactRootID) : false;
}

/**
 * Returns the first (deepest) ancestor of a node which is rendered by this copy
 * of React.
 */
function findFirstReactDOMImpl(node) {
  // This node might be from another React instance, so we make sure not to
  // examine the node cache here
  for (; node && node.parentNode !== node; node = node.parentNode) {
    if (node.nodeType !== 1) {
      // Not a DOMElement, therefore not a React component
      continue;
    }
    var nodeID = internalGetID(node);
    if (!nodeID) {
      continue;
    }
    var reactRootID = ReactInstanceHandles.getReactRootIDFromNodeID(nodeID);

    // If containersByReactRootID contains the container we find by crawling up
    // the tree, we know that this instance of React rendered the node.
    // nb. isValid's strategy (with containsNode) does not work because render
    // trees may be nested and we don't want a false positive in that case.
    var current = node;
    var lastID;
    do {
      lastID = internalGetID(current);
      current = current.parentNode;
      if (current == null) {
        // The passed-in node has been detached from the container it was
        // originally rendered into.
        return null;
      }
    } while (lastID !== reactRootID);

    if (current === containersByReactRootID[reactRootID]) {
      return node;
    }
  }
  return null;
}

/**
 * Temporary (?) hack so that we can store all top-level pending updates on
 * composites instead of having to worry about different types of components
 * here.
 */
var TopLevelWrapper = function () {};
TopLevelWrapper.prototype.isReactComponent = {};
if ("production" !== 'production') {
  TopLevelWrapper.displayName = 'TopLevelWrapper';
}
TopLevelWrapper.prototype.render = function () {
  // this.props is actually a ReactElement
  return this.props;
};

/**
 * Mounting is the process of initializing a React component by creating its
 * representative DOM elements and inserting them into a supplied `container`.
 * Any prior content inside `container` is destroyed in the process.
 *
 *   ReactMount.render(
 *     component,
 *     document.getElementById('container')
 *   );
 *
 *   <div id="container">                   <-- Supplied `container`.
 *     <div data-reactid=".3">              <-- Rendered reactRoot of React
 *       // ...                                 component.
 *     </div>
 *   </div>
 *
 * Inside of `container`, the first element rendered is the "reactRoot".
 */
var ReactMount = {

  TopLevelWrapper: TopLevelWrapper,

  /** Exposed for debugging purposes **/
  _instancesByReactRootID: instancesByReactRootID,

  /**
   * This is a hook provided to support rendering React components while
   * ensuring that the apparent scroll position of its `container` does not
   * change.
   *
   * @param {DOMElement} container The `container` being rendered into.
   * @param {function} renderCallback This must be called once to do the render.
   */
  scrollMonitor: function (container, renderCallback) {
    renderCallback();
  },

  /**
   * Take a component that's already mounted into the DOM and replace its props
   * @param {ReactComponent} prevComponent component instance already in the DOM
   * @param {ReactElement} nextElement component instance to render
   * @param {DOMElement} container container to render into
   * @param {?function} callback function triggered on completion
   */
  _updateRootComponent: function (prevComponent, nextElement, container, callback) {
    ReactMount.scrollMonitor(container, function () {
      ReactUpdateQueue.enqueueElementInternal(prevComponent, nextElement);
      if (callback) {
        ReactUpdateQueue.enqueueCallbackInternal(prevComponent, callback);
      }
    });

    if ("production" !== 'production') {
      // Record the root element in case it later gets transplanted.
      rootElementsByReactRootID[getReactRootID(container)] = getReactRootElementInContainer(container);
    }

    return prevComponent;
  },

  /**
   * Register a component into the instance map and starts scroll value
   * monitoring
   * @param {ReactComponent} nextComponent component instance to render
   * @param {DOMElement} container container to render into
   * @return {string} reactRoot ID prefix
   */
  _registerComponent: function (nextComponent, container) {
    !(container && (container.nodeType === ELEMENT_NODE_TYPE || container.nodeType === DOC_NODE_TYPE || container.nodeType === DOCUMENT_FRAGMENT_NODE_TYPE)) ? "production" !== 'production' ? invariant(false, '_registerComponent(...): Target container is not a DOM element.') : invariant(false) : undefined;

    ReactBrowserEventEmitter.ensureScrollValueMonitoring();

    var reactRootID = ReactMount.registerContainer(container);
    instancesByReactRootID[reactRootID] = nextComponent;
    return reactRootID;
  },

  /**
   * Render a new component into the DOM.
   * @param {ReactElement} nextElement element to render
   * @param {DOMElement} container container to render into
   * @param {boolean} shouldReuseMarkup if we should skip the markup insertion
   * @return {ReactComponent} nextComponent
   */
  _renderNewRootComponent: function (nextElement, container, shouldReuseMarkup, context) {
    // Various parts of our code (such as ReactCompositeComponent's
    // _renderValidatedComponent) assume that calls to render aren't nested;
    // verify that that's the case.
    "production" !== 'production' ? warning(ReactCurrentOwner.current == null, '_renderNewRootComponent(): Render methods should be a pure function ' + 'of props and state; triggering nested component updates from ' + 'render is not allowed. If necessary, trigger nested updates in ' + 'componentDidUpdate. Check the render method of %s.', ReactCurrentOwner.current && ReactCurrentOwner.current.getName() || 'ReactCompositeComponent') : undefined;

    var componentInstance = instantiateReactComponent(nextElement, null);
    var reactRootID = ReactMount._registerComponent(componentInstance, container);

    // The initial render is synchronous but any updates that happen during
    // rendering, in componentWillMount or componentDidMount, will be batched
    // according to the current batching strategy.

    ReactUpdates.batchedUpdates(batchedMountComponentIntoNode, componentInstance, reactRootID, container, shouldReuseMarkup, context);

    if ("production" !== 'production') {
      // Record the root element in case it later gets transplanted.
      rootElementsByReactRootID[reactRootID] = getReactRootElementInContainer(container);
    }

    return componentInstance;
  },

  /**
   * Renders a React component into the DOM in the supplied `container`.
   *
   * If the React component was previously rendered into `container`, this will
   * perform an update on it and only mutate the DOM as necessary to reflect the
   * latest React component.
   *
   * @param {ReactComponent} parentComponent The conceptual parent of this render tree.
   * @param {ReactElement} nextElement Component element to render.
   * @param {DOMElement} container DOM element to render into.
   * @param {?function} callback function triggered on completion
   * @return {ReactComponent} Component instance rendered in `container`.
   */
  renderSubtreeIntoContainer: function (parentComponent, nextElement, container, callback) {
    !(parentComponent != null && parentComponent._reactInternalInstance != null) ? "production" !== 'production' ? invariant(false, 'parentComponent must be a valid React Component') : invariant(false) : undefined;
    return ReactMount._renderSubtreeIntoContainer(parentComponent, nextElement, container, callback);
  },

  _renderSubtreeIntoContainer: function (parentComponent, nextElement, container, callback) {
    !ReactElement.isValidElement(nextElement) ? "production" !== 'production' ? invariant(false, 'ReactDOM.render(): Invalid component element.%s', typeof nextElement === 'string' ? ' Instead of passing an element string, make sure to instantiate ' + 'it by passing it to React.createElement.' : typeof nextElement === 'function' ? ' Instead of passing a component class, make sure to instantiate ' + 'it by passing it to React.createElement.' :
    // Check if it quacks like an element
    nextElement != null && nextElement.props !== undefined ? ' This may be caused by unintentionally loading two independent ' + 'copies of React.' : '') : invariant(false) : undefined;

    "production" !== 'production' ? warning(!container || !container.tagName || container.tagName.toUpperCase() !== 'BODY', 'render(): Rendering components directly into document.body is ' + 'discouraged, since its children are often manipulated by third-party ' + 'scripts and browser extensions. This may lead to subtle ' + 'reconciliation issues. Try rendering into a container element created ' + 'for your app.') : undefined;

    var nextWrappedElement = new ReactElement(TopLevelWrapper, null, null, null, null, null, nextElement);

    var prevComponent = instancesByReactRootID[getReactRootID(container)];

    if (prevComponent) {
      var prevWrappedElement = prevComponent._currentElement;
      var prevElement = prevWrappedElement.props;
      if (shouldUpdateReactComponent(prevElement, nextElement)) {
        var publicInst = prevComponent._renderedComponent.getPublicInstance();
        var updatedCallback = callback && function () {
          callback.call(publicInst);
        };
        ReactMount._updateRootComponent(prevComponent, nextWrappedElement, container, updatedCallback);
        return publicInst;
      } else {
        ReactMount.unmountComponentAtNode(container);
      }
    }

    var reactRootElement = getReactRootElementInContainer(container);
    var containerHasReactMarkup = reactRootElement && !!internalGetID(reactRootElement);
    var containerHasNonRootReactChild = hasNonRootReactChild(container);

    if ("production" !== 'production') {
      "production" !== 'production' ? warning(!containerHasNonRootReactChild, 'render(...): Replacing React-rendered children with a new root ' + 'component. If you intended to update the children of this node, ' + 'you should instead have the existing children update their state ' + 'and render the new components instead of calling ReactDOM.render.') : undefined;

      if (!containerHasReactMarkup || reactRootElement.nextSibling) {
        var rootElementSibling = reactRootElement;
        while (rootElementSibling) {
          if (internalGetID(rootElementSibling)) {
            "production" !== 'production' ? warning(false, 'render(): Target node has markup rendered by React, but there ' + 'are unrelated nodes as well. This is most commonly caused by ' + 'white-space inserted around server-rendered markup.') : undefined;
            break;
          }
          rootElementSibling = rootElementSibling.nextSibling;
        }
      }
    }

    var shouldReuseMarkup = containerHasReactMarkup && !prevComponent && !containerHasNonRootReactChild;
    var component = ReactMount._renderNewRootComponent(nextWrappedElement, container, shouldReuseMarkup, parentComponent != null ? parentComponent._reactInternalInstance._processChildContext(parentComponent._reactInternalInstance._context) : emptyObject)._renderedComponent.getPublicInstance();
    if (callback) {
      callback.call(component);
    }
    return component;
  },

  /**
   * Renders a React component into the DOM in the supplied `container`.
   *
   * If the React component was previously rendered into `container`, this will
   * perform an update on it and only mutate the DOM as necessary to reflect the
   * latest React component.
   *
   * @param {ReactElement} nextElement Component element to render.
   * @param {DOMElement} container DOM element to render into.
   * @param {?function} callback function triggered on completion
   * @return {ReactComponent} Component instance rendered in `container`.
   */
  render: function (nextElement, container, callback) {
    return ReactMount._renderSubtreeIntoContainer(null, nextElement, container, callback);
  },

  /**
   * Registers a container node into which React components will be rendered.
   * This also creates the "reactRoot" ID that will be assigned to the element
   * rendered within.
   *
   * @param {DOMElement} container DOM element to register as a container.
   * @return {string} The "reactRoot" ID of elements rendered within.
   */
  registerContainer: function (container) {
    var reactRootID = getReactRootID(container);
    if (reactRootID) {
      // If one exists, make sure it is a valid "reactRoot" ID.
      reactRootID = ReactInstanceHandles.getReactRootIDFromNodeID(reactRootID);
    }
    if (!reactRootID) {
      // No valid "reactRoot" ID found, create one.
      reactRootID = ReactInstanceHandles.createReactRootID();
    }
    containersByReactRootID[reactRootID] = container;
    return reactRootID;
  },

  /**
   * Unmounts and destroys the React component rendered in the `container`.
   *
   * @param {DOMElement} container DOM element containing a React component.
   * @return {boolean} True if a component was found in and unmounted from
   *                   `container`
   */
  unmountComponentAtNode: function (container) {
    // Various parts of our code (such as ReactCompositeComponent's
    // _renderValidatedComponent) assume that calls to render aren't nested;
    // verify that that's the case. (Strictly speaking, unmounting won't cause a
    // render but we still don't expect to be in a render call here.)
    "production" !== 'production' ? warning(ReactCurrentOwner.current == null, 'unmountComponentAtNode(): Render methods should be a pure function ' + 'of props and state; triggering nested component updates from render ' + 'is not allowed. If necessary, trigger nested updates in ' + 'componentDidUpdate. Check the render method of %s.', ReactCurrentOwner.current && ReactCurrentOwner.current.getName() || 'ReactCompositeComponent') : undefined;

    !(container && (container.nodeType === ELEMENT_NODE_TYPE || container.nodeType === DOC_NODE_TYPE || container.nodeType === DOCUMENT_FRAGMENT_NODE_TYPE)) ? "production" !== 'production' ? invariant(false, 'unmountComponentAtNode(...): Target container is not a DOM element.') : invariant(false) : undefined;

    var reactRootID = getReactRootID(container);
    var component = instancesByReactRootID[reactRootID];
    if (!component) {
      // Check if the node being unmounted was rendered by React, but isn't a
      // root node.
      var containerHasNonRootReactChild = hasNonRootReactChild(container);

      // Check if the container itself is a React root node.
      var containerID = internalGetID(container);
      var isContainerReactRoot = containerID && containerID === ReactInstanceHandles.getReactRootIDFromNodeID(containerID);

      if ("production" !== 'production') {
        "production" !== 'production' ? warning(!containerHasNonRootReactChild, 'unmountComponentAtNode(): The node you\'re attempting to unmount ' + 'was rendered by React and is not a top-level container. %s', isContainerReactRoot ? 'You may have accidentally passed in a React root node instead ' + 'of its container.' : 'Instead, have the parent component update its state and ' + 'rerender in order to remove this component.') : undefined;
      }

      return false;
    }
    ReactUpdates.batchedUpdates(unmountComponentFromNode, component, container);
    delete instancesByReactRootID[reactRootID];
    delete containersByReactRootID[reactRootID];
    if ("production" !== 'production') {
      delete rootElementsByReactRootID[reactRootID];
    }
    return true;
  },

  /**
   * Finds the container DOM element that contains React component to which the
   * supplied DOM `id` belongs.
   *
   * @param {string} id The ID of an element rendered by a React component.
   * @return {?DOMElement} DOM element that contains the `id`.
   */
  findReactContainerForID: function (id) {
    var reactRootID = ReactInstanceHandles.getReactRootIDFromNodeID(id);
    var container = containersByReactRootID[reactRootID];

    if ("production" !== 'production') {
      var rootElement = rootElementsByReactRootID[reactRootID];
      if (rootElement && rootElement.parentNode !== container) {
        "production" !== 'production' ? warning(
        // Call internalGetID here because getID calls isValid which calls
        // findReactContainerForID (this function).
        internalGetID(rootElement) === reactRootID, 'ReactMount: Root element ID differed from reactRootID.') : undefined;
        var containerChild = container.firstChild;
        if (containerChild && reactRootID === internalGetID(containerChild)) {
          // If the container has a new child with the same ID as the old
          // root element, then rootElementsByReactRootID[reactRootID] is
          // just stale and needs to be updated. The case that deserves a
          // warning is when the container is empty.
          rootElementsByReactRootID[reactRootID] = containerChild;
        } else {
          "production" !== 'production' ? warning(false, 'ReactMount: Root element has been removed from its original ' + 'container. New container: %s', rootElement.parentNode) : undefined;
        }
      }
    }

    return container;
  },

  /**
   * Finds an element rendered by React with the supplied ID.
   *
   * @param {string} id ID of a DOM node in the React component.
   * @return {DOMElement} Root DOM node of the React component.
   */
  findReactNodeByID: function (id) {
    var reactRoot = ReactMount.findReactContainerForID(id);
    return ReactMount.findComponentRoot(reactRoot, id);
  },

  /**
   * Traverses up the ancestors of the supplied node to find a node that is a
   * DOM representation of a React component rendered by this copy of React.
   *
   * @param {*} node
   * @return {?DOMEventTarget}
   * @internal
   */
  getFirstReactDOM: function (node) {
    return findFirstReactDOMImpl(node);
  },

  /**
   * Finds a node with the supplied `targetID` inside of the supplied
   * `ancestorNode`.  Exploits the ID naming scheme to perform the search
   * quickly.
   *
   * @param {DOMEventTarget} ancestorNode Search from this root.
   * @pararm {string} targetID ID of the DOM representation of the component.
   * @return {DOMEventTarget} DOM node with the supplied `targetID`.
   * @internal
   */
  findComponentRoot: function (ancestorNode, targetID) {
    var firstChildren = findComponentRootReusableArray;
    var childIndex = 0;

    var deepestAncestor = findDeepestCachedAncestor(targetID) || ancestorNode;

    if ("production" !== 'production') {
      // This will throw on the next line; give an early warning
      "production" !== 'production' ? warning(deepestAncestor != null, 'React can\'t find the root component node for data-reactid value ' + '`%s`. If you\'re seeing this message, it probably means that ' + 'you\'ve loaded two copies of React on the page. At this time, only ' + 'a single copy of React can be loaded at a time.', targetID) : undefined;
    }

    firstChildren[0] = deepestAncestor.firstChild;
    firstChildren.length = 1;

    while (childIndex < firstChildren.length) {
      var child = firstChildren[childIndex++];
      var targetChild;

      while (child) {
        var childID = ReactMount.getID(child);
        if (childID) {
          // Even if we find the node we're looking for, we finish looping
          // through its siblings to ensure they're cached so that we don't have
          // to revisit this node again. Otherwise, we make n^2 calls to getID
          // when visiting the many children of a single node in order.

          if (targetID === childID) {
            targetChild = child;
          } else if (ReactInstanceHandles.isAncestorIDOf(childID, targetID)) {
            // If we find a child whose ID is an ancestor of the given ID,
            // then we can be sure that we only want to search the subtree
            // rooted at this child, so we can throw out the rest of the
            // search state.
            firstChildren.length = childIndex = 0;
            firstChildren.push(child.firstChild);
          }
        } else {
          // If this child had no ID, then there's a chance that it was
          // injected automatically by the browser, as when a `<table>`
          // element sprouts an extra `<tbody>` child as a side effect of
          // `.innerHTML` parsing. Optimistically continue down this
          // branch, but not before examining the other siblings.
          firstChildren.push(child.firstChild);
        }

        child = child.nextSibling;
      }

      if (targetChild) {
        // Emptying firstChildren/findComponentRootReusableArray is
        // not necessary for correctness, but it helps the GC reclaim
        // any nodes that were left at the end of the search.
        firstChildren.length = 0;

        return targetChild;
      }
    }

    firstChildren.length = 0;

    !false ? "production" !== 'production' ? invariant(false, 'findComponentRoot(..., %s): Unable to find element. This probably ' + 'means the DOM was unexpectedly mutated (e.g., by the browser), ' + 'usually due to forgetting a <tbody> when using tables, nesting tags ' + 'like <form>, <p>, or <a>, or using non-SVG elements in an <svg> ' + 'parent. ' + 'Try inspecting the child nodes of the element with React ID `%s`.', targetID, ReactMount.getID(ancestorNode)) : invariant(false) : undefined;
  },

  _mountImageIntoNode: function (markup, container, shouldReuseMarkup, transaction) {
    !(container && (container.nodeType === ELEMENT_NODE_TYPE || container.nodeType === DOC_NODE_TYPE || container.nodeType === DOCUMENT_FRAGMENT_NODE_TYPE)) ? "production" !== 'production' ? invariant(false, 'mountComponentIntoNode(...): Target container is not valid.') : invariant(false) : undefined;

    if (shouldReuseMarkup) {
      var rootElement = getReactRootElementInContainer(container);
      if (ReactMarkupChecksum.canReuseMarkup(markup, rootElement)) {
        return;
      } else {
        var checksum = rootElement.getAttribute(ReactMarkupChecksum.CHECKSUM_ATTR_NAME);
        rootElement.removeAttribute(ReactMarkupChecksum.CHECKSUM_ATTR_NAME);

        var rootMarkup = rootElement.outerHTML;
        rootElement.setAttribute(ReactMarkupChecksum.CHECKSUM_ATTR_NAME, checksum);

        var normalizedMarkup = markup;
        if ("production" !== 'production') {
          // because rootMarkup is retrieved from the DOM, various normalizations
          // will have occurred which will not be present in `markup`. Here,
          // insert markup into a <div> or <iframe> depending on the container
          // type to perform the same normalizations before comparing.
          var normalizer;
          if (container.nodeType === ELEMENT_NODE_TYPE) {
            normalizer = document.createElement('div');
            normalizer.innerHTML = markup;
            normalizedMarkup = normalizer.innerHTML;
          } else {
            normalizer = document.createElement('iframe');
            document.body.appendChild(normalizer);
            normalizer.contentDocument.write(markup);
            normalizedMarkup = normalizer.contentDocument.documentElement.outerHTML;
            document.body.removeChild(normalizer);
          }
        }

        var diffIndex = firstDifferenceIndex(normalizedMarkup, rootMarkup);
        var difference = ' (client) ' + normalizedMarkup.substring(diffIndex - 20, diffIndex + 20) + '\n (server) ' + rootMarkup.substring(diffIndex - 20, diffIndex + 20);

        !(container.nodeType !== DOC_NODE_TYPE) ? "production" !== 'production' ? invariant(false, 'You\'re trying to render a component to the document using ' + 'server rendering but the checksum was invalid. This usually ' + 'means you rendered a different component type or props on ' + 'the client from the one on the server, or your render() ' + 'methods are impure. React cannot handle this case due to ' + 'cross-browser quirks by rendering at the document root. You ' + 'should look for environment dependent code in your components ' + 'and ensure the props are the same client and server side:\n%s', difference) : invariant(false) : undefined;

        if ("production" !== 'production') {
          "production" !== 'production' ? warning(false, 'React attempted to reuse markup in a container but the ' + 'checksum was invalid. This generally means that you are ' + 'using server rendering and the markup generated on the ' + 'server was not what the client was expecting. React injected ' + 'new markup to compensate which works but you have lost many ' + 'of the benefits of server rendering. Instead, figure out ' + 'why the markup being generated is different on the client ' + 'or server:\n%s', difference) : undefined;
        }
      }
    }

    !(container.nodeType !== DOC_NODE_TYPE) ? "production" !== 'production' ? invariant(false, 'You\'re trying to render a component to the document but ' + 'you didn\'t use server rendering. We can\'t do this ' + 'without using server rendering due to cross-browser quirks. ' + 'See ReactDOMServer.renderToString() for server rendering.') : invariant(false) : undefined;

    if (transaction.useCreateElement) {
      while (container.lastChild) {
        container.removeChild(container.lastChild);
      }
      container.appendChild(markup);
    } else {
      setInnerHTML(container, markup);
    }
  },

  ownerDocumentContextKey: ownerDocumentContextKey,

  /**
   * React ID utilities.
   */

  getReactRootID: getReactRootID,

  getID: getID,

  setID: setID,

  getNode: getNode,

  getNodeFromInstance: getNodeFromInstance,

  isValid: isValid,

  purgeID: purgeID
};

ReactPerf.measureMethods(ReactMount, 'ReactMount', {
  _renderNewRootComponent: '_renderNewRootComponent',
  _mountImageIntoNode: '_mountImageIntoNode'
});

module.exports = ReactMount;
},{"./DOMProperty":236,"./Object.assign":249,"./ReactBrowserEventEmitter":253,"./ReactCurrentOwner":261,"./ReactDOMFeatureFlags":266,"./ReactElement":279,"./ReactEmptyComponentRegistry":282,"./ReactInstanceHandles":288,"./ReactInstanceMap":289,"./ReactMarkupChecksum":291,"./ReactPerf":298,"./ReactReconciler":303,"./ReactUpdateQueue":309,"./ReactUpdates":310,"./instantiateReactComponent":345,"./setInnerHTML":351,"./shouldUpdateReactComponent":353,"./validateDOMNesting":355,"fbjs/lib/containsNode":140,"fbjs/lib/emptyObject":144,"fbjs/lib/invariant":151,"fbjs/lib/warning":162}],293:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactMultiChild
 * @typechecks static-only
 */

'use strict';

var ReactComponentEnvironment = require('./ReactComponentEnvironment');
var ReactMultiChildUpdateTypes = require('./ReactMultiChildUpdateTypes');

var ReactCurrentOwner = require('./ReactCurrentOwner');
var ReactReconciler = require('./ReactReconciler');
var ReactChildReconciler = require('./ReactChildReconciler');

var flattenChildren = require('./flattenChildren');

/**
 * Updating children of a component may trigger recursive updates. The depth is
 * used to batch recursive updates to render markup more efficiently.
 *
 * @type {number}
 * @private
 */
var updateDepth = 0;

/**
 * Queue of update configuration objects.
 *
 * Each object has a `type` property that is in `ReactMultiChildUpdateTypes`.
 *
 * @type {array<object>}
 * @private
 */
var updateQueue = [];

/**
 * Queue of markup to be rendered.
 *
 * @type {array<string>}
 * @private
 */
var markupQueue = [];

/**
 * Enqueues markup to be rendered and inserted at a supplied index.
 *
 * @param {string} parentID ID of the parent component.
 * @param {string} markup Markup that renders into an element.
 * @param {number} toIndex Destination index.
 * @private
 */
function enqueueInsertMarkup(parentID, markup, toIndex) {
  // NOTE: Null values reduce hidden classes.
  updateQueue.push({
    parentID: parentID,
    parentNode: null,
    type: ReactMultiChildUpdateTypes.INSERT_MARKUP,
    markupIndex: markupQueue.push(markup) - 1,
    content: null,
    fromIndex: null,
    toIndex: toIndex
  });
}

/**
 * Enqueues moving an existing element to another index.
 *
 * @param {string} parentID ID of the parent component.
 * @param {number} fromIndex Source index of the existing element.
 * @param {number} toIndex Destination index of the element.
 * @private
 */
function enqueueMove(parentID, fromIndex, toIndex) {
  // NOTE: Null values reduce hidden classes.
  updateQueue.push({
    parentID: parentID,
    parentNode: null,
    type: ReactMultiChildUpdateTypes.MOVE_EXISTING,
    markupIndex: null,
    content: null,
    fromIndex: fromIndex,
    toIndex: toIndex
  });
}

/**
 * Enqueues removing an element at an index.
 *
 * @param {string} parentID ID of the parent component.
 * @param {number} fromIndex Index of the element to remove.
 * @private
 */
function enqueueRemove(parentID, fromIndex) {
  // NOTE: Null values reduce hidden classes.
  updateQueue.push({
    parentID: parentID,
    parentNode: null,
    type: ReactMultiChildUpdateTypes.REMOVE_NODE,
    markupIndex: null,
    content: null,
    fromIndex: fromIndex,
    toIndex: null
  });
}

/**
 * Enqueues setting the markup of a node.
 *
 * @param {string} parentID ID of the parent component.
 * @param {string} markup Markup that renders into an element.
 * @private
 */
function enqueueSetMarkup(parentID, markup) {
  // NOTE: Null values reduce hidden classes.
  updateQueue.push({
    parentID: parentID,
    parentNode: null,
    type: ReactMultiChildUpdateTypes.SET_MARKUP,
    markupIndex: null,
    content: markup,
    fromIndex: null,
    toIndex: null
  });
}

/**
 * Enqueues setting the text content.
 *
 * @param {string} parentID ID of the parent component.
 * @param {string} textContent Text content to set.
 * @private
 */
function enqueueTextContent(parentID, textContent) {
  // NOTE: Null values reduce hidden classes.
  updateQueue.push({
    parentID: parentID,
    parentNode: null,
    type: ReactMultiChildUpdateTypes.TEXT_CONTENT,
    markupIndex: null,
    content: textContent,
    fromIndex: null,
    toIndex: null
  });
}

/**
 * Processes any enqueued updates.
 *
 * @private
 */
function processQueue() {
  if (updateQueue.length) {
    ReactComponentEnvironment.processChildrenUpdates(updateQueue, markupQueue);
    clearQueue();
  }
}

/**
 * Clears any enqueued updates.
 *
 * @private
 */
function clearQueue() {
  updateQueue.length = 0;
  markupQueue.length = 0;
}

/**
 * ReactMultiChild are capable of reconciling multiple children.
 *
 * @class ReactMultiChild
 * @internal
 */
var ReactMultiChild = {

  /**
   * Provides common functionality for components that must reconcile multiple
   * children. This is used by `ReactDOMComponent` to mount, update, and
   * unmount child components.
   *
   * @lends {ReactMultiChild.prototype}
   */
  Mixin: {

    _reconcilerInstantiateChildren: function (nestedChildren, transaction, context) {
      if ("production" !== 'production') {
        if (this._currentElement) {
          try {
            ReactCurrentOwner.current = this._currentElement._owner;
            return ReactChildReconciler.instantiateChildren(nestedChildren, transaction, context);
          } finally {
            ReactCurrentOwner.current = null;
          }
        }
      }
      return ReactChildReconciler.instantiateChildren(nestedChildren, transaction, context);
    },

    _reconcilerUpdateChildren: function (prevChildren, nextNestedChildrenElements, transaction, context) {
      var nextChildren;
      if ("production" !== 'production') {
        if (this._currentElement) {
          try {
            ReactCurrentOwner.current = this._currentElement._owner;
            nextChildren = flattenChildren(nextNestedChildrenElements);
          } finally {
            ReactCurrentOwner.current = null;
          }
          return ReactChildReconciler.updateChildren(prevChildren, nextChildren, transaction, context);
        }
      }
      nextChildren = flattenChildren(nextNestedChildrenElements);
      return ReactChildReconciler.updateChildren(prevChildren, nextChildren, transaction, context);
    },

    /**
     * Generates a "mount image" for each of the supplied children. In the case
     * of `ReactDOMComponent`, a mount image is a string of markup.
     *
     * @param {?object} nestedChildren Nested child maps.
     * @return {array} An array of mounted representations.
     * @internal
     */
    mountChildren: function (nestedChildren, transaction, context) {
      var children = this._reconcilerInstantiateChildren(nestedChildren, transaction, context);
      this._renderedChildren = children;
      var mountImages = [];
      var index = 0;
      for (var name in children) {
        if (children.hasOwnProperty(name)) {
          var child = children[name];
          // Inlined for performance, see `ReactInstanceHandles.createReactID`.
          var rootID = this._rootNodeID + name;
          var mountImage = ReactReconciler.mountComponent(child, rootID, transaction, context);
          child._mountIndex = index++;
          mountImages.push(mountImage);
        }
      }
      return mountImages;
    },

    /**
     * Replaces any rendered children with a text content string.
     *
     * @param {string} nextContent String of content.
     * @internal
     */
    updateTextContent: function (nextContent) {
      updateDepth++;
      var errorThrown = true;
      try {
        var prevChildren = this._renderedChildren;
        // Remove any rendered children.
        ReactChildReconciler.unmountChildren(prevChildren);
        // TODO: The setTextContent operation should be enough
        for (var name in prevChildren) {
          if (prevChildren.hasOwnProperty(name)) {
            this._unmountChild(prevChildren[name]);
          }
        }
        // Set new text content.
        this.setTextContent(nextContent);
        errorThrown = false;
      } finally {
        updateDepth--;
        if (!updateDepth) {
          if (errorThrown) {
            clearQueue();
          } else {
            processQueue();
          }
        }
      }
    },

    /**
     * Replaces any rendered children with a markup string.
     *
     * @param {string} nextMarkup String of markup.
     * @internal
     */
    updateMarkup: function (nextMarkup) {
      updateDepth++;
      var errorThrown = true;
      try {
        var prevChildren = this._renderedChildren;
        // Remove any rendered children.
        ReactChildReconciler.unmountChildren(prevChildren);
        for (var name in prevChildren) {
          if (prevChildren.hasOwnProperty(name)) {
            this._unmountChildByName(prevChildren[name], name);
          }
        }
        this.setMarkup(nextMarkup);
        errorThrown = false;
      } finally {
        updateDepth--;
        if (!updateDepth) {
          if (errorThrown) {
            clearQueue();
          } else {
            processQueue();
          }
        }
      }
    },

    /**
     * Updates the rendered children with new children.
     *
     * @param {?object} nextNestedChildrenElements Nested child element maps.
     * @param {ReactReconcileTransaction} transaction
     * @internal
     */
    updateChildren: function (nextNestedChildrenElements, transaction, context) {
      updateDepth++;
      var errorThrown = true;
      try {
        this._updateChildren(nextNestedChildrenElements, transaction, context);
        errorThrown = false;
      } finally {
        updateDepth--;
        if (!updateDepth) {
          if (errorThrown) {
            clearQueue();
          } else {
            processQueue();
          }
        }
      }
    },

    /**
     * Improve performance by isolating this hot code path from the try/catch
     * block in `updateChildren`.
     *
     * @param {?object} nextNestedChildrenElements Nested child element maps.
     * @param {ReactReconcileTransaction} transaction
     * @final
     * @protected
     */
    _updateChildren: function (nextNestedChildrenElements, transaction, context) {
      var prevChildren = this._renderedChildren;
      var nextChildren = this._reconcilerUpdateChildren(prevChildren, nextNestedChildrenElements, transaction, context);
      this._renderedChildren = nextChildren;
      if (!nextChildren && !prevChildren) {
        return;
      }
      var name;
      // `nextIndex` will increment for each child in `nextChildren`, but
      // `lastIndex` will be the last index visited in `prevChildren`.
      var lastIndex = 0;
      var nextIndex = 0;
      for (name in nextChildren) {
        if (!nextChildren.hasOwnProperty(name)) {
          continue;
        }
        var prevChild = prevChildren && prevChildren[name];
        var nextChild = nextChildren[name];
        if (prevChild === nextChild) {
          this.moveChild(prevChild, nextIndex, lastIndex);
          lastIndex = Math.max(prevChild._mountIndex, lastIndex);
          prevChild._mountIndex = nextIndex;
        } else {
          if (prevChild) {
            // Update `lastIndex` before `_mountIndex` gets unset by unmounting.
            lastIndex = Math.max(prevChild._mountIndex, lastIndex);
            this._unmountChild(prevChild);
          }
          // The child must be instantiated before it's mounted.
          this._mountChildByNameAtIndex(nextChild, name, nextIndex, transaction, context);
        }
        nextIndex++;
      }
      // Remove children that are no longer present.
      for (name in prevChildren) {
        if (prevChildren.hasOwnProperty(name) && !(nextChildren && nextChildren.hasOwnProperty(name))) {
          this._unmountChild(prevChildren[name]);
        }
      }
    },

    /**
     * Unmounts all rendered children. This should be used to clean up children
     * when this component is unmounted.
     *
     * @internal
     */
    unmountChildren: function () {
      var renderedChildren = this._renderedChildren;
      ReactChildReconciler.unmountChildren(renderedChildren);
      this._renderedChildren = null;
    },

    /**
     * Moves a child component to the supplied index.
     *
     * @param {ReactComponent} child Component to move.
     * @param {number} toIndex Destination index of the element.
     * @param {number} lastIndex Last index visited of the siblings of `child`.
     * @protected
     */
    moveChild: function (child, toIndex, lastIndex) {
      // If the index of `child` is less than `lastIndex`, then it needs to
      // be moved. Otherwise, we do not need to move it because a child will be
      // inserted or moved before `child`.
      if (child._mountIndex < lastIndex) {
        enqueueMove(this._rootNodeID, child._mountIndex, toIndex);
      }
    },

    /**
     * Creates a child component.
     *
     * @param {ReactComponent} child Component to create.
     * @param {string} mountImage Markup to insert.
     * @protected
     */
    createChild: function (child, mountImage) {
      enqueueInsertMarkup(this._rootNodeID, mountImage, child._mountIndex);
    },

    /**
     * Removes a child component.
     *
     * @param {ReactComponent} child Child to remove.
     * @protected
     */
    removeChild: function (child) {
      enqueueRemove(this._rootNodeID, child._mountIndex);
    },

    /**
     * Sets this text content string.
     *
     * @param {string} textContent Text content to set.
     * @protected
     */
    setTextContent: function (textContent) {
      enqueueTextContent(this._rootNodeID, textContent);
    },

    /**
     * Sets this markup string.
     *
     * @param {string} markup Markup to set.
     * @protected
     */
    setMarkup: function (markup) {
      enqueueSetMarkup(this._rootNodeID, markup);
    },

    /**
     * Mounts a child with the supplied name.
     *
     * NOTE: This is part of `updateChildren` and is here for readability.
     *
     * @param {ReactComponent} child Component to mount.
     * @param {string} name Name of the child.
     * @param {number} index Index at which to insert the child.
     * @param {ReactReconcileTransaction} transaction
     * @private
     */
    _mountChildByNameAtIndex: function (child, name, index, transaction, context) {
      // Inlined for performance, see `ReactInstanceHandles.createReactID`.
      var rootID = this._rootNodeID + name;
      var mountImage = ReactReconciler.mountComponent(child, rootID, transaction, context);
      child._mountIndex = index;
      this.createChild(child, mountImage);
    },

    /**
     * Unmounts a rendered child.
     *
     * NOTE: This is part of `updateChildren` and is here for readability.
     *
     * @param {ReactComponent} child Component to unmount.
     * @private
     */
    _unmountChild: function (child) {
      this.removeChild(child);
      child._mountIndex = null;
    }

  }

};

module.exports = ReactMultiChild;
},{"./ReactChildReconciler":254,"./ReactComponentEnvironment":259,"./ReactCurrentOwner":261,"./ReactMultiChildUpdateTypes":294,"./ReactReconciler":303,"./flattenChildren":336}],294:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactMultiChildUpdateTypes
 */

'use strict';

var keyMirror = require('fbjs/lib/keyMirror');

/**
 * When a component's children are updated, a series of update configuration
 * objects are created in order to batch and serialize the required changes.
 *
 * Enumerates all the possible types of update configurations.
 *
 * @internal
 */
var ReactMultiChildUpdateTypes = keyMirror({
  INSERT_MARKUP: null,
  MOVE_EXISTING: null,
  REMOVE_NODE: null,
  SET_MARKUP: null,
  TEXT_CONTENT: null
});

module.exports = ReactMultiChildUpdateTypes;
},{"fbjs/lib/keyMirror":154}],295:[function(require,module,exports){
/**
 * Copyright 2014-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactNativeComponent
 */

'use strict';

var assign = require('./Object.assign');
var invariant = require('fbjs/lib/invariant');

var autoGenerateWrapperClass = null;
var genericComponentClass = null;
// This registry keeps track of wrapper classes around native tags.
var tagToComponentClass = {};
var textComponentClass = null;

var ReactNativeComponentInjection = {
  // This accepts a class that receives the tag string. This is a catch all
  // that can render any kind of tag.
  injectGenericComponentClass: function (componentClass) {
    genericComponentClass = componentClass;
  },
  // This accepts a text component class that takes the text string to be
  // rendered as props.
  injectTextComponentClass: function (componentClass) {
    textComponentClass = componentClass;
  },
  // This accepts a keyed object with classes as values. Each key represents a
  // tag. That particular tag will use this class instead of the generic one.
  injectComponentClasses: function (componentClasses) {
    assign(tagToComponentClass, componentClasses);
  }
};

/**
 * Get a composite component wrapper class for a specific tag.
 *
 * @param {ReactElement} element The tag for which to get the class.
 * @return {function} The React class constructor function.
 */
function getComponentClassForElement(element) {
  if (typeof element.type === 'function') {
    return element.type;
  }
  var tag = element.type;
  var componentClass = tagToComponentClass[tag];
  if (componentClass == null) {
    tagToComponentClass[tag] = componentClass = autoGenerateWrapperClass(tag);
  }
  return componentClass;
}

/**
 * Get a native internal component class for a specific tag.
 *
 * @param {ReactElement} element The element to create.
 * @return {function} The internal class constructor function.
 */
function createInternalComponent(element) {
  !genericComponentClass ? "production" !== 'production' ? invariant(false, 'There is no registered component for the tag %s', element.type) : invariant(false) : undefined;
  return new genericComponentClass(element.type, element.props);
}

/**
 * @param {ReactText} text
 * @return {ReactComponent}
 */
function createInstanceForText(text) {
  return new textComponentClass(text);
}

/**
 * @param {ReactComponent} component
 * @return {boolean}
 */
function isTextComponent(component) {
  return component instanceof textComponentClass;
}

var ReactNativeComponent = {
  getComponentClassForElement: getComponentClassForElement,
  createInternalComponent: createInternalComponent,
  createInstanceForText: createInstanceForText,
  isTextComponent: isTextComponent,
  injection: ReactNativeComponentInjection
};

module.exports = ReactNativeComponent;
},{"./Object.assign":249,"fbjs/lib/invariant":151}],296:[function(require,module,exports){
/**
 * Copyright 2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactNoopUpdateQueue
 */

'use strict';

var warning = require('fbjs/lib/warning');

function warnTDZ(publicInstance, callerName) {
  if ("production" !== 'production') {
    "production" !== 'production' ? warning(false, '%s(...): Can only update a mounted or mounting component. ' + 'This usually means you called %s() on an unmounted component. ' + 'This is a no-op. Please check the code for the %s component.', callerName, callerName, publicInstance.constructor && publicInstance.constructor.displayName || '') : undefined;
  }
}

/**
 * This is the abstract API for an update queue.
 */
var ReactNoopUpdateQueue = {

  /**
   * Checks whether or not this composite component is mounted.
   * @param {ReactClass} publicInstance The instance we want to test.
   * @return {boolean} True if mounted, false otherwise.
   * @protected
   * @final
   */
  isMounted: function (publicInstance) {
    return false;
  },

  /**
   * Enqueue a callback that will be executed after all the pending updates
   * have processed.
   *
   * @param {ReactClass} publicInstance The instance to use as `this` context.
   * @param {?function} callback Called after state is updated.
   * @internal
   */
  enqueueCallback: function (publicInstance, callback) {},

  /**
   * Forces an update. This should only be invoked when it is known with
   * certainty that we are **not** in a DOM transaction.
   *
   * You may want to call this when you know that some deeper aspect of the
   * component's state has changed but `setState` was not called.
   *
   * This will not invoke `shouldComponentUpdate`, but it will invoke
   * `componentWillUpdate` and `componentDidUpdate`.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @internal
   */
  enqueueForceUpdate: function (publicInstance) {
    warnTDZ(publicInstance, 'forceUpdate');
  },

  /**
   * Replaces all of the state. Always use this or `setState` to mutate state.
   * You should treat `this.state` as immutable.
   *
   * There is no guarantee that `this.state` will be immediately updated, so
   * accessing `this.state` after calling this method may return the old value.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @param {object} completeState Next state.
   * @internal
   */
  enqueueReplaceState: function (publicInstance, completeState) {
    warnTDZ(publicInstance, 'replaceState');
  },

  /**
   * Sets a subset of the state. This only exists because _pendingState is
   * internal. This provides a merging strategy that is not available to deep
   * properties which is confusing. TODO: Expose pendingState or don't use it
   * during the merge.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @param {object} partialState Next partial state to be merged with state.
   * @internal
   */
  enqueueSetState: function (publicInstance, partialState) {
    warnTDZ(publicInstance, 'setState');
  },

  /**
   * Sets a subset of the props.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @param {object} partialProps Subset of the next props.
   * @internal
   */
  enqueueSetProps: function (publicInstance, partialProps) {
    warnTDZ(publicInstance, 'setProps');
  },

  /**
   * Replaces all of the props.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @param {object} props New props.
   * @internal
   */
  enqueueReplaceProps: function (publicInstance, props) {
    warnTDZ(publicInstance, 'replaceProps');
  }

};

module.exports = ReactNoopUpdateQueue;
},{"fbjs/lib/warning":162}],297:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactOwner
 */

'use strict';

var invariant = require('fbjs/lib/invariant');

/**
 * ReactOwners are capable of storing references to owned components.
 *
 * All components are capable of //being// referenced by owner components, but
 * only ReactOwner components are capable of //referencing// owned components.
 * The named reference is known as a "ref".
 *
 * Refs are available when mounted and updated during reconciliation.
 *
 *   var MyComponent = React.createClass({
 *     render: function() {
 *       return (
 *         <div onClick={this.handleClick}>
 *           <CustomComponent ref="custom" />
 *         </div>
 *       );
 *     },
 *     handleClick: function() {
 *       this.refs.custom.handleClick();
 *     },
 *     componentDidMount: function() {
 *       this.refs.custom.initialize();
 *     }
 *   });
 *
 * Refs should rarely be used. When refs are used, they should only be done to
 * control data that is not handled by React's data flow.
 *
 * @class ReactOwner
 */
var ReactOwner = {

  /**
   * @param {?object} object
   * @return {boolean} True if `object` is a valid owner.
   * @final
   */
  isValidOwner: function (object) {
    return !!(object && typeof object.attachRef === 'function' && typeof object.detachRef === 'function');
  },

  /**
   * Adds a component by ref to an owner component.
   *
   * @param {ReactComponent} component Component to reference.
   * @param {string} ref Name by which to refer to the component.
   * @param {ReactOwner} owner Component on which to record the ref.
   * @final
   * @internal
   */
  addComponentAsRefTo: function (component, ref, owner) {
    !ReactOwner.isValidOwner(owner) ? "production" !== 'production' ? invariant(false, 'addComponentAsRefTo(...): Only a ReactOwner can have refs. You might ' + 'be adding a ref to a component that was not created inside a component\'s ' + '`render` method, or you have multiple copies of React loaded ' + '(details: https://fb.me/react-refs-must-have-owner).') : invariant(false) : undefined;
    owner.attachRef(ref, component);
  },

  /**
   * Removes a component by ref from an owner component.
   *
   * @param {ReactComponent} component Component to dereference.
   * @param {string} ref Name of the ref to remove.
   * @param {ReactOwner} owner Component on which the ref is recorded.
   * @final
   * @internal
   */
  removeComponentAsRefFrom: function (component, ref, owner) {
    !ReactOwner.isValidOwner(owner) ? "production" !== 'production' ? invariant(false, 'removeComponentAsRefFrom(...): Only a ReactOwner can have refs. You might ' + 'be removing a ref to a component that was not created inside a component\'s ' + '`render` method, or you have multiple copies of React loaded ' + '(details: https://fb.me/react-refs-must-have-owner).') : invariant(false) : undefined;
    // Check that `component` is still the current ref because we do not want to
    // detach the ref if another component stole it.
    if (owner.getPublicInstance().refs[ref] === component.getPublicInstance()) {
      owner.detachRef(ref);
    }
  }

};

module.exports = ReactOwner;
},{"fbjs/lib/invariant":151}],298:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactPerf
 * @typechecks static-only
 */

'use strict';

/**
 * ReactPerf is a general AOP system designed to measure performance. This
 * module only has the hooks: see ReactDefaultPerf for the analysis tool.
 */
var ReactPerf = {
  /**
   * Boolean to enable/disable measurement. Set to false by default to prevent
   * accidental logging and perf loss.
   */
  enableMeasure: false,

  /**
   * Holds onto the measure function in use. By default, don't measure
   * anything, but we'll override this if we inject a measure function.
   */
  storedMeasure: _noMeasure,

  /**
   * @param {object} object
   * @param {string} objectName
   * @param {object<string>} methodNames
   */
  measureMethods: function (object, objectName, methodNames) {
    if ("production" !== 'production') {
      for (var key in methodNames) {
        if (!methodNames.hasOwnProperty(key)) {
          continue;
        }
        object[key] = ReactPerf.measure(objectName, methodNames[key], object[key]);
      }
    }
  },

  /**
   * Use this to wrap methods you want to measure. Zero overhead in production.
   *
   * @param {string} objName
   * @param {string} fnName
   * @param {function} func
   * @return {function}
   */
  measure: function (objName, fnName, func) {
    if ("production" !== 'production') {
      var measuredFunc = null;
      var wrapper = function () {
        if (ReactPerf.enableMeasure) {
          if (!measuredFunc) {
            measuredFunc = ReactPerf.storedMeasure(objName, fnName, func);
          }
          return measuredFunc.apply(this, arguments);
        }
        return func.apply(this, arguments);
      };
      wrapper.displayName = objName + '_' + fnName;
      return wrapper;
    }
    return func;
  },

  injection: {
    /**
     * @param {function} measure
     */
    injectMeasure: function (measure) {
      ReactPerf.storedMeasure = measure;
    }
  }
};

/**
 * Simply passes through the measured function, without measuring it.
 *
 * @param {string} objName
 * @param {string} fnName
 * @param {function} func
 * @return {function}
 */
function _noMeasure(objName, fnName, func) {
  return func;
}

module.exports = ReactPerf;
},{}],299:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactPropTypeLocationNames
 */

'use strict';

var ReactPropTypeLocationNames = {};

if ("production" !== 'production') {
  ReactPropTypeLocationNames = {
    prop: 'prop',
    context: 'context',
    childContext: 'child context'
  };
}

module.exports = ReactPropTypeLocationNames;
},{}],300:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactPropTypeLocations
 */

'use strict';

var keyMirror = require('fbjs/lib/keyMirror');

var ReactPropTypeLocations = keyMirror({
  prop: null,
  context: null,
  childContext: null
});

module.exports = ReactPropTypeLocations;
},{"fbjs/lib/keyMirror":154}],301:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactPropTypes
 */

'use strict';

var ReactElement = require('./ReactElement');
var ReactPropTypeLocationNames = require('./ReactPropTypeLocationNames');

var emptyFunction = require('fbjs/lib/emptyFunction');
var getIteratorFn = require('./getIteratorFn');

/**
 * Collection of methods that allow declaration and validation of props that are
 * supplied to React components. Example usage:
 *
 *   var Props = require('ReactPropTypes');
 *   var MyArticle = React.createClass({
 *     propTypes: {
 *       // An optional string prop named "description".
 *       description: Props.string,
 *
 *       // A required enum prop named "category".
 *       category: Props.oneOf(['News','Photos']).isRequired,
 *
 *       // A prop named "dialog" that requires an instance of Dialog.
 *       dialog: Props.instanceOf(Dialog).isRequired
 *     },
 *     render: function() { ... }
 *   });
 *
 * A more formal specification of how these methods are used:
 *
 *   type := array|bool|func|object|number|string|oneOf([...])|instanceOf(...)
 *   decl := ReactPropTypes.{type}(.isRequired)?
 *
 * Each and every declaration produces a function with the same signature. This
 * allows the creation of custom validation functions. For example:
 *
 *  var MyLink = React.createClass({
 *    propTypes: {
 *      // An optional string or URI prop named "href".
 *      href: function(props, propName, componentName) {
 *        var propValue = props[propName];
 *        if (propValue != null && typeof propValue !== 'string' &&
 *            !(propValue instanceof URI)) {
 *          return new Error(
 *            'Expected a string or an URI for ' + propName + ' in ' +
 *            componentName
 *          );
 *        }
 *      }
 *    },
 *    render: function() {...}
 *  });
 *
 * @internal
 */

var ANONYMOUS = '<<anonymous>>';

var ReactPropTypes = {
  array: createPrimitiveTypeChecker('array'),
  bool: createPrimitiveTypeChecker('boolean'),
  func: createPrimitiveTypeChecker('function'),
  number: createPrimitiveTypeChecker('number'),
  object: createPrimitiveTypeChecker('object'),
  string: createPrimitiveTypeChecker('string'),

  any: createAnyTypeChecker(),
  arrayOf: createArrayOfTypeChecker,
  element: createElementTypeChecker(),
  instanceOf: createInstanceTypeChecker,
  node: createNodeChecker(),
  objectOf: createObjectOfTypeChecker,
  oneOf: createEnumTypeChecker,
  oneOfType: createUnionTypeChecker,
  shape: createShapeTypeChecker
};

function createChainableTypeChecker(validate) {
  function checkType(isRequired, props, propName, componentName, location, propFullName) {
    componentName = componentName || ANONYMOUS;
    propFullName = propFullName || propName;
    if (props[propName] == null) {
      var locationName = ReactPropTypeLocationNames[location];
      if (isRequired) {
        return new Error('Required ' + locationName + ' `' + propFullName + '` was not specified in ' + ('`' + componentName + '`.'));
      }
      return null;
    } else {
      return validate(props, propName, componentName, location, propFullName);
    }
  }

  var chainedCheckType = checkType.bind(null, false);
  chainedCheckType.isRequired = checkType.bind(null, true);

  return chainedCheckType;
}

function createPrimitiveTypeChecker(expectedType) {
  function validate(props, propName, componentName, location, propFullName) {
    var propValue = props[propName];
    var propType = getPropType(propValue);
    if (propType !== expectedType) {
      var locationName = ReactPropTypeLocationNames[location];
      // `propValue` being instance of, say, date/regexp, pass the 'object'
      // check, but we can offer a more precise error message here rather than
      // 'of type `object`'.
      var preciseType = getPreciseType(propValue);

      return new Error('Invalid ' + locationName + ' `' + propFullName + '` of type ' + ('`' + preciseType + '` supplied to `' + componentName + '`, expected ') + ('`' + expectedType + '`.'));
    }
    return null;
  }
  return createChainableTypeChecker(validate);
}

function createAnyTypeChecker() {
  return createChainableTypeChecker(emptyFunction.thatReturns(null));
}

function createArrayOfTypeChecker(typeChecker) {
  function validate(props, propName, componentName, location, propFullName) {
    var propValue = props[propName];
    if (!Array.isArray(propValue)) {
      var locationName = ReactPropTypeLocationNames[location];
      var propType = getPropType(propValue);
      return new Error('Invalid ' + locationName + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected an array.'));
    }
    for (var i = 0; i < propValue.length; i++) {
      var error = typeChecker(propValue, i, componentName, location, propFullName + '[' + i + ']');
      if (error instanceof Error) {
        return error;
      }
    }
    return null;
  }
  return createChainableTypeChecker(validate);
}

function createElementTypeChecker() {
  function validate(props, propName, componentName, location, propFullName) {
    if (!ReactElement.isValidElement(props[propName])) {
      var locationName = ReactPropTypeLocationNames[location];
      return new Error('Invalid ' + locationName + ' `' + propFullName + '` supplied to ' + ('`' + componentName + '`, expected a single ReactElement.'));
    }
    return null;
  }
  return createChainableTypeChecker(validate);
}

function createInstanceTypeChecker(expectedClass) {
  function validate(props, propName, componentName, location, propFullName) {
    if (!(props[propName] instanceof expectedClass)) {
      var locationName = ReactPropTypeLocationNames[location];
      var expectedClassName = expectedClass.name || ANONYMOUS;
      var actualClassName = getClassName(props[propName]);
      return new Error('Invalid ' + locationName + ' `' + propFullName + '` of type ' + ('`' + actualClassName + '` supplied to `' + componentName + '`, expected ') + ('instance of `' + expectedClassName + '`.'));
    }
    return null;
  }
  return createChainableTypeChecker(validate);
}

function createEnumTypeChecker(expectedValues) {
  if (!Array.isArray(expectedValues)) {
    return createChainableTypeChecker(function () {
      return new Error('Invalid argument supplied to oneOf, expected an instance of array.');
    });
  }

  function validate(props, propName, componentName, location, propFullName) {
    var propValue = props[propName];
    for (var i = 0; i < expectedValues.length; i++) {
      if (propValue === expectedValues[i]) {
        return null;
      }
    }

    var locationName = ReactPropTypeLocationNames[location];
    var valuesString = JSON.stringify(expectedValues);
    return new Error('Invalid ' + locationName + ' `' + propFullName + '` of value `' + propValue + '` ' + ('supplied to `' + componentName + '`, expected one of ' + valuesString + '.'));
  }
  return createChainableTypeChecker(validate);
}

function createObjectOfTypeChecker(typeChecker) {
  function validate(props, propName, componentName, location, propFullName) {
    var propValue = props[propName];
    var propType = getPropType(propValue);
    if (propType !== 'object') {
      var locationName = ReactPropTypeLocationNames[location];
      return new Error('Invalid ' + locationName + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected an object.'));
    }
    for (var key in propValue) {
      if (propValue.hasOwnProperty(key)) {
        var error = typeChecker(propValue, key, componentName, location, propFullName + '.' + key);
        if (error instanceof Error) {
          return error;
        }
      }
    }
    return null;
  }
  return createChainableTypeChecker(validate);
}

function createUnionTypeChecker(arrayOfTypeCheckers) {
  if (!Array.isArray(arrayOfTypeCheckers)) {
    return createChainableTypeChecker(function () {
      return new Error('Invalid argument supplied to oneOfType, expected an instance of array.');
    });
  }

  function validate(props, propName, componentName, location, propFullName) {
    for (var i = 0; i < arrayOfTypeCheckers.length; i++) {
      var checker = arrayOfTypeCheckers[i];
      if (checker(props, propName, componentName, location, propFullName) == null) {
        return null;
      }
    }

    var locationName = ReactPropTypeLocationNames[location];
    return new Error('Invalid ' + locationName + ' `' + propFullName + '` supplied to ' + ('`' + componentName + '`.'));
  }
  return createChainableTypeChecker(validate);
}

function createNodeChecker() {
  function validate(props, propName, componentName, location, propFullName) {
    if (!isNode(props[propName])) {
      var locationName = ReactPropTypeLocationNames[location];
      return new Error('Invalid ' + locationName + ' `' + propFullName + '` supplied to ' + ('`' + componentName + '`, expected a ReactNode.'));
    }
    return null;
  }
  return createChainableTypeChecker(validate);
}

function createShapeTypeChecker(shapeTypes) {
  function validate(props, propName, componentName, location, propFullName) {
    var propValue = props[propName];
    var propType = getPropType(propValue);
    if (propType !== 'object') {
      var locationName = ReactPropTypeLocationNames[location];
      return new Error('Invalid ' + locationName + ' `' + propFullName + '` of type `' + propType + '` ' + ('supplied to `' + componentName + '`, expected `object`.'));
    }
    for (var key in shapeTypes) {
      var checker = shapeTypes[key];
      if (!checker) {
        continue;
      }
      var error = checker(propValue, key, componentName, location, propFullName + '.' + key);
      if (error) {
        return error;
      }
    }
    return null;
  }
  return createChainableTypeChecker(validate);
}

function isNode(propValue) {
  switch (typeof propValue) {
    case 'number':
    case 'string':
    case 'undefined':
      return true;
    case 'boolean':
      return !propValue;
    case 'object':
      if (Array.isArray(propValue)) {
        return propValue.every(isNode);
      }
      if (propValue === null || ReactElement.isValidElement(propValue)) {
        return true;
      }

      var iteratorFn = getIteratorFn(propValue);
      if (iteratorFn) {
        var iterator = iteratorFn.call(propValue);
        var step;
        if (iteratorFn !== propValue.entries) {
          while (!(step = iterator.next()).done) {
            if (!isNode(step.value)) {
              return false;
            }
          }
        } else {
          // Iterator will provide entry [k,v] tuples rather than values.
          while (!(step = iterator.next()).done) {
            var entry = step.value;
            if (entry) {
              if (!isNode(entry[1])) {
                return false;
              }
            }
          }
        }
      } else {
        return false;
      }

      return true;
    default:
      return false;
  }
}

// Equivalent of `typeof` but with special handling for array and regexp.
function getPropType(propValue) {
  var propType = typeof propValue;
  if (Array.isArray(propValue)) {
    return 'array';
  }
  if (propValue instanceof RegExp) {
    // Old webkits (at least until Android 4.0) return 'function' rather than
    // 'object' for typeof a RegExp. We'll normalize this here so that /bla/
    // passes PropTypes.object.
    return 'object';
  }
  return propType;
}

// This handles more types than `getPropType`. Only used for error messages.
// See `createPrimitiveTypeChecker`.
function getPreciseType(propValue) {
  var propType = getPropType(propValue);
  if (propType === 'object') {
    if (propValue instanceof Date) {
      return 'date';
    } else if (propValue instanceof RegExp) {
      return 'regexp';
    }
  }
  return propType;
}

// Returns class name of the object, if any.
function getClassName(propValue) {
  if (!propValue.constructor || !propValue.constructor.name) {
    return '<<anonymous>>';
  }
  return propValue.constructor.name;
}

module.exports = ReactPropTypes;
},{"./ReactElement":279,"./ReactPropTypeLocationNames":299,"./getIteratorFn":342,"fbjs/lib/emptyFunction":143}],302:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactReconcileTransaction
 * @typechecks static-only
 */

'use strict';

var CallbackQueue = require('./CallbackQueue');
var PooledClass = require('./PooledClass');
var ReactBrowserEventEmitter = require('./ReactBrowserEventEmitter');
var ReactDOMFeatureFlags = require('./ReactDOMFeatureFlags');
var ReactInputSelection = require('./ReactInputSelection');
var Transaction = require('./Transaction');

var assign = require('./Object.assign');

/**
 * Ensures that, when possible, the selection range (currently selected text
 * input) is not disturbed by performing the transaction.
 */
var SELECTION_RESTORATION = {
  /**
   * @return {Selection} Selection information.
   */
  initialize: ReactInputSelection.getSelectionInformation,
  /**
   * @param {Selection} sel Selection information returned from `initialize`.
   */
  close: ReactInputSelection.restoreSelection
};

/**
 * Suppresses events (blur/focus) that could be inadvertently dispatched due to
 * high level DOM manipulations (like temporarily removing a text input from the
 * DOM).
 */
var EVENT_SUPPRESSION = {
  /**
   * @return {boolean} The enabled status of `ReactBrowserEventEmitter` before
   * the reconciliation.
   */
  initialize: function () {
    var currentlyEnabled = ReactBrowserEventEmitter.isEnabled();
    ReactBrowserEventEmitter.setEnabled(false);
    return currentlyEnabled;
  },

  /**
   * @param {boolean} previouslyEnabled Enabled status of
   *   `ReactBrowserEventEmitter` before the reconciliation occurred. `close`
   *   restores the previous value.
   */
  close: function (previouslyEnabled) {
    ReactBrowserEventEmitter.setEnabled(previouslyEnabled);
  }
};

/**
 * Provides a queue for collecting `componentDidMount` and
 * `componentDidUpdate` callbacks during the the transaction.
 */
var ON_DOM_READY_QUEUEING = {
  /**
   * Initializes the internal `onDOMReady` queue.
   */
  initialize: function () {
    this.reactMountReady.reset();
  },

  /**
   * After DOM is flushed, invoke all registered `onDOMReady` callbacks.
   */
  close: function () {
    this.reactMountReady.notifyAll();
  }
};

/**
 * Executed within the scope of the `Transaction` instance. Consider these as
 * being member methods, but with an implied ordering while being isolated from
 * each other.
 */
var TRANSACTION_WRAPPERS = [SELECTION_RESTORATION, EVENT_SUPPRESSION, ON_DOM_READY_QUEUEING];

/**
 * Currently:
 * - The order that these are listed in the transaction is critical:
 * - Suppresses events.
 * - Restores selection range.
 *
 * Future:
 * - Restore document/overflow scroll positions that were unintentionally
 *   modified via DOM insertions above the top viewport boundary.
 * - Implement/integrate with customized constraint based layout system and keep
 *   track of which dimensions must be remeasured.
 *
 * @class ReactReconcileTransaction
 */
function ReactReconcileTransaction(forceHTML) {
  this.reinitializeTransaction();
  // Only server-side rendering really needs this option (see
  // `ReactServerRendering`), but server-side uses
  // `ReactServerRenderingTransaction` instead. This option is here so that it's
  // accessible and defaults to false when `ReactDOMComponent` and
  // `ReactTextComponent` checks it in `mountComponent`.`
  this.renderToStaticMarkup = false;
  this.reactMountReady = CallbackQueue.getPooled(null);
  this.useCreateElement = !forceHTML && ReactDOMFeatureFlags.useCreateElement;
}

var Mixin = {
  /**
   * @see Transaction
   * @abstract
   * @final
   * @return {array<object>} List of operation wrap procedures.
   *   TODO: convert to array<TransactionWrapper>
   */
  getTransactionWrappers: function () {
    return TRANSACTION_WRAPPERS;
  },

  /**
   * @return {object} The queue to collect `onDOMReady` callbacks with.
   */
  getReactMountReady: function () {
    return this.reactMountReady;
  },

  /**
   * `PooledClass` looks for this, and will invoke this before allowing this
   * instance to be reused.
   */
  destructor: function () {
    CallbackQueue.release(this.reactMountReady);
    this.reactMountReady = null;
  }
};

assign(ReactReconcileTransaction.prototype, Transaction.Mixin, Mixin);

PooledClass.addPoolingTo(ReactReconcileTransaction);

module.exports = ReactReconcileTransaction;
},{"./CallbackQueue":232,"./Object.assign":249,"./PooledClass":250,"./ReactBrowserEventEmitter":253,"./ReactDOMFeatureFlags":266,"./ReactInputSelection":287,"./Transaction":327}],303:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactReconciler
 */

'use strict';

var ReactRef = require('./ReactRef');

/**
 * Helper to call ReactRef.attachRefs with this composite component, split out
 * to avoid allocations in the transaction mount-ready queue.
 */
function attachRefs() {
  ReactRef.attachRefs(this, this._currentElement);
}

var ReactReconciler = {

  /**
   * Initializes the component, renders markup, and registers event listeners.
   *
   * @param {ReactComponent} internalInstance
   * @param {string} rootID DOM ID of the root node.
   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
   * @return {?string} Rendered markup to be inserted into the DOM.
   * @final
   * @internal
   */
  mountComponent: function (internalInstance, rootID, transaction, context) {
    var markup = internalInstance.mountComponent(rootID, transaction, context);
    if (internalInstance._currentElement && internalInstance._currentElement.ref != null) {
      transaction.getReactMountReady().enqueue(attachRefs, internalInstance);
    }
    return markup;
  },

  /**
   * Releases any resources allocated by `mountComponent`.
   *
   * @final
   * @internal
   */
  unmountComponent: function (internalInstance) {
    ReactRef.detachRefs(internalInstance, internalInstance._currentElement);
    internalInstance.unmountComponent();
  },

  /**
   * Update a component using a new element.
   *
   * @param {ReactComponent} internalInstance
   * @param {ReactElement} nextElement
   * @param {ReactReconcileTransaction} transaction
   * @param {object} context
   * @internal
   */
  receiveComponent: function (internalInstance, nextElement, transaction, context) {
    var prevElement = internalInstance._currentElement;

    if (nextElement === prevElement && context === internalInstance._context) {
      // Since elements are immutable after the owner is rendered,
      // we can do a cheap identity compare here to determine if this is a
      // superfluous reconcile. It's possible for state to be mutable but such
      // change should trigger an update of the owner which would recreate
      // the element. We explicitly check for the existence of an owner since
      // it's possible for an element created outside a composite to be
      // deeply mutated and reused.

      // TODO: Bailing out early is just a perf optimization right?
      // TODO: Removing the return statement should affect correctness?
      return;
    }

    var refsChanged = ReactRef.shouldUpdateRefs(prevElement, nextElement);

    if (refsChanged) {
      ReactRef.detachRefs(internalInstance, prevElement);
    }

    internalInstance.receiveComponent(nextElement, transaction, context);

    if (refsChanged && internalInstance._currentElement && internalInstance._currentElement.ref != null) {
      transaction.getReactMountReady().enqueue(attachRefs, internalInstance);
    }
  },

  /**
   * Flush any dirty changes in a component.
   *
   * @param {ReactComponent} internalInstance
   * @param {ReactReconcileTransaction} transaction
   * @internal
   */
  performUpdateIfNecessary: function (internalInstance, transaction) {
    internalInstance.performUpdateIfNecessary(transaction);
  }

};

module.exports = ReactReconciler;
},{"./ReactRef":304}],304:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactRef
 */

'use strict';

var ReactOwner = require('./ReactOwner');

var ReactRef = {};

function attachRef(ref, component, owner) {
  if (typeof ref === 'function') {
    ref(component.getPublicInstance());
  } else {
    // Legacy ref
    ReactOwner.addComponentAsRefTo(component, ref, owner);
  }
}

function detachRef(ref, component, owner) {
  if (typeof ref === 'function') {
    ref(null);
  } else {
    // Legacy ref
    ReactOwner.removeComponentAsRefFrom(component, ref, owner);
  }
}

ReactRef.attachRefs = function (instance, element) {
  if (element === null || element === false) {
    return;
  }
  var ref = element.ref;
  if (ref != null) {
    attachRef(ref, instance, element._owner);
  }
};

ReactRef.shouldUpdateRefs = function (prevElement, nextElement) {
  // If either the owner or a `ref` has changed, make sure the newest owner
  // has stored a reference to `this`, and the previous owner (if different)
  // has forgotten the reference to `this`. We use the element instead
  // of the public this.props because the post processing cannot determine
  // a ref. The ref conceptually lives on the element.

  // TODO: Should this even be possible? The owner cannot change because
  // it's forbidden by shouldUpdateReactComponent. The ref can change
  // if you swap the keys of but not the refs. Reconsider where this check
  // is made. It probably belongs where the key checking and
  // instantiateReactComponent is done.

  var prevEmpty = prevElement === null || prevElement === false;
  var nextEmpty = nextElement === null || nextElement === false;

  return(
    // This has a few false positives w/r/t empty components.
    prevEmpty || nextEmpty || nextElement._owner !== prevElement._owner || nextElement.ref !== prevElement.ref
  );
};

ReactRef.detachRefs = function (instance, element) {
  if (element === null || element === false) {
    return;
  }
  var ref = element.ref;
  if (ref != null) {
    detachRef(ref, instance, element._owner);
  }
};

module.exports = ReactRef;
},{"./ReactOwner":297}],305:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactRootIndex
 * @typechecks
 */

'use strict';

var ReactRootIndexInjection = {
  /**
   * @param {function} _createReactRootIndex
   */
  injectCreateReactRootIndex: function (_createReactRootIndex) {
    ReactRootIndex.createReactRootIndex = _createReactRootIndex;
  }
};

var ReactRootIndex = {
  createReactRootIndex: null,
  injection: ReactRootIndexInjection
};

module.exports = ReactRootIndex;
},{}],306:[function(require,module,exports){
/**
 * Copyright 2014-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactServerBatchingStrategy
 * @typechecks
 */

'use strict';

var ReactServerBatchingStrategy = {
  isBatchingUpdates: false,
  batchedUpdates: function (callback) {
    // Don't do anything here. During the server rendering we don't want to
    // schedule any updates. We will simply ignore them.
  }
};

module.exports = ReactServerBatchingStrategy;
},{}],307:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @typechecks static-only
 * @providesModule ReactServerRendering
 */
'use strict';

var ReactDefaultBatchingStrategy = require('./ReactDefaultBatchingStrategy');
var ReactElement = require('./ReactElement');
var ReactInstanceHandles = require('./ReactInstanceHandles');
var ReactMarkupChecksum = require('./ReactMarkupChecksum');
var ReactServerBatchingStrategy = require('./ReactServerBatchingStrategy');
var ReactServerRenderingTransaction = require('./ReactServerRenderingTransaction');
var ReactUpdates = require('./ReactUpdates');

var emptyObject = require('fbjs/lib/emptyObject');
var instantiateReactComponent = require('./instantiateReactComponent');
var invariant = require('fbjs/lib/invariant');

/**
 * @param {ReactElement} element
 * @return {string} the HTML markup
 */
function renderToString(element) {
  !ReactElement.isValidElement(element) ? "production" !== 'production' ? invariant(false, 'renderToString(): You must pass a valid ReactElement.') : invariant(false) : undefined;

  var transaction;
  try {
    ReactUpdates.injection.injectBatchingStrategy(ReactServerBatchingStrategy);

    var id = ReactInstanceHandles.createReactRootID();
    transaction = ReactServerRenderingTransaction.getPooled(false);

    return transaction.perform(function () {
      var componentInstance = instantiateReactComponent(element, null);
      var markup = componentInstance.mountComponent(id, transaction, emptyObject);
      return ReactMarkupChecksum.addChecksumToMarkup(markup);
    }, null);
  } finally {
    ReactServerRenderingTransaction.release(transaction);
    // Revert to the DOM batching strategy since these two renderers
    // currently share these stateful modules.
    ReactUpdates.injection.injectBatchingStrategy(ReactDefaultBatchingStrategy);
  }
}

/**
 * @param {ReactElement} element
 * @return {string} the HTML markup, without the extra React ID and checksum
 * (for generating static pages)
 */
function renderToStaticMarkup(element) {
  !ReactElement.isValidElement(element) ? "production" !== 'production' ? invariant(false, 'renderToStaticMarkup(): You must pass a valid ReactElement.') : invariant(false) : undefined;

  var transaction;
  try {
    ReactUpdates.injection.injectBatchingStrategy(ReactServerBatchingStrategy);

    var id = ReactInstanceHandles.createReactRootID();
    transaction = ReactServerRenderingTransaction.getPooled(true);

    return transaction.perform(function () {
      var componentInstance = instantiateReactComponent(element, null);
      return componentInstance.mountComponent(id, transaction, emptyObject);
    }, null);
  } finally {
    ReactServerRenderingTransaction.release(transaction);
    // Revert to the DOM batching strategy since these two renderers
    // currently share these stateful modules.
    ReactUpdates.injection.injectBatchingStrategy(ReactDefaultBatchingStrategy);
  }
}

module.exports = {
  renderToString: renderToString,
  renderToStaticMarkup: renderToStaticMarkup
};
},{"./ReactDefaultBatchingStrategy":275,"./ReactElement":279,"./ReactInstanceHandles":288,"./ReactMarkupChecksum":291,"./ReactServerBatchingStrategy":306,"./ReactServerRenderingTransaction":308,"./ReactUpdates":310,"./instantiateReactComponent":345,"fbjs/lib/emptyObject":144,"fbjs/lib/invariant":151}],308:[function(require,module,exports){
/**
 * Copyright 2014-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactServerRenderingTransaction
 * @typechecks
 */

'use strict';

var PooledClass = require('./PooledClass');
var CallbackQueue = require('./CallbackQueue');
var Transaction = require('./Transaction');

var assign = require('./Object.assign');
var emptyFunction = require('fbjs/lib/emptyFunction');

/**
 * Provides a `CallbackQueue` queue for collecting `onDOMReady` callbacks
 * during the performing of the transaction.
 */
var ON_DOM_READY_QUEUEING = {
  /**
   * Initializes the internal `onDOMReady` queue.
   */
  initialize: function () {
    this.reactMountReady.reset();
  },

  close: emptyFunction
};

/**
 * Executed within the scope of the `Transaction` instance. Consider these as
 * being member methods, but with an implied ordering while being isolated from
 * each other.
 */
var TRANSACTION_WRAPPERS = [ON_DOM_READY_QUEUEING];

/**
 * @class ReactServerRenderingTransaction
 * @param {boolean} renderToStaticMarkup
 */
function ReactServerRenderingTransaction(renderToStaticMarkup) {
  this.reinitializeTransaction();
  this.renderToStaticMarkup = renderToStaticMarkup;
  this.reactMountReady = CallbackQueue.getPooled(null);
  this.useCreateElement = false;
}

var Mixin = {
  /**
   * @see Transaction
   * @abstract
   * @final
   * @return {array} Empty list of operation wrap procedures.
   */
  getTransactionWrappers: function () {
    return TRANSACTION_WRAPPERS;
  },

  /**
   * @return {object} The queue to collect `onDOMReady` callbacks with.
   */
  getReactMountReady: function () {
    return this.reactMountReady;
  },

  /**
   * `PooledClass` looks for this, and will invoke this before allowing this
   * instance to be reused.
   */
  destructor: function () {
    CallbackQueue.release(this.reactMountReady);
    this.reactMountReady = null;
  }
};

assign(ReactServerRenderingTransaction.prototype, Transaction.Mixin, Mixin);

PooledClass.addPoolingTo(ReactServerRenderingTransaction);

module.exports = ReactServerRenderingTransaction;
},{"./CallbackQueue":232,"./Object.assign":249,"./PooledClass":250,"./Transaction":327,"fbjs/lib/emptyFunction":143}],309:[function(require,module,exports){
/**
 * Copyright 2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactUpdateQueue
 */

'use strict';

var ReactCurrentOwner = require('./ReactCurrentOwner');
var ReactElement = require('./ReactElement');
var ReactInstanceMap = require('./ReactInstanceMap');
var ReactUpdates = require('./ReactUpdates');

var assign = require('./Object.assign');
var invariant = require('fbjs/lib/invariant');
var warning = require('fbjs/lib/warning');

function enqueueUpdate(internalInstance) {
  ReactUpdates.enqueueUpdate(internalInstance);
}

function getInternalInstanceReadyForUpdate(publicInstance, callerName) {
  var internalInstance = ReactInstanceMap.get(publicInstance);
  if (!internalInstance) {
    if ("production" !== 'production') {
      // Only warn when we have a callerName. Otherwise we should be silent.
      // We're probably calling from enqueueCallback. We don't want to warn
      // there because we already warned for the corresponding lifecycle method.
      "production" !== 'production' ? warning(!callerName, '%s(...): Can only update a mounted or mounting component. ' + 'This usually means you called %s() on an unmounted component. ' + 'This is a no-op. Please check the code for the %s component.', callerName, callerName, publicInstance.constructor.displayName) : undefined;
    }
    return null;
  }

  if ("production" !== 'production') {
    "production" !== 'production' ? warning(ReactCurrentOwner.current == null, '%s(...): Cannot update during an existing state transition ' + '(such as within `render`). Render methods should be a pure function ' + 'of props and state.', callerName) : undefined;
  }

  return internalInstance;
}

/**
 * ReactUpdateQueue allows for state updates to be scheduled into a later
 * reconciliation step.
 */
var ReactUpdateQueue = {

  /**
   * Checks whether or not this composite component is mounted.
   * @param {ReactClass} publicInstance The instance we want to test.
   * @return {boolean} True if mounted, false otherwise.
   * @protected
   * @final
   */
  isMounted: function (publicInstance) {
    if ("production" !== 'production') {
      var owner = ReactCurrentOwner.current;
      if (owner !== null) {
        "production" !== 'production' ? warning(owner._warnedAboutRefsInRender, '%s is accessing isMounted inside its render() function. ' + 'render() should be a pure function of props and state. It should ' + 'never access something that requires stale data from the previous ' + 'render, such as refs. Move this logic to componentDidMount and ' + 'componentDidUpdate instead.', owner.getName() || 'A component') : undefined;
        owner._warnedAboutRefsInRender = true;
      }
    }
    var internalInstance = ReactInstanceMap.get(publicInstance);
    if (internalInstance) {
      // During componentWillMount and render this will still be null but after
      // that will always render to something. At least for now. So we can use
      // this hack.
      return !!internalInstance._renderedComponent;
    } else {
      return false;
    }
  },

  /**
   * Enqueue a callback that will be executed after all the pending updates
   * have processed.
   *
   * @param {ReactClass} publicInstance The instance to use as `this` context.
   * @param {?function} callback Called after state is updated.
   * @internal
   */
  enqueueCallback: function (publicInstance, callback) {
    !(typeof callback === 'function') ? "production" !== 'production' ? invariant(false, 'enqueueCallback(...): You called `setProps`, `replaceProps`, ' + '`setState`, `replaceState`, or `forceUpdate` with a callback that ' + 'isn\'t callable.') : invariant(false) : undefined;
    var internalInstance = getInternalInstanceReadyForUpdate(publicInstance);

    // Previously we would throw an error if we didn't have an internal
    // instance. Since we want to make it a no-op instead, we mirror the same
    // behavior we have in other enqueue* methods.
    // We also need to ignore callbacks in componentWillMount. See
    // enqueueUpdates.
    if (!internalInstance) {
      return null;
    }

    if (internalInstance._pendingCallbacks) {
      internalInstance._pendingCallbacks.push(callback);
    } else {
      internalInstance._pendingCallbacks = [callback];
    }
    // TODO: The callback here is ignored when setState is called from
    // componentWillMount. Either fix it or disallow doing so completely in
    // favor of getInitialState. Alternatively, we can disallow
    // componentWillMount during server-side rendering.
    enqueueUpdate(internalInstance);
  },

  enqueueCallbackInternal: function (internalInstance, callback) {
    !(typeof callback === 'function') ? "production" !== 'production' ? invariant(false, 'enqueueCallback(...): You called `setProps`, `replaceProps`, ' + '`setState`, `replaceState`, or `forceUpdate` with a callback that ' + 'isn\'t callable.') : invariant(false) : undefined;
    if (internalInstance._pendingCallbacks) {
      internalInstance._pendingCallbacks.push(callback);
    } else {
      internalInstance._pendingCallbacks = [callback];
    }
    enqueueUpdate(internalInstance);
  },

  /**
   * Forces an update. This should only be invoked when it is known with
   * certainty that we are **not** in a DOM transaction.
   *
   * You may want to call this when you know that some deeper aspect of the
   * component's state has changed but `setState` was not called.
   *
   * This will not invoke `shouldComponentUpdate`, but it will invoke
   * `componentWillUpdate` and `componentDidUpdate`.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @internal
   */
  enqueueForceUpdate: function (publicInstance) {
    var internalInstance = getInternalInstanceReadyForUpdate(publicInstance, 'forceUpdate');

    if (!internalInstance) {
      return;
    }

    internalInstance._pendingForceUpdate = true;

    enqueueUpdate(internalInstance);
  },

  /**
   * Replaces all of the state. Always use this or `setState` to mutate state.
   * You should treat `this.state` as immutable.
   *
   * There is no guarantee that `this.state` will be immediately updated, so
   * accessing `this.state` after calling this method may return the old value.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @param {object} completeState Next state.
   * @internal
   */
  enqueueReplaceState: function (publicInstance, completeState) {
    var internalInstance = getInternalInstanceReadyForUpdate(publicInstance, 'replaceState');

    if (!internalInstance) {
      return;
    }

    internalInstance._pendingStateQueue = [completeState];
    internalInstance._pendingReplaceState = true;

    enqueueUpdate(internalInstance);
  },

  /**
   * Sets a subset of the state. This only exists because _pendingState is
   * internal. This provides a merging strategy that is not available to deep
   * properties which is confusing. TODO: Expose pendingState or don't use it
   * during the merge.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @param {object} partialState Next partial state to be merged with state.
   * @internal
   */
  enqueueSetState: function (publicInstance, partialState) {
    var internalInstance = getInternalInstanceReadyForUpdate(publicInstance, 'setState');

    if (!internalInstance) {
      return;
    }

    var queue = internalInstance._pendingStateQueue || (internalInstance._pendingStateQueue = []);
    queue.push(partialState);

    enqueueUpdate(internalInstance);
  },

  /**
   * Sets a subset of the props.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @param {object} partialProps Subset of the next props.
   * @internal
   */
  enqueueSetProps: function (publicInstance, partialProps) {
    var internalInstance = getInternalInstanceReadyForUpdate(publicInstance, 'setProps');
    if (!internalInstance) {
      return;
    }
    ReactUpdateQueue.enqueueSetPropsInternal(internalInstance, partialProps);
  },

  enqueueSetPropsInternal: function (internalInstance, partialProps) {
    var topLevelWrapper = internalInstance._topLevelWrapper;
    !topLevelWrapper ? "production" !== 'production' ? invariant(false, 'setProps(...): You called `setProps` on a ' + 'component with a parent. This is an anti-pattern since props will ' + 'get reactively updated when rendered. Instead, change the owner\'s ' + '`render` method to pass the correct value as props to the component ' + 'where it is created.') : invariant(false) : undefined;

    // Merge with the pending element if it exists, otherwise with existing
    // element props.
    var wrapElement = topLevelWrapper._pendingElement || topLevelWrapper._currentElement;
    var element = wrapElement.props;
    var props = assign({}, element.props, partialProps);
    topLevelWrapper._pendingElement = ReactElement.cloneAndReplaceProps(wrapElement, ReactElement.cloneAndReplaceProps(element, props));

    enqueueUpdate(topLevelWrapper);
  },

  /**
   * Replaces all of the props.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @param {object} props New props.
   * @internal
   */
  enqueueReplaceProps: function (publicInstance, props) {
    var internalInstance = getInternalInstanceReadyForUpdate(publicInstance, 'replaceProps');
    if (!internalInstance) {
      return;
    }
    ReactUpdateQueue.enqueueReplacePropsInternal(internalInstance, props);
  },

  enqueueReplacePropsInternal: function (internalInstance, props) {
    var topLevelWrapper = internalInstance._topLevelWrapper;
    !topLevelWrapper ? "production" !== 'production' ? invariant(false, 'replaceProps(...): You called `replaceProps` on a ' + 'component with a parent. This is an anti-pattern since props will ' + 'get reactively updated when rendered. Instead, change the owner\'s ' + '`render` method to pass the correct value as props to the component ' + 'where it is created.') : invariant(false) : undefined;

    // Merge with the pending element if it exists, otherwise with existing
    // element props.
    var wrapElement = topLevelWrapper._pendingElement || topLevelWrapper._currentElement;
    var element = wrapElement.props;
    topLevelWrapper._pendingElement = ReactElement.cloneAndReplaceProps(wrapElement, ReactElement.cloneAndReplaceProps(element, props));

    enqueueUpdate(topLevelWrapper);
  },

  enqueueElementInternal: function (internalInstance, newElement) {
    internalInstance._pendingElement = newElement;
    enqueueUpdate(internalInstance);
  }

};

module.exports = ReactUpdateQueue;
},{"./Object.assign":249,"./ReactCurrentOwner":261,"./ReactElement":279,"./ReactInstanceMap":289,"./ReactUpdates":310,"fbjs/lib/invariant":151,"fbjs/lib/warning":162}],310:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactUpdates
 */

'use strict';

var CallbackQueue = require('./CallbackQueue');
var PooledClass = require('./PooledClass');
var ReactPerf = require('./ReactPerf');
var ReactReconciler = require('./ReactReconciler');
var Transaction = require('./Transaction');

var assign = require('./Object.assign');
var invariant = require('fbjs/lib/invariant');

var dirtyComponents = [];
var asapCallbackQueue = CallbackQueue.getPooled();
var asapEnqueued = false;

var batchingStrategy = null;

function ensureInjected() {
  !(ReactUpdates.ReactReconcileTransaction && batchingStrategy) ? "production" !== 'production' ? invariant(false, 'ReactUpdates: must inject a reconcile transaction class and batching ' + 'strategy') : invariant(false) : undefined;
}

var NESTED_UPDATES = {
  initialize: function () {
    this.dirtyComponentsLength = dirtyComponents.length;
  },
  close: function () {
    if (this.dirtyComponentsLength !== dirtyComponents.length) {
      // Additional updates were enqueued by componentDidUpdate handlers or
      // similar; before our own UPDATE_QUEUEING wrapper closes, we want to run
      // these new updates so that if A's componentDidUpdate calls setState on
      // B, B will update before the callback A's updater provided when calling
      // setState.
      dirtyComponents.splice(0, this.dirtyComponentsLength);
      flushBatchedUpdates();
    } else {
      dirtyComponents.length = 0;
    }
  }
};

var UPDATE_QUEUEING = {
  initialize: function () {
    this.callbackQueue.reset();
  },
  close: function () {
    this.callbackQueue.notifyAll();
  }
};

var TRANSACTION_WRAPPERS = [NESTED_UPDATES, UPDATE_QUEUEING];

function ReactUpdatesFlushTransaction() {
  this.reinitializeTransaction();
  this.dirtyComponentsLength = null;
  this.callbackQueue = CallbackQueue.getPooled();
  this.reconcileTransaction = ReactUpdates.ReactReconcileTransaction.getPooled( /* forceHTML */false);
}

assign(ReactUpdatesFlushTransaction.prototype, Transaction.Mixin, {
  getTransactionWrappers: function () {
    return TRANSACTION_WRAPPERS;
  },

  destructor: function () {
    this.dirtyComponentsLength = null;
    CallbackQueue.release(this.callbackQueue);
    this.callbackQueue = null;
    ReactUpdates.ReactReconcileTransaction.release(this.reconcileTransaction);
    this.reconcileTransaction = null;
  },

  perform: function (method, scope, a) {
    // Essentially calls `this.reconcileTransaction.perform(method, scope, a)`
    // with this transaction's wrappers around it.
    return Transaction.Mixin.perform.call(this, this.reconcileTransaction.perform, this.reconcileTransaction, method, scope, a);
  }
});

PooledClass.addPoolingTo(ReactUpdatesFlushTransaction);

function batchedUpdates(callback, a, b, c, d, e) {
  ensureInjected();
  batchingStrategy.batchedUpdates(callback, a, b, c, d, e);
}

/**
 * Array comparator for ReactComponents by mount ordering.
 *
 * @param {ReactComponent} c1 first component you're comparing
 * @param {ReactComponent} c2 second component you're comparing
 * @return {number} Return value usable by Array.prototype.sort().
 */
function mountOrderComparator(c1, c2) {
  return c1._mountOrder - c2._mountOrder;
}

function runBatchedUpdates(transaction) {
  var len = transaction.dirtyComponentsLength;
  !(len === dirtyComponents.length) ? "production" !== 'production' ? invariant(false, 'Expected flush transaction\'s stored dirty-components length (%s) to ' + 'match dirty-components array length (%s).', len, dirtyComponents.length) : invariant(false) : undefined;

  // Since reconciling a component higher in the owner hierarchy usually (not
  // always -- see shouldComponentUpdate()) will reconcile children, reconcile
  // them before their children by sorting the array.
  dirtyComponents.sort(mountOrderComparator);

  for (var i = 0; i < len; i++) {
    // If a component is unmounted before pending changes apply, it will still
    // be here, but we assume that it has cleared its _pendingCallbacks and
    // that performUpdateIfNecessary is a noop.
    var component = dirtyComponents[i];

    // If performUpdateIfNecessary happens to enqueue any new updates, we
    // shouldn't execute the callbacks until the next render happens, so
    // stash the callbacks first
    var callbacks = component._pendingCallbacks;
    component._pendingCallbacks = null;

    ReactReconciler.performUpdateIfNecessary(component, transaction.reconcileTransaction);

    if (callbacks) {
      for (var j = 0; j < callbacks.length; j++) {
        transaction.callbackQueue.enqueue(callbacks[j], component.getPublicInstance());
      }
    }
  }
}

var flushBatchedUpdates = function () {
  // ReactUpdatesFlushTransaction's wrappers will clear the dirtyComponents
  // array and perform any updates enqueued by mount-ready handlers (i.e.,
  // componentDidUpdate) but we need to check here too in order to catch
  // updates enqueued by setState callbacks and asap calls.
  while (dirtyComponents.length || asapEnqueued) {
    if (dirtyComponents.length) {
      var transaction = ReactUpdatesFlushTransaction.getPooled();
      transaction.perform(runBatchedUpdates, null, transaction);
      ReactUpdatesFlushTransaction.release(transaction);
    }

    if (asapEnqueued) {
      asapEnqueued = false;
      var queue = asapCallbackQueue;
      asapCallbackQueue = CallbackQueue.getPooled();
      queue.notifyAll();
      CallbackQueue.release(queue);
    }
  }
};
flushBatchedUpdates = ReactPerf.measure('ReactUpdates', 'flushBatchedUpdates', flushBatchedUpdates);

/**
 * Mark a component as needing a rerender, adding an optional callback to a
 * list of functions which will be executed once the rerender occurs.
 */
function enqueueUpdate(component) {
  ensureInjected();

  // Various parts of our code (such as ReactCompositeComponent's
  // _renderValidatedComponent) assume that calls to render aren't nested;
  // verify that that's the case. (This is called by each top-level update
  // function, like setProps, setState, forceUpdate, etc.; creation and
  // destruction of top-level components is guarded in ReactMount.)

  if (!batchingStrategy.isBatchingUpdates) {
    batchingStrategy.batchedUpdates(enqueueUpdate, component);
    return;
  }

  dirtyComponents.push(component);
}

/**
 * Enqueue a callback to be run at the end of the current batching cycle. Throws
 * if no updates are currently being performed.
 */
function asap(callback, context) {
  !batchingStrategy.isBatchingUpdates ? "production" !== 'production' ? invariant(false, 'ReactUpdates.asap: Can\'t enqueue an asap callback in a context where' + 'updates are not being batched.') : invariant(false) : undefined;
  asapCallbackQueue.enqueue(callback, context);
  asapEnqueued = true;
}

var ReactUpdatesInjection = {
  injectReconcileTransaction: function (ReconcileTransaction) {
    !ReconcileTransaction ? "production" !== 'production' ? invariant(false, 'ReactUpdates: must provide a reconcile transaction class') : invariant(false) : undefined;
    ReactUpdates.ReactReconcileTransaction = ReconcileTransaction;
  },

  injectBatchingStrategy: function (_batchingStrategy) {
    !_batchingStrategy ? "production" !== 'production' ? invariant(false, 'ReactUpdates: must provide a batching strategy') : invariant(false) : undefined;
    !(typeof _batchingStrategy.batchedUpdates === 'function') ? "production" !== 'production' ? invariant(false, 'ReactUpdates: must provide a batchedUpdates() function') : invariant(false) : undefined;
    !(typeof _batchingStrategy.isBatchingUpdates === 'boolean') ? "production" !== 'production' ? invariant(false, 'ReactUpdates: must provide an isBatchingUpdates boolean attribute') : invariant(false) : undefined;
    batchingStrategy = _batchingStrategy;
  }
};

var ReactUpdates = {
  /**
   * React references `ReactReconcileTransaction` using this property in order
   * to allow dependency injection.
   *
   * @internal
   */
  ReactReconcileTransaction: null,

  batchedUpdates: batchedUpdates,
  enqueueUpdate: enqueueUpdate,
  flushBatchedUpdates: flushBatchedUpdates,
  injection: ReactUpdatesInjection,
  asap: asap
};

module.exports = ReactUpdates;
},{"./CallbackQueue":232,"./Object.assign":249,"./PooledClass":250,"./ReactPerf":298,"./ReactReconciler":303,"./Transaction":327,"fbjs/lib/invariant":151}],311:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ReactVersion
 */

'use strict';

module.exports = '0.14.8';
},{}],312:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule SVGDOMPropertyConfig
 */

'use strict';

var DOMProperty = require('./DOMProperty');

var MUST_USE_ATTRIBUTE = DOMProperty.injection.MUST_USE_ATTRIBUTE;

var NS = {
  xlink: 'http://www.w3.org/1999/xlink',
  xml: 'http://www.w3.org/XML/1998/namespace'
};

var SVGDOMPropertyConfig = {
  Properties: {
    clipPath: MUST_USE_ATTRIBUTE,
    cx: MUST_USE_ATTRIBUTE,
    cy: MUST_USE_ATTRIBUTE,
    d: MUST_USE_ATTRIBUTE,
    dx: MUST_USE_ATTRIBUTE,
    dy: MUST_USE_ATTRIBUTE,
    fill: MUST_USE_ATTRIBUTE,
    fillOpacity: MUST_USE_ATTRIBUTE,
    fontFamily: MUST_USE_ATTRIBUTE,
    fontSize: MUST_USE_ATTRIBUTE,
    fx: MUST_USE_ATTRIBUTE,
    fy: MUST_USE_ATTRIBUTE,
    gradientTransform: MUST_USE_ATTRIBUTE,
    gradientUnits: MUST_USE_ATTRIBUTE,
    markerEnd: MUST_USE_ATTRIBUTE,
    markerMid: MUST_USE_ATTRIBUTE,
    markerStart: MUST_USE_ATTRIBUTE,
    offset: MUST_USE_ATTRIBUTE,
    opacity: MUST_USE_ATTRIBUTE,
    patternContentUnits: MUST_USE_ATTRIBUTE,
    patternUnits: MUST_USE_ATTRIBUTE,
    points: MUST_USE_ATTRIBUTE,
    preserveAspectRatio: MUST_USE_ATTRIBUTE,
    r: MUST_USE_ATTRIBUTE,
    rx: MUST_USE_ATTRIBUTE,
    ry: MUST_USE_ATTRIBUTE,
    spreadMethod: MUST_USE_ATTRIBUTE,
    stopColor: MUST_USE_ATTRIBUTE,
    stopOpacity: MUST_USE_ATTRIBUTE,
    stroke: MUST_USE_ATTRIBUTE,
    strokeDasharray: MUST_USE_ATTRIBUTE,
    strokeLinecap: MUST_USE_ATTRIBUTE,
    strokeOpacity: MUST_USE_ATTRIBUTE,
    strokeWidth: MUST_USE_ATTRIBUTE,
    textAnchor: MUST_USE_ATTRIBUTE,
    transform: MUST_USE_ATTRIBUTE,
    version: MUST_USE_ATTRIBUTE,
    viewBox: MUST_USE_ATTRIBUTE,
    x1: MUST_USE_ATTRIBUTE,
    x2: MUST_USE_ATTRIBUTE,
    x: MUST_USE_ATTRIBUTE,
    xlinkActuate: MUST_USE_ATTRIBUTE,
    xlinkArcrole: MUST_USE_ATTRIBUTE,
    xlinkHref: MUST_USE_ATTRIBUTE,
    xlinkRole: MUST_USE_ATTRIBUTE,
    xlinkShow: MUST_USE_ATTRIBUTE,
    xlinkTitle: MUST_USE_ATTRIBUTE,
    xlinkType: MUST_USE_ATTRIBUTE,
    xmlBase: MUST_USE_ATTRIBUTE,
    xmlLang: MUST_USE_ATTRIBUTE,
    xmlSpace: MUST_USE_ATTRIBUTE,
    y1: MUST_USE_ATTRIBUTE,
    y2: MUST_USE_ATTRIBUTE,
    y: MUST_USE_ATTRIBUTE
  },
  DOMAttributeNamespaces: {
    xlinkActuate: NS.xlink,
    xlinkArcrole: NS.xlink,
    xlinkHref: NS.xlink,
    xlinkRole: NS.xlink,
    xlinkShow: NS.xlink,
    xlinkTitle: NS.xlink,
    xlinkType: NS.xlink,
    xmlBase: NS.xml,
    xmlLang: NS.xml,
    xmlSpace: NS.xml
  },
  DOMAttributeNames: {
    clipPath: 'clip-path',
    fillOpacity: 'fill-opacity',
    fontFamily: 'font-family',
    fontSize: 'font-size',
    gradientTransform: 'gradientTransform',
    gradientUnits: 'gradientUnits',
    markerEnd: 'marker-end',
    markerMid: 'marker-mid',
    markerStart: 'marker-start',
    patternContentUnits: 'patternContentUnits',
    patternUnits: 'patternUnits',
    preserveAspectRatio: 'preserveAspectRatio',
    spreadMethod: 'spreadMethod',
    stopColor: 'stop-color',
    stopOpacity: 'stop-opacity',
    strokeDasharray: 'stroke-dasharray',
    strokeLinecap: 'stroke-linecap',
    strokeOpacity: 'stroke-opacity',
    strokeWidth: 'stroke-width',
    textAnchor: 'text-anchor',
    viewBox: 'viewBox',
    xlinkActuate: 'xlink:actuate',
    xlinkArcrole: 'xlink:arcrole',
    xlinkHref: 'xlink:href',
    xlinkRole: 'xlink:role',
    xlinkShow: 'xlink:show',
    xlinkTitle: 'xlink:title',
    xlinkType: 'xlink:type',
    xmlBase: 'xml:base',
    xmlLang: 'xml:lang',
    xmlSpace: 'xml:space'
  }
};

module.exports = SVGDOMPropertyConfig;
},{"./DOMProperty":236}],313:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule SelectEventPlugin
 */

'use strict';

var EventConstants = require('./EventConstants');
var EventPropagators = require('./EventPropagators');
var ExecutionEnvironment = require('fbjs/lib/ExecutionEnvironment');
var ReactInputSelection = require('./ReactInputSelection');
var SyntheticEvent = require('./SyntheticEvent');

var getActiveElement = require('fbjs/lib/getActiveElement');
var isTextInputElement = require('./isTextInputElement');
var keyOf = require('fbjs/lib/keyOf');
var shallowEqual = require('fbjs/lib/shallowEqual');

var topLevelTypes = EventConstants.topLevelTypes;

var skipSelectionChangeEvent = ExecutionEnvironment.canUseDOM && 'documentMode' in document && document.documentMode <= 11;

var eventTypes = {
  select: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onSelect: null }),
      captured: keyOf({ onSelectCapture: null })
    },
    dependencies: [topLevelTypes.topBlur, topLevelTypes.topContextMenu, topLevelTypes.topFocus, topLevelTypes.topKeyDown, topLevelTypes.topMouseDown, topLevelTypes.topMouseUp, topLevelTypes.topSelectionChange]
  }
};

var activeElement = null;
var activeElementID = null;
var lastSelection = null;
var mouseDown = false;

// Track whether a listener exists for this plugin. If none exist, we do
// not extract events.
var hasListener = false;
var ON_SELECT_KEY = keyOf({ onSelect: null });

/**
 * Get an object which is a unique representation of the current selection.
 *
 * The return value will not be consistent across nodes or browsers, but
 * two identical selections on the same node will return identical objects.
 *
 * @param {DOMElement} node
 * @return {object}
 */
function getSelection(node) {
  if ('selectionStart' in node && ReactInputSelection.hasSelectionCapabilities(node)) {
    return {
      start: node.selectionStart,
      end: node.selectionEnd
    };
  } else if (window.getSelection) {
    var selection = window.getSelection();
    return {
      anchorNode: selection.anchorNode,
      anchorOffset: selection.anchorOffset,
      focusNode: selection.focusNode,
      focusOffset: selection.focusOffset
    };
  } else if (document.selection) {
    var range = document.selection.createRange();
    return {
      parentElement: range.parentElement(),
      text: range.text,
      top: range.boundingTop,
      left: range.boundingLeft
    };
  }
}

/**
 * Poll selection to see whether it's changed.
 *
 * @param {object} nativeEvent
 * @return {?SyntheticEvent}
 */
function constructSelectEvent(nativeEvent, nativeEventTarget) {
  // Ensure we have the right element, and that the user is not dragging a
  // selection (this matches native `select` event behavior). In HTML5, select
  // fires only on input and textarea thus if there's no focused element we
  // won't dispatch.
  if (mouseDown || activeElement == null || activeElement !== getActiveElement()) {
    return null;
  }

  // Only fire when selection has actually changed.
  var currentSelection = getSelection(activeElement);
  if (!lastSelection || !shallowEqual(lastSelection, currentSelection)) {
    lastSelection = currentSelection;

    var syntheticEvent = SyntheticEvent.getPooled(eventTypes.select, activeElementID, nativeEvent, nativeEventTarget);

    syntheticEvent.type = 'select';
    syntheticEvent.target = activeElement;

    EventPropagators.accumulateTwoPhaseDispatches(syntheticEvent);

    return syntheticEvent;
  }

  return null;
}

/**
 * This plugin creates an `onSelect` event that normalizes select events
 * across form elements.
 *
 * Supported elements are:
 * - input (see `isTextInputElement`)
 * - textarea
 * - contentEditable
 *
 * This differs from native browser implementations in the following ways:
 * - Fires on contentEditable fields as well as inputs.
 * - Fires for collapsed selection.
 * - Fires after user input.
 */
var SelectEventPlugin = {

  eventTypes: eventTypes,

  /**
   * @param {string} topLevelType Record from `EventConstants`.
   * @param {DOMEventTarget} topLevelTarget The listening component root node.
   * @param {string} topLevelTargetID ID of `topLevelTarget`.
   * @param {object} nativeEvent Native browser event.
   * @return {*} An accumulation of synthetic events.
   * @see {EventPluginHub.extractEvents}
   */
  extractEvents: function (topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget) {
    if (!hasListener) {
      return null;
    }

    switch (topLevelType) {
      // Track the input node that has focus.
      case topLevelTypes.topFocus:
        if (isTextInputElement(topLevelTarget) || topLevelTarget.contentEditable === 'true') {
          activeElement = topLevelTarget;
          activeElementID = topLevelTargetID;
          lastSelection = null;
        }
        break;
      case topLevelTypes.topBlur:
        activeElement = null;
        activeElementID = null;
        lastSelection = null;
        break;

      // Don't fire the event while the user is dragging. This matches the
      // semantics of the native select event.
      case topLevelTypes.topMouseDown:
        mouseDown = true;
        break;
      case topLevelTypes.topContextMenu:
      case topLevelTypes.topMouseUp:
        mouseDown = false;
        return constructSelectEvent(nativeEvent, nativeEventTarget);

      // Chrome and IE fire non-standard event when selection is changed (and
      // sometimes when it hasn't). IE's event fires out of order with respect
      // to key and input events on deletion, so we discard it.
      //
      // Firefox doesn't support selectionchange, so check selection status
      // after each key entry. The selection changes after keydown and before
      // keyup, but we check on keydown as well in the case of holding down a
      // key, when multiple keydown events are fired but only one keyup is.
      // This is also our approach for IE handling, for the reason above.
      case topLevelTypes.topSelectionChange:
        if (skipSelectionChangeEvent) {
          break;
        }
      // falls through
      case topLevelTypes.topKeyDown:
      case topLevelTypes.topKeyUp:
        return constructSelectEvent(nativeEvent, nativeEventTarget);
    }

    return null;
  },

  didPutListener: function (id, registrationName, listener) {
    if (registrationName === ON_SELECT_KEY) {
      hasListener = true;
    }
  }
};

module.exports = SelectEventPlugin;
},{"./EventConstants":241,"./EventPropagators":245,"./ReactInputSelection":287,"./SyntheticEvent":319,"./isTextInputElement":347,"fbjs/lib/ExecutionEnvironment":137,"fbjs/lib/getActiveElement":146,"fbjs/lib/keyOf":155,"fbjs/lib/shallowEqual":160}],314:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ServerReactRootIndex
 * @typechecks
 */

'use strict';

/**
 * Size of the reactRoot ID space. We generate random numbers for React root
 * IDs and if there's a collision the events and DOM update system will
 * get confused. In the future we need a way to generate GUIDs but for
 * now this will work on a smaller scale.
 */
var GLOBAL_MOUNT_POINT_MAX = Math.pow(2, 53);

var ServerReactRootIndex = {
  createReactRootIndex: function () {
    return Math.ceil(Math.random() * GLOBAL_MOUNT_POINT_MAX);
  }
};

module.exports = ServerReactRootIndex;
},{}],315:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule SimpleEventPlugin
 */

'use strict';

var EventConstants = require('./EventConstants');
var EventListener = require('fbjs/lib/EventListener');
var EventPropagators = require('./EventPropagators');
var ReactMount = require('./ReactMount');
var SyntheticClipboardEvent = require('./SyntheticClipboardEvent');
var SyntheticEvent = require('./SyntheticEvent');
var SyntheticFocusEvent = require('./SyntheticFocusEvent');
var SyntheticKeyboardEvent = require('./SyntheticKeyboardEvent');
var SyntheticMouseEvent = require('./SyntheticMouseEvent');
var SyntheticDragEvent = require('./SyntheticDragEvent');
var SyntheticTouchEvent = require('./SyntheticTouchEvent');
var SyntheticUIEvent = require('./SyntheticUIEvent');
var SyntheticWheelEvent = require('./SyntheticWheelEvent');

var emptyFunction = require('fbjs/lib/emptyFunction');
var getEventCharCode = require('./getEventCharCode');
var invariant = require('fbjs/lib/invariant');
var keyOf = require('fbjs/lib/keyOf');

var topLevelTypes = EventConstants.topLevelTypes;

var eventTypes = {
  abort: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onAbort: true }),
      captured: keyOf({ onAbortCapture: true })
    }
  },
  blur: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onBlur: true }),
      captured: keyOf({ onBlurCapture: true })
    }
  },
  canPlay: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onCanPlay: true }),
      captured: keyOf({ onCanPlayCapture: true })
    }
  },
  canPlayThrough: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onCanPlayThrough: true }),
      captured: keyOf({ onCanPlayThroughCapture: true })
    }
  },
  click: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onClick: true }),
      captured: keyOf({ onClickCapture: true })
    }
  },
  contextMenu: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onContextMenu: true }),
      captured: keyOf({ onContextMenuCapture: true })
    }
  },
  copy: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onCopy: true }),
      captured: keyOf({ onCopyCapture: true })
    }
  },
  cut: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onCut: true }),
      captured: keyOf({ onCutCapture: true })
    }
  },
  doubleClick: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onDoubleClick: true }),
      captured: keyOf({ onDoubleClickCapture: true })
    }
  },
  drag: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onDrag: true }),
      captured: keyOf({ onDragCapture: true })
    }
  },
  dragEnd: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onDragEnd: true }),
      captured: keyOf({ onDragEndCapture: true })
    }
  },
  dragEnter: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onDragEnter: true }),
      captured: keyOf({ onDragEnterCapture: true })
    }
  },
  dragExit: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onDragExit: true }),
      captured: keyOf({ onDragExitCapture: true })
    }
  },
  dragLeave: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onDragLeave: true }),
      captured: keyOf({ onDragLeaveCapture: true })
    }
  },
  dragOver: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onDragOver: true }),
      captured: keyOf({ onDragOverCapture: true })
    }
  },
  dragStart: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onDragStart: true }),
      captured: keyOf({ onDragStartCapture: true })
    }
  },
  drop: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onDrop: true }),
      captured: keyOf({ onDropCapture: true })
    }
  },
  durationChange: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onDurationChange: true }),
      captured: keyOf({ onDurationChangeCapture: true })
    }
  },
  emptied: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onEmptied: true }),
      captured: keyOf({ onEmptiedCapture: true })
    }
  },
  encrypted: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onEncrypted: true }),
      captured: keyOf({ onEncryptedCapture: true })
    }
  },
  ended: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onEnded: true }),
      captured: keyOf({ onEndedCapture: true })
    }
  },
  error: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onError: true }),
      captured: keyOf({ onErrorCapture: true })
    }
  },
  focus: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onFocus: true }),
      captured: keyOf({ onFocusCapture: true })
    }
  },
  input: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onInput: true }),
      captured: keyOf({ onInputCapture: true })
    }
  },
  keyDown: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onKeyDown: true }),
      captured: keyOf({ onKeyDownCapture: true })
    }
  },
  keyPress: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onKeyPress: true }),
      captured: keyOf({ onKeyPressCapture: true })
    }
  },
  keyUp: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onKeyUp: true }),
      captured: keyOf({ onKeyUpCapture: true })
    }
  },
  load: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onLoad: true }),
      captured: keyOf({ onLoadCapture: true })
    }
  },
  loadedData: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onLoadedData: true }),
      captured: keyOf({ onLoadedDataCapture: true })
    }
  },
  loadedMetadata: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onLoadedMetadata: true }),
      captured: keyOf({ onLoadedMetadataCapture: true })
    }
  },
  loadStart: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onLoadStart: true }),
      captured: keyOf({ onLoadStartCapture: true })
    }
  },
  // Note: We do not allow listening to mouseOver events. Instead, use the
  // onMouseEnter/onMouseLeave created by `EnterLeaveEventPlugin`.
  mouseDown: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onMouseDown: true }),
      captured: keyOf({ onMouseDownCapture: true })
    }
  },
  mouseMove: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onMouseMove: true }),
      captured: keyOf({ onMouseMoveCapture: true })
    }
  },
  mouseOut: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onMouseOut: true }),
      captured: keyOf({ onMouseOutCapture: true })
    }
  },
  mouseOver: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onMouseOver: true }),
      captured: keyOf({ onMouseOverCapture: true })
    }
  },
  mouseUp: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onMouseUp: true }),
      captured: keyOf({ onMouseUpCapture: true })
    }
  },
  paste: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onPaste: true }),
      captured: keyOf({ onPasteCapture: true })
    }
  },
  pause: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onPause: true }),
      captured: keyOf({ onPauseCapture: true })
    }
  },
  play: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onPlay: true }),
      captured: keyOf({ onPlayCapture: true })
    }
  },
  playing: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onPlaying: true }),
      captured: keyOf({ onPlayingCapture: true })
    }
  },
  progress: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onProgress: true }),
      captured: keyOf({ onProgressCapture: true })
    }
  },
  rateChange: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onRateChange: true }),
      captured: keyOf({ onRateChangeCapture: true })
    }
  },
  reset: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onReset: true }),
      captured: keyOf({ onResetCapture: true })
    }
  },
  scroll: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onScroll: true }),
      captured: keyOf({ onScrollCapture: true })
    }
  },
  seeked: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onSeeked: true }),
      captured: keyOf({ onSeekedCapture: true })
    }
  },
  seeking: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onSeeking: true }),
      captured: keyOf({ onSeekingCapture: true })
    }
  },
  stalled: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onStalled: true }),
      captured: keyOf({ onStalledCapture: true })
    }
  },
  submit: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onSubmit: true }),
      captured: keyOf({ onSubmitCapture: true })
    }
  },
  suspend: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onSuspend: true }),
      captured: keyOf({ onSuspendCapture: true })
    }
  },
  timeUpdate: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onTimeUpdate: true }),
      captured: keyOf({ onTimeUpdateCapture: true })
    }
  },
  touchCancel: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onTouchCancel: true }),
      captured: keyOf({ onTouchCancelCapture: true })
    }
  },
  touchEnd: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onTouchEnd: true }),
      captured: keyOf({ onTouchEndCapture: true })
    }
  },
  touchMove: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onTouchMove: true }),
      captured: keyOf({ onTouchMoveCapture: true })
    }
  },
  touchStart: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onTouchStart: true }),
      captured: keyOf({ onTouchStartCapture: true })
    }
  },
  volumeChange: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onVolumeChange: true }),
      captured: keyOf({ onVolumeChangeCapture: true })
    }
  },
  waiting: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onWaiting: true }),
      captured: keyOf({ onWaitingCapture: true })
    }
  },
  wheel: {
    phasedRegistrationNames: {
      bubbled: keyOf({ onWheel: true }),
      captured: keyOf({ onWheelCapture: true })
    }
  }
};

var topLevelEventsToDispatchConfig = {
  topAbort: eventTypes.abort,
  topBlur: eventTypes.blur,
  topCanPlay: eventTypes.canPlay,
  topCanPlayThrough: eventTypes.canPlayThrough,
  topClick: eventTypes.click,
  topContextMenu: eventTypes.contextMenu,
  topCopy: eventTypes.copy,
  topCut: eventTypes.cut,
  topDoubleClick: eventTypes.doubleClick,
  topDrag: eventTypes.drag,
  topDragEnd: eventTypes.dragEnd,
  topDragEnter: eventTypes.dragEnter,
  topDragExit: eventTypes.dragExit,
  topDragLeave: eventTypes.dragLeave,
  topDragOver: eventTypes.dragOver,
  topDragStart: eventTypes.dragStart,
  topDrop: eventTypes.drop,
  topDurationChange: eventTypes.durationChange,
  topEmptied: eventTypes.emptied,
  topEncrypted: eventTypes.encrypted,
  topEnded: eventTypes.ended,
  topError: eventTypes.error,
  topFocus: eventTypes.focus,
  topInput: eventTypes.input,
  topKeyDown: eventTypes.keyDown,
  topKeyPress: eventTypes.keyPress,
  topKeyUp: eventTypes.keyUp,
  topLoad: eventTypes.load,
  topLoadedData: eventTypes.loadedData,
  topLoadedMetadata: eventTypes.loadedMetadata,
  topLoadStart: eventTypes.loadStart,
  topMouseDown: eventTypes.mouseDown,
  topMouseMove: eventTypes.mouseMove,
  topMouseOut: eventTypes.mouseOut,
  topMouseOver: eventTypes.mouseOver,
  topMouseUp: eventTypes.mouseUp,
  topPaste: eventTypes.paste,
  topPause: eventTypes.pause,
  topPlay: eventTypes.play,
  topPlaying: eventTypes.playing,
  topProgress: eventTypes.progress,
  topRateChange: eventTypes.rateChange,
  topReset: eventTypes.reset,
  topScroll: eventTypes.scroll,
  topSeeked: eventTypes.seeked,
  topSeeking: eventTypes.seeking,
  topStalled: eventTypes.stalled,
  topSubmit: eventTypes.submit,
  topSuspend: eventTypes.suspend,
  topTimeUpdate: eventTypes.timeUpdate,
  topTouchCancel: eventTypes.touchCancel,
  topTouchEnd: eventTypes.touchEnd,
  topTouchMove: eventTypes.touchMove,
  topTouchStart: eventTypes.touchStart,
  topVolumeChange: eventTypes.volumeChange,
  topWaiting: eventTypes.waiting,
  topWheel: eventTypes.wheel
};

for (var type in topLevelEventsToDispatchConfig) {
  topLevelEventsToDispatchConfig[type].dependencies = [type];
}

var ON_CLICK_KEY = keyOf({ onClick: null });
var onClickListeners = {};

var SimpleEventPlugin = {

  eventTypes: eventTypes,

  /**
   * @param {string} topLevelType Record from `EventConstants`.
   * @param {DOMEventTarget} topLevelTarget The listening component root node.
   * @param {string} topLevelTargetID ID of `topLevelTarget`.
   * @param {object} nativeEvent Native browser event.
   * @return {*} An accumulation of synthetic events.
   * @see {EventPluginHub.extractEvents}
   */
  extractEvents: function (topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget) {
    var dispatchConfig = topLevelEventsToDispatchConfig[topLevelType];
    if (!dispatchConfig) {
      return null;
    }
    var EventConstructor;
    switch (topLevelType) {
      case topLevelTypes.topAbort:
      case topLevelTypes.topCanPlay:
      case topLevelTypes.topCanPlayThrough:
      case topLevelTypes.topDurationChange:
      case topLevelTypes.topEmptied:
      case topLevelTypes.topEncrypted:
      case topLevelTypes.topEnded:
      case topLevelTypes.topError:
      case topLevelTypes.topInput:
      case topLevelTypes.topLoad:
      case topLevelTypes.topLoadedData:
      case topLevelTypes.topLoadedMetadata:
      case topLevelTypes.topLoadStart:
      case topLevelTypes.topPause:
      case topLevelTypes.topPlay:
      case topLevelTypes.topPlaying:
      case topLevelTypes.topProgress:
      case topLevelTypes.topRateChange:
      case topLevelTypes.topReset:
      case topLevelTypes.topSeeked:
      case topLevelTypes.topSeeking:
      case topLevelTypes.topStalled:
      case topLevelTypes.topSubmit:
      case topLevelTypes.topSuspend:
      case topLevelTypes.topTimeUpdate:
      case topLevelTypes.topVolumeChange:
      case topLevelTypes.topWaiting:
        // HTML Events
        // @see http://www.w3.org/TR/html5/index.html#events-0
        EventConstructor = SyntheticEvent;
        break;
      case topLevelTypes.topKeyPress:
        // FireFox creates a keypress event for function keys too. This removes
        // the unwanted keypress events. Enter is however both printable and
        // non-printable. One would expect Tab to be as well (but it isn't).
        if (getEventCharCode(nativeEvent) === 0) {
          return null;
        }
      /* falls through */
      case topLevelTypes.topKeyDown:
      case topLevelTypes.topKeyUp:
        EventConstructor = SyntheticKeyboardEvent;
        break;
      case topLevelTypes.topBlur:
      case topLevelTypes.topFocus:
        EventConstructor = SyntheticFocusEvent;
        break;
      case topLevelTypes.topClick:
        // Firefox creates a click event on right mouse clicks. This removes the
        // unwanted click events.
        if (nativeEvent.button === 2) {
          return null;
        }
      /* falls through */
      case topLevelTypes.topContextMenu:
      case topLevelTypes.topDoubleClick:
      case topLevelTypes.topMouseDown:
      case topLevelTypes.topMouseMove:
      case topLevelTypes.topMouseOut:
      case topLevelTypes.topMouseOver:
      case topLevelTypes.topMouseUp:
        EventConstructor = SyntheticMouseEvent;
        break;
      case topLevelTypes.topDrag:
      case topLevelTypes.topDragEnd:
      case topLevelTypes.topDragEnter:
      case topLevelTypes.topDragExit:
      case topLevelTypes.topDragLeave:
      case topLevelTypes.topDragOver:
      case topLevelTypes.topDragStart:
      case topLevelTypes.topDrop:
        EventConstructor = SyntheticDragEvent;
        break;
      case topLevelTypes.topTouchCancel:
      case topLevelTypes.topTouchEnd:
      case topLevelTypes.topTouchMove:
      case topLevelTypes.topTouchStart:
        EventConstructor = SyntheticTouchEvent;
        break;
      case topLevelTypes.topScroll:
        EventConstructor = SyntheticUIEvent;
        break;
      case topLevelTypes.topWheel:
        EventConstructor = SyntheticWheelEvent;
        break;
      case topLevelTypes.topCopy:
      case topLevelTypes.topCut:
      case topLevelTypes.topPaste:
        EventConstructor = SyntheticClipboardEvent;
        break;
    }
    !EventConstructor ? "production" !== 'production' ? invariant(false, 'SimpleEventPlugin: Unhandled event type, `%s`.', topLevelType) : invariant(false) : undefined;
    var event = EventConstructor.getPooled(dispatchConfig, topLevelTargetID, nativeEvent, nativeEventTarget);
    EventPropagators.accumulateTwoPhaseDispatches(event);
    return event;
  },

  didPutListener: function (id, registrationName, listener) {
    // Mobile Safari does not fire properly bubble click events on
    // non-interactive elements, which means delegated click listeners do not
    // fire. The workaround for this bug involves attaching an empty click
    // listener on the target node.
    if (registrationName === ON_CLICK_KEY) {
      var node = ReactMount.getNode(id);
      if (!onClickListeners[id]) {
        onClickListeners[id] = EventListener.listen(node, 'click', emptyFunction);
      }
    }
  },

  willDeleteListener: function (id, registrationName) {
    if (registrationName === ON_CLICK_KEY) {
      onClickListeners[id].remove();
      delete onClickListeners[id];
    }
  }

};

module.exports = SimpleEventPlugin;
},{"./EventConstants":241,"./EventPropagators":245,"./ReactMount":292,"./SyntheticClipboardEvent":316,"./SyntheticDragEvent":318,"./SyntheticEvent":319,"./SyntheticFocusEvent":320,"./SyntheticKeyboardEvent":322,"./SyntheticMouseEvent":323,"./SyntheticTouchEvent":324,"./SyntheticUIEvent":325,"./SyntheticWheelEvent":326,"./getEventCharCode":338,"fbjs/lib/EventListener":136,"fbjs/lib/emptyFunction":143,"fbjs/lib/invariant":151,"fbjs/lib/keyOf":155}],316:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule SyntheticClipboardEvent
 * @typechecks static-only
 */

'use strict';

var SyntheticEvent = require('./SyntheticEvent');

/**
 * @interface Event
 * @see http://www.w3.org/TR/clipboard-apis/
 */
var ClipboardEventInterface = {
  clipboardData: function (event) {
    return 'clipboardData' in event ? event.clipboardData : window.clipboardData;
  }
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticUIEvent}
 */
function SyntheticClipboardEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  SyntheticEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticEvent.augmentClass(SyntheticClipboardEvent, ClipboardEventInterface);

module.exports = SyntheticClipboardEvent;
},{"./SyntheticEvent":319}],317:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule SyntheticCompositionEvent
 * @typechecks static-only
 */

'use strict';

var SyntheticEvent = require('./SyntheticEvent');

/**
 * @interface Event
 * @see http://www.w3.org/TR/DOM-Level-3-Events/#events-compositionevents
 */
var CompositionEventInterface = {
  data: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticUIEvent}
 */
function SyntheticCompositionEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  SyntheticEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticEvent.augmentClass(SyntheticCompositionEvent, CompositionEventInterface);

module.exports = SyntheticCompositionEvent;
},{"./SyntheticEvent":319}],318:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule SyntheticDragEvent
 * @typechecks static-only
 */

'use strict';

var SyntheticMouseEvent = require('./SyntheticMouseEvent');

/**
 * @interface DragEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var DragEventInterface = {
  dataTransfer: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticUIEvent}
 */
function SyntheticDragEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  SyntheticMouseEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticMouseEvent.augmentClass(SyntheticDragEvent, DragEventInterface);

module.exports = SyntheticDragEvent;
},{"./SyntheticMouseEvent":323}],319:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule SyntheticEvent
 * @typechecks static-only
 */

'use strict';

var PooledClass = require('./PooledClass');

var assign = require('./Object.assign');
var emptyFunction = require('fbjs/lib/emptyFunction');
var warning = require('fbjs/lib/warning');

/**
 * @interface Event
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var EventInterface = {
  type: null,
  target: null,
  // currentTarget is set when dispatching; no use in copying it here
  currentTarget: emptyFunction.thatReturnsNull,
  eventPhase: null,
  bubbles: null,
  cancelable: null,
  timeStamp: function (event) {
    return event.timeStamp || Date.now();
  },
  defaultPrevented: null,
  isTrusted: null
};

/**
 * Synthetic events are dispatched by event plugins, typically in response to a
 * top-level event delegation handler.
 *
 * These systems should generally use pooling to reduce the frequency of garbage
 * collection. The system should check `isPersistent` to determine whether the
 * event should be released into the pool after being dispatched. Users that
 * need a persisted event should invoke `persist`.
 *
 * Synthetic events (and subclasses) implement the DOM Level 3 Events API by
 * normalizing browser quirks. Subclasses do not necessarily have to implement a
 * DOM interface; custom application-specific events can also subclass this.
 *
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 */
function SyntheticEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  this.dispatchConfig = dispatchConfig;
  this.dispatchMarker = dispatchMarker;
  this.nativeEvent = nativeEvent;

  var Interface = this.constructor.Interface;
  for (var propName in Interface) {
    if (!Interface.hasOwnProperty(propName)) {
      continue;
    }
    var normalize = Interface[propName];
    if (normalize) {
      this[propName] = normalize(nativeEvent);
    } else {
      if (propName === 'target') {
        this.target = nativeEventTarget;
      } else {
        this[propName] = nativeEvent[propName];
      }
    }
  }

  var defaultPrevented = nativeEvent.defaultPrevented != null ? nativeEvent.defaultPrevented : nativeEvent.returnValue === false;
  if (defaultPrevented) {
    this.isDefaultPrevented = emptyFunction.thatReturnsTrue;
  } else {
    this.isDefaultPrevented = emptyFunction.thatReturnsFalse;
  }
  this.isPropagationStopped = emptyFunction.thatReturnsFalse;
}

assign(SyntheticEvent.prototype, {

  preventDefault: function () {
    this.defaultPrevented = true;
    var event = this.nativeEvent;
    if ("production" !== 'production') {
      "production" !== 'production' ? warning(event, 'This synthetic event is reused for performance reasons. If you\'re ' + 'seeing this, you\'re calling `preventDefault` on a ' + 'released/nullified synthetic event. This is a no-op. See ' + 'https://fb.me/react-event-pooling for more information.') : undefined;
    }
    if (!event) {
      return;
    }

    if (event.preventDefault) {
      event.preventDefault();
    } else {
      event.returnValue = false;
    }
    this.isDefaultPrevented = emptyFunction.thatReturnsTrue;
  },

  stopPropagation: function () {
    var event = this.nativeEvent;
    if ("production" !== 'production') {
      "production" !== 'production' ? warning(event, 'This synthetic event is reused for performance reasons. If you\'re ' + 'seeing this, you\'re calling `stopPropagation` on a ' + 'released/nullified synthetic event. This is a no-op. See ' + 'https://fb.me/react-event-pooling for more information.') : undefined;
    }
    if (!event) {
      return;
    }

    if (event.stopPropagation) {
      event.stopPropagation();
    } else {
      event.cancelBubble = true;
    }
    this.isPropagationStopped = emptyFunction.thatReturnsTrue;
  },

  /**
   * We release all dispatched `SyntheticEvent`s after each event loop, adding
   * them back into the pool. This allows a way to hold onto a reference that
   * won't be added back into the pool.
   */
  persist: function () {
    this.isPersistent = emptyFunction.thatReturnsTrue;
  },

  /**
   * Checks if this event should be released back into the pool.
   *
   * @return {boolean} True if this should not be released, false otherwise.
   */
  isPersistent: emptyFunction.thatReturnsFalse,

  /**
   * `PooledClass` looks for `destructor` on each instance it releases.
   */
  destructor: function () {
    var Interface = this.constructor.Interface;
    for (var propName in Interface) {
      this[propName] = null;
    }
    this.dispatchConfig = null;
    this.dispatchMarker = null;
    this.nativeEvent = null;
  }

});

SyntheticEvent.Interface = EventInterface;

/**
 * Helper to reduce boilerplate when creating subclasses.
 *
 * @param {function} Class
 * @param {?object} Interface
 */
SyntheticEvent.augmentClass = function (Class, Interface) {
  var Super = this;

  var prototype = Object.create(Super.prototype);
  assign(prototype, Class.prototype);
  Class.prototype = prototype;
  Class.prototype.constructor = Class;

  Class.Interface = assign({}, Super.Interface, Interface);
  Class.augmentClass = Super.augmentClass;

  PooledClass.addPoolingTo(Class, PooledClass.fourArgumentPooler);
};

PooledClass.addPoolingTo(SyntheticEvent, PooledClass.fourArgumentPooler);

module.exports = SyntheticEvent;
},{"./Object.assign":249,"./PooledClass":250,"fbjs/lib/emptyFunction":143,"fbjs/lib/warning":162}],320:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule SyntheticFocusEvent
 * @typechecks static-only
 */

'use strict';

var SyntheticUIEvent = require('./SyntheticUIEvent');

/**
 * @interface FocusEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var FocusEventInterface = {
  relatedTarget: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticUIEvent}
 */
function SyntheticFocusEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticUIEvent.augmentClass(SyntheticFocusEvent, FocusEventInterface);

module.exports = SyntheticFocusEvent;
},{"./SyntheticUIEvent":325}],321:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule SyntheticInputEvent
 * @typechecks static-only
 */

'use strict';

var SyntheticEvent = require('./SyntheticEvent');

/**
 * @interface Event
 * @see http://www.w3.org/TR/2013/WD-DOM-Level-3-Events-20131105
 *      /#events-inputevents
 */
var InputEventInterface = {
  data: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticUIEvent}
 */
function SyntheticInputEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  SyntheticEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticEvent.augmentClass(SyntheticInputEvent, InputEventInterface);

module.exports = SyntheticInputEvent;
},{"./SyntheticEvent":319}],322:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule SyntheticKeyboardEvent
 * @typechecks static-only
 */

'use strict';

var SyntheticUIEvent = require('./SyntheticUIEvent');

var getEventCharCode = require('./getEventCharCode');
var getEventKey = require('./getEventKey');
var getEventModifierState = require('./getEventModifierState');

/**
 * @interface KeyboardEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var KeyboardEventInterface = {
  key: getEventKey,
  location: null,
  ctrlKey: null,
  shiftKey: null,
  altKey: null,
  metaKey: null,
  repeat: null,
  locale: null,
  getModifierState: getEventModifierState,
  // Legacy Interface
  charCode: function (event) {
    // `charCode` is the result of a KeyPress event and represents the value of
    // the actual printable character.

    // KeyPress is deprecated, but its replacement is not yet final and not
    // implemented in any major browser. Only KeyPress has charCode.
    if (event.type === 'keypress') {
      return getEventCharCode(event);
    }
    return 0;
  },
  keyCode: function (event) {
    // `keyCode` is the result of a KeyDown/Up event and represents the value of
    // physical keyboard key.

    // The actual meaning of the value depends on the users' keyboard layout
    // which cannot be detected. Assuming that it is a US keyboard layout
    // provides a surprisingly accurate mapping for US and European users.
    // Due to this, it is left to the user to implement at this time.
    if (event.type === 'keydown' || event.type === 'keyup') {
      return event.keyCode;
    }
    return 0;
  },
  which: function (event) {
    // `which` is an alias for either `keyCode` or `charCode` depending on the
    // type of the event.
    if (event.type === 'keypress') {
      return getEventCharCode(event);
    }
    if (event.type === 'keydown' || event.type === 'keyup') {
      return event.keyCode;
    }
    return 0;
  }
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticUIEvent}
 */
function SyntheticKeyboardEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticUIEvent.augmentClass(SyntheticKeyboardEvent, KeyboardEventInterface);

module.exports = SyntheticKeyboardEvent;
},{"./SyntheticUIEvent":325,"./getEventCharCode":338,"./getEventKey":339,"./getEventModifierState":340}],323:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule SyntheticMouseEvent
 * @typechecks static-only
 */

'use strict';

var SyntheticUIEvent = require('./SyntheticUIEvent');
var ViewportMetrics = require('./ViewportMetrics');

var getEventModifierState = require('./getEventModifierState');

/**
 * @interface MouseEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var MouseEventInterface = {
  screenX: null,
  screenY: null,
  clientX: null,
  clientY: null,
  ctrlKey: null,
  shiftKey: null,
  altKey: null,
  metaKey: null,
  getModifierState: getEventModifierState,
  button: function (event) {
    // Webkit, Firefox, IE9+
    // which:  1 2 3
    // button: 0 1 2 (standard)
    var button = event.button;
    if ('which' in event) {
      return button;
    }
    // IE<9
    // which:  undefined
    // button: 0 0 0
    // button: 1 4 2 (onmouseup)
    return button === 2 ? 2 : button === 4 ? 1 : 0;
  },
  buttons: null,
  relatedTarget: function (event) {
    return event.relatedTarget || (event.fromElement === event.srcElement ? event.toElement : event.fromElement);
  },
  // "Proprietary" Interface.
  pageX: function (event) {
    return 'pageX' in event ? event.pageX : event.clientX + ViewportMetrics.currentScrollLeft;
  },
  pageY: function (event) {
    return 'pageY' in event ? event.pageY : event.clientY + ViewportMetrics.currentScrollTop;
  }
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticUIEvent}
 */
function SyntheticMouseEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticUIEvent.augmentClass(SyntheticMouseEvent, MouseEventInterface);

module.exports = SyntheticMouseEvent;
},{"./SyntheticUIEvent":325,"./ViewportMetrics":328,"./getEventModifierState":340}],324:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule SyntheticTouchEvent
 * @typechecks static-only
 */

'use strict';

var SyntheticUIEvent = require('./SyntheticUIEvent');

var getEventModifierState = require('./getEventModifierState');

/**
 * @interface TouchEvent
 * @see http://www.w3.org/TR/touch-events/
 */
var TouchEventInterface = {
  touches: null,
  targetTouches: null,
  changedTouches: null,
  altKey: null,
  metaKey: null,
  ctrlKey: null,
  shiftKey: null,
  getModifierState: getEventModifierState
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticUIEvent}
 */
function SyntheticTouchEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticUIEvent.augmentClass(SyntheticTouchEvent, TouchEventInterface);

module.exports = SyntheticTouchEvent;
},{"./SyntheticUIEvent":325,"./getEventModifierState":340}],325:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule SyntheticUIEvent
 * @typechecks static-only
 */

'use strict';

var SyntheticEvent = require('./SyntheticEvent');

var getEventTarget = require('./getEventTarget');

/**
 * @interface UIEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var UIEventInterface = {
  view: function (event) {
    if (event.view) {
      return event.view;
    }

    var target = getEventTarget(event);
    if (target != null && target.window === target) {
      // target is a window object
      return target;
    }

    var doc = target.ownerDocument;
    // TODO: Figure out why `ownerDocument` is sometimes undefined in IE8.
    if (doc) {
      return doc.defaultView || doc.parentWindow;
    } else {
      return window;
    }
  },
  detail: function (event) {
    return event.detail || 0;
  }
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticEvent}
 */
function SyntheticUIEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  SyntheticEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticEvent.augmentClass(SyntheticUIEvent, UIEventInterface);

module.exports = SyntheticUIEvent;
},{"./SyntheticEvent":319,"./getEventTarget":341}],326:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule SyntheticWheelEvent
 * @typechecks static-only
 */

'use strict';

var SyntheticMouseEvent = require('./SyntheticMouseEvent');

/**
 * @interface WheelEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var WheelEventInterface = {
  deltaX: function (event) {
    return 'deltaX' in event ? event.deltaX :
    // Fallback to `wheelDeltaX` for Webkit and normalize (right is positive).
    'wheelDeltaX' in event ? -event.wheelDeltaX : 0;
  },
  deltaY: function (event) {
    return 'deltaY' in event ? event.deltaY :
    // Fallback to `wheelDeltaY` for Webkit and normalize (down is positive).
    'wheelDeltaY' in event ? -event.wheelDeltaY :
    // Fallback to `wheelDelta` for IE<9 and normalize (down is positive).
    'wheelDelta' in event ? -event.wheelDelta : 0;
  },
  deltaZ: null,

  // Browsers without "deltaMode" is reporting in raw wheel delta where one
  // notch on the scroll is always +/- 120, roughly equivalent to pixels.
  // A good approximation of DOM_DELTA_LINE (1) is 5% of viewport size or
  // ~40 pixels, for DOM_DELTA_SCREEN (2) it is 87.5% of viewport size.
  deltaMode: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticMouseEvent}
 */
function SyntheticWheelEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  SyntheticMouseEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticMouseEvent.augmentClass(SyntheticWheelEvent, WheelEventInterface);

module.exports = SyntheticWheelEvent;
},{"./SyntheticMouseEvent":323}],327:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule Transaction
 */

'use strict';

var invariant = require('fbjs/lib/invariant');

/**
 * `Transaction` creates a black box that is able to wrap any method such that
 * certain invariants are maintained before and after the method is invoked
 * (Even if an exception is thrown while invoking the wrapped method). Whoever
 * instantiates a transaction can provide enforcers of the invariants at
 * creation time. The `Transaction` class itself will supply one additional
 * automatic invariant for you - the invariant that any transaction instance
 * should not be run while it is already being run. You would typically create a
 * single instance of a `Transaction` for reuse multiple times, that potentially
 * is used to wrap several different methods. Wrappers are extremely simple -
 * they only require implementing two methods.
 *
 * <pre>
 *                       wrappers (injected at creation time)
 *                                      +        +
 *                                      |        |
 *                    +-----------------|--------|--------------+
 *                    |                 v        |              |
 *                    |      +---------------+   |              |
 *                    |   +--|    wrapper1   |---|----+         |
 *                    |   |  +---------------+   v    |         |
 *                    |   |          +-------------+  |         |
 *                    |   |     +----|   wrapper2  |--------+   |
 *                    |   |     |    +-------------+  |     |   |
 *                    |   |     |                     |     |   |
 *                    |   v     v                     v     v   | wrapper
 *                    | +---+ +---+   +---------+   +---+ +---+ | invariants
 * perform(anyMethod) | |   | |   |   |         |   |   | |   | | maintained
 * +----------------->|-|---|-|---|-->|anyMethod|---|---|-|---|-|-------->
 *                    | |   | |   |   |         |   |   | |   | |
 *                    | |   | |   |   |         |   |   | |   | |
 *                    | |   | |   |   |         |   |   | |   | |
 *                    | +---+ +---+   +---------+   +---+ +---+ |
 *                    |  initialize                    close    |
 *                    +-----------------------------------------+
 * </pre>
 *
 * Use cases:
 * - Preserving the input selection ranges before/after reconciliation.
 *   Restoring selection even in the event of an unexpected error.
 * - Deactivating events while rearranging the DOM, preventing blurs/focuses,
 *   while guaranteeing that afterwards, the event system is reactivated.
 * - Flushing a queue of collected DOM mutations to the main UI thread after a
 *   reconciliation takes place in a worker thread.
 * - Invoking any collected `componentDidUpdate` callbacks after rendering new
 *   content.
 * - (Future use case): Wrapping particular flushes of the `ReactWorker` queue
 *   to preserve the `scrollTop` (an automatic scroll aware DOM).
 * - (Future use case): Layout calculations before and after DOM updates.
 *
 * Transactional plugin API:
 * - A module that has an `initialize` method that returns any precomputation.
 * - and a `close` method that accepts the precomputation. `close` is invoked
 *   when the wrapped process is completed, or has failed.
 *
 * @param {Array<TransactionalWrapper>} transactionWrapper Wrapper modules
 * that implement `initialize` and `close`.
 * @return {Transaction} Single transaction for reuse in thread.
 *
 * @class Transaction
 */
var Mixin = {
  /**
   * Sets up this instance so that it is prepared for collecting metrics. Does
   * so such that this setup method may be used on an instance that is already
   * initialized, in a way that does not consume additional memory upon reuse.
   * That can be useful if you decide to make your subclass of this mixin a
   * "PooledClass".
   */
  reinitializeTransaction: function () {
    this.transactionWrappers = this.getTransactionWrappers();
    if (this.wrapperInitData) {
      this.wrapperInitData.length = 0;
    } else {
      this.wrapperInitData = [];
    }
    this._isInTransaction = false;
  },

  _isInTransaction: false,

  /**
   * @abstract
   * @return {Array<TransactionWrapper>} Array of transaction wrappers.
   */
  getTransactionWrappers: null,

  isInTransaction: function () {
    return !!this._isInTransaction;
  },

  /**
   * Executes the function within a safety window. Use this for the top level
   * methods that result in large amounts of computation/mutations that would
   * need to be safety checked. The optional arguments helps prevent the need
   * to bind in many cases.
   *
   * @param {function} method Member of scope to call.
   * @param {Object} scope Scope to invoke from.
   * @param {Object?=} a Argument to pass to the method.
   * @param {Object?=} b Argument to pass to the method.
   * @param {Object?=} c Argument to pass to the method.
   * @param {Object?=} d Argument to pass to the method.
   * @param {Object?=} e Argument to pass to the method.
   * @param {Object?=} f Argument to pass to the method.
   *
   * @return {*} Return value from `method`.
   */
  perform: function (method, scope, a, b, c, d, e, f) {
    !!this.isInTransaction() ? "production" !== 'production' ? invariant(false, 'Transaction.perform(...): Cannot initialize a transaction when there ' + 'is already an outstanding transaction.') : invariant(false) : undefined;
    var errorThrown;
    var ret;
    try {
      this._isInTransaction = true;
      // Catching errors makes debugging more difficult, so we start with
      // errorThrown set to true before setting it to false after calling
      // close -- if it's still set to true in the finally block, it means
      // one of these calls threw.
      errorThrown = true;
      this.initializeAll(0);
      ret = method.call(scope, a, b, c, d, e, f);
      errorThrown = false;
    } finally {
      try {
        if (errorThrown) {
          // If `method` throws, prefer to show that stack trace over any thrown
          // by invoking `closeAll`.
          try {
            this.closeAll(0);
          } catch (err) {}
        } else {
          // Since `method` didn't throw, we don't want to silence the exception
          // here.
          this.closeAll(0);
        }
      } finally {
        this._isInTransaction = false;
      }
    }
    return ret;
  },

  initializeAll: function (startIndex) {
    var transactionWrappers = this.transactionWrappers;
    for (var i = startIndex; i < transactionWrappers.length; i++) {
      var wrapper = transactionWrappers[i];
      try {
        // Catching errors makes debugging more difficult, so we start with the
        // OBSERVED_ERROR state before overwriting it with the real return value
        // of initialize -- if it's still set to OBSERVED_ERROR in the finally
        // block, it means wrapper.initialize threw.
        this.wrapperInitData[i] = Transaction.OBSERVED_ERROR;
        this.wrapperInitData[i] = wrapper.initialize ? wrapper.initialize.call(this) : null;
      } finally {
        if (this.wrapperInitData[i] === Transaction.OBSERVED_ERROR) {
          // The initializer for wrapper i threw an error; initialize the
          // remaining wrappers but silence any exceptions from them to ensure
          // that the first error is the one to bubble up.
          try {
            this.initializeAll(i + 1);
          } catch (err) {}
        }
      }
    }
  },

  /**
   * Invokes each of `this.transactionWrappers.close[i]` functions, passing into
   * them the respective return values of `this.transactionWrappers.init[i]`
   * (`close`rs that correspond to initializers that failed will not be
   * invoked).
   */
  closeAll: function (startIndex) {
    !this.isInTransaction() ? "production" !== 'production' ? invariant(false, 'Transaction.closeAll(): Cannot close transaction when none are open.') : invariant(false) : undefined;
    var transactionWrappers = this.transactionWrappers;
    for (var i = startIndex; i < transactionWrappers.length; i++) {
      var wrapper = transactionWrappers[i];
      var initData = this.wrapperInitData[i];
      var errorThrown;
      try {
        // Catching errors makes debugging more difficult, so we start with
        // errorThrown set to true before setting it to false after calling
        // close -- if it's still set to true in the finally block, it means
        // wrapper.close threw.
        errorThrown = true;
        if (initData !== Transaction.OBSERVED_ERROR && wrapper.close) {
          wrapper.close.call(this, initData);
        }
        errorThrown = false;
      } finally {
        if (errorThrown) {
          // The closer for wrapper i threw an error; close the remaining
          // wrappers but silence any exceptions from them to ensure that the
          // first error is the one to bubble up.
          try {
            this.closeAll(i + 1);
          } catch (e) {}
        }
      }
    }
    this.wrapperInitData.length = 0;
  }
};

var Transaction = {

  Mixin: Mixin,

  /**
   * Token to look for to determine if an error occurred.
   */
  OBSERVED_ERROR: {}

};

module.exports = Transaction;
},{"fbjs/lib/invariant":151}],328:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule ViewportMetrics
 */

'use strict';

var ViewportMetrics = {

  currentScrollLeft: 0,

  currentScrollTop: 0,

  refreshScrollValues: function (scrollPosition) {
    ViewportMetrics.currentScrollLeft = scrollPosition.x;
    ViewportMetrics.currentScrollTop = scrollPosition.y;
  }

};

module.exports = ViewportMetrics;
},{}],329:[function(require,module,exports){
/**
 * Copyright 2014-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule accumulateInto
 */

'use strict';

var invariant = require('fbjs/lib/invariant');

/**
 *
 * Accumulates items that must not be null or undefined into the first one. This
 * is used to conserve memory by avoiding array allocations, and thus sacrifices
 * API cleanness. Since `current` can be null before being passed in and not
 * null after this function, make sure to assign it back to `current`:
 *
 * `a = accumulateInto(a, b);`
 *
 * This API should be sparingly used. Try `accumulate` for something cleaner.
 *
 * @return {*|array<*>} An accumulation of items.
 */

function accumulateInto(current, next) {
  !(next != null) ? "production" !== 'production' ? invariant(false, 'accumulateInto(...): Accumulated items must not be null or undefined.') : invariant(false) : undefined;
  if (current == null) {
    return next;
  }

  // Both are not empty. Warning: Never call x.concat(y) when you are not
  // certain that x is an Array (x could be a string with concat method).
  var currentIsArray = Array.isArray(current);
  var nextIsArray = Array.isArray(next);

  if (currentIsArray && nextIsArray) {
    current.push.apply(current, next);
    return current;
  }

  if (currentIsArray) {
    current.push(next);
    return current;
  }

  if (nextIsArray) {
    // A bit too dangerous to mutate `next`.
    return [current].concat(next);
  }

  return [current, next];
}

module.exports = accumulateInto;
},{"fbjs/lib/invariant":151}],330:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule adler32
 */

'use strict';

var MOD = 65521;

// adler32 is not cryptographically strong, and is only used to sanity check that
// markup generated on the server matches the markup generated on the client.
// This implementation (a modified version of the SheetJS version) has been optimized
// for our use case, at the expense of conforming to the adler32 specification
// for non-ascii inputs.
function adler32(data) {
  var a = 1;
  var b = 0;
  var i = 0;
  var l = data.length;
  var m = l & ~0x3;
  while (i < m) {
    for (; i < Math.min(i + 4096, m); i += 4) {
      b += (a += data.charCodeAt(i)) + (a += data.charCodeAt(i + 1)) + (a += data.charCodeAt(i + 2)) + (a += data.charCodeAt(i + 3));
    }
    a %= MOD;
    b %= MOD;
  }
  for (; i < l; i++) {
    b += a += data.charCodeAt(i);
  }
  a %= MOD;
  b %= MOD;
  return a | b << 16;
}

module.exports = adler32;
},{}],331:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule canDefineProperty
 */

'use strict';

var canDefineProperty = false;
if ("production" !== 'production') {
  try {
    Object.defineProperty({}, 'x', { get: function () {} });
    canDefineProperty = true;
  } catch (x) {
    // IE will fail on defineProperty
  }
}

module.exports = canDefineProperty;
},{}],332:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule dangerousStyleValue
 * @typechecks static-only
 */

'use strict';

var CSSProperty = require('./CSSProperty');

var isUnitlessNumber = CSSProperty.isUnitlessNumber;

/**
 * Convert a value into the proper css writable value. The style name `name`
 * should be logical (no hyphens), as specified
 * in `CSSProperty.isUnitlessNumber`.
 *
 * @param {string} name CSS property name such as `topMargin`.
 * @param {*} value CSS property value such as `10px`.
 * @return {string} Normalized style value with dimensions applied.
 */
function dangerousStyleValue(name, value) {
  // Note that we've removed escapeTextForBrowser() calls here since the
  // whole string will be escaped when the attribute is injected into
  // the markup. If you provide unsafe user data here they can inject
  // arbitrary CSS which may be problematic (I couldn't repro this):
  // https://www.owasp.org/index.php/XSS_Filter_Evasion_Cheat_Sheet
  // http://www.thespanner.co.uk/2007/11/26/ultimate-xss-css-injection/
  // This is not an XSS hole but instead a potential CSS injection issue
  // which has lead to a greater discussion about how we're going to
  // trust URLs moving forward. See #2115901

  var isEmpty = value == null || typeof value === 'boolean' || value === '';
  if (isEmpty) {
    return '';
  }

  var isNonNumeric = isNaN(value);
  if (isNonNumeric || value === 0 || isUnitlessNumber.hasOwnProperty(name) && isUnitlessNumber[name]) {
    return '' + value; // cast to string
  }

  if (typeof value === 'string') {
    value = value.trim();
  }
  return value + 'px';
}

module.exports = dangerousStyleValue;
},{"./CSSProperty":230}],333:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule deprecated
 */

'use strict';

var assign = require('./Object.assign');
var warning = require('fbjs/lib/warning');

/**
 * This will log a single deprecation notice per function and forward the call
 * on to the new API.
 *
 * @param {string} fnName The name of the function
 * @param {string} newModule The module that fn will exist in
 * @param {string} newPackage The module that fn will exist in
 * @param {*} ctx The context this forwarded call should run in
 * @param {function} fn The function to forward on to
 * @return {function} The function that will warn once and then call fn
 */
function deprecated(fnName, newModule, newPackage, ctx, fn) {
  var warned = false;
  if ("production" !== 'production') {
    var newFn = function () {
      "production" !== 'production' ? warning(warned,
      // Require examples in this string must be split to prevent React's
      // build tools from mistaking them for real requires.
      // Otherwise the build tools will attempt to build a '%s' module.
      'React.%s is deprecated. Please use %s.%s from require' + '(\'%s\') ' + 'instead.', fnName, newModule, fnName, newPackage) : undefined;
      warned = true;
      return fn.apply(ctx, arguments);
    };
    // We need to make sure all properties of the original fn are copied over.
    // In particular, this is needed to support PropTypes
    return assign(newFn, fn);
  }

  return fn;
}

module.exports = deprecated;
},{"./Object.assign":249,"fbjs/lib/warning":162}],334:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule escapeTextContentForBrowser
 */

'use strict';

var ESCAPE_LOOKUP = {
  '&': '&amp;',
  '>': '&gt;',
  '<': '&lt;',
  '"': '&quot;',
  '\'': '&#x27;'
};

var ESCAPE_REGEX = /[&><"']/g;

function escaper(match) {
  return ESCAPE_LOOKUP[match];
}

/**
 * Escapes text to prevent scripting attacks.
 *
 * @param {*} text Text value to escape.
 * @return {string} An escaped string.
 */
function escapeTextContentForBrowser(text) {
  return ('' + text).replace(ESCAPE_REGEX, escaper);
}

module.exports = escapeTextContentForBrowser;
},{}],335:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule findDOMNode
 * @typechecks static-only
 */

'use strict';

var ReactCurrentOwner = require('./ReactCurrentOwner');
var ReactInstanceMap = require('./ReactInstanceMap');
var ReactMount = require('./ReactMount');

var invariant = require('fbjs/lib/invariant');
var warning = require('fbjs/lib/warning');

/**
 * Returns the DOM node rendered by this element.
 *
 * @param {ReactComponent|DOMElement} componentOrElement
 * @return {?DOMElement} The root node of this element.
 */
function findDOMNode(componentOrElement) {
  if ("production" !== 'production') {
    var owner = ReactCurrentOwner.current;
    if (owner !== null) {
      "production" !== 'production' ? warning(owner._warnedAboutRefsInRender, '%s is accessing getDOMNode or findDOMNode inside its render(). ' + 'render() should be a pure function of props and state. It should ' + 'never access something that requires stale data from the previous ' + 'render, such as refs. Move this logic to componentDidMount and ' + 'componentDidUpdate instead.', owner.getName() || 'A component') : undefined;
      owner._warnedAboutRefsInRender = true;
    }
  }
  if (componentOrElement == null) {
    return null;
  }
  if (componentOrElement.nodeType === 1) {
    return componentOrElement;
  }
  if (ReactInstanceMap.has(componentOrElement)) {
    return ReactMount.getNodeFromInstance(componentOrElement);
  }
  !(componentOrElement.render == null || typeof componentOrElement.render !== 'function') ? "production" !== 'production' ? invariant(false, 'findDOMNode was called on an unmounted component.') : invariant(false) : undefined;
  !false ? "production" !== 'production' ? invariant(false, 'Element appears to be neither ReactComponent nor DOMNode (keys: %s)', Object.keys(componentOrElement)) : invariant(false) : undefined;
}

module.exports = findDOMNode;
},{"./ReactCurrentOwner":261,"./ReactInstanceMap":289,"./ReactMount":292,"fbjs/lib/invariant":151,"fbjs/lib/warning":162}],336:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule flattenChildren
 */

'use strict';

var traverseAllChildren = require('./traverseAllChildren');
var warning = require('fbjs/lib/warning');

/**
 * @param {function} traverseContext Context passed through traversal.
 * @param {?ReactComponent} child React child component.
 * @param {!string} name String name of key path to child.
 */
function flattenSingleChildIntoContext(traverseContext, child, name) {
  // We found a component instance.
  var result = traverseContext;
  var keyUnique = result[name] === undefined;
  if ("production" !== 'production') {
    "production" !== 'production' ? warning(keyUnique, 'flattenChildren(...): Encountered two children with the same key, ' + '`%s`. Child keys must be unique; when two children share a key, only ' + 'the first child will be used.', name) : undefined;
  }
  if (keyUnique && child != null) {
    result[name] = child;
  }
}

/**
 * Flattens children that are typically specified as `props.children`. Any null
 * children will not be included in the resulting object.
 * @return {!object} flattened children keyed by name.
 */
function flattenChildren(children) {
  if (children == null) {
    return children;
  }
  var result = {};
  traverseAllChildren(children, flattenSingleChildIntoContext, result);
  return result;
}

module.exports = flattenChildren;
},{"./traverseAllChildren":354,"fbjs/lib/warning":162}],337:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule forEachAccumulated
 */

'use strict';

/**
 * @param {array} arr an "accumulation" of items which is either an Array or
 * a single item. Useful when paired with the `accumulate` module. This is a
 * simple utility that allows us to reason about a collection of items, but
 * handling the case when there is exactly one item (and we do not need to
 * allocate an array).
 */
var forEachAccumulated = function (arr, cb, scope) {
  if (Array.isArray(arr)) {
    arr.forEach(cb, scope);
  } else if (arr) {
    cb.call(scope, arr);
  }
};

module.exports = forEachAccumulated;
},{}],338:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule getEventCharCode
 * @typechecks static-only
 */

'use strict';

/**
 * `charCode` represents the actual "character code" and is safe to use with
 * `String.fromCharCode`. As such, only keys that correspond to printable
 * characters produce a valid `charCode`, the only exception to this is Enter.
 * The Tab-key is considered non-printable and does not have a `charCode`,
 * presumably because it does not produce a tab-character in browsers.
 *
 * @param {object} nativeEvent Native browser event.
 * @return {number} Normalized `charCode` property.
 */
function getEventCharCode(nativeEvent) {
  var charCode;
  var keyCode = nativeEvent.keyCode;

  if ('charCode' in nativeEvent) {
    charCode = nativeEvent.charCode;

    // FF does not set `charCode` for the Enter-key, check against `keyCode`.
    if (charCode === 0 && keyCode === 13) {
      charCode = 13;
    }
  } else {
    // IE8 does not implement `charCode`, but `keyCode` has the correct value.
    charCode = keyCode;
  }

  // Some non-printable keys are reported in `charCode`/`keyCode`, discard them.
  // Must not discard the (non-)printable Enter-key.
  if (charCode >= 32 || charCode === 13) {
    return charCode;
  }

  return 0;
}

module.exports = getEventCharCode;
},{}],339:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule getEventKey
 * @typechecks static-only
 */

'use strict';

var getEventCharCode = require('./getEventCharCode');

/**
 * Normalization of deprecated HTML5 `key` values
 * @see https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent#Key_names
 */
var normalizeKey = {
  'Esc': 'Escape',
  'Spacebar': ' ',
  'Left': 'ArrowLeft',
  'Up': 'ArrowUp',
  'Right': 'ArrowRight',
  'Down': 'ArrowDown',
  'Del': 'Delete',
  'Win': 'OS',
  'Menu': 'ContextMenu',
  'Apps': 'ContextMenu',
  'Scroll': 'ScrollLock',
  'MozPrintableKey': 'Unidentified'
};

/**
 * Translation from legacy `keyCode` to HTML5 `key`
 * Only special keys supported, all others depend on keyboard layout or browser
 * @see https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent#Key_names
 */
var translateToKey = {
  8: 'Backspace',
  9: 'Tab',
  12: 'Clear',
  13: 'Enter',
  16: 'Shift',
  17: 'Control',
  18: 'Alt',
  19: 'Pause',
  20: 'CapsLock',
  27: 'Escape',
  32: ' ',
  33: 'PageUp',
  34: 'PageDown',
  35: 'End',
  36: 'Home',
  37: 'ArrowLeft',
  38: 'ArrowUp',
  39: 'ArrowRight',
  40: 'ArrowDown',
  45: 'Insert',
  46: 'Delete',
  112: 'F1', 113: 'F2', 114: 'F3', 115: 'F4', 116: 'F5', 117: 'F6',
  118: 'F7', 119: 'F8', 120: 'F9', 121: 'F10', 122: 'F11', 123: 'F12',
  144: 'NumLock',
  145: 'ScrollLock',
  224: 'Meta'
};

/**
 * @param {object} nativeEvent Native browser event.
 * @return {string} Normalized `key` property.
 */
function getEventKey(nativeEvent) {
  if (nativeEvent.key) {
    // Normalize inconsistent values reported by browsers due to
    // implementations of a working draft specification.

    // FireFox implements `key` but returns `MozPrintableKey` for all
    // printable characters (normalized to `Unidentified`), ignore it.
    var key = normalizeKey[nativeEvent.key] || nativeEvent.key;
    if (key !== 'Unidentified') {
      return key;
    }
  }

  // Browser does not implement `key`, polyfill as much of it as we can.
  if (nativeEvent.type === 'keypress') {
    var charCode = getEventCharCode(nativeEvent);

    // The enter-key is technically both printable and non-printable and can
    // thus be captured by `keypress`, no other non-printable key should.
    return charCode === 13 ? 'Enter' : String.fromCharCode(charCode);
  }
  if (nativeEvent.type === 'keydown' || nativeEvent.type === 'keyup') {
    // While user keyboard layout determines the actual meaning of each
    // `keyCode` value, almost all function keys have a universal value.
    return translateToKey[nativeEvent.keyCode] || 'Unidentified';
  }
  return '';
}

module.exports = getEventKey;
},{"./getEventCharCode":338}],340:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule getEventModifierState
 * @typechecks static-only
 */

'use strict';

/**
 * Translation from modifier key to the associated property in the event.
 * @see http://www.w3.org/TR/DOM-Level-3-Events/#keys-Modifiers
 */

var modifierKeyToProp = {
  'Alt': 'altKey',
  'Control': 'ctrlKey',
  'Meta': 'metaKey',
  'Shift': 'shiftKey'
};

// IE8 does not implement getModifierState so we simply map it to the only
// modifier keys exposed by the event itself, does not support Lock-keys.
// Currently, all major browsers except Chrome seems to support Lock-keys.
function modifierStateGetter(keyArg) {
  var syntheticEvent = this;
  var nativeEvent = syntheticEvent.nativeEvent;
  if (nativeEvent.getModifierState) {
    return nativeEvent.getModifierState(keyArg);
  }
  var keyProp = modifierKeyToProp[keyArg];
  return keyProp ? !!nativeEvent[keyProp] : false;
}

function getEventModifierState(nativeEvent) {
  return modifierStateGetter;
}

module.exports = getEventModifierState;
},{}],341:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule getEventTarget
 * @typechecks static-only
 */

'use strict';

/**
 * Gets the target node from a native browser event by accounting for
 * inconsistencies in browser DOM APIs.
 *
 * @param {object} nativeEvent Native browser event.
 * @return {DOMEventTarget} Target node.
 */
function getEventTarget(nativeEvent) {
  var target = nativeEvent.target || nativeEvent.srcElement || window;
  // Safari may fire events on text nodes (Node.TEXT_NODE is 3).
  // @see http://www.quirksmode.org/js/events_properties.html
  return target.nodeType === 3 ? target.parentNode : target;
}

module.exports = getEventTarget;
},{}],342:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule getIteratorFn
 * @typechecks static-only
 */

'use strict';

/* global Symbol */
var ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;
var FAUX_ITERATOR_SYMBOL = '@@iterator'; // Before Symbol spec.

/**
 * Returns the iterator method function contained on the iterable object.
 *
 * Be sure to invoke the function with the iterable as context:
 *
 *     var iteratorFn = getIteratorFn(myIterable);
 *     if (iteratorFn) {
 *       var iterator = iteratorFn.call(myIterable);
 *       ...
 *     }
 *
 * @param {?object} maybeIterable
 * @return {?function}
 */
function getIteratorFn(maybeIterable) {
  var iteratorFn = maybeIterable && (ITERATOR_SYMBOL && maybeIterable[ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL]);
  if (typeof iteratorFn === 'function') {
    return iteratorFn;
  }
}

module.exports = getIteratorFn;
},{}],343:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule getNodeForCharacterOffset
 */

'use strict';

/**
 * Given any node return the first leaf node without children.
 *
 * @param {DOMElement|DOMTextNode} node
 * @return {DOMElement|DOMTextNode}
 */
function getLeafNode(node) {
  while (node && node.firstChild) {
    node = node.firstChild;
  }
  return node;
}

/**
 * Get the next sibling within a container. This will walk up the
 * DOM if a node's siblings have been exhausted.
 *
 * @param {DOMElement|DOMTextNode} node
 * @return {?DOMElement|DOMTextNode}
 */
function getSiblingNode(node) {
  while (node) {
    if (node.nextSibling) {
      return node.nextSibling;
    }
    node = node.parentNode;
  }
}

/**
 * Get object describing the nodes which contain characters at offset.
 *
 * @param {DOMElement|DOMTextNode} root
 * @param {number} offset
 * @return {?object}
 */
function getNodeForCharacterOffset(root, offset) {
  var node = getLeafNode(root);
  var nodeStart = 0;
  var nodeEnd = 0;

  while (node) {
    if (node.nodeType === 3) {
      nodeEnd = nodeStart + node.textContent.length;

      if (nodeStart <= offset && nodeEnd >= offset) {
        return {
          node: node,
          offset: offset - nodeStart
        };
      }

      nodeStart = nodeEnd;
    }

    node = getLeafNode(getSiblingNode(node));
  }
}

module.exports = getNodeForCharacterOffset;
},{}],344:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule getTextContentAccessor
 */

'use strict';

var ExecutionEnvironment = require('fbjs/lib/ExecutionEnvironment');

var contentKey = null;

/**
 * Gets the key used to access text content on a DOM node.
 *
 * @return {?string} Key used to access text content.
 * @internal
 */
function getTextContentAccessor() {
  if (!contentKey && ExecutionEnvironment.canUseDOM) {
    // Prefer textContent to innerText because many browsers support both but
    // SVG <text> elements don't support innerText even when <div> does.
    contentKey = 'textContent' in document.documentElement ? 'textContent' : 'innerText';
  }
  return contentKey;
}

module.exports = getTextContentAccessor;
},{"fbjs/lib/ExecutionEnvironment":137}],345:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule instantiateReactComponent
 * @typechecks static-only
 */

'use strict';

var ReactCompositeComponent = require('./ReactCompositeComponent');
var ReactEmptyComponent = require('./ReactEmptyComponent');
var ReactNativeComponent = require('./ReactNativeComponent');

var assign = require('./Object.assign');
var invariant = require('fbjs/lib/invariant');
var warning = require('fbjs/lib/warning');

// To avoid a cyclic dependency, we create the final class in this module
var ReactCompositeComponentWrapper = function () {};
assign(ReactCompositeComponentWrapper.prototype, ReactCompositeComponent.Mixin, {
  _instantiateReactComponent: instantiateReactComponent
});

function getDeclarationErrorAddendum(owner) {
  if (owner) {
    var name = owner.getName();
    if (name) {
      return ' Check the render method of `' + name + '`.';
    }
  }
  return '';
}

/**
 * Check if the type reference is a known internal type. I.e. not a user
 * provided composite type.
 *
 * @param {function} type
 * @return {boolean} Returns true if this is a valid internal type.
 */
function isInternalComponentType(type) {
  return typeof type === 'function' && typeof type.prototype !== 'undefined' && typeof type.prototype.mountComponent === 'function' && typeof type.prototype.receiveComponent === 'function';
}

/**
 * Given a ReactNode, create an instance that will actually be mounted.
 *
 * @param {ReactNode} node
 * @return {object} A new instance of the element's constructor.
 * @protected
 */
function instantiateReactComponent(node) {
  var instance;

  if (node === null || node === false) {
    instance = new ReactEmptyComponent(instantiateReactComponent);
  } else if (typeof node === 'object') {
    var element = node;
    !(element && (typeof element.type === 'function' || typeof element.type === 'string')) ? "production" !== 'production' ? invariant(false, 'Element type is invalid: expected a string (for built-in components) ' + 'or a class/function (for composite components) but got: %s.%s', element.type == null ? element.type : typeof element.type, getDeclarationErrorAddendum(element._owner)) : invariant(false) : undefined;

    // Special case string values
    if (typeof element.type === 'string') {
      instance = ReactNativeComponent.createInternalComponent(element);
    } else if (isInternalComponentType(element.type)) {
      // This is temporarily available for custom components that are not string
      // representations. I.e. ART. Once those are updated to use the string
      // representation, we can drop this code path.
      instance = new element.type(element);
    } else {
      instance = new ReactCompositeComponentWrapper();
    }
  } else if (typeof node === 'string' || typeof node === 'number') {
    instance = ReactNativeComponent.createInstanceForText(node);
  } else {
    !false ? "production" !== 'production' ? invariant(false, 'Encountered invalid React node of type %s', typeof node) : invariant(false) : undefined;
  }

  if ("production" !== 'production') {
    "production" !== 'production' ? warning(typeof instance.construct === 'function' && typeof instance.mountComponent === 'function' && typeof instance.receiveComponent === 'function' && typeof instance.unmountComponent === 'function', 'Only React Components can be mounted.') : undefined;
  }

  // Sets up the instance. This can probably just move into the constructor now.
  instance.construct(node);

  // These two fields are used by the DOM and ART diffing algorithms
  // respectively. Instead of using expandos on components, we should be
  // storing the state needed by the diffing algorithms elsewhere.
  instance._mountIndex = 0;
  instance._mountImage = null;

  if ("production" !== 'production') {
    instance._isOwnerNecessary = false;
    instance._warnedAboutRefsInRender = false;
  }

  // Internal instances should fully constructed at this point, so they should
  // not get any new fields added to them at this point.
  if ("production" !== 'production') {
    if (Object.preventExtensions) {
      Object.preventExtensions(instance);
    }
  }

  return instance;
}

module.exports = instantiateReactComponent;
},{"./Object.assign":249,"./ReactCompositeComponent":260,"./ReactEmptyComponent":281,"./ReactNativeComponent":295,"fbjs/lib/invariant":151,"fbjs/lib/warning":162}],346:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule isEventSupported
 */

'use strict';

var ExecutionEnvironment = require('fbjs/lib/ExecutionEnvironment');

var useHasFeature;
if (ExecutionEnvironment.canUseDOM) {
  useHasFeature = document.implementation && document.implementation.hasFeature &&
  // always returns true in newer browsers as per the standard.
  // @see http://dom.spec.whatwg.org/#dom-domimplementation-hasfeature
  document.implementation.hasFeature('', '') !== true;
}

/**
 * Checks if an event is supported in the current execution environment.
 *
 * NOTE: This will not work correctly for non-generic events such as `change`,
 * `reset`, `load`, `error`, and `select`.
 *
 * Borrows from Modernizr.
 *
 * @param {string} eventNameSuffix Event name, e.g. "click".
 * @param {?boolean} capture Check if the capture phase is supported.
 * @return {boolean} True if the event is supported.
 * @internal
 * @license Modernizr 3.0.0pre (Custom Build) | MIT
 */
function isEventSupported(eventNameSuffix, capture) {
  if (!ExecutionEnvironment.canUseDOM || capture && !('addEventListener' in document)) {
    return false;
  }

  var eventName = 'on' + eventNameSuffix;
  var isSupported = (eventName in document);

  if (!isSupported) {
    var element = document.createElement('div');
    element.setAttribute(eventName, 'return;');
    isSupported = typeof element[eventName] === 'function';
  }

  if (!isSupported && useHasFeature && eventNameSuffix === 'wheel') {
    // This is the only way to test support for the `wheel` event in IE9+.
    isSupported = document.implementation.hasFeature('Events.wheel', '3.0');
  }

  return isSupported;
}

module.exports = isEventSupported;
},{"fbjs/lib/ExecutionEnvironment":137}],347:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule isTextInputElement
 */

'use strict';

/**
 * @see http://www.whatwg.org/specs/web-apps/current-work/multipage/the-input-element.html#input-type-attr-summary
 */
var supportedInputTypes = {
  'color': true,
  'date': true,
  'datetime': true,
  'datetime-local': true,
  'email': true,
  'month': true,
  'number': true,
  'password': true,
  'range': true,
  'search': true,
  'tel': true,
  'text': true,
  'time': true,
  'url': true,
  'week': true
};

function isTextInputElement(elem) {
  var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();
  return nodeName && (nodeName === 'input' && supportedInputTypes[elem.type] || nodeName === 'textarea');
}

module.exports = isTextInputElement;
},{}],348:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule onlyChild
 */
'use strict';

var ReactElement = require('./ReactElement');

var invariant = require('fbjs/lib/invariant');

/**
 * Returns the first child in a collection of children and verifies that there
 * is only one child in the collection. The current implementation of this
 * function assumes that a single child gets passed without a wrapper, but the
 * purpose of this helper function is to abstract away the particular structure
 * of children.
 *
 * @param {?object} children Child collection structure.
 * @return {ReactComponent} The first and only `ReactComponent` contained in the
 * structure.
 */
function onlyChild(children) {
  !ReactElement.isValidElement(children) ? "production" !== 'production' ? invariant(false, 'onlyChild must be passed a children with exactly one child.') : invariant(false) : undefined;
  return children;
}

module.exports = onlyChild;
},{"./ReactElement":279,"fbjs/lib/invariant":151}],349:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule quoteAttributeValueForBrowser
 */

'use strict';

var escapeTextContentForBrowser = require('./escapeTextContentForBrowser');

/**
 * Escapes attribute value to prevent scripting attacks.
 *
 * @param {*} value Value to escape.
 * @return {string} An escaped string.
 */
function quoteAttributeValueForBrowser(value) {
  return '"' + escapeTextContentForBrowser(value) + '"';
}

module.exports = quoteAttributeValueForBrowser;
},{"./escapeTextContentForBrowser":334}],350:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
* @providesModule renderSubtreeIntoContainer
*/

'use strict';

var ReactMount = require('./ReactMount');

module.exports = ReactMount.renderSubtreeIntoContainer;
},{"./ReactMount":292}],351:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule setInnerHTML
 */

/* globals MSApp */

'use strict';

var ExecutionEnvironment = require('fbjs/lib/ExecutionEnvironment');

var WHITESPACE_TEST = /^[ \r\n\t\f]/;
var NONVISIBLE_TEST = /<(!--|link|noscript|meta|script|style)[ \r\n\t\f\/>]/;

/**
 * Set the innerHTML property of a node, ensuring that whitespace is preserved
 * even in IE8.
 *
 * @param {DOMElement} node
 * @param {string} html
 * @internal
 */
var setInnerHTML = function (node, html) {
  node.innerHTML = html;
};

// Win8 apps: Allow all html to be inserted
if (typeof MSApp !== 'undefined' && MSApp.execUnsafeLocalFunction) {
  setInnerHTML = function (node, html) {
    MSApp.execUnsafeLocalFunction(function () {
      node.innerHTML = html;
    });
  };
}

if (ExecutionEnvironment.canUseDOM) {
  // IE8: When updating a just created node with innerHTML only leading
  // whitespace is removed. When updating an existing node with innerHTML
  // whitespace in root TextNodes is also collapsed.
  // @see quirksmode.org/bugreports/archives/2004/11/innerhtml_and_t.html

  // Feature detection; only IE8 is known to behave improperly like this.
  var testElement = document.createElement('div');
  testElement.innerHTML = ' ';
  if (testElement.innerHTML === '') {
    setInnerHTML = function (node, html) {
      // Magic theory: IE8 supposedly differentiates between added and updated
      // nodes when processing innerHTML, innerHTML on updated nodes suffers
      // from worse whitespace behavior. Re-adding a node like this triggers
      // the initial and more favorable whitespace behavior.
      // TODO: What to do on a detached node?
      if (node.parentNode) {
        node.parentNode.replaceChild(node, node);
      }

      // We also implement a workaround for non-visible tags disappearing into
      // thin air on IE8, this only happens if there is no visible text
      // in-front of the non-visible tags. Piggyback on the whitespace fix
      // and simply check if any non-visible tags appear in the source.
      if (WHITESPACE_TEST.test(html) || html[0] === '<' && NONVISIBLE_TEST.test(html)) {
        // Recover leading whitespace by temporarily prepending any character.
        // \uFEFF has the potential advantage of being zero-width/invisible.
        // UglifyJS drops U+FEFF chars when parsing, so use String.fromCharCode
        // in hopes that this is preserved even if "\uFEFF" is transformed to
        // the actual Unicode character (by Babel, for example).
        // https://github.com/mishoo/UglifyJS2/blob/v2.4.20/lib/parse.js#L216
        node.innerHTML = String.fromCharCode(0xFEFF) + html;

        // deleteData leaves an empty `TextNode` which offsets the index of all
        // children. Definitely want to avoid this.
        var textNode = node.firstChild;
        if (textNode.data.length === 1) {
          node.removeChild(textNode);
        } else {
          textNode.deleteData(0, 1);
        }
      } else {
        node.innerHTML = html;
      }
    };
  }
}

module.exports = setInnerHTML;
},{"fbjs/lib/ExecutionEnvironment":137}],352:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule setTextContent
 */

'use strict';

var ExecutionEnvironment = require('fbjs/lib/ExecutionEnvironment');
var escapeTextContentForBrowser = require('./escapeTextContentForBrowser');
var setInnerHTML = require('./setInnerHTML');

/**
 * Set the textContent property of a node, ensuring that whitespace is preserved
 * even in IE8. innerText is a poor substitute for textContent and, among many
 * issues, inserts <br> instead of the literal newline chars. innerHTML behaves
 * as it should.
 *
 * @param {DOMElement} node
 * @param {string} text
 * @internal
 */
var setTextContent = function (node, text) {
  node.textContent = text;
};

if (ExecutionEnvironment.canUseDOM) {
  if (!('textContent' in document.documentElement)) {
    setTextContent = function (node, text) {
      setInnerHTML(node, escapeTextContentForBrowser(text));
    };
  }
}

module.exports = setTextContent;
},{"./escapeTextContentForBrowser":334,"./setInnerHTML":351,"fbjs/lib/ExecutionEnvironment":137}],353:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule shouldUpdateReactComponent
 * @typechecks static-only
 */

'use strict';

/**
 * Given a `prevElement` and `nextElement`, determines if the existing
 * instance should be updated as opposed to being destroyed or replaced by a new
 * instance. Both arguments are elements. This ensures that this logic can
 * operate on stateless trees without any backing instance.
 *
 * @param {?object} prevElement
 * @param {?object} nextElement
 * @return {boolean} True if the existing instance should be updated.
 * @protected
 */
function shouldUpdateReactComponent(prevElement, nextElement) {
  var prevEmpty = prevElement === null || prevElement === false;
  var nextEmpty = nextElement === null || nextElement === false;
  if (prevEmpty || nextEmpty) {
    return prevEmpty === nextEmpty;
  }

  var prevType = typeof prevElement;
  var nextType = typeof nextElement;
  if (prevType === 'string' || prevType === 'number') {
    return nextType === 'string' || nextType === 'number';
  } else {
    return nextType === 'object' && prevElement.type === nextElement.type && prevElement.key === nextElement.key;
  }
  return false;
}

module.exports = shouldUpdateReactComponent;
},{}],354:[function(require,module,exports){
/**
 * Copyright 2013-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule traverseAllChildren
 */

'use strict';

var ReactCurrentOwner = require('./ReactCurrentOwner');
var ReactElement = require('./ReactElement');
var ReactInstanceHandles = require('./ReactInstanceHandles');

var getIteratorFn = require('./getIteratorFn');
var invariant = require('fbjs/lib/invariant');
var warning = require('fbjs/lib/warning');

var SEPARATOR = ReactInstanceHandles.SEPARATOR;
var SUBSEPARATOR = ':';

/**
 * TODO: Test that a single child and an array with one item have the same key
 * pattern.
 */

var userProvidedKeyEscaperLookup = {
  '=': '=0',
  '.': '=1',
  ':': '=2'
};

var userProvidedKeyEscapeRegex = /[=.:]/g;

var didWarnAboutMaps = false;

function userProvidedKeyEscaper(match) {
  return userProvidedKeyEscaperLookup[match];
}

/**
 * Generate a key string that identifies a component within a set.
 *
 * @param {*} component A component that could contain a manual key.
 * @param {number} index Index that is used if a manual key is not provided.
 * @return {string}
 */
function getComponentKey(component, index) {
  if (component && component.key != null) {
    // Explicit key
    return wrapUserProvidedKey(component.key);
  }
  // Implicit key determined by the index in the set
  return index.toString(36);
}

/**
 * Escape a component key so that it is safe to use in a reactid.
 *
 * @param {*} text Component key to be escaped.
 * @return {string} An escaped string.
 */
function escapeUserProvidedKey(text) {
  return ('' + text).replace(userProvidedKeyEscapeRegex, userProvidedKeyEscaper);
}

/**
 * Wrap a `key` value explicitly provided by the user to distinguish it from
 * implicitly-generated keys generated by a component's index in its parent.
 *
 * @param {string} key Value of a user-provided `key` attribute
 * @return {string}
 */
function wrapUserProvidedKey(key) {
  return '$' + escapeUserProvidedKey(key);
}

/**
 * @param {?*} children Children tree container.
 * @param {!string} nameSoFar Name of the key path so far.
 * @param {!function} callback Callback to invoke with each child found.
 * @param {?*} traverseContext Used to pass information throughout the traversal
 * process.
 * @return {!number} The number of children in this subtree.
 */
function traverseAllChildrenImpl(children, nameSoFar, callback, traverseContext) {
  var type = typeof children;

  if (type === 'undefined' || type === 'boolean') {
    // All of the above are perceived as null.
    children = null;
  }

  if (children === null || type === 'string' || type === 'number' || ReactElement.isValidElement(children)) {
    callback(traverseContext, children,
    // If it's the only child, treat the name as if it was wrapped in an array
    // so that it's consistent if the number of children grows.
    nameSoFar === '' ? SEPARATOR + getComponentKey(children, 0) : nameSoFar);
    return 1;
  }

  var child;
  var nextName;
  var subtreeCount = 0; // Count of children found in the current subtree.
  var nextNamePrefix = nameSoFar === '' ? SEPARATOR : nameSoFar + SUBSEPARATOR;

  if (Array.isArray(children)) {
    for (var i = 0; i < children.length; i++) {
      child = children[i];
      nextName = nextNamePrefix + getComponentKey(child, i);
      subtreeCount += traverseAllChildrenImpl(child, nextName, callback, traverseContext);
    }
  } else {
    var iteratorFn = getIteratorFn(children);
    if (iteratorFn) {
      var iterator = iteratorFn.call(children);
      var step;
      if (iteratorFn !== children.entries) {
        var ii = 0;
        while (!(step = iterator.next()).done) {
          child = step.value;
          nextName = nextNamePrefix + getComponentKey(child, ii++);
          subtreeCount += traverseAllChildrenImpl(child, nextName, callback, traverseContext);
        }
      } else {
        if ("production" !== 'production') {
          "production" !== 'production' ? warning(didWarnAboutMaps, 'Using Maps as children is not yet fully supported. It is an ' + 'experimental feature that might be removed. Convert it to a ' + 'sequence / iterable of keyed ReactElements instead.') : undefined;
          didWarnAboutMaps = true;
        }
        // Iterator will provide entry [k,v] tuples rather than values.
        while (!(step = iterator.next()).done) {
          var entry = step.value;
          if (entry) {
            child = entry[1];
            nextName = nextNamePrefix + wrapUserProvidedKey(entry[0]) + SUBSEPARATOR + getComponentKey(child, 0);
            subtreeCount += traverseAllChildrenImpl(child, nextName, callback, traverseContext);
          }
        }
      }
    } else if (type === 'object') {
      var addendum = '';
      if ("production" !== 'production') {
        addendum = ' If you meant to render a collection of children, use an array ' + 'instead or wrap the object using createFragment(object) from the ' + 'React add-ons.';
        if (children._isReactElement) {
          addendum = ' It looks like you\'re using an element created by a different ' + 'version of React. Make sure to use only one copy of React.';
        }
        if (ReactCurrentOwner.current) {
          var name = ReactCurrentOwner.current.getName();
          if (name) {
            addendum += ' Check the render method of `' + name + '`.';
          }
        }
      }
      var childrenString = String(children);
      !false ? "production" !== 'production' ? invariant(false, 'Objects are not valid as a React child (found: %s).%s', childrenString === '[object Object]' ? 'object with keys {' + Object.keys(children).join(', ') + '}' : childrenString, addendum) : invariant(false) : undefined;
    }
  }

  return subtreeCount;
}

/**
 * Traverses children that are typically specified as `props.children`, but
 * might also be specified through attributes:
 *
 * - `traverseAllChildren(this.props.children, ...)`
 * - `traverseAllChildren(this.props.leftPanelChildren, ...)`
 *
 * The `traverseContext` is an optional argument that is passed through the
 * entire traversal. It can be used to store accumulations or anything else that
 * the callback might find relevant.
 *
 * @param {?*} children Children tree object.
 * @param {!function} callback To invoke upon traversing each child.
 * @param {?*} traverseContext Context for traversal.
 * @return {!number} The number of children in this subtree.
 */
function traverseAllChildren(children, callback, traverseContext) {
  if (children == null) {
    return 0;
  }

  return traverseAllChildrenImpl(children, '', callback, traverseContext);
}

module.exports = traverseAllChildren;
},{"./ReactCurrentOwner":261,"./ReactElement":279,"./ReactInstanceHandles":288,"./getIteratorFn":342,"fbjs/lib/invariant":151,"fbjs/lib/warning":162}],355:[function(require,module,exports){
/**
 * Copyright 2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * @providesModule validateDOMNesting
 */

'use strict';

var assign = require('./Object.assign');
var emptyFunction = require('fbjs/lib/emptyFunction');
var warning = require('fbjs/lib/warning');

var validateDOMNesting = emptyFunction;

if ("production" !== 'production') {
  // This validation code was written based on the HTML5 parsing spec:
  // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-scope
  //
  // Note: this does not catch all invalid nesting, nor does it try to (as it's
  // not clear what practical benefit doing so provides); instead, we warn only
  // for cases where the parser will give a parse tree differing from what React
  // intended. For example, <b><div></div></b> is invalid but we don't warn
  // because it still parses correctly; we do warn for other cases like nested
  // <p> tags where the beginning of the second element implicitly closes the
  // first, causing a confusing mess.

  // https://html.spec.whatwg.org/multipage/syntax.html#special
  var specialTags = ['address', 'applet', 'area', 'article', 'aside', 'base', 'basefont', 'bgsound', 'blockquote', 'body', 'br', 'button', 'caption', 'center', 'col', 'colgroup', 'dd', 'details', 'dir', 'div', 'dl', 'dt', 'embed', 'fieldset', 'figcaption', 'figure', 'footer', 'form', 'frame', 'frameset', 'h1', 'h2', 'h3', 'h4', 'h5', 'h6', 'head', 'header', 'hgroup', 'hr', 'html', 'iframe', 'img', 'input', 'isindex', 'li', 'link', 'listing', 'main', 'marquee', 'menu', 'menuitem', 'meta', 'nav', 'noembed', 'noframes', 'noscript', 'object', 'ol', 'p', 'param', 'plaintext', 'pre', 'script', 'section', 'select', 'source', 'style', 'summary', 'table', 'tbody', 'td', 'template', 'textarea', 'tfoot', 'th', 'thead', 'title', 'tr', 'track', 'ul', 'wbr', 'xmp'];

  // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-scope
  var inScopeTags = ['applet', 'caption', 'html', 'table', 'td', 'th', 'marquee', 'object', 'template',

  // https://html.spec.whatwg.org/multipage/syntax.html#html-integration-point
  // TODO: Distinguish by namespace here -- for <title>, including it here
  // errs on the side of fewer warnings
  'foreignObject', 'desc', 'title'];

  // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-button-scope
  var buttonScopeTags = inScopeTags.concat(['button']);

  // https://html.spec.whatwg.org/multipage/syntax.html#generate-implied-end-tags
  var impliedEndTags = ['dd', 'dt', 'li', 'option', 'optgroup', 'p', 'rp', 'rt'];

  var emptyAncestorInfo = {
    parentTag: null,

    formTag: null,
    aTagInScope: null,
    buttonTagInScope: null,
    nobrTagInScope: null,
    pTagInButtonScope: null,

    listItemTagAutoclosing: null,
    dlItemTagAutoclosing: null
  };

  var updatedAncestorInfo = function (oldInfo, tag, instance) {
    var ancestorInfo = assign({}, oldInfo || emptyAncestorInfo);
    var info = { tag: tag, instance: instance };

    if (inScopeTags.indexOf(tag) !== -1) {
      ancestorInfo.aTagInScope = null;
      ancestorInfo.buttonTagInScope = null;
      ancestorInfo.nobrTagInScope = null;
    }
    if (buttonScopeTags.indexOf(tag) !== -1) {
      ancestorInfo.pTagInButtonScope = null;
    }

    // See rules for 'li', 'dd', 'dt' start tags in
    // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inbody
    if (specialTags.indexOf(tag) !== -1 && tag !== 'address' && tag !== 'div' && tag !== 'p') {
      ancestorInfo.listItemTagAutoclosing = null;
      ancestorInfo.dlItemTagAutoclosing = null;
    }

    ancestorInfo.parentTag = info;

    if (tag === 'form') {
      ancestorInfo.formTag = info;
    }
    if (tag === 'a') {
      ancestorInfo.aTagInScope = info;
    }
    if (tag === 'button') {
      ancestorInfo.buttonTagInScope = info;
    }
    if (tag === 'nobr') {
      ancestorInfo.nobrTagInScope = info;
    }
    if (tag === 'p') {
      ancestorInfo.pTagInButtonScope = info;
    }
    if (tag === 'li') {
      ancestorInfo.listItemTagAutoclosing = info;
    }
    if (tag === 'dd' || tag === 'dt') {
      ancestorInfo.dlItemTagAutoclosing = info;
    }

    return ancestorInfo;
  };

  /**
   * Returns whether
   */
  var isTagValidWithParent = function (tag, parentTag) {
    // First, let's check if we're in an unusual parsing mode...
    switch (parentTag) {
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inselect
      case 'select':
        return tag === 'option' || tag === 'optgroup' || tag === '#text';
      case 'optgroup':
        return tag === 'option' || tag === '#text';
      // Strictly speaking, seeing an <option> doesn't mean we're in a <select>
      // but
      case 'option':
        return tag === '#text';

      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intd
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-incaption
      // No special behavior since these rules fall back to "in body" mode for
      // all except special table nodes which cause bad parsing behavior anyway.

      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intr
      case 'tr':
        return tag === 'th' || tag === 'td' || tag === 'style' || tag === 'script' || tag === 'template';

      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intbody
      case 'tbody':
      case 'thead':
      case 'tfoot':
        return tag === 'tr' || tag === 'style' || tag === 'script' || tag === 'template';

      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-incolgroup
      case 'colgroup':
        return tag === 'col' || tag === 'template';

      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intable
      case 'table':
        return tag === 'caption' || tag === 'colgroup' || tag === 'tbody' || tag === 'tfoot' || tag === 'thead' || tag === 'style' || tag === 'script' || tag === 'template';

      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inhead
      case 'head':
        return tag === 'base' || tag === 'basefont' || tag === 'bgsound' || tag === 'link' || tag === 'meta' || tag === 'title' || tag === 'noscript' || tag === 'noframes' || tag === 'style' || tag === 'script' || tag === 'template';

      // https://html.spec.whatwg.org/multipage/semantics.html#the-html-element
      case 'html':
        return tag === 'head' || tag === 'body';
    }

    // Probably in the "in body" parsing mode, so we outlaw only tag combos
    // where the parsing rules cause implicit opens or closes to be added.
    // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inbody
    switch (tag) {
      case 'h1':
      case 'h2':
      case 'h3':
      case 'h4':
      case 'h5':
      case 'h6':
        return parentTag !== 'h1' && parentTag !== 'h2' && parentTag !== 'h3' && parentTag !== 'h4' && parentTag !== 'h5' && parentTag !== 'h6';

      case 'rp':
      case 'rt':
        return impliedEndTags.indexOf(parentTag) === -1;

      case 'caption':
      case 'col':
      case 'colgroup':
      case 'frame':
      case 'head':
      case 'tbody':
      case 'td':
      case 'tfoot':
      case 'th':
      case 'thead':
      case 'tr':
        // These tags are only valid with a few parents that have special child
        // parsing rules -- if we're down here, then none of those matched and
        // so we allow it only if we don't know what the parent is, as all other
        // cases are invalid.
        return parentTag == null;
    }

    return true;
  };

  /**
   * Returns whether
   */
  var findInvalidAncestorForTag = function (tag, ancestorInfo) {
    switch (tag) {
      case 'address':
      case 'article':
      case 'aside':
      case 'blockquote':
      case 'center':
      case 'details':
      case 'dialog':
      case 'dir':
      case 'div':
      case 'dl':
      case 'fieldset':
      case 'figcaption':
      case 'figure':
      case 'footer':
      case 'header':
      case 'hgroup':
      case 'main':
      case 'menu':
      case 'nav':
      case 'ol':
      case 'p':
      case 'section':
      case 'summary':
      case 'ul':

      case 'pre':
      case 'listing':

      case 'table':

      case 'hr':

      case 'xmp':

      case 'h1':
      case 'h2':
      case 'h3':
      case 'h4':
      case 'h5':
      case 'h6':
        return ancestorInfo.pTagInButtonScope;

      case 'form':
        return ancestorInfo.formTag || ancestorInfo.pTagInButtonScope;

      case 'li':
        return ancestorInfo.listItemTagAutoclosing;

      case 'dd':
      case 'dt':
        return ancestorInfo.dlItemTagAutoclosing;

      case 'button':
        return ancestorInfo.buttonTagInScope;

      case 'a':
        // Spec says something about storing a list of markers, but it sounds
        // equivalent to this check.
        return ancestorInfo.aTagInScope;

      case 'nobr':
        return ancestorInfo.nobrTagInScope;
    }

    return null;
  };

  /**
   * Given a ReactCompositeComponent instance, return a list of its recursive
   * owners, starting at the root and ending with the instance itself.
   */
  var findOwnerStack = function (instance) {
    if (!instance) {
      return [];
    }

    var stack = [];
    /*eslint-disable space-after-keywords */
    do {
      /*eslint-enable space-after-keywords */
      stack.push(instance);
    } while (instance = instance._currentElement._owner);
    stack.reverse();
    return stack;
  };

  var didWarn = {};

  validateDOMNesting = function (childTag, childInstance, ancestorInfo) {
    ancestorInfo = ancestorInfo || emptyAncestorInfo;
    var parentInfo = ancestorInfo.parentTag;
    var parentTag = parentInfo && parentInfo.tag;

    var invalidParent = isTagValidWithParent(childTag, parentTag) ? null : parentInfo;
    var invalidAncestor = invalidParent ? null : findInvalidAncestorForTag(childTag, ancestorInfo);
    var problematic = invalidParent || invalidAncestor;

    if (problematic) {
      var ancestorTag = problematic.tag;
      var ancestorInstance = problematic.instance;

      var childOwner = childInstance && childInstance._currentElement._owner;
      var ancestorOwner = ancestorInstance && ancestorInstance._currentElement._owner;

      var childOwners = findOwnerStack(childOwner);
      var ancestorOwners = findOwnerStack(ancestorOwner);

      var minStackLen = Math.min(childOwners.length, ancestorOwners.length);
      var i;

      var deepestCommon = -1;
      for (i = 0; i < minStackLen; i++) {
        if (childOwners[i] === ancestorOwners[i]) {
          deepestCommon = i;
        } else {
          break;
        }
      }

      var UNKNOWN = '(unknown)';
      var childOwnerNames = childOwners.slice(deepestCommon + 1).map(function (inst) {
        return inst.getName() || UNKNOWN;
      });
      var ancestorOwnerNames = ancestorOwners.slice(deepestCommon + 1).map(function (inst) {
        return inst.getName() || UNKNOWN;
      });
      var ownerInfo = [].concat(
      // If the parent and child instances have a common owner ancestor, start
      // with that -- otherwise we just start with the parent's owners.
      deepestCommon !== -1 ? childOwners[deepestCommon].getName() || UNKNOWN : [], ancestorOwnerNames, ancestorTag,
      // If we're warning about an invalid (non-parent) ancestry, add '...'
      invalidAncestor ? ['...'] : [], childOwnerNames, childTag).join(' > ');

      var warnKey = !!invalidParent + '|' + childTag + '|' + ancestorTag + '|' + ownerInfo;
      if (didWarn[warnKey]) {
        return;
      }
      didWarn[warnKey] = true;

      if (invalidParent) {
        var info = '';
        if (ancestorTag === 'table' && childTag === 'tr') {
          info += ' Add a <tbody> to your code to match the DOM tree generated by ' + 'the browser.';
        }
        "production" !== 'production' ? warning(false, 'validateDOMNesting(...): <%s> cannot appear as a child of <%s>. ' + 'See %s.%s', childTag, ancestorTag, ownerInfo, info) : undefined;
      } else {
        "production" !== 'production' ? warning(false, 'validateDOMNesting(...): <%s> cannot appear as a descendant of ' + '<%s>. See %s.', childTag, ancestorTag, ownerInfo) : undefined;
      }
    }
  };

  validateDOMNesting.ancestorInfoContextKey = '__validateDOMNesting_ancestorInfo$' + Math.random().toString(36).slice(2);

  validateDOMNesting.updatedAncestorInfo = updatedAncestorInfo;

  // For testing
  validateDOMNesting.isTagValidInContext = function (tag, ancestorInfo) {
    ancestorInfo = ancestorInfo || emptyAncestorInfo;
    var parentInfo = ancestorInfo.parentTag;
    var parentTag = parentInfo && parentInfo.tag;
    return isTagValidWithParent(tag, parentTag) && !findInvalidAncestorForTag(tag, ancestorInfo);
  };
}

module.exports = validateDOMNesting;
},{"./Object.assign":249,"fbjs/lib/emptyFunction":143,"fbjs/lib/warning":162}],356:[function(require,module,exports){
'use strict';

module.exports = require('./lib/React');

},{"./lib/React":251}],357:[function(require,module,exports){
'use strict';
module.exports = function (str) {
	return encodeURIComponent(str).replace(/[!'()*]/g, function (c) {
		return '%' + c.charCodeAt(0).toString(16).toUpperCase();
	});
};

},{}],358:[function(require,module,exports){
/**
 * Copyright 2014-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 */

'use strict';

/**
 * Similar to invariant but only logs a warning if the condition is not met.
 * This can be used to log issues in development environments in critical
 * paths. Removing the logging code for production environments will keep the
 * same logic and follow the same code paths.
 */

var warning = function() {};

if ("production" !== 'production') {
  warning = function(condition, format, args) {
    var len = arguments.length;
    args = new Array(len > 2 ? len - 2 : 0);
    for (var key = 2; key < len; key++) {
      args[key - 2] = arguments[key];
    }
    if (format === undefined) {
      throw new Error(
        '`warning(condition, format, ...args)` requires a warning ' +
        'message argument'
      );
    }

    if (format.length < 10 || (/^[s\W]*$/).test(format)) {
      throw new Error(
        'The warning format should be able to uniquely identify this ' +
        'warning. Please, use a more descriptive format than: ' + format
      );
    }

    if (!condition) {
      var argIndex = 0;
      var message = 'Warning: ' +
        format.replace(/%s/g, function() {
          return args[argIndex++];
        });
      if (typeof console !== 'undefined') {
        console.error(message);
      }
      try {
        // This error was thrown as a convenience so that you can use this stack
        // to find the callsite that caused this warning to fire.
        throw new Error(message);
      } catch(x) {}
    }
  };
}

module.exports = warning;

},{}],359:[function(require,module,exports){
arguments[4][358][0].apply(exports,arguments)
},{"dup":358}],360:[function(require,module,exports){
(function(self) {
  'use strict';

  if (self.fetch) {
    return
  }

  var support = {
    searchParams: 'URLSearchParams' in self,
    iterable: 'Symbol' in self && 'iterator' in Symbol,
    blob: 'FileReader' in self && 'Blob' in self && (function() {
      try {
        new Blob()
        return true
      } catch(e) {
        return false
      }
    })(),
    formData: 'FormData' in self,
    arrayBuffer: 'ArrayBuffer' in self
  }

  function normalizeName(name) {
    if (typeof name !== 'string') {
      name = String(name)
    }
    if (/[^a-z0-9\-#$%&'*+.\^_`|~]/i.test(name)) {
      throw new TypeError('Invalid character in header field name')
    }
    return name.toLowerCase()
  }

  function normalizeValue(value) {
    if (typeof value !== 'string') {
      value = String(value)
    }
    return value
  }

  // Build a destructive iterator for the value list
  function iteratorFor(items) {
    var iterator = {
      next: function() {
        var value = items.shift()
        return {done: value === undefined, value: value}
      }
    }

    if (support.iterable) {
      iterator[Symbol.iterator] = function() {
        return iterator
      }
    }

    return iterator
  }

  function Headers(headers) {
    this.map = {}

    if (headers instanceof Headers) {
      headers.forEach(function(value, name) {
        this.append(name, value)
      }, this)

    } else if (headers) {
      Object.getOwnPropertyNames(headers).forEach(function(name) {
        this.append(name, headers[name])
      }, this)
    }
  }

  Headers.prototype.append = function(name, value) {
    name = normalizeName(name)
    value = normalizeValue(value)
    var list = this.map[name]
    if (!list) {
      list = []
      this.map[name] = list
    }
    list.push(value)
  }

  Headers.prototype['delete'] = function(name) {
    delete this.map[normalizeName(name)]
  }

  Headers.prototype.get = function(name) {
    var values = this.map[normalizeName(name)]
    return values ? values[0] : null
  }

  Headers.prototype.getAll = function(name) {
    return this.map[normalizeName(name)] || []
  }

  Headers.prototype.has = function(name) {
    return this.map.hasOwnProperty(normalizeName(name))
  }

  Headers.prototype.set = function(name, value) {
    this.map[normalizeName(name)] = [normalizeValue(value)]
  }

  Headers.prototype.forEach = function(callback, thisArg) {
    Object.getOwnPropertyNames(this.map).forEach(function(name) {
      this.map[name].forEach(function(value) {
        callback.call(thisArg, value, name, this)
      }, this)
    }, this)
  }

  Headers.prototype.keys = function() {
    var items = []
    this.forEach(function(value, name) { items.push(name) })
    return iteratorFor(items)
  }

  Headers.prototype.values = function() {
    var items = []
    this.forEach(function(value) { items.push(value) })
    return iteratorFor(items)
  }

  Headers.prototype.entries = function() {
    var items = []
    this.forEach(function(value, name) { items.push([name, value]) })
    return iteratorFor(items)
  }

  if (support.iterable) {
    Headers.prototype[Symbol.iterator] = Headers.prototype.entries
  }

  function consumed(body) {
    if (body.bodyUsed) {
      return Promise.reject(new TypeError('Already read'))
    }
    body.bodyUsed = true
  }

  function fileReaderReady(reader) {
    return new Promise(function(resolve, reject) {
      reader.onload = function() {
        resolve(reader.result)
      }
      reader.onerror = function() {
        reject(reader.error)
      }
    })
  }

  function readBlobAsArrayBuffer(blob) {
    var reader = new FileReader()
    reader.readAsArrayBuffer(blob)
    return fileReaderReady(reader)
  }

  function readBlobAsText(blob) {
    var reader = new FileReader()
    reader.readAsText(blob)
    return fileReaderReady(reader)
  }

  function Body() {
    this.bodyUsed = false

    this._initBody = function(body) {
      this._bodyInit = body
      if (typeof body === 'string') {
        this._bodyText = body
      } else if (support.blob && Blob.prototype.isPrototypeOf(body)) {
        this._bodyBlob = body
      } else if (support.formData && FormData.prototype.isPrototypeOf(body)) {
        this._bodyFormData = body
      } else if (support.searchParams && URLSearchParams.prototype.isPrototypeOf(body)) {
        this._bodyText = body.toString()
      } else if (!body) {
        this._bodyText = ''
      } else if (support.arrayBuffer && ArrayBuffer.prototype.isPrototypeOf(body)) {
        // Only support ArrayBuffers for POST method.
        // Receiving ArrayBuffers happens via Blobs, instead.
      } else {
        throw new Error('unsupported BodyInit type')
      }

      if (!this.headers.get('content-type')) {
        if (typeof body === 'string') {
          this.headers.set('content-type', 'text/plain;charset=UTF-8')
        } else if (this._bodyBlob && this._bodyBlob.type) {
          this.headers.set('content-type', this._bodyBlob.type)
        } else if (support.searchParams && URLSearchParams.prototype.isPrototypeOf(body)) {
          this.headers.set('content-type', 'application/x-www-form-urlencoded;charset=UTF-8')
        }
      }
    }

    if (support.blob) {
      this.blob = function() {
        var rejected = consumed(this)
        if (rejected) {
          return rejected
        }

        if (this._bodyBlob) {
          return Promise.resolve(this._bodyBlob)
        } else if (this._bodyFormData) {
          throw new Error('could not read FormData body as blob')
        } else {
          return Promise.resolve(new Blob([this._bodyText]))
        }
      }

      this.arrayBuffer = function() {
        return this.blob().then(readBlobAsArrayBuffer)
      }

      this.text = function() {
        var rejected = consumed(this)
        if (rejected) {
          return rejected
        }

        if (this._bodyBlob) {
          return readBlobAsText(this._bodyBlob)
        } else if (this._bodyFormData) {
          throw new Error('could not read FormData body as text')
        } else {
          return Promise.resolve(this._bodyText)
        }
      }
    } else {
      this.text = function() {
        var rejected = consumed(this)
        return rejected ? rejected : Promise.resolve(this._bodyText)
      }
    }

    if (support.formData) {
      this.formData = function() {
        return this.text().then(decode)
      }
    }

    this.json = function() {
      return this.text().then(JSON.parse)
    }

    return this
  }

  // HTTP methods whose capitalization should be normalized
  var methods = ['DELETE', 'GET', 'HEAD', 'OPTIONS', 'POST', 'PUT']

  function normalizeMethod(method) {
    var upcased = method.toUpperCase()
    return (methods.indexOf(upcased) > -1) ? upcased : method
  }

  function Request(input, options) {
    options = options || {}
    var body = options.body
    if (Request.prototype.isPrototypeOf(input)) {
      if (input.bodyUsed) {
        throw new TypeError('Already read')
      }
      this.url = input.url
      this.credentials = input.credentials
      if (!options.headers) {
        this.headers = new Headers(input.headers)
      }
      this.method = input.method
      this.mode = input.mode
      if (!body) {
        body = input._bodyInit
        input.bodyUsed = true
      }
    } else {
      this.url = input
    }

    this.credentials = options.credentials || this.credentials || 'omit'
    if (options.headers || !this.headers) {
      this.headers = new Headers(options.headers)
    }
    this.method = normalizeMethod(options.method || this.method || 'GET')
    this.mode = options.mode || this.mode || null
    this.referrer = null

    if ((this.method === 'GET' || this.method === 'HEAD') && body) {
      throw new TypeError('Body not allowed for GET or HEAD requests')
    }
    this._initBody(body)
  }

  Request.prototype.clone = function() {
    return new Request(this)
  }

  function decode(body) {
    var form = new FormData()
    body.trim().split('&').forEach(function(bytes) {
      if (bytes) {
        var split = bytes.split('=')
        var name = split.shift().replace(/\+/g, ' ')
        var value = split.join('=').replace(/\+/g, ' ')
        form.append(decodeURIComponent(name), decodeURIComponent(value))
      }
    })
    return form
  }

  function headers(xhr) {
    var head = new Headers()
    var pairs = (xhr.getAllResponseHeaders() || '').trim().split('\n')
    pairs.forEach(function(header) {
      var split = header.trim().split(':')
      var key = split.shift().trim()
      var value = split.join(':').trim()
      head.append(key, value)
    })
    return head
  }

  Body.call(Request.prototype)

  function Response(bodyInit, options) {
    if (!options) {
      options = {}
    }

    this.type = 'default'
    this.status = options.status
    this.ok = this.status >= 200 && this.status < 300
    this.statusText = options.statusText
    this.headers = options.headers instanceof Headers ? options.headers : new Headers(options.headers)
    this.url = options.url || ''
    this._initBody(bodyInit)
  }

  Body.call(Response.prototype)

  Response.prototype.clone = function() {
    return new Response(this._bodyInit, {
      status: this.status,
      statusText: this.statusText,
      headers: new Headers(this.headers),
      url: this.url
    })
  }

  Response.error = function() {
    var response = new Response(null, {status: 0, statusText: ''})
    response.type = 'error'
    return response
  }

  var redirectStatuses = [301, 302, 303, 307, 308]

  Response.redirect = function(url, status) {
    if (redirectStatuses.indexOf(status) === -1) {
      throw new RangeError('Invalid status code')
    }

    return new Response(null, {status: status, headers: {location: url}})
  }

  self.Headers = Headers
  self.Request = Request
  self.Response = Response

  self.fetch = function(input, init) {
    return new Promise(function(resolve, reject) {
      var request
      if (Request.prototype.isPrototypeOf(input) && !init) {
        request = input
      } else {
        request = new Request(input, init)
      }

      var xhr = new XMLHttpRequest()

      function responseURL() {
        if ('responseURL' in xhr) {
          return xhr.responseURL
        }

        // Avoid security warnings on getResponseHeader when not allowed by CORS
        if (/^X-Request-URL:/m.test(xhr.getAllResponseHeaders())) {
          return xhr.getResponseHeader('X-Request-URL')
        }

        return
      }

      xhr.onload = function() {
        var options = {
          status: xhr.status,
          statusText: xhr.statusText,
          headers: headers(xhr),
          url: responseURL()
        }
        var body = 'response' in xhr ? xhr.response : xhr.responseText
        resolve(new Response(body, options))
      }

      xhr.onerror = function() {
        reject(new TypeError('Network request failed'))
      }

      xhr.ontimeout = function() {
        reject(new TypeError('Network request failed'))
      }

      xhr.open(request.method, request.url, true)

      if (request.credentials === 'include') {
        xhr.withCredentials = true
      }

      if ('responseType' in xhr && support.blob) {
        xhr.responseType = 'blob'
      }

      request.headers.forEach(function(value, name) {
        xhr.setRequestHeader(name, value)
      })

      xhr.send(typeof request._bodyInit === 'undefined' ? null : request._bodyInit)
    })
  }
  self.fetch.polyfill = true
})(typeof self !== 'undefined' ? self : this);

},{}]},{},[1]);
